# ASGI triplet — backend request path (bead enhancedchannelmanager-j9xrz + -6rrl5.1 + -6rrl5.2
# + enhancedchannelmanager-3hgrf).
#
# starlette floored at 1.6.0. The 1.3.1 floor cleared CVE-2026-48818/48817/54283/54282 (4 CVEs,
# v0.18.0 release-scan blocker) and remains the security minimum; 1.6.0 is current, and is what
# mcp-server already pins, so this converges the two services rather than diverging them.
#
# WHAT THE PIN CHAIN ACTUALLY REQUIRES. fastapi's own dependency declaration is the only hard
# link, and it is one-directional: fastapi 0.136.1 declares `starlette>=0.46.0` with no upper
# bound, so it constrains starlette's MINIMUM and nothing else. starlette may therefore move ahead
# of the version fastapi was released against, and does so here. What must never happen is
# starlette falling below fastapi's declared floor. uvicorn is not in that chain at all — nothing
# in this set depends on it; it is the server, pinned exactly for reproducibility.
#
# WHY FASTAPI IS HELD AT 0.136.1, and it is not a starlette problem. fastapi 0.137.0 stopped
# flattening included routers into `app.routes`. backend/main.py resolves a request path to its
# route template by scanning `app.routes`, and auth/mcp_capabilities.py treats an unresolved
# template as DENY — so under 0.137+ the MCP service-principal path fails closed on every route
# (proven behaviourally: 56 failures across 11 modules). The migration to `iter_route_contexts()`
# is bead enhancedchannelmanager-g4pnh, deliberately its own PR per ADR-001's one-major-bump rule.
#
# An earlier revision of this comment read "Keep this pin set aligned; a fastapi/starlette break
# must bump both at once." The project no longer follows that rule: on 2026-08-24 the PO ruled
# that starlette and uvicorn may upgrade independently of fastapi. Evidence for this pairing —
# an isolation probe (fastapi 0.136.1 + starlette 1.6.0 + uvicorn 0.52.4) ran all 11 modules that
# introspect `app.routes` green, and the full backend suite passes on the compiled lock below.
# There is precedent in this repo: bd-3hgrf moved starlette 1.0.1 -> 1.3.1 against a held fastapi
# 0.136.1 for the same reason, so the "bump both at once" wording never described actual practice.
fastapi==0.136.1
starlette>=1.6.0
uvicorn[standard]==0.52.4
httpx>=0.28.0
pydantic>=2.10.0
email-validator>=2.1.0
python-multipart>=0.0.31

# Authentication
bcrypt>=4.0.0
slowapi>=0.1.9
# HS256 JWT signing/validation. Keep this independent of josepy, which is the
# ACME JOSE implementation below and serves a different protocol.
pyjwt>=2.10.0
# Floored at 48.0.1 to clear GHSA-537c-gmf6-5ccf (v0.18.0 release-scan blocker).
cryptography>=50.0.0

# TLS/ACME (Let's Encrypt)
josepy>=1.14.0
boto3>=1.34.0  # AWS Route53 DNS provider

pytz>=2024.1
sqlalchemy>=2.0.0
# Capped below 1.19. Alembic 1.19 added CHECK-constraint comparison to
# autogenerate, and that surfaces a real pre-existing divergence rather than a
# tool bug: migration 0012 creates ``ck_task_schedules_interval_positive``, but
# ``TaskSchedule.__table_args__`` in backend/models.py never declared it. So
# autogenerate now emits ``remove_constraint`` and
# tests/unit/test_alembic_baseline.py::test_baseline_matches_metadata_no_drift
# fails. Bisected to alembic alone: 1.18.4 passes and 1.19.1 fails with
# sqlalchemy held at 2.0.49. Lifting this cap means declaring the constraint on
# the model so metadata matches the migration DDL — a schema-correctness change
# that needs its own bead, not a currency bump.
alembic>=1.13.0,<1.19
aiofiles>=23.0.0
croniter>=2.0.0
# 3.14.0 fixes CVE-2026-34993 + CVE-2026-47265 (flagged by pip-audit at the
# v0.17.4 release cut). 3.14.1 clears a further 8 CVEs (CVE-2026-54273..54280,
# v0.18.0 release-scan blocker). Floor raised so the lock can't regress below the fix.
aiohttp>=3.14.3

# ReDoS-guarded regex wrapper (backend/safe_regex.py, bd-eio04.5)
regex>=2024.5.15

# Fuzzy string matching for the interactive stream-to-channel dedup epic
# (bd-7xo8e / BD-A; see ADR-008). Used by backend/services/dedup_matcher.py
# via fuzz.token_set_ratio for the non-exact match path. The 3.x line is
# stable and the maintainer has been responsive on CVE fixes; the <4 cap
# guards against a future major-version break per ADR-001 pin guidance.
rapidfuzz>=3.0,<4.0

# Observability — Prometheus metrics exposition
prometheus-client>=0.20.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
respx>=0.21.0
# Property-based tests (bd-eio04.15 — safe_regex call-site equivalence)
hypothesis>=6.100.0

# Performance benchmarks (bd-skqln.10 — Stats v2 hot-query regression gate).
# Runs only against backend/tests/performance/ via the perf-benchmarks
# CI workflow; the default unit/integration suite ignores tests/performance/.
pytest-benchmark>=5.0.0
