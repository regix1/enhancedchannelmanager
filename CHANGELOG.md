# Changelog

All notable changes to Enhanced Channel Manager are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.18.1] — 2026-08-30

### Security

- **Removed the unpatched `ecdsa` runtime dependency by replacing `python-jose` with PyJWT for ECM's existing HS256-only tokens (bead `enhancedchannelmanager-tm9ma`, build 0169).** This resolves Dependabot alert #1 / [GHSA-wj6h-64fc-37mp](https://github.com/advisories/GHSA-wj6h-64fc-37mp) without changing access, refresh, password-reset, expiry, claim-validation, or revocation behavior. The separate `josepy` dependency remains for ACME certificate management.

### Fixed

- **Auto-Sync no longer lets one provider silently decide channel-profile membership when several source groups feed the same effective channel group (bead `enhancedchannelmanager-fes61`, build 0168).** ECM evaluates the complete source/account fan-in without depending on API row order. If the sources select different non-empty Channel Profile sets, membership writes and normalization for that effective group stop before any channel is changed. An app-root **Profile choice required** dialog names the effective group, competing profiles, source groups, and M3U accounts; the operator's explicit choice is recorded before ECM harmonizes every source row. The notification center can reopen a choice deferred for the current browser session. Partial account failures stay visible and retry automatically, and a conflict whose source shape changes becomes a new question instead of applying a stale decision.

  **Operator workaround if the dialog cannot be reached:** open **M3U Manager**, open **Manage Groups** for each named M3U account, and set **Auto-Sync Settings → Channel Profile Assignment** to the same profile set for every source group named in the notification. Run **Save & Refresh** again. Existing channel-profile membership remains frozen while the choices disagree; ECM does not guess based on provider refresh or response order.

- **Malformed trusted-media-network settings no longer echo their raw values into attribution debug logs (bead `enhancedchannelmanager-dz5`, build 0165).** The skip diagnostic remains available using fixed text only.

- **Supported MCP Compose deployments now reconcile the owner-only API-key authority before readiness instead of leaving the sidecar permanently not ready (bead `enhancedchannelmanager-71von`, build 0164).** Fresh installs and upgrades use the canonical credential lifecycle to provision or republish `api-key` without requiring an operator API call, while explicit revocation remains revoked and `/health` remains fail-closed. The container gate now proves both fresh startup and recreation from persisted settings. Startup also leaves syntactically valid non-object `settings.json` content byte-for-byte intact: authority validation and orphan reconciliation still run, but startup-only settings writers are suppressed so recovery evidence is not replaced by defaults.

### Added

- **Backend logs now persist across restarts as newline-delimited JSON in a process-safe rotating set under `CONFIG_DIR` (bead `enhancedchannelmanager-n9ix6`, build 0167).** The default active file plus four backups nominally retains 50 MiB; the admin-only size and backup settings validate at the API and take effect after restart. Credentials are scrubbed before persistence, oversized records become bounded NDJSON drop markers, and unsafe pre-scrub log epochs or replaced/non-regular filesystem objects fail closed. Debug bundles read up to the newest 50 MiB without blocking peer log writers, report the applied rotation policy plus source/window/truncation metadata, and retain the exact in-memory ring as a scrubbed fallback when persistence is unavailable or degraded. Bundle start and download always require the initiating human admin, including when general authentication is disabled; MCP and anonymous callers are denied, completed artifacts use private temporary files, and independent TTL cleanup bounds retention.

- **Debug bundles now include the M3U group settings and channel-profile names needed to diagnose profile reconciliation (bead `enhancedchannelmanager-hrukw`, build 0166).** The group-settings artifact reuses the account data the bundle already fetches and carries both per-account rows and the derived conflict-aware winner view; only channel profiles add an upstream request. An unavailable account source, failed collapse derivation, or failed profile request is reported without preventing bundle creation.

- **The repository now carries a software bill of materials — a committed inventory of what is inside ECM, so an advisory can be answered by reading a file instead of rebuilding and rescanning every image (bead `enhancedchannelmanager-3t0ht`, build 0147).** Each release gets `sbom/vX.Y.Z/`: two SPDX 2.3 documents, one for the ECM image and one for the MCP image, plus an index. Release directories accumulate and are kept forever, because the question an advisory actually raises is *which shipped versions contain the affected package*, and only the history can answer it. A separate `sbom/dev/` holds a single transient snapshot of what `dev` currently carries; it is superseded rather than accumulated and is not an artifact of record. The two are kept apart by path rather than by convention — the shape of the version string decides the directory, so a build-numbered version cannot land in the permanent release namespace.

  **Read what these documents cover before quoting one to anybody.** They inventory the *language* dependencies taken from the manifests in this repository: every Python distribution pinned in each image's `requirements.txt`, and every npm package resolved in `frontend/package-lock.json`, each with its exact version and a package URL. They do **not** inventory the **operating-system package layer** — the Debian and Alpine packages inside the base images — and that is the gap to know about, because a large share of container CVEs land there. It is not an oversight: both Dockerfiles run an OS upgrade (`apt-get upgrade` / `apk upgrade`) at build time, so the installed OS package set is not derivable from the source tree at all. What *is* recorded is every base and build image **by digest**, so a Debian or Alpine advisory can still be correlated to the exact image that shipped; for the package-level question inside that image, scan it — the Trivy jobs already do, on every candidate, before it can be published.

  **The inventory is enforced rather than trusted.** `sbom/vX.Y.Z/index.json` records the SHA-256 of every manifest the documents were derived from, and the Release Cut Gate runs `scripts/generate_sbom.py verify` as a new item **G8** on every release PR: it regenerates both documents from the release branch and byte-compares. A dependency bumped without regenerating, a hand-edited document, a deleted document, an extra file smuggled in, and a directory cut for the wrong version all fail the gate. No document asserts an image digest, and none should be added by hand — the images a release publishes are built by the merge commit the release PR creates, which does not exist yet when the SBOM is cut.

- **Your backup now carries the DVR recordings you have scheduled but not yet recorded, so a restored instance still records them (bead `enhancedchannelmanager-ciabe`, build 0145).** No backup category covered DVR recordings at all. **DVR Rules** carried your *recurring* rules; the individual scheduled recordings were excluded wholesale as per-instance state, so a restore or a cross-instance sync produced an instance with none of your one-off recordings and nothing anywhere said so. Recordings that have not started yet now travel in a new **Upcoming Recordings** category, and every recording left behind is named and counted in the run report instead of vanishing.

  **Two kinds are deliberately left behind, and the report says which and how many.** A recording that has already started or finished *is* a media file on the source instance's disk — no API can carry that, and Dispatcharr refuses a past-dated create outright (`400 "End time must be in the future."`, measured on 0.29.0) — so the report counts them and tells you to copy those files across by hand if you want them. A recording one of your recurring rules generated is already replicated by the rule itself: the destination's own hourly maintainer recreates it, and carrying it as well would duplicate rather than merge. A recording whose start time passes between the backup and the restore is skipped as **Scheduled time has passed**, which is an ordinary no-op and never counted as something lost — the destination could not have held it either.

  **A recording that cannot be restored never rolls the rest back.** Nothing else in a restore points at a recording, and the refusal it most plausibly meets is a stale timestamp — a property of how old the backup is, which no retry fixes. One recording Dispatcharr will not schedule is one programme you record by hand; rolling the run back over it would cost you your channels, groups, profiles and settings.

- **Your replica now inherits Dispatcharr's core settings — streaming, DVR, system, user-limit and proxy behaviour — instead of running on defaults (bead `enhancedchannelmanager-10wnq`, build 0145).** ADR-013 has listed core settings in the per-cycle sync set since it was written, and they had never been built: `core_settings` appeared nowhere in the sync engine at any layer, so a replica silently ran on Dispatcharr's out-of-the-box values while your primary ran on yours. Six of the seven settings blobs now replicate on every cycle, each with a written reason.

  **Two of them you can decline per target, and one never replicates at all.** **Proxy settings** (buffering and connection tuning, which can be wrong on slower replica hardware) and **Backup settings** (which would have both instances backing themselves up on one schedule, with the replica's retention bounded by the replica's storage) each carry a per-target opt-out, so you decline a blob rather than losing every setting with it. **Network access** — the CIDR allowlists that gate every surface Dispatcharr has — is never replicated and cannot be opted back into: replicating it can lock you out of the replica or open it up, depending on which way the two instances differ. The per-target list can only narrow what crosses, never widen it.

  **Three settings that are really pointers to other rows are handled rather than forwarded blindly.** `default_user_agent` and `default_stream_profile` name rows by id, and those ids mean something different on the replica, so they are remapped onto the replica's own rows. `hdhr_output_profile_id` points at something ECM has no category for, so it is dropped and reported rather than guessed. This is worth stating because a settings blob is a value Dispatcharr stores *without validating* — forwarding the primary's ids would have pointed your replica's defaults at whichever unrelated rows happened to hold those numbers, with no error, no counter and nothing in the report.

  **A settings failure no longer deletes the replica it was building.** Settings are a fatal category, and this change is the first time a settings step could fail on the sync path at all — before it, core settings synced nowhere. One destination that could not answer `GET /api/core/settings/`, or one blob a version-skewed replica does not have, would have rolled the entire cycle back and deleted every account, source, group, profile and channel the run had just created. It would also have fixed nothing, because settings are never rolled back — there is nothing to roll them back with. On the **sync path only**, a settings failure is now counted, listed in the failure details and still forbids a clean success, but the replica survives and the next scheduled cycle retries. A one-shot archive restore is bit-for-bit unchanged and still rolls back on a settings failure, which is the ruling you made on 2026-08-03: that ruling was about an action you watch and can retry, and an unattended cycle is not that.

- **A replica now keeps the server groups that make your provider accounts share one connection limit (bead `enhancedchannelmanager-tyrg1`, build 0145).** Dispatcharr's **server groups** are what make several M3U accounts count against a single provider connection counter. Cross-instance sync used to drop that link — deliberately, because forwarding the primary's raw id made the replica answer `400 "Invalid pk"` and roll the whole cycle back — which left you with a replica whose accounts did not share a connection limit until you recreated the grouping by hand. Now that a provisioned replica really does connect to your provider, that shared counter is live rather than theoretical. Server groups replicate as their own category, ordered ahead of the M3U accounts that point at them, and each account's link is remapped onto the replica's own group. Where the link genuinely cannot be resolved it is still dropped, and now reported — never forwarded as a raw id, which is the failure that made dropping it the right floor in the first place. The category appears in the restore and sync summary as **Server groups**.

- **Your replica now receives your provider credentials on every sync, so it serves video with nothing for you to type (PO ruling 2026-08-22, ADR-013 amendment (b), supersedes the one-time provisioning action added in build 0135, build 0136).** A replicated instance already received everything structural — channels, groups, profiles, profile memberships, logo bindings, EPG links. What it never had was a working provider credential, so every stream URL on it read `.../live/***REDACTED***/***REDACTED***/.ts` and returned 404. The M3U account username and password, the credentials inside a plain-M3U or Xtream Codes URL, the credentials inside every stream address and every provider logo address now cross with everything else, on every cycle. The replica authenticates and plays on the **same cycle that creates its channels** — no second pass, no orphaned streams, and no recovery procedure.

  **Change your provider password on the primary and the replica picks it up on its next scheduled sync.** Rotation needs no feature and no action: the replica is given the current value every time, so "the standby's credential went stale" is not a state that can arise.

  **The only credential you ever type is a Schedules Direct password, and you type it once.** Dispatcharr marks that one write-only and never returns it, so there is nothing on the primary to read. The sync-target form asks for it **only if this instance actually has a Schedules Direct EPG source**, stores it encrypted on the target, and re-sends it every cycle. Everything else needs zero input.

  **What this costs you, stated where you decide it.** The replica becomes a place your provider credential lives — its database, its backups, its logs and its own stream rows will hold it, and nothing retracts that. If you turn **Allow insecure TLS** on for a target, those credentials cross in clear on **every** cycle rather than once. ECM does not block that: the target row carries a **Certificate check off** badge saying so, the card's banner says so, and every credential-carrying cycle logs a warning and an audit entry recording how many provider records carried a credential and which ones by name and field name — never a value. If you want a replica to stop holding the credential, delete or disable its sync target, clear the credentials on the replica yourself, and **rotate the credential at your provider** — only the last step ends the exposure.

  **Removed with this change**, because they described machinery that no longer exists: the one-time **Provision Credentials** and **De-provision Credentials** actions and their two API routes; the refusal that stopped a credential-holding target from disabling TLS verification; and the two provisioning marker columns on the sync target (dropped by migration `0047`). The guide's credential re-entry recovery procedure is gone too — there is nothing to recover from.

- **A sync target's settings can now be corrected in place, instead of the target being deleted and rebuilt (bead `enhancedchannelmanager-a3lby`, build 0134).** Every field the create form sets — name, base URL, credentials, and the **Allow insecure TLS** opt-out — was write-once, so fixing a mistyped base URL or a changed password meant deleting the target and building a replacement. That costs more than it looks: a new target is created with **logo replication off** whatever the old one had, and it inherits the deleted target's execution history, because the id it is given is reusable (bead `enhancedchannelmanager-5dp92`). The insecure-TLS opt-out is the one with a security consequence — an operator who ticked it during setup could only untick it by rebuilding the target. Each target row now carries an **edit** button that reopens the same form on that target, and a correction writes only the fields that form edits, so the target's enable/disable state, its logo-replication choice and its fuzzy stream matching are left exactly as they were.

  **Credentials are all-or-nothing, and the boxes are deliberately not prefilled.** ECM cannot read a stored secret back — what it holds is a last-4 mask, and putting that in the box would invite you to "keep" a value that is not the secret. Leave the credential boxes blank and the stored credentials are untouched. To change them, enter them in full: a partial entry, or a switch between **Username & password** and **API key**, is refused with the reason rather than blanking the half you left empty.

- **Logo replication to a sync target can now be turned on from Settings, without calling the API (bead `enhancedchannelmanager-8gnik`, build 0133).** Cross-instance sync can copy your primary's channel artwork to the replica, but the setting that enables it had no control anywhere in the web UI: the only ways to reach it were a raw `PUT /api/sync-targets/{id}` or the MCP tool, and this guide said so. Each target row on the **Cross-Instance Sync** card now carries a **Logos off / Logos on** toggle beside its enable/disable switch. It shows the target's stored setting and changes it in place, so what you see on the card is what that target will do.

  **It is still off by default, and turning it on is still your decision.** Copying artwork means fetching and uploading every logo the primary holds, which is a real cost to repeat on a schedule — so a target you create is created with logo replication off, exactly as before, and nothing turns it on for you. Sync also never deletes or bulk-clears the replica's own logos, whether the setting is on or off.

- **You can now create a redacted configuration backup directly from Backup & Restore (bead `enhancedchannelmanager-pui76`, build 0115).** The new **Configuration Backup** card creates the same `.zip` artifact as a scheduled backup and refreshes **Saved Backups** when it finishes. The card states what ECM replaces or removes before you create it: recognized M3U and EPG provider credential fields, credential-bearing URL values, and ECM login accounts. Operator-authored free text may still contain a secret ECM cannot recognize, so inspect the artifact before sharing it. Use **Encrypted Backup (Migration)** with **Include credentials** when the structured credentials and accounts need to travel to another install.

### Fixed

- **Apply All now fails closed if a bulk-created channel loses an expected stream assignment while it is being staged (bead `enhancedchannelmanager-ydmu3`, build 0152).** Retained logs from the affected run show one request containing 316 creates and no assignments, but the exact historical trigger remains unproven: a disposable reproduction through the current real hook staged all 632 operations. The App bulk-create path stages each create and its deduplicated, nonempty assignment set in one state update and one undo unit. Ledger format 3 persists that expected set and discards older records whose intent cannot be known. Apply evaluates stream operations in submitted sequence and refuses an incomplete expected set or a reorder that is not a permutation at its actual position before sending a request. Temp-channel stream operations stay beside their create in that same submitted order.

  The backend is authoritative for every bulk request, consolidated or not. `createChannel.tempId` is strictly negative and unique request-wide; every negative `channelId`, including entries in list-valued `channelIds`, must target exactly one earlier create in the raw request, and the effective plan is checked again after consolidation. Dangling, forward, duplicate, and nonnegative temp-id requests reject before any upstream mutation regardless of `continueOnError`. Consolidation no longer folds add/remove pairs or hoists/deduplicates reorders: stream operations retain submitted relative order, sequence-aware simulation validates reorders where they execute, and `expectedStreamIds` remains a required final-state subset. Intentional streamless creates that omit `expectedStreamIds`, positive existing-channel operations (including the 316-channel path), and the prior create+add+remove fail-closed behavior remain supported. A known-missing stream still fails before reading or updating its channel; a failed stream lookup still does not claim absence.

- **Starting a new cross-instance preview now revokes the prior Apply authorization immediately (build 0149).** A failed or interrupted re-preview can no longer leave stale **Apply** available for a source-wins overwrite.

- **Cross-instance sync reports now name the M3U accounts and fields each cycle converged instead of showing only an aggregate updated-item count (bead `enhancedchannelmanager-9puak`, build 0148).** Apply summaries identify account names and fields that converged or could not converge, and previews identify fields that would converge, while retaining the existing aggregate updated-item count. Reports include account names and field names only: converged field values and provider URLs remain excluded.

- **A setting you change on your primary now reaches a replica that already has the account, including a rotated provider password (beads `enhancedchannelmanager-zszjd`, `enhancedchannelmanager-296hi`, build 0145).** An M3U account already present on the replica was matched "already exists, identical" and then never written to again — for *every* field, not one. Anything you changed on the primary afterwards diverged silently and permanently: the server URL, username and password, max streams, user agent, refresh interval, custom properties, account type, priority, stale-stream days, active state and the four preference toggles alike. **Auto-enable new groups (Live)** is the one that was measured — flipped on the primary, a cycle ran, and the replica stayed on the old value forever — but it was one field of twenty.

  **This completes the per-cycle credential cascade shipped in build 0136.** That change put your real provider username and password into every cycle, and the path that *creates* an account on the replica wrote them. The path that met an account already there issued no write at all — so a password you rotated at your provider reached a brand-new replica and never reached an existing one. There is now exactly one writer for that row, and a field set on the primary is the field set on the replica after the next cycle.

  **The exceptions are named rather than implied.** The account **name** is the key the two instances are matched on, so a rename is a new account rather than drift. The **channel group selection** carries ids that address different groups on the replica and is owned by the group-selection path, which remaps them. **Status** and **last message** are the replica's own account health, and overwriting them would destroy the two fields ECM reads to tell you a replica is broken. Five more are read-only on Dispatcharr's own API, so no write could move them anyway.

  **Drift is reported while it exists, because silence was the defect's worst property.** The run report counts the fields that differed — including the ones the cycle then converged, so the counter clears itself once the replica matches — and separately counts fields a cycle *tried* to write and could not, which forbids a clean success. Field **names** only: a converging account carries `username`, `password` and a credential-bearing `server_url`, and no value ever enters the report. A refused write degrades the run rather than rolling the replica back over one setting.

- **Changing one field on a cloud backup target no longer wipes the others (bead `enhancedchannelmanager-ybr3u`, build 0145).** The cloud-target editor said "Leave fields empty to keep existing values. Only changed fields will be updated" and then submitted only the box you touched — while the route behind it *replaces* the whole credential set rather than merging into it. So rotating one S3 secret key blanked the bucket name, the access key id and the region. The form also prefilled every credential box from the stored last-4 mask, inviting you to "keep" a value that is not the secret — and submitting it would have written the mask itself into the store.

  **Credentials are now all-or-nothing, and the boxes open empty.** Touch any credential box and you must supply the whole set for that provider; a partial entry is refused naming the fields it is missing, rather than half-written. Nothing is prefilled from a mask. What each box does tell you is whether it currently holds a value — **currently set — retype to replace**, or **not set** — which is everything you need without revealing anything. The hint now says what actually happens: entering any credential replaces the whole set, and anything left blank is cleared.

  **Replace rather than merge is a deliberate choice, disclosed rather than hidden.** A merge cannot express credential *removal* — rotating to a smaller credential set, or moving a WebDAV target from user-and-password to anonymous, would leave the superseded secret in the store with no way to clear it short of deleting and rebuilding the target. The same route serves the `update_cloud_target` MCP tool and the documented API, sync targets already settled on replace, and two adjacent credential editors with opposite rules is its own trap. The edit form also declines to run **Test** against a half-entered set: that would fail against a credential set which is neither what is stored nor what would be saved, and read as "your stored credentials are broken". The create form keeps its test-as-you-go button, where there is no stored set to misrepresent.

- **A newly created sync target no longer shows the deleted target's run history (bead `enhancedchannelmanager-5dp92`, build 0145).** Delete a sync target and create another, and the new one opened showing the old one's runs and failures. Target ids are reused after a delete, and removing a target pruned its schedule rows but not its execution history — so a clean new target looked like it had a history of problems, on exactly the surface you use to judge whether it is healthy. A run is now attributed to a target only when that target already existed when the run **started**, and an id with no live target behind it has no history at all. Both surfaces are covered, because the Task History screen and the `get_task_history` MCP tool read through the same funnel — fixing only one would have left the wrong history reachable one screen over. A run still in flight when its target is deleted is not interrupted, and if it finishes after the replacement is created it still is not the replacement's run. Nothing is purged: the old rows stay for the audit trail of what the previous target did, and retire on the ordinary 30-day history purge.

- **Every dev-tag publish since the authorize gate reopened failed its own digest check, so no image had actually published under the fixed gate (bead `enhancedchannelmanager-5z48v`, build 0144).** The check was right to refuse — it was comparing two different objects. `docker/build-push-action` enables provenance attestations by default, so BuildKit wraps the platform image manifest in a nested index holding the image plus an `unknown/unknown` attestation manifest; the verification step read that wrapper index's digest, while `skopeo copy` resolves the archive to the inner image manifest and pushes that one. `--preserve-digests` was working correctly the whole time — it preserved the digest of the object it copied, which was never the object being compared against.

  **This was ours, not a skopeo bug and not a registry problem.** Verified against all four real candidate archives from the run that failed: each carries the nested index, and resolving through it to the inner manifest reproduces exactly the digest GHCR stores for that image. The check is not weakened by this fix — it is stronger. The digest now names the one manifest that Trivy scanned, that skopeo pushes, and that `docker buildx imagetools create` dereferences when it assembles the multi-arch tag, instead of three different objects that happened to share a build.

  **A third failure, not yet hit, was fixed by the same change.** `publish-manifests` dereferenced `$image@$ECM_AMD64` — a reference built from the wrapper index's digest, which is never pushed to any registry and so could never have resolved. It would have failed next.

  Also: the digest comparison used a bare `test`, which printed nothing and exited 1 under `set -euo pipefail` — a step that died with no message at all, and part of why this took a full day to diagnose. The comparison now names both digests on a mismatch.

- **Dev images stopped publishing on 2026-08-18 and nothing said so for six days; publication is restored (bead `enhancedchannelmanager-0dsa4`, build 0143).** If you run the `dev` tag, the image you have been pulling is build `0123` while `dev` itself had moved on to `0142` — **nineteen builds that never became an image**, among them two P1 fixes for provider credentials leaking into logs and the stream-probe fix for XC providers that redirect https to http. Pull `dev` again once this build lands and you get the whole backlog at once.

  **Nothing looked wrong the entire time, which is why it ran for six days.** Images built, Trivy scans passed, attestation passed, and all four required checks were green on every merge. The publish workflow was invoked on every one of those merges too — it declined to publish, and reported success for doing so.

  **This was ours, not the registry's and not a provider's.** Publication runs as a workflow called by the test workflow, and a called workflow is shown the *calling* workflow's event rather than its own. The gate that was meant to recognise the dev publication path tested for the wrong one of those two, so it never matched and every publish step skipped.

  **Nothing about what publishes changed.** The same exact-SHA verification still gates every image: the branch head must equal the revision being published, and successful tests, a successful build and all four architecture digests must exist for that exact revision. The only thing that changed is that a gate which was wrongly closed is now open.

- **Stream probes against an XC provider that redirects https to http now run instead of failing wholesale, and a probe failure now says why (beads `enhancedchannelmanager-iyvl9`, `enhancedchannelmanager-3dn59`, build 0142).** Every probe against one operator's live provider was failing, logged only as `probe failed (SSRFError)` hundreds of times with no reason, redirect target, or distinction between causes. **This was our regression, not the provider changing behaviour:** the provider's `https://.../live/<user>/<pass>/<id>.ts` URL has redirected to a plain-HTTP edge node for as long as anyone checked; what changed was the downgrade guard ECM shipped 2026-08-17/18, which started refusing to follow that redirect. The refusal bought nothing here — the provider serves the actual video over plain HTTP at that edge regardless, so playback was already unencrypted in transit, and the redirect target's path is an opaque token carrying no credentials, so following it discloses nothing the initial request hadn't already sent. Refusing the redirect only cost the operator their probe capability for that provider. The stream-probe path, and only it, may now follow an https→http redirect, via an explicit `SchemeDowngrade.ALLOW_STREAM_PROBE` argument on `validate_redirect` — not a global flag, setting, or environment variable. Every other outbound destination — cloud backup targets, EPG sources, a second Dispatcharr instance — still refuses a downgrade by doing nothing differently, and the waiver itself is narrow: it relaxes only the scheme-downgrade check, not the denylist, resolve-by-IP, redirect depth cap, or origin pinning, all of which still apply on the probe path unchanged. Probe failures now also say why, with a deliberate limit kept in place: a message ECM's own guard raises (a fixed string like "Refusing redirect that downgrades https -> http") is safe to log and now is, but text from ffprobe or another subprocess still is not, because it can carry the provider URL with embedded credentials.

- **A replica now inherits which of your provider's groups are switched on, so it ingests what your primary ingests (bead `enhancedchannelmanager-avrix`, build 0134).** On an Xtream Codes account the per-group **enabled** selection is the setting that decides what the account pulls in — on a real 777-category account, enabling exactly two is what keeps an instance at 316 channels instead of the provider's 53,661 streams. Cross-instance sync was not carrying that selection: the replica's copy of the provider account arrived with **no group rows at all**, and nothing in the run said so. `channel_group_drift` reported `0` throughout, correctly — it answers a different question (which group a *channel* sits in), so the report was silent rather than wrong.

  **What the replica then did was worse than losing a setting, in either direction.** Measured live: given its own credentials, a replica in that state discovered all 777 groups, found none enabled, and refused to import anything — zero streams against the primary's 316. And which way it fails is not even stable, because it depends on **Auto-enable new groups (Live)**, which the replica faithfully inherits from your primary and which Dispatcharr turns **on** by default: with that on, the same missing selection made the replica enable **all 777 categories**, i.e. the provider's entire catalogue.

  Sync now writes the primary's per-group selection to the replica's provider account, remapped onto the replica's own group ids, and **keeps doing it on every cycle** — a replica tracks its source, not a snapshot of the day it was built. (Deferring the selection only on the cycle that first creates the account was the same defect one step later: enabling a third provider category on the primary left the replica on two, under a `success` with every counter at zero.) **It still never triggers the replica's provider refresh** — that restriction is unchanged and deliberate; what changed is that the replica's own stored settings are no longer left blank. When some of the selection cannot be delivered — a group that is not on the destination, or a write the destination refuses — the run now **counts and names it**, and the one-line summary an unattended scheduled run produces says the replica will not ingest the same content as this instance. A selection that fully lands reports nothing, so a converged cycle stays quiet.

- **An entity your backup or sync could not deliver, because something it needs is not on the destination, is now named and counted instead of filed away as a no-op (bead `enhancedchannelmanager-4mkoe`, build 0133).** When a restore or a sync cannot resolve a reference an archived entity carries — a stream profile pointing at a user agent, an EPG source pointing at an M3U account, a channel pointing at a group — it does not create that entity. Until now it recorded that as a **skip**, in the same bucket as "already exists, identical", which is a genuine non-event. Nothing in the counts, the outcome, or the one-line summary said the replica was short. A backup whose upstream fetch for one category failed — which ECM already reports as a degraded backup — could therefore restore with every dependant of that category silently missing, under a clean `success … failed 0`.

  Those entities now count toward **`entities_blocked_by_dependency`**, which joins the delivery-shortfall set: an apply that ends with any of them resolves to **Completed with failures** rather than success, at warning severity with the same per-task alert opt-out as its siblings, and the one-line summary carries a clause saying how many archived items were not restored. Which ones they were is in each category's skip details, as before. Nothing is rolled back — everything that did apply is real and kept — and a **dry-run preview never downgrades**: a preview that predicts the shortfall predicted it.

  **What is deliberately NOT counted**, because reporting it would be crying wolf on every unattended cycle forever: a reference into a category you asked the run to *leave out*. An archived channel's membership of a channel profile you excluded was never going to arrive, and its absence is exactly what you asked for. Those rows now read **Dependency excluded by operator** instead of **Dependency unresolved**, and never touch the outcome. The two used to be one value, which is why neither could be surfaced: reporting the reason would have reported the deliberate exclusion as loudly as the real loss.

- **A sync that could not read the replica it describes no longer leaves a `success` behind in the record (bead `enhancedchannelmanager-bj442`, build 0133).** When a cycle cannot reach or cannot authenticate to its sync target, the task result and the message you see have said so since bead `enhancedchannelmanager-jqfxm`. What that fix did not correct is what the system *records*: the run's own `outcome` field still read `success`, and every surface that reads it inherited that — the **outcome column in Task History**, the `sync_outbound` audit entry, the target's stored **last outcome** and **last full sync** time on the Cross-Instance Sync card and in `GET /api/sync-targets`, and the `details.outcome` value an API or MCP consumer gets back. So the toast said one thing and the history you consult a week later said another.

  The counts are what made it possible: when a destination read fails, each importer carries on as if the destination were empty, so everything looks like a clean create and nothing gets counted as failed. Those counts describe your primary, not the replica.

  A realized cycle that could not read its destination now records that verdict **once**, where the outcome is decided, so every one of those surfaces reports the same thing. It is recorded as a failure, not as a degraded warning — a cycle that never got an answer out of the target knows neither what the target holds nor what it applied, so there is nothing to opt out of an alert about. **`last full sync` is no longer stamped** for such a cycle, which means a target that has been silently failing to authenticate will now show its drift instead of reading as current.

  A cycle that reaches its target is unaffected and still reports `success`, and a **preview** still has no recorded outcome — a plan is not a realized run.

- **A sync that loses your guide data or your channel artwork no longer reports a clean success (bead `enhancedchannelmanager-posm1`, build 0133).** A real cross-instance sync reported *"Sync success: created 133, updated 0, failed 0 across 9 categories"* while the replica it had just produced held 6 of 59 EPG links and not one channel logo. Every row genuinely succeeded, so every count was genuinely zero — and the run's own report already carried `epg_links_unrestored: 53`. The information existed; the word "success" sat on top of it.

  A cycle that finishes with a replica **missing something your primary had and the sync was asked to carry** now reports **completed with failures** instead of success. Four things count as missing: channels left with no stream that can play, streams holding an address the provider's credentials were cut out of, channels that arrived without the guide link your primary gave them, and logos your primary had that the replica does not. The applied state is still real and still kept — nothing is rolled back, and "completed with failures" has always meant exactly that.

  **It is a warning, not a red "Task Failed".** The severity comes from the outcome, not from which shortfall fired, which is how a lost logo is kept from shouting louder than a lineup that cannot play — an inversion this subsystem measured once already. If you have a shortfall you know about and have decided to live with, the per-task **Alert on warning** setting silences the alert without the code going back to calling it a success.

  **A replica that lost nothing is unaffected.** A channel your primary has no guide link for has not lost one, and a source with no credentials to strip has nothing to report — those cycles still read *"Sync success"*, with no clause added. That boundary is the point: a warning that fires on a faithful replica is a warning operators learn to ignore.

  **What operators will notice.** A recurring sync against a replica that is missing guide data or artwork will start reporting *completed with warnings* on every cycle instead of *success*, and — if you have alerts configured — will send one warning-level notification per cycle for as long as the shortfall lasts. It stops the first cycle after you fix it. Nothing about what sync writes to the replica has changed, and a **preview** never downgrades: a dry run that predicts a shortfall predicted it.

- **A restored or replicated provider account no longer keeps asking for credentials you have already re-entered (bead `enhancedchannelmanager-posm1`, build 0133).** Dispatcharr stores the provider's own sign-in reply on an Xtream Codes account, and that reply echoes back the username and password it authenticated with. ECM redacts credentials wherever it finds them, including in there — so the copy inside that cached reply was reported as another field to re-enter. There is no such field on any screen. On a live replica this meant the summary still read *"1 account(s) need credentials re-entered"* after the operator had entered the real credentials and the account was working, and no amount of doing what the message asked would clear it. The cached reply is now left out of the action item; the replica rewrites it itself on its next successful refresh. Credential fields you can actually re-enter — an account's own username and password, an EPG source's API key, a guide URL — are named exactly as before.

- **A scheduled sync that named 126 unplayable streams and two accounts needing credentials no longer goes quiet on the very next cycle while nothing has been fixed (bead `enhancedchannelmanager-ukjx5`, build 0133).** Cross-instance sync runs unattended, so most operators only ever see its one-line summary. Three of the numbers in that line were counting the wrong thing: they described what the cycle had just *written* rather than what the replica was *holding*. A cycle creates those rows exactly once — after that the rows are already there and are skipped — so the first cycle named the shortfall honestly and every cycle after it reported nothing, over a replica where nothing had changed. That is the more dangerous shape: the first cycle's honesty is what makes the later silence convincing, and the green becomes evidence.

  Measured on a 132-channel replica: cycle 1 reported *126 stream(s) restored without a playable URL* and *2 sources/accounts needing credentials re-entered*; cycle 2 reported neither, while the replica still held 126 streams with a redacted address, 126 channels bound to them, and not one credential re-entered. Both numbers now come from reading the replica itself, so they repeat on every cycle for as long as the condition lasts — and stop the first cycle after you fix it. That was measured too: re-entering the provider credentials on the replica dropped the credential clause; giving one stream a working address dropped the count from 126 to 125.

  **The channel-profile counter had the same confusion pointing the other way, and is fixed with it.** *"N profile membership(s) corrected"* counted every membership the cycle re-asserted, not the ones that had actually drifted — so a fully converged replica reported the same non-zero number forever: a finding that never changes, which is how operators learn to stop reading the line. It now counts only memberships that genuinely differed from your primary. On the same replica it went from 112 on the first cycle, where Dispatcharr really had exposed 112 channels a profile was built to hide, to 0 once converged — and back to 1, naming the channel, the moment one of those memberships was switched back on at the replica. Sync still re-asserts every membership on every cycle exactly as before; only the reporting changed.

  **What operators will notice.** These clauses now behave like a warning light rather than a one-time notice: on while the condition lasts, off when it clears. Nothing about what sync writes to the replica has changed. One number reads `null` instead of `0` on a **preview**: the redacted-stream count is measured by a pass that cannot run before an apply, so a preview now says "not predicted" rather than claiming the replica holds none.

- **A logo your provider supplies as a web address now reaches the replica, instead of only the logos ECM holds as files (bead `enhancedchannelmanager-sgrez`, build 0133).** Sync built the set of logos it would replicate from the two places that hold image *bytes*: files in ECM's own `/config/uploads/logos/` folder, and the Dispatcharr-hosted images an earlier fix taught it to fetch (bead `enhancedchannelmanager-cfxml`). A logo whose address is an absolute `http(s)` URL — which is how a provider-supplied logo normally arrives — is in neither set, so it produced no record at all: not a copy, not a miss, not a failure, nothing. On the documentation environment's primary, 59 of 60 logos were remote addresses and exactly one was Dispatcharr-hosted, so a cycle with logo replication switched on replicated **one logo in sixty** and said nothing was wrong. The channel-to-logo binding fix below is what made it visible: once bindings started crossing, the same run began reporting *"58 logo(s) could not be reinstated"* where it had silently reported none.

  **The address is copied, not the picture.** Dispatcharr stores such a logo as a name and a URL, and the replica fetches the image itself from that address, exactly as your primary does — the primary holds only the pointer too, so re-hosting the bytes would make the replica *diverge* from what it is meant to replicate, and would spend 59 network fetches on every unattended cycle, forever, for a copy nobody asked for. Logos that genuinely are files, ECM-local or Dispatcharr-hosted, still travel as bytes exactly as before.

  **A logo address that carries your provider's credentials is dropped rather than copied.** A provider that embeds a username and password in its stream URLs generally does the same in its logo URLs, so every candidate address now goes through the same credential scrub as the stream fix in **Security** below. Where that scrub finds nothing, the address crosses unchanged. Where it finds credentials, the logo record still travels but **without** its address, and the run names the logo and the channels it affected — a replica row pointing at an address that can only return "not found" is worse than an honest miss.

- **Channels on the replica now show the logos the replica already received (bead `enhancedchannelmanager-xgbjm`, build 0133).** Getting the image files to the replica and *attaching* them to channels are two different things, and only the first was fixed. Read off a replica after a cycle reporting `success, created 134, failed 0`: its Logo Manager listed the synced logo with usage **UNUSED**, and every channel on it carried no logo at all, while the matching channel on the primary carried one. So the replica held the right picture and no channel was using it — cosmetic in that nothing is unplayable, but it is the most visible difference between a primary and its replica, and it is the first thing an operator sees on opening the replica.

  A channel's logo is recorded as the *primary's* own id for that logo, which means nothing on the replica, so the channels importer deliberately leaves it out when creating the channel — sending it would either be rejected or, worse, quietly attach an unrelated picture that happens to hold that number on the replica. The pass that puts the right logo back afterwards already existed and was already correct; it had simply never been connected to the sync path, only to the archive-restore path. It now runs on both. It runs *after* the logos step rather than before the channels step, because it needs to know both what the replica called each channel and what it called each logo, and neither is known until both have been created.

  **On a replica that already has its lineup, your primary's logo wins.** Logo replication is off until you turn it on, so the realistic order of events is that cycles run and build the replica first, and the toggle goes on later — by which point every channel on the replica already exists and is being matched rather than created. A rule that only attached logos to brand-new channels would therefore attach nothing at all, on that cycle or any cycle after it, and the new toggle would look broken.

- **A sync that leaves your channels without guide data now says so, instead of reporting an unqualified success (bead `enhancedchannelmanager-v7d37`, build 0133).** Measured live against the documentation environment, reading both databases after one cycle: the run reported *"Sync success: created 133, updated 0, failed 0 across 9 categories"*, while the replica held 59 channels of which **6 had a guide link and 53 did not**, and its Xtream Codes EPG source had no address at all. The count that proves it — 53 unrestored guide links — was already being calculated and recorded; it simply never reached any surface an operator looks at, because the sync task rendered a narrower summary than the restore path did. Sync now renders the same summary the restore path renders, so the two cannot describe the same numbers differently, and a **dry run** states it in the future tense as a prediction.

  **This fix does not bring the guide links across, and they still do not cross.** An Xtream Codes guide address authenticates by carrying the username and password inside the URL itself, so the pass that removes credentials cannot separate the secret from the address and removes the whole value; the replica is then left with a source that has no address to fetch from. A guide URL on the same instance that carried no credentials crossed intact — that contrast is exactly why 6 channels kept their guide and 53 lost it. Restoring them is still open work.

  **The message now distinguishes a missing address from a missing password**, because *"N account(s) need credentials re-entered"* is the wrong instruction here: there is no password field to fill in, and nothing on screen recording where the source used to point. A lost address gets its own clause naming the address as the thing to restore. The same run now reads *"…53 channel(s) restored without an EPG link…"* alongside the counts it always showed.

  **Carrying the address with only the credentials blanked out is deliberately not done yet.** The redactor recognises credentials in a URL's sign-in portion and in its query string, but by design cannot see them in its *path*. Relaxing the all-or-nothing rule today would let an address carrying credentials in both places reach the replica with the path half intact — trading a visible loss for a silent leak. That is held open rather than guessed at.

- **A channel profile that hides channels on your primary no longer arrives on the replica showing all of them (bead `enhancedchannelmanager-38c5a`, build 0133).** A channel profile is a **restriction**. Measured on both databases after one cycle that reported `success, created 134, failed 0`: the primary's *Kids & Family* profile had **6 of 59** channels enabled, and the replica's had **59 of 59**. The profile itself crossed, and every counter an operator would check agreed that it had — the profile count matched on both sides. What did not cross was the *selection*, so the replica did not merely lose a setting: it inverted the setting's purpose, and it did so by failing **open**, silently. This is the same shape as the user-account defect below, where an empty list of profiles means unrestricted rather than no access.

  The selection was never lost in transit — it was carried and then dropped. The pass that re-asserts which channels a profile enables had been connected to the archive-restore path only, and never to the sync path; with nothing asserting it, Dispatcharr applied its own default, which is to enable a newly created channel on **every** profile. The same pass now runs on the sync path, under the same channel-profile category gate, **dry run included** — a preview that cannot warn you a cycle is about to expose the 53 channels a profile exists to hide is silent at the one moment you can still act on it.

  **Every way this can now go wrong fails closed rather than open.** A profile that arrives carrying no selection at all — as distinct from an empty one, which is a real "nothing is enabled" — now switches off the memberships *this run created* and names the profile in the report, rather than leaving Dispatcharr's enable-everything default standing. It never touches a membership you set yourself on a channel the replica already had. A membership the replica refuses is now a counted failure that names itself, which forbids the run from reporting success; it deliberately does **not** roll the cycle back, so everything that did apply is kept.

- **Restoring a Dispatcharr user account no longer attaches it to the wrong channel profiles (bead `enhancedchannelmanager-if05f`, build 0127).** Dispatcharr numbers channel profiles per instance, and a user account carries the list of profiles it is limited to as those numbers. ECM sent the backup's numbers to the destination unchanged, so a restored account was attached to whichever profiles happened to hold those numbers there — a different set of channels than the account was meant to see, with the restore reporting a clean success. This is the same class of defect as the M3U account's user-agent and server-group links below, but a quieter one: those produced a visible error, and this produced a wrong answer that looked right. Each limit is now translated to the destination's own matching profile. If some of an account's profiles are missing on the destination, it is restored with only the ones that exist and the report names it. If **none** of them exist, the account is deliberately **not restored** and the report says why — Dispatcharr treats a user with no channel profiles as having unrestricted access, so creating the account would widen its reach rather than restore it; restore the channel-profile category first, then re-run the user category. Restoring users remains opt-in and off by default, and user accounts remain permanently excluded from cross-instance sync, so only a restore was ever affected.

- **Turning off fuzzy stream matching for a sync target now actually turns it off, so a replica channel can no longer end up playing a different channel's stream (bead `enhancedchannelmanager-efvyg`, build 0127).** **Fuzzy stream matching** is off by default precisely because a guess is dangerous on a schedule: a name that merely *resembles* one of the replica's streams must not be treated as a match. The setting was honoured while sync created the channels — and then silently discarded by the pass that runs afterwards. Sync attaches a stand-in stream to any channel whose real stream the replica does not have yet, then re-runs the matcher once the replica's provider streams exist; that second pass never received the setting and always ran the full ladder, guesses included. On the instance this was found on, with the setting off, the replica's **XDMRU News One** was bound to the stream named **XDMRU News Two** and the cycle reported success. That is the worst shape this subsystem can produce: not a missing channel, which you would see, but a channel that plays the wrong thing, which you would not. The setting now governs every stream-matching decision in a cycle, at every stage. With it off, a channel whose stream the replica does not yet have keeps its stand-in — which carries the primary's own stream URL and plays the right content — instead of being guessed onto a neighbour. With it on, nothing changes: the fuzzy rung is still reachable, as the setting promises.

  **What operators will notice.** A replica already carrying a wrong binding from the old behaviour repairs itself: each cycle re-attaches a channel's streams from the primary, so the next apply after this fix replaces the wrong stream with a stand-in carrying the primary's own URL. That was measured on a two-instance stack — a channel deliberately left bound to its neighbour's stream came back bound to the right content on the following apply, with no manual intervention. Restoring a backup is unaffected and deliberately so: a restore still matches on the full ladder, because on a fresh instance the second pass is where essentially all of a restore's stream matching happens, and flooring it would leave restored channels on stand-ins.

- **A cross-instance sync dry run can no longer report success against a replica it never reached, and no longer offers Apply afterwards (bead `enhancedchannelmanager-jqfxm`, build 0127).** "Dry run, then apply" is how you confirm a sync is safe before it overwrites the replica, so the preview has to be able to tell you when something is wrong. It could not. Every count a preview shows is a claim about the replica — "would create 27" means "the replica does not have these 27" — but each category quietly treated a failed read of the replica as *the replica is empty*, so a preview that could not sign in produced exactly the same result as a preview against a brand-new instance: a full would-create plan, zero failures, and the **Apply** button unlocked. On the instance this was found on, a deliberately wrong password produced `would create 27, failed 0` while the replica logged eight rejected sign-in attempts, and re-running with the correct password showed the honest answer: **would create 0, skip 27** — the replica already had everything. The number described the primary, wearing the replica's clothes.

  A sync cycle now asks the replica one authenticated question before it does anything else, and stops if it does not get an answer. Wrong credentials, an unreachable host, a refused TLS handshake, a DNS failure, a blocked address, a server error, and a rate limit each stop the cycle with a plain sentence saying which one it was — a rate limit in particular is now named as a rate limit rather than reported as a credential problem, so you are not sent to rotate a password that was fine. No counts are shown, because they would describe the wrong instance. A read that fails partway through a cycle is reported the same way instead of quietly becoming "the replica is empty". This applies to a confirmed Apply as well: a replica that cannot be read is not written to.

  **What operators will notice.** A preview that used to come back green against an unreachable or misconfigured replica now reports a failure and does not offer **Apply**; the message names the cause. A cycle that cannot reach the replica also stops after a single sign-in attempt instead of one per category — on the instance this was found on, eight attempts in one preview, enough to trip the replica's own sign-in rate limit and make the failure look like something it was not.

- **An M3U account with a custom user agent no longer aborts the entire cross-instance sync (bead `enhancedchannelmanager-9h6cv`, build 0127).** A Dispatcharr M3U account's **User-Agent** setting is a link to a user-agent record, and each instance numbers its own records differently. Sync sent the primary's numbering straight to the replica, which rejected it as an unknown record — and because M3U accounts are the first thing a cycle writes, that single rejection rolled the whole cycle back. Nothing synced at all: not channels, not EPG sources, not groups or profiles, on every cycle, for as long as any one account used a non-default user agent. The replica simply stayed frozen at whatever it last held while the task reported a failure. User agents are now replicated ahead of everything that links to one, and an account's link is re-pointed at the replica's own copy. If the replica has no matching user agent at all, the account is now created without one — it falls back to the default agent, and the sync report names the account so you can pick an agent on the replica — rather than the account, and the cycle, being lost. The same ordering fix applies to restoring a backup onto a fresh instance, which had the identical flaw.

- **An M3U account that belongs to a server group no longer aborts the entire cross-instance sync (bead `enhancedchannelmanager-g8tyd`, build 0127).** Dispatcharr's **server group** — the grouping that makes several M3U accounts share one provider's connection limit — is a link to a server-group record, and each instance numbers its own records differently. Sync sent the primary's numbering straight to the replica, which rejected it as an unknown record; because M3U accounts are the first thing a cycle writes, that single rejection rolled the whole cycle back and nothing synced at all — the same total failure the custom-user-agent fix below describes, from a second field on the same account. It went unnoticed only because no account on either validation instance had a server group set. Unlike a user agent, a server group cannot simply be re-pointed: ECM does not replicate server groups, so there is nothing on the replica to point at. An account that belongs to one is now created on the replica without it, and the sync report names the account so you can re-assign it. Nothing else about the account changes, and the rest of the cycle completes normally. If the accounts sharing that group need a shared connection limit on the replica, create the server group there and assign them once; the assignment then stays put, because sync never removes one it did not set.

- **Cross-instance sync now replicates user agents, so a stream profile that uses a custom one is created on the replica instead of being silently skipped (bead `enhancedchannelmanager-hiacv`, build 0127).** ADR-013 already listed user agents among the configuration categories synced every cycle, but the engine neither read them from the source nor imported them on the destination. Any stream profile pointing at a custom user agent therefore had an unresolvable link and was skipped, leaving the replica without that playback configuration while the cycle still reported its normal outcome. User agents are now read each cycle and imported immediately before stream profiles, so the profile's user-agent link is re-pointed at the replica's own copy. User *accounts* remain permanently never-synced; a user agent is a different entity.

- **Cross-instance sync now sends your actual logos to the replica instead of whatever happens to be in ECM's own upload folder (bead `enhancedchannelmanager-cfxml`, build 0127).** Dispatcharr, not ECM, is where a logo's image file lives: a logo you upload through ECM's Logo Manager is written into Dispatcharr's storage, and ECM's own `/config/uploads/logos/` folder holds at most a stale copy left over from an older install. Sync read only that folder, so a replica with logo sync enabled received only those leftovers — on the instance this was found on, two files from March, while Dispatcharr held the real set. Backups were taught to fetch the real image bytes from Dispatcharr in an earlier fix (bead `enhancedchannelmanager-xb58a`); sync now uses the same path, so a replica ends up holding every logo the primary can serve. Where both sources describe the same logo, Dispatcharr's copy is the one that travels. Logo sync remains **off by default and per target**, sync still never deletes or bulk-clears the replica's logos, and images are still fetched and uploaded one at a time rather than all held in memory at once. Because sync runs unattended on a schedule, the image fetching is now time-bounded per image and per cycle; a very large logo set that runs out of budget is reported as missed logos for that cycle and picked up on the next one, so the replica still catches up.

- **A cross-instance sync that leaves a channel unable to play no longer announces itself as a failure, and no longer hides behind one either (bead `enhancedchannelmanager-daziw`, build 0127).** A restore that finishes with a channel holding no playable stream has reported `completed_with_failures` rather than success since build 0027, and alerted as a warning. Cross-instance sync runs on the same engine, so it already produced the same outcome — but it builds its own task result, and never learned the second half of that rule: the identical shortfall arrived as a red **Task Failed** alert, with a task-history row reading `failed` beside `0 failed` items. Nothing had failed and nothing had been rolled back; the replica simply had a channel that would not play. Such a run is now reported the way the restore path reports it — **Task Completed with Warnings**, with a `warning` notification — and the one-line summary now names how many channels have no playable stream instead of leaving the alert to say `failed 0` and nothing else.

  **What operators will notice.** Sync runs that previously looked clean can now end in `completed_with_failures`, and where alerts are configured with **Warning** enabled, they will send a notification they did not send before. That is the point: the run really did leave a channel that cannot play, and sync runs unattended on a schedule, so nobody was going to catch it by hand. If a target has a shortfall you already know about and accept, turn **Warning** alerts off for that target's sync task; the outcome, the in-app notification and the task history stay honest while the external alert goes quiet. Rolled-back and unreachable-replica failures are unaffected and still alert as errors, and nothing about what sync writes to the replica has changed.

### Changed

- **60 dependency updates across backend, mcp-server and frontend, with no functional change intended and everything gated green (epic `enhancedchannelmanager-gflo5`, build 0146).** 39 backend packages, 2 mcp-server packages and 17 frontend packages move to their latest compatible release, plus `starlette` and `uvicorn` moving separately — 60 in total. Every gate ran green against a venv and lock built fresh from the new pins: backend `pytest tests/` 11522 passed / 9 skipped, the canonical `scripts/backend-gate.sh` 11456 passed / 3 skipped / 2 deselected, and frontend 253 files / 3568 tests passed, with lint and `tsc --noEmit` clean and `npm ci` reproducing the lock.

  **Build 0146 introduced no new known vulnerabilities, but it left the standing `ecdsa` PYSEC-2026-1325 advisory unresolved at that time.** The dependency was transitive via `python-jose`, had no published fix, and remained ignored in CI by an explicit waiver. Build 0169 / bead `enhancedchannelmanager-tm9ma` subsequently replaced `python-jose` with PyJWT, removing both `ecdsa` and the CI waiver. The build 0146 work was a currency sweep, not a security fix. Nothing in that sweep hardened ECM against a threat it wasn't already resistant to.

  **Frontend dependencies are now hard-pinned.** Every caret and tilde in `frontend/package.json` is gone, so an install resolves exactly the versions that were tested rather than a range.

  **`starlette` moves to 1.6.0, converging the backend with `mcp-server`**, which already ran that version. `fastapi` is deliberately held at 0.136.1 — see below.

  **Deliberately not upgraded, and why:**
  - `fastapi` 0.137+ stops flattening included routers into `app.routes`, which breaks the MCP capability gate's route-template resolution (bead `enhancedchannelmanager-g4pnh`)
  - `mcp` 2.x renames `FastMCP` to `MCPServer` and moves its import paths, which fails at import time (bead `enhancedchannelmanager-wiezs`)
  - `alembic` 1.19 adds a CHECK-constraint comparison that trips a tool bug against migration 0012 (bead `enhancedchannelmanager-y6j4g`)
  - Four frontend majors — `typescript` 7, `recharts` 3, `jsdom` 30, `@testing-library/jest-dom` 7 — are held for individual evaluation, one major per PR (bead `enhancedchannelmanager-4ba6m`)

  All four are tracked under epic `enhancedchannelmanager-gflo5`.

- **Logo replication to a sync target is now ON by default, on a 24-hour clock (bead `enhancedchannelmanager-2yq19`, build 0145).** It shipped OFF, and the recorded reason was cost rather than correctness — which meant a replica silently arrived with no artwork unless you found and flipped the toggle. The cost is now answered by a throttle instead of by an unbranded replica: the logo slice still does not run on every cycle, but a new **logo sync interval** (default 24 hours, `0` meaning every cycle) decides when it does. A brand-new target carries its logos on its **first** cycle rather than waiting out an interval with nothing on the replica at all.

  **A target you have already turned logos off on stays off.** The change moves the default for targets you create from here on and rewrites no stored value, because a stored *off* is indistinguishable from a deliberate choice you made and a migration is the wrong place to overturn it. If ECM cannot read a target's interval it falls back to running the slice every cycle rather than never — a value that cannot be trusted has to fail toward the faithful copy, because failing the other way reinstates the silent omission invisibly.

- **The security-governance audit now runs on the exact `dev` push SHA despite GitHub's default-branch workflow-dispatch rule (bead `enhancedchannelmanager-04c0u.11`, build 0125).** The audit remains available for quarterly schedules and manual dispatch after it reaches `main`, and is now also reusable. The already default-branch-active Tests workflow calls it on every `dev` push with read-only contents and security-event permissions, so branch-protection settings, the authoritative beads branch, dependency-alert services, automated fixes, and open secret-scanning alerts are checked immediately without mutating `main` or relying on a workflow file that GitHub cannot yet dispatch.

- **Container verification now runs independently of the test workflow, while publication remains exact-commit and fail-closed (bead `enhancedchannelmanager-04c0u.11`, build 0124).** The retired 30-minute `Wait for Test Suite` polling job has been removed. Each AMD64 and native ARM64 ECM and MCP image is built once as an immutable OCI candidate; Trivy and ZAP inspect that same archived candidate, and publication promotes the four attested manifest digests without rebuilding. Image verification therefore remains visible even when a separate GitHub-hosted test runner stalls. Mutable tags change only after the current branch SHA has matching successful Tests and Build push evidence, all four candidate digests are present and unchanged, and dependency-changing dev pushes have successful frontend and backend dependency scans. Stale heads, missing architectures, digest substitution, skipped required scans, reruns for another commit, and failed or incomplete evidence all fail closed and retain the last known-good image. The backend lock also advances `aiohttp` and `cryptography` to their fixed releases. Release governance gains dependency-change enforcement, an authoritative-board P0/P1 gate, pinned workflow dependencies, and auditable security-control checks.

- **An unscheduled install can no longer silence its only backup reminder forever (bead `enhancedchannelmanager-iij6s`, build 0115).** Dismissing **Backups are not scheduled yet** now snoozes it for 30 days. The reminder returns while no DBAS backup schedule is enabled, and enabling a schedule ends the reminders. A manual backup does not dismiss the warning because it protects only the point in time when it was created.

### Security

- **MCP key rotation and revocation can no longer be undone by a stale settings save, startup, restore, or crash (bead `enhancedchannelmanager-s6pg3`, builds 0153, 0155, 0156, 0157, 0158, 0159, 0160, 0161, and 0162).** The owner-only `api-key` sidecar is the single live authority; `settings.json:mcp_api_key` is a compatibility mirror that generic saves and restores cannot promote over it, and an explicit empty authority remains the durable revocation tombstone. Authority reads now distinguish valid, absent, and present-but-untrusted artifacts. Invalid content, mode, owner, file type, or link count is preserved, cached as degraded state, and exposed as no MCP key so startup, health, and JWT authentication continue; non-following path fingerprints automatically revalidate after an operator or peer repairs or creates the authority. Malformed or untrusted `.api-key.recovery` records are likewise preserved and cannot initialize or transition over independently validated state. Explicit save, rotate, and revoke operations remain fail-closed and their public routes return stable, repair-oriented `503` responses without logging credential values or resolved secret paths.

  Dedicated transitions still stage a startup-inert `prepared` redo before replacing authority and mark it `recovery-active` only after replacement. Before a successor can stage, any active predecessor is reapplied and its authority parent-directory fsync must succeed; failure preserves the predecessor record, while a failed successor stage can leave only an inert prepared record after the predecessor value is durable. No credential pre-image is stored, so recovery cannot resurrect a revoked key. First install and legacy migration have no predecessor and therefore publish authority directly without transition WAL; if parent-directory fsync is refused, the same key remains in the settings mirror so restart republishes it. Ordinary load now sweeps both authority and recovery temporary names without following symlink targets; an unlink refusal there is advisory and leaves the refused temporary in place while authority and recovery are validated independently. Invalid settings and degraded artifacts use stable caches rather than taking the lifecycle flock per request, and cached absence detects a peer-created authority. Descriptor reads use nonblocking opens so FIFOs and devices are rejected as non-regular artifacts without hanging. Recovery-active keys containing any `splitlines()` separator are preserved as untrusted and cannot replay over authority; the empty key remains a valid revocation. The existing durability-indeterminate response remains for a replaced authority whose final durability state cannot be proven; the UI preserves a returned active rotation key and reports active revocation without serializing the key into generic logs. Build 0160 has no runtime change; it updates the password-reset-origin integration fixture to install the complete cache state introduced in build 0159, restoring the canonical backend gate without weakening file-change convergence. Build 0161 centralized the sanitized `MCPApiKeyStorageError` mapping for public settings writers. Build 0162 narrows that mapping to MCP authority and recovery preflight failures, preserving lock-timeout and unrelated settings-file contracts; rotate and revoke now type pre-commit filesystem failures without changing authority. A completed M3U account deletion now returns and journals a sanitized partial result when linked-settings cleanup fails, with explicit no-retry guidance, while selective YAML restore marks only the settings section failed and continues later selections. It also corrects the README and architecture references to name `api-key` as sole live authority and `settings.json:mcp_api_key` as its compatibility mirror.

- **Destructive follow-up work now stops at failed settings boundaries (bead `enhancedchannelmanager-s6pg3`, build 0163).** Deleting an M3U account reports partial success for every linked-settings cleanup failure, not only MCP authority failures, so an operator is warned not to retry an upstream deletion that already landed. Selective YAML restore now applies a selected settings section before Dispatcharr-backed sections; if settings cannot be saved, independent local sections continue but Dispatcharr-backed sections are reported failed without contacting and potentially deleting data from the previously configured server.

- **The MCP image now fails closed on the fixed Alpine OpenSSL packages for CVE-2026-14456 (bead `enhancedchannelmanager-ihwjt`, build 0150).** Fresh PR image scans found `libcrypto3` and `libssl3` `3.5.7-r0` on both published architectures. Docker Hub's current official `python:3.12-alpine` manifest is still the reviewed digest already pinned by ECM, so changing only the digest could not invalidate BuildKit's cached vulnerable `apk upgrade` layer. The layer now explicitly requires both packages at `3.5.8-r0` or newer after the full Alpine upgrade, changing the cache key and making the build fail if a repository cannot supply the fixed versions. The MCP supply-chain gate enforces the digest and both package floors together.

- **Operator security guidance now matches what ECM actually enforces, and the destructive Settings controls name what they are about to destroy (bead `enhancedchannelmanager-04c0u.12`, build 0141).** The MCP reference now states the credential model as it is: three separate credentials, only one of which you ever handle, plus an explicit list of what the MCP key cannot do (taking or restoring backups, TLS and private-key lifecycle, identity and authorization administration, rotating its own key, and creating, changing, deleting or testing outbound destinations). It states plainly that enabling ECM's TLS setting does **not** protect MCP, because the sidecar is a separate listener on its own port with no TLS of its own, and that rotation is a human administrator action which a leaked key cannot perform on itself. The disaster-recovery runbook was exercised against a live instance rather than reread, which found three defects an operator would have hit mid-incident: every ECM API call named port 8080, which ECM has never served on; every in-container probe called `curl`, which the ECM image does not ship; and the residue-cleanup step used a Dispatcharr path that returns the web UI with HTTP 200 instead of deleting anything, so the cleanup would have reported success and removed nothing. The runbook now also states that restored Dispatcharr accounts receive a random password ECM discards and have every privilege flag forced off, that a username collision is skipped rather than overwritten, and that a DBAS restore never reinstates ECM's TLS certificate. The guide now also qualifies that list by authentication mode: most of it is enforced only while **Require Authentication** is on, and the same sentence appears in the Disable Authentication dialog, where the operator actually makes that choice. A new section states what a leaked MCP key exposes (the household viewing history, `/api/journal`, and per-user statistics), the three properties that make a leak worse than it looks (the key never expires, one key is shared by every client, and a leak is not detectable from ECM's logs), and a post-leak checklist, because rotation does not undo what was already read. In Settings, disabling authentication, rotating or revoking the MCP key, deleting a saved backup, and restoring a full backup over the running instance now each require a type-to-confirm dialog; the two that act on a named artifact put that filename in both the warning and the phrase you must type, and the other three name the capability being turned off. Every confirmation dialog now exposes its warning body to screen readers via `aria-describedby`, which previously went unread because focus lands on the confirmation input. The behavioral claims are pinned by tests that read the enforcing code rather than the prose: the human-only route list in both directions, so a route can no longer be quietly un-reserved; the gates that keep enforcing while authentication is off; the confirmation TTL; the restored-account privilege defaults; and the two port numbers. Those cannot drift from the behavior silently. That defect was not confined to the one runbook. A sweep of the whole documentation tree — reading indented fenced blocks and inline code spans, not only top-level ones — found fourteen `docker exec … curl` or `… wget` invocations across eleven files, and all fourteen are replaced here with a `python3` equivalent that the image can actually run. Two sentences that named the old command shape are re-pointed at the new one. A fifteenth is deliberately left as it is: it is quoted inside the warning that tells you it does not work.

- **A stolen or mishandled backup no longer hands over your TLS private key, your provider playlists, or a world-readable copy of anything (bead `enhancedchannelmanager-04c0u.13`, build 0140).** Every backup class now has a stated confidentiality policy, and `docs/user_guide/backup-restore/backup-overview.md` carries it as a table you can check an artifact against. The **Full Backup (legacy `.zip`)** no longer copies the `tls/` and `m3u_uploads/` directories, so a plaintext artifact stops carrying TLS private keys and uploaded playlists whose stream URLs embed provider credentials; logos and the configuration tables still travel, and restoring an artifact taken by an older build still reinstates the trees it already contains, so no existing backup loses material. Locally persisted `.zip`, `.yaml`, and `.sha256` files are created owner-only (`0600`) instead of inheriting the process umask, and an existing file with broader permissions is corrected on write. Scheduled backups remain the keyless recovery path on purpose: they contain no ECM accounts, password, session or reset hashes, storage credentials, TLS private keys, or raw playlists, so unattended recovery needs no passphrase that could be lost with the host. An encrypted artifact's header is now checked against the exact supported envelope (format version, KDF and AEAD identifiers, scrypt `n`/`r`/`p`/key length, salt and nonce lengths, and chunk size) **before** any key derivation runs, so a hostile artifact cannot make ECM perform attacker-chosen scrypt work by declaring an enormous cost parameter. A malformed or extreme header is refused with the same generic error as a wrong passphrase. The **Create Full Backup** card and the API reference no longer describe the archive as carrying TLS certificates and M3U files, which it stopped doing with this change: an operator who read the old text, rebuilt a container, and restored would have found the TLS private key permanently gone, because there is no second copy. Both surfaces now say plainly that a full backup is not a copy of your TLS certificate, and the card's warning describes the format that actually ships. The legacy `settings.json` also gains the value-aware URL scrubber the DBAS formats already used, so a credential written into a URL setting - the Dispatcharr address, and equally an Emby, Jellyfin, or Plex base URL - no longer travels in clear; restoring keeps whatever address the destination already has, so a same-instance restore loses nothing. A legacy restore that reinstates a `tls/` directory now writes it owner-only (`0700` directory, `0600` files) to match how ECM writes that tree itself, rather than at the process umask; restoring a current artifact leaves an existing `tls/` directory untouched. Local artifact writes refuse to follow a symlink planted at the destination path, and `passphrase` joins the credential-key denylist so a future writer cannot ship one inside a task or alert-method configuration blob.
- **Overlapping settings saves can no longer leave ECM reporting a credential that is not the one on disk, and a saved settings file now survives a host crash (bead `enhancedchannelmanager-04c0u.10`, build 0139).** When ECM saves your settings it already wrote through a private temporary file and swapped it into place in one step, so a save interrupted part-way left the previous file intact. This build closes the three gaps that remained in that writer. Two settings saves that overlap are now serialized, inside ECM and across processes, using an exclusive lock on `/config/.settings.lock`, so the save that wrote the file last is also the one left in ECM's in-memory view: previously two overlapping saves could leave the Settings page and the MCP sidecar disagreeing about the current MCP API key until ECM was restarted. That lock is taken with a five-second ceiling rather than waiting indefinitely, so a settings save that cannot get it in time now fails with `503 Service Unavailable` and can be retried, instead of stalling every other request in the process behind it. The directory holding `settings.json` is synced after the swap, so losing power straight after a key rotation can no longer resurrect the previous credentials, and a filesystem that refuses that sync no longer reports a failure for a save that in fact succeeded. The file's permissions are now always owner-read/write rather than depending on the environment of the process that happened to perform the save; this makes the mode predictable rather than narrower, since the previous writer could never produce a wider one. ECM also removes, at startup, any leftover temporary settings file that a killed or power-cut save left behind. Each of those is a full copy of your credentials, so after a key rotation an orphan would otherwise preserve the replaced key indefinitely.

  Two known limits remain, both tracked for follow-up work. This change serializes one save against another save; it does not serialize a settings *read* against a save, so a read that began just before a save can still finish afterwards and leave ECM's in-memory copy behind the file until the next save. And that in-memory copy is per process: when HTTPS is enabled ECM runs a second process, so a settings change made over HTTPS is not picked up by the main process until ECM restarts. Neither affects what is stored on disk, and neither weakens authentication.

  This entry describes ECM's own settings writer only. Restoring a configuration backup writes `settings.json` through a separate code path that does not yet use the lock, the temporary-file swap, or the explicit permissions.

- **Enabling ECM's HTTPS listener now stops browsers from carrying your login over the plain HTTP port (bead `enhancedchannelmanager-04c0u.9`, build 0138).** Login and refresh cookies were always issued without the `Secure` flag, so on an install with TLS turned on, any browser that reached ECM over HTTP still sent a working session in clear text, and the UI called that port a normal "HTTP fallback". Session cookies are now `Secure`, `HttpOnly`, and `SameSite=Lax` whenever ECM's own TLS is active with a certificate and key in place, or whenever the canonical public base URL is an `https://` origin, which is how a reverse-proxy deployment declares itself. That decision is made from your saved configuration only: a caller-supplied `X-Forwarded-Proto` or `X-Forwarded-Host` header cannot talk ECM into either treating a plaintext request as secure or emitting HSTS, and HSTS is sent only by ECM's own HTTPS listener, where ECM terminated the connection itself. The HTTP port keeps serving health checks and the unauthenticated recovery surfaces, and ECM does not redirect it, because the HTTPS listener may be on a different port or be the thing that is broken. For the case where TLS breaks and locks you out, there is one explicit break-glass path, off by default and in two places: **Emergency recovery: allow authenticated sessions over HTTP** in TLS Settings for an administrator who can still sign in, and `ECM_ALLOW_HTTP_SESSION_COOKIES=true` on the container for one recovery restart when nobody can. Either one puts your session credentials back in clear text on the network, so turn it off as soon as HTTPS works again. Turning TLS on before the certificate and key exist does not lock you out; the protection engages when the key material is actually present. The one-time forced sign-out that closes off pre-activation cookies now happens wherever TLS actually starts terminating: issuing or completing a Let's Encrypt certificate, uploading one by hand, or a manual renewal that issues your first certificate. It is no longer tied to the TLS switch itself, which on neither of the two real flows is the moment activation happens. The plaintext-session warning banner now also appears on reverse-proxy installs that have set an `https://` public base URL, where break-glass really was stripping the `Secure` flag while the panel looked untouched. And an unreadable `/config/tls_settings.json` no longer locks every account out of an install that never had TLS: session cookies still fail safe, but sign-in is only refused where a certificate or a running HTTPS listener says TLS really is terminating. Saving TLS settings now writes a new file and renames it into place, so a full disk or a container killed mid-save can no longer leave a truncated one behind.

  Four things about this are worth knowing before you upgrade. **Turning TLS on signs everyone out once**, including the administrator making the change: ECM revokes every existing browser session at that moment, because a browser that already holds a pre-TLS cookie would otherwise keep replaying it on the HTTP port for up to a week. **Signing in over the HTTP port now fails immediately and says so** instead of appearing to work and then dropping you back at the login screen, which previously made people retype their password in clear text several times over. **Emergency recovery works on reverse-proxy installs too.** It is checked before the public-base-URL signal, so setting it actually has an effect whichever way you run ECM. While it is active the TLS panel shows a warning banner, `GET /api/tls/status` reports it, and ECM writes a warning to the log at startup and when it first downgrades a cookie, so it can no longer be left on silently. Saving TLS settings from an older or scripted client no longer switches emergency recovery off behind your back: a request that does not mention the setting leaves it as it was. **A note about the one-year HSTS pin ECM's HTTPS listener sends:** browsers apply it to the whole hostname regardless of port, so after you visit the HTTPS address once, your browser will refuse plain HTTP on that hostname (including the recovery port) for a year, with no way to click through. Reach ECM by its IP address instead, or clear the pin (`chrome://net-internals/#hsts` in Chrome, *Forget About This Site* in Firefox). README's Port Configuration section walks through this.

  On the header question, the earlier wording above overstated what ECM controls, so to be precise: ECM's own policy code never reads `X-Forwarded-Proto`, `X-Forwarded-Host` or `Forwarded` for cookie or HSTS decisions. uvicorn, the server ECM runs under, enables its own proxy-header handling by default and may rewrite the request scheme for clients on the loopback address before any ECM code sees the request. That is uvicorn's behavior rather than ECM's, it can only push the scheme toward `https`, and HSTS asserted over a genuinely cleartext connection is ignored by the browser.

- **The debug bundle no longer ships your provider username and password in clear (bead `enhancedchannelmanager-d0hoc`, build 0137).** Measured on a live instance: one bundle's `channels.csv` and `channels.json` each held **316 copies** of the real Xtream Codes username and password, as `https://example.com/live/<username>/<password>/13580.ts`. The hostname *was* replaced, so the file looked redacted to anyone who opened it — which is why this survived so long on the one artifact the product tells you to attach to a support ticket. **If you generated a debug bundle before this build and posted it anywhere, treat those credentials as disclosed and rotate them with your provider.**

  **The cause was that the credential rule was written against a URL shape your provider does not use.** It matched a three-segment path, `/<username>/<password>/<id>.ts`. Dispatcharr renders four — `/live/<username>/<password>/<id>.ts` — and also `/movie/`, `/series/`, and `/server1/live/...`. So the one shape that got scrubbed was the only one that never appears, and every real stream URL went through untouched.

  **The fix does not match shapes as its primary rule; it matches your actual credentials.** ECM now reads the credential values out of your own M3U accounts, EPG sources and settings before it writes anything, and removes those values wherever they appear in the bundle — in a path, in a query string, in a log line that is not a URL at all, in any of the ten files, however deeply nested. A URL-shape rule is kept as a second layer, widened to any prefix depth, so that a credential ECM cannot enumerate — a stale stream row, a URL you typed by hand — is still caught. `manifest.json` now states whether the value-based rule was able to run, so a bundle built while your provider was unreachable says so instead of looking equally clean.

  **What it still cannot promise.** A credential ECM does not hold, in a URL shape the fallback does not recognize, can survive; so can a very short credential written outside a URL. The wording in Settings and in the MCP tool has been corrected to say redaction is thorough rather than complete, and to suggest skimming a bundle before posting it publicly.

- **Cross-instance sync sends your provider's credentials to the replica, and now sends nothing else (bead `enhancedchannelmanager-fmtg0`, build 0137).** The per-cycle credential cascade added in build 0136 carries credentials in the clear inside two sections of the payload — your M3U accounts and your EPG sources — and redacts everything else. Which *fields* it carried was decided by ECM's whole credential vocabulary, 25 names long, including `smtp_password`, `plex_token`, `mcp_api_key`, `dispatcharr_api_key`, `telegram_bot_token`, `discord_webhook_url` and `private_key`. None of those authenticates to an IPTV provider; they authenticate to ECM itself, or to a service you run.

  **That width was reachable, not hypothetical.** The rule matches on field *name* at any depth, and Dispatcharr stores your provider's raw `player_api` reply verbatim inside an M3U account profile — a block of data whose field names come from the provider, not from ECM. Any of those names appearing there would have crossed in the clear. What crosses is now limited to the two credential fields Dispatcharr actually puts on an M3U account or an EPG source, `username` and `password`, and a test pins ECM's own secret names as excluded so widening the credential list later cannot quietly re-admit them.

  **Nothing you rely on changed.** Xtream Codes usernames and passwords, a plain-M3U account's whole `server_url`, an authenticated XMLTV source's `url`, and the Schedules Direct password you typed on the sync target all still reach the replica exactly as before.

- **A replica whose channels cannot play is no longer reported as a clean success, and it now repairs itself (bead `enhancedchannelmanager-1td94`, build 0133).** The credential fix above deliberately keeps a stream's address and replaces only the credential segments, so a replicated Xtream Codes stream arrives reading `http://provider/live/***REDACTED***/***REDACTED***/1234.ts`. That address cannot fetch anything — but it is not *empty*, and the check that decides whether a channel has a playable stream only asked whether the address field had something in it. So a replica whose lineup was 90% dead reported **zero channels with no playable stream** and a clean success. Measured on a 59-channel replica: 53 of its channels returned HTTP 404 when their stream was actually fetched, while the run reported none.

  Worse, it did not recover. The stream matcher identifies the same stream on both instances by comparing addresses — and once both the primary's record and the replica's stand-in carried the same redacted text, the stand-in became a perfect match for itself. Re-entering the provider credentials on the replica and refreshing brought the real streams in beside the stand-ins, and every following cycle re-picked the stand-in anyway, permanently. The published fix for this was to delete an ECM-created account by hand between the two steps.

  A redacted address is now treated as what it is — the record that an address was removed, not an address — everywhere the question "can this stream play?" is asked. A channel left on one is counted and named, the run reports **completed with failures** instead of success, and the matcher will not accept a stand-in it cannot play when a real stream is available, on any of its matching rungs. It still accepts one when there is genuinely nothing else, so a replica waiting on its credentials does not accumulate a fresh set of stand-ins on every unattended cycle.

  **What operators will notice.** A cycle that leaves channels unable to play now says so and names them, rather than reporting success. Recovery is two steps and neither is destructive: re-enter the credentials on the replica's matching M3U account and refresh it, then let one more cycle run. Measured end to end on the same 59-channel replica: after the refresh the channels were still on the 53 dead stand-ins; after one more cycle all 53 were bound to the real streams under the replica's own provider account, every dead stand-in had been cleaned up, and the run was back to success. **Do not delete the `ECM Custom Streams (DBAS restore)` account by hand** — that step is no longer needed, and on a replica that is also a restore destination the account can hold stand-ins other channels are still using.

- **Cross-instance sync no longer sends your provider's username and password to the replica inside every stream URL (bead `enhancedchannelmanager-msqf7`, build 0133).** Sync has always promised that credentials are stripped before anything goes on the wire, and for an Xtream Codes provider that promise was not kept for the one field nobody thought to look at. An Xtream Codes stream address *is* the credential: the provider serves each stream at `…/live/<username>/<password>/<id>.ts`, so the username and password were part of the URL, and the URL is copied to the replica by design — it is how sync recognises the same stream on both instances. Nothing in the run said so; the summary reported that credentials had been stripped, and they had been, everywhere except there. On a real provider sampled while investigating this, **every one of its 1.4 million stream URLs** carried the pair that way, and each scheduled cycle put them into the replica's database — and therefore into the replica's own backups — again.

  ECM now recognises those path segments and replaces them before the URL leaves the primary. It does this by matching the credential values the primary actually holds, not by guessing which part of an address looks like a password, so a stream URL that merely *resembles* the pattern — a channel named after a number, a path segment that happens to match a username — crosses byte-identical and plays on the replica exactly as before. Only a URL that genuinely contains your provider password is rewritten.

  **What operators will notice.** A replicated Xtream Codes stream now shows an address like `http://provider/live/***REDACTED***/***REDACTED***/1234.ts` on the replica. You can still see which provider and which stream it was — the address is kept, deliberately, rather than blanked — but it will not play until the replica has its own copy of the provider account. The run says so, in the summary line an unattended scheduled cycle produces: *"N stream(s) restored without a playable URL (it carried the provider's credentials)"*. To give the replica working streams, re-enter the provider credentials on its matching M3U account and refresh it; the replica then fetches the provider's real URLs itself. To start it off that way instead, do the first migration with an encrypted backup, which is the one path that carries credentials on purpose. Streams from a provider whose URLs carry no credential are unaffected.

- **A compromised MCP sidecar deployed with the updated Compose definition can no longer reach any ECM secret except its own MCP credentials (bead `enhancedchannelmanager-04c0u.8`, build 0126).** The AI-facing sidecar used to mount ECM's entire `/config` volume read-only, which put `settings.json`, `auth_settings.json`, the audit journal, TLS private keys, and every stored backup one file read away from the process most exposed to prompt injection. It now mounts only a dedicated credential volume that ECM writes and that holds nothing but MCP key material: the public client key operators paste into Claude Desktop or Claude Code, and the separate private projection carrying the sidecar's backend principal key and its destructive-confirmation signing key. All three remain distinct, independently generated credentials, written owner-only, and readable by the sidecar solely because it runs under the same `PUID`/`PGID` as ECM. The sidecar container now also runs with a read-only root filesystem, every Linux capability dropped, `no-new-privileges` set, a bounded `noexec`/`nosuid`/`nodev` `/tmp`, and process, memory, and CPU limits. Key rotation still takes effect on the next request without a restart, and a superseded key stops working immediately. CI builds both images and asserts all of this end to end on every run: a real `docker compose` bring-up on brand-new volumes, ECM writing the projection, the sidecar reading it, an authenticated tool listing, and live rotation. It runs twice: once at the default identity, once at a non-default `PUID`/`PGID`.

  A first-run deployment needs no manual privilege or ownership step: ECM's entrypoint creates and takes ownership of the projection directory while it still has root, for any `PUID`/`PGID` pair and for a bind mount as well as a named volume, and reports a named preflight error if it cannot. A projection directory ECM cannot write now degrades the MCP sidecar rather than stopping ECM from starting.

  **Operators using the MCP sidecar must update their Compose file, not only recreate the container.** Building from source, `docker compose -f docker-compose.yml -f docker-compose.mcp.yml up -d` carries the change. Running the published images, add the `ecm-mcp-secrets` volume and `MCP_SECRETS_DIR=/run/secrets/ecm-mcp` to **both** services as shown in the README quickstart. Recreating alone leaves the sidecar pointed at an empty directory, reporting `api_key_status: file_not_found` permanently, because the sidecar image now defaults `MCP_SECRETS_DIR` and no longer falls back to `/config`. Once the volume is in place, saving ECM Settings once, or regenerating the key under **Settings > MCP Integration**, publishes the projection. `PUID` and `PGID` must be identical for the `ecm` and `ecm-mcp` services, as they already had to be.

  Two things this does not do. Without the MCP overlay, `MCP_SECRETS_DIR` falls back to `CONFIG_DIR` so a newer backend keeps working under an older Compose file. That means every deployment writes `api-key` and `mcp-service.json` into `CONFIG_DIR`, and operators who bind-mount a host directory there should exclude `api-key` from sweep-everything backup tools. And the upgrade leaves the previous `<CONFIG_DIR>/mcp-service.json` in place; it authenticates nothing once the projection has moved, but ECM logs a warning naming it on every start rather than deleting credential material on your behalf. Both are documented under *Where the MCP credentials live* in the MCP integration guide.

- **Every destructive MCP tool now requires a mutation-free preview and a short-lived, content-bound confirmation (bead `enhancedchannelmanager-04c0u.7`, build 0123).** The live MCP registry is exhaustively classified as read-only, mutating, or destructive and carries matching MCP annotations. Registration fails closed when a tool is added without a classification. Destructive and bulk operations refuse to enter their handler on the first call, return a preview token bound to the exact tool and arguments, reject expired tokens or input drift, and retain a 500-item hard cap for list-shaped batches. Existing backend-resolved bulk confirmations now use expiring HMAC tokens as well, so a caller cannot fabricate or indefinitely replay a confirmation from a prior target set.

- **The MCP key is now a limited service credential instead of an unrestricted ECM administrator (bead `enhancedchannelmanager-04c0u.4`, build 0122).** Every backend route declared by the MCP sidecar now has an explicit method-and-route authority verdict, and an unknown or newly added route fails closed for the MCP service principal. Normal channel, stream, rule, statistics, and settings automation remains available. Human identity and authorization administration, TLS and private-key lifecycle, MCP-key rotation, all backup artifact creation, listing, download, deletion and restore operations, outbound connection tests, and cloud, sync, M3U, and EPG destination or credential changes now reject the MCP key with `403` and require a signed-in human administrator. The same restriction applies to starting, cancelling, or scheduling DBAS backup, restore, and cross-instance sync tasks; this prevents alternate parameter types or future task parameters from bypassing the human approval required for credentials, outbound upload, or retention pruning. These checks run in ECM's backend before route handlers, so a caller using the key directly against `/api/*` cannot bypass sidecar confirmations or tool descriptions. Human JWT administrators are unchanged, and auth-disabled first-run and headless behavior retains the existing setup-mode rules.

- **The administrator-equivalent MCP credential no longer travels in URLs or through an accidentally public default listener (bead `enhancedchannelmanager-04c0u.3`, build 0121).** MCP clients now authenticate only with an `Authorization: Bearer` header; a legacy `?api_key=` request is rejected even when its value is correct. The repository's Claude Code example, the Settings-generated Claude Desktop and Claude Code snippets, and the operator and release documentation carry environment-backed header references instead of a key in a URL, generated configuration value, shell history, or process argument. The executable and image bind to loopback by default, while the standard Compose overlay explicitly publishes port 6101 on host loopback only. Access from another machine is an explicit overlay that requires an HTTPS reverse proxy, an exact Host allowlist, and a configured trusted-proxy address; non-HTTPS MCP traffic is refused. Browser Origin values are checked against an exact allowlist with no wildcard, userinfo, path, query, or fragment, and the MCP SDK repeats the Host and Origin checks at its own transport boundary. Uvicorn's raw request-target access log is disabled because it includes query strings; ECM replaces it with a bounded log containing only a fixed method class, fixed route class (`health`, `mcp`, or `other`), and status. Trusted-proxy configuration rejects trust-all networks and malformed entries at startup, and production-path tests prove forwarded HTTPS is honored only from an explicitly trusted proxy. Rotation and revocation still take effect on the next request without restarting the sidecar because the key is re-read for every request. Operators upgrading from a query-string configuration should remove the query, configure the Bearer header, and regenerate the old key because request URLs may have been retained by client history or older access logs.

- **Stream previews and probes now enforce ECM's outbound-address policy before connecting (bead `enhancedchannelmanager-04c0u.6`, build 0120).** Passthrough previews and bitrate measurement resolve each HTTP destination once, reject the request if any DNS answer is forbidden, connect to the validated IP while preserving the original HTTP Host and TLS SNI name, and independently validate every redirect through a five-hop cap. Redirects cannot downgrade HTTPS to HTTP, and a redirect to another origin does not receive the Dispatcharr authorization header. Literal, DNS, mixed-answer, redirect, and request-time rebinding cases reject metadata, link-local, multicast, reserved, and policy-disallowed LAN destinations; `lan_friendly` continues to permit configured RFC1918 IPTV and Dispatcharr targets. FFmpeg and ffprobe receive no provider HTTP(S) URL: ECM validates the initial chain before spawn, gives the subprocess an opaque URL on an ephemeral pinned loopback relay, rewrites HLS playlists, segments, and AES-128 key URLs through the same relay, and strips authorization on cross-origin resources. HTTP(S) therefore has no subprocess-owned provider DNS or redirect residual. Direct UDP, RTP, and RTMP inputs remain compatible and are revalidated before every spawn and retry; only those direct transports retain a short DNS validation-to-connect race.

- **Published MCP images now have dependency and container vulnerability gates (bead `enhancedchannelmanager-04c0u.5`, build 0119).** MCP's locked Python dependencies were refreshed within the compatible 1.x SDK line, removing the 23 advisories found by the new audit. The runtime moved from Debian slim, whose current image retained 14 unfixed Critical/High OS findings after upgrade, to a digest-pinned Alpine base that produced zero Critical/High findings in the built image; available Alpine security updates are applied during each build. Publication requires the MCP dependency audit and blocks the multi-architecture manifest until both the AMD64 and ARM64 image digests pass Trivy with no Critical or High findings, fixed or unfixed; no vulnerability waiver or global suppression is present. A deliberately vulnerable fixture proves the dependency gate rejects known-vulnerable input. Third-party actions on the publication path and Docker base/helper images are pinned to immutable revisions, the production frontend build installs from its lockfile with `npm ci`, and the release-cut gate verifies the downloaded `bd` archive against its pinned SHA-256 digest before extraction.

- **Password recovery now has bounded work and closes existing sessions after a reset (bead `enhancedchannelmanager-04c0u.2`, build 0118).** Both public recovery endpoints have per-client request limits, while database-backed account limits prevent distributed callers from repeatedly generating email or password-hash work for one account. Requesting a reset retains one unused token per local account, suppresses repeat email for five minutes, and removes expired, consumed, or superseded rows. Reset links use an indexed SHA-256 token digest instead of serially bcrypt-checking every unused row, and each link permits ten password-validation attempts during its one-hour lifetime. Successful reset atomically consumes the link, changes the password, invalidates previously issued access tokens, and revokes every refresh session for that account; two callers racing the same link cannot both succeed. Existing access tokens remain usable during the upgrade itself and are invalidated only after their account completes a password reset. The public forgot-password response remains identical for known and unknown addresses. Reset links issued before this upgrade used the retired bcrypt representation and must be requested again.

- **Legacy backup restores now bound both upload and decompression work (bead `enhancedchannelmanager-9kwzp.3`, build 0117).** The authenticated and first-run legacy restore endpoints stream uploads to an owner-only temporary file through the existing 2 GiB hard cap; normal error exits and cancellation make a best-effort removal of a partial file. Before reading ZIP members, they reject excessive entry counts, per-member or cumulative declared expansion, and excessive compression ratios. The 100× ratio applies generally; only legacy `journal.db` and `m3u_uploads/` members may use up to 1000×, preserving ECM-produced 292×–650× data while rejecting the demonstrated ~1028× journal bomb. Validating the embedded database reads only its 16-byte header.

- **Blocked the malformed-Host MCP authentication bypass (bead enhancedchannelmanager-04c0u.1, build 0116).** The MCP image now carries Starlette 1.6.0 (CVE-2026-48710 is fixed from 1.0.1), the public `/health` exemption is decided from Uvicorn's raw ASGI path instead of Host-derived `request.url.path`, and both the outer app and MCP transport enforce a fail-closed Host allowlist. Defaults cover loopback and the canonical `ecm-mcp` Compose name; LAN hostnames/IPs are explicit via `MCP_ALLOWED_HOSTS`. Raw TCP Uvicorn/h11 regression coverage proves a poisoned Host request to routed `/mcp` returns 401 without reaching downstream tool dispatch, while valid-host `/health` stays public. Operators upgrading from an affected build must regenerate the MCP key and update clients; no production key is rotated automatically. (bead enhancedchannelmanager-04c0u.1)

- **A standard backup now replaces recognized provider credential fields and credential-bearing URL values, and carries none of your ECM accounts (bead `enhancedchannelmanager-gi4zn`, build 0113).** The default, unencrypted backup was described to you simply as "redacted", and that word was doing more work than the code was. A round-trip drill found an Xtream Codes account's **username** sitting in clear beside a correctly redacted password, in both places it appears. For that kind of provider the username is half the credential pair and the half that names your subscription, so an artifact carrying it was not a redacted artifact. Checking a real backup against the corrected rule found five more instances of the same shape (EPG source usernames, the core-settings `username` beside its redacted password, `smtp_user` beside `smtp_password`, and the SMTP username and Telegram chat ID inside alert methods) plus three whole bearer credentials that had never matched anything: your **Emby and Jellyfin API keys and your Plex token**, all exported verbatim in every standard backup. Credentials hidden inside URLs are now caught too, by looking at the value rather than the field name, since no list of credential-sounding field names can see a password in a `get.php?username=...&password=...` query string. **Then an external security review found worse.** The backup's copy of ECM's own database carried your `users` table: your admin account's bcrypt **password hash**, offline-crackable at an attacker's leisure, with the username and email address to crack it against, plus live session tokens, the IP address and browser you administer from, password-reset tokens, and any linked OIDC, SAML or LDAP identity. All of that structured account and credential data is gone from a standard backup now, along with your journal, your telemetry (which included Emby, Plex, Jellyfin and Dispatcharr account names, and viewer IP addresses), your cloud-storage and sync-target credential fields, and the M3U digest email recipient list. The database is compacted afterwards, so the removed rows are not recoverable from the file either, which was measured rather than assumed. The one deliberate exception is your **Dispatcharr** users' usernames, which are your own instance's accounts and are what the restore matches on. Operator-authored free text such as source names and rule notes may still travel verbatim and could contain a secret ECM cannot recognize; credential-free provider addresses also remain, so inspect that content before sharing an artifact. **The encrypted backup with Include credentials is unchanged**, and is still the way to migrate structured credentials and accounts between instances; note that a passphrase alone does not preserve those fields, **Include credentials** does. `docs/user_guide/backup-restore/backup-overview.md` now sets out exactly what a standard backup does and does not contain.

- **A backup that cannot redact itself now fails instead of quietly shipping everything (bead `enhancedchannelmanager-gi4zn`, build 0113).** Every error path in the redaction step used to fall back to including ECM's database untouched, behind a result that looked like success: an unopenable database, an unreadable table, and an alert method whose configuration would not parse were each shipped raw while everything around them was correctly redacted. All three now fail the whole backup and write no artifact, and the unredacted working copy is destroyed rather than left in the container's temp directory. **What this costs you:** a damaged or unreadable `/config/journal.db`, a full `/config` volume, or a read-only mount now produces a failed backup where it previously produced a backup you should not have trusted. [Take a backup](https://github.com/MotWakorb/enhancedchannelmanager/blob/main/docs/user_guide/backup-restore/take-a-backup.md) has the diagnosis steps, and the disaster-recovery runbook covers the case where the incident itself is what broke the database.

- **Connection tests are no longer anonymous on an auth-disabled instance that has an operator account (bead `enhancedchannelmanager-2u4e0`, build 0110).** `require_auth: false` is a supported ECM mode, and until now every one of the twelve **Test Connection** endpoints stayed wide open in it: the Dispatcharr, SMTP, Discord and Telegram tests, the Emby, Plex and Jellyfin test-connection routes, the alert-method and M3U-digest test sends, both cloud-target tests, and the DNS-provider test. Each of those reaches the network with credentials your instance has already stored, to a host the caller can often name, and reports the upstream verdict back, so anyone who could open a socket to the instance could spend a secret they never had to learn and read an in-band port scan off the reply. Inside a single router it was self-contradictory: `POST /api/tls/test-dns-provider` would exercise your stored DNS-provider credentials and enumerate your hosted zone for an anonymous caller, while `GET /api/tls/settings`, which shows those same credentials only *masked*, was refused. All twelve now require a real, signed-in admin whenever the instance already holds an operator identity, joining the three identity primitives closed under bead `enhancedchannelmanager-jy006`. **What this costs you:** on an auth-disabled instance that has a user account, a browser that is not signed in now gets `403` from every Test Connection button in Settings. Nothing is permanently lost and `require_auth` does not have to change: browse to `/login`, sign in, and they all work again. An instance that never created a user, the supported headless posture, is unaffected, because the no-identity carve-out still serves it anonymously. `docs/auth_middleware.md` carries the full rule, and a route-level test now asserts the whole enforced set so no member of the family can drift back out of it.

- **A successful password reset no longer writes the account's email address into the log, twice (bead `enhancedchannelmanager-5u5h9`, build 0110).** `POST /api/auth/forgot-password` logged `Password reset email sent to: <address>` in the request handler, and the SMTP helper it called logged the same message about the same event, so one reset put a subscriber's email address into ECM's log two times. That is the log operators paste into GitHub issues, which is the premise bead `enhancedchannelmanager-cb1e1` was filed on when it moved the *failure* branch of this same flow to a user id; the success branch was left alone at the time precisely because changing one line while the other still named the address would have been more confusing, not less. There is now one line for one event, `Password reset email sent for user_id=<id>`, matching the shape every other `[AUTH]` line in the file uses. The duplicate also overstated send counts for anyone reading that line as a metric, and that is fixed with it. The regression test drives the real handler all the way down to the bytes handed to `smtplib`, then asserts the address appears in none of the log records the request emitted, so a future line that leaks it somewhere else in the flow fails too.

- **The password-reset link's host is no longer chosen by whoever asked for the reset (bead `enhancedchannelmanager-qsqfv`, build 0109).** `POST /api/auth/forgot-password` built the emailed link's origin from the `X-Forwarded-Proto` and `X-Forwarded-Host` request headers, falling back to the request's own `Host` header. All three are supplied by the caller and nothing in ECM constrained them: no `TrustedHost` middleware, no allowlist. An unauthenticated caller who knew an operator's email address could therefore have ECM send that operator a genuine reset email, from ECM's own SMTP, carrying a live reset token in a link pointing at the caller's host; one click handed over the account. A new admin-only **Public Base URL** setting (Settings > Email) now supplies that origin. When it is set it is used verbatim and the request headers are ignored entirely. It is validated as a bare `scheme://host[:port]` origin, rejecting a missing scheme, a path, a query string, a fragment, embedded credentials, whitespace and a bad port, and normalized once on save so every consumer reads the same shape. When it is unset the old header-derived behaviour is preserved deliberately, so no existing install's reset email stops working on upgrade, and ECM logs a warning naming the setting and the exposure: once at startup, and once per process afterwards rather than on every request. Tests assert on the bytes handed to the SMTP layer, the message the account actually receives, for both the configured and the unset path.

- **A password-reset email that fails to send no longer writes the live reset token into ECM's log (bead `enhancedchannelmanager-cb1e1`, build 0107).** `POST /api/auth/forgot-password` mints a single-use reset token, stores only a bcrypt hash of it, and emails the account a link carrying the plaintext value. When that send failed, the handler logged the plaintext token next to the account's email address, on the reasoning that an operator might still want it for debugging. The effect was that an ordinary operational failure, an SMTP outage or a wrong SMTP password or a network blip, was enough to hand a working password-reset credential to anyone who can read the log, with no attacker involvement at any point; ECM logs get pasted into GitHub issues routinely, and this project has already imported two such issues containing log excerpts. The failure branch now records the user id and nothing else, in the `user_id=` shape the surrounding `[AUTH]` lines already use. No operator capability is lost with it: an admin can set any account's password directly through `PATCH /api/admin/users/{user_id}`, and `reset_password.py` does the same from the command line when the web UI is not reachable. A regression test drives the real handler down the send-failure branch and asserts that the token it minted appears in none of the log records the request emitted, rather than inspecting the arguments of one logging call, so a future line that leaks the same value somewhere else in the flow fails the test too.

- **A session no longer dies because a token-refresh response never arrived (beads `enhancedchannelmanager-upkp1`, `enhancedchannelmanager-yhk3r`, build 0106).** ECM rotates the refresh token on every call to `POST /api/auth/refresh`. The token being replaced used to stay usable for exactly ten seconds afterwards, which covered two browser tabs racing the same rotation but nothing slower than that. A client that never received the rotated response, because the tab navigated or the request was aborted mid-flight (browser automation does this constantly), was left holding a token the server had already replaced: fine for ten seconds, permanently locked out after, with no way back except logging in again. QA reproduced it in about fifteen seconds, the same request returning 200 at t+1s and 401 at t+13s with the delay as the only variable. The window is now gone rather than widened. The prior refresh token stays acceptable until its successor is actually used, which the server already knows without storing anything new: accepting a successor overwrites the predecessor's hash out of the session row in the same statement, so "valid until the successor is used" is literally "valid until it is overwritten". Presenting the prior token returns a fresh access-token cookie and no refresh cookie, never rotates, and never extends the session's expiry, so a client living on those answers is still sent back through an interactive login when the session's own seven-day lifetime runs out, while a healthy client that rotates keeps sliding forward. Exactly one generation is kept, so a token two rotations old is still refused; logout still kills the current token and its predecessor together; and the token's own signature and expiry are still checked before the database is consulted at all. **Accepted risk, recorded deliberately:** the superseded token's usable lifetime goes from ten seconds to the session's lifetime. An earlier draft of this note justified that by saying every realistic way to obtain the superseded token also yields the current one; a pre-merge review was right that this is not generally true, and the honest version is worth stating because it is what the decision rests on. Anything that reads the cookie jar *continuously* (malware, a shared machine, a backup that keeps being restored) always holds whatever token is current, which is valid for seven days and slides forward on every rotation, so the predecessor adds nothing there. A *one-time* snapshot is different: a jar copied in the moment between a client sending a refresh and that client using the result captures the superseded token without its successor, and under this change that copy stays usable until the successor is used or the session runs out, where before it died ten seconds later. That is a real increase for that one vector. It is accepted against a single-operator, LAN-facing deployment, bounded by the session's own expiry (which this path never extends) and revocable at any time by logging out, which kills the current token and its predecessor together; and it is weighed against a stranding bug that reproduces in fifteen seconds and leaves the operator with no way back except logging in again. The mitigations that were doing the real work (httpOnly, `path=/api/auth`, `samesite=lax`, signature and expiry before any lookup, no expiry extension, logout revoking on either hash) all stay. **A refresh that is refused now says so.** The endpoint previously logged nothing on any path, success or failure: 38 hours of container logs held zero refresh lines, which is why the stranding bug had to be diagnosed from a browser's error body instead of from the server that produced the 401. Every refusal now logs one WARNING carrying a distinct reason code, the client address, a truncated user agent, and the token's `jti` and subject, never the token itself and never its hash. **That last guarantee is about the line, not about the fields ECM picked.** A pre-merge review found the first version of it defeated by the two values the caller supplies: `X-Forwarded-For` was written into the log exactly as sent, with no length limit and no check that it was even an address, and `User-Agent` was cut at eighty characters, which a sha256 hex digest fits inside whole. Anyone who could reach the endpoint, which is to say anyone, could therefore put a refresh token or its hash into ECM's auth log themselves, including into the login-failure lines, which had used the same unchecked header since long before this change. The forwarded address is now parsed as an IP address and re-rendered from the parsed value, so what reaches the log is an address ECM produced or the socket's own peer, never a substring of a header; a header that is not an address is discarded rather than trimmed, because trimming still prints whatever the caller chose. The user agent, which has no shape to check it against, has secret-shaped runs redacted and is reduced to printable ASCII before it is truncated, so it can carry neither a credential nor a line break that would forge a second log entry. The test that missed this asserted the negative over cookie-derived tokens only and now asserts it over both headers on every refresh outcome. The case worth watching, a valid signature presented against no live session, gets a message of its own, because with one generation retained that is the closest thing to a replayed-token signal the server can emit. Since the endpoint is unauthenticated and unrate-limited, repeats are deduped per reason and client for a minute and the skipped count is reported on the next line rather than dropped. An unknown token is refused and logged, never answered with a revocation: one session row is the entire token chain here, so revoking on suspicion would log the operator out of every tab from an endpoint anyone can POST to, and the false-positive cases (a restored tab, a second device from one cookie jar, a copied cookie in a script) are all legitimate and all more likely than the theft it would catch.

- **A rejected form on any ECM endpoint no longer prints what you typed into the log or hands it back in the error (bead `enhancedchannelmanager-9kwzp`, build 0101).** When a request fails validation, ECM returns HTTP 422 and logs the failure. That handler used to log the raw request body, pydantic's per-error `input` echo and `exc.body` at ERROR, and repeat the body in the response, for every path outside `/api/auth` and `/api/tls`. Those two prefixes were the ones an earlier fix had evidence for. A pre-merge review named four more routes carrying secrets in their bodies: `POST /api/settings` (Dispatcharr password and API key, SMTP credentials, Discord webhook, Telegram bot token), `POST`/`PATCH /api/cloud-targets` and `POST`/`PUT /api/sync-targets` (arbitrary `credentials` mappings), and `POST`/`PATCH /api/admin/users` (a plaintext password). Walking the running app for write routes whose request models hold credential-shaped fields put the real figure at sixteen route/model pairs, adding the SMTP, Discord, Telegram, Emby, Plex and Jellyfin connection-test routes and the EPG guide-migration route. Adding sixteen prefixes to the list would have restored exactly the situation that produced them, because an allowlist of credential-bearing paths defaults to leaking for every path nobody remembers to add, silently and with the tests still green. Redacting by field name instead has the same defect one layer down, since a credential whose name nobody anticipated defaults to leaking again. So the list is gone: request-body values are now withheld on every path, with nothing to maintain and nothing to forget. A 422 is not less useful for it. Every validation error still reports which field was wrong, what was wrong with it and the error type, on every route; the log line still reports the body's size and content type, so an empty body is still distinguishable from an unparseable one. The only thing withheld is the echo of the value itself, which for a missing-field error was the whole request body anyway. The test that let this through asserted that the prefix tuple contained the two prefixes, which stayed true while sixteen routes leaked. It is replaced by one that walks the live app for credential-bearing request models and runs the real handler against a real validation failure of each.

- **A failed TLS certificate renewal, and a rejected TLS settings form, no longer write your DNS-provider credentials into the application log (bead `enhancedchannelmanager-2owpi`, build 0101).** `/config/tls_settings.json` holds the DNS-01 ACME credentials in clear so ECM can renew unattended. Bead `2owpi` was filed over that and recalibrated: the file is already mode 0600, is not in git, and is not swept into backups, and encrypting it would put the decryption key in the same directory, so the PO declined encryption and scoped this to the exposure paths nobody had actually checked. Two of them were real. First, `main.py`'s validation-error handler logged the raw request body, `exc.body` and pydantic's per-error `input` echo for every path outside `/api/auth`, and echoed them back in the 422. `POST /api/tls/configure` and `POST /api/tls/test-dns-provider` both take `dns_api_token`, `aws_access_key_id` and `aws_secret_access_key` in the body, so any TLS settings save rejected for an unrelated reason, a bad port or a missing field, printed live credentials at ERROR into the log operators paste into GitHub issues. Redaction was first scoped to a `CREDENTIAL_BEARING_BODY_PREFIXES` constant covering `/api/auth` and `/api/tls`, and it strips the per-error `input` too, because for a missing-field error that value is the entire body. That constant no longer exists; the entry below on bead `enhancedchannelmanager-9kwzp` explains what replaced it and why, and it is the behaviour that ships. Second, an operator-supplied Cloudflare token containing a character illegal in an HTTP header value, a trailing newline being the shape a copy-paste produces, made the HTTP stack raise an exception whose text is `Illegal header value b'Bearer <the whole token>'`. That string was logged verbatim by `POST /api/tls/test-dns-provider`, and on the renewal path it was stored into `last_renewal_error`, which is persisted back into `tls_settings.json` and served by `GET /api/tls/status`, the weakest-gated route in the TLS router. Every error string the DNS providers, the renewal routine and the TLS router derive from an exception now goes through `tls/redaction.py`, which deletes the credential values it is holding by identity before running the existing `redact_secrets` pattern sweep, so redaction no longer depends on guessing a token's charset. `GET /api/tls/settings` was audited and was already correct: all three credential fields are masked to their last four characters, which is unchanged. **Audited clean:** no other route in the router, and no other router, returns a stored TLS credential. **Operator note:** if a renewal has failed on your instance since before this change, the stored error text is redacted on the way out, but treat any credential that appeared in a log you have shared as exposed and rotate it.

- **The credential redactor that keeps secrets out of ECM's logs is now named `redact_secrets`, and the rename is what unblocked the CodeQL security gate on this branch (bead `enhancedchannelmanager-9kwzp`, build 0103).** The TLS credential fix above routes every error string through `tls/redaction.py`, which deletes known credential values by identity and then runs the shared pattern sweep from `cloud_storage/upload_security.py`. That put the sweep's output directly into six log calls, and CodeQL's `py/clear-text-logging-sensitive-data` rule promptly reported all six as HIGH: it decides what is "sensitive data" from names, so a function called `mask_secrets` was assumed to RETURN a secret, and logging its return value read as logging a credential in clear. The alerts were false. The analysis SARIF shows one data-flow path per alert and every path starts at the masking call itself, not at a credential; driving all six log calls with synthetic credentials emits only `***REDACTED***`. Rather than suppress the rule, the sweep now carries a name CodeQL already understands: its heuristic subtracts a list of terms meaning "this data has been rendered non-sensitive", and `redact` is one of them, which is why the neighbouring `redact_secret_values` was never flagged. The rename is behaviour-preserving, it teaches the scanner something true rather than hiding a finding from it, and it keeps working for code written later. `docs/security/codeql-config.md` now documents the technique, and how to tell a name-caused alert from a real one, so the next false positive of this shape does not get suppressed by reflex. **Operator note:** nothing changes at runtime; no log line, API response or stored value differs.

- **`tls_settings.json` file permissions are now probed at every container start, alongside the cloud-backup encryption key (bead `enhancedchannelmanager-2owpi`, extending bead `enhancedchannelmanager-m40pn`, build 0101).** ECM chmods this file to 0600 on every write, so drift comes from outside: a manual edit, a restore, a volume copied without permissions. Nothing noticed. The startup probe `m40pn` built for the Fernet key now has a sibling for the file holding the DNS-01 provider credentials, called from the same startup block. Mode drift is repaired to 0600 and reported at WARNING with `tls_settings_status=mode_repaired`; a repair that fails, or a foreign owner on a non-root process that cannot take ownership, is an ERROR. Ownership is advisory under root, matching `m40pn`, because root reads any file regardless of owner and mode is the control that actually holds. The probe reads metadata only, never the file's contents, and never raises: a TLS configuration permission problem must not take down channel management.

- **Backup artifacts no longer carry the Discord webhook URL or the Telegram chat id, and the two redaction lists that had to agree about that are now one list (bead `enhancedchannelmanager-9kwzp.9`, build 0101).** Bead `9ej7f` made `GET /api/settings` withhold `discord_webhook_url`, `telegram_bot_token` and `telegram_chat_id` from every caller it classifies as non-admin, which deliberately includes the static MCP service principal. The backup read path defeated two thirds of that. `GET /api/backup/create`, `GET /api/backup/export` and `GET /api/backup/saved/{filename}` carry `RequireAdminIfEnabled`, which ADMITS that principal, and the artifact's redaction denylist was a separate hand-maintained tuple that happened to list the bot token and not the other two. The MCP key just refused those values on the settings endpoint could read them out of a standard backup instead. The fix is not the two missing strings. Those alone would leave the class open, so that the next field added to the settings partition would leak the same way. There is now exactly one definition, `config.ADMIN_ONLY_READ_REDACTED_FIELDS`; the settings router and the backup artifact producer both derive from it, a test fails if they ever diverge, and the debug-bundle redactor's private "these two aren't credential-class in the backup contract" extension is gone because that statement was the defect, not a distinction. **Operator note:** a plain (non-passphrase) backup now restores without your Discord webhook URL and Telegram chat id, exactly as it already behaved for the Telegram bot token. The restore preserves whatever the destination already had rather than overwriting it, and reports the fields as needing re-entry. The opt-in passphrase-encrypted artifact still carries them.

- **`POST /api/backup/restore-initial` is now tested against real signed tokens, and its silent dependency on the SQLite pool class is recorded and pinned (bead `enhancedchannelmanager-9kwzp.5`, build 0101).** Two findings from the security review of the `lf29s` branch. First: every existing test of the restore-initial identity gate patched `get_current_user` and `get_token_from_request`, so no test ever presented a real token, and a refactor reading `is_admin` out of the JWT body instead of the users row would have shipped green through the whole suite, which is a privilege escalation to full instance takeover. Thirteen non-mocked cases now mint real tokens via `auth.tokens.create_access_token` and drive the endpoint over a real `Authorization` header, including a differential pair that mints one token and flips only the database row, and a validly-signed token that asserts `is_admin: true` in its own payload and is refused. Second: the endpoint holds an open SQLite read transaction across the restore's `close_db()` → file-overwrite → `init_db()` sequence, which is safe only because the engine uses `poolclass=StaticPool`. Under SQLAlchemy's default `QueuePool` the review reproduced a restore that returns `200` with `integrity_check=ok` while silently reverting to the pre-restore data. That coupling is now documented at both ends and pinned by a test whose failure message points at restore-initial, so moving off `StaticPool` for a concurrency reason cannot quietly break restore.

- **`require_auth: false` now refuses three identity primitives, and what it still permits is written down.** Beads `enhancedchannelmanager-jy006` and `enhancedchannelmanager-ne2yy` (build 0101). `require_auth: false` is a supported ECM operating mode, but what it should still refuse had never been decided, so every admin gate short-circuited to "anonymous is fine" and an anonymous LAN caller on such an instance could replace the entire database, plant a persistent admin-equivalent `mcp_api_key`, and install their own TLS private key. Three surfaces now require an authenticated human admin even in that mode: `POST /api/backup/restore-initial`, `POST`/`DELETE /api/settings/mcp-api-key`, and the ten `/api/tls` certificate/key-material and HTTPS-lifecycle routes. The line is not how destructive a route is. `POST /api/settings` and `POST /api/backup/restore` stay open and both do real damage. The line is durability of the resulting identity: each of the three leaves the caller holding a credential or key that keeps working after authentication is switched back on. Everything else stays open in that mode and is now documented, warnings and known residuals included, in `docs/auth_middleware.md` → "What `require_auth: false` permits". The mechanism is a new `enforce_when_auth_disabled` option on `auth.dependencies.require_admin_if_enabled`, carried by `RequireHumanAdminForServiceCredential` and `RequireHumanAdminForTLSMaterial`; `restore-initial` applies the same rule in its handler because it must also survive a damaged `setup_complete`. All three now share one ownership predicate, `auth.dependencies.instance_has_operator_identity`, which `routers/backup.py` had held a private copy of. **No lockout:** an instance with no operator identity at all (a genuine first run, or a headless auth-disabled deployment that never created a user) still reaches all three anonymously, which is the carve-out bead `lf29s` already shipped and this change reuses rather than reinvents. **Operator note:** browse to `/login` and sign in to use the TLS or MCP settings sections on such an instance; bead `enhancedchannelmanager-p388h` makes that route reachable while `require_auth` is false. Separately, `AUTH_EXEMPT_PATHS`, the entire authentication gate for `/api/*`, is now pinned by a snapshot test whose failure message is the reviewer's checklist, so adding a data route to it can no longer be a one-line, silent, total-exposure change that ships green.

- **The first-run password hint now states the policy ECM enforces, instead of the composition rules it deliberately dropped (bead `enhancedchannelmanager-mkocf`, build 0101).** The setup form, the password-reset form and the change-password modal all promised "At least 8 characters with uppercase, lowercase, and number". `backend/auth/password.py` requires none of that and says so in its own module docstring: ECM follows NIST 800-63B, which recommends against composition rules because they push people toward predictable substitutions such as `Password1!` while adding little entropy. So the first screen a new operator ever sees was telling them to do the thing the policy had deliberately stopped requiring, and it withheld the two rules that actually reject a password, which they could otherwise discover only by being refused. All three surfaces now read "At least 8 characters. Common and previously breached passwords are rejected, and it cannot contain your username", which is exactly what `validate_password()` checks. `docs/user_guide/getting-started/first-run.md` no longer has to warn readers that the on-screen hint is wrong.

- **Operator-shell settings-navigation specs are repointed at the navigation the shell renders, and two specs are added (build 0100).** Four statements in `e2e/operator-shell.spec.ts` still targeted a `Channel default settings` link in the Channel Manager page header. That link was removed with Channel Manager's `settingsLinks` entry (bead `enhancedchannelmanager-mer2o`), so three specs had been failing against a control that no longer exists. No product code changed. `contextual settings links use stable current hashes` now covers the two routes that still render the header's `Related settings` nav, Channel Pipeline and M3U Changes, pinning rendered label, Settings hash, exactly one link per route, and the resulting page heading. The two edit-mode specs move to the primary rail, whose Settings destination drills the rail into its sections and defers navigation while the exit guard is pending; each keeps asserting the exact landing page rather than a bare `#settings`. Two specs are added for arcs that moved off the contextual link. `a deep settings destination exits an emptied edit session without prompting` covers the `exit-and-navigate` decision carrying a settings page, reached by staging a change, letting the guard block a Settings activation, then undoing back to nothing staged. `Keep Editing cancels a deep settings request and leaves the rail drilled in` covers cancelling a pending route that carries a settings page, and pins the post-cancellation rail state: still the Settings sections rather than the primary destinations, with General current because the route never moved. Each spec was proven red without the behavior it claims, by four mutations: `getGuardedRouteDecision` forced to always navigate, Channel Pipeline's contextual link dropped, the settings page dropped from the `exit-and-navigate` branch, and the confirm branch skipped for deep requests.
- **The JWT issuer now honors the configured token lifetimes it had been ignoring, and a refreshed access token stops claiming to belong to `user_<id>` (bead `enhancedchannelmanager-suuoh`, build 0101).** `jwt.access_token_expire_minutes` and `jwt.refresh_token_expire_days` in `auth_settings.json` were already read by three places: the `access_token` and `refresh_token` cookies' `max_age`, the `UserSession.expires_at` written at login and at every rotation, and the `access_token_expires_in` metadata the frontend schedules its proactive refresh from. `create_access_token()` and `create_refresh_token()` read neither, baking in the module constants `ACCESS_TOKEN_EXPIRE_MINUTES = 30` and `REFRESH_TOKEN_EXPIRE_DAYS = 7` instead, so an operator who edited either value got a half-applied change with no warning. Both directions of the refresh mismatch are user-visible. A configured lifetime SHORTER than seven days put `UserSession.expires_at` ahead of the JWT's own `exp`, so `_cleanup_expired_sessions` deleted the row while the cookie still decoded, and the next `POST /api/auth/refresh` answered `401 {"detail":"Session not found or revoked"}` against a token the client had every reason to believe was good. A configured lifetime LONGER than seven days did the reverse, logging the operator out at day seven while the cookie and the settings file both advertised more. Both issuers now read the configured values, falling back to the constants under exactly the conditions `_get_secret_key()` already tolerates (settings module unavailable, or the config directory unreadable) and logging when they do. Separately, `rotate_refresh_token()` minted the post-refresh access token with a fabricated `username` claim of `user_<subject id>`, because it never loaded the user row. That claim is not an authorization input, since `get_current_user()` resolves the caller from `sub`, but `main.py` logs it verbatim as the acting operator in its deprecated-admin-router warning, so every request made after a token refresh was attributed to `user_2` rather than to the account. The refresh handler already holds the real `User`, and now passes the name through. **Operator note:** on a default install nothing changes, because the configured values are the same 30 minutes and 7 days the constants held. The fix matters the moment either value is edited.

- **Agent instructions gain a verification-discipline section, and a wrong claim about stale-base merges is corrected (build 0099).** A retrospective on a run of agent errors found that every judgment error in the session shared one shape: a check ran against something adjacent to the claim, and the claim got reported anyway. `CLAUDE.md` now carries four rules against that. First, smoke-test an instrument on a known-good and a known-bad input before trusting it, since a monitor that fails silently turns absence of evidence into apparent evidence of absence; four monitors failed that way in one session, one of them using a `gh pr checks --json` flag that does not exist. Second, report the check that ran rather than the conclusion drawn, the rule `scripts/check_publish.py` already applies to its own PASS line. Third, never close a bead on the claim that another bead covers it without opening that bead. Fourth, treat your own verification as needing verification too, and prefer uncurated external adversarial review of your judgment, not only of your code. The same change corrects the stale-base bullet in the worktree section, which had claimed a stale-base branch "reverts everything dev gained since the stale base, on merge." That is wrong: a three-way merge takes the stale commit as its merge base and preserves target-side work, confirmed by direct experiment in a throwaway repository. The real hazard is a branch written against an old API surface, whose gates passed against a tree it will not land in, so the corrected text now describes a quietly incomplete fix rather than mass reversion. The standing rule to verify the merge-base before merging is unchanged.

- **`docs/shipping.md` corrected to eight required status checks on `dev`, closing three spots a prior pass missed (build 0098).** `Operator Docs` became the eighth required context shortly before this build; a previous documentation pass fixed `docs/testing.md` and part of `docs/shipping.md` but left three places in `docs/shipping.md` naming the numeral 7 instead of the word: the branch-protection summary, the "one job per name" invariant, and a shell comment above the `gh pr view` polling command, whose jq check-name list was also still missing `Operator Docs` as an entry. A repo-wide sweep for both spellings found two more genuine hits outside documentation scope, `scripts/ci_junit_summary.py` and `scripts/classify_changed_paths.py`, both docstrings, and one in `.github/workflows/build.yml`; all three are code or workflow files and are filed as a follow-up rather than edited here. That follow-up landed the same build: `ci_junit_summary.py` now says six of the eight (the numerator was never seven's, only the denominator moved, since `Operator Docs` runs its six gates on every pull request rather than gating on the changed-path classifier), `build.yml`'s ownership comment now reads two of the eight, and `classify_changed_paths.py` now states `dev` and `main` separately rather than incrementing a shared number, because live branch protection shows `main` requiring a narrower four contexts, not seven or eight.

- **`Operator Docs` is pinned as a branch-protection required context before it becomes one, so the edit that would wedge every pull request fails a test instead (build 0097).** The job hosts six gates: `npm run docs:check`, the em-dash ratchet, the personal-identifier and ECM-credential ratchet, the generic detect-secrets ratchet, the added binary-artifact ratchet, and `mkdocs build --strict`. Four of those are security ratchets and none of them could block a merge, so a failure showed up only as a permanent `UNSTABLE` that a shipper had to notice and refuse to merge past. Two consecutive pull requests on the same day proved the exposure: one tripped the secrets scanner on synthetic credentials, one carried a dead cross-file docs anchor, and both were mergeable. **The reason this needed a test first rather than just a protection change**: branch protection matches a required check by the DISPLAYED job name, and GitHub reports no conclusion at all for a job it skips. A required name that nothing emits therefore does not fail one pull request, it wedges every open one, and protection on `dev` is classic with `enforce_admins=true`, so there is no admin bypass to unstick it. `Operator Docs` is safe to require today because it has no job-level `if:`, no path filter, and only step-level `if: github.event_name == 'pull_request'` conditions on the ratchets, which skip a STEP and leave the JOB reporting. Nothing stopped a later edit from taking that away. **What the contract now refuses**, in `backend/tests/unit/test_classify_changed_paths.py::TestOperatorDocsRequiredContext`: any job-level key outside an allowlist of keys that provably cannot skip, rename, or hollow the job, which covers `if:`, `needs:`, `paths:`, `strategy:` (a matrix renames the context to `Operator Docs (<value>)`, exactly as it does for `CodeQL Analysis` in `build.yml`), a reusable-workflow `uses:` (reports as `Operator Docs / <inner job>`), and `continue-on-error:`; a change to the job key or the displayed `name:`, both pinned because they fail differently and deleting `name:` orphans the context just as surely as renaming it; and any of the six gates ceasing to be invoked by this job, whether it was deleted or moved somewhere that is not the required context, which is the same outcome for a merge gate. Each gate is also checked to be able to fail the job: no `continue-on-error`, and the only step condition permitted is the pull-request-only one the ratchets need because a push has no merge base to diff against. **The gate list is half derived, not fully hardcoded.** The job's own run summary reports one line per gate from that step's `outcome`, so the workflow states its own inventory; the test asserts that inventory equals the pinned table, which means a seventh gate cannot be wired in, and an existing one cannot be dropped, without a deliberate edit recording whether the required check is now trusted to enforce it. `Operator Docs` is also registered in the existing `TestWorkflowContract` required-context list, which independently forbids a second job emitting the same name. **Every failure mode was demonstrated red before the change was accepted**: an added job-level `if:`, `paths:` and `strategy:`; a renamed display name, a renamed job key and a deleted job; a deleted gate, a gate moved into `Backend Tests`, a gate neutered with `continue-on-error`, a narrowed step condition, and an unregistered seventh gate. Nothing the six gates actually do was changed. The `operator-docs` job in `.github/workflows/test.yml` now carries the constraint as a comment, so an editor meets it in the file they are editing rather than in a test failure.
- **Alert-method reads no longer return the Discord webhook URL, the Telegram bot token or the SMTP password, which they had been returning in plaintext to every permitted caller (bead enhancedchannelmanager-9kwzp.13, build 0096).** `GET /api/alert-methods` and `GET /api/alert-methods/{id}` each hand-rolled their response dictionary and put `json.loads(m.config)` into it verbatim, so the whole credential blob went out unmodified. The masking to prevent that already existed and had existed all along: `AlertMethod.to_dict(include_sensitive=False)` in `backend/models.py` substitutes `********` for `password`, `bot_token`, `webhook_url` and `api_key`, and its docstring says "By default, sensitive config values are masked." Neither handler called it. Both do now, and that is the entire behavioural change: `to_dict` is the single masking implementation, reused rather than reimplemented, because a second copy is exactly how the backup redaction denylist and the model's key set would drift apart. **This is what makes the previous build's decision safe.** Build 0095 admitted the MCP service principal to `GET /api/alert-methods` on the PO's reversal, so the shipped `list_alert_methods` tool would keep working, and recorded the plaintext disclosure as an accepted open residual rated P1. That residual is now closed at the response rather than at the gate, so the tool keeps working and the automation credential no longer reads a credential value through it. Every authorization verdict from build 0095 is unchanged. **What a read now discloses, so the fix is not overread**: the id, name, type, enabled flag, severity filters, alert-source filter, timestamps, and every config key that is not in the masking set. For a Telegram method that includes the destination `chat_id`, which is not a credential (posting to it still requires the masked `bot_token`) but is also not nothing, and note bead 9ej7f does withhold `telegram_chat_id` from this same principal on `GET /api/settings`; the two surfaces therefore still disagree about that one field, which is recorded here rather than quietly widened. For an SMTP method it includes the recipient list, which the Settings tab needs to populate the Email alert recipients field. A stored credential that is empty reads back as `null` instead of `********`, preserving the difference between "configured, value withheld" and "not configured". **No caller receives the unmasked form over HTTP**, and that is a deliberate verdict rather than an omission: there is no route, query parameter, header or caller tier that reaches it, none was added, and a parsed source-level assertion over the whole router package now fails if one appears. The legitimate need to prove a credential works is served by `POST /api/alert-methods/{id}/test`, which sends with the stored value without disclosing it. **The other handlers in the router were checked rather than assumed**, since the bead named only the two reads. `POST` returns the abbreviated `{id, name, method_type, enabled}` and never carried `config`; `PATCH` and `DELETE` return `{"success": true}`; `POST /{id}/test` returns the upstream verdict as `{success, message}`; `GET /api/alert-methods/types` returns the static class catalogue, no install data and no stored value. The two reads were the only leaks. **A false comment that helped hide this is corrected rather than deleted.** `backend/routers/backup.py` asserted that its DBAS alert-method redaction denylist "Matches the masking set in AlertMethod.to_dict (models.py) so backup redaction stays in lock-step with the API-response masking already shipped to clients". The final clause was untrue when written, and it is the reason the hole stayed invisible: anyone reasoning about where alert credentials can leak got "the API already masks these" from that comment and stopped. The claim is true as of this build, and the comment now says which part of it used to be false and what makes it true. **Response shape**, for anyone parsing these endpoints: `to_dict` returns one field the hand-rolled dictionaries did not, `updated_at`. Nothing was removed. **Do not echo a read response back into a write.** `********` is not recognized as a sentinel on the write side, so replaying a masked `config` into `PATCH` would overwrite a live credential with that literal. No caller in this repository does that (the one config-writing UI path, the SMTP recipient save, builds a fresh `{to_emails}` object), and the write side was deliberately left alone rather than being taught to strip a placeholder, since a sentinel the writer honours is a second mechanism to keep in step. Related and documented while adjacent: `PATCH /api/alert-methods/{id}` REPLACES `config` wholesale rather than merging into it, so an omitted key is dropped; `validate_config()` rejects a blob missing that type's required fields with `400`, which is what keeps a partial write from silently deleting a webhook URL. The durable half of this work is `backend/tests/routers/test_9kwzp13_alert_method_masking.py`, which pins the property (no credential value in either read body, checked against the raw response text and not only the intended field) against the same key tuple `routers/backup.py` redacts with, so the API-masking surface and the backup-redaction surface cannot drift apart again without a test going red.

- **Five more authorization verdicts from the `RequireAdminIfEnabled` audit, closing two routers that had no gate at all and moving the outbound-policy write and the two DBAS restore paths off a gate that admits the automation credential (beads enhancedchannelmanager-9kwzp.10 and enhancedchannelmanager-9kwzp.12, build 0095).** Read every statement below with the standing condition that all of these gates no-op while `require_auth` is false or setup is incomplete, so none of them can refuse a caller on a first-run or auth-disabled instance. That is a statement about these gates and not a claim that every first-run flow is healthy: the one first-run caller in this set, the backup-destination prompt, writes its already-answered flag before awaiting its request and swallows the result, so a failure of any kind silently retires the prompt with nothing persisted. That predates this build and is tracked separately. **`/api/cloud-targets` and `/api/alert-methods` carried no route-level authorization on any of their CRUD routes**, so any authenticated non-admin account could create a cloud upload destination with its own credentials, or create, change and delete the operator's alert methods, while bead 9kwzp.6 had already put both routers' `/test` verbs behind the human-admin gate: a caller could write a credential-bearing destination and not be allowed to test it. The alert-method half turned out to be sharper on READ than on write, and that is the most consequential finding in this build. `GET /api/alert-methods` and `GET /api/alert-methods/{id}` return each method's `config` blob verbatim with no masking of any kind, and that blob is where the Discord webhook URL, the Telegram bot token and the SMTP password live. Those are the exact three families bead 9ej7f withheld from the MCP service principal on `GET /api/settings` in build 0089, handed out in clear through a second table, which made that redaction largely decorative for anyone who thought to ask the other endpoint. Every route in that router now requires an admin, which is the half of the fix that is unambiguous. The MCP half is split, and the split is a product decision that leaves a known disclosure open, so it is stated here rather than left to be inferred from a dependency name. `POST /api/alert-methods`, `GET /api/alert-methods/{id}`, `PATCH` and `DELETE` carry a new `RequireHumanAdminForNotificationCredential` gate that refuses the automation credential; no MCP tool calls any of those four, so the denial costs nothing. **`GET /api/alert-methods` does NOT, and the automation credential can therefore read every configured method's Discord webhook URL, Telegram bot token and SMTP password in clear through it.** That is the accepted cost of keeping the shipped `list_alert_methods` tool working, which is the operator's inventory of their own alert methods, and it is worth being blunt that it also makes the gate on `GET /api/alert-methods/{id}` disclosure-vacuous against that principal, because the list already returns the same fields for every method. The right fix is the response rather than the gate: `AlertMethod.to_dict(include_sensitive=False)` already builds a masked `config` and neither read handler calls it. That is tracked as bead enhancedchannelmanager-9kwzp.13, it is open and rated P1, and closing it removes this exposure without taking the tool away. Until then, treat the output of `list_alert_methods` as secret; the tool itself prints only names, ids, types and severity filters and deliberately never echoes `config`. `GET /api/alert-methods/types` also stays on the ordinary admin tier, on much stronger grounds: it returns a static catalogue of the method types this build supports and holds no install data at all. No response body changed in this build; this is authorization, not masking. **The two outbound-destination routers, `/api/cloud-targets` and `/api/sync-targets`, are admin-gated end to end and continue to admit the automation credential on every route, writes included.** Sync-target CRUD was already admin-gated on all five routes, so that half was filed as a decision rather than a defect; cloud-target CRUD is where the real defect was, and it is closed. The decision on the automation credential went in favour of keeping capability. Bead jcj0f ships `create`, `update` and `delete` tools for both routers, six tools over exactly the six write routes, and a human-admin gate returned `403` to all six. Managing backup-upload and sync destinations is work the sidecar exists to do, so that capability loss was judged not worth what the denial bought. What it would have bought, recorded so the residual is visible rather than implied: a write to either router names a host, stores the credentials a scheduled background job will authenticate to that host with, and sets the `insecure` flag that turns off TLS verification for that traffic. A cloud target is where the DBAS backup task uploads the operator's archive, and a sync target is the remote instance the sync task pushes configuration to on a timer, with the target's own scheduled task registered as a side effect of creating it, so updating either repoints a flow the operator already configured, and the sync router's own documentation is explicit that the authoritative SSRF check runs at execute time rather than at write time. Encryption at rest and the last-four-characters masking bound disclosure of a stored credential and say nothing about redirection. The reads disclose the destination URL and upload path, the `insecure` flag, the names of the credential keys, each credential's last four characters, the credential version and revocation state, and the outcome of past syncs, which is network topology plus credential fingerprints; what the masking buys is only that no stored secret value can be reconstructed, so a read alone cannot authenticate to the destination. What does hold the line on this surface is that every route requires an admin, that the outbound-policy write below still refuses the automation credential, and that both cloud-target `/test` verbs have refused it since build 0089. Operator-visible consequence, stated plainly: no MCP tool loses access in this build. `list_cloud_targets`, `create_cloud_target`, `update_cloud_target`, `delete_cloud_target`, `list_sync_targets`, `get_sync_target`, `create_sync_target`, `update_sync_target`, `delete_sync_target` and `list_alert_methods` all keep working on an install with authentication enabled; `restore_dbas_backup_saved` newly returns `403` only when called with `confirm_apply=true`, and keeps its documented default preview mode. `test_alert_method` and `test_cloud_target` have been refused since build 0089 and are unchanged here. In the UI, the Alert Methods section of the Settings tab now shows an administrators-only notice to a non-admin instead of a failed request, the Email alert recipients field and its Save button are disabled for a non-admin and no longer fire a request that could only fail, and the Reset Statistics button is disabled for a non-admin; the cloud-target, sync-target and outbound-policy cards were already inside the admin-only Backup and Restore page. Note the predicate for all of those is whether the caller may act as an administrator, which on an install running with authentication turned off means every control stays available, matching the backend gates that no-op in the same state. **`PATCH /api/settings/security` moved off the plain admin tier onto a new `RequireHumanAdminForOutboundPolicy` gate.** It is the only field-specific writer of `ssrf_outbound_mode`, the setting that decides which hosts every outbound path in ECM may reach, so the automation credential could widen the policy from public-only to LAN-friendly. Gating the eleven outbound sinks of beads i4qrp, 9kwzp.6 and 9kwzp.7 while leaving their policy writable by the same principal was a partial control: it could not drive the probe, but it could move the fence the probe was measured against. The always-on half of the denylist, covering link-local and the cloud metadata address, IPv6 ULA, CGNAT and multicast, is not operator-togglable and is unaffected either way. The first-run backup-destination prompt still records the operator's choice, because the gate no-ops before setup completes. **The two DBAS restore endpoints now refuse the automation credential, and they do it differently from each other on purpose.** This looked like a plain inconsistency against the three legacy `/restore*` endpoints and was not; the repository history says so in three commits. `21f93e683` (2026-06-19) shipped `POST /api/backup/restore-dbas` on the ordinary admin tier, at a time when a DBAS restore applied denylist-filtered settings to the Dispatcharr upstream and never touched ECM's own `settings.json`. `e83d31b1` (2026-07-08, bead 6n76m) put the legacy `/restore`, `/restore-saved` and `/restore-yaml` paths behind the human-admin gate because those already did write ECM's settings blob, and deliberately left the DBAS routes alone for exactly that distinction. `fd63235d` (2026-08-04, bead dfkbn item 4) then added `dbas/importers/ecm_settings.py`, a generic attribute-assignment loop over the archive's settings mapping followed by a save, and that is what made the distinction stale. The importer excludes only the live Dispatcharr connection, install-local bookkeeping and redaction sentinels, and therefore not the media-server base URLs, the notification credentials, the auto-creation safety caps or `ssrf_outbound_mode`, so a caller supplying its own artifact could set every admin-only field the field-level gate on `POST /api/settings` refuses it. The gate went stale when the capability grew rather than being wrong when it was written, which is a failure mode the route inventory structurally cannot catch, because nothing about the route changed. That reasoning covers the APPLY and not a preview that writes nothing, so the two routes diverge. `POST /api/backup/restore-dbas` takes a caller-supplied upload, streamed to the config partition and decoded before anything examines `confirm_apply`, and the sidecar has no tool for it because there is no file upload over MCP, so it is refused outright including its dry run. `POST /api/backup/restore-dbas-saved` names an artifact already on the server, which only an admin could have saved there, so it keeps the ordinary admin tier and refuses only the apply: the MCP tool `restore_dbas_backup_saved` continues to work in its documented default mode, a counts-only preview, and returns `403` with a message naming the recovery when called with `confirm_apply=true`. The zero-mutation property of that preview is structural rather than a flag being trusted, because the restore orchestrator forces the dry-run marker whenever `confirm_apply` is false and treats that as the single choke point a caller can never opt out of. **`POST /api/settings/reset-stats` carried no dependency at all and deletes every row of seven statistics tables**, so any authenticated non-admin could wipe the operator's watch, bandwidth, popularity, telemetry and client-connection history. It now carries a new `RequireHumanAdminForStatisticsReset` gate, and the MCP half was decided on its own merits rather than by copying the sibling it was split from: `POST /api/settings/restart-services` kept admitting the automation credential in build 0089 because it rebuilds background services from already-saved settings, work a settings write schedules for itself anyway, so refusing it would have refused a restart the principal can already trigger indirectly. Nothing about a statistics reset is recoverable that way. There is no compensating write, no rollback ledger and no other route that re-derives those tables, which is also what distinguishes it from the Channel Pipeline rollback routes that remain admitted precisely because they are the pipeline's own reversal mechanism. Three new denial messages were added over the same `require_admin_if_enabled` factory the existing gates use, rather than three new mechanisms, for the reason that message is a per-call-site parameter at all: a caller refused on an alert-method write being told it "cannot perform backup restore" or "cannot run connection tests" would send incident triage at a subsystem the route never touches, and the seven messages are now asserted pairwise distinct. A fourth, `RequireHumanAdminForOutboundDestination`, was built for the destination writes and then removed rather than left unused, because a shipped gate constant whose prose says a route is refused is worse than no constant when the route is in fact admitted. The trap this whole epic exists to avoid remains `RequireAdminIfEnabled`, which refuses a non-admin but admits the MCP principal because the auth layer stamps that principal `is_admin=True`, so it closes half a hole while reading as fixed in review. Every gate added here is deliberately coarser than the risk that motivated it, and that is a trade rather than an oversight: a display-name or severity-filter change on an alert method is refused exactly like a credential replacement, and tightening the outbound policy from LAN-friendly to public-only is refused exactly like widening it. Field-level branching is the machinery `POST /api/settings` needs because it mixes admin configuration with per-user display preferences in one blob and cannot be split; none of these routes has that problem, nothing on them is a per-user preference, so a coarse gate costs an automation credential nothing it had a legitimate claim to, fails in the safe direction, and is far cheaper to verify. Every verdict here is proved against the principal it names rather than inferred from a dependency's name: nineteen routes are exercised in four postures each, the DBAS preview/apply split carries six dedicated cases of its own, asserting a witness side effect so a refusal is proved to be the gate answering rather than a handler that happened to fail, and `backend/tests/test_admin_gate_inventory.py` continues to assert both the MCP-denied and MCP-admitted sets exactly, with each entry filed under a group whose comment records why.

- **The whole `/api/tls` router now enforces authorization per route, where it previously enforced none (bead enhancedchannelmanager-9kwzp.11, build 0094).** All thirteen TLS routes carried zero route-level auth dependencies. Everything rested on the global auth middleware, which establishes only that a caller is authenticated: no `/api/tls` path is or ever was auth-exempt, so authentication was always required and authorization never was. The practical effect was that any authenticated NON-ADMIN account, and the static MCP service principal (which the auth layer stamps `is_admin=True` and which the middleware accepts in place of a JWT across the entire `/api/` surface), could upload attacker-supplied certificate and private-key material and have ECM serve it as the instance's transport identity, delete the operator's own certificate with no undo path short of a fresh issuance, rewrite TLS configuration including the DNS-provider credentials that bead 2owpi records as living in plaintext in `/config/tls_settings.json`, exercise those stored credentials against the provider API, drive ACME issuance and renewal, and stop HTTPS termination outright, dropping the instance back to plaintext HTTP on 6100. Three tiers now apply, decided per route rather than blanket-applied, because the router mixes three different risk shapes. Ten routes take a new `RequireHumanAdminForTLSMaterial` gate, which requires an admin AND refuses the MCP principal: `POST /configure`, `/request-cert`, `/complete-challenge`, `/upload-cert`, `/renew`, the `/https/start`, `/https/stop` and `/https/restart` trio, `DELETE /certificate`, and `GET /settings`. That last one is on the denied side deliberately: masked is not absent, since the response discloses the final four characters of the DNS API token and of both AWS credentials plus `dns_zone_id` and the ACME contact address in clear, which is the same class of read bead 9ej7f withheld from this principal on `GET /api/settings`. `POST /test-dns-provider` instead reuses the existing `RequireHumanAdminForOutboundTest`, because it is the credential-validity oracle shape of the eleven sinks closed by beads i4qrp, 9kwzp.6 and 9kwzp.7 and its refusal message should read like theirs. The two remaining reads, `GET /status` and `GET /https/status`, take the ordinary admin tier and continue to admit the automation credential: neither returns credential material, and a certificate's subject, issuer and validity window are served to every TLS client anyway. A fourth denial message was added rather than a fourth mechanism, over the same `require_admin_if_enabled` factory the other three gates already use, for the reason that message is a per-call-site parameter at all: telling a caller refused on a certificate upload that it "cannot perform backup restore", "cannot manage the MCP API key" or "cannot run connection tests" would send incident triage at a subsystem these routes never touch. The trap this bead exists to avoid is `RequireAdminIfEnabled`, which closes the non-admin half of the hole and leaves the MCP half wide open while reading as fixed in review; it has now caught two engineers on this codebase, so the MCP-refused case is asserted per route rather than inferred from a dependency's name. Operators lose nothing: the TLS Certificates settings section is already admin-only in the UI, no first-run or pre-auth path calls any of the thirteen routes (the setup page imports exactly one API function and references TLS nowhere), the MCP sidecar exposes no TLS tool, the HTTPS subprocess proxy forwards no `/api/tls` path, and the renewal scheduler calls the renewal routine in-process rather than over HTTP. Every gate no-ops while `require_auth` is false or setup is incomplete, so auth-disabled and first-run instances are unaffected, and that is pinned for all thirteen routes across all three of those postures. The redaction and round-trip behavior of `GET /settings` and `POST /configure` is untouched: this change is authorization only.

- **CI now fails a pull request that ADDS a credential-bearing or bulk binary artifact to the repository (bead enhancedchannelmanager-6mqn5, build 0093).** This entry is developer/operator-facing only; it has no effect on ECM's running behavior. Build 0092 added `.playwright-mcp/` to `.gitignore` after that directory was found holding 2,321 files totalling 3.1 GB, including `enc-artifact.zip`, an `ECMBKENC`-encrypted ECM backup envelope carrying a live XC provider credential, untracked and unignored in a public repository and one `git add -A` from history. Naming one directory does not close the class: Playwright's upload sandbox refuses any path outside the repository root, so every backup/restore drill round that uploads an artifact through the Restore-from-artifact UI is structurally forced to copy that artifact into the working tree first, and the next such directory has a name nobody has invented yet. `scripts/check_binary_artifacts.py` is the backstop, and it is deliberately not written in terms of `.playwright-mcp/` at all. The three existing ratchets could not have caught this: `check_secrets.py`, `check_pii.py`, and `check_em_dashes.py` all read added LINES, and a ChaCha20-Poly1305 ciphertext has no lines for detect-secrets to report on, so the unit of inspection here is the added FILE. Three rules apply, to files the change adds relative to its merge base and to files merely sitting untracked and unignored in the working tree, which is what the incident actually looked like. First, a leading-byte signature match against ten high-signal container formats (the `ECMBKENC` envelope, ZIP, called out separately as the DBAS artifact shape when the archive carries `manifest.json`, plus gzip, bzip2, xz, zstd, 7-Zip, RAR, tar, and SQLite), which runs regardless of extension because the motivating artifact happened to be named `.zip` while the real risk is the blob under any name; a ZIP renamed to `.png` is still blocked. Second, binary content (a NUL byte in the first 8 KiB) whose suffix is not one of the eleven image and font types a web application genuinely needs. Third, a 2 MiB per-file ceiling and a 25 MiB whole-change ceiling, the latter being the only rule a directory of 2,321 individually unremarkable trace files would trip. The suffix list is a list of TYPES rather than of PATHS on purpose: a path allowlist would red-line every new user-guide section that ships a screenshot, and a guard whose false positives are expensive is a guard that gets turned off. That premise is tested rather than asserted. `backend/tests/unit/test_check_binary_artifacts.py::TestTrackedTreeIsClean` runs the guard across the live output of `git ls-files`, currently 1,861 paths, and requires zero findings, so the 151 tracked PNGs (visual-regression baselines under `e2e/snapshots/`, operator-guide screenshots under `docs/images/`, and the frontend favicons and logo) are proved clean rather than assumed clean, and a newly tracked binary type surfaces as one failing test to triage instead of as a red pull request for whoever added it. The escape hatch is `.binary-artifacts.allowlist` at the repository root: one exact repository-relative path per line with a mandatory `#` reason, shipped empty because the repository needs no exemption today. A checked-in file was chosen because it mirrors `.secrets.baseline`, the nearest existing convention here; the inline-comment conventions (`em-dash-ok`, `fake-test-ok`) cannot apply to a file with no lines to comment on, and a commit-message token like the commit-msg hook's `[no-bead]` scrolls out of view the moment the pull request merges, while the reason for keeping a binary in a repository has to stay readable years later. Globs are rejected, because `*.zip` would re-open the hole in one line. The guard runs in the `Operator Docs` job beside the other three ratchets and reports its own outcome in the run summary, and it runs HEAD's copy of the script rather than the merge base's, so a false-positive fix takes one pull request instead of two; what base-pinning would have bought is bought instead by `TestPolicyIsPinned`, which pins the format list, the suffix list, and both ceilings under the required `Backend Tests` context, so weakening the guard inside its own pull request fails a required check rather than reading as housekeeping. Verified against the real preserved drill artifacts: both were re-planted in the working tree and the guard named `enc-artifact.zip` as an ECMBKENC envelope and `std-artifact.zip` as a DBAS artifact, exiting 1, with the same run exiting 0 once they were removed.

- **Repository hygiene: contained the remaining local scratch artifacts, pinned the backend test gate to the project venv, and recorded three agent-discipline rules (build 0092).** This entry is developer/operator-facing only; it has no effect on ECM's running behavior. `.playwright-mcp/` was never ignored and held 2,321 files totalling 3.1 GB, including an `ECMBKENC`-encrypted backup envelope; a single `git add -A` would have staged it. Three root-level scratch files were also unignored, one of them (`uliyr-post-reload.yml`) exactly the a11y-snapshot class the earlier `h9av3` anchored globs were written to catch and did not. This entry closes the remaining gap in that same sweep: five `.beads/` local tool-runtime artifacts (`backup/`, `embeddeddolt/`, `interactions.jsonl`, `last-touched`, `.auto-import-issues.jsonl`) and a root-level `.coverage` db were still untracked and unignored on every branch; `.beads/.gitignore` and the root `.gitignore` now exclude them, verified against `git ls-files .beads/` so nothing genuinely tracked (`issues.jsonl`, the real issue board) was touched. Separately, the documented backend test gate used ambient `python`, which resolves an older `cryptography` and silently self-skips 9 TLS tests rather than failing, so the run reported success either way; measured `9270 passed / 18 skipped` on ambient versus `9279 passed / 9 skipped` on the project venv. Fixed in `backend/CLAUDE.md`, `docs/pytest_conventions.md`, `docs/testing.md`, and `docs/shipping.md`, the last of which had been contradicting itself by pinning `.venv` for `check_publish.py` but not for pytest. `CLAUDE.md` gained two agent-discipline rules: close every bead your work fulfills (a validity audit closed 46 beads that were already-shipped work never closed, leaving the backlog overstating remaining work by roughly 17 percent), and a third worktree caveat recording that worktrees are branched from `origin/main` rather than `dev`, measured at 858 files behind, with clean status and passing gates hiding it. `docs/pytest_conventions.md` also gained a credential-fixture convention for security tests, because the secrets ratchet deliberately disables the inline `pragma: allowlist secret` that detect-secrets' own output recommends.

- **Minting and revoking the MCP API key are now human-admin operations; the endpoints previously carried no authorization of any kind (bead enhancedchannelmanager-9kwzp.8, build 0091).** `POST /api/settings/mcp-api-key` and `DELETE /api/settings/mcp-api-key` took no parameters and declared no dependency, so the only thing standing between any authenticated caller and the MCP credential was the global `/api/*` authentication middleware, which cares that you are logged in and nothing more. That is privilege escalation rather than a missing admin tier, because the key these endpoints mint is not an ordinary setting: `backend/auth/dependencies.py` builds the MCP service principal with `is_admin=True`, and `backend/main.py` accepts the static key as an alternative to a JWT across the whole `/api/` surface, so an authenticated non-admin could mint itself a credential the rest of the application treats as admin and then use it for everything the admin tier allows. The DELETE half is the mirror image: a one-call denial of service on every sidecar integration on the instance. Both handlers now carry a new `RequireHumanAdminForServiceCredential` dependency. It is a sibling of the `RequireHumanAdminForOutboundTest` gate that build 0089 introduced rather than a reuse of it, and the reason is the 403 body rather than the behavior: nothing on this route reaches the network, so a refusal reading "cannot run connection tests" would send incident triage at a probe that never happens here, which is precisely why `mcp_denial_detail` is a per-call-site parameter. The static MCP principal is refused on top of the non-admin refusal for a reason unlike either group already denied: it would be the bearer of a credential rotating and revoking that same credential. A minted key is disclosed only in the response body, so a holder of a leaked key could mint a successor that survives the operator's own rotation, and a revoke driven by the sidecar is a self-inflicted outage with no operator in the loop. Credential lifecycle belongs to the human operator who owns the credential, which is the same principle `reject_mcp_service_principal_mutation` already applies to the self-mutation auth routes. Setup mode is unaffected and was verified rather than assumed: no first-run path calls either endpoint, `SetupPage.tsx` imports exactly one API function (`completeSetup`), and the sole caller of `generateMCPApiKey` and `revokeMCPApiKey` is `MCPSettingsSection`, rendered from the post-auth Settings tab behind its own `isAdmin` guard, so no operator-visible behavior changes for an admin or for an install running with authentication turned off. `backend/tests/test_admin_gate_inventory.py` records the verdict under a new `_SERVICE_CREDENTIAL_LIFECYCLE` group and pins the classifier against the fourth shipped dependency, so the MCP-denied set is now 16 routes asserted exactly.

- **Every outbound connection test now refuses the MCP service key, and the two admin gates that look interchangeable are pinned apart by a live inventory test (beads enhancedchannelmanager-i4qrp + enhancedchannelmanager-9kwzp.6 + enhancedchannelmanager-9kwzp.7, build 0089).** Eleven endpoints reach the network with operator-supplied or STORED credentials and report the upstream verdict back, which is a status-code oracle and an in-band port scanner, and for the stored-credential members it is a send the caller never had to know a secret to perform: the four in `backend/routers/settings.py` (`POST /api/settings/test`, `/test-smtp`, `/test-discord`, `/test-telegram`), the three media-server probes (`POST /api/settings/emby/test-connection`, `/plex/test-connection`, `/jellyfin/test-connection`), and four more (`POST /api/alert-methods/{method_id}/test`, `POST /api/m3u/digest/test`, `POST /api/cloud-targets/{target_id}/test`, `POST /api/cloud-targets/test`). All eleven now carry a new `RequireHumanAdminForOutboundTest` dependency, built on the existing `reject_mcp_service_principal` path, which admits a human operator admin and refuses the static MCP service principal with `403` and a per-call-site message naming the MCP principal, so incident triage can tell that refusal apart from a plain non-admin one. The subtlety worth writing down for future readers is that `RequireAdminIfEnabled` does NOT refuse the MCP key, because `auth.dependencies._build_mcp_service_principal` sets `is_admin=True`: the three media-server endpoints already carried `RequireAdminIfEnabled` and were still reachable by the automation credential, so reaching for the plain admin gate closes the non-admin half of a hole, leaves the MCP half wide open, and reads as fixed in review. Several of the eleven, including the alert-method and cloud-target tests, carried no route dependency at all beforehand, so any authenticated caller could drive them. `POST /api/settings/restart-services`, which also had no gate, now carries the ordinary admin tier and deliberately still admits the MCP key: it names no host, sends no secret and echoes no upstream status, and it rebuilds background services from already-saved settings, work that a settings write schedules for itself anyway. `backend/tests/test_admin_gate_inventory.py` walks the live FastAPI dependency tree and reads the captured `reject_mcp_service_principal` flag off each gate closure rather than comparing `Depends` identities, then asserts both the MCP-denied set (14 routes) and the MCP-admitted set (79 routes) EXACTLY, each under a group whose comment records why that verdict was reached, so a later change in either direction fails a test instead of silently altering the security contract. Operator-visible consequence, stated plainly: the MCP tools `test_cloud_target` and `test_alert_method` now return `403` and their tool descriptions say so and point at the UI instead, and the "Test" buttons for alert methods and cloud targets return `403` for non-admin users. Admin operators, and any install running with authentication turned off, are unaffected.

- **`GET /api/settings` no longer hands the Discord webhook URL or the Telegram bot token and chat ID to a non-admin or to the MCP service key (bead enhancedchannelmanager-9ej7f, build 0089).** The read path returned `discord_webhook_url`, `telegram_bot_token` and `telegram_chat_id` in clear to any authenticated non-admin and to the MCP service key, while `backend/routers/settings.py::_resolve_settings_admin` already denied that same principal WRITE on those exact three fields. The gate was therefore asymmetric in the direction that matters: a principal trusted to set none of them could read all of them. The read path now depends on the same predicate and returns an EMPTY value for each of the three to a principal the predicate does not treat as admin. Empty redaction was chosen over a masked tail deliberately, and the reason is a write-path interaction rather than a display preference: `SettingsTab` loads these values into text inputs and POSTs them straight back, and `update_settings` writes the webhook literally with no preserve-on-omit, so any non-empty placeholder would have been round-tripped verbatim and would have overwritten a live webhook on the next save by a non-admin. Admins keep literal read and literal write, because blanking a webhook to disable Discord is a real operator action and a preserve-on-omit rule would have taken it away.

- **Channel Pipeline rules now recognize the channels they created on earlier runs (GitHub #801, bead enhancedchannelmanager-0ippw, build 0090).** ECM marked each channel it created as auto-created in memory only; Dispatcharr never stored that marker, so on the next run a rule read its own channels as hand-built, the manual-channel guard rejected them, and the rule re-created its whole set while orphan cleanup deleted the previous one. Every channel ID changed on every run and `if_exists: merge` could never merge across runs. Provenance now comes from the rule's persisted managed-channel ledger, which already survived runs, so a Create Channel action with `if_exists` merge or skip reuses its existing channels and IDs stay stable. Genuinely hand-built channels are still protected: the ledger holds only channels the pipeline created or already managed, never one a rule merely merged into. Event Sync promotion re-adopts its promoted channels through the same ledger, so its internal manual-merge override has been removed, closing a path that could adopt a hand-built channel in the promotion target group and then place it in scope for orphan deletion.

- **The rule analyzer now flags a merge lookup pinned to a group the rule never creates channels in (GitHub #801, bead enhancedchannelmanager-rtst2.1, build 0090).** A rule whose Merge lookup scope names a specific group, while its Create Channel action lands elsewhere, searches a group its channels are never in, so every lookup misses and the rule delete/recreates its whole channel set on every run with orphan action Delete. The new `MERGE_SCOPE_PINNED_TO_OTHER_GROUP` warning names both group IDs and the fix, resolving the destination through the same chain the executor uses and staying silent when a Create Group action makes that destination a runtime decision. It is the mirror of the existing `MERGE_SCOPE_NOT_TARGET_GROUP` advisory, which only ever fires when the scope is off. The rule builder surfaces it while the rule is still a draft.

- **The MCP sidecar can now set a rule's manual-merge option (GitHub #801, bead enhancedchannelmanager-0fn69, build 0090).** `create_auto_creation_rule` and `update_auto_creation_rule` advertised `allow_manual_channel_merge` and the API accepted it, but the sidecar's own endpoint contract omitted it, so the call was rejected before it left the sidecar and the documented workaround for the churn above was unreachable. The field is now declared, and a contract test runs the check in both directions so a field the API accepts can no longer be missing from the contract without either being added or recorded as deliberately not exposed.

- **Normalize-in-group followed by Merge Streams now carries one deterministic target identity between phases (GitHub #845, bead enhancedchannelmanager-ukgj9, build 0088).** Auto-target merge actions ignore stale hidden explicit-lookup fields and consume the firing rule's already computed normalized name. A unique existing or same-run-created channel receives the stream; absent or ambiguous identities remain skipped, without creating duplicate channels or streams or exposing lookup values in ambiguity diagnostics.

- **Cross-instance sync now preserves channel EPG assignments by portable guide identity (GitHub #842, bead enhancedchannelmanager-h3jq2, build 0087).** Sync resolves each source `epg_data_id` to its guide row's normalized `tvg_id`, then resolves that identity independently on the destination instead of sending an instance-local database ID. Unique matches apply to both matched and newly created channels. Null links remain null; incomplete source lookup and missing or ambiguous destination identities never fall back to the channel's unrelated `tvg_id`, preserve existing assignments, and increment the named unresolved-link warning for both existing and newly created channels. Lookup failures disclose only their error category, not exception payloads.

- **Raw stream probe and preview failures now explain ECM's network boundary without exposing provider URLs (GitHub #806, bead enhancedchannelmanager-po29f, build 0086).** Raw stream probes and browser previews connect from the ECM container directly to the provider; they do not traverse Dispatcharr's channel proxy or inherit its VPN namespace. Connection failures now give value-free guidance to place ECM on the same VPN/routed network, while channel previews remain explicitly identified as Dispatcharr-proxied. FFprobe diagnostics redact the complete stream URL before logging or persistence, and non-network media failures retain their generic behavior.

- **Pipeline and manual channel-name sorting now preserve numeric brand prefixes (GitHub #838, bead enhancedchannelmanager-g4xhp, build 0085).** With “ignore channel numbers in names” enabled, only explicit generated-number separators (`|`, `-`, or `.`) are stripped; a bare space no longer turns names such as `360 Tunebox` into `Tunebox`. Numeric names with and without spaces now follow the same deterministic natural ordering, including multi-digit values, while descending order, country-prefix handling, case-insensitivity, and stable equal-key ordering retain their existing contracts.

- **Pipeline no-sort selections now survive save/reopen and reruns preserve manual channel numbers (GitHub #833, bead enhancedchannelmanager-pjj97, build 0084).** Editing an existing rule now treats its persisted null stream-sort value as the explicit “No sorting” selection instead of substituting the Smart Sort default; every supported stream-sort mode is pinned through UI and API round trips. Orphan cleanup now closes numbering gaps only when the rule explicitly enables Channel Sort, while fixed/range create numbering still applies to genuinely new channels and existing merge behavior is unchanged.

- **LAN-friendly outbound policy now accepts RFC 6598 shared-address peers (GitHub #836, bead enhancedchannelmanager-z0cqm, build 0083).** Literal IPs and hostnames resolving solely into `100.64.0.0/10`, including the reported `100.80.3.24` cross-instance Sync destination, follow the same explicit mode toggle as RFC1918 and loopback: allowed in LAN-friendly and denied in Public internet only. Redirects are revalidated under that same rule. The carve-out is limited to RFC 6598; mixed DNS answers still reject the whole request, and link-local/metadata, multicast, unspecified, IPv6 ULA, and every other non-global special-use range remain blocked.

- **Frontend asynchronous tests now synchronize on settled operator-visible state (bead enhancedchannelmanager-7zp6g, build 0082).** The Sticky Section Navigation re-arm test waits for the newly requested section to become current before checking its queued scroll, and the outbound-policy default test waits for the loaded radio selection rather than merely observing that its settings request started. This removes two coverage-order races without weakening their behavioral assertions.

- **The final four tab-owned modal callers now complete the 75-caller accessibility inventory (bead enhancedchannelmanager-hr4ft.5, build 0081).** The standard EPG source editor, Logo delete confirmation, Settings probe-results dialog, and Settings reordered-channels dialog now own unique names resolved from visible headings, dialog/modal semantics, and the shared topmost focus lifecycle. EPG save and Logo delete states block every direct dismissal while work is pending; the probe-results re-run intentionally closes its results dialog before starting the background operation. `ModalOverlay` remains neutral. The production-build modal catalog now renders every catalogued state and verifies that each ModalOverlay has exactly one named modal semantic owner, except explicitly enumerated nested stacks which require exactly two.

- **Settings-family dialogs now have caller-owned names, focus lifecycle, and busy-safe dismissal (bead enhancedchannelmanager-hr4ft.6, build 0080).** The Cloud Target editor and delete confirmation, Linked Accounts credential dialog, four Normalization Engine editors/import/apply surfaces, and two Tag Engine dialogs now expose unique names resolved from visible headings and opt into the shared topmost focus containment/restoration helper. The existing descendant-owned Normalization apply confirmation remains the sole nested semantic surface. Async create, link, import, apply, save, test, and delete states prevent Escape, header Close, and footer Cancel from dismissing work in progress; `ModalOverlay` remains neutral. The exact 75-caller manifest changes only these nine settings-family entries from recorded debt to managed semantics.

- **Stacked and stateful modal families now preserve accessible identity and focus order (bead enhancedchannelmanager-hr4ft.2, build 0079).** The Streams bulk-create conflict, Task Editor with both nested schedule editors, and User Menu profile/password states now expose caller-owned named dialog semantics with unique heading references and managed focus. Nested schedule Escape closes only the top dialog and restores its opener inside the parent before a later Escape reaches the parent; topmost Tab containment follows the shared lifecycle. Existing save/create busy states suppress Escape dismissal. User Menu transitions stage focus on the persistent menu trigger so closing either state restores a connected opener. `ModalOverlay` remains neutral, and only these six recorded manifest entries move from debt to managed semantics.

- **Standalone modal dialogs now expose complete caller-owned semantics and focus lifecycle (bead enhancedchannelmanager-hr4ft.1, build 0078).** The 31 recorded standalone callers from Auto-Sync Settings through Stream Profiles, including the VLC protocol helper, now own `role="dialog"`, modal state, unique names resolved from their visible headings, initial in-dialog focus, topmost Tab containment, and opener restoration. Escape and busy-state policy remain caller-controlled, and the shared `ModalOverlay` stays neutral. The closed 75-caller manifest changes only this bounded family from recorded debt to managed semantics; component tests pin accessible-name resolution, Escape behavior, focus containment, and duplicate-instance ID uniqueness.

- **Eleven existing semantic dialogs now expose unique caller-owned accessible names (bead enhancedchannelmanager-hr4ft.4, build 0077).** The three Dummy EPG manager dialogs, five Channel Pipeline dialogs, Event Sync auto-sync confirmation, Event Sync rule discard confirmation, and Rule Builder discard confirmation now bind their visible headings through unique `aria-labelledby` IDs. Overlay-owned dialogs keep their existing role and modal state on `ModalOverlay`; descendant-owned alert dialogs keep the shared overlay neutral, including the nested Rule Builder confirmation. Component tests pin names, duplicate-instance ID uniqueness, nested exactly-two-dialog semantics, and existing busy/Escape behavior. The closed 75-caller manifest changes only these eleven naming states; the remaining recorded accessibility debt is unchanged.

- **Modal accessibility debt now has a closed 75-caller ownership and focus-policy manifest (bead enhancedchannelmanager-hr4ft.3, build 0076).** The former list of only 50 missing callers is replaced by an exact bidirectional inventory of every production `ModalOverlay` use, including current overlay/descendant ownership, independently measured role, modal and naming status, lexical nesting, family, and focus policy. Recorded missing or partial semantics remain explicit debt for the bounded hr4ft child beads, not accessibility exceptions. The AST audit rejects new or stale callers, ownership drift, partial or nested semantic surfaces, empty names, and role/modal/name regressions while keeping the shared overlay neutral. A new caller-opt-in focus lifecycle helper supplies eligible initial focus, a programmatically focusable container fallback when every control is unavailable, topmost Tab/Shift+Tab containment and recapture, and stack-safe opener restoration without taking over Escape policy. `TypeToConfirmDialog`, already a named semantic dialog, is the bounded integration pilot; its busy callback still suppresses Escape, its all-disabled state focuses the dialog container, and nested dialogs restore focus in stack order.

- **Modal responsiveness now follows three reviewed semantic tiers at accessible browser zoom (beads enhancedchannelmanager-79uf8 + enhancedchannelmanager-hw8e3, build 0075).** A 1280px desktop at 200% zoom has a 640 CSS-pixel viewport, so the former 600px rules did not run at the required zoom point. The common zoom/reflow tier now begins at 700px with 8px overlay inset, a 95vh height cap, 8px 16px header padding, 16px body padding, 14px 16px wrapping footer padding, and single-column shared form rows. The 820px complex-layout collapse and 500px compact-content behavior remain separate. VLC, stream-preview, Rule Builder footer/date fields, and Dummy EPG picker content reflow at the zoom tier. M3U filter rows retain exposed, visibly labelled Action and Order values instead of hiding them, while their desktop column headers remain the sole visible labels above the tier. Bulk LCN truncation is responsive only at the compact tier, and Logo's compact rule now has sufficient ownership to render. A caller-derived, bidirectional census follows every direct stylesheet import from every `ModalOverlay` caller, including host-page styles, and records every media block. An exact-build, animation-frozen browser harness pins 640x360 catalog rendering, viewport containment, chrome values, accessibility labels, and boundary behavior.

- **Filter-select chrome is privately owned by each route instead of leaking across eager and lazy CSS chunks (bead enhancedchannelmanager-8epq2, build 0074).** StreamsPane's two single-select alternatives, Journal's three frozen-header filters, and M3U Changes' four frozen-header filters now use distinct owner classes that remain valid after `RouteHeaderSlot` portals the latter controls. The previously effective box, trigger, hover, focus/open, and responsive declarations are preserved under those owners, while the shared `filter-select` token and its three cross-chunk debt entries are gone. A token-aware production census pins zero legacy uses and exact 2/3/4 source ownership. The production route deliberately renders StreamsPane's multi-select branch, so a production-build Playwright guard asserts those two alternatives remain absent there and compares the rendered Journal/M3U controls across forward, reverse, and revisit session orders at 1280x720 and 1920x1080, including wrapper/trigger geometry, chrome, focus/open state, frozen-header height, clipping, and overflow. Responsive behavior is exercised at 768px and 600px. The guard stubs route data only to render deterministic controls; it separately requires both exact-build and checkout-server modes and verifies the fingerprinted CSS asset actually served. Reviewers run the canonical `npm run test:css-guard:filter-select`; the guard remains proportional/manual rather than extending the required Frontend Tests browser step.

- **The final 101 unscaled modal text elements now use semantic type-scale owners (bead enhancedchannelmanager-8rszp, build 0073).** The live 82-dialog catalog was remeasured before editing and still contained the recorded 42 spans, 17 strong elements, 16 paragraphs, 11 summaries, 5 labels, 5 list items, 3 definition terms, and 2 legends at the 16px root default. Each residual is now assigned by meaning: running copy inherits the body token, form and disclosure labels use the label token, review definition titles use the item-title token, and inline emphasis inherits from its corrected container. A reviewed 101-element decision ledger binds every exact measured row and per-owner count to its chosen token; no modal-body-wide default was introduced. The required Frontend Tests context now renders all 82 catalog entries in Chromium at 1280x720, requires every non-gap entry to report a successful harness render with visible production DOM, and rejects a live governed bare-tag residual at 16px, so a production addition or render crash cannot hide behind the historical ledger. The Stream Create empty state's remaining `0.8rem` literal also joins the body scale. Animation-frozen harness captures at 1280x720 and 1920x1080 reduce this exact bare-tag population from 101 to zero without horizontal overflow; the committed historical baseline was not recaptured.

- **Destructive DBAS restore dialogs now expose stable, caller-owned accessible names (bead enhancedchannelmanager-czmph, build 0072).** The uploaded-backup restore, saved-backup restore, and their reusable type-to-confirm gate now carry `role="dialog"`, `aria-modal="true"`, and unique `aria-labelledby` title IDs without changing the neutral `ModalOverlay` primitive or its existing focus behavior. Nested confirmation dialogs retain distinct names and IDs, and the confirmation input still receives focus while Escape remains disabled during a busy operation. A repository AST census accounts for all 75 `ModalOverlay` call sites and explicitly records the 50 remaining semantics omissions as debt owned by enhancedchannelmanager-hr4ft; a recorded omission is not an accessibility exception.

- **The final two modal close-button exceptions now use the shared 32px chrome (bead enhancedchannelmanager-0f2ln, build 0071).** Pending Merges' bulk confirmation and Tag Engine's create-group dialog spelled the control `.modal-close`, bypassing ModalBase's padding, centering, radius, colour, transition, and hover treatment while local CSS compensated only for the glyph size. Both now use `.modal-close-btn`, and the four obsolete private rules are gone. The shared E2E close selector follows the canonical class, while an AST-backed source census rejects statically authored legacy tokens across JSX expression shapes. Component tests retain close, focus-trap, Escape, and focus-restoration behavior. Exact-build browser checks compare both dialogs with a canonical control in normal and hover states at 1280x720 and 1920x1080; both targets are 32x32 with an 18px glyph and no horizontal overflow.

- **Visiting Channel Pipeline no longer restyles textareas in every later modal (bead enhancedchannelmanager-dlcl2, build 0070).** Its lazy stylesheet declared import/export textarea and focus rules beneath the shared `.modal-body` class without a route ancestor, so loading that chunk permanently changed unrelated dialogs for the rest of the browser session. `ModalOverlay` renders in place rather than through a portal, which means both intended YAML textareas remain descendants of `.channel-pipeline-tab`; both rules are now scoped to that root with unchanged declarations. A CSS-structure ratchet pins both selectors, component coverage proves both dialog textareas remain under the root, and a browser regression compares a foreign modal textarea before and after loading the Channel Pipeline chunk.

- **Capped GitHub file lists now fail safe instead of hiding unexamined changes (bead enhancedchannelmanager-cyd0h, build 0069).** The shared changed-path action transports an explicit `{complete, paths}` envelope into the classifier. GitHub documents that compare responses expose at most 300 changed files and pull-request file responses at most 3,000, so an exact-cap response is incomplete: both `code_paths_changed` and `docs_site_affected` become `true`, running every code gate and rebuilding the documentation site. Responses below the boundary retain precise path verdicts, including both sides of renames. Missing, malformed, failed, or incomplete acquisition also fails safe without printing path values. The action retains paginated, slurped JSON transport; deterministic fixtures pin compare 299/300 and paginated pull-request 2,999/3,000 boundaries.

- **Changed-path acquisition has one structured workflow authority (bead enhancedchannelmanager-ifx16, build 0068).** Every `.yml` and `.yaml` workflow that classifies changed paths now invokes the same repository composite action. That action alone owns the pull-request and compare API pagination, lossless JSON transport, rename-source extraction, and classifier call, so adding another workflow cannot silently copy an incomplete filename-only implementation. YAML-structure tests discover the action across every job regardless of job name, require exactly one invocation per registered consumer workflow, and constrain its job to checkout plus the stable action with both outputs bound to that step; a fourth consumer requires an explicit manifest update and review. No additional or shell `run` step is permitted in that authority job. A closed-world reserved-identifier policy rejects the classifier name or path in every workflow `run` scalar, even in shell comments or echoed prose, and rejects noncanonical action references. Every occurrence of either verdict name in parsed workflow keys or scalar values must match an explicit location-and-value manifest covering the authority outputs and the current downstream gates. Every job-output mapping, every job/step `if`, and every job/step `continue-on-error` value across the entire workflow directory is exact-snapshotted by structural location and value, including unrelated cache, release-cut, digest and flake controls. There is no lexical expression filter: fully constructed bracket/format output access and dynamic failure suppression are controls and require deliberate manifest review. Reusable workflows may not export either reserved verdict, and reusable-workflow jobs may not feed those verdicts through `needs`; unrelated reusable outputs remain valid. The composite action separately pins both public output mappings and an exact one-step structure with the stable classifier id, fixed environment, reserved-name occurrences, and one canonical `$GITHUB_OUTPUT` writer across all action run scalars; swapped outputs, added steps, split-key fabrication, or later writes fail. This deliberately accepts review friction to avoid interpreting shell semantics or building a recursive workflow-call graph; real YAML comments remain ignored. The shared action's endpoint-specific jq behavior is validated once. The local classifier command uses the explicit NUL input contract, and the test-dependency fast-fail distinguishes required Python packages from the required `jq` OS executable with separate installation guidance.

### Security
- **Fresh installs now engage authentication as part of creating the first admin (bead enhancedchannelmanager-qg14z, build 0066).** `POST /api/auth/setup` previously committed the user and identity rows without persisting `setup_complete=true`. Because the global middleware enforces only when both `require_auth` and `setup_complete` are true, every admin-gated route remained anonymous until a later status request happened to repair the flag, including the backup download that contains the application database. Setup now writes the gate atomically before it can return success. The HTTP and TLS request-serving processes detect the shared file's changed inode and reload it instead of retaining a process-local first-run answer. A cold process that finds the file missing, unreadable, or invalid permits setup only after proving the user database is empty; durable ownership or an unverifiable database fails closed without creating or overwriting the settings path. The missing-file proof, absence recheck, and initial write share the same host-wide settings lock as every later settings write, so a stale process cannot overwrite a peer's completed setup with open defaults. A database failure restores the prior flag only after an independent query proves that no user became durable; an ambiguous commit remains fail-closed and recoverable. A separate host-wide non-blocking lock prevents two processes from racing first-admin transactions. The frontend re-reads auth status when the setup wizard completes, without relying on a page reload, and routes the new operator to login because setup intentionally creates no session. ASGI integration tests perform setup against isolated database and settings files, then immediately prove anonymous settings and backup downloads are refused; retryable failure paths remain recoverable, while an operator's explicit `require_auth=false` choice remains intentionally ungated.
- **The PII ratchet now also blocks ECM-shaped credentials, backed by a pinned general secret scanner (bead enhancedchannelmanager-h9av3, build 0064).** GitHub's free public-repository secret scanning covers partner/provider patterns but cannot enable its paid non-provider layer, so ECM's own credential formats previously had no preventive control. The existing added-lines guard now recognizes three deliberately precise classes: real-shaped values assigned to ECM's named credential fields (including the MCP `api_key` query form), complete Discord webhook URLs on every host accepted by Settings (`discord.com`, legacy `discordapp.com`, canary, and PTB), and exact-shape Telegram bot tokens. Names, declarations, documentation placeholders, low-entropy identifiers, and generic high-entropy strings do not fire this arm; precision is load-bearing because a noisy guard would be disabled and nothing GitHub-side replaces it. `detect-secrets==1.5.0` supplies the complementary generic/provider layer over changed files with network verification disabled. Its plugin/filter policy and suppression baseline are derived by freshly scanning the merge-base tree, so a PR cannot authorize its own secret, remove a detector, or weaken a threshold; the first-baseline bootstrap is accepted only when its hash-only baseline exactly matches that fresh base scan. Both layers report location and type only, never a value or length oracle; the ECM arm inherits added-lines behavior and fail-closed handling for invalid UTF-8/NUL content. Neither working-tree layer claims to scan history. The completed offline history sweep found two past MCP keys, confirmed both already dead by offline hash comparison without authenticating, and the PO ruled against rewriting history.
- **CI now rejects personal identifiers introduced on added documentation or bead-export lines without republishing them in public logs (bead enhancedchannelmanager-il1xz, build 0063).** The guard covers the identifier classes observed in the repository's PII sweep: personal email domains, real-user home paths, private-share hosts, and a hash-only deny list for known account terms. Hash creation and scanning share one account-token grammar, including hyphen, underscore, and dot separators, without matching inside an alphanumeric component. It deliberately ratchets only added lines, so accepted historical debt and untouched lines do not block unrelated work, while moved or rewritten text is checked again. Reports contain only path, line, rule, and the constant `REDACTED`; they disclose no value fragment or exact length. In-scope invalid UTF-8, NUL/binary, or unreadable files fail closed with path and `unscannable` only. Documentation examples, service identities, placeholder paths, and bead identity fields are precisely allowlisted; source trees remain out of scope because their synthetic-fixture density would make this control noisy. The existing history is not rewritten.
- **`POST /api/backup/restore-initial` no longer lets an anonymous caller take over an instance that already has an admin account (bead enhancedchannelmanager-lf29s, build 0054).** The endpoint restores a legacy ZIP during first-run setup, which means it rewrites `/config/journal.db` wholesale, and `journal.db` holds the `users` table. It sat in `main.py`'s global auth-middleware exemption list with no auth dependency of any kind, and its only guard was `settings.is_configured()`, which reports whether a Dispatcharr URL and credentials are present. That is a Dispatcharr-configuration check standing in for an authentication check, and the two conditions are unrelated. Any instance whose Dispatcharr connection was not yet configured therefore accepted an unauthenticated ZIP upload that replaced every admin password hash and cleared the `tls`, `uploads/logos` and `m3u_uploads` directories, after which the attacker logged in with credentials of their own choosing. The exposed states are a fresh container and, more importantly, a disaster-recovery rebuild sitting empty waiting for its restore, a state this project enters deliberately during backup and restore drills. Reproduced end to end on a disposable instance before the fix: with authentication enabled and enforcing (anonymous `GET /api/settings`, `GET /api/backup/create` and `POST /api/backup/restore` all answering 401), an anonymous `POST /api/backup/restore-initial` returned 200, the real operator's login then failed with "Invalid username or password", and the uploaded account logged in as admin. The endpoint now refuses anyone but an authenticated human admin once the instance holds an operator identity, and the check keys on whether a user row exists rather than on the `setup_complete` configuration flag, so damaged or interrupted configuration persistence cannot reopen the route. A genuinely empty instance still restores without credentials, because serving that case is the endpoint's reason to exist. The static MCP service key is rejected as well, matching `POST /api/backup/restore`. The path is also removed from the middleware exemption list, which costs no first-run reachability (the middleware only enforces once `setup_complete` is true, and the handler already refuses an anonymous caller in that state) and turns the anonymous upload away before FastAPI spools an attacker-controlled ZIP to disk. The frontend needed one change to match: `ProtectedRoute` re-reads `GET /api/auth/status` when the setup wizard reports completion, instead of deciding what to render from the status it cached at mount. `POST /api/auth/setup` issues no session cookie, so the status cached before setup still says setup is incomplete and would render the app with no session. The operator now lands on the login page immediately after creating the admin account, and the restore they run after signing in carries their session. Both the failure and the fix were reproduced in a browser on a disposable instance with no reload between finishing setup and clicking restore.
- **Renaming a code file into a `.md` path no longer hides it from the CI changed-path classifier, so a pull request that deletes code can no longer classify documentation-only and pass every required check having run nothing (bead enhancedchannelmanager-9ogyd, build 0061).** The `detect` job in `test.yml`, `build.yml` and `docs-pages.yml` built its changed-file set with the jq expression `.[].filename`, and `filename` on the GitHub pull-request and compare APIs is the rename DESTINATION only. The source path lives in `previous_filename`, which the expression discarded, exactly as `git diff --name-only` with default rename detection prints only the destination. Verified on this repository's own commit `b47ced96`: `git diff --name-only b47ced96^...b47ced96` lists 18 paths, the same command with `--no-renames` lists 19, and the extra path is the rename source `frontend/src/components/settings/SecuritySettingsSection.css`. Because six of the seven required status checks gate every step on that classifier's `docs_only` verdict, and `dev` branch protection requires no reviews, `git mv backend/tests/unit/test_auth_middleware.py docs/legacy_auth_test_notes.md` titled as a documentation change presented one Markdown path, classified documentation-only, and turned all seven required contexts green without executing a single test, lint or CodeQL query. The three `detect` jobs now use `.[] | .filename, (.previous_filename // empty)` so both sides of a rename reach the classifier. The same omission bit `docs-pages.yml` in the opposite direction: moving a page out of `docs/user_guide/` showed only the destination, so the published site did not rebuild and the removed page lingered. No job name, workflow trigger or required status check name changed, and the classifier script itself was not touched. `backend/tests/unit/test_classify_changed_paths.py` now runs each workflow's real jq expression over a recorded API payload from `b47ced96` and over the rename-into-Markdown reproduction, and fails if either side of a rename is dropped again.

### Added

- **Staged Edit Mode work now survives your session expiring, instead of dying with it (bead `enhancedchannelmanager-jazna`, epic `enhancedchannelmanager-r93hq`, build 0111).** Every other way out of Edit Mode now asks before it discards staged work: in-app navigation, browser **Back** and **Forward**, and **Sign Out**. One way out could never be guarded, because the app is not what leaves. The *session* does. A token refresh fails, or the server stops recognising your sign-in, ECM swaps itself for the login page, and the staged ledger, which lives only in memory, goes with it. There is nothing to ask, either: applying at that moment is impossible, because the session that would authorise the writes has already ended. Staged work is now saved when this happens, and you are **offered it back** when you sign in again, with an account of anything that no longer applies. It is offered rather than restored automatically on purpose: part of a saved session can fail its freshness checks, and being dropped straight into Edit Mode holding a quietly smaller ledger, with **Apply All** as the obvious next action, is worse than being asked. A **Restored** badge then sits in the Edit Mode header for the rest of the session, because a restored change count reads identically to a fresh one and that difference is exactly what decides whether applying is safe. **Four limits, all deliberate, and the first is the one that matters.** Staged work is bound to the account that staged it: if a *different* person signs in on that tab, the work is **destroyed rather than offered to them**. Two people sharing a workstation is the whole risk. Handing A's staged edits to B means B applies a few hundred channel changes under their own credentials and the Journal records every one of them as B's, which is an audit trail naming the wrong person and therefore worse than none. It also **dies with the tab**, so it will not follow you to another browser or machine; it **expires after 12 hours**, long enough for an interrupted working session and short enough that a lineup which has had days to move is never applied against stale assumptions; and **a duplicate channel number you already confirmed may be checked again**, because your confirmation named the channels that were on that number, and if that set changed while you were away the confirmation no longer describes the collision you agreed to. The restore dialog names those upfront. Nothing about a saved ledger is trusted blindly: the baseline it compares against is deliberately *not* saved, so a restored session checks against today's lineup rather than yesterday's.

- **Apply now checks the channel numbering you would end up with, not just each change on its own (beads `enhancedchannelmanager-ic884.2`, `enhancedchannelmanager-ic884.5`, build 0111).** Validating staged numbering changes one at a time misses the collisions that only their *combination* produces: two edits that are each fine, a create and a range renumber that quietly land on the same number, a move that vacates a number a later change was counting on. ECM now builds the complete lineup your staged work would produce, server state plus every staged create, edit, move, delete and range renumber, and validates *that* before anything is written. A conflict stops the apply with the number and the channels named, and **nothing reaches Dispatcharr**, so you are correcting a plan rather than repairing a half-applied one. The check runs on both sides: the browser's is the one that can name the staged operations you recognise, and the server's covers any caller, including scripts and MCP agents that never touch the interface. Range renumbering, clearing a number, and numbering-driven renames all gained their own guards in the same pass. A range is validated for its start, its end, its count and its final-state overlap before it is staged at all. **Clearing a number is now treated as a deliberate act rather than an empty field**, and if the channel's *name* carries that number you are warned that clearing it leaves the number stranded in the name, because automatic renaming only rewrites a name when there is a new number to write. And every rename that a numbering change would cause is now **listed in the Apply dialog before you commit**, one before-and-after line per channel, so no rename arrives as a surprise. Those renames are explained, not double-counted: they were always part of the change total, and the list only says which ones they are. **What this costs you:** an apply that used to go through and fail partway now stops beforehand and asks you to fix the plan. On a batch ECM cannot check at all, because it could not read the current lineup, a third-party caller gets a refusal by default rather than a guess.

- **ECM warns you before applying over a channel number somebody else changed while you were editing (bead `enhancedchannelmanager-ic884.4`, build 0111).** An Edit Mode session captures the lineup when it starts. If another operator, or Dispatcharr itself, moves one of those channels in the meantime, applying your staged work used to silently overwrite their change. Before Apply writes anything, ECM now re-reads the channel list and compares it against the baseline your session started from, scoped to the channels this session will actually write to. Where a number moved underneath you, a **Channel Numbers Changed While You Were Editing** dialog opens *instead of* committing. Nothing has been applied at that point, and everything else you staged is untouched. For each affected channel it shows what it was on when you started, what it is on now, and what your change would put it on, and you choose **Use my number** or **Keep the server's**. **There is no pre-selected option and Apply stays disabled until you have answered every row**, because a pre-ticked "keep mine" is a silent overwrite wearing a checkbox. Two consequences of keeping the server's number are worth knowing before you meet them. **A range renumber is dropped whole, never trimmed:** its numbers run in sequence, so surrendering one channel in the middle would shift every channel after it onto numbers you were never shown, and ECM would rather drop the renumbering and say so. **An automatic rename goes with the number it came from**, so surrendering a number also surrenders the rename ECM generated from it, and you are not left with a channel called `150 | Alpha` sitting on 199. A name you typed yourself is never touched. Your decision is bound to the values it was made about: if the same channel moves *again* before the apply lands, you are asked once more rather than having a stale answer applied on your behalf. **Be clear about what this is: a check, not a lock.** It closes the long window between your browser reading the lineup and the write going out. It cannot close the last one, because Dispatcharr 0.28.x has no conditional update to send. That was measured rather than assumed, and the finding is written down in `docs/dispatcharr_api.md`.

- **Bulk commit gains three operation types, and four response fields that say what actually happened (beads `enhancedchannelmanager-kz089`, `enhancedchannelmanager-r9py9`, `enhancedchannelmanager-e9e5o`, `enhancedchannelmanager-ic884.3`, `enhancedchannelmanager-ic884.4`, build 0111).** `POST /api/channels/bulk-commit` now accepts `setProfileMembership`, `restoreChannelGroup` and `clearStreamStats`, which is what lets the interface stage three actions that used to write straight through. On the way back, the envelope gained `journalRowsUnwritten` (also on `PATCH /api/channels/{id}`), which is non-zero when your changes landed and their record did not, so those operations must not be retried; `numberingRecovery`, which names each channel a half-applied numbering plan could not put back along with the exact step that fixes it; and an `applied: true` flag on an error entry, meaning that operation's write **reached Dispatcharr** and only ECM's own bookkeeping afterwards failed. That last one matters most to anyone writing against this API: an entry carrying it is counted as applied and never as failed, precisely so an integrator does not retry it and create the same entity twice. That is enforced rather than promised, in one place, by an audit that raises on a self-contradictory envelope instead of logging one. Two request-side fields are new for the same reason: `acknowledgedDuplicate`, which records consent to a *specific* collision and is checked against the channels that consent actually named, and `expectedNumber`, which refuses to write a number over a change the caller has not seen. `docs/api.md` documents all of it, including two things that were true before and had never been written down: the default response to a bulk commit is a **202 job handle** rather than the result envelope, and `updateChannel`'s `data` is an unvalidated field bag forwarded to Dispatcharr wholesale, which is a documented residual tracked as open bead `enhancedchannelmanager-t683u`.

- **REST and MCP callers can now tell a failed normalization from a normalization they never asked for (bead `enhancedchannelmanager-e9e5o`, build 0111).** A caller sending `{"name": "US: CNN", "normalize": true}` to `POST /api/channels` used to get a warning in the container log, a raw name, and a `200` that was observationally identical to `normalize: false`. The capability it asked for had not happened and nothing in the response said so. Two additive fields close that. `POST /api/channels` gains a `normalization` object, present when and only when the request asked for `normalize`, carrying `requested`, `applied`, `nameApplied` and `error`; `applied: false` means the engine did not run and `nameApplied` is the name exactly as you sent it. `POST /api/channels/bulk-commit` gains a `normalizationFailures` list in its envelope, always present and empty on a clean batch, with one entry per `createChannel` operation that asked for normalization and did not get it (`tempId`, `name`, `nameApplied`, `error`). Both are additive in the strict sense: a caller that never sends `normalize` receives a byte-identical response body, and detection is a positive signal rather than a probe for a key that may not exist, so branch on `applied === false` or on a non-empty list. A normalization failure does not fail the operation or the batch. The channel is created, under the raw name, which is exactly why the response has to say so, since the result is otherwise indistinguishable from `normalize: false`. The MCP `create_channel` and `bulk_commit_channels` tools render both, because a tool that receives the indicator and drops it leaves its own caller as blind as before. The Channel Pipeline's auto-create had the same swallow with a disclosure channel already beside it, so its normalization failures now appear in the execution log's action details next to the name-transform error. Documented in `docs/api.md`.

- **Post-merge publish check, so a merge that never reached the registry stops being invisible (bead enhancedchannelmanager-t8fqg, build 0044).** The published `:dev` tag has silently lagged the `dev` branch four times, from four unrelated causes: a Buildx flake that skipped the multi-arch manifest, a GitHub Actions outage that orphaned queued runs, and two test flakes that correctly gated the publish. Every time the gate behaved correctly and nothing surfaced the drift; on the most recent occasion (PR #793) `dev` carried the fix and the registry did not, for about five hours, until an unrelated merge republished it by accident. `scripts/check_publish.py` closes that gap. It confirms the **Build and Push Docker Image** run for a commit concluded `success`, and reads the published tag's `ECM_VERSION` build marker straight out of the registry's image config (no layer download) to compare it against `frontend/package.json` **at that commit**. `docs/shipping.md` section 6 now ends at a verified image rather than at the merge. It is deliberately **not** a CI gate: a check that runs after the merge cannot gate the merge it follows, and wiring it into the PR flow would add a permanently-failing context to every open PR. Run before a merge it prints a `PRE-MERGE RUN` banner explaining that the mismatch is expected. The restore drill gains a matching Step 0 image gate that reads the same marker with `docker rmi` / `docker pull` / `docker inspect`, for operators without repository access.
- **Em-dash lint ratchet over documentation prose (bead enhancedchannelmanager-3tflw, build 0044).** `docs/style_guide.md` bans the em-dash in prose and had no enforcement, so two documentation passes shipped 144 violations through two merged PRs without three reviewers noticing. `scripts/check_em_dashes.py` now runs as a step of the **Operator Docs** CI job, in both `test.yml` and `docs-only-pass.yml` (the latter is the copy that runs on a Markdown-only PR, which is exactly how the 144 arrived). It is a **ratchet, not a cliff**: it fails only on lines a PR adds, so pre-existing debt is tolerated and reported as a count rather than red-lining CI on day one. It honours the exemptions the rule itself grants: fenced code blocks, inline code spans, link destinations, and URLs in Markdown. Only U+2014 is flagged; en-dashes (`–`) and arrows (`→`) are not em-dashes and are never touched. A genuinely-quoted line can be suppressed with `em-dash-ok: <reason>`, mirroring the existing `fake-test-ok` convention. **Scope: documentation only.** The rule's text also names code comments, but the PO scoped automated enforcement down to Markdown before this shipped: scanning Python and TypeScript comments would have caught 9,211 and 1,551 violations respectively, against 1,271 across the 188-file Markdown scan surface, and the PO judged that surface disproportionate to the risk the guard exists to close. The code-comment clause remains a style-guide convention enforced by review, not by CI.
- **Test Connection now tells you when you're on a Dispatcharr version ECM hasn't been tested against, and every Dispatcharr endpoint ECM calls is now checked against a recorded copy of the real API (bead enhancedchannelmanager-ax0kf, [ADR-014](docs/adr/ADR-014-dispatcharr-api-drift-strategy.md), build 0022).** Three shipped bugs — a schema endpoint that returned YAML (`q6xjl` A), a settings detail route keyed by the wrong field so a restore 404'd on 7/7 keys (`q6xjl` B), and `/api/dvr/rules/`, a path that exists on no Dispatcharr version and silently emptied every DVR-rules backup (`lsa0s`) — all trace to one deferred "verify this later" comment in ECM's Dispatcharr client. Each was found by an operator, never by a test. Two things change. **(1) A contract sweep.** Every `(method, URL template)` the client can issue — 98 call sites over 59 distinct URL templates — is now derived from the client's own source with an `ast` walk and checked against a recorded manifest of the live Dispatcharr 0.28.2 OpenAPI document (all 224 paths, bodies stripped; `scripts/record_dispatcharr_openapi_manifest.py` records it, and never touches the network at test time, so CI stays hermetic). It asserts the path exists, the method is allowed, and the path parameters line up — **not** response shape or semantics, which remain the job of the existing deep recorded fixtures; a green sweep means "no endpoint is imaginary," not "the integration works." An unresolvable path expression, a new raw HTTP call site, or an extractor that stops finding calls all fail the test loudly rather than quietly reducing coverage. **(2) A version advisory.** The **Test Connection** button now also reads `GET /api/core/version/` after the credentials check out, and if the reported version is outside the set ECM's contract is recorded against (`0.28.x` today), it shows a warning notification naming the version. **It never blocks:** the connection still verifies, the settings still save, and a Dispatcharr too old to have the version endpoint — or any timeout or unreadable answer — produces silence rather than a nag you can't act on. Also documented: a re-record recipe in `docs/dispatcharr_api.md`, and an operational rule that a Dispatcharr **major**-version upgrade should be followed by a real backup→restore exercise before you trust it.
- **Restore runbook gets a "Recurring maintenance" section for the highest-care `dbas-backup` component (bead enhancedchannelmanager-nvhg7, build 0021).** `COMPONENTS.md` has tagged the backup/restore subsystem "restore must stay periodically exercised" since onboarding, but nothing prompted or enforced it — the only round-trip exercise this pipeline had ever received was a manual one-off. `docs/runbooks/disaster-recovery-restore.md` now documents a monthly dry-run-preview habit against the actual most-recent scheduled artifact: what to check in the report (category counts, FAILED rows, the settings-key `DEPENDENCY_UNRESOLVED` signal from build 0015, the degraded-backup warning from build 0019), and to file a bead rather than silently re-run on a surprise. Home-lab tier: documentation, not enforced infrastructure — a future CI-wired exercise is noted as a possible path (beads `1zwmr` / `zqtjj`) without implying it's committed. Cross-referenced from the operator guide's `verify-a-backup.md`.
- **Docs: 21 new operator-guide articles land, covering Channels & Streams, Normalization, Notifications, and Troubleshooting (bead enhancedchannelmanager-sbtyw, build 0012).** New articles: `channels-streams/{assign-streams-to-channels,channel-groups-and-tags,logos,streams-overview,the-journal}.md`; `normalization/{apply-to-existing-channels,author-your-first-rule,concepts,condition-and-action-types,rule-groups-and-ordering,when-things-look-wrong}.md`; `notifications/{alert-routing-patterns,discord-webhook-customization,email-recipients-deep-dive,telegram-bot-setup}.md`; and `troubleshooting/{common-issues,escalation-paths,gather-support-information,read-the-logs,recovery-patterns,ui-banners-and-warnings}.md`. Their 24 paired screenshots were individually PII-audited and cleared under bead enhancedchannelmanager-2mluw before landing here; no second audit was performed. `channels-streams/the-journal.md` and `troubleshooting/read-the-logs.md` unblock documentation defects DEF-017, DEF-018 and DEF-020, which could not be fixed while the articles they reference existed on no branch.
- **Startup now warns when your ECM data has nowhere durable to live (bead enhancedchannelmanager-ebl4n, build 0004).** Preflight has always reported `✓ Config directory exists` / `✓ Config directory is writable` — both true even when `/config` is just the container's own writable layer, with no bind mount and no named volume behind it. That is exactly the configuration the operator in the field case above was running, and it means every byte of ECM state (`settings.json`, channel data, `journal.db`, uploaded logos, TLS certificates) is destroyed the moment the container is removed or recreated — including by a Portainer/Unraid/Watchtower "update" and by the `--force-recreate` this release otherwise recommends. Preflight now detects whether `CONFIG_DIR` (or any parent of it) is actually mounted, by reading `/proc/self/mountinfo`, and prints a loud multi-line warning when it is not: what is at stake, what destroys it, how to add a mount, and to take a backup **before** recreating. The warning is advisory and never blocks startup — a first-run or throwaway container without a mount is legitimate — and a correctly-mounted container gets a single `✓ Config directory /config is on a mounted volume` line instead.
- **Cross-instance sync: opt-in logo replication completes the entity slice (bead enhancedchannelmanager-7ipq2.1, build 0001).** Channels + embedded streams already sync every cycle (kcxie); this adds the LOGO slice as a per-`SyncTarget` opt-in (`sync_logos`, default OFF — API/DB flag, migration 0040), implementing ADR-013 S9's exit path rather than forcing logos into the unconditional per-cycle set. When enabled, each cycle 3-tier-matches source logos (files under `uploads/logos`, correlated to the source Dispatcharr logo id/name by URL basename — the same enumeration the backup builder archives) against destination B and streams only the MISSES: the sync plan carries metadata-only logo records (never bytes), and the reused logos importer gains an optional lazy `content_provider` that reads one missed logo's file at a time immediately before validation + upload (D8 — a matched logo never reads its file; the archive-restore path is unchanged). The sync path can never bulk-delete B's logos: the destructive `clear_existing` pre-step is hard-disabled in the sync step, pinned by a stateful round-trip test that also proves convergence, second-run idempotency (zero re-uploads), and that a B-only logo survives. All four security validations (basename/traversal guard, declared+decoded 50MB caps, magic-byte MIME check) run before any upload, provider failures degrade to per-logo path-free VALIDATION_ERRORs, and the run's `sync_outbound` journal row now lists the categories the run actually processed.

### Changed
- **Restoring a backup no longer signs you out of your own instance, and now tells you what the backup could not carry (bead `enhancedchannelmanager-gi4zn`, build 0113).** Because a standard backup no longer contains any ECM accounts, restoring one had to stop meaning "replace the accounts on this instance with the ones in the file". ECM now takes a snapshot of the destination's own accounts before it writes the restored database and puts them back afterwards, so an admin restoring a **Full Backup (legacy .zip)** onto a running instance stays signed in. An instance that genuinely has no accounts reinstates nothing, which keeps the disaster-recovery path working: you land at first-run setup, create an admin, and sign in. A side effect worth knowing is that a backup's account list can no longer silently overwrite a live one. **What is new on screen:** the restore result now carries **notices** describing what the artifact could not bring with it, because the "files restored" count reports what landed and by its nature cannot report what was never there. One notice explains the first-run-setup outcome above. The other names any configured surface this instance had before the restore and does not have after, which for a standard backup means cloud storage targets, sync targets, M3U digest settings, or event-sync exclusions. Both are read from your instance after the restore rather than predicted from the file, so you are never told to go re-establish something you never configured. The **Restore DBAS Backup** flow does not write ECM's database at all and is unaffected in either direction.

- **Restore a new backup only with this ECM build or a newer one (bead `enhancedchannelmanager-gi4zn`, build 0113).** The full-redaction change did not move the backup's `schema_version`, because the file's structure is unchanged, so nothing refuses an older ECM being handed a newer artifact. Avoid that combination yourself. Restoring a new backup with an ECM that predates this change has three problems, all arriving behind an apparently successful restore, and none fixable after the fact: the older code has no step that preserves the destination's own accounts, so it can **sign you out of the instance you are restoring** and drop you at the setup wizard; alert-method usernames and Telegram chat IDs are written in as the literal text `***REDACTED***`; and an alert method whose configuration could not be read at backup time is left as that literal text in its entirety, so it stops sending notifications until you reconfigure it. If you need to roll ECM back, roll back to a backup taken **before** the upgrade rather than restoring a newer one onto the older build. The backup overview and the disaster-recovery runbook both carry this warning where a rollback or a cross-version recovery would run into it.

- **Channel numbers now have one written-down definition, and every place you can type one enforces it (bead `enhancedchannelmanager-ic884.1`, build 0109).** A channel number is a non-negative number with at most one decimal place, because that is Dispatcharr's resolution: only the tenths place carries meaning. Nothing in the stack checked that before. There is no ECM database column for `channel_number` at all (channels live in Dispatcharr, which declares the field a non-unique float and enforces neither uniqueness nor precision), the CSV importer accepted any positive float, and every UI field parsed with a bare `parseFloat`, while the channel list was already formatting numbers to one decimal place on the way out. A value like `1.05` could therefore be typed, imported, or posted, stored as-is, and then displayed as something it was not. The domain and its check now live in exactly one place per side, `backend/channel_number.py` and `frontend/src/utils/channelNumber.ts`, and every entry point consumes them: create channel, edit channel, the inline number editor in the channel list, merge, assign numbers, the edit-mode bulk commit (create, update and assign operations), CSV import and preview, and a Channel Pipeline rule's literal channel number. **Out-of-contract values are rejected, not rounded**, so a CSV import that used to succeed on a bad row will now fail on that row with the reason: surfacing bad data beats silently altering it. The message is the same sentence everywhere, browser or API: "Channel numbers must be 0 or greater and support one decimal place (for example 1.0 or 1.1)." Two long-standing quirks go with it: an unassigned channel's Edit Channel box showed the literal text `null`, and Merge Channels defaulted to `Infinity` when no source channel had a number. Uniqueness is deliberately NOT enforced, since Dispatcharr permits duplicate channel numbers and real lineups have them.
- **Changed-path gating now names and computes the safe decision positively (bead enhancedchannelmanager-gnz19, build 0067).** The shared classifier emits `code_paths_changed`, defaults it to `true` when input is absent, malformed or unreadable, and every workflow and local shipping hook consumes that exact key. The only false verdict is a change made entirely of approved root machine-generated beads state: `.beads/*.jsonl`, `.beads/*.jsonl.bak`, and the exact `.beads/metadata.json`. Every other path is conservatively code-gated, regardless of suffix, case, location, symlink type, or intended use. This intentionally removes documentation and directory-prefix optimizations: reliably proving an arbitrary file inert would require observing every present and future consumer, while treating it as code costs only CI time. Beads configuration, nested artifacts, documentation, images, Graphify output, workflow support files, and undeclared paths all run the gates. Changed paths travel as JSON arrays from GitHub APIs and as NUL-delimited data from local Git, preserving rename sources, destinations, deletions and unusual POSIX filenames without newline splitting, whitespace stripping, or backslash rewriting; malformed or noncanonical input runs all gates. The independent `docs_site_affected` verdict is unchanged: published pages and `mkdocs.yml` still rebuild the site.
- **Claude Code's machine-local permission settings are no longer tracked (bead enhancedchannelmanager-pp84m, build 0065).** `.claude/settings.local.json` is now root-ignored and removed from repository tracking; shared project settings remain in `.claude/settings.json`. This maintained checkout's local file was explicitly preserved byte-for-byte during the branch transition, but Git may delete the formerly tracked file when another existing clone pulls or switches to the updated branch. Contributors with local permission changes should back up the file before updating and recreate it afterward if Git removes it. Once updated, each checkout's local allow-list stays untracked instead of appearing in unrelated application pull requests.
- **M3U account rows and EPG source rows now keep their action buttons at full size and move the setup actions into a per-row kebab menu (bead enhancedchannelmanager-xh33o, build 0058).** Every row action is meant to be a 32x32 target, but the buttons sat in a flex row inside a fixed-width column and were allowed to shrink to fit, so a busy row squeezed them well under the 24x24 minimum that WCAG 2.5.8 asks for: an eight-button M3U account row rendered its buttons 19px wide, and a five-button EPG source row 20.81px. The buttons no longer shrink, and because a row that no longer shrinks would instead be clipped by the list's own `overflow: hidden`, each row now shows only the actions its column actually holds. **M3U account rows** keep Enable/Disable, Refresh, Edit and Delete, and move Refresh VOD, Manage Groups, Manage Account Profiles and Manage Filters into the row's new `⋮` menu. **EPG source rows** keep Enable/Disable, Refresh and Edit, and move Copy URL and Delete into theirs. Nothing is removed; the moved actions are one click further away and are named in full text rather than by icon alone. The EPG Actions column widens from 120px to 144px to hold the four remaining controls, which widens the minimum row width from 880px to 904px before the source list starts to scroll horizontally. Both conditional actions (Refresh VOD appears only for XtreamCodes accounts, Copy URL only when the source has a URL and URL-hiding is off) now live in the menu, so the visible button count no longer varies by row.
- **Checkbox rows are the same height as radio rows (bead enhancedchannelmanager-4nn2o, build 0058).** A checkbox option carried 8px of vertical padding that its radio equivalent did not, so mixed forms rendered checkbox rows at 35.5px against the radios' 24px and lists of checkboxes sat looser than lists of radios for no reason anyone had chosen. The padding is gone and both now render at 24px. The clickable target does not shrink with it: the 24px floor is supplied by the shared rule that makes the whole label a target, not by the padding, and it still measures 24px on both.
- **User guide reworked to read as current operator documentation rather than a build-status page (build 0053).** The landing page (`docs/user_guide/index.md`) now opens with what ECM is and who the guide is for, plus a "Start here" table and sections grouped by task. The Stub / In-progress coverage markers are gone: they were inaccurate, labelling well-covered sections (Normalization, Troubleshooting) as stubs and an in-progress section (Backup & Restore) as complete. Two pages that were internal engineering material rather than operator documentation are removed: the restore-drill procedure (`run-a-restore-drill.md`) and the retained legacy task reference (`legacy-task-reference.md`). Their still-relevant content moved to the sections that own it: VLC protocol-handler setup, password reset, self-deactivation protection, stats auto-refresh, channel-pipeline orphan cleanup and exclusion filters, and a new "Restoring onto an already-populated target" section covering the relink-mode reference. A new page, `channels-streams/stream-preview.md`, documents the stream/channel preview modal. The guide now describes current 0.18.1 behaviour rather than carrying per-build history.
- **Restore drill repinned to run 18 / `0.18.1-0052`, six defects (D1-D6) and three documentation gaps fixed, and four other user-guide pages corrected for the staged-group fixes below (build 0052).** `run-a-restore-drill.md` carried six stale or self-contradicting claims found in doc-following mode: a still-live security-relevant transient the article's own Known-failures table already said was fixed; a quoted Delete-Group dialog string ("Ungrouped") the product no longer shows; a stale empty-state string for the EPG guide picker; a version pin five builds behind; a false claim that the restore-complete modal reloads the Channels pane automatically; and, the highest-value fix, a Step 2 stream-profile curl recipe missing `command`/`parameters`, which Dispatcharr silently accepts as an unrunnable profile and is what built the unplayable channel behind a false P1 filed during run 18. Also documented: the previously-unnamed `POST /api/backup/restore-dbas` endpoint for scripted callers, the single-channel "Move Channel to Group" drag path in `channel-groups-and-tags.md` (previously only the multi-select move was covered), and a Step 8 warning that `preserve` recreates a deleted archived group empty, so the baseline reset must happen before the second restore, not after. A follow-up pass then fixed four pages that still asserted "Create new channel group is not staged" (`channels-overview.md`, `bulk-edit.md`, `your-first-channels.md`, and `channel-groups-and-tags.md`), a claim the `vtapf` fix below falsified inside this same branch, which would otherwise have shipped a contradiction in one PR. Gates run clean: `scripts/check_em_dashes.py --base-ref origin/dev` (no new em-dashes) and `mkdocs build --strict` (no errors, no broken links).
- **Restore drill repinned to `0.18.1-0049` on the back of run 16, and two sentences that had become false are gone (bead enhancedchannelmanager-dqrjn, build 0050).** The article's opening admonition said the fresh-target round trip was last measured by run 12 on `0.18.1-0040` and the populated-target round by run 13 on `0.18.1-0042`, that "no functional change has landed in the restore path since `0.18.1-0042`", and that the round trip had never been re-measured on `0.18.1-0047`. Both of the latter claims stopped being true the same day. Drill run `2026-08-08-run16` drove a **full** round trip on the published `:dev` image at `0.18.1-0047`: both artifact variants reproduced the instance on a freshly-wiped target with **zero inventory-diff findings** (encrypted `outcome: success` with playback baseline-identical on the first restore; standard redacted `outcome: completed_with_failures` recovering in Step 6a's documented two steps, no third restore), the logo-failure round passed all five assertions with `logo_misses: 1` named and nothing rolled back, and the populated-target round showed `preserve` reporting 7 channel-group drifts and moving none against `overwrite` reporting the same 7 and moving all 7, with the drift detail visible on the dry-run preview before either apply. Then `0.18.1-0048` (`bdmby`) changed `backend/task_scheduler.py`, which **is** in the restore path, and `0.18.1-0049` (`o88e9`) changed the channel-group controls Step 8's construction drives. The admonition now pins `0.18.1-0049`, attributes every claim to the build it was actually measured on, and names the two builds of functional change that landed *after* the measurement rather than asserting there was none. Also recorded honestly: run 16 did not exercise `oebpv`, `sdpzy`, `cytzj` or `zt801` at all, and could not complete Step 8's outright-deletion or name-collision sub-constructions, because the `o88e9` defect dead-ended them, so name-collision handling stays proven by construction only as far as run 13's `0.18.1-0042`. `bdmby` and `o88e9` join Known failures with the build each was fixed in, and the running tally is recounted from fifteen beads to **seventeen**, sixteen of them fully fixed. Step 8's Construction step 2 gains a build-scoped note (completable inside ECM on `0.18.1-0049` and later; finish it in Dispatcharr on `0.18.1-0048` and earlier), and Improved reporting gains the `0.18.1-0048` behaviour change for anyone who waits for a restore by polling `GET /api/tasks/dbas_restore`. Documentation only.
- **Restore drill's version pin refreshed to `0.18.1-0046`, and the last 3 em-dashes in the backup-restore section are gone (bead enhancedchannelmanager-wkr09, build 0046).** `run-a-restore-drill.md`'s opening danger admonition still pinned `0.18.1-0040`, five builds behind `dev`. Rather than silently bumping the pin, and asserting claims measured on `0040` were re-measured on `0046`, the admonition now says what's actually defensible: the article is maintained against `0.18.1-0046` (Dispatcharr `0.28.2`), the build this review happened at, the fresh-target round trip was last measured by run 12 on `0.18.1-0040`, the populated-target round was last measured by run 13 on `0.18.1-0042`, and no functional change has landed in the restore path since `0042` (`git diff` over `backend/dbas`, `backend/tasks`, and `frontend/src/components` between those builds is empty), so both measurements still hold. The historical `Fixed as of 0.18.1-00XX` references elsewhere in the article are untouched. Separately, the three em-dashes in the backup-restore section left out of scope by the earlier cleanup (bead 5skhh, which covered `run-a-restore-drill.md` and `troubleshoot-restore.md`) are rewritten per `docs/style_guide.md`, one each in `index.md`, `restore-a-backup.md`, and `verify-a-backup.md`: each was an independent clause joined by an em-dash, now split into two sentences. `docs/user_guide/backup-restore/` is now at zero em-dashes. Documentation only.
- **Restore drill's "not yet proven" language for r1ei7, 3t74w and tddmw retired: run 13 proved all three live against a populated target (bead enhancedchannelmanager-rjroq, build 0045).** `run-a-restore-drill.md` said in four places that the `0.18.1-0041` fixes for those three beads had not yet been proven by a live drill run against a populated target. Run 13 (2026-08-07), against the published `:dev` image at `0.18.1-0042` (digest `sha256:75986c5eee0b80a47ade10e4132b126eb888381e069fbdc52e8f51642225c359`), ran a targeted Step 8 round on a target rebuilt from run 12's own verified artifact and measured all three live: `preserve` reported channel-group drift for all 7 affected channels and moved none, and `overwrite`, re-run from an identical baseline, reported and moved all 7; `already_exists_name_match` appeared exactly 376 times, matching the skipped channel-group count, while every other category still reported `already_exists_identical`; and the preview's `channel_groups` caveat and `Streams: not predicted` row both rendered. The opening danger admonition, the Step 8 introduction, the relink-mode comparison table's grouping-row note, and the r1ei7 row in Known failures are updated to cite run 13 and the `0.18.1-0042` pin. One assertion in Step 8's own checklist, deselecting the `channel_groups` category and confirming the "not checked" note, was found **not reachable from the current restore UI**: the DBAS restore modal exposes no category-selection control at either stage, and `GET /api/tasks/dbas_restore/parameter-schema` returns empty `parameters` and `run_parameters`. The article now says so plainly instead of instructing an operator to do something the UI doesn't expose. Run 13 was a targeted Step 8 round, not a full round trip; the fresh-target round trip remains run 12's result. Documentation only.
- **Backup/restore operator docs brought into line with the style guide's no-em-dash-in-prose rule (build 0043).** `run-a-restore-drill.md`, `migrate-to-a-new-install.md`, and `troubleshoot-restore.md` are edited for punctuation and sentence structure only: every em-dash in prose (introduced by PRs #793 and #794, plus two pre-existing ones from build 0040 in `run-a-restore-drill.md`) is rewritten per `docs/style_guide.md`'s prescribed strategies, independent clauses split into sentences, appositives recast as colons or parentheses, trailing elaborations split into new sentences. No factual claim, version pin, bead id, or hedge changed. Documentation only.
- **Restore drill gains an executable populated-target round, Step 8 (bead enhancedchannelmanager-z9xen, build 0042).** The article's executable procedure was Steps 1-7, all against a freshly-wiped target; "Restoring onto a populated target" was reference material describing behaviour, but no step ever told a drill-runner to construct a populated target and test it, so a drill following the article never exercised that path. Step 8 now has an operator construct the divergence (rename an archived group; delete another after moving its channels out, since Dispatcharr refuses to delete a non-empty group; create a name collision with a different id and different members; diverge one channel's logo and clear another's EPG link so the relink modes are distinguishable) and restore twice from an identical baseline — preserve, then overwrite — asserting by measurement rather than assumption: drift reported non-zero and nothing moved under preserve; reported and moved under overwrite; the dry-run preview showing the drift count and its channel_groups caveat; `already_exists_name_match` for channel groups versus `already_exists_identical` for profiles; and playback verified afterward. Step 8 is what will let a future drill run prove enhancedchannelmanager-r1ei7, -3t74w and -tddmw (fixed in 0.18.1-0041) against a live populated target — this change adds the procedure, it does not itself run it; the article's existing "not yet proven by a live drill" language for those fixes stays true and stays in place until such a run happens. Documentation only.
- **Restore drill's playback verification timeout raised from 40 to 60 seconds, now measured across three runs (build 0040).** `run-a-restore-drill.md` Step 7 previously told operators to use a 40-second hard timeout on the first post-restore playback fetch, sourced from a single measurement (finding D-6, drill run `2026-08-06-run9`, where a 25-second deadline returned 0 bytes and 40 seconds passed). **`2026-08-06-run10-verify` is the run that shows 40 seconds itself is insufficient: a 40-second deadline returned 0 bytes on the first post-restore fetch, and the identical fetch passed at 60 seconds** (run 10's report initially mis-transcribed this failing deadline as 25 seconds and has since been corrected at source). `2026-08-07-run11-published`, against the published `0.18.1-0038` image, used a 60-second deadline throughout and passed — corroborating that 60 seconds works, though it did not retest 40 seconds itself. 40 seconds is confirmed too tight on a cold first fetch and would have caused an operator calibrating on that figure to file a false playback failure — the exact trap the timeout guidance exists to prevent. The article now documents 60 seconds as the floor (stated as what has been observed to work on this drill's host and provider, not a guarantee), explains why the first fetch is slower (a fresh upstream connection to the provider), and states explicitly that a timeout hit before any bytes arrive is inconclusive on a cold fetch and should be retried at a longer deadline before concluding playback is broken. The existing guidance that a read-deadline hit *after* bytes have started arriving counts as a pass is unchanged. Documentation only.
- **Backup/restore docs re-aligned against a live round-trip drill on build 0035, run in doc-following mode (bead enhancedchannelmanager-mlb9v, build 0037).** Drill run 9 held only the published site, no repo access, and found eight documentation defects. The load-bearing one: this article told operators to expect every EPG link to be lost on a restore, in three separate places, when run 9 measured 9 of 9 seeded links surviving on both artifact variants; that guidance is corrected in `run-a-restore-drill.md` and `migrate-to-a-new-install.md`, and the underlying bead (`dfkbn`) is closed. Also corrected: Step 3's status check pointed at the Saved Backups panel, which has no status field at all; the check actually happens in the Notifications panel, now documented. Step 2's seed table named a custom stream profile and user agent without saying where to create them; both are Dispatcharr objects with no create UI on 0.28.2, so the article now documents the REST API path the drill had to use. The playback assertion's "hard timeout" now carries a figure (40 seconds), after a 25-second deadline read a genuine restore as failed. The two backup artifacts share one filename pattern with nothing to tell them apart; the article now promotes the existing `BadZipFile` check to the actual identification method. The `parameter-schema` endpoint warning is retired: as of build 0035 the endpoint documents the encrypted-backup options correctly. The article's own version banner is repinned from `0.18.1-0023` to `0.18.1-0036`, the build these fixes ship in (build 0037, this docs-only PR, does not itself change runtime behavior). Also folded in as measured, not just expected: the genuine non-fatal-logo-failure path was exercised live for the first time and behaved correctly, and the three restore-reporting fixes from PR #788 (`enhancedchannelmanager-k2r7m` logo-miss double-count, `enhancedchannelmanager-cwmid` inverted degraded-restore alert severity, `enhancedchannelmanager-d0bd3` restore-complete UI omitting the no-playable-stream panel) are documented in Known Failures and Improved Reporting. Documentation only.
- **Backup docs no longer tell you to delete your uploaded logos before taking a backup (beads enhancedchannelmanager-xb58a + enhancedchannelmanager-d0agi, build 0035).** Three backup/restore articles carried a prominent warning that a logo uploaded through ECM's own Logo Manager would abort and roll back an entire restore, and that the only way to complete one was to **delete those logo records before backing up** and re-add them by hand afterwards — accepting permanent loss of the logos. That stopped being true in `0.18.1-0024`: uploaded logos have their image bytes archived in the backup and restore intact, and a logo that cannot be restored is counted and named rather than rolling anything back. The warning had gone stale in the operator's *disfavour*, advising them to throw away data the product now preserves correctly, and is replaced with what actually happens. The old behaviour is retained as a version-scoped note for anyone still running an earlier build. The restore drill's own guidance is inverted to match: uploading a logo is now the useful thing to seed, because it exercises a path earlier runs could not reach. Documentation only.
- **Backup & restore troubleshooting docs re-aligned with what builds 0029–0033 actually do (bead enhancedchannelmanager-mlb9v, build 0034).** Five defects found by the live restore drill have since been fixed, and the operator articles still described the broken behaviour — telling people a redacted restore needs three steps when it now needs two, that a preview's health counters cannot be trusted when they now match the apply, and that a repeat restore can silently drop a stream slot when it no longer does. Every stale claim is now version-scoped rather than deleted, so operators on an older build still get the guidance that applies to them, and the advice that outlived its example — verify playback rather than trusting a summary counter — is kept as durable practice. The two-step recovery is documented with its real coverage limits: it is the individual-account **Refresh** and the scheduled refresh task that reattach channels; a refresh-all, or one performed in Dispatcharr's own UI, is picked up on the next scheduled refresh instead. Documentation only.
- **Backup & restore documentation corrected against a live round-trip drill on build 0027 (bead enhancedchannelmanager-zimw5, build 0028).** The run-4 restore drill measured published `0.18.1-0027` against Dispatcharr 0.28.2 and found four operator-facing articles stale or silent. The load-bearing fix: *Troubleshoot a Restore* told operators that re-running a restore "hits the same collision and leaves the same slot behind" for a name-collision placeholder. It does not — the second run clears the slot by **dropping** it (3 streams to 2), silently reorders the survivors, leaves the still-working stream unbound, and reports `outcome: success` with empty `notes[]`. Also newly documented: a second restore's health counters can read `0` while a channel returns HTTP 500 (with the confirming `unique_channel_stream` log check); the dry-run preview's **category** counts are trustworthy while its **health/split** counters under-report on a fresh target (with the populated-target contrast, so operators do not over-correct); an ECM-uploaded logo's bytes live in Dispatcharr's `/data/logos/`, not ECM's `/config/uploads/logos/` as the overview claimed; and authentication turns itself on the first time the ECM web UI is opened, which strands anyone scripting a restore against a new install (stated as observed, not proven). Documentation only — no runtime behaviour changes.
- **Cross-instance sync targets now sync independently and concurrently (bead enhancedchannelmanager-7ipq2.3, build 0003).** Implements ADR-013 S6's per-target trigger model: each `SyncTarget` now owns its own registered task (`dbas_sync_<target_id>`, registered dynamically at startup and on target create/rename/delete) instead of v1's single shared `dbas_sync` id. Under the shared id, one slow or unreachable target B serialized — and, when two targets' schedules came due in the same scheduler tick, silently swallowed — every other target's run; distinct targets now run concurrently while the task engine's per-task_id `ALREADY_RUNNING` guard becomes a true per-target lock (a second run against the *same* B is refused non-silently and retried on its next interval). Concurrency is bounded by a small cross-target cap (`ECM_SYNC_MAX_CONCURRENT`, default 3; excess runs queue rather than being dropped). Per-target registry instances remove cross-target parameter leakage structurally, the 7ipq2.2 one-shot disarm now resets to the *bound* target id (extended red-first to the concurrent case), and a run parameter naming a *different* target than the task is bound to hard-fails without running. Existing `dbas_sync` schedule rows are migrated to their per-target id at startup (rows referencing a deleted target are disabled with a warning, never migrated to a dead id; alert/notification preferences of the legacy shared task row do not carry over — reconfigure per target if you had customized them). Observability sharpens with the id change: `ecm_task_schedule_last_success_timestamp` now tracks each target separately, so per-target drift is visible where the shared id let one healthy replica mask another's — and both sync signals are now keyed per target (the `ECMSyncStalledTargetDrift` alert and `ecm_sync_runs_total`; see the next entry for the exact metrics, which changed in the same release — update your Prometheus rules copy). Per-target sync task ids inherit the same admin gate as the DBAS backup/restore tasks, and the Scheduled Tasks UI now lists one sync task per target (per-target schedules, history, and alert toggles). The "Sync now"/Apply buttons target the per-target task id, and the card now tracks in-flight state per target (previously one scalar marker, so starting a second target re-enabled the first target's row mid-request) — deploy backend and frontend together.
- **Cross-instance sync drift alerting is now apply-only and per-target, and the run counter names the target (bead enhancedchannelmanager-7ipq2.3, build 0003).** Two observability corrections that ship with the per-target work. (1) `ECMSyncStalledTargetDrift` now keys on a new `ecm_sync_last_full_success_timestamp{sync_target_id}` gauge, stamped only by a run that actually APPLIED and returned a clean SUCCESS. It previously used the generic `ecm_task_schedule_last_success_timestamp`, which the task engine stamps on ANY successful run — and a dry-run preview succeeds without writing anything to B, so a recurring preview reset the drift clock while the replica kept diverging. The generic gauge remains as the "is this task running at all" signal; the runbook and SLO doc now state the distinction explicitly. (2) `ecm_sync_runs_total` gains a `sync_target_id` label (the SyncTarget pk — immutable, so renaming a target cannot fork the series), so the runbook's "B unreachable vs applies-but-half-fails" triage step can name WHICH replica is failing now that targets run concurrently; both sync signals are keyed on the same target identifier. Operators with a copy of `docs/sre/prometheus_rules.yaml` must take the updated rule, and any saved dashboard query against `ecm_sync_runs_total` should add a `sync_target_id` filter (an unfiltered query now sums across targets). Also fixed in the same pass: a legacy `dbas_sync` schedule that was actively firing before the upgrade kept firing after it — the migration now preserves the effective enabled state onto the new per-target task (previously the re-keyed schedule landed on a default-disabled parent and silently stopped running), and startup registration never moves an *existing* per-target task's enabled gate in either direction — an enabled sync survives every ordinary container restart, and one an operator switched off is never quietly switched back on — and the live-B validation recipe in `tests/dbas-test-env/README.md` was updated to the per-target endpoint and metric labels.
- **The "Update available" header pill is gone — the same notice now lives in the notification centre, and the container needs GitHub egress to produce it (bead enhancedchannelmanager-nhkd4, build 0005).** The version-update check moved from the browser to the backend: previously only your browser talked to GitHub to check for a new release; now the container does, on a periodic timer and at startup. **Operationally, this means the container now needs outbound network access to GitHub** — if it doesn't have any, the check fails soft (a bounded HTTP timeout, a WARNING log line, previous state left alone) rather than blocking startup or crashing the check loop. The information itself hasn't changed — "a newer version is available" — but it's a notification-centre entry now instead of a pill in the header, and it retires itself automatically once you update, rather than continuing to advertise a version you've already moved past. Moving the check server-side also closes a race across multiple open browser tabs, since the backend is now the single writer.
- **Type scale unified across all ten route pages and the entire modal estate (82 dialogs), and the app's chrome is visibly tighter (beads enhancedchannelmanager-6z299, enhancedchannelmanager-xhldy, enhancedchannelmanager-f4yc7, enhancedchannelmanager-99o0x, enhancedchannelmanager-vh6hh, enhancedchannelmanager-9bpkk, enhancedchannelmanager-sl7dx, enhancedchannelmanager-57pp3, build 0005).** Route pages and dialogs had drifted onto inconsistent, overlapping font sizes — body text, item titles and section headings scattered across 12-18px — because shared CSS classes were being redeclared per page instead of owned by one shared layer, so the same class could render a different size depending which tabs you'd visited that session. Everything now maps onto a small set of named type roles (metric / section / body / item-title / meta / micro / badge) defined once. Visible consequences: the top bar shrank 75px → 45px, dialog header bands shrank 65px → 49px (dialog titles 18px → 16px), route header bands are tighter across all eleven routes, Stats panel headings read sentence case instead of ALL-CAPS, and the notification centre's text — previously inheriting the browser's 16px root size because its panel declared no font-size of its own — is now on the same 13px-body scale as the rest of the app. Channel Manager's two-pane split is rebalanced from 58/42 to an even 50/50 (the old ratio was an unset component default, never a deliberate page decision, and had been read as "wrong" for a while). Checkbox and radio controls render at one consistent 16px size everywhere instead of five different sizes across the app. This consolidation also surfaced and fixed several real rendering bugs along the way: the Manage Channels list was rendering names inside a 32px grid track from a cross-component CSS collision, Channel Manager's stream filters silently picked up Journal's font size after a single visit to that tab, and several row-action toggle buttons rendered 22px instead of their intended 32px after an unrelated CSS hoist changed load order.
- **Settings' drill-in destinations are grouped into six labelled sections instead of one flat list of eighteen, and each section's heading moved into the shared route header (beads enhancedchannelmanager-70u0r.2, enhancedchannelmanager-70u0r.3, enhancedchannelmanager-70u0r.4, enhancedchannelmanager-70u0r.6, enhancedchannelmanager-70u0r.7, enhancedchannelmanager-2896r.16, enhancedchannelmanager-2896r.17, build 0005).** The Settings sidebar previously listed all eighteen destinations as one undifferentiated list ("just one giant blob of things," per PO feedback); they're now grouped as Connections, Channel Processing, Notifications & Reports, Upkeep, Workspace, and Administration. Settings pages no longer render their own heading block — the shared route header above the content pane now carries a third breadcrumb and the section's description, returning that vertical space to the content itself. The "back to Settings" link at the top of the navigation rail is now sticky rather than scrolling out of view: grouping added enough height that, at smaller viewports, Administration — and the only exit from Settings — could scroll off screen.
- **Docs: reverse-proxy / HTTP-2 request concurrency explained where the `Exceeded concurrency limit` log line sends you (bead enhancedchannelmanager-hns2y, GitHub #755, build 0008).** The #755 fix above (see Fixed) explains what broke in that specific case; operators who hit the same `Exceeded concurrency limit` log line for a *different* reason — a genuinely large one-off burst, or another client issuing one request per item — had nowhere to go for **why** the failure shows up behind a reverse proxy and not in direct testing. `docs/runbooks/request-timeout.md` now covers it: a browser talking HTTP/1.1 caps itself at roughly 6 connections per origin, throttling a burst in transit; a reverse proxy speaking HTTP/2 to the browser multiplexes over one connection with no such cap, so the same burst arrives at ECM all at once. **HTTP/2 is not the fix and running behind one is not the problem** — it only removes a throttle the browser was otherwise applying incidentally, which is why the failure can be invisible in direct testing and reproduce every time through a proxy. Indexed in `docs/runbooks/README.md`.
- **Docs: backup/restore guides gain two troubleshooting notes (bead enhancedchannelmanager-ixdaw).** The troubleshoot-restore guide now covers a restore that leaves a multi-stream channel bound to its placeholder **ECM Custom Streams (DBAS restore)** streams: when two of the channel's streams have names that differ only in ways the name matcher normalizes away (capitalization, spacing, punctuation), both resolve to the same real stream, Dispatcharr rejects the resulting duplicate-stream save, and the whole channel falls back to placeholders — with the by-hand reattach steps to repair it. The take-a-backup guide now notes that encrypted and standard artifacts share the same `ecm-backup-<date>_<time>.zip` filename convention; nothing in the filename, the `.sha256` sidecar, or the Saved Backups list tells them apart, so operators need to track which saved files are encrypted themselves.

### Removed
- **Lookup Tables removed entirely — UI, API, template pipe and database table. ACTION REQUIRED BEFORE UPGRADE if you have any rows (bead enhancedchannelmanager-70u0r.1, PO decision D2 on epic enhancedchannelmanager-70u0r).** The **Settings → Lookup Tables** destination, the five `/api/lookup-tables` CRUD endpoints, the `LookupTable` model, `_resolve_lookups`, the `inline_lookups` / `global_lookup_ids` fields on `POST /api/dummy-epg/preview{,/batch}`, and the `{key|lookup:<table>}` pipe in **both** template engines are gone, along with the `lookup_tables` table (destructive Alembic revision `0041`). **Export first:** the Lookup Tables page shipped as a reachable Settings destination in every release from v0.16.0 through v0.18.0, so any instance may hold operator-authored rows, and `alembic downgrade 0040` restores the table's schema but NOT its rows. Full export procedure: [Lookup Tables retired](docs/user_guide/epg/lookup-tables-retired.md). As a safety net the migration writes every row it is about to delete to `/config/lookup_tables_dropped_0041.json` and logs the path at WARNING (best-effort — a read-only or full `/config` makes it fail, and the log line says so). **Your generated EPG output does not change.** The pipe only ever resolved in *preview*: `generate_xmltv()` called `generate_channel_xml` with six positional args, leaving `lookups` at its `None` default, so XMLTV generation raised "unknown lookup table", took `render_template`'s fallback and emitted the RAW template text verbatim as the programme `<title>`/`<desc>`. After removal the same template raises "unknown transform" and takes the same fallback — byte-identical XMLTV. What changes is that preview now agrees with the guide instead of contradicting it, which is why removal was chosen over documenting the divergence. Bookmarked `#settings/lookup-tables` URLs resolve to Settings → General via a compatibility alias rather than the silent unknown-page fallback; the preview endpoints *ignore* the two retired request fields rather than rejecting them, so a stale cached browser tab keeps working. Remaining pipes (`uppercase`, `lowercase`, `titlecase`, `trim`, `strip`, `replace`, `normalize`), conditionals, pattern variants and substitution pairs are untouched.

### Fixed

- **Cancelling a whole-queue bulk action on Pending Merges could leave the wrong list on screen, and it no longer can (beads `enhancedchannelmanager-5dckk`, `enhancedchannelmanager-vmk2m.2`, build 0114).** Choosing a bulk action scoped to the whole queue replaces your current page of rows with a server snapshot of every matching row, so the count you're asked to confirm and the records it covers are one coherent set. Pressing **Cancel** is supposed to put your original paginated view back. The check that decided whether to restore consulted a value React updates in a passive effect, which is not guaranteed to be current at the instant the click is handled. When that value hadn't caught up yet, the check concluded the view had already moved on when it had not, and Cancel closed the dialog without restoring anything: you were left looking at the whole-queue list instead of the page you started on. The window opens when the dialog is committed and closes one scheduler task later, so hitting it took the browser's main thread busy enough that the bookkeeping lagged behind the click. It was found by the test suite, which clicks faster than a person can, not by an operator report. The comparison now runs entirely off values already settled at the moment the click is handled: the exact list handed to the page, checked against the list currently rendered. Neither one needs anything to catch up, so the decision can no longer be stale.

- **Two frontend tests that intermittently blocked the Docker image from publishing have had their underlying races removed, not just their tolerances raised again (beads `enhancedchannelmanager-vmk2m.1`, `enhancedchannelmanager-vmk2m.2`, `enhancedchannelmanager-5dckk`, build 0114).** Both tests failed only on CI, only under load, and each failure stopped the `dev` image from publishing: operators tracking the `dev` tag stopped receiving new builds while the code on the branch kept moving. It blocked three publishes. Both tests had been "fixed" before by giving them more time to pass, and both came back. This round removes the races they were waiting out instead: the `StickySectionNav` test now controls the animation-frame clock it was previously waiting on, and the `PendingMergesPage` test asserts synchronously, because the behavior it checks completes within a single render and a wait could only have hidden a failure, never revealed one. If it helps explain the impact, the symptom you would have seen was a `dev` image that stopped updating, not anything wrong with a running instance.

- **A Journal row now describes what actually happened, on three paths where it described what was about to (beads `enhancedchannelmanager-ftidn`, `enhancedchannelmanager-kz089`, build 0112).** Rows for **Assign Numbers** and **Bulk Merge** are now built the moment their write lands, so an interrupted run still leaves a trail. Building them early is right and stays. What it briefly introduced was two ways for a row to exist and be wrong. **Assign Numbers without a starting number**, which is the request that asks Dispatcharr to pick the numbers, failed with an internal error after the renumber had already landed upstream, leaving no row at all for a change that had happened. Dispatcharr does not report which numbers it picked, so ECM now reads them back and finalises each row from what it finds. A row it could not read back says the number is not yet known rather than naming one nobody observed. **A bulk merge whose source channels could not be deleted** claimed the merge had completed. The target's streams really had moved, but every source channel was still there, and the row said they had been merged away. Each group's row is now rewritten from the outcome as it happens, names the sources that are still upstream, and reads as an incomplete merge rather than a finished one. The response carries the same count, so the envelope and the Journal cannot tell you different things about one group. **And a request that was cancelled or disconnected mid-flight no longer has its own ending replaced.** If writing the trail on the way out hit a fatal error of its own, that error took the place of whatever was already unwinding, which in the worst case turned a disconnected request into a terminating worker. The original always wins now, and a write failure with nothing else in flight still surfaces.

- **The Journal's “Merge Not Applied” filter and its row badge now say the same thing (bead `enhancedchannelmanager-i5ic0`, build 0112).** The filter dropdown offered **Merge Not Applied** while the row badge for those same entries read **Merge Unapplied**, and the Pending Merges notice sent you to look for the first wording. An operator who followed that instruction found a badge with different words and nothing to say the two were the same kind of row.

- **The Journal's Action dropdown now offers “Bulk Merge Incomplete”, so the bulk merges that did not finish can be filtered to (bead `enhancedchannelmanager-ftidn`, build 0112).** A bulk merge moves each group's streams onto the target channel and then deletes that group's source channels, and a group whose source channels are not all gone gets its own **Bulk Merge Incomplete** row naming the channels still upstream. The advice for finding those rows was to filter the Journal on the action type, and in the ECM interface there was nothing to select: the action type reached the frontend as one nobody had told the frontend about, so the dropdown had no entry for it and the badge was rendered by a generic fallback rather than by anything that knew the words. Hunting for the affected groups meant leaving the dropdown on **All Actions** and searching for each target channel by name. The entry is now in the dropdown next to **Merge Not Applied**, where an operator scanning for merge trouble finds the two together, and the badge and the filter option are now built from one string so they cannot drift apart again. **The badge wording is deliberately unchanged.** The operator guide names it **Bulk Merge Incomplete** throughout, including where it tells you this is the badge worth reading for, so renaming it while adding its filter would have recreated exactly the mismatch the entry above exists to fix. `GET /api/journal` already accepted `action_type=bulk_merge_incomplete` and is unaffected; this closes the gap on the UI side only. **What it does not change:** a row you can now filter to is still only as complete as the write that produced it, and outside the handful of endpoints that report `journalRowsUnwritten` a Journal row that was never written is still silent.

- **Accepting a pending merge no longer reports success when Dispatcharr may never have been updated (bead `enhancedchannelmanager-i5ic0`, build 0112).** Clicking **Merge** on the **Pending Merges** page is meant to do two things: record your decision, and add the stream to the candidate channel in Dispatcharr. The second depends on ECM resolving the stream's name to exactly one Dispatcharr stream, and when it could not, ECM resolved the queue row anyway and told you nothing. The merge read as confirmed, the row left the queue, the audit trail agreed with the confirmation, and there was no signal anywhere that anything had been skipped. Finding the affected merges afterwards meant comparing ECM against Dispatcharr by hand. **The answer is now on the response, and it has three values rather than two**, because the honest answers are three. Applied. Recorded and not applied. And "this request cannot tell", which is what an idempotent replay of an already-resolved merge is: it contacts Dispatcharr not at all, so it has no evidence about what the original request did, and calling that success is the same false claim one branch over. Each gets its own treatment on screen. A merge ECM recorded without applying raises a warning notice naming the stream and the reason in words you can act on, and a replay raises a separate, deliberately different notice pointing you at the Journal against that channel rather than telling you to go and add a stream that is probably already there. **A merge ECM could not apply stays in Pending Merges, flagged as not applied and carrying its reason on the row itself.** It does not resolve and it does not leave the queue, because it was not carried out. It stays counted by the subnav badge, it keeps its place against a re-import queuing a duplicate for the same stream, and pressing **Merge** again once the cause has cleared is an ordinary accept that resolves it and clears the flag. The notice above the list is a summary of what your last click or bulk run did, which is why it names the streams: after a bulk run over a long queue, the flagged rows are not necessarily on screen. The outcome is also written to the Journal, under its own **Merge Not Applied** action type, filterable rather than buried in prose. **The replay is the one case that leaves nothing in the list**, and correctly so. It is a second accept of a merge that already resolved, so there is no row left to flag and the notice is the only place its explanation can live. **If you scrape ECM's Prometheus metrics**, `ecm_dedup_merge_requests_total` now emits `status="unapplied"` for these accepts. It is deliberately not counted as `success`, because the queue row does not resolve, and counting it as one would have shown a queue clearing while flagged rows piled up in it. Existing queries are unaffected. Five things can stop a name resolving, and the notice distinguishes them, because they call for different responses. Nothing matched. Several streams matched. The lookup itself failed, which is **not** evidence that your stream is missing and used to be reported as though it were. The one match carried no usable id, which is a gap on ECM's side and not in your catalogue. Or the search filled its single page, in which case ECM refuses even when exactly one exact match is visible, because a full page means further matches may sit on pages it never asked for and adding one of several possible streams is not the merge you asked for. **Be clear about what this does and does not buy you.** ECM now detects and reports; it prevents nothing. Dispatcharr 0.28.x offers no conditional update, which was measured against the live schema rather than assumed, so a change landing between ECM's read and its write is still lost. **What this costs you:** an accept that used to disappear quietly now sometimes leaves a notice on screen with work in it for you. That work was always there. You were not being told about it.

- **Deleting a channel group outside Edit Mode now leaves the same Journal trail Edit Mode already left (bead `enhancedchannelmanager-jd3kn`, build 0112).** Deleting a group moves its channels to **Default Group** first, because Dispatcharr refuses to delete a group that still holds channels. Applied through Edit Mode, that wrote a row per moved channel plus a row for the deletion, all under one batch identifier. Through the direct route, which is the one an MCP agent and any API client take, it wrote nothing at all. The same operator-visible action therefore left a full trail through one path and silence through the other, and an operator who found channels sitting in **Default Group** the next morning had no row anywhere explaining how they got there. The Journal article tells you to trace a channel's history by searching for its name; for this action through this route, it could not be done. Both routes now write the same rows. **When the deletion fails after the moves have landed, the error says so rather than leaving you to discover it.** The channels really did move, they stay moved, and the group is still there, so the response names how many were moved and sends you to the Journal for which ones before you retry against a membership that has already changed underneath you. Two limits, both real and both deliberate. On a genuine server fault ECM replaces the response detail wholesale to keep internals off the wire, so on that exit the advisory reaches the container log and not you. And a group with M3U sync settings is hidden rather than deleted, which performs no Dispatcharr write and still writes no Journal row.

- **Ten channel operations now tell you when the change landed but its Journal row did not (bead `enhancedchannelmanager-ftidn`, build 0112).** ECM's journal reports a failed write by returning nothing, not by raising, so an endpoint that wrote a row for its effect and ignored the result could not tell a recorded change from an unrecorded one. On a journal database that was read only, unavailable or out of disk, the Dispatcharr change landed, no row was written, and the response said neither. Ten endpoints now carry `journalRowsUnwritten` on their success response: creating a channel, deleting one, merging channels, bulk merging them, adding one stream, adding several at once, removing one, reordering a channel's streams, assigning channel numbers, and clearing the auto-created flag. It is the same advisory the bulk commit envelope and `PATCH /api/channels/{id}` already carried, and the lost rows go to the container log. It rides on the success response on purpose: the change landed, and answering with a failure is what makes an integrator retry something that already applied. Several of these also queue their rows **at the moment each write lands** rather than assembling them afterwards, which matters on the paths that delete things: a bulk merge interrupted partway used to unwind with the sources already gone and not one row built, and those rows are the only remaining record that those channels existed. **Be precise about what this does not cover, because the honest number is large.** Sixty-five other places in the backend still discard that result, across twenty-one modules, and that is a deliberate residue rather than an oversight. Each needs its own decision about where an advisory could even hang: some return a bare upstream object with no envelope to extend, some return lists, and the scheduled and task-engine paths have no caller waiting to be told anything. What has been fixed is the highest-traffic operator surface plus the bulk commit and group delete paths. **A `journalRowsUnwritten` of zero from one of those endpoints is not a statement about the Journal's reliability generally.** Everywhere else, a missing row is still silent.

- **A bulk operation that landed part of its work and then failed now says so, instead of reporting a plain failure (bead `enhancedchannelmanager-1e4at`, build 0112).** One operation in an Edit Mode commit has two upstream writes in sequence: deleting a channel group moves the group's channels out first, then deletes the emptied group. If the move landed and the delete failed, the envelope had exactly one outcome per operation and no way to express "the channels moved but the group is still there", so it reported a failure. That was the safer of the two available misstatements, since calling it applied would say the group is gone when it is not, but it still is not what happened: the channels really did move and they stay moved, and an operator who retries the failed delete finds them already elsewhere with nothing explaining why. The envelope now carries `operationsPartiallyApplied`, and the operation's own error entry carries `sideEffectsLanded`, so the count says how many and the entries say which. It is a **subset of the failures, never an extra category**, so the numbers still add up to the operations you submitted; an integration that sums all three will overcount. The MCP bulk commit tool renders these in their own **PARTIALLY APPLIED** block, separate from both the plain failures and the operations that applied incompletely, because "retry me" is the wrong instruction for all three and it is wrong in a different way for each. One consequence worth knowing if you read the envelope programmatically: a run whose only operation failed after landing a write now reports zero applied and `partial: true`. Read `partial` as "state changed and the run did not finish cleanly", not as "something succeeded".

- **Edit Mode now stages what it always implied it staged, and says so plainly where it does not (beads `enhancedchannelmanager-kz089`, `enhancedchannelmanager-i4yk1`, build 0111).** Edit Mode presents itself as a staging area: make changes, then **Apply All** or **Discard**. That was true of some actions and false of others, with nothing on screen distinguishing them. An operator who set logos across two hundred channels, changed their mind and pressed **Discard** had already changed two hundred channels. The fix goes in both directions. **Five things that used to write immediately now stage** and are covered by the change count, by Undo, and by Discard: **Set Logo from M3U**, **Set Logo from EPG**, **Profile visibility** (enabling and disabling channels in a profile), **clearing probe stats**, and **restoring a hidden channel group**. Stream deduplication merges staged too, so accepting a "this looks like an existing channel" offer while in Edit Mode is now reversible like everything else. **Four things still write immediately, and every one of them now says so at the point of action rather than leaving you to remember.** **Merge**, from either the toolbar or **Find Duplicates**, and **Import CSV** carry a notice stating the action is not staged and cannot be undone by Discard, Cancel or Undo, with an **"I understand this cannot be discarded"** checkbox that must be ticked before the button enables. **Probe** carries a shorter note, on all three of its entry points (a single channel's actions menu, the selection toolbar, and a group menu), explaining that it writes the stream stats it measures. Probing is not destructive, so it asks for no checkbox. And **creating, renaming or deleting a channel profile** is labelled immediate in the profiles list, with the clarification that changing which channels are *in* a profile does stage. **One residue is deliberately left, because it is real:** pulling a logo from an M3U or EPG source adds that image to your Logo Manager library straight away, before the assignment is staged. Discarding leaves the channel untouched and the artwork behind as an unused entry. Nothing on screen says so; clean them up with Logo Manager's **Unused only** filter.

- **Keeping current numbers in the Reorder Group dialog no longer looks like it saved an arrangement it never saved (bead `enhancedchannelmanager-zll44`, build 0111).** Dragging channel groups to reorder them always opens the **Reorder Group** dialog, which defaults to renumbering, and that default path genuinely does persist: it stages channel-number changes, and because the group list is sorted by lowest channel number, applying them makes the arrangement durable. The loss was confined to choosing **Keep current numbers**, which rearranged the groups on screen and wrote nothing. It counted as no change, produced no unsaved-changes indicator, and **Apply All** did not persist it, so the arrangement was gone on the next page load with nothing having warned you. That option now says in the dialog that the resulting order is display-only.

- **The Journal records per-channel rows for Edit Mode changes, so a channel's history can be traced by name again (bead `enhancedchannelmanager-r9py9`, build 0111).** Following the Journal article literally, filtering to the **Channel** category and searching for a channel by name, returned "No journal entries" for any channel that had only ever been changed through the interface. The category held nothing but **Bulk Commit** summary rows whose Entity and Action both read "Bulk Commit", so no row anywhere carried a channel name and the search could not find one. The per-channel recording itself worked; it was specifically the Edit Mode commit path that never wrote to it, so the same change made by an MCP agent was traceable and the same change made by a person was not. Applying now writes a row per channel, the same rows the single-channel endpoints and MCP agents already produced, queued **at the moment the upstream write lands** rather than assembled afterwards, so a run that fails partway still records what it actually did. **One correction to what the Journal article used to promise:** an Edit Mode session does not produce exactly one **Bulk Commit** row. **Apply All** sends one request for the creates and then one per 200 remaining operations, and each of those writes its own summary row, so a large session writes several. They all share one **Batch** identifier, which is what ties the session together, and each summary's counters describe *its own request* rather than the whole session. Where ECM applied a change without ever having read what a field held beforehand, the expanded row now says so in words instead of printing an internal placeholder key, and still shows the fields it did read.

- **Typing a channel number that is already taken now warns you before it is staged, instead of failing at Apply (bead `enhancedchannelmanager-vdxbx`, build 0111).** Both inline channel-number editing and the **Edit Channel** modal accepted a number another channel was already using and staged it without comment. The conflict surfaced only when the session was applied, which is the worst moment to find out: the batch is already running, and what you get back is a failure to interpret rather than a question to answer. Both surfaces now check before staging, against the *effective* state of your session, which means staged number changes and channels you created in this session count, not just the last list loaded from the server. The channel being edited is excluded, so keeping its own number never warns. A **Channel Number Already Used** dialog names the number and every channel currently on it, states plainly that Dispatcharr does allow duplicate numbers so this is not an error, and offers **Go Back** or **Use It Anyway**. **Go Back** from the inline editor puts back exactly the text you typed, so nothing is retyped. **Use It Anyway** stages the change and remembers that you approved *this particular collision*, so the final-state check at Apply does not ask again. That approval is deliberately narrow: it is recorded against the channels the dialog named, so if a different channel moves onto that number before you apply, you are asked again rather than having consent you never gave applied on your behalf.

- **A channel-number swap or renumber no longer collides with itself partway through, and a half-applied one now tells you exactly how to finish it (bead `enhancedchannelmanager-ic884.3`, build 0111).** Numbering changes were sent in whatever order the client happened to submit them, so a plan that was perfectly valid as a final state could fail in the middle of being applied. Swapping two channels' numbers is the simplest case. Writes are now ordered so that every intermediate state is one you would recognise, and where an order genuinely cannot exist, which is what a swap or a rotation is, exactly one channel is released early onto a transiently shared number rather than the run stopping. That ordering is proven exhaustively over every plan shape that fits in four channels and four numbers, and it is proven to terminate. When part of a numbering plan lands and part fails, ECM now attempts to put every landed change back, replaying them newest-first. **Be precise about the guarantee, because it is not a rollback.** A compensating write is *attempted* for every write that landed. Nothing can promise it succeeds: Dispatcharr offers no transaction and no conditional update, so this is best-effort repair, and it does not run at all if the run dies outright rather than failing an operation. Where a repair fails, you are not left guessing. The response names each channel that could not be put back, the number it is on, the number it should be on, and a plain instruction that fixes it, and the interface shows those steps in the same "Some Changes Were Not Applied" dialog it already uses. It also says not to retry the batch until they are fixed.

- **Browser Back and Forward, and two links inside the app, no longer throw away your staged Edit Mode work (bead `enhancedchannelmanager-6fi7p`, build 0111).** Edit Mode holds every channel change in memory and writes nothing to Dispatcharr until you press **Apply All**, so leaving Channel Manager with work staged has always raised the exit dialog offering **Apply**, **Discard** or **Keep Editing**. Three ways out went around that dialog entirely, because it was only ever reachable from a click on ECM's own navigation. The Notification Center's link to a scheduled task's editor and the Channel List Filters panel's **Clean up empty groups** link both changed the route directly. The browser's **Back** and **Forward** buttons never consulted the guard at all: enter Edit Mode, stage a change, press **Back**, and ECM left the route with no confirmation of any kind and the staged work gone. An external reviewer found the history case and it reproduced every time. All three now ask. When you choose to leave, **Back** also goes back properly: pressing **Back** and then **Apply** or **Discard** lands you on the entry you pressed **Back** to, with the surrounding history entries intact, rather than on a new entry with the one you left still sitting behind you. **Keep Editing** leaves you exactly where you were, still on Channel Manager, still in Edit Mode, staged work untouched. One thing fixed alongside it looked like an unrelated bug and was not: the Notification Center's task-editor link records which task to open *before* it navigates, and when the guard held that navigation and you chose **Keep Editing**, the record stayed behind. Your next visit to *any* Settings page was then silently redirected to **Scheduled Tasks**, once, with nothing to explain it. **Keep Editing** now takes that record back. Choosing to leave still carries it through, so the task editor opens as it always did. **What this costs you:** a **Back** press that used to leave Channel Manager immediately now waits for an answer whenever you have staged changes. **Discard** is the old behaviour, one click away. **A history move is a request, not an instruction, and the guard now treats it as one.** When ECM refuses a **Back**, it asks the browser to put you back on the entry you were on, and that request takes effect a moment later. Pressing **Back** again in that moment, or **Back** and then **Forward**, used to hand ECM a move it had not asked for while it was waiting for one it had, and after a short burst of presses it could end up waiting forever: from that point it stopped asking about **Back** at all, silently, with the address bar showing one route and the page showing another. ECM now records which history entry each of its own moves is aimed at and counts nothing else as that move, re-anchors on the entry you are actually on rather than chasing one you have left, and takes down anything it was waiting on if the browser declines the move outright or simply never performs it. A **Back** onto an entry ECM did not number itself, from an older release or from an in-page section link, is handled the same way: ECM re-anchors its bookkeeping onto that entry instead of continuing to describe a slot you are no longer in. And if the browser declines to replay the move after you press **Apply** or **Discard**, you are carried to the destination you asked for by ordinary navigation rather than left standing where you were.

- **Signing out with staged channel work now asks first, instead of destroying it on the way to the login page (bead `enhancedchannelmanager-r93hq`, build 0111).** **Sign Out** in the user menu ran the sign-out immediately and consulted nothing. Signing out replaces the whole app with the login page, and the Edit Mode ledger lives in memory, so every staged channel change went with it: no warning, no dialog, no chance to apply. It is a state change inside the app rather than a page unload, so the browser's own "leave this site?" prompt never fired either. The user menu sits in the header on every route, including Channel Manager in Edit Mode, which is exactly the state you would be in when you decide you are done for the day. **Sign Out** now raises the same exit dialog the rest of Edit Mode uses: **Apply All** applies your staged work and *then* signs you out, **Discard** throws it away and signs you out, and **Keep Editing** cancels the sign-out and leaves you where you were. With nothing staged, **Sign Out** is immediate exactly as before. This guard is deliberately stricter than the one on navigation, and the difference is worth knowing: moving *to* Channel Manager while in Edit Mode is let through without a prompt, because Edit Mode survives it. Nothing survives a sign-out, so standing on Channel Manager in Edit Mode with staged changes raises the dialog here even though the same position raises nothing for a route change.

- **Deleting a channel group now moves its channels out first on the immediate delete too, so the confirm dialog keeps its promise (bead `enhancedchannelmanager-auocn`, build 0111).** Dispatcharr refuses to delete a group that still has channels: measured on a live 0.28.2 instance, `DELETE /api/channels/groups/377/` answers `400 {"error":"Cannot delete group with associated channels"}` on a six-channel group and `204` on an empty one. `DELETE /api/channel-groups/{id}` issued a bare delete with nothing to empty the group first, so it could only ever succeed on a group that was already empty, while the confirm dialog leading to it promises in so many words that the channels will be moved. The failure was at least visible rather than silent, but there was no way to succeed short of emptying the group by hand first. The Edit Mode bulk commit had already been fixed under bead `enhancedchannelmanager-ayfn9`; the immediate delete had not. Both paths now run one shared implementation, because one dialog makes the promise and two implementations would drift. A non-empty group's channels are moved to the group named **Default Group** before the delete, resolved by name rather than by id so renaming your way around a numbering assumption cannot break it. Moving them to no group at all is not an option: measured on the same instance, `PATCH /api/channels/channels/1/` with `{"channel_group_id": null}` answers `400 {"channel_group_id":["This field may not be null."]}`. Two cases now refuse the delete with the reason in the message rather than an opaque `500`: there is no **Default Group** to move the channels to, and the group you are deleting *is* **Default Group**, whose own members would have nowhere to go. An already-empty group needs no destination and still deletes cleanly on an instance that has neither. The response now reports `channels_moved`. **Where this reaches you:** the group actions menu that hosts **Delete Group** is behind the Edit Mode gate, so the in-product route currently stages the delete instead. The immediate delete stays reachable, and until now stayed broken, through the MCP `delete_channel_group` tool and any direct API client.

- **Emptying a channel group no longer silently overwrites another operator's concurrent move, though this narrows the race rather than closing it (bead `enhancedchannelmanager-auocn`, build 0111).** The step above reads a group's membership and then moves each channel it found. That read is deliberately live relative to the caller, but it is not atomic relative to its own writes. Anyone else moving one of those channels to a third group in between had their move replaced by **Default Group**, with nothing in the log to say it had happened. Every write is now preceded by a re-read of that one channel, and a channel that has already left is skipped and logged instead of overwritten. The count reported back as `channels_moved` is what actually moved, not what was found. **Be clear about what this is: a check, not a transaction.** A move that lands between the re-read and the write is still lost. What it removes is the long window. Without it, a channel was exposed from the moment its row was read, through the remaining pages of the scan, the destination lookup, and every earlier write in the loop, so the exposure grew with the size of the group. With it, the exposure is one round trip, and the failure becomes a logged skip rather than a silent overwrite. A conditional update would close the race properly, and Dispatcharr 0.28.x does not offer one. That was measured, not assumed: against the live 0.28.2 schema on 2026-08-15, `GET /api/schema/?format=json` returned `200` and roughly 717KB containing zero occurrences of `If-Match`, `If-None-Match`, `If-Unmodified-Since`, `ETag` or `412` anywhere in the document, and the `Channel` and `PatchedChannel` schemas carry no version or modified-at field to compare against even client-side. The finding is written down in `docs/dispatcharr_api.md` so the next attempt at this does not have to re-measure it. One deliberate asymmetry: a re-read that fails, or that comes back without a readable group id, proceeds with the write. Withholding it on no evidence would leave the group non-empty and turn a working delete into Dispatcharr's refusal, which is a worse outcome than the race.

- **The Create Channels dialog's "Normalization Rules" toggle now decides whether names are normalized, and the channel count it shows you is the count you get (bead `enhancedchannelmanager-e9e5o`, build 0111).** The toggle drove the preview and nothing else. Names were normalized whichever way you set it, so an operator who wanted the raw provider names had no way to get them and nothing on screen said otherwise. The question is now answered once, before anything is staged, and the name that answer produces is final. **Turning the toggle off changes how many channels you get, not only what they are called.** The resolved name is the key the bulk create merges streams on. With normalization on and a rule that strips a country prefix, `US: CNN` and `CNN` resolve to the same name and collapse into one channel carrying two streams; with it off they stay two separate channels. That moves the channel count, the channel numbers the batch claims, and the size of any push-down conflict plan. The dialog used to count from the raw provider names whatever the toggle said, so with normalization on it could promise two channels and stage one. (The toggle's starting position comes from **Settings → Channel Normalization → "Apply normalization by default when creating channels"**, which is off unless you have turned it on; the dialog lets you flip it per operation.) Every surface reading that count now reads the same resolution the create uses: the preview list, the "N streams" merge badges, the conflict check, the push-down plan, the channel number range line and the button label. A normalization failure is no longer silent either, and it could not have been distinguished before: an unreachable normalization engine returned every name mapped to itself, which is byte-for-byte what turning the toggle off produces. The create now raises a **Normalization failed** notification, and the dialog's preview says it could not reach the normalization engine instead of claiming that no names will change. The channels are still created, under the raw names, because abandoning a create you have already confirmed is worse than completing it and reporting it. Manual channel entry keeps the control and now honours it, with a live preview of the name you are typing as you type it, debounced so it keeps up without a request per keystroke. When the engine cannot be reached the preview says so directly, rather than showing an empty result that reads as "nothing will change": *"Could not reach the normalization engine, so there is no preview. Creating now would use the raw provider names."* **What does not change:** the duplicate check that offers to merge a stream into an existing channel does not depend on this toggle at all. It reads the raw provider name and applies its own separate cleaner, so turning normalization off does not weaken duplicate protection.

    **Four further rounds hardened this after the entry above was first written**, and they are worth recording because each one closed a way the original fix could still mislead. Name resolution became **all-or-nothing**: a partial result used to leave each caller deciding for itself what a missing name meant, which is how the preview and the create could disagree about the same channel. A normalization failure is now only reported **for a channel that actually exists**, so the failure list can no longer name something that was never created. The create resolves its names **once** and never plans off an unresolved one. And the accounting behind the whole envelope became an **enforced invariant** rather than a convention: `operationsApplied` and `operationsFailed` now have exactly one writer, every operation resolves to exactly one outcome, and an operation whose write reached Dispatcharr is never reported as a total failure, because reporting one as a failure is what makes an integrator retry and create the same channel twice. A self-contradictory result now raises rather than shipping as a quiet log line. Three review rounds had each fixed the reproduction they were handed before the property itself was enforced in one place.

- **The duplicate check now says what it did, instead of leaving four different outcomes to look identical (bead `enhancedchannelmanager-ok8tj`, build 0111).** When you create a channel from a single stream, either by dragging it onto a channel group or through the Streams panel selection strip's **Create in…** menu, ECM checks the target group for an existing channel whose name is close enough to be worth offering a merge. Find one and you get the merge prompt. Otherwise you got nothing at all, and "nothing at all" covered three separate stories: nothing in the group was similar enough, the check itself failed, or the check never ran. The report this was filed on is an operator who selected two streams, one named `US: CNN`, created them into a group that already held a channel called `CNN`, and got no prompt. The matcher scores that pair at 100%, so the score was never the reason. The check had not run: it only ever runs on a *single*-stream selection, because bulk duplicate handling is the separate **Pending Merges** queue. Nothing said so, so an absent prompt read as "nothing matched". Both interactive paths, drag-drop and **Create in…**, now name the outcome in the same words. **No duplicate found** means the check ran and nothing in the target group was close enough, and it points at the dedup confidence threshold in Settings if you expected a prompt. **Duplicate check unavailable** means the lookup itself failed and the channel is being created without a duplicate check. **Duplicate check skipped** means the check never ran, either because you selected or dropped more than one stream, or because ECM could not read the stream's name. A match still says nothing, because the merge prompt is the message. The matcher is untouched: no threshold, score or candidate changes.

- **A Channel Pipeline rule that names channel number `0` no longer creates a second channel `0` (bead `enhancedchannelmanager-ay3iq`, build 0110).** `0` is a channel number like any other, and a pipeline run starts by reading your lineup to work out which numbers are already taken. That reading treated a channel numbered `0` as a channel with no number at all, so the slot was recorded as free. A **Create Channel** or **Set Channel Number** action set to `0` then created a duplicate at `0` instead of stepping up to the next free number, every run, for as long as the rule kept firing. It now steps up. Channels that genuinely have no number are still read as unnumbered, which is what they are, and `auto` numbering is unchanged because it has always started at `1`. This one predates the tenths work below and has been present since the engine was written.

- **A Channel Pipeline rule with a full channel-number range no longer lands on a number that is already taken (bead `enhancedchannelmanager-ay3iq`, build 0110).** A **Create Channel** action can be given a range to number from, such as `100-105` or the `Starting from...` field's `100-99999`. The engine walks the range and takes the first free number in it; when every number in the range is already used it deliberately spills past the top rather than failing, which is the behaviour you want. The spill, though, took the number one above the range without ever checking whether *that* number was free. So a rule set to `1-2` on a lineup already holding channels `1`, `2` and `3` was handed `3`, a number the run itself had just recorded as taken, and created a second channel on it. A run that created several channels this way put every one of them on the same number. The spill now keeps searching upward from the top of the range for the first genuinely free number, exactly as `auto` and a plain starting number already did, so `1-2` against `1`, `2`, `3` gives you `4`. Nothing else about ranges changes: a range with room in it still fills from its own bottom up, ranges still count in whole numbers, and a full range still assigns above itself instead of failing the action. This one predates the tenths work below and has been present since ranges were added.

- **Inserting a channel by hand at a number that is already taken now warns you, and "Push channels down" actually pushes (bead `enhancedchannelmanager-fprsq`, build 0110).** The **Create Channel** button in the Channels pane opens the same dialog as creating channels from streams, but the manual path skipped the conflict check entirely. Typing a channel number another channel already held produced a second channel on that number with no warning of any kind, no dialog, and nothing to undo but the change itself. The check was not merely unable to honour "Push channels down" in manual entry, as originally reported: the **Channel Number Conflict** dialog was never shown there at all, so the button was never even offered. Manual entry now runs the same conflict check and, when you choose it, the same push-down planner the bulk path uses, so the channels already sitting at that number move up together and nothing is left holding a duplicate. All three answers work: **Push channels down**, **Insert at end** (which used to be doubly broken here, since the manual path ignored the number it hands over and created the channel back at the conflicting one), and **Create anyway** for when the duplicate is what you want. A decimal insert pushes down on the tenths grid exactly as a bulk insert does, so inserting at `38.1` moves `38.1` to `38.2` and carries the ripple on from there rather than jumping to `39`. The dialog's wording is sized for a single channel: it offers to shift by one, not by the zero streams manual entry has selected. Creating a channel with **no** number is still a normal thing to do and never raises the dialog, since an unassigned channel cannot collide with anything. Two smaller things fixed with it: the create dialog no longer previews a channel range for a single manual channel, where it read "Channels 38.1 - 38.0"; and the conflict dialog now closes once the channel is created instead of staying on screen. Manual channel creation is an edit-mode feature and the push-down works by staging, so a push-down requested outside edit mode is now refused with a message rather than quietly creating the duplicate anyway.

- **Starting-number fields no longer turn a typed `1.5` into `1` without telling you (bead `enhancedchannelmanager-j3pyx`, build 0110).** **Sort & Renumber**, **Renumber** on a selection, **Renumber All Groups** and its per-group starting numbers, the group reorder custom number, the **Move to group** custom starting number, and (in the Channel Pipeline rule builder) a **Sort Group** action's **Starting Channel Number** all seed a run of sequential numbers from one value you type. Every one of them read that field with a whole-number parse and then said nothing: typing `1.5` renumbered from `1`, the preview underneath cheerfully announced "Channels will be numbered 1 - 12", and the confirm button stayed live the whole time. You asked for one thing and got another, which is the exact behaviour the channel-number rules were changed to stop (bead `enhancedchannelmanager-ic884.1`). These fields stay whole-number-only, because sequential renumbering counts in whole numbers and there is no sensible run starting at `1.5`. What changes is that they now refuse: one message, the same in every dialog and in the rule builder, saying what is accepted ("Starting channel numbers must be whole numbers of 1 or greater"). The preview and the confirm button are both derived from the same reading of the field as the operation itself, so a value that will not be honoured cannot be previewed and cannot be confirmed. In the rule builder the field goes red and the action is left with no starting number at all, so a rule can never be saved carrying a number you did not type. While the field is red, **Save** and the Enter key both refuse the whole rule and put the cursor back in the offending field, from whichever wizard step you pressed **Save** on. Blocking the save is the other half of refusing the entry: without it you could read the red message, save anyway, and get a rule that renumbers from the group's current lowest, which is not the number you typed either. A blank **Sort Group** starting number is the normal "start from the group's current lowest" behaviour. This does not narrow anything you could do before: channel numbers with one decimal place are still accepted everywhere they were, including when you create channels from streams and when you edit a channel directly. It is only the *starting number of a renumbering run* that has to be whole, and it always was.

- **A Channel Pipeline rule can now be set to a channel number like `1.1`, and the channel actually gets `1.1` (bead `enhancedchannelmanager-ay3iq`, build 0110).** Making one decimal place canonical (bead `enhancedchannelmanager-ic884.1`) meant `1.1` became a valid channel number everywhere you can type one, including a rule's **Set Channel Number** value and a **Create Channel** action's Channel Number. The pipeline engine, though, understood only whole numbers: it read `"auto"`, a whole number, and a whole-number `min-max` range, and quietly numbered everything else automatically. So a rule set to `1.1` was accepted, saved, and then produced a channel numbered `3`, or whatever the next free integer happened to be, with nothing in the execution log to say so. The engine now walks the same tenths grid the Channels pane already uses when you insert at a decimal position: a rule set to `1.1` lands on `1.1`, and if `1.1` is already taken it steps to `1.2`, not to `2`. A rule set to a whole number still steps by whole numbers, exactly as before. The arithmetic is done in whole tenths and converted back once at the end, so a run of decimal assignments cannot drift into a number Dispatcharr will not hold (`1.1 + 0.1` is `1.2000000000000002` in plain floating point). A whole number stays whole: `800.0` is written as `800`, in the payload and in the `800 - Channel Name` prefix, not as `800.0`. Two shapes are still refused when you save the rule, because the engine cannot honour them and silently renumbering is worse than a clear message: a range naming a tenth (`1.1-1.9`, since ranges count in whole numbers) and a number not written as plain digits (`1e3`, `.5`, `+7`). The message says so directly. The **Create Channel** action's **Starting from...** number builds one of those ranges, and it used to quietly drop the decimal before you ever got that far: typing `1.5` there wrote a range starting at `1`, so the save-time refusal for a fractional range could never actually reach you. That field now refuses the entry where you type it, with the same "Starting channel numbers must be whole numbers of 1 or greater" message every other starting-number field uses, and leaves the action with no starting number rather than one you did not ask for. The rule cannot be saved while that field is red, either: **Save** and the Enter key both refuse it and focus the field, so a red message can no longer be saved past into a rule that numbers channels automatically. That refusal now survives editing the action list. Each action card keeps its own identity, so deleting or reordering the actions above a red field leaves the red field exactly where it was, still refusing. Previously, deleting an earlier action moved every card up a slot and took the refusal with it: the red message vanished, and **Save** went straight through with the action carrying no starting number at all, which is automatic numbering, silently, the whole thing being fixed here. The same identity fix cures a longer-standing annoyance with no connection to numbering: reordering actions used to leave each card showing the *previous* action's **Channel Numbering** choice and **Transform Channel Name** toggle, so the cards and the rule disagreed until you closed and reopened the editor. And pressing **Save** from the **Targeting** or **Output & Run** step now actually lands you on the red field: the editor already returned you to **Logic**, but the cursor stayed on the Save button, leaving you on a step change with no visible explanation. If a rule saved before this release carries one of those, the engine now writes a warning to the log naming the value it could not honour, instead of numbering silently. There is one magnitude at which a tenth stops existing at all: above about 562,949,953,421,312, far past any real lineup, the numbers a computer can store are more than a tenth apart, so several tenths collapse onto one stored value. A rule naming a tenth up there is now answered with a whole number and a log line saying why, rather than with the number the engine had just established was taken. One thing deliberately unchanged: a rule whose Create Channel number names a tenth does not drive the rule-level **Channel Sort** renumber pass, which counts in whole numbers, so that pass is skipped exactly as it is for `auto` numbering. The numbers assigned at creation stand.

- **The password-reset CLI accepts the same passwords as the rest of ECM (bead `enhancedchannelmanager-xztoc`, build 0110).** `reset_password.py` is the tool the README and the user guide both point you at when you are locked out, and it carried its own password rules demanding an uppercase letter, a lowercase letter and a digit. ECM dropped exactly those composition rules on purpose, following NIST 800-63B, so the one path an operator reaches for in a lockout was the one path that refused a passphrase the web UI and the API would have accepted. It now calls the same validator everything else does: at least 8 characters, not a common or breached password, and not containing the username. It also prints those rules when it refuses, which it never did before, so you are no longer told only that the password "does not meet requirements" without being told what they are. `--force` still skips validation. The rules themselves are unchanged, and no composition rule was re-added anywhere.

- **The e2e suite no longer defaults to the product owner's live ECM instance (bead `enhancedchannelmanager-536bl`, build 0109).** `playwright.config.ts` resolved an unset `E2E_BASE_URL` to `http://localhost:6100`, which is the PO's live instance holding real channel data; any spec that stages and applies a channel-number push-down, a cross-group move, a merge, a delete, or any other write would have mutated that real lineup for anyone who ran the suite locally without overriding the variable, and nothing warned about it. This is why bead `i85dg` shipped its push-down coverage as a component test instead of an E2E: writing one against the live default was unsafe. `resolveE2EBaseURL()` in `playwright-base-url.mjs` now throws at config load, before any browser launches or any request is made, when `E2E_BASE_URL` is unset and `E2E_EXACT_BUILD` is not `true`; the message names the removed default, the hazard it created, and the exact commands to set the variable or use the self-hosted `E2E_START_SERVER=true E2E_EXACT_BUILD=true` mode instead. The `Screen-Reader-Only Rendering Guard` and `Operator Workspace Release Matrix` CI jobs already run with `E2E_EXACT_BUILD=true`, which builds and serves the checked-out source on its own isolated preview port and never depended on `E2E_BASE_URL`, so both are unaffected; verified by listing each job's exact spec invocation against the changed config with `E2E_BASE_URL` unset. `docs/testing.md` records the new contract everywhere it previously described the `:6100` default.

- **Inserting channels no longer renumbers groups it has no reason to touch (beads `enhancedchannelmanager-i85dg`, `enhancedchannelmanager-nzwtw`, build 0108).** Dropping streams into a group and choosing "Push channels down" was renumbering the target group and then every group above it, even when a wide gap between them made that unnecessary. The cause was the model, not a stray comparison. The push-down treated each channel group as one continuous span of numbers from its lowest to its highest, and cascaded into the next group whenever the target group's highest number, once shifted, reached the next group's lowest. Nothing in ECM, in Dispatcharr's API or in the database makes that model true: a group may contain holes, may sit inside another group's range, and `channel_number` is a plain non-unique number that permits duplicates. So a single stray high number anywhere in the target group, one placeholder at 9999 for instance, inflated the shifted maximum past every later group's minimum and swept the whole lineup. The same model quietly produced duplicates in the opposite case, where a group starting below the target group but reaching past its tail was never examined at all, and its numbers were landed on. It also compared unrounded sums, so 0.7 plus 0.1 compared as less than 0.8 and invented a gap that was not there. Planning now ignores groups entirely and looks only at which numbers are occupied: it walks up from the insertion point over the numbers the insert actually claims, extends the run of channels it moves past anything those claims would land on, and moves exactly the channels in between, each by the same amount. Every occupant of a number moves together, so lineups that already carry duplicates keep them together instead of having them split apart, and no new duplicate is created. **What counts as "in the way" is the numbers the insert takes, not the space between them.** A pre-merge review caught the first version of this planner requiring a whole step-wide stretch of numbers to be clear, which is not the same thing: inserting one channel at 300 takes the number 300, not everything from 300.000 to 300.999. On a lineup holding only 300.5, that moved 300.5 to 301.5 although the slot 300 was free; on 300 and 300.5, it moved both although moving 300 to 301 was enough. The two readings agree for whole numbers on a whole-number step, which is why an all-integer test suite could not tell them apart, so the tests now check the rule with an independent reference and sample decimal and off-lattice lineups as well. A channel sitting off the step INSIDE a run that is moving still moves with it, otherwise it would end up below the channel that was above it. Where the gap between two groups is narrower than the insert, the channels beyond it do move, but by the size of the insert rather than by an amount re-derived from a group maximum, which is how one small overlap used to become a lineup-wide renumber. The conflict dialog now also states how many existing channels "Push channels down" would renumber, which is usually far more than the conflicting channels it reported before, so the size of the operation is visible before it is chosen. Both places that planned this shift, bulk creation from streams and the cross-group channel move, now share one planner; the cross-group move previously had no gap check of any kind and pushed the target group's tail onto the next group's numbers in silence. **"Close gaps in source group" can no longer undo the planner's work either.** Moving a channel out of a group compacts whatever is left behind, and that compaction was calculated separately, against the numbers as they stood before the move: moving channel 15 to a custom number 11 while 10 and 30 stayed behind renumbered 30 to 11 as well, so the move produced the duplicate the planner had just avoided. The two are still separate operations, because closing holes in one group and making room at an insertion point are different things and the planner deliberately knows nothing about groups, but they now share one record of which numbers are taken, so the compaction allocates around the move and the push-down instead of on top of them. A channel the push-down has already moved keeps that number rather than being handed a second, conflicting one. **Numbers finer than three decimals survive a shift intact.** Comparisons are made on a three-decimal grid, and nothing in ECM, the CSV importer or Dispatcharr's contract stops an import from carrying 1.0001 and 1.0004; both land on the same grid point, which is the right answer for deciding whether they are the same slot and the wrong one for deciding what to write back, since it would turn a pair that was merely close into an actual duplicate. Shifted numbers now keep whatever precision they arrived with. The planner is covered by property tests including the reported case, each of which was demonstrated failing against the replaced logic first.

- **The screen-reader-only rendering guard no longer depends on the fail-open defect it shared a branch with (bead `enhancedchannelmanager-9kwzp`, build 0104).** The `Screen-Reader-Only Rendering Guard` CI job builds the checked-out frontend and serves it on an isolated preview port with no backend, so every `/api` call fails, including the two probes that tell the app whether this instance requires a sign-in. It rendered the whole app shell anyway, because an unanswered probe used to resolve to "authentication is not required": the guard had been riding the exact fail-open the entry above removes, and nobody had noticed, because the job was green. With that fixed the app correctly renders its "Cannot reach ECM" screen instead, the shell never mounts, and the fixture's 30 second wait for the navigation rail times out. This was verified by running the job's own command locally and reading the failure screenshot rather than inferring it from the diff. The product code is unchanged and the fail-open fix stands. `e2e/fixtures/css-guard.ts` now states the auth posture itself when `E2E_EXACT_BUILD` is set, which is the flag that means "no backend": it fulfils `/api/auth/status`, `/api/auth/setup-required`, `/api/auth/me` and `/api/session-start` with the same four responses `e2e/operator-shell.spec.ts` and `e2e/filter-select-ownership.spec.ts` already install on the same preview build, describing the same instance the guard has always measured, namely authentication disabled, setup complete, no session. No data endpoint is stubbed, so both routes still render the same empty and error states, and the guard still asserts it measured at least one element per route so it cannot pass vacuously. The six sibling guards that run against a live ECM container are untouched and keep exercising the real login. `docs/testing.md` and the job's own comment recorded the old assumption as fact and now name the stub.

- **Signing in on an instance with authentication turned off now survives a page reload (bead `enhancedchannelmanager-9kwzp`, build 0101).** ECM lets you reach `/login` even when authentication is switched off, because three surfaces (restoring a backup into a fresh instance, the MCP API key, and TLS key material) require a signed-in human admin regardless of that setting. Signing in worked, but the session evaporated on the next page load: the client skipped its "who am I" check whenever the server said authentication was not required, so a refresh or a second tab dropped you back to an anonymous view and the TLS and MCP settings sections went back to reporting "Admin access required". You had to sign in again every single time, which made the route close to useless. The client now resolves an existing session cookie whether or not authentication is required, and skips the check only before first-run setup, when there is no account for a cookie to name.

- **The header now offers a Sign in button on an instance with authentication turned off (bead `enhancedchannelmanager-9kwzp`, build 0101).** Paired with the fix above. On such an instance the app shell showed no sign-in affordance at all, so the login page was reachable only by typing the URL. The button appears whenever there is no active session and the instance has an operator account to sign in as, and it is absent otherwise: before first-run setup there is no account to sign in as, and when authentication IS required the login page replaces the app shell anyway.

- **An ECM the browser cannot reach no longer renders as an ECM that needs no login, and admin navigation no longer hides itself when there is no user to check (bead `enhancedchannelmanager-p388h`, absorbing `ee5f1`, build 0101).** `useAuthRequired()` returned `false` whenever the auth-status probe had not produced an answer, and `ProtectedRoute`'s setup probe did the same in its `catch`, under the comment "If we can't check, assume setup is not required". Two different unknowns both resolved to "no authentication here", so a degraded backend produced the full application shell with no session: every panel erroring, no login prompt (there is no global 401-to-login redirect anywhere in `httpClient.ts`), and `/login` rewritten to `/` so the operator could not reach a sign-in form even by typing the URL. That last part is what makes this a recoverability defect rather than a cosmetic one. Both probes now carry an explicit third state, and an unresolved posture renders a "Cannot reach ECM" screen with a Retry button and a working **Log in** link instead of the application. The opposite polarity is fixed in the same pass: sidebar navigation gated the Administration settings group on `Boolean(user?.is_admin)`, which is permanently false on a `require_auth: false` instance because there is no user object at all, so the group stayed hidden while the backend served it to anyone. `SettingsTab` already read a null user as PERMITTED for exactly that reason (bead `enhancedchannelmanager-9kwzp.10`); the navigation into it was the last place resolving unknown to hidden. **Operator note:** `/login` is now reachable on an auth-disabled instance that already holds an operator account, which is what bead `enhancedchannelmanager-jy006` needs: it made initial restore, the MCP API key and TLS key material require a signed-in human admin even in that mode, and the UI offered no way to sign in. Neither half changes backend enforcement, and the test suite no longer defaults every auth test to the failed-probe state that hid this.
- **The Stats watch-history filter row no longer changes layout depending on which tabs you visited first (bead enhancedchannelmanager-albfq, build 0058).** The row is rendered in exactly one place, the Watch History panel on Stats, but the rule keeping it from being squeezed by the table beside it was being supplied by two dead copies of the same class in the Journal and M3U Changes stylesheets, neither of which renders that row at all. Those stylesheets load only once you visit their tab and are never unloaded, so whether the filter row held its size depended on your navigation history in that session. The dead copies are deleted and the rule now lives with the row it belongs to, which makes the layout the same on every visit. The Journal entry detail pane's JSON boxes were leaking the same way and are likewise now scoped to Journal. Also removes an unreferenced `ChannelDetail` component that nothing had imported for some time; it was not reachable from the UI and nothing changes on screen.
- **Dev-tooling: the agent-side version-advance guard no longer forces a documentation-only pull request to burn a build number (beads enhancedchannelmanager-uf2gh + enhancedchannelmanager-9ogyd, build 0062).** The `.claude/hooks/version-advance-guard.sh` PreToolUse hook blocks `gh pr create` when the build number in `frontend/package.json` has not advanced past `dev`. It performed no path classification, so it fired on changes that carry no build to advance, while the CI half of the same gate has always skipped them: `.github/workflows/test.yml` gates its build-advance step on `needs.detect.outputs.docs_only != 'true'`. The hook was therefore stricter than the rule it mirrors, which its own header describes as "the agent-side half of the build-advance gate" whose false blocks are "a friction bug, not a safety gap". Bead `w9irb`, which created the guard, had already specified scoping it with the existing docs-only classification; only the CI half ever implemented that, so this was a half-finished bead rather than a new requirement. Measured cost: a two-line `.beads/issues.jsonl` redaction was forced to bump `frontend/package.json`, `backend/main.py` and `backend/routers/backup.py`, consuming a build number that an in-flight branch already held, which is a conflict generator whenever several PRs are open against `dev` at once. The hook now calls `scripts/classify_changed_paths.py`, the same script the CI `detect` job calls, against the same baseline ref it uses for the version comparison, and skips the check on a `docs_only=true` verdict while saying why it skipped. Classification trouble fails open with a loud warning rather than wedging shipping, because CI re-enforces the same rule server-side against the real PR head; a missing or unrecognised value for the `docs_only` key is never read as `false`, so a broken classifier can neither skip the gate nor manufacture a block. That warning is delivered rather than merely emitted: the hook contract sends the stderr of a hook that exits 0 to the debug log alone, so every non-blocking message this guard produces now travels as JSON on stdout, under `hookSpecificOutput.additionalContext` for the agent and `systemMessage` for the operator. Without that, the fail-open residual would have degraded in silence, which is the failure shape the by-key extraction removed one layer in. The blocking path is unchanged and deliberately stays stderr-only, because a hook that exits 2 has its stdout ignored. The verdict is read by key out of the classifier's `key=value` output rather than by whole-output equality, so the `docs_site_affected` line added by `t4d5w` below does not silently turn the guard into a permanent no-op. Hook routing remains broad for Bash, and the hook performs one conservative raw-literal candidate test for whitespace-separated `gh pr create` anywhere in command text. It deliberately does not interpret quotes, heredocs, substitutions, tokens or command positions: inert prose may run the repository-state check, an accepted friction direction, while candidate presence can never be narrowed into a silent bypass. Diagnostics describe a possible PR creation command rather than claiming execution. Once a candidate is found, the hook resolves the input `cwd` with `git rev-parse --show-toplevel` and performs repository-state checks only; inline `cd ... && gh pr create` is unsupported because the hook runs before the directory change, and the documented workflow opens PRs from the branch checked out in the main checkout. The hook does not parse `--base main`; the shared version checker already accepts release and hotfix versions without a build suffix. Ships with a fixture-based hook state and raw-candidate harness.
- **A documentation-only pull request now gets a check that actually reads documentation, and a green required check says what it ran (bead enhancedchannelmanager-t4d5w, build 0060).** On a documentation-only change, six of the seven required status checks on `dev` correctly gate their expensive steps off and pass having executed nothing, while `Operator Docs` - the one job that examines documentation - is not a required context. Nothing had exercised that path in practice, because `.claude/hooks/version-advance-guard.sh` blocks `gh pr create` unconditionally, so every pull request carries a version bump and none classifies documentation-only; 15 of the last 40 merged PRs would have without it. The hole is not theoretical: `backend/tests/unit/test_check_em_dashes.py` reads a repository `.md` file as a live fixture, and commit `185a4009` exists because deleting a different `.md` broke `Backend Tests`, caught only because that pull request also touched code. Three additive changes, none of which alters which contexts exist or which jobs emit them. `mkdocs build --strict` now runs on every pull request as a step of the ungated `Operator Docs` job, so a broken user-guide link surfaces on the pull request instead of as a failed Pages deploy after the merge. `Operator Docs` is not a required context, so this does not BLOCK the merge: it parks the pull request at `mergeStateStatus=UNSTABLE`, and the enforcement is the shipper reading the rollup per `docs/shipping.md` section "When `UNSTABLE` is the terminal state". Making it required is a branch-protection change and is deliberately out of scope (absorbs bead `enhancedchannelmanager-pb2s4`; it checks something disjoint from `npm run docs:check`, which passed the link that the strict build caught). `scripts/classify_changed_paths.py` gains a second output, `docs_site_affected`, that `.github/workflows/docs-pages.yml` now reads in place of its own `paths:` filter, so the list of paths the published site is built from has one definition instead of two, and a test derives the check from the `mkdocs.yml` nav so a published page added outside the known prefixes fails the pull request rather than quietly disabling its deploy. Every required job writes one line to the run summary naming what it did - `documentation-only change, the pytest suite was NOT run` versus `ran the backend pytest suite. 2147 tests, 0 failed, 0 errored` - with the counts read from the JUnit report by `scripts/ci_junit_summary.py` rather than asserted. Each line reports the real `steps.<id>.outcome` of the step that does the work, so a job that died at lint cannot claim the test suite ran, and a step that was never reached prints `did not run` rather than a count from a stale report. `docs/shipping.md` step 6 no longer tells the shipper to wait on `gh pr checks --watch`, which reports check conclusions only and is blind to merge conflicts; it now reads `statusCheckRollup` per context alongside `mergeable` and `mergeStateStatus`. CI and documentation plumbing only, with no runtime behaviour change.
- **Edit Mode no longer silently drops a channel staged into a group that is still pending in the same batch, and a partially-failed bulk commit is no longer hidden from the operator (beads enhancedchannelmanager-udq1j + enhancedchannelmanager-vtapf + enhancedchannelmanager-75k49, build 0052).** Drill run 18 (`0.18.1-0051`, Dispatcharr `0.28.2`) staged 12 channels in one Edit Mode session, one of them into a channel group still PENDING in the same batch: 11 channels were created, the 12th vanished, Edit Mode exited normally, and the operator was told nothing. The bulk-commit wire protocol resolves new groups by name (`groupsToCreate` up, `groupIdMap` back), but reaching a pending group through "Create in... -> \<pending group\>" put its negative staging id on the wire verbatim, and Dispatcharr answered `400 {"channel_group_id": ["Invalid pk \"-1000\" - object does not exist."]}`. `buildBulkOperations` now rewrites every reference to a staged group as that group's name, on `createChannel.groupId` before the group-creation phase and on `updateChannel.data.channel_group_id` once `groupIdMap` comes back. The identical defect was waiting on a second path: dragging an existing channel onto a pending group's header. A negative id matching no staged group can no longer be posted at all; the commit refuses to start and leaves the session's staged work intact rather than losing it. `_run_bulk_commit` gains a matching backend backstop, refusing a negative `channel_group_id` from any client by name instead of relaying an opaque upstream 400. Separately, the backend already reported `success=False, applied=11, failed=1` correctly for the dropped channel, but the frontend routed that into a header status pill and closed the dialog anyway; a commit that does not fully apply now holds the Exit Edit Mode dialog open on the applied/failed counts and the errors behind them, and it must be acknowledged before the session clears. "Create new channel group" inside Edit Mode called `api.createChannelGroup` directly: written straight to Dispatcharr, absent from the exit ledger, with no undo entry, and Discard could not take it back. Its catch block only logged, so a duplicate-name `400` from Dispatcharr was invisible. It now stages through `stageCreateGroup` like every other edit, and every staging failure raises a toast. The Exit Edit Mode summary line is also corrected: it previously undercounted ("24 pending changes" over lines summing to 26) because it was hand-totalled instead of derived from the buckets the dialog renders; the headline is now `summary.totalChanges`, and every staged change type has a bucket, so none can go unnamed.
- **The Move Channel to Group dialog can no longer show an enabled Move button that silently does nothing (bead enhancedchannelmanager-gddai, build 0052).** Drill run 18: dragging a channel onto another group's header opened the dialog with neither numbering radio checked while the Move button reported `disabled=false`; clicking it did nothing, every time, whenever the destination group was empty. Two faults compounded: the preselected `'suggested'` option had no backing control when the destination held no numbered channel (`suggestedChannelNumber` was null), and `isMoveButtonEnabled` only guarded the `'custom'` case while `handleMoveButtonClick` guarded both and fell silently out of the switch otherwise, so "enabled" and "will do something" were two different predicates that could disagree. `resolveMoveNumbering` now returns either an executable instruction or an operator-readable reason, and the dialog derives its `disabled` state, its click behaviour and a visible explanation from that single call, so an enabled button that no-ops is no longer expressible.
- **The Channels pane stays populated after a restore, and now has the same manual refresh control the Streams pane already had (bead enhancedchannelmanager-eelgi, build 0052).** Drill run 18 applied a restore whose own modal reported "Channels 12 CREATED"; after closing it, with no page reload, the channel-group filter listed every restored group correctly while the pane behind it read "0 Empty" / "CHANNELS 0", though Dispatcharr demonstrably held all 12. `useServerDataInvalidation` had no `channels` key, so the DBAS restore modals published only `channel-groups`. `channels` is now a key, both restore modals publish it on an applied restore (never on a dry-run preview, which changes nothing), and the Channels pane subscribes and refreshes (skipped while Edit Mode is active so a refetch cannot fight a working copy mid-session). The Channels pane also gains its own refresh control, matching the Streams pane, likewise hidden during Edit Mode.
- **A guide download that finishes after you leave EPG Manager now reaches the Edit Channel guide picker without a page reload (bead enhancedchannelmanager-1twap, build 0052).** Bead `3vtim`'s earlier fix published guide-download completion from EPG Manager's own poller, which only runs while that destination is mounted. Drill run 18 walked the ordinary fresh-install sequence, adding an EPG source and then going to link channels, navigating away from EPG Manager while the guide was still parsing. The guide picker kept its empty startup snapshot: "Guide data has not loaded yet," with zero backend requests, for a source that had already reached `status=success` with 14,663 rows. The watch moves to a module-scope service (`services/epgGuideWatch`) with its detection logic unchanged, plus its own bounded fallback poll (five minutes, only while a watched download is genuinely in flight) so completion is still detected with nobody feeding it rows. No standing polling and no refetch-on-focus outside that window, so bead `5z7c9`'s closing condition still holds.
- **A batch containing nothing but new channel groups no longer leaves Edit Mode stuck open (bead enhancedchannelmanager-ius9c, build 0052).** Follow-on to `vtapf` above, found in review rather than in the drill: new groups travel to the backend in `groupsToCreate`, a phase that contributes nothing to `operationsApplied`. Once "Create new channel group" started staging instead of writing straight through, a batch of nothing but groups (create a group, Done, Apply All, exactly how drill run 18 produced `Drill Empty Group`) came back with `operationsApplied: 0`, which the commit read as "nothing happened" and never reset the session. The groups really had been created; Edit Mode just stayed open holding them under their temp ids. A commit that creates groups now counts as a success.
- **Backup/restore drill run17 fixes three restore-path/UI defects and closes two operator-facing copy gaps (beads enhancedchannelmanager-qka89 + enhancedchannelmanager-ayfn9 + enhancedchannelmanager-3vtim, build 0051).** All facts below were measured live on the published `:dev` image at `0.18.1-0050` against Dispatcharr `0.28.2` during backup/restore drill run `2026-08-08-run17`, then re-verified after the fix in a patched container. **(1) Relink `replace` left a stale `tvg_id` (bead qka89).** Restoring with relink mode `replace` restored a channel's `epg_data_id` but not its `tvg_id`, leaving the channel resolving to one guide row while advertising another. Measured: archived ch103 had `tvg_id='KERA(PBS)(KERA).us'` and `epg_data_id=3425`; the operator changed it to `KERA-DT2(HD06)(KERADT2).us` and `3427`; after `replace` the id came back as `3425` but the `tvg_id` stayed at the operator's `KERA-DT2...` value. The artifact carried the archived value all along, in both `tvg_id` and `epg_data_tvg_id`, so this was a restore-path bug, not a backup gap; ECM's own Edit Channel modal writes both fields together. It matters because `tvg_id` is the EPG matching key and what goes out in M3U/XMLTV, so a later refresh keyed on it could silently undo the restore. `backend/dbas/channel_reattach.py` gains `_relink_payload`, which writes the channel's own archived `tvg_id` alongside the resolved `epg_data_id`, never `epg_data_tvg_id` (the two are allowed to disagree). `preserve` is unchanged and still keeps the operator's values. `tvc_guide_stationid` is deliberately not included: the reattach pass never writes it, so it has no half-restored counterpart to desync. **(2) Delete Group silently did nothing, twice over (bead ayfn9).** Deleting a channel group that still contained channels reported success and changed nothing. The confirm dialog promised the channels would move to "Ungrouped"; ECM never reparented them and issued a bare `DELETE`, which Dispatcharr rejected with `400 {"error":"Cannot delete group with associated channels"}`, and the Edit-Mode bulk commit then reported overall success anyway. Two defects: the missing reparent, and a swallowed per-operation failure. The first fix attempt reparented to `channel_group_id: null` and still failed live, because Dispatcharr rejects that too (`400 {"channel_group_id":["This field may not be null."]}`); a channel cannot be group-less, so "Ungrouped" is a render state ECM can read but never write. `backend/routers/channels.py` now resolves a real destination by name (`Default Group`, never by assuming id 1), reparents the members to it, then deletes; it refuses with an actionable message when no such group exists or when the group being deleted is the destination, and an empty group still deletes with no destination needed. The bulk commit's `success` is now `operationsFailed == 0` unconditionally, so one successful operation can no longer launder a failure beside it. The dialog copy now names the real destination. Verified live: 6 channels reparented to `Default Group`, `DELETE ... 204`, `success=True, applied=1, failed=0`, nothing lost. **(3) Stale client caches made present data look missing (bead 3vtim).** After adding an EPG source that reached `status=success`, the Edit Channel "EPG Data" picker reported "No EPG data found" for guide rows that demonstrably existed (14663 rows including `KERA`), with zero `/api/epg/*` requests reaching the backend during the failure, a stale client cache, not a query bug, until a full reload. The channel-group list had the same problem after a DBAS restore. Both now publish on the existing `useServerDataInvalidation` bus (no polling, no refetch-on-focus, so bead `5z7c9`'s condition still holds); guide-download completion is detected on the rows themselves and fed from all three fetch paths, anchored on `updated_at` so a stale `success` reading cannot satisfy it, and a source already downloading when the tab mounts is adopted rather than missed. `EditChannelModal` also gains a distinct "Guide data has not loaded yet" empty state so an unloaded cache no longer renders as "no match". **(4) Two operator-facing copy fixes.** The restore-complete summary pluralises correctly ("1 account needs ... before it will work"), and the unplayable-channels note now points at `stream_reattach_details` (which already carried the names) instead of promising "each named channel" without saying where. Documentation: `docs/user_guide/backup-restore/run-a-restore-drill.md` was updated in the same change to record the three-step Edit-Mode commit, the type-to-confirm restore apply, the two known-defect notes, and the two-step redacted-artifact recovery.
- **An empty channel group can now be renamed and deleted from Channel Manager, instead of only from Dispatcharr (bead enhancedchannelmanager-o88e9, build 0049).** In **Channel Manager → Edit Mode**, the `⋮` **Group actions** menu on a group header is the only place ECM offers **Rename Group** and **Delete Group**. It was hidden for any group with zero channels, so a group lost both actions the moment its last channel left, and a group created by ECM's own **Create new channel group** button never had them at all. Backup/restore drill run 2026-08-08-run16 measured it on published `:dev` `0.18.1-0047` by reading control state out of the DOM: of three groups in one list, the one holding 7 channels had the menu and the two holding none did not. The empty state is exactly the one an operator needs the menu in. An empty group created by mistake, or left behind after its channels were moved elsewhere, could not be removed from the screen that created it, and the group-deletion step of [Run a restore drill](docs/user_guide/backup-restore/run-a-restore-drill.md) (which has you move a group's channels out first, because Dispatcharr refuses to delete a group that still has channels) dead-ended in ECM and had to be finished in Dispatcharr's UI. The `!isEmpty` guard was inherited: it was written for the standalone "sort streams by quality" button this menu replaced in `0.14.0-0021`, where needing channels was correct, and it was carried over wholesale onto the menu that later absorbed Rename and Delete. The menu is now gated per item rather than as a whole. **Rename Group** and **Delete Group** do not depend on members and are offered on an empty group; **Probe Group**, **Sort Streams** and **Sort & Renumber** do, and stay hidden until the group has some. **Delete Group remains restricted to manually created groups.** That was never a member-count rule, and a group backed by an M3U provider is still not deletable from ECM, empty or not, because the next provider refresh recreates it.
- **A restore that completed with warnings no longer reports itself as failed to scripts and log greps (bead enhancedchannelmanager-bdmby, build 0048).** Backup/restore drill run 2026-08-08-run16 measured a DBAS restore that ended `completed_with_failures` with zero failed items in every category and nothing rolled back. Its task-history row said `completed_with_warnings`, its alert was a `warning`, and the restore dialog said "nothing was rolled back". But `GET /api/tasks/dbas_restore` published `progress.status: "failed"` beside `failed_count: 0`, and the log carried `task_scheduler [dbas_restore] Task failed: ...` one line away from `task_engine [dbas_restore] Task completed in a degraded state: ...` for the same event. `progress.status` is the field a script polls to know a restore has finished, and `Task failed` is a string the troubleshooting guide tells operators to grep for, so both gave a false positive on every degraded restore. The live progress status and the finalized progress notification now come from the same single derivation as the history row, so the three cannot disagree, and the scheduler logs "Task completed with warnings" for a run that completed and kept its state. `failed` stays reserved for runs that genuinely failed: a rolled-back restore, an orchestration error, or an exception. A cancelled run now reports `cancelled` rather than `failed`.
- **A required CI check can no longer report green from a job that ran nothing (bead enhancedchannelmanager-5rwzy, build 0047).** `dev` and `main` branch protection require seven status checks. Until now two workflows could emit the same name on one commit: `test.yml` ran on `paths-ignore: ['**.md', '.beads/**']`, and a sentinel workflow, `docs-only-pass.yml`, ran on `paths: ['**.md', '.beads/**']` with jobs named exactly those seven whose only step was an `echo`. Those filters look like complements and are not. A pull request touching both code and Markdown matches both, so every required context existed twice: once real, once permanently green. Every shipped change carries a `CHANGELOG.md` entry, so that is the shape of nearly every pull request in this repo. It went live on PR #797, where `Backend Tests` reported **`failure` and `success` on the same commit**; the failure was a genuine test defect and was caught only because the reviewer read each instance of the context individually rather than the aggregate. The fix is one source of truth per context. Neither `test.yml` nor `build.yml` has a path filter any more, so both run on every pull request and each required name is emitted exactly once. A new `detect` job in each classifies the changed file set through `scripts/classify_changed_paths.py`, which now holds the only definition of the documentation-only rule. Every job carrying a required name **always runs** and gates its expensive **steps** on that classification, so a genuinely documentation-only change does a cheap no-op and passes honestly instead of being covered for by a stub. `docs-only-pass.yml` is deleted. Two deliberate non-choices, both recorded in `docs/testing.md`: skipping the duplicate job would not have worked, because GitHub counts a skipped job as satisfying a required check, and making the duplicate fail would have blocked every mixed pull request. The classifier fails **open**: any input it cannot classify is treated as code, because green-without-running is the only dangerous direction. CI plumbing only, with no runtime behaviour change.
- **Restoring onto an install that already has channels now tells you which channels the backup puts in a different group — and "Replace" puts them back (beads enhancedchannelmanager-r1ei7 / -3t74w / -tddmw).** Every restore drill before run 12 (2026-08-07) restored onto a freshly-wiped target, where every channel is created and the destination has no opinion to disagree with. Run 12 restored the same artifact onto a *populated* target and found three things a clean-looking report was hiding. **(1) Channel grouping was never reconciled and never mentioned.** A channel that already exists is matched and deliberately never overwritten, so its group was never written: seven channels stayed in whatever group the target had them in, in *both* relink modes, while the restore reported `success` with `0 failed` and counts that reconciled exactly. The relink choice now covers grouping as well as guide data and logos: **Keep their current guide data, logos, and grouping** reports every channel whose group differs — naming the channel, the group it is in, and the group the backup assigns it — and still changes nothing; **Replace their guide data, logos, and grouping with the backup's** additionally moves each one back, and still lists what it moved. The count is predicted on the preview, so the decision can be made before applying rather than read about afterwards. And if you leave the **Channel groups** category *unticked*, the grouping check cannot run at all — so the restore now says so in as many words, in the results panel and in the task-history line, rather than showing a zero that looks like a clean bill of health. **(2) A same-named group is no longer reported as "identical".** A channel group carries nothing but a name, and its contents live on the channels, which are restored after it — so at match time there is nothing to compare. The restore still adopts a destination group on its name (that is the only identity a group has across two installs, and refusing would strand every channel pointing at it), but the report now says *matched by name* instead of asserting an "identical" it never checked. **(3) The preview no longer omits the Streams category.** The apply reported `Streams 9 CREATED` while the preview showed no Streams row at all — not zero, absent. Streams genuinely cannot be predicted (they are matched against provider streams that only arrive during the restore itself), so the row now renders as **not predicted** with an explanation, and the Channel groups row carries a note saying its create/skip split can differ from the apply for the same reason.
- **A saved setting, an uploaded logo and a freshly created backup now appear where they belong, without reloading the page (bead enhancedchannelmanager-5z7c9, build 0039).** Drill run 9 hit the same defect three times in one session, and each time the screen said a write had not happened when it had. **(1)** Saving the Dispatcharr connection left the Settings → General summary card reading *"Server URL: Not configured / Auth Method: Username & Password"* while `GET /api/settings` was already returning the saved URL and `auth_method: api_key`. **(2)** A logo uploaded through Logo Manager appeared in Logo Manager and landed in Dispatcharr's `/data/logos/`, but the Edit Channel logo picker kept offering 12 options instead of 13, and searching the new logo's name found nothing. **(3)** An encrypted backup created from the Encrypted Backup card was written and announced with a success toast, yet the Saved Backups list on the same page kept showing only the previous artifact — including after navigating to another settings section and coming back. All three needed a full page reload. One root cause, not three: ECM keeps server data in whichever component renders it, refetched on that component's own mount, and there was no way for a component performing a write to tell a *different* component that its copy had gone stale. The connection modal is rendered both by the Settings tab and by App (which opens it unprompted on a first run) and only one of those refreshed the card; Logo Manager owns its own paged list while the picker reads App's catalogue; the Encrypted Backup card and the Saved Backups list are siblings that had never been introduced. There is now one small invalidation channel: a component that mutates something publishes the affected key, and the components holding a copy refetch. Deliberately **not** a cache, **not** polling, and **not** refetch-on-focus — trading a cosmetic staleness bug for standing background load against Dispatcharr would be the worse deal. A failed mutation publishes nothing, and a plain page view still issues exactly the fetches it always did. The third instance is the one that mattered most: the action an operator repeats when they think a backup failed is "create another credential-bearing encrypted backup".
- **The M3U password field no longer tells you to "leave blank to keep current" when there is no current password to keep (bead enhancedchannelmanager-tsbdq, build 0039).** A standard backup replaces the recognized provider credential fields, so after restoring one the M3U account's password is genuinely empty. This is correct, explains why the documented Step 6a recovery exists, and is recorded account-by-account and field-by-field in the restore report's `credentials_needing_reentry`. The edit form still showed the generic *"Leave blank to keep current"* hint, advising the one action that guarantees the account keeps failing to authenticate, in precisely the situation the recovery flow puts the operator in. When the account holds no stored password, the field now says so and asks you to enter it. It also carries a warning-level note explaining that a standard backup replaces that structured credential field, so the empty field is expected and must be filled. When a password really is stored the wording is unchanged, because there it is correct. The signal is the account's own `password` field, which is truthful rather than a redaction sentinel. The drill's inventory captures record it `present` with a stable fingerprint before the backup and `absent` after a redacted restore, on the same account.
- **A backup that archived fifteen categories and missed one no longer reports "0 ok, 1 failed" (bead enhancedchannelmanager-fexq1, build 0038).** When a Dispatcharr category cannot be fetched, the backup is still built, verified and restorable — it is just missing that one category, and the run is reported as a warning naming it. But the numbers alongside that warning were a boolean wearing the shape of item counts: one total item, and one of `ok`/`failed` set to 1. So the Journal row, the task-history row and the progress notification all read **`0 ok, 1 failed`** for a run that produced a perfectly usable artifact, which reads as total failure to anyone scanning at a glance — the same problem the degraded-backup warning was introduced to solve, one surface over. The counts now mean what they say: the item is a **category**, so a backup that archived 15 of 16 reports `15 ok, 1 failed`, and two degraded categories are two failed items rather than one degraded run rounded to a boolean. Even the worst case is honest — when Dispatcharr is entirely unreachable, the local settings, rules and profiles that *did* archive are counted as the successes they are instead of the whole artifact being written off. A clean backup's completion notification now names the unit too ("Backup artifact built. All 16 categories archived in 1.8s" rather than "1 items processed"). Severity is deliberately unchanged and pinned by tests: a clean backup still reports success, a degraded one still reports a warning, and a failed cloud upload still reports an error — the counts got more honest without moving what any run is called.
- **A restore that completed no longer announces itself as "Task Failed", a single failed logo is no longer counted as two, and the restore-complete screen now tells you when nothing can play (beads enhancedchannelmanager-k2r7m + enhancedchannelmanager-cwmid + enhancedchannelmanager-d0bd3, build 0036).** Three operator-facing reporting defects found by the backup/restore round-trip drill on build 0035, all in the exact surface an operator reads to decide what to do next. **(1) One failed logo counted twice.** Two independent passes report the same lost logo — the importer, which knows the logo's name, and the channel-reattach pass that then cannot resolve the reference and only has the archive id. Both reports were correct and both were appended, so a restore whose category counts read `FAILED 1` announced "2 logo(s) could not be reinstated" in the same sentence, sending the operator hunting for a second broken logo that did not exist. Reports of one archived logo now merge into one row keyed on the archive id, keeping the operator-facing name over the synthesized `logo #13 (archived)` label and the union of the affected channels; a miss with no archive id still gets its own row, because under-counting a real loss is the failure this surface exists to prevent. **(2) The severity ordering was inverted.** A restore degraded only by that non-fatal logo miss alerted as `error` / "Task Failed: DBAS Restore", while a restore in which **not one of twelve channels could play** correctly alerted as a warning — same `outcome` value, opposite severity, and the louder one was the cosmetic case. "Task Failed" was also untrue: the restore completed and rolled nothing back, and the docs warn that re-running a restore after a failed attempt starts from an unknown state, so the false alarm could push an operator into a genuinely risky action. The severity now follows the outcome itself — a restore that reached `completed_with_failures` raises a **warning** regardless of which non-fatal category degraded, and `error` is reserved for the two rolled-back/indeterminate outcomes. **(3) The restore-complete screen never said "nothing can play".** The report correctly carried 12 of 12 channels with no playable stream and named every one, and the dialog rendered only the credentials panel and the count grid — while the documentation tells operators to read those panels instead of re-deriving the information by hand. A new panel sits beside the credentials one, naming the affected channels and keeping the two populations apart: channels holding a leftover placeholder slot alongside a real stream still play and are tidy-up, while channels with no URL-bearing stream at all cannot play and are the reason the instance is unusable. **(4) The history row and the alert contradicted each other about the same run (bead enhancedchannelmanager-fexq1).** A run's severity was re-derived at four separate points from two fields that do not carry it — `success` and `failed_count` — so they disagreed: the degraded restore above alerted correctly as a warning while its task-history row read `status: "failed"`, `success: false`, its Journal line read `Failed DBAS Restore`, and the History panel had to guess the middle state in the browser from `success && failed_count > 0` — a guess that cannot see a degraded run whose counts are clean. There is now one derivation (`task_outcome`) producing an explicit outcome — success, warning, error or cancelled — and the notification, the Journal row and the stored history row all map from it. The history vocabulary gains `completed_with_warnings` (migration 0042 widens the column to hold it); the Journal leads with the severity instead of writing `Completed X: 0 ok, 1 failed` beside `success: true`; and the restore modals, which polled for `completed` or `failed` only, accept it too — without that a degraded restore would have polled until its retries ran out and reported "Restore failed" for a restore that completed. Deliberately unchanged: the `ecm_task_schedule_last_success_timestamp` gauge behind the stale-schedule alerts keeps its existing trigger, so a degraded run still does not reset the staleness clock and cannot mask a task that has stopped running. Rows written by earlier builds keep rendering exactly as they did.
- **After restoring a standard backup, re-entering your provider password and refreshing now actually fixes your channels without a second restore (beads enhancedchannelmanager-2o0cz + enhancedchannelmanager-juu3c, build 0033).** A standard backup replaces the recognized provider credential fields and credential-bearing URL values, so the restored account cannot authenticate and the restore leaves every channel on a placeholder. Re-entering the credential and refreshing the M3U account is the obvious next move, but until now it changed nothing you could see: the real streams appeared *beside* the placeholders and not one channel was rebound, because the reattach pass ran only once, during the restore itself, before the credential existed. Recovering actually required running the entire restore a second time, which nothing told you. A completed M3U refresh now reattaches any channel still holding a leftover placeholder, so the recovery is: **restore → re-enter the credential and refresh.** Measured end to end: 12 channels went from unplayable to all playing after the refresh alone, with the leftover placeholder streams and their synthetic account cleaned up in the same pass. Only ECM's own placeholder streams are ever touched. A custom stream you created yourself is never rebound or removed. Covers the Refresh button and the scheduled refresh task; a refresh-all, or one done in Dispatcharr's own UI, is picked up on the next scheduled refresh rather than instantly. Separately, a dry-run summary now uses future tense throughout. It previously reported profile memberships as "corrected" for changes it would only make on apply.
- **The restore preview now tells you what the apply will actually do, and a restore no longer leaves dead placeholder streams behind (bead enhancedchannelmanager-dgnms, build 0032).** Previewing a restore is the step operators are told to trust before committing, but its health numbers were read off work the preview never does. On a fresh target a dry run reported **0 logos to reattach** where the apply reattached 11, **0 channels needing attention** where the apply reported 12 unplayable, and **0 profile memberships to correct** where the apply corrected 6 — confident zeroes derived from having looked at nothing. The per-category create/update/skip counts were always accurate and are unchanged. Now the logo split and the profile-membership count are genuinely predicted and match the apply exactly; and the two stream-health counters, which cannot be known before the apply's deferred M3U refresh materialises the provider streams, report **null — "not predicted"** rather than a misleading `0`. Separately, every redacted restore cycle used to leave placeholder streams behind: cleanup only removed placeholders created by the current run, so ones stranded by an earlier restore accumulated forever (14 of them, attached to nothing, after a single round trip). A restore now also removes placeholder streams that sit on ECM's own synthetic account, carry no URL, and are referenced by no channel — and drops that synthetic account once nothing is left under it, regardless of which run created it. Nothing an operator created themselves is ever touched.
- **A restore that finishes with warnings no longer also shows you a red error, and the encrypted-backup options are finally discoverable over the API (beads enhancedchannelmanager-asf3n + enhancedchannelmanager-sdpzy, build 0031).** A single task run touches two notification records — the completion notification, and the progress notification created when the run starts and re-typed when it ends — and each decided its own severity. Only one of them knew about the "completed with warnings" rule, so a DBAS restore that degraded showed an operator a **warning and an error side by side for the same event**, with nothing to say which was authoritative. Both now read from one shared severity map, so a run reports exactly one verdict; the wording of each notification is unchanged. Separately, `GET /api/tasks/dbas_backup/parameter-schema` reported *"No configurable parameters"* even though the nested `parameters` body of a manual run is the **only** way to produce an encrypted, credential-bearing backup — and sending those fields at the top level instead is silently ignored, handing back a plain artifact to a caller who believes otherwise. The task now declares them and the endpoint surfaces them under a separate **`run_parameters`** key. They are deliberately kept out of `parameters`: that list is the schedule form and is persisted with the schedule, and a stored passphrase would make every unattended run emit an encrypted credential-bearing artifact.
- **A channel whose streams' names differ only in capitalisation now restores completely, and a repeat restore no longer reports a clean bill of health over a channel that cannot play (beads enhancedchannelmanager-ixdaw + enhancedchannelmanager-oebpv, build 0029).** The restore's exact-name matching tiers compare case-folded names, so a destination pair differing only in case was a single match to them: a channel archived with `TX | DALLAS | PBS KERA` and `TX | Dallas | PBS KERA` had **both** slots handed the same stream while the byte-identical match for the other sat unused. On an encrypted backup that silently cost the channel a stream; on a standard backup the duplicate made Dispatcharr reject the entire channel update (`unique_channel_stream`) and the channel would not play at all. Each exact-name tier now prefers a candidate whose raw name matches byte-for-byte before falling back to case-folded comparison — a restore with no such pair behaves exactly as before. Separately, the "does this channel still play?" audit only ever examined placeholders created by the *current* restore, so a channel left stranded by an **earlier** restore was invisible: a repeat run reported zero channels needing attention while one answered HTTP 500. The audit now covers every restored channel, judged on what it is actually left holding, and names the offending stream properly instead of `<unknown>`. A channel holding a leftover placeholder alongside a working stream still reports success, unchanged. Verified end-to-end on a disposable stack: the full backup → destroy → restore round trip now reproduces the instance with **zero** inventory drift, and the affected channel comes back with all three streams in their archived order and plays on both backup formats.
- **A restored channel carrying two similarly-named streams is no longer left unplayable (bead enhancedchannelmanager-ixdaw, build 0026).** Found on the 2026-08-05 backup/restore round-trip drill (run 3, ECM 0.18.1-0024 against Dispatcharr 0.28.2), reproduced on both artifact variants. Eleven of twelve channels played; the twelfth — the only one with more than one stream — came back bound to three empty placeholder streams and returned an error instead of video, while the restore still reported `success` with zero failures. The seed that triggered it was three streams named `TX | Dallas | PBS KERA`, `TX | DALLAS | PBS KERA` and `TX | Austin | PBS KLRU`: the first two differ **only** in capitalization. When the restore re-runs its stream matcher after your provider's streams have loaded, it compares names case-insensitively — correctly — so both of those slots were pointed at the same provider stream. Dispatcharr does not allow a channel to hold the same stream twice and rejected the update outright, and because that update is all-or-nothing, ECM's own error handling then put the channel back on placeholders **including the KLRU slot that had matched perfectly well**. One ambiguous stream cost the whole channel. The rebind now tracks which streams a channel has already been given — including ones the restore had matched for real earlier in the run — and when a second slot resolves to a stream already taken, that one slot keeps its placeholder and is listed in the restore report's "needs a stream reattached" section like any other unmatched stream. The channel's other streams rebind normally and it plays, the archived stream order is unchanged, and the one ambiguous slot is a named, one-click fix in the UI rather than a dead channel. Re-running the restore no longer helps or hurts — it was previously the same collision every time. The operator workaround documented for build 0025 is no longer needed.
- **Channel EPG links now survive a restore, and a restore no longer resets the guide data and logos of channels you already had (bead enhancedchannelmanager-dfkbn).** The second half of the round-trip drill's `dfkbn` finding. Run 2 (ECM 0.18.1-0023, Dispatcharr 0.28.2) measured all 7 EPG-linked channels coming back with no guide link on both artifact variants, honestly reported as `epg_links_unrestored: 7` but still lost. **The cause was in the BACKUP, not the restore.** A Dispatcharr EPG row's id is instance-local and is re-minted whenever the destination re-downloads its guide, so the restore correctly relinks by `tvg_id` instead. But a channel's OWN `tvg_id` field is not the link, and ECM's own channel API leaves it null when it sets a guide link (verified directly: `PATCH {epg_data_id: 1}` leaves `tvg_id` null). Every archived channel therefore reached the restore with nothing to match on. The backup now resolves each linked channel's guide row at gather time and records THAT row's `tvg_id` alongside the link, in one bounded read for the whole export rather than one per channel. Old backups are unaffected and restore exactly as before; new backups stay readable by older ECM versions. **Second, a choice about channels you already have.** Making the link restorable also made it reach channels a restore MATCHES rather than creates, so restoring into a live, populated install to recover something else (channel-profile membership, say) would have silently reset the guide link and logo of every matched channel to the backup's view, with no way to undo it. The restore dialog now asks, defaulting to **keeping your channels as they are**; replacing them is an explicit choice. On an empty target every channel is created and the two options do exactly the same thing, so disaster recovery is unaffected. The preview reports that split before you commit to anything: how many links land on channels the restore creates versus how many would replace one you already had, naming those channels. It is careful about what a preview can honestly know. It never claims a guide link or a logo is lost, because on a fresh target the guide and the logos are things the restore itself puts there, and a preview that resolved against the not-yet-restored state would report a working restore as a total failure. **Third, backups now tell you when a link cannot come back**: a channel pointing at a guide entry that no longer exists is counted in the backup's task result rather than only logged, and a guide read that hit its size ceiling says so distinctly instead of reporting perfectly good links as broken. That count is informational and does not mark the backup as degraded: a dangling guide reference is common and usually nothing you can act on.
- **Uploaded channel logos are now backed up, a logo failure no longer destroys the whole restore, and the restore preview stops inventing logo failures (beads enhancedchannelmanager-xb58a, enhancedchannelmanager-d0agi, enhancedchannelmanager-dgnms).** Found on the 2026-08-04 backup/restore round-trip drill, which surfaced one chain of defects with three separate causes. **(1) The backup never carried the image.** A logo you upload through ECM's Logo Manager is written to *Dispatcharr's* `/data/logos/`, because Dispatcharr is ECM's source of truth for logos; the artifact's binary subtree only ever collected ECM's own (in practice empty) `/config/uploads/logos/`, so `binary/metadata.json` read `{"logo_count": 0}` on an instance that had an uploaded logo assigned to a channel, and that logo could not be restored on any artifact variant. The premise behind that design, that the bytes were unreachable, was false, and the drill proved it by accident: fetching the logo over Dispatcharr's own cache endpoint with the API key ECM already holds returned a byte-identical copy. The backup now fetches the bytes of every Dispatcharr-hosted logo at gather time and archives them with their filename, one payload at a time through a temp spool so memory stays bounded. Genuinely remote CDN logos are unchanged: they round-trip from their archived URL (verified 10 of 10 byte-identical) and archiving their bytes would be waste. **(2) One unwritable image rolled back the entire restore.** A logo failure was classified as fatal, so a restore that had already created 44 entities was aborted and compensating-rolled-back because a single image could not be written. The drill lost an M3U account, an EPG source, channel groups, a profile, 12 channels and 14 streams to one missing picture. Logos join Dispatcharr users as a non-fatal category: a logo that cannot be restored is counted, named in the report, and the restore completes as *Completed with failures* with nothing rolled back. Nothing else in a restore depends on a logo, and logos run last, so a logo failure could only ever destroy work that had already succeeded. **(3) The preview cried wolf.** The dry run skipped the URL re-create path entirely, so it reported all 11 archived logos as `validation_error: unsafe or empty logo filename` for an artifact whose apply then restored 10 of them, and buried the one genuinely unrestorable logo in ten invented failures that read identically. An operator following the product's own "preview first, always" guidance would reasonably have aborted a restore that was going to work. The preview now simulates the same decision the apply makes and its per-category counts match. Also fixed alongside these: a restored logo is no longer reported to you as lost (the red "N logos are missing after this restore" banner and the summary line now count only logos that actually failed to come back, with the definition written down in one place for all three producers); a stale copy in ECM's own uploads directory can no longer shadow the authoritative Dispatcharr image and silently leave a channel on the wrong artwork; and a backup that could not fetch some logo bytes is reported as a degraded, warning-level run naming the count, instead of a clean success.
- **DBAS restore round-trip drill: six defects that let a "success" restore leave channels unplayable, credentials broken, and settings/logos/EPG links silently dropped (beads enhancedchannelmanager-lvfwd, enhancedchannelmanager-y65si, enhancedchannelmanager-6pilh, enhancedchannelmanager-2o0cz, enhancedchannelmanager-dfkbn, enhancedchannelmanager-cytzj, build 0023).** A round-trip drill (bead a429n, ECM 0.18.1-0022 / Dispatcharr 0.28.2) restored onto a genuinely fresh Dispatcharr and reported "Restore success: created 32, failed 0" for an instance where nothing played. **Two P0 whole-restore aborts** (lvfwd, y65si): a stream profile bound to a custom user agent 400'd the entire restore because user agents imported AFTER stream profiles with the agent id sent unremapped — user agents now import first and the FK remaps through the same table every cross-category reference uses; and a Dispatcharr user-create failure (missing password field) 500'd and rolled the whole restore back — user creation now sends a fresh random password requiring an operator reset, and a user-row failure is the one category that no longer aborts the run (new `RestoreOutcome.COMPLETED_WITH_FAILURES`). **Redacted-artifact credential corruption** (6pilh): restoring a STANDARD (redact-by-default) artifact wrote the literal `***REDACTED***` string into the destination's M3U/EPG password field — worse than leaving it empty, since it reads as "configured" — now stripped at import, with the affected field named in a new `credentials_needing_reentry` report section. **Nothing playing** (2o0cz): the M3U account's enabled-group selection didn't round-trip (a group that was merely enabled, not auto-syncing, restored to PENDING SETUP with 0 groups and never ingested anything), and archived streams synthesized as URL-less placeholders were never rebound once the deferred M3U refresh ran — both fixed, with whatever still can't be resolved now counted and named instead of silently passing. **Silent data loss** (dfkbn): logos, EPG links, channel-profile membership, and ECM's own `settings.json` (timezone, poll interval) were all dropped by the restore with nothing counted as a failure or a skip. Logos, profile membership and ECM settings are restored by this build, and a miss is now reported by name instead of read as zero. **EPG links were NOT fixed by this build.** A second drill run on 0.18.1-0023 measured all 7 linked channels still restoring with no guide link, correctly reported as `epg_links_unrestored`. See the separate EPG-link entry above for the fix that lands it. **Encryption leaking into scheduled runs** (cytzj): a one-off "Create Encrypted Backup" left the passphrase / credentials-included / unrecoverable-acknowledged flags live on the backup task's singleton instance, so every LATER run — including an unattended scheduled one — silently produced an encrypted, credential-bearing artifact under that same passphrase until the container restarted; the transients now reset after every run, success or failure.
- **Dispatcharr developer API docs corrected three stale/missing contracts (bead enhancedchannelmanager-r9oqx, build 0021).** `docs/dispatcharr_api.md` and `docs/architecture.md` said the schema endpoint was `/swagger.json`, "returns YAML despite the name" — wrong for Dispatcharr 0.28.2, where the real endpoint is `GET /api/schema/` (drf-spectacular, YAML by default; `?format=json` is required). This staleness sat behind three shipped bugs before anyone re-checked it against a live instance. Also newly documented: the `/api/core/settings/` list endpoint and its integer-pk-keyed detail route (the `q6xjl` 404 root cause), the list→key→id resolution pattern ECM now uses (one GET per run, bare-list vs. DRF-paginated-envelope handling, a 20-page cap, and a drop-don't-guess rule for unusable rows), and that `/api/dvr/rules/` does not exist on any Dispatcharr version — the real recurring-rules resource is `/api/channels/recurring-rules/` (`lsa0s`), series rules live inside the `dvr_settings` core-setting, and `/api/channels/dvr/comskip-config/` exists but returns only `{path, exists}` on GET and isn't a usable backup source. Added a "how to verify a path against the live schema" note citing the project's recorded fixtures as the in-repo source of truth.
- **Upload-path DBAS restore no longer applies with one click — it now requires typing the uploaded file's name in a confirm dialog, matching the saved-backups path (bead enhancedchannelmanager-d20jf, build 0020).** Follow-on F from the q6xjl investigation (the missing confirmation dialog was called out but explicitly not covered by the q6xjl fix itself, build 0014). `DbasRestoreSavedModal` has gated its "Apply these changes" action behind a `TypeToConfirmDialog` since build 0019 (bead rzhid); `DbasRestoreModal` — the operator-uploaded-file path — never had one (`git log -S TypeToConfirmDialog` on that file was empty; the guardrail was never present, not removed). Two entry points called `start(true)` directly with no confirmation: the post-dry-run "Apply these changes" button, and the configure-step "Apply" radio + primary button, which could skip the dry run entirely with only a static warning banner. Both are now gated behind the same `TypeToConfirmDialog`, requiring the operator to type the uploaded file's name (the saved path's equivalent token is the artifact filename; the upload path has no server-side filename until the file is saved, but the `File` object's own name is present and stable for the whole modal lifetime, so it plays the identical role). The configure-step trigger button reads "Apply restore…" (ellipsis) rather than "Apply restore" so it cannot be confused with the confirm dialog's own "Apply restore" button once both are on screen. Preview (dry run) is unaffected — still one click, no confirmation required.
- **DBAS backup no longer reports a clean SUCCESS when a Dispatcharr category couldn't be fetched — it's now a named "Completed with Warnings" (bead enhancedchannelmanager-zt3kf, build 0019).** Follow-on D from the q6xjl investigation. `_gather_dispatcharr_sections` has always converted an upstream fetch failure into a `{"_warning": ...}` stub written into the artifact in place of that category's real data — but nothing downstream inspected it, so a backup missing an entire category (e.g. every `dvr_rules.yaml` on a run where Dispatcharr errored) still reported a clean `TaskResult(success=True, failed_count=0)`, both in Task History and in the completion notification. The gather is now also isolated per category as an explicit contract rather than an accident of the artifact builder's one-key-at-a-time call pattern: each Dispatcharr section's upstream fetch is wrapped in its OWN try/except, so one failing endpoint degrades only that section, never its siblings requested in the same export call (the legacy `/export?sections=a,b,c` path could previously lose ALL of `a,b,c` to one failing fetch). `build_backup_artifact` now reports which categories degraded on the returned artifact; `DbasBackupTask` threads that into a WARNING-level result (`success=True`, `failed_count>0`) that reaches task_engine's existing "Completed with Warnings" branch — reusing that branch, not a new notification path — with a message naming the degraded category/categories so an operator never has to open the artifact to find out what's missing. A clean gather is unaffected (unchanged clean-success envelope). Separately, the PO reaffirmed the DBAS restore rollback policy (parked note from q6xjl): a settings-key `DEPENDENCY_UNRESOLVED` failure still aborts the WHOLE restore and triggers full rollback like any other category failure — no per-key skip-with-warning — and the orchestrator now documents that rule's rationale and adds an explicit report note after such a rollback stating that a retry against the same destination cannot succeed (the archive references a settings key the destination doesn't have; the fix is to edit category selection or restore against a different destination). Two documentation-only parked notes also land: a one-line comment on the duplicate-settings-key last-write-wins assumption in `get_core_setting_id_map`, and an enum-reuse annotation on `FailureReason.DEPENDENCY_UNRESOLVED` noting its settings-key-absent reuse (no new enum value).
- **Operator user guide: defect-sweep fixes from doc-test runs 2-3 land, validated three independent ways (build 0018).** The documentation fixes queued behind runs 2 and 3 of the operator-guide test (`~/ecm/doc-test-runs/2026-08-02-run2/`, run 3's targeted re-verification of those fixes) are shipped as-is per PO decision 2026-08-03. Validation: run 3 re-walked each fixed article and confirmed the specific defect was gone; run 4 then performed a naive full walk with no foreknowledge of what had changed and found the day-one path clean of defects (a first for the series) and a 102-page site-link sweep clean; and `mkdocs build --strict` passes against this exact tree. Three MINOR/MAJOR findings from run 4 itself — a missing Result line on the encrypted-backup walkthrough, a fresh-install no-op in the normalization first-rule article (all 8 shipped rule groups start disabled, so "see what your rules do" has nothing to show), and a Built-in/Custom label mismatch — are deliberately **not** fixed here; they're tracked in bead `enhancedchannelmanager-63lik` for a follow-up pass.
- **Backups no longer ship an empty "DVR Rules" category — ECM was asking Dispatcharr for a URL that doesn't exist (bead enhancedchannelmanager-lsa0s, build 0017).** The third defect from the same doc-test run as q6xjl. ECM fetched DVR rules from `/api/dvr/rules/`, a path guessed when the category was built and never live-verified. Dispatcharr 0.28.2 has no such route, and — because the miss lands on the frontend's single-page-app catch-all rather than the API router — it answered **200 with an HTML page** instead of a 404. The JSON parse then died on the first byte, and the backup builder converted that into a `_warning: Dispatcharr not connected` stub for the category, on **every backup run**: the artifact looked complete, and the DVR Rules category inside it was empty every time. Restoring one silently restored nothing, and a restore *to* Dispatcharr would have failed 405 on the same dead path. The category is now bound to Dispatcharr's real DVR-rule resource, `/api/channels/recurring-rules/` (a recurring recording rule: channel, weekdays, start/end time, date window, enabled flag), which matches what the restore importer already expected — one `channel` foreign key remapped through the restored channels, name-based collision detection, and delete-based rollback. Two neighbouring DVR surfaces are deliberately **not** in this category, both to avoid restoring the same state twice or restoring state that cannot mean anything on the destination: **series rules**, which Dispatcharr stores inside the `dvr_settings` row that the Core Settings category already backs up and restores; and **recordings**, which are individual scheduled or completed recording instances pinned to absolute timestamps and to a media file on the source instance's disk. Three further corrections ship with it. (1) Unnamed rules are now matched by their schedule (channel + weekdays + clock window + date range + enabled state) rather than by name alone — Dispatcharr leaves a rule's name optional, so restoring the same artifact twice used to duplicate every unnamed rule. The enabled state is part of that identity on purpose: an archived rule that is switched *on* is not satisfied by a switched-*off* rule already occupying the same slot on the destination, so restoring it never silently leaves your recordings disabled. (2) A rule whose channel didn't survive to the destination is now reported the same way by a dry-run preview and by a real apply — same `DEPENDENCY_UNRESOLVED` reason, same message; the preview previously called it a skip while the apply called it a failure (the same preview-lies class as y6zg6). (3) A code comment claiming Dispatcharr has no separate comskip endpoint was wrong — `/api/channels/dvr/comskip-config/` exists — but it is still not a backup source: its GET returns only whether a custom `comskip.ini` exists and where, never the file's contents, and its POST takes a file upload. Comskip **settings** continue to come from the core-settings namespace exactly as before. **Existing backups keep restoring**: an artifact taken before this fix carries the warning stub instead of a rule list, and now decodes to an empty DVR Rules category (a reported "0") rather than a gap or an error, with every other category unaffected. Every endpoint claim above is pinned by a recorded live capture from Dispatcharr 0.28.2 (`backend/tests/fixtures/dispatcharr_dvr_recurring_rules_recorded.json`), including what the dead path actually returned.
- **DBAS restore settings hardening: bounded pagination on the core-settings id map, a missing PATCH-failure test, exception-typed resolver logging, and imprecise dry-run wording (bead enhancedchannelmanager-kahzn, build 0016).** Four small follow-ons from the PR #765/#766 review rounds. (1) `get_core_setting_id_map` now fetches every page of a DRF-paginated response to completion instead of reading only page 1 — a non-null `next` is treated as a more-pages signal (never dereferenced as a URL; the client re-requests the same endpoint with an incrementing page number, per this client's path-only request convention), bounded at 20 pages and raising loudly if exceeded rather than silently resolving a partial map (the live recorded response is a bare unpaginated list today; this guards a future/larger settings list). (2) Added the missing regression pin for a settings-category PATCH that fails *after* successful id resolution: that key alone fails `UPSTREAM_API_ERROR`, other keys in the same blob still apply — the exact original q6xjl failure text had no dedicated test. (3) The resolver's fetch-failure warning in `dbas/importers/settings_agents.py` now includes the exception's TYPE name (never its message text) so an operator can distinguish timeout vs 5xx vs unreachable at 3 AM, without any new key-name/URL exposure. (4) The resolver fetch-failure per-key message no longer overclaims "...could not be applied" when the failure happens during a dry-run preview (nothing was ever attempted to apply at that point) — reworded to "...could not be resolved/applied," the same string used on apply (message parity, single source).
- **DBAS restore dry-run preview now reports WOULD-FAIL for a settings key that doesn't resolve on the destination, instead of silently certifying WOULD-UPDATE (bead enhancedchannelmanager-y6zg6, build 0015).** Follow-on to the q6xjl fix directly below: that fix corrected the *apply* path (resolve each `core_settings`/`comskip` key against the destination's own row id before PATCHing), but the counts-only dry-run branch still never contacted upstream at all — it unconditionally incremented `would_update` for every safe key, which is exactly how the original q6xjl incident's preview certified "Settings 7 WILL UPDATE / 0 FAILED" for an apply that then failed 7/7. The dry-run branch now reuses the SAME run-scoped `CoreSettingIdResolver` the apply path uses (still one `GET /api/core/settings/` for the whole run, shared across both blobs) and resolves each safe key before deciding would-update vs would-fail: a key present on the destination is WOULD-UPDATE; a key absent is WOULD-FAIL with the identical `DEPENDENCY_UNRESOLVED` reason and wording the apply path reports, so the preview and the apply verdict can never again disagree. A resolver fetch failure fails every key `UPSTREAM_API_ERROR` on preview too (fail-closed beats a lying preview). Denylisted (credential/auth/instance-identity) keys are unchanged — still would-skip by name, still never resolved. The fetch stays lazy: a dry-run of a backup with no settings categories selected, or an empty settings blob, still costs zero GETs.
- **DBAS restore failed/conflict counts are now visible in the Task History one-liner and the completion notification, not just the rich report UI (bead enhancedchannelmanager-tyei5, PR #766 code review, build 0015).** The y6zg6 fix above corrects the rich dry-run report itself, but two narrower gaps meant a WOULD-FAIL dry-run still read as an unremarkable success everywhere else. (1) `_summary_message`'s dry-run branch summarized "would create N, update N, skip N across N categories" with no failed/conflict count at all — the apply branch already reported `failed`; the dry-run branch now sums the same per-category `failed` field into an "N conflict(s)" phrase, mirroring `dbas_sync.py`'s existing dry-run summary exactly. (2) The task's `TaskResult` never set `failed_count`/`skipped_count`/real `success_count`/`total_items` — they were hardcoded to the 13-stage progress-bar count, not the actual item counts — so `task_engine.py`'s `elif result.failed_count > 0` ("Completed with Warnings") branch could never fire for a DBAS restore no matter how many categories failed. Both fields (and `success_count`/`total_items`) are now summed from the report's real per-category counts via a `RestoreCounts` helper, the same pattern `dbas_sync.py`'s `SyncCounts` already uses, so the human-readable message and the numeric Task History badges can never disagree.
- **DBAS restore: settings no longer fail 7-for-7 with "Upstream rejected applying setting", and Dispatcharr users no longer fail closed on an unreadable schema (bead enhancedchannelmanager-q6xjl, build 0014).** Found in a fresh-install doc-test run against Dispatcharr 0.28.2: a DBAS backup taken and restored onto the *same* instance previewed cleanly (Settings 7 WILL UPDATE, 0 FAILED) and then failed every one of those seven keys on Apply, rolling the whole restore back. Two independent contract mistakes, both now corrected. (1) ECM was PATCHing `/api/core/settings/<key>/` — a URL that matches no route. Dispatcharr's core-settings viewset is a plain DRF `ModelViewSet` with the default primary-key lookup, so the detail route is `/api/core/settings/<integer id>/`, and every key-string PATCH came back 404 (`[DISPATCHARR] API request failed: PATCH - status: 404`, seven times, then rollback). Row ids are per-instance and are deliberately not carried in a backup artifact, so the restore now resolves them against the *destination* at apply time: one `GET /api/core/settings/` per apply run builds a key→id map that both the `core_settings` and `comskip` blobs share (they live in the same Dispatcharr namespace), and each safe key is PATCHed at its own destination row. The denylist that refuses credential/auth/instance-identity keys is unchanged — a skipped key is not even resolved. A key that genuinely does not exist on the destination now fails **that key alone**, by name ("Setting key 'X' is not present on the destination Dispatcharr"), instead of surfacing as an opaque upstream error. (2) The Dispatcharr **users** category failed closed on every restore with `[DBAS-USERS] Could not read User schema for capability check: Expecting value: line 1 column 1 (char 0)`. ECM reads `/api/schema/` to confirm the destination's user-create schema exposes no pre-hashed-password write field before it will restore users; that endpoint is served by drf-spectacular, whose default renderer is **YAML**, so the JSON parse failed on the first byte and the safety check could never confirm anything. ECM now requests the JSON rendering explicitly. The capability check itself is unchanged and still fails closed — it can now actually run. **This entry does not cover the missing "Apply these changes" confirmation dialog, the DVR-rules fetch failure, or the dry-run preview passing where apply fails**, which were reported in the same run and are tracked separately.
- **Operator user guide: the three documentation defects deferred from build 0011 are closed, plus two more caught in the same pass (bead enhancedchannelmanager-mtkw1, build 0013).** `channels-streams/the-journal.md`'s headline task, "Trace the history of one channel," documented a search that doesn't work for every channel: per-channel Journal rows do exist and are searchable by name, but only for changes made through an MCP agent (Source: AI); a change committed through Edit Mode in the UI (Source: UI) writes only a single Bulk Commit summary row per session, with no per-channel rows underneath it, so a UI-only channel's history can't be traced by name today (DEF-017). The article now carries that distinction explicitly, restores the screenshot that shows it (Source column visible, AI rows individually and the one UI row as a Bulk Commit summary), and points at the open product defect, bead `enhancedchannelmanager-r9py9` (unresolved as of this pass, evidence added to it); the "one Bulk Commit row plus the individual rows" claim is corrected to state which path does which. The same article's "Find the rest of a batch" section told operators to reconstruct a batch by calling `GET /api/journal?batch_id=<id>` directly, which is not an action reachable from the browser. It now states the UI limitation (no batch filter, id is not clickable) without presenting a raw API call as the operator's route, referencing bead `enhancedchannelmanager-d2kdg` (DEF-018). `troubleshooting/read-the-logs.md` hardcoded the maintainer's own container name, `ecm-ecm-1`, in all seven example commands with no instruction to substitute; every example now reads `docker logs <container>`, matching `getting-started/first-run.md`'s existing convention (DEF-020). Found separately during this pass: `backup-restore/backup-overview.md` and `backup-restore/configure-cloud-destinations.md` each carried one instance of the wrong sidebar labels DEF-011 already fixed elsewhere ("Settings → Task Schedules" / "Settings → Task History" in place of **Scheduled Tasks** and its per-task **History** expander), now corrected to match `take-a-backup.md`'s wording; and `notifications/index.md` documented the recipient-dedup toast as always reading "Removed N duplicate recipients," when the live UI shows the singular "Removed 1 duplicate recipient" for exactly one removal, now documented as singular/plural-aware.
- **Copying or reordering a Channel Pipeline rule no longer floods the server with one request per rule, and the copy now lands in the right place without a page refresh (bead enhancedchannelmanager-hns2y, GitHub #755, build 0006).** Field-reported on 0.18.0: copying a rule dropped it at the *bottom* of the list and raised an error toast, and dragging a rule to a new position raised another error toast and looked like it had failed — yet after a refresh both operations turned out to have partly worked. The server log told the story: a successful `POST /rules` followed by an unbroken run of `WARNING: Exceeded concurrency limit.`. The rules list was renumbering priorities by issuing **one `PUT /rules/{id}` per rule, all at once** — so a single copy on a 122-rule instance put 123 writes in flight inside 28ms. That exceeds uvicorn's `--limit-concurrency` (default 100), and uvicorn answers everything past the limit with a 503. Measured on a reproduction instance seeded to that size and driven through a reverse proxy (HTTP/2, which multiplexes rather than capping connections the way a browser does): 124 requests, of which **115 came back 503** and 115 matching `Exceeded concurrency limit` warnings landed in the log. The write amplification scaled with rule count, so raising `ECM_LIMIT_CONCURRENCY` would only have moved the failure to the next operator with a larger rule set; the fan-out now collapses to the single bulk `POST /rules/reorder` endpoint that already existed and was simply not being used — the same operation is **2 requests for a copy and 1 for a reorder**, regardless of how many rules you have. The stranded copy was the visible half of the same bug: the list's in-memory update ran only *after* every write resolved, so one rejection skipped it entirely and left the page showing an order the server did not have until you reloaded. The list is now updated with the same rule the server applies, and if a reorder write does fail it resyncs from the server before surfacing the error — so what you are looking at always matches what was actually saved, with no refresh required.
- **Dispatcharr, Emby, Plex and Jellyfin base URLs on `localhost` / `127.0.0.1` can be saved again — a hardcoded blocklist was overriding your outbound-URL policy setting (GitHub #754, bead enhancedchannelmanager-0yh70, build 0007).** Field-reported on 0.18.0 by a first-run operator whose Dispatcharr runs behind a shared gluetun network, where `http://localhost:9191` is the only address that reaches it: **Test Connection succeeded**, and saving the same URL was refused with `Invalid host — loopback and link-local addresses are not allowed`. Their workaround was to stand the stack up outside gluetun, stop it, hand-edit `settings.json`, and put gluetun back — after which ECM ran perfectly well on that URL, which is the tell: the validator was refusing a configuration the running application is happy with. Cause: the save path ran two validation stages, and the first carried its own loopback/link-local denylist that rejected `localhost` unconditionally, before the canonical stage ever ran. That second stage is mode-aware and reads `ssrf_outbound_mode`, which ships as `lan_friendly` and — per its own documentation and ADR-012 D4 — explicitly allows loopback and RFC1918. So the setting existed, defaulted to permissive, was documented, and could not take effect. Host policy now comes from exactly one place: loopback (`127.0.0.0/8` and `::1`) and RFC1918 follow `ssrf_outbound_mode` (allowed under the `lan_friendly` default, refused under `public_only`), while **link-local `169.254.0.0/16` — including the cloud metadata address `169.254.169.254` — CGNAT, IPv6 ULA, multicast and non-`http(s)` schemes stay refused in both modes, with no setting that re-enables them**. That always-on half is what closed the SSRF + key-exfiltration hole in bead kgz3k and it is unchanged. Two related fixes ship with it. (1) IPv6 loopback `::1` moved from the always-on list to the mode-toggled band: it is the same trust domain as `127.0.0.1`, so denying one while allowing the other prevented nothing — and because Docker's generated `/etc/hosts` maps `localhost` to **both** `::1` and `127.0.0.1`, and one denied address rejects the whole URL, leaving it in the always-on list would have left `http://localhost:<port>` broken for every container that is not `network_mode: host` even after the main fix. (2) **Test Connection and Save now apply the same policy.** The Dispatcharr Test Connection endpoint previously applied no host policy at all — which is why you could prove a connection worked and then be refused permission to store it, and also meant it would issue a credential-bearing request to any host it was given, including `169.254.169.254`, while the Emby/Plex/Jellyfin equivalents were guarded. It now routes through the same chokepoint, so what you can test is exactly what you can save. Unreachable hosts are still saveable and testable — a LAN media server that is merely powered off must not become un-configurable — because the runtime client re-validates before it ever connects.
- **Preflight now fails on an unusable uvicorn launcher instead of leaving it to the exec line, and says so when it discards an interpreter override (bead enhancedchannelmanager-wpzo6, build 0004).** Two follow-ups to the interpreter pin below, both reproduced independently during review. (1) `check_python` probed `"$ECM_PYTHON" --version` and `import fastapi, uvicorn` — the *module* — but never checked that `$ECM_UVICORN` was executable, so a container missing the launcher printed an all-green preflight and then died on `exec gosu appuser "$ECM_UVICORN"` with a bare not-found and zero diagnostics. That is precisely the failure class the pin exists to eliminate ("pinning the checks alone would have moved the same failure one line down"), and `print_python_diagnostics` already computed the answer for it (`NOT EXECUTABLE — the app launch would fail here too`) while being reachable only from branches that could never be this case. Preflight now asserts the launcher and routes the failure through those diagnostics, and prints the launcher path on success so a log paste always names what will be exec'd. (2) The `[ ! -x ]` fallback could not distinguish "the default `/opt/venv` pin is absent, fall back" — a supported configuration — from "the operator set `ECM_PYTHON`/`ECM_UVICORN` explicitly and typo'd it", where falling back silently resolves something else and hands them back the very diagnostics that told them to set the variable. An explicit override that is not executable is now reported by name (`! ECM_PYTHON=/opt/venv/bin/pyhton is not executable — falling back to a PATH lookup`) while an absent default still falls back silently.
- **Container no longer refuses to start with "Required Python packages missing" / `ModuleNotFoundError: No module named 'fastapi'` (bead enhancedchannelmanager-0oi96, build 0004).** Field-reported on 0.18.0: preflight printed a reassuring `✓ Python 3.12.13 found` and then failed on the very next line, with the app never starting. The image was fine — `entrypoint.sh` was locating `python3` and `uvicorn` through `PATH`, and ECM's packages live in the image virtualenv at `/opt/venv`. Any container whose `PATH` does not include `/opt/venv/bin` therefore ran the bare Debian system interpreter, which has none of ECM's dependencies. The real-world trigger is not a hand-edited compose file: an "update/recreate" in Portainer, Unraid, or Watchtower rebuilds the container from the new image while **copying the old container's environment onto it**, so any container created before 0.16.0 (when `/opt/venv` was introduced) inherits a pre-venv `PATH` and breaks on the next update — the reporting operator's container had `ECM_VERSION=0.8.4-0007` baked into a current image. The entrypoint now resolves its interpreter once, pinned to `/opt/venv/bin/python` and `/opt/venv/bin/uvicorn` (both overridable via `ECM_PYTHON` / `ECM_UVICORN`, both falling back to a `PATH` lookup if the virtualenv is absent), and launches uvicorn by that absolute path — `uvicorn` exists *only* in the virtualenv, so pinning the checks alone would have moved the same failure one line down. The Dockerfile healthcheck is pinned the same way. When an interpreter check does fail, preflight now prints what it resolved and why: the interpreter path, its `sys.prefix`, the container's `PATH`, whether `/opt/venv/bin/python` is still present, and what to do about it — enough to diagnose the case from a single log paste. **If you hit this:** recreate the container from your compose file (`docker compose up -d --force-recreate`) rather than using your update tool's in-place update, so it picks up the image's own environment — but read the mount warning below first.
- **Cross-instance sync survives its first contact with a real Dispatcharr-B (bead enhancedchannelmanager-7ipq2.2, build 0002).** The first live two-instance validation (ECM-A + disposable Dispatcharr-B 0.28.2, per the ADR-013 matrix) surfaced five defects the mock tier structurally could not see, all fixed red-first: (1) live-gathered channel rows carried A-side provenance and derived echoes the archive shape never had — `auto_created_by` (an A-local pk) failed EVERY channel create with a 400, and `auto_created=true` would have marked B's replicas for B's own auto-created garbage collection; the channels importer now strips provenance (`auto_created*`, `source_stream`, `override`), derived `effective_*` echoes, and the destination-assigned `uuid`; (2) the custom-stream fallback forwarded A-local `channel_group`/`stream_profile_id` pks (every orphan create 400'd) — it now resolves them through the shared remap and drops what doesn't resolve, and strips live-GET derived echoes (viewer counts, staleness, stats, server hash, `is_custom`); (3) sync-gathered logo records are metadata-only, so uploads fell back to `application/octet-stream`, which Dispatcharr rejects ("Unsupported file type") — the upload content-type is now derived from the already-validated magic bytes; (4) the report claimed "deferred auto-sync applied" even when the sync path's ADR-013 S9 suppressor dropped the settings — the note now counts what the apply fn actually applied — and an apply whose only failures were post-hoc name-conflict details reported `outcome=success` alongside `failed>0`, violating the never-SUCCESS-on-mixed-state invariant (now downgraded consistently); (5) `dbas_sync` is now ONE-SHOT armed: ad-hoc run parameters merged into the singleton task leaked into later runs (observed live as an abort on a stale captured `credential_version`; worst case was a retained `confirm_apply=true` turning an intended dry-run into a source-wins apply) — per-invocation state resets after every run. Also fixed-by-addition: `run_sync` now stamps the persisted per-target status columns (`last_outcome` per realized apply; `last_full_sync_at` only on a FULL success), which existed since migration 0024 but were never written. Validation extras: the reusable live-B recipe (superuser + SECRET_KEY gotcha, teardown) is documented in `tests/dbas-test-env/README.md`; the stalled-drift runbook now records that an unreachable B surfaces as `partial` (per-item fail-soft + rollback), not `failed`; the user guide states the recreate-not-prune replica semantics.
- **Build-advance guard now understands the post-release counter reset (beads enhancedchannelmanager-92fhj, enhancedchannelmanager-w9irb, build 0000).** `scripts/check_version_advances.py` compared BUILD numbers only, so the documented post-release reopen at the next target's `-0000` (shipping.md §Cut Mechanics step 8) was blocked by both the agent-side ship hook and the CI version-consistency job — first hit by the v0.18.0 back-sync PR (`0.18.1-0000` vs baseline `0.17.6-0174`). The comparator is now prefix-aware: a strictly greater `MAJOR.MINOR.PATCH` prefix passes with the counter reset (`advanced_prefix`), and the converse hole is closed — a prefix *regression* now fails outright (`prefix_regression`) even when its build number exceeds the baseline's, where previously it slipped through the build-only compare. Red-first unit coverage for both directions plus the unchanged same-prefix rules.
- **Destructive database migrations that "succeeded" without actually running are now healed automatically on the next start — you may see previously-orphaned tables disappear on first boot after this upgrade (bead enhancedchannelmanager-nywpw, build 0005).** The startup fast path that skips re-running Alembic when the live schema already matches the current model only checked that every model-declared table and column *existed* — it never checked for tables the model had stopped declaring, so a drop-only migration was invisible to it by construction and got silently stamped over ("Running stamp_revision" in the log) instead of actually executed. This instance hit it twice: migration 0041 (drop `lookup_tables`, see Removed below) logged as stamped rather than run, and revision 0029 had been stamped past months earlier, leaving `ffmpeg_saved_configs`, `playlist_profiles`, `publish_configurations` and `publish_history` behind with nothing to indicate it. Detection is now AST-based — every revision module is parsed for `drop_table` / `drop_column` / `drop_index` / `drop_constraint` calls and DROP / DELETE FROM / TRUNCATE SQL literals, so a drop-only migration is caught structurally rather than relying on its author to remember a marker. On upgrade, each destructive pending revision now replays individually against its real `upgrade()` (with its own row dump where one exists) while non-destructive revisions still stamp over as before. **Self-heal:** an instance already stamped past a destructive revision is detected at boot — the version table claims the revision applied but the table it should have dropped is still physically present — and is healed automatically, with rows already deleted written out first wherever the migration provides a dump path. No operator action is required; this only removes tables the application no longer uses.
- **"Reset Stuck Probe" is no longer hidden by the exact condition it exists to recover from (bead enhancedchannelmanager-xg9gp, build 0005).** Settings gated the entire Reset Stuck Probe section on the probe *not* being in progress — so once a probe actually got stuck (reporting "in progress" forever), the one control built to clear that state disappeared along with it, leaving no in-UI recovery at all short of starting an unrelated probe from a different browser tab. The section now always renders: Reset stays enabled while a probe is running, and Clear All Probe Stats is disabled with a visible on-screen reason instead. Reset has no confirmation dialog — against a healthy probe its worst-case effect is identical to the already-unconfirmed Cancel button one card above it, and it destroys nothing: results already written stay written and the run is re-runnable.
- **Long Stats and Settings pages no longer yank you down the page seconds after you land on a deep link, and Scheduled Tasks links no longer silently break when a task is renamed (beads enhancedchannelmanager-mch8j, enhancedchannelmanager-b32co, enhancedchannelmanager-de6u1, enhancedchannelmanager-ue130, build 0005).** The section-navigation rail used for in-page jump links and `?section=` deep links on Stats and Settings only discovers a section once it renders a heading; several panels rendered a heading-less loading state until their own data fetch settled, so both the rail and any pending deep-link scroll were incomplete for as long as that fetch took — minutes, on a large library mid-probe. Worse, the deep-link scroll re-fired on every rail change instead of firing once, so a reader who had already started reading the top of the page could get yanked as far as 1993px down mid-read, once the last panel's data finally arrived. Panels now expose their section id and label from first paint, and the deep-link scroll now fires exactly once per requested `?section=` value; an unresolvable `?section=` is dropped from the URL and announced via an on-screen notice instead of silently pointing at nothing. Separately, Scheduled Tasks' rail anchors are now pinned to each task's stable internal id instead of a slug of its display name, so renaming a task — or a cross-instance sync target, whose task name embeds the target's own name — no longer silently moves a previously-shared link. Five of seventeen existing task anchors change shape as a result (cleanup, m3u_digest, auto_creation, failed_stream_reprobe, stats_v2_rollup); old links to those five now surface a clear notice instead of landing nowhere without explanation.
- **Accessibility: every checkbox and radio now meets its WCAG 2.5.8 target size or has an accessible name, and the last known contrast failures are cleared (beads enhancedchannelmanager-m26f8, enhancedchannelmanager-7lwe0, enhancedchannelmanager-dlavh, enhancedchannelmanager-0zq1p, enhancedchannelmanager-4pzvg, enhancedchannelmanager-zncyv, build 0005).** A full census of checkboxes and radios across the ten primary routes, seventeen Settings sub-routes and 81 of 82 dialogs found 211 controls rendering at five different sizes (13-18px), none reaching the 24x24 WCAG target; 44 of them had no `<label>` at all, so their bare 16px box was the entire click/tap target. All 44 now sit inside a real target across their five source locations (column-select checkboxes, sort-priority toggles, print-guide group rows, task-schedule toggles, the pending-merges selector), and checkbox/radio boxes are standardized on one 16px size app-wide instead of five. The same census caught something worse than target size: 6 of those 44 controls had no accessible name at all (announced to a screen reader as a bare "checkbox") and 8 more were named only via a `title` attribute, which is invisible to touch and keyboard users — both groups now have real labels or `aria-label`s. Separately, the remaining WCAG contrast failures — the primary navigation's selected-row highlight, the probe-group icon button, and error text in the stream preview dialog — are corrected, each measured on the live rendered page across both supported viewports and all three themes; a bug in the contrast-measuring guard itself (it was compounding layered opacity in the wrong direction, making some passing combinations read as failing) is also fixed. Also fixed: two screen-reader-only status regions — one on the Dashboard, one in Settings — were rendering as ordinary visible text instead of being hidden, because the CSS that hides them only shipped inside the Channel Pipeline page's bundle and hadn't loaded until you'd visited that page at least once in your session.
- **All 211 checkbox and radio controls now clear the WCAG 2.5.8 24x24 target — the boxes themselves are unchanged size, only short rows grew (bead enhancedchannelmanager-3h2u1, build 0006).** Follow-on to the census above: of the 211 labelled checkbox/radio controls, 91 were still failing 2.5.8, every one of them on height only (18-22px tall; none was too narrow). All 211 now clear the floor — 72 of the measurement groups grew to exactly 24px, and the other 84, already at or above it, didn't move at all. **The checkboxes and radio buttons did not get bigger** — they're still their existing 16px box — the fix gives each short row a minimum height instead, so nothing that was already sized correctly changed. Nine Settings sub-pages grew a little taller as a result (up to about 29px) and nine dialogs grew a little too (up to about 12px); nothing that fit on screen before now scrolls.
- **Flaky test defused: the EPG large-file event-loop check no longer fails on a loaded CI runner (bead enhancedchannelmanager-ssvvl, build 0006).** `test_large_xml_index_parse_does_not_block_event_loop` was a wall-clock-budget assertion — the same shape that had already failed CI twice and been fixed elsewhere — so it was only a matter of time before it failed here too on a busy runner. Replaced with a signal-based check that has no clock in its assertion. No production code or user-facing behavior changed; test-only.
- **Operator user guide: 17 documented steps corrected against a live-instance test pass, including one BLOCKER (bead enhancedchannelmanager-i78vd, build 0011).** A documentation test run against a fresh install (`~/ecm/doc-test-runs/2026-08-02/DEFECTS.md`) walked the guide end-to-end and found places where the documented UI did not match the running app. Highlights: `take-a-backup.md` sent operators to a "Backup" card and "Back Up Now" button that do not exist (DEF-010, BLOCKER); it now documents the working path (Scheduled Tasks → DBAS Backup → Run Now) and flags that a one-click control is tracked separately (bead `pui76`). The same article's "Saved Backups" section claimed to list all local backups when it is scoped to YAML exports only and never shows DBAS artifacts (DEF-012), and used the wrong sidebar labels "Task Schedules"/"Task History" for "Scheduled Tasks" and its per-task History expander (DEF-011). `connect-dispatcharr.md` sent operators to a nonexistent "Account → API Keys" path in Dispatcharr 0.28.2; it now documents the real path (User dialog → API & XC → Generate API Key) (DEF-003). `your-first-channels.md` claimed group selection "immediately re-pulls" streams when convergence can take on the order of tens of minutes with no progress indicator (DEF-007), and separately named the wrong fields for XtreamCodes accounts, omitted that gzipped XMLTV feeds work, undersold initial-pull timing, didn't warn that the Channels group filter resets to "No groups selected" on reload, and didn't mention the numbering dialog that opens when dragging a channel onto a group header (DEF-004, DEF-005, DEF-006, DEF-008, DEF-009). `channel-to-epg-matching.md` and `guide/index.md` didn't say that a fresh channel-to-EPG match requires an EPG Refresh task run before it shows in Guide (confirmed as expected product behavior, not a bug), and `guide/index.md` wrongly claimed its own grid Refresh button would surface it (DEF-014); the same matching article also pointed operators at an MCP-tool/API-only audit with no UI path and now says so plainly (DEF-015). `installation.md`'s two ordered lists rendered broken on the published site (a later list item silently glued onto the previous item's trailing paragraph as literal text). Reproduced against the site's actual `mkdocs-material` + `pymdownx.superfences` toolchain, the fix indents continuation content to 4 spaces and blank-lines between items (DEF-001); its companion `getting-started/index.md` promised prerequisites content that `installation.md` didn't have and listed shipped articles as "planned" (DEF-002), so a Prerequisites section was added and the index brought current. `notifications/index.md` didn't document the Alert Methods list at all, including that it does not live-update after saving a channel's settings (DEF-016). `rules-overview.md` and `test-a-rule.md` both described a "⋮ (more actions) menu" on the Channel Pipeline page that does not exist: Dry Run, Import, Export, and Pipeline Debug Bundle are always-visible toolbar buttons instead (DEF-019). `schedules-direct.md` didn't say what to do when an SD account is already at its lineup cap, or that removing a lineup consumes the same daily change quota as adding one (DEF-013). Three findings from the same run remain out of scope here because their target articles (`the-journal.md`, `read-the-logs.md`) are not yet on `dev` (DEF-017, DEF-018, DEF-020); two more are tracked as product defects, not documentation (beads `3p7jg`, `r9py9`).
- **A restore that leaves a channel with no playable stream now reports "completed with failures" instead of success, and raises a warning (bead enhancedchannelmanager-daziw, build 0027).** A DBAS restore could finish reporting `outcome: success, failed: 0` for an instance holding a channel that would not play at all — the round-trip drill measured exactly that, with the channel returning an HTTP 500 and zero bytes while the restore called itself a success. From this build, a restore (or a cross-instance sync, which runs on the same engine) that applies everything and still leaves a channel with **not one real, URL-bearing stream** finishes as `completed_with_failures`, names those channels in the report, and sends a **warning** alert titled "Task Completed with Warnings" rather than a green success. **Nothing is rolled back and nothing is undone** — the restored state is real, kept, and one attached stream away from working; only the label and the alert change. **A channel that still plays is NOT counted.** If a channel kept its real streams and is merely holding one leftover placeholder stream in a slot — the normal, designed outcome when two archived stream names collide, shipped in build 0026 — that channel plays, it is not counted as unplayable, and it does not stop the restore reporting success. Two numbers now appear in the report and in the one-line task summary, and they mean different things: `channels_with_no_playable_stream` (these are dead; this is what downgrades the outcome) and `channels_needing_stream_reattach` (these hold a placeholder slot; most of them play). The old summary wording called the second of those "channel(s) have NO playable stream", a claim it could not support — that phrase now belongs to the first number only. What to expect operationally: if you have alerts configured, a restore or sync that used to look clean while leaving a dead channel will now alert you as a warning, once, naming the channels to fix; if your restores leave every channel playing, nothing changes for you.

- **Discarding or applying staged work on the way out no longer leaves it waiting for you at the next sign-in (bead `enhancedchannelmanager-r93hq`, build 0111).** Staged Edit Mode work is written to the browser tab's session storage as you stage it, which is what lets a session that dies under you offer the work back afterwards. Answering the **Sign Out** prompt with **Discard** or **Apply All** cleared that copy a moment too late: signing out replaces the entire app with the login page, so the cleanup never ran. Signing back in as the same operator in that tab then offered the work again. After **Discard** that contradicts the decision outright, and the guide's promise that a discard leaves things "as if it never happened". After **Apply All** it is worse: restoring and applying a second time repeats the creates, so a channel you made once could be made twice. Both answers now destroy the saved copy at the moment you give them, before anything can be torn down. **A session that ends without an answer is untouched, deliberately**: an expired token, a forced sign-out, a closed tab all still leave the work exactly where it was, because recovering it is the entire point.

- **A duplicate-number approval that survives a dead session is now judged by the same rule Apply uses (bead `enhancedchannelmanager-vdxbx`, build 0111).** Approving a duplicate channel number records which channels the dialog named, so approval of one collision cannot be spent on a different one. Apply accepts a collision that got *smaller*: agreeing to share a number with two channels covers sharing it with one of them, because you were shown the worse case. The restore path demanded an exact match instead, so if a channel moved off that number while you were away, the restore dialog announced your approval had been withdrawn and would be checked again, and the check then passed without asking anything. You were re-interrogated about a situation that had strictly improved. Restore now applies the same subset rule as both preflights and the documented API contract. A channel you were never shown joining that number still withdraws the approval, which is the case the record exists for.

- **The two Edit Mode dialogs added this release are now real dialogs to a screen reader, and the numbering-conflict one is measured by the modal inventory (beads `enhancedchannelmanager-r93hq`, `enhancedchannelmanager-ic884.4`, build 0111).** The **Channel Numbers Changed While You Were Editing** dialog carried no dialog role, no modal state and no accessible name, so assistive technology announced it as ordinary page content and the repository's modal inventory could not see it at all. Both it and the staged-work restore offer now trap Tab, take focus on open, and are covered every CI run by the same accessibility and typography gates as every other dialog. Focus deliberately lands on the harmless button in each: **Keep Editing** rather than the disabled **Apply With These Choices**, and **Restore** rather than **Discard**. Their running text also sits on the application's type scale now, instead of rendering three sizes larger than the same sentence anywhere else.

## [0.18.0] — 2026-07-26

### Security
- **Release-branch stabilization: cleared the main-target security gates for the v0.18.0 cut (release PR #748).** The release-cut PR's merge-ref scans — which dev-targeting PRs don't run at main's thresholds — surfaced four accumulated items, all fixed on `release/v0.18.0`: (1) `backend/task_engine.py`'s `_param_keys()` sanitizer had a raw-passthrough fallback for non-dict parameters, keeping the `py/clear-text-logging-sensitive-data` taint path alive (CodeQL alerts #1770/#1771, HIGH, open on `dev` since 2026-07-08) — the fallback now returns a `<non-dict:type>` placeholder and a regression test pins that non-dict parameters never reach a log line raw; (2) an unanchored `/example\.com/` regex in `ActionEditor.test.tsx` (CodeQL alert #1769, `js/regex/missing-regexp-anchor`, HIGH) replaced with an exact placeholder-literal match; (3) backend dependency bumps for advisories published since the 0.17.5 cut: `click` 8.3.2→8.3.3 (PYSEC-2026-2132) and `pyasn1` 0.6.3→0.6.4 (PYSEC-2026-3455/3456/3457); (4) frontend transitive bumps via non-`--force` `npm audit fix`: `brace-expansion`→5.0.8 (GHSA-3jxr-9vmj-r5cp, GHSA-mh99-v99m-4gvg), `postcss`→8.5.23 (GHSA-r28c-9q8g-f849), `undici`→7.29.0 (7 advisories) — `npm audit --audit-level=high` now reports zero.

### Changed
- **Docs: BaseException-containment known limitation, and a stale user-guide IA tree fixed (bead enhancedchannelmanager-17v07, build 0172).** `docs/auth_middleware.md` now documents that `BaseExceptionContainmentMiddleware` (registered first/innermost, `backend/main.py:205`) cannot cover the bodies of outer `@app.middleware("http")` functions — including `auth_middleware` itself (`backend/main.py:525-547`) — since each outer middleware body runs outside the guard's task boundary; this is a known, accepted structural ceiling of Starlette's `BaseHTTPMiddleware` model (closing it is a middleware-stack/order redesign — removing the task-boundary layers and placing containment so it actually wraps the affected bodies, then revalidating handler containment), no field occurrence has been observed as of 2026-07-26, and the doc states the exact `docker logs` signature to look for if one ever fires (the atexit `[EXIT-DIAG]` line still logs; what's absent is the containment middleware's own CRITICAL traceback above it). A pointer comment was added at the `auth_middleware` definition (comment only, no behavior change). Separately, `docs/user_guide/README.md`'s "Information architecture" tree was missing six sections added by the gsnw0 per-tab-tutorial epic (`settings/`, `guide/`, `m3u-manager/`, `logo-manager/`, `journal/`, `m3u-changes/`); the tree now lists them, marked **Planned** to match `docs/user_guide/index.md`'s "By tab" table.
- **Channels pane selection is now keyboard-operable (bead enhancedchannelmanager-s8xpd, build 0166).** Follow-on from bead zwhw4's StreamsPane review, which flagged the same bare-span selection flaw in ChannelsPane: the per-channel row selector and the group header's select-all were clickable `<span>`s with no native control semantics — not focusable, no `aria-checked`/`aria-pressed`, no keyboard handler, gating every selection-dependent action (the floating SelectionActionBar, bulk ops, Move-to-group) behind a mouse. Both are now real `<button>`s: the row selector carries `role="checkbox"` + `aria-checked` (mirroring StreamsPane's per-stream selector), and the group header's select-all also carries `role="checkbox"` + a tri-state `aria-checked` (`true`/`false`/`"mixed"`) rather than the boolean `aria-pressed` the first pass shipped, which announced "none selected" and "some selected" identically even though the indeterminate glyph already distinguished them. The tri-state fix was applied to **both** panes' group-header select-all (ChannelsPane's `group-checkbox` and StreamsPane's `group-selection-checkbox`), keeping the two panes' equivalent interactions semantically consistent. A round-2 review also found the select-all button nested inside each pane's group-header `role="button"` div — a focusable-inside-focusable conflict; in both panes the expand/collapse toggle is now its own sibling `<button>` (native Enter/Space handling) instead of the div itself carrying `role="button"`/`tabIndex`, which also let the old bd-6n14l keydown-target-check guard be removed as structurally unnecessary. Both glyphs stay `aria-hidden`, and a `:focus-visible` accent outline was added to each new/changed button.
- **Streams pane: right-click context menu replaced by an accessible "Create in…" menu (bead enhancedchannelmanager-zwhw4, build 0161).** The hand-rolled right-click menu on stream rows and group headers ("Create channel(s) in group / in new group") is gone — it was mouse-only, invisible to screen readers, and unreachable by keyboard. Its capabilities now live in the selection strip at the top of the Streams pane: with streams selected in Edit Mode, a new "Create in…" button opens a keyboard-navigable chooser listing the enabled channel groups (with type-to-filter, matching the Channel Manager selection bar's Move-to-group chooser) plus a pinned "Create in new group…" action. The single-stream duplicate-channel check (dedup prompt) is preserved on the choose-a-group path. Right-clicking a stream row or group header now shows the browser's native context menu, matching the Channels pane.

### Added
- **WebDAV cloud backup targets are now enabled end-to-end (bead enhancedchannelmanager-0i2vt.8, build 0170).** The WebDAV adapter was already built, SSRF-hardened, and registered in `SUPPORTED_PROVIDERS`, but the cloud-target CRUD layer's `provider_type` validation blocked it — creating a WebDAV target was impossible from the UI or API. `webdav` is now accepted across create/update/test requests, and the Cloud Target editor offers it with its `base_url`/`username`/`password` fields. Self-signed endpoints are supported through a validated top-level `insecure` flag (persisted per target, surfaced as an advanced checkbox with an explicit warning): TLS policy derives from that flag ONLY — `insecure` inside the credentials dict is reserved and rejected at the API boundary — so connection tests use exactly the policy scheduled uploads use, and every verification skip writes an audit row on test paths just as on uploads. OneDrive and Dropbox appear greyed out and labeled "not yet supported" in the create dropdown (deferred providers — PO decision 2026-07-25); existing rows of those types still render and edit. Covered by an end-to-end integration test: API create with real credential encryption → saved-target test-connection through the real adapter and SSRF chokepoint → real-adapter streamed upload.
- **Cloud-backup encryption key integrity is probed at every container start (bead enhancedchannelmanager-m40pn, build 0170).** File mode/ownership violations on the Fernet key now surface as an unmissable ERROR log line at startup instead of at the first scheduled backup. Posture is log-loudly-but-boot: the probe never blocks startup. At use time, a decrypt failure reports the credentials as unavailable and the credential-consuming backup operation aborts before any outbound use.
- **Restore logo-miss details now list the affected channels (bead enhancedchannelmanager-cm9bi, build 0170).** Each logo miss in the restore report carries the channels whose archived `logo_id` referenced it — destination Dispatcharr channel id (resolved through the channel remap; channels import before logos) plus the operator-facing name. To make this work on genuine backups, the artifact builder now joins each archived logo file to its source Dispatcharr logo record (by URL basename) and preserves the stable source logo id through `binary/metadata.json`, where the artifact decoder re-attaches it to each logo record — previously real artifacts lost that id and the affected-channel list was always empty. The banner's aggregate copy is logo-oriented ("1 logo is missing…" / "N logos are missing…" — `logo_misses` counts missed logos, one shared logo affecting several channels is one miss) with the per-channel impact in nested rows carrying Dispatcharr links; Dispatcharr's SPA has no per-channel route (channel editing is a modal on `/channels`), so the links open the channels list page. `role=alert` and the colorblind-safe treatment are unchanged, and dry-runs never emit provisional ids.
- **DBAS backup artifacts now carry user agents, DVR rules, core settings, and comskip config — completing round-trip coverage of all 12 categories in the v0.18 scope; plugins remain excluded per ADR-012 D10 (bead enhancedchannelmanager-lc6zu, build 0170).** The restore importers for these categories were fully wired (PR #742) but the artifact builder never produced their sections, so restoring them silently no-opped against a real backup. The builder now emits all four (Dispatcharr has no separate comskip endpoint: comskip keys live in the core-settings namespace and are split out by prefix, matching how the importer applies them per-key), and the artifact decoder maps them into the restore plan. Core-setting values whose key names look credential-bearing (the same conservative denylist the restore importer skips by name) are redacted in the artifact — always, even on credential-carrying migration exports, since the importer would never apply them. The restore summary now labels the Settings category's apply counts.
- **Builtin Quality Tags seed now includes modern UHD labels: `2160P`, `3840P`, `4320P`, `8K` (bead enhancedchannelmanager-lecyo, build 0168).** Follow-up from bead 0tryb: the builtin Quality Tags group predates modern UHD naming, so names like "DE: RTL 3840P" kept their resolution suffix through normalization on fresh installs. The builtin tag sync runs at every startup and tops up missing tags into existing installations, so existing installs gain the new labels on their next restart after upgrading; tags a user already added by hand with the same value are skipped untouched (exact-value dedupe -- no duplicates, no flag changes).
- **Journal Noise Purge covers two more automated-noise buckets (bead enhancedchannelmanager-gjb01, build 0160).** The daily Journal Noise Purge task now also purges run-on-refresh suppression notices (the "auto-creation after M3U refresh skipped" entries written on every refresh while the circuit breaker is tripped) and routine start/complete lifecycle rows from scheduled task runs, both older than the shared retention window (default 3 days). Each bucket has its own toggle in the task's settings (Settings → Scheduled Tasks → Journal Noise Purge), enabled by default. Kept regardless of age: manually-triggered task runs, task cancel/fail/error entries, and every other journal category — and full task execution history remains available in Task History under its own retention. Duplicating a Channel Pipeline rule now also writes a journal create entry (previously a duplicated-then-deleted rule left a delete-only pair in the audit trail).
- **Catch-up (timeshift) badge on channel and stream rows (bead enhancedchannelmanager-sy1sz, build 0155).** Channels and streams that Dispatcharr flags as catch-up-capable now show a compact `replay` badge in the Channels pane and stream lists, with a tooltip stating the archive depth ("Catch-up: 7 days", or "Catch-up available" when the day count is unknown). The badge is driven solely by Dispatcharr's authoritative `is_catchup` flag — a stream reporting archive days without the flag set shows no badge.
- **M3U Auto-Sync groups now honor their channel-profile selection (bead enhancedchannelmanager-y3m6o.3, GH #720 Part B, build 0153).** The Auto-Sync settings modal already let an operator pick which channel profiles an auto-synced group's channels should belong to, but nothing applied that choice — Dispatcharr's default of auto-joining every new channel to every profile stood, so the selection was silently ignored. ECM now reconciles each group's stored selection SUBTRACTIVELY: for every profile in the universe the group's channels are enabled if the profile is selected and disabled otherwise, at a cost of one bulk update per profile regardless of channel count. The **converging backbone** — the M3U change monitor's every-pass sweep plus the post-refresh completion poll — is the ongoing convergence mechanism; the modal save applies it immediately on a best-effort basis and surfaces a warning toast if that apply is incomplete. Selection is **enforced globally per channel-group**: on save it is propagated to every M3U account row carrying that group so it takes effect regardless of which account it was made on (a defensive collapse still resolves legacy rows and flags any residual conflict). Concurrent reconciles are serialized per effective group **within the process** (single-operator assumption; when ECM runs a TLS request subprocess, that subprocess forwards exactly the lock-participating paths — the group-settings save cascade, the single-account M3U refresh, the Channel Pipeline / Auto-Create run endpoints and their `/api/auto-creation` alias, and every background task run AND cancel — to the main process so main stays the sole writer of those paths and the scheduler/lock stay authoritative there; the forward imposes no timeout ceiling of its own so a legitimately long task run is not cut to a false error, and main's request-timeout budget remains authoritative. Direct channel-profile membership endpoints are a separate pre-existing concern, not part of this forwarding — bead nq3ed). Two failure classes are kept distinct: a reconcile that fails AFTER the selection was safely written returns HTTP 200 with a warning in the response body (the save stuck; convergence retries on the next sweep), whereas a **pre-write safety failure** — the lock key is unavailable, or the account list needed to clear a selection cannot be read/enumerated safely — is NOT written and returns HTTP 503 (retry) rather than a false success. A CLEAR that partially applied before a sibling write failed reports the TRUTHFUL partial: a 503 that names which accounts were cleared and which failed, leaves the authoritative account's selection intact (no resurrection), and journals the accounts that actually changed so the operator can re-save to complete. A coalesced periodic sweep (another sweep already in progress) is reported as DEFERRED — a task warning, not a green success — and converges on the next pass. NOTE: reverting ECM to a build before 0.17.6-0153 does not restore prior channel-profile memberships — the membership changes live in Dispatcharr, not an ECM snapshot; re-select in the Auto-Sync modal to recover a state. **Pipeline ownership with handoff:** channels whose profile membership was set by a Channel Pipeline `assign_channel_profile` rule (standard or Event Sync) are excluded — a pipeline choice outranks a group selection — but ownership is automatically handed back to Auto-Sync once that rule is disabled, deleted, or stops assigning profiles (a rule CONDITION change that merely stops matching a channel does not release it — out of scope). Recovery guardrails: an absent or empty selection is a no-op that leaves existing memberships unchanged (never "disable everywhere"); if the entire stored selection references profiles that have since been deleted, the group is left untouched rather than stranded in zero profiles; if a selected-profile write transiently fails, the reconcile aborts before any destructive disable and retries on the next pass; and if the profile universe cannot be fetched the reconcile reports `degraded` (enables applied, exclusivity not enforced) rather than a false success. The modal shows status-specific recovery guidance on an incomplete apply.
- **Recovery note (channel-profile reconcile, GH #720 Part B):** this feature changes Dispatcharr channel-profile MEMBERSHIP as a side effect of M3U Auto-Sync saves and the periodic sweep. Reverting ECM to a build before 0.17.6-0153 does NOT restore prior memberships — the membership changes live in Dispatcharr, not in an ECM snapshot, and ECM's own rollback/undo never claims to reverse profile membership. To recover a specific membership state, re-select the desired profiles in the Auto-Sync settings modal (or adjust the owning Channel Pipeline rule) and let the reconcile re-apply it.
- Public user-guide documentation site: MkDocs + Material scaffold that publishes `docs/user_guide/` to GitHub Pages (developer/internal docs excluded). (enhancedchannelmanager-o8ezw, GH #381)
- **Date-windowed Channel Pipeline rules (bead enhancedchannelmanager-6lhmb, GH #646, build 0147).** Standard and Event Sync rules can now define optional inclusive UTC `active_from` and `active_until` calendar dates. Rules outside their window are excluded consistently from manual, selected, run-on-refresh, and Event Sync auto-run execution without undoing changes made by earlier runs. Both editors expose the window, CRUD/bulk/duplicate operations preserve or clear it explicitly, and YAML export/import round-trips quoted, unquoted, open, and null bounds with atomic validation.
- **Preview-first IPTV ↔ Gracenote guide migration (bead enhancedchannelmanager-96tnl, GH #592, build 0146).** EPG Manager now lets an administrator choose an XMLTV or Schedules Direct target and preview every channel guide assignment before changing anything. ECM maps the current guide through the XMLTV `<gnid>`/`<lcn>` station identifier (Schedules Direct exposes the same identifier as `EPGData.tvg_id`), then classifies each channel as ready, already on target, unassigned, missing LCN, missing target, ambiguous, or unsupported origin. Only explicitly confirmed, unambiguous XMLTV/Schedules Direct rows are changed; unresolved, dummy, and unknown-source rows are never overwritten. Five-minute previews use a domain-separated key derived from the instance's rotatable signing secret and bind the actor, instance/audience, ordered assignments, LCN, and exact current/target EPG identities. Apply returns `202`, runs outside the request timeout, and exposes live counters and per-row outcomes through an actor-bound polling endpoint; the dialog polls until terminal while open with no arbitrary wall-clock cutoff, retains the batch ID and partial results across transient failures, aborts polling on close, and provides reconciliation guidance after a restart. Concurrent applies fail fast with `409`, running jobs are never pruned, and terminal jobs expire 30 minutes after completion. Inside the serialized job ECM rebuilds one bounded XML/source snapshot, re-proves every target is still the exact sole candidate, and refetches each current/target EPG row and channel immediately before PATCH. XML/JSON parsing and large index construction run off the event loop. XMLTV reads use ECM's pinned outbound SSRF transport with per-hop redirect validation, bounded gzip expansion, and DTD/entity rejection. Channel/EPG enumeration stops at cap+1 (1,000/50,000). Audit attempts share a 128-bit Journal batch ID; sessions rollback and close on failure, which is reported as `updated_audit_failed`. Dispatcharr PATCH, HTTP acknowledgement, and Journal commit are not atomic, so interrupted jobs require a fresh preview plus direct Dispatcharr reconciliation.
- **Pending Merges bulk actions (bead enhancedchannelmanager-ixcf1, GH #642, ADR-008 §D10, build 0145).** The Pending Merges queue now provides accessible row checkboxes plus Select all, Deselect all, Merge selected, Clear selected, Merge all, and Clear all controls. Select all spans the complete paginated queue. Merge/clear operations use an accessible focus-trapped confirmation dialog with the exact target count and consequence, then reuse the existing per-row accept/dismiss endpoints sequentially to avoid concurrent Dispatcharr mutations. A persistent accessible progress panel reports completion and failures; Stop cooperatively finishes the request already in flight, skips the remainder, and leaves those records selected for retry. Successful rows disappear as they resolve; a partial failure does not stop later rows, and failed rows remain selected and rendered with their backend error and retry controls. An admin-gated, read-only snapshot endpoint returns the complete pending queue from one ordered database query with a 20,000-row fail-closed cap; Select all, queue-wide actions, and Refresh with an active selection consume that coherent snapshot. Canceling confirmation restores the prior paginated view. Merge all is irreversible within ECM and its exact-count confirmation is the safety boundary.
- **Smart Sort can prefer catch-up-enabled streams (bead enhancedchannelmanager-jnbka, GH #652, build 0144).** Settings → Channel Defaults → Smart Sort Priority now includes an eighth, default-disabled **Catch-up** criterion with the standard history/clock icon and the subheading “Catch-up enabled.” When enabled, streams whose Dispatcharr `is_catchup` flag is true sort ahead of streams without catch-up; ties continue through the remaining enabled criteria. Existing saved priority lists automatically gain the new criterion at the end without changing current behavior. The criterion is also available in channel, group, pane, and selection-bar sort menus; direct **By Catch-up** sorting treats catch-up capability as probe-independent metadata, so an unprobed catch-up stream still ranks ahead of a successfully probed non-catch-up stream.
- **Channel Manager: type-to-filter for the selection bar's Move-to-group submenu (bead hzzcv, build 0143).** The Move-to-group submenu shipped in build 0141 lists every channel group, sorted and scrollable — workable at dozens of groups, painful at the live instance's ~2,940. A filter input now sits at the top of the submenu and focuses automatically the moment it opens (by click or by ArrowRight), narrowing the list as you type. Filtering matches the Channels pane's existing searchable group filter (bead mn8): case-insensitive substring match on the raw, untrimmed query. "Uncategorized" and "New group…" are pinned outside the filter — fixed actions, not filterable named groups — so both stay reachable at any filter state, including a zero-match filter. A filtered-to-nothing state shows `No groups match "<query>"` instead of a blank panel, with a one-click clear button. The existing keyboard contract is preserved end to end: arrows traverse the filtered results, Enter activates the focused group, and Escape closes the submenu only (returning focus to its trigger) without closing the whole menu or clearing the selection — matching-input keys (Left/Right/Home/End) stop propagating to the menu's roving-focus handlers so normal text-cursor editing inside the filter still works. The filter resets each time the submenu closes. (bead enhancedchannelmanager-hzzcv)
- **Channel Manager: floating selection action bar over both panes (bead 09x38.17, build 0141).** Selecting channels in Edit Mode now surfaces a Gmail/Drive-style action bar fixed across the bottom of the viewport — full-width so pane widths are untouched at every breakpoint. Top tier: Delete, Probe, Find Duplicates, Renumber, Assign EPG, Merge (2+ selected only), a '⋮ More' overflow that opens upward, and Clear. The More menu carries three sections: Move ("Move to group" submenu listing every group plus "New group…"), Selection (Normalize Names, Set Logo from M3U/EPG, Sort Streams, Fetch Gracenote IDs), and Profiles (a single "Profile visibility" flyout replacing the duplicate Enable-in-Profile/Disable-in-Profile lists). The hand-rolled right-click context menu on channel rows and group headers is removed with full capability parity (Move-to-group, New-group-and-move, profile toggles, and Merge all live in the bar), the header 'More actions' kebab is reduced to pane-level items only (Channel Profiles, Hidden Groups, Sort All Streams, Renumber All Groups, CSV Template/Export/Import), and the mini "N selected" header strip is absorbed by the bar. Accessibility: all bar buttons are real buttons, the menu is fully keyboard-navigable (arrows/Home/End/Enter, ArrowRight/ArrowLeft for submenus, Escape closes and restores focus), and Escape with no menu open clears the selection. (bead enhancedchannelmanager-09x38.17)
- **Scheduled Tasks: "Enabled, won't run" visibility for tasks whose schedule cannot fire (bead vkktd.4, build 0140).** Task cards now surface the backend's `effective_enabled` state: a task that is enabled but has no enabled schedule that will actually fire shows an amber "Enabled, won't run" status pill (with a one-click "Fix it" action) instead of a green check. The task editor shows an inline warning for the same condition plus a reconcile toast when saving auto-resolves it, and the Channel Pipeline tab gains a dismissible gate banner when the Auto-Create Channels task is disabled or won't run.
- **Journal: automated-noise auto-purge (bead uliyr, build 0140).** New daily scheduled task "Journal Noise Purge" (03:45 UTC default) deletes journal entries older than a settings-visible retention window (default 3 days, PO-decided) for exactly two automated-noise categories: watch start/stop telemetry from the bandwidth tracker, and Channel Pipeline rule create/delete churn from automated test clients. Operator-initiated rule create/delete entries are kept via a new write-time provenance marker (`journal_entries.automated_client`, migration 0037): clients self-declare automation with the `X-ECM-Automated-Client` header (the E2E harness now sends it), requests without the header are operator-classed and never purged, and unmarked pre-marker legacy rows age out until that set reaches zero. All other journal categories are untouched — the manual Purge control remains the tool for everything else. Retention days and both category toggles are editable in Settings → Scheduled Tasks.
- **Event Sync: opt-in promotion of unmatched secondary-only events to ECM-managed channels (bead ti939.4.1, build 0139, GH epic ti939).** The ONE sanctioned exception to Event Sync's "ECM never creates channels" principle, strictly opt-in per rule via `promote_unmatched: true` + a required dedicated `promote_target_group_id` (the master group and secondary groups are refused — ownership rails; absent keys leave the feature completely invisible, byte-identical behavior everywhere). With it on, each unmatched secondary-only event with a complete parsed identity gets its own ECM-managed channel in the target group, with every provider's stream for that event attached (journaled under category `event_sync` with `kind="event_sync_promote"` content-fingerprint provenance — provider id + normalized-name hash + event key; IDs display-only). **Lifecycle is reconciliation-driven (PO decision):** a promoted channel is kept only while its justifying stream is observed in the provider playlist on the current run; Pass 4 orphan reconciliation deletes it per the rule's `orphan_action` (default delete) when it drops out — no wall-clock arithmetic, no timestamps, no run counters; a run that could not observe (provider fetch failure, invalid config) makes no delete decision, so a transient error can never mass-delete promoted channels. **Clustering is exact-event-key only (PO decision):** same-run streams from any provider sharing one normalized event identity form ONE promotion unit (no fuzzy clustering; promoted channels never enter the matcher candidate set); dateless `assume_current_date` parses inherit the t6bin identity semantics — no synthesized date ever appears in the channel name or identity, so the re-run after midnight adopts the same channel. Create-or-adopt idempotence rides the existing group-scoped duplicate lookup; if the event later appears in the master group, its streams attach to the master and the promoted duplicate reconciles away the same run (self-healing). `max_promote_per_run` (default 25, max 200) caps NEW channels per run with a `event_sync_promote_capped` warning on overage; the rule's dummy EPG profile covers promoted channels exactly like masters; rollback of a promotion run uses the standard snapshot path (created channels deleted, master stream lists restored). **Preview parity by construction:** the preview computes the promotion plan with the same pure helper the live run executes (`services/event_sync_promote.py`) — new "Would promote (N)" section + per-unmatched-row annotations in the preview panel and payload, promotion counts in the summary line, and a distinct "Would promote"/PROMOTE block in the MCP `preview_event_sync` renderer. Pinned semantics: an operator never-attach exclusion blocks the ATTACH to that specific master, not promotion — an `excluded_by_operator` stream still promotes; `ambiguous` streams never do (open review questions). Editor: collapsed "Promote unmatched events" subgroup on the Behavior step with explicit copy that ECM will CREATE and DELETE channels in the target group. Docs: `docs/event_sync.md` → "Promoting unmatched events (Phase 3 opt-in)" (the master-as-ceiling section no longer says promotion doesn't exist). (bead enhancedchannelmanager-ti939.4.1)
- **Event Sync: operator team-alias dictionary for the team-token matcher — known equivalences instead of a lower threshold (bead ti939.4.2, build 0138, GH epic ti939).** Recurring missed matches on abbreviation-heavy providers traced to team-name variants the matcher's abbreviation/initialism logic cannot bridge (nicknames like "Red Devils" share no letters with "Manchester United", so the pair HARD-rejects as a team-token conflict at any score). New instance-wide dictionary of alias groups — each a set of known-equivalent team spellings (`Man Utd == Manchester United == MUFC`) plus an optional evidence note — consulted by the team-token layer on BOTH paths: hard-reject rescue (aliased spellings at the same kickoff stop rejecting as `team_token_conflict`) and boost (aliased sides count as full team agreement, so `max(fuzzy, team_score)` lifts the pair into the attach band). This is the safe direction for recall: one declared equivalence instead of lowering the evidence bar for every pair. Safety contract pinned by tests: the lookup is strictly MONOTONIC (an alias can only raise team similarity — different-group or no-group pairs score byte-identically to a no-alias run, so an alias can never CREATE a disagree), the qualifier rail (women's/U21/reserves) outranks aliases, and no admission floor changes. Storage is a JSON setting (`event_sync_team_aliases` on DispatcharrSettings — no DB table, no migration), written only through the new validated + journaled `GET`/`PUT /api/event-sync/team-aliases` endpoints (terms checked against the matcher's own team normalization: ≥2 terms per group, no blank/identity-free terms, one group per term); the general settings POST preserves the field. The matcher loads the dictionary at the `match_streams`/`score_pair` boundary (`team_aliases=None`), so attach runs, previews, and the debug bundle share one dictionary by construction while tests and the frozen corpus inject explicit fixtures — the corpus gate stays byte-stable. Operator surface: new "Event Sync Team Aliases" section on Settings → Channel Pipeline (tag-list group editor with per-group evidence note, own Save); MCP tools `get_event_sync_team_aliases` / `update_event_sync_team_aliases` (full-replace write mirroring the PUT). The shipped dictionary is EMPTY by design — aliases are corpus-gated: add a group only with preview/journal evidence proving the equivalence, because a wrong alias is a new false-positive vector (docs/event_sync.md → "Team aliases (operator dictionary)"). (bead enhancedchannelmanager-ti939.4.2)
- **MCP: four recently-shipped ECM features are now surfaced through MCP tools (bead fltt3, build 0137).** A coverage audit found today's ships partially invisible to MCP — an operator driving ECM through the MCP tool set had no way to reach them even though the REST API and UI already had them. Closed: (1) new `get_stale_stream_ids` tool (`GET /api/streams/stale-ids`, bead po78p) — the cheap Dispatcharr `is_stale` id set, cross-referenced with the heavier `get_stale_streams` report so operators pick the right one; (2) `get_provider_stats` gains a `metric="stream_usage"` branch (`GET /api/stats/providers/stream-usage`, bead n5cwp) rendering the per-provider assigned/total/utilization table, including the synthetic "Unknown" bucket; (3) new `get_m3u_digest_settings` / `update_m3u_digest_settings` tools (`GET`/`PUT /api/m3u/digest/settings`) — the update tool only sends fields the caller passes, including the `account_ids` filter (bead wwovg); (4) `preview_event_sync`'s renderer now surfaces `preflight.warnings` (e.g. the stale-dateless rail-inert warning, bead 2ey2y) and the `stale_suspect_streams` / `freshness_unknown_streams` staleness counts in its summary line. All four follow the existing `Endpoint` contract + `@mcp.tool` pattern, validated against the live backend OpenAPI spec by the MCP tool contract test. (bead enhancedchannelmanager-fltt3)
- **Event Sync: operator never-attach exclusions — durable "never attach this provider stream to that event" standing orders (bead ti939.3.5, build 0136, GH epic ti939).** Stateless recompute meant a false-positive attach the operator manually detached was re-attached on the next run, forever, until the pattern/threshold changed. New `event_sync_exclusions` table (Alembic migration 0036) keyed on content fingerprints ONLY — `(rule_id, provider_id, stream_name_hash, event_key)`, never stream/channel ids (both churn) — so an exclusion survives provider refreshes and stream-ID churn by construction. The shared resolver consults exclusions BEFORE the attach band is honored (and before accept-upgrades: an exclusion OUTRANKS a prior review-queue accept for the same pairing — the two never both apply); an excluded pairing can neither auto-attach nor re-enter the review queue, and reports as the distinct `excluded_by_operator` disposition in preview rows/summary counts, run summaries, and the one-line drift detector. Operator surface: a "Never attach" action on Event Sync Review queue cards (records the exclusion, closes the question) plus a new Never-attach Exclusions panel below the queue to list and remove standing orders (removal makes the pairing matchable again on the next run/preview). Create/delete journal under category `event_sync` (`exclusion_create`/`exclusion_delete`); exclusions are preserved and re-keyed by rule name across config backup/restore, mirroring the review-decision preservation. New `GET/POST /api/event-sync-exclusions` + `DELETE /{id}` endpoints and MCP tools `list_event_sync_exclusions` / `create_event_sync_exclusion` / `delete_event_sync_exclusion` (two-step confirm on delete); `preview_event_sync` MCP output renders the excluded count and per-row EXCLUDED lines. (bead enhancedchannelmanager-ti939.3.5)
- **Stats: new Provider Stream Usage section shows per-provider assigned-stream counts and utilization (bead n5cwp, build 0135, GH-482).** Neither Dispatcharr nor ECM previously surfaced how much of each M3U provider's stream catalog is actually wired into a channel. New `GET /api/stats/providers/stream-usage` endpoint reuses the existing cached reverse stream→channel index (`routers.streams._get_stream_channel_index`) rather than re-sweeping every channel, and is itself cached for 300s. Two counting metrics are surfaced intentionally: `assigned_streams` (PRIMARY) — distinct streams from the provider assigned to at least one channel (a stream in 3 channels counts once); `total_assignments` (secondary) — sum of channel-memberships across those streams (a stream in 2 channels counts twice), surfacing providers whose streams get heavily reused. `total_streams` (the provider's full catalog size) and `utilization_pct` give scale/context; an assigned stream whose `m3u_account` doesn't resolve to a known/current provider lands in a synthetic "Unknown" bucket rather than being silently dropped. Not admin-gated — Dispatcharr-derived catalog/assignment data, the same trust tier as `GET /api/stats/channels`, not per-user watch history like the sibling `/providers/*` telemetry endpoints. New sortable `ProviderStreamUsagePanel` renders the table on the Stats tab. (bead enhancedchannelmanager-n5cwp)
- **Stats: Live Stats provider display condenses to a table when more than 6 providers are configured (bead 49obj, build 0135, GH-481).** Reported on an 11-provider install: the per-provider tile badges in the Live Stats header wrap into several rows and providers scroll out of view once there are more than a handful. Above a 6-provider threshold, the tile badges are replaced by a condensed table (name + live connection count per provider) inside a 220px max-height scroll container, so the header stays a fixed, legible size regardless of provider count; installs with 6 or fewer providers keep the existing tile layout unchanged. A settings-level toggle to force the condensed/tile layout independent of provider count was considered and deliberately deferred as a follow-up candidate — the auto-switch threshold covers the reported case. (bead enhancedchannelmanager-49obj)
- **M3U Change Digest: notifications can now be scoped to selected M3U accounts — change logging stays complete (bead wwovg, build 0135, GH-496).** An operator running a high-churn "FAST" provider (10k+ stream URL changes/hour) alongside slow-changing standard providers wanted that provider's changes excluded from digest emails/Discord without losing its change history. New "Account Filter" section on Settings → M3U Digest (reusing the shared `GroupMultiSelectDropdown` component) lets an operator pick a subset of M3U accounts to include in digest notifications; `M3UChangeLog` DB logging is never filtered by this setting, regardless of selection — only what gets built into an email/Discord notification is scoped. Empty/unset selection means "all accounts" (unchanged default behavior for every existing install). New `M3UDigestSettings.account_ids` column (nullable JSON, Alembic migration 0035) backs the setting; `tasks/m3u_digest.py`'s digest-build query applies the scope via `M3UChangeLog.m3u_account_id.in_(account_ids)` when no single-account filter is already active. The immediate-digest path (fires for one account right after its own M3U refresh) skips sending entirely — without building anything — when that account isn't in a non-empty selection, logging at INFO; the operator deliberately excluded it. (bead enhancedchannelmanager-wwovg)
- **Event Sync preview surfaces staleness-signal counts and warns when the stale-dateless demote rail is inert (bead 2ey2y, build 0132).** The jqwfq stale-dateless demote rail fails open by design when no qualifying M3U snapshot exists (young deployment, change monitor disabled, group not captured) — correct, but previously silent: an operator who turned the rail on had no way to see it wasn't protecting anything. The preview pre-flight now emits an explicit WARNING (check id `staleness_rail_snapshots`) when a rule has `assume_current_date` + `demote_stale_dateless` on but zero secondary streams are covered by a usable previous-day snapshot, and the preview summary carries `stale_suspect_streams` / `freshness_unknown_streams` counts so the tri-state per-stream signal is visible at a glance. Warnings never flip pre-flight `ok` to false — nothing is misconfigured; the rail just has no data yet. Preview panel renders the warning and both counts. (bead enhancedchannelmanager-2ey2y)
- **Stale-stream indication in the Channels and Streams panes (bead po78p, build 0130, closes GH-696).** Dispatcharr flags a stream `is_stale` (+ `last_seen`) when the provider's own M3U refresh no longer lists it in the source playlist, but ECM had no visual indication when this happened — an operator had to notice a channel silently stop working with no clue why. New cached `GET /api/streams/stale-ids` endpoint (`backend/routers/streams.py`) pages `client.get_streams()` collecting the stale id set + last-seen timestamps (300s TTL, `bypass_cache` param), avoiding an N+1 against Dispatcharr; it's the single source of truth the frontend resolves against. **Channels pane:** a channel with a stale assigned stream gets an amber indicator on the stream-count area (icon + count, tooltip naming how many streams are stale) — slotted into the existing failed/black-screen/low-fps indicator precedence, right after has-failed — plus an amber left-border + background tint on the row (Option B, mirrors the existing "no streams" row treatment). **Expanded channel stream list:** stale streams get an amber "STALE" badge (modeled on the probe-failed badge), with last-seen in the tooltip when available. **Streams pane:** group headers show an amber stale-count pill when the group contains stale streams (unchanged for groups with none); stale stream rows get the same STALE badge and amber row treatment, reading Dispatcharr's `is_stale` field directly (already passed through this pane's data). All amber styling uses the `--warning`/`--warning-bg`/`--warning-text` design tokens — not red, which stays reserved for probe failures. (bead enhancedchannelmanager-po78p)
- **Pending Merges: candidate channel name, number, and group surfaced in the API + UI, instead of a bare id (bead 09x38.14, build 0126).** UX pass-3 (source-confirmed: `PendingMergeRecord` in `frontend/src/services/api.ts` had no name field, and the backend payload never carried one) found that an operator deciding Merge-vs-Create-New on a Pending Merges row had to leave the page to find out what channel a non-exact-match candidate would actually merge into — only the bare `candidate_channel_id` rendered. `GET /api/channel-merges` now resolves `candidate_channel_name`, `candidate_channel_number`, and `candidate_channel_group_name` from Dispatcharr in one additional batched call per list-page request (`get_channels(page_size=1000)`, mirroring the existing "candidate pool" convention already used by `GET /candidates` and the enqueue endpoint in the same router) rather than one Dispatcharr round-trip per row — an N+1 pattern that would have scaled with queue depth (up to 200 rows/page). All three fields are additive and `null` when the candidate cannot be resolved (deleted since queuing, or a Dispatcharr fetch failure — both degrade to the same fallback rather than a 500). The Pending Merges page now renders the resolved name (plus `#number` and group, when present) as the primary label, with the raw Dispatcharr id kept as secondary text/tooltip; an unresolved candidate renders an explicit "Channel no longer exists (id N)" state instead of a blank. The MCP tool `list_pending_channel_merges` is a pure passthrough of this same endpoint's JSON, so it gains the new fields automatically with no code change — only its docstring's field list was updated for accuracy. Additive-only: no existing field removed or renamed. (bead enhancedchannelmanager-09x38.14)

- **Streams pane: collapsible prefix categories over the flat group list (bead 09x38.5, build 0121).** Channel Manager's Streams pane rendered ~90+ provider stream groups as one flat alphabetical accordion despite clear naming-convention prefixes, with no mitigation beyond the group filter dropdown (unlike M3U Manage Groups, which has search/filters at 791 groups). Groups now bucket under collapsible category headers derived from the group name's prefix — the token before the earliest `|` or `:` delimiter, trimmed (e.g. "CA", "UK", "US", "USA", "Sports"); a field-value survey of the live `/api/stream-groups` response showed both delimiter conventions in production use, so restricting to `|` alone (as originally scoped) would have dumped ~35 of 137 live groups into "Other" for no reason. Groups with no recognizable prefix fall into a final "Other" bucket. Categories default to collapsed and persist expand/collapse state per session in `localStorage` (`ecm-streams-category-expanded`), matching the pane's existing filter-persistence idiom; "Expand all"/"Collapse all" now also expand/collapse every category. While a search is active, categories auto-expand to surface matches immediately without mutating the persisted collapse state. Category headers are real `<button>` elements with `aria-expanded` and a visible name+count label (name + group count), so the icon-only-button a11y audit passes with no extra `aria-label`. Categorization applies to the already-filtered (provider/group-filtered, search-filtered) visible set — existing behavior (group expand, drag-to-channel, provider/group filters, "Hide mapped", Edit Mode selection) is unchanged. (bead enhancedchannelmanager-09x38.5)

- **Logo Manager: sortable Name/Used-By columns + server-side "unused only" filter, at 2,946-logo scale (bead 09x38.13, build 0119).** Logo Manager previously loaded every logo into the browser on tab-open and paginated/searched client-side, with no way to sort and no way to isolate unused logos — bulk-identifying cleanup candidates across ~2,946 rows was impractical. Dispatcharr's own Logo API has no ordering support and its `search` param was silently a no-op (only `name`/`used`/`ids` are read), so `GET /api/channels/logos` gained `sort_by` (`name` | `channel_count`), `sort_order` (`asc` | `desc`), and `unused_only` params: when any of sort/filter/search is requested, ECM fetches the complete Dispatcharr logo list in one call (via Dispatcharr's `no_pagination=true` escape hatch) and sorts/filters/paginates it server-side, so results are truthful across pages; requests using none of those params keep the original zero-overhead single-page passthrough (LogoModal picker, AutoSyncSettingsModal, GuideTab, and `getAllLogos()` are unaffected). LogoManagerTab now fetches one page at a time instead of the full catalog, with clickable Name/Used By column headers (M3UChangesTab's sortable-header idiom) and an "Unused only" toggle that composes with search and pagination. (bead enhancedchannelmanager-09x38.13)

- **User guide: "Set Up Your First Channels" end-to-end workflow tutorial (bead gsnw0.16, build 0113).** New `docs/user_guide/getting-started/your-first-channels.md` walks a new operator through one continuous path — add an M3U account, add an EPG source, choose which stream groups to sync, refresh, then create channels, channel groups, and stream assignments in Channel Manager — with 12 screenshots captured per the user-guide screenshot conventions. Getting Started section status advances from Stub to In progress. Docs only, no functional change. (bead enhancedchannelmanager-gsnw0.16)

### Fixed
- **Two Settings controls gain programmatic labels for assistive technology (beads enhancedchannelmanager-db8ae + enhancedchannelmanager-in1o0, build 0174).** (1) The Full Backup ZIP file chooser (Settings → Backup & Restore → Restore Full Backup) had no associated label or accessible name — AT users couldn't identify a high-consequence restore control or its expected file type (WCAG 1.3.1, 3.3.2, 4.1.2). It now carries a visible, programmatically-associated label ("Choose ECM full-backup ZIP (.zip)", `htmlFor`/`id`), so the control's purpose and accepted .zip format are announced and `getByLabel` locates it uniquely. (2) The Authentication minimum-password-length number input (Settings → Authentication → Local Authentication) had a bare `<label>` with no `htmlFor` and an input with no id/aria attributes. The visible "Minimum Password Length" label is now associated via `htmlFor`/`id`, and the 6-32 constraint guidance is exposed to AT via `aria-describedby`. Component tests pin both associations (getByLabelText uniqueness, accessible description, and the labeled input still driving the save payload); rendered-browser verification via Playwright `getByLabel` passed at 1920x1080 and 1280x720 for both controls. No behavior change — label association and one CSS rule only.
- **Settings → M3U Digest: a failed settings load no longer retries in a tight loop or storms error toasts — it lands in a recoverable inline error state (bead enhancedchannelmanager-fi3dq, build 0173).** When `GET /api/m3u/digest/settings` (or the M3U-accounts fetch in the same batch) failed, the page-activation effect re-armed itself every time the loading flag flipped back to false with settings still null, re-issuing the request batch in an unbounded loop, pushing one error toast per failed attempt, and leaving the page stuck at "Loading digest settings…" with no way to recover. Two-part fix: (1) the digest page now latches a `digestError` state on failure — the activation effect stops re-issuing requests while it is set, so one failed page activation costs exactly one request batch and zero toasts — and renders a stable inline `role="alert"` error panel (the Channel Pipeline tab's existing error-state idiom) with the failure message, actionable guidance, and a keyboard-focusable Retry button that clears the latch and issues exactly one new batch; (2) the toast provider (`NotificationContext`) now deduplicates equivalent notifications (same type + title + message) app-wide — repeat callers get the existing toast's id back instead of stacking a duplicate — so no misbehaving caller can flood the viewport again (the visible-toast cap of 5 with a non-interactive "+N more" overflow indicator was already in place and is now pinned by test). Component tests cover the one-batch-per-activation, no-toast, retry-recovers, and retry-fails-again paths plus provider dedup/cap semantics; rendered-browser verification (injected digest-endpoint failure) confirmed one request per activation, zero toasts, in-viewport keyboard-reachable Retry, and full recovery at both 1920x1080 and 1280x720. (bead enhancedchannelmanager-fi3dq)
- **Flaky test: PendingMergesPage cancel-restore race (bead enhancedchannelmanager-6wkgm, build 0171).** The 'restores the original paginated view when a 51-row all action is cancelled' test raced its assertions against the post-cancel refetch: after clicking Cancel it `waitFor`'d the snapshot row "Stream 51" to disappear, but dialog close + refetch + re-render took ~1976ms on a loaded CI runner vs testing-library's 1000ms default `waitFor` timeout (1 failure in ~100 executions on dev-push run 30178431720). Test-only fix, same family as the earlier 681a1 `vi.waitFor` hardening: the test now first waits for the deterministic intermediate state (dialog removal), then waits for the paginated view's restoration — with the checkbox-count assertion moved inside the same `waitFor` so it can't fire before the re-render — under an explicit 5000ms timeout. No component behavior change; file verified green across 10 consecutive runs.
- **M3U change digest was blind to pure renames — a group whose stream COUNT stays the same but whose stream NAMES change now records the swap (bead enhancedchannelmanager-wawwh, build 0167).** `M3UChangeDetector.detect_changes` only ever compared prev/curr stream-name sets when a group's stream count actually changed, so a provider that swaps one dateless slot name for another in place (the PPV-slot pattern: "Event A" out, "Event B" in, count unchanged) recorded nothing in `M3UChangeLog` — silently invisible to the M3U Changes tab and digest notifications, both of which read `M3UChangeLog`. (The jqwfq stale-dateless guard is a separate code path and was never affected by this gap: it reads `M3USnapshot.groups_data` directly via `previous_day_names()`, and `create_snapshot()` writes that column in full on every refresh regardless of what `detect_changes` records to the change log.) The per-group prev/curr name sets were already being built unconditionally every refresh for every common group (not just count-changed ones), so this was a comparison the code was throwing away, not a missing fetch: `detect_changes` now also diffs those sets when the count is stable, but ONLY when both snapshots' TRUE group totals are below the per-group name-capture cap (`tasks.m3u_refresh.MAX_STREAM_NAMES`, 500) — a review round (PR #740) found that "both sides captured some names" alone is not sufficient: Dispatcharr's stream listing carries no explicit ordering, so a group at or above the cap can have its captured name list drawn from a different page each refresh with byte-identical real membership, which would otherwise fabricate a phantom rename. A capped group therefore stays silent here — a known, accepted limitation, matching the codebase's existing philosophy for this exact cap (`services/event_sync_staleness.py`: "the cap can cause missed detections, never false staleness"), not a claim that capped groups are covered. A second review round found the count-stable diff itself used set semantics, which collapse duplicate names — `["Event A", "Event A"]` → `["Event A", "Event B"]` recorded an addition with no matching removal, hiding the replaced duplicate occurrence; the diff now uses `collections.Counter` multiset subtraction so duplicate-name replacements are recorded correctly. Recorded as a normal `streams_added` + `streams_removed` pair per swap, so existing M3U Changes tab filters/rendering need no changes. (bead enhancedchannelmanager-wawwh)

### Changed
- **UX polish batch — 9 minors from walkthrough passes 1-3 (bead 09x38.15, build 0127).** A holding bead of small, independent findings from the 3-pass UX walkthrough, promoted together as one mechanical batch: (1) EPG Manager rows sharing a priority now get an inline `.badge-warning` "tie" chip next to the priority number — previously the tie was only explained via the column-header tooltip. (3) Channel Manager's Channel List Filters panel gains a "Clean up empty groups…" link that navigates to Settings → Maintenance → Orphaned Channel Groups (a new `ecm:navigate-settings-maintenance` window event, mirroring the existing `ecm:open-task-editor` cross-tab navigation pattern) rather than embedding the scan/delete tool inline. (4) The two same-named "Debug Bundle" actions are disambiguated: Channel Pipeline's header button is now "Pipeline Debug Bundle" (with a tooltip pointing at the whole-app one), and General Settings' is now "Generate App Debug Bundle" with copy stating its whole-app scope. (5) Normalization groups list now reads "1 rule" (singular) instead of "1 rules". (6) The Standard Rule editor's Output & Run step gains an inline orange warning (matching the adjacent merge-scope control's `.norm-hint` idiom) when Orphan Cleanup is on its destructive default ("Delete orphaned channels"). (7) Backup & Restore gains a "Which one do I need?" card giving top-level guidance across the three overlapping-sounding restore mechanisms — YAML Export, DBAS Backup artifact, and legacy Full Backup — before the cards that implement each. (8) The Guide tab's EPG-grid channel-name column widens from 200px to 260px at 1280px+ viewports, giving names more room before truncating (native title tooltip remains the fallback below that width). (9) The Stats tab's ~10 stacked sections (4 conditional + 6 always-mounted panels) gain a "Jump to section" dropdown, reusing the `OverflowMenu` component (which gained an optional `icon` prop for this non-kebab use) with `scrollIntoView` targeting new per-section anchor ids; the dropdown only lists sections actually present in the DOM. (12) Backup export/restore section labels renamed for clarity: "Scheduled Tasks" → "Task Settings & Alerts" and "Task Schedules" → "Task Run Schedules" (`backend/routers/backup.py` `RESTORABLE_SECTIONS`, `frontend/src/components/BackupRestoreModal.tsx` `SECTION_LABELS`) — a prior engineering spike confirmed `ScheduledTask` and `TaskSchedule` are genuinely distinct entities, so this is a label-only rename, not a collapse. **Explicitly out of scope, reported not built:** item 2 (a contextual multi-select action bar on ChannelsPane) is a real design feature, promoted to its own bead rather than built as a minor; item 10 (M3U Changes row-drawer) had no trivial enrichment worth doing standalone; item 11 (Journal automated-entry volume) is a data-retention policy question for the PO. (bead enhancedchannelmanager-09x38.15)
- **Channel Pipeline: the Standard Rule editor becomes a true step-at-a-time wizard, matching the Event Sync editor's interaction model (bead 09x38.10, build 0125).** UX pass-2 (DOM-confirmed) found the Standard Rule editor and the Event Sync Rule editor shared the identical `.modal-stepper` pill strip but behaved oppositely: Event Sync is a true wizard (one step rendered at a time, Back/Next footer) while the Standard editor rendered all three regions (Logic → Targeting → Output & Run) as siblings in one continuous scroll-jump form — so learning one built the wrong mental model for the other. **PO decision (2026-07-17): convert, not visually differentiate.** The Standard editor now renders exactly one step at a time via the shared numbered step-pill strip (`aria-current="step"`), with an adaptive footer (Cancel · Back · Save · Next; Next becomes the primary Save on the last step). The whole-rule synthesis rail (live plain-language intent sentence, advisory analyzer chips, save hint) stays **persistent across every step** — it reads full form state, which exists regardless of which step renders — so the operator never loses the "what this rule will do" summary while stepping. Validation display is **routed per-step**: every error surfaces on Step 1 (the rule name in the always-visible header, plus conditions and actions in the Logic step), so pressing Save from any step routes back to Step 1 with the error visible and focused. There is **no Review step** (unlike Event Sync's step 4): the Standard rule has no Preview/write gate to reach first and the persistent rail already reviews the whole rule continuously, so a Review step would only duplicate the rail. **Save is exposed on every step** rather than last-step-only: the Standard rule has no preview gate and its step-2/3 fields all carry safe defaults, so a valid rule can be saved from any step (this is exactly the Event Sync editor's editing-an-existing-rule footer, applied uniformly). **No rule behavior, validation logic, or save-path change** — only which step a given field is revealed on and where validation errors are displayed; an existing full-config rule round-trips to a byte-identical save payload (proven by test). The old IntersectionObserver scroll-spy is removed; step focus moves to the active step's heading on navigation. (bead enhancedchannelmanager-09x38.10)
- **EPG Manager: legacy "Dummy EPG Sources" section folds away; Dummy EPG Profiles is the supported dummy EPG path (bead 09x38.4, build 0124).** The EPG Manager tab showed two adjacent, near-identically described dummy EPG sections — legacy Dispatcharr `source_type=dummy` "Dummy EPG Sources" and "ECM Dummy EPG Profiles" — with no way for an operator to tell which to use. PO decision (Option B): the **Dummy EPG Profiles** path is the supported one (it is what Event Sync integrates with, where the live guide-data profiles are authored, and the surface with live preview + richer templates); the legacy `source_type=dummy` section is deprecated. The legacy section now renders **only when such sources already exist** on the instance — on an instance with zero (the common case) it disappears entirely, no empty state and no "+ Add Dummy EPG". Where legacy sources do exist (grandfathered on other deployments) they render as before — visible and fully editable (toggle/edit/delete), retitled "Dummy EPG Sources (Legacy)" with a deprecation notice pointing at Dummy EPG Profiles — but the new-creation affordance is gone, so no new legacy dummy sources can be made through ECM. The supported section drops its "ECM" qualifier and is now simply titled **Dummy EPG Profiles** (loading state, empty-state copy, and the per-row type label updated to match). **Zero data deletion, no migration, no backend change:** legacy dummy-source creation was always the generic `POST /api/epg/sources` with `source_type=dummy` (Dispatcharr parity) — that API is untouched and still reachable; this is a UI-level fold only. The live `B1G Advanced EPG` profile and all five real EPG sources are unaffected. Docs updated: `docs/template_engine.md` gains a supported-surface/legacy-deprecation note (nothing removed — it stays authoritative for the shared engine), and `docs/user_guide/epg/index.md` gains a Dummy EPG section clarifying the supported path and the grandfathered legacy note. (bead enhancedchannelmanager-09x38.4)
- **Auto-Sync Settings modal: the 3 collision-prone sections now name their own entity and cross-reference the similarly-named screen they are NOT (bead 09x38.8, build 0123).** UX pass-1 flagged "Channel Profile Assignment", "Stream Profile Assignment", and "Channel Name Filter (Regex)" as apparent duplicates of the per-account "Manage Account Profiles", the top-level "Stream Profiles" catalog, and the per-account "Manage Filters" screen, respectively — same English words, different entities. A follow-up engineering pass verified all three are functionally distinct (two carry live operator config — "LiveTV" channel profile and "Proxy" stream profile on the Infinity account's "USA | Flo Racing" group) and dropping them would have been a regression, not a dedup; the PO rescoped from deletion to disambiguation-in-place. Each section's help text now states which Dispatcharr entity it configures and why the similarly-named screen elsewhere doesn't cover the same ground: Channel Profile Assignment sets Dispatcharr Channel Profiles (client-facing visibility) for channels synced from this group, distinct from "Manage Account Profiles" (M3U stream failover); Stream Profile Assignment is where a channel's stream/transcode profile actually gets assigned, since the top-level "Stream Profiles" screen is a read-only catalog; Channel Name Filter (Regex) filters this group's already-imported streams at sync time, distinct from "Manage Filters" (account-wide, at M3U import time). No field removed, no save/behavior change. Also fixes the disabled Manage Groups gear tooltip, which named only the Auto-Sync prerequisite — it now also names the group's Enabled toggle when that's the one blocking it. (bead enhancedchannelmanager-09x38.8)
- **Administration → "Security" settings page relocated into Backup & Restore, and its lone control now uses explicit Save semantics (bead 09x38.12, build 0122).** UX audit found the page contained exactly one setting — the backup-destination SSRF allowlist (LAN-friendly vs public-only) — despite its broad "Security" name, and persisted instantly on radio click with no Save button, unlike every other Settings page's explicit "Save Settings" — the wrong inconsistency for a security-relevant control. The card (new `OutboundPolicyCard`, replacing `SecuritySettingsSection`) moved into **Settings → Backup & Restore**, just below the backup-schedule banner, since the setting configures a backup *destination* and that page is its natural home; the standalone "Security" page and its Administration nav item are gone. The control now stages the radio choice as local state and only calls the (unchanged) `PATCH /api/settings/security` endpoint when the operator clicks the new "Save Settings" button — matching the pattern every sibling Settings section (`TLSSettingsSection`, `AuthSettingsSection`, …) already uses; navigating away without saving discards the staged choice, same as those siblings. Bookmarked/shared `#settings/security` URLs keep resolving to `#settings/backup-restore` via the existing legacy-alias hash-routing mechanism (the same one used for the Auto-Creation → Channel Pipeline rename) instead of silently falling back to Settings → General. Doc and in-app copy pointing at "Settings > Security" (the first-run backup-destination modal, `api.ts` comments, `docs/user_guide/backup-restore/configure-cloud-destinations.md`) updated to the new location. Backend endpoint untouched — it was already a focused, idempotent single-field write that fits the staged-save pattern without modification. (bead enhancedchannelmanager-09x38.12)
- **Channel Pipeline: Execution History's "Rollback" and "Undo this run" row actions now explain — and cross-reference — what each actually does (bead 09x38.9, build 0120).** DOM-confirmed UX finding: the two icon-only, destructive actions sat side by side with asymmetric explanation — `title="Rollback"` had zero elaboration next to `title="Undo this run — restores pre-run channel state from snapshot"` — so an operator couldn't tell what distinguished them before clicking. Reading the handlers down to the engine (`rollback_execution` vs `restore_snapshot` in `channel_pipeline_engine.py`) confirmed they are genuinely different mechanisms: **Rollback** is the legacy per-run undo — it deletes the channel(s) this run created and reverts the channel(s) it modified using this run's own recorded changes (or a surgical journal-driven un-merge when the journal fully covers the run), and does NOT touch the pre-run snapshot. **Undo this run** is a full whole-run restore from the pre-run `ChannelPipelineSnapshot` — it overwrites every affected channel's current stream assignments with the exact state captured before the run started, an intentional "optimistic overwrite" (ADR-010 §D5) that also discards any edits made after the run (manual edits, later merges, Dispatcharr drift). Both row tooltips now state this and reference each other by name; the "Confirm Rollback" dialog body was rewritten from a bare one-line count ("This will delete N created channels") to name the legacy-undo mechanism and point at "Undo this run" as the full-restore alternative; the "Undo This Run" dialog gained one added sentence contrasting it with Rollback. The Rollback button also gets the shared `.action-btn.danger` red hover treatment (matching the Delete-rule button elsewhere in this file) so its color no longer matches the Undo button's neutral hover, on top of the two Material Icons glyphs (`undo` vs `settings_backup_restore`) already being visually distinct. No behavior, endpoint, or confirm-flow change — copy and one class name only. (bead enhancedchannelmanager-09x38.9)

### Removed
- **Orphaned status-page components, dead `/api/services` API functions, and the non-functional OIDC "link account" button are gone (beads zipet / f9rlc, build 0062).** The coverage audit found `frontend/src/components/status/*` (StatusIndicator, ServiceCard, ServiceControlPanel, MaintenanceBanner, IncidentTimeline, AlertConfigurationPanel) was an orphaned component cluster never mounted anywhere, alongside dead `/api/services` functions in the API layer — all deleted (zipet). The Linked Accounts settings section no longer renders an OIDC link button whose backend was never implemented, so it could only ever fail (f9rlc). No functional change — none of this code was reachable. (beads zipet / f9rlc), along with their now-dead code; cloud storage targets were relocated, not removed (beads vrrxv / 1w428, build 0029).** Two rarely-used tabs were retired to shrink the surface area. **FFMPEG Builder:** the visual ffmpeg-command builder tab and its entire frontend cluster (`components/ffmpegBuilder/`, `types/ffmpegBuilder.ts`, `data/ffmpegExplanations.ts`), the backend `/api/ffmpeg` router and the `ffmpeg_builder/` engine package, and the `ffmpeg_saved_configs` table are gone. **Export:** the Export tab (playlist export profiles, generate/preview/download M3U+XMLTV, scheduled publish, and publish history) and its frontend cluster (`components/export/`, `services/exportApi.ts`, `types/export.ts`), the corresponding `/api/export/profiles*`, `/api/export/publish-configs*`, and `/api/export/publish-history*` endpoints, the `export_manager` / `publish_pipeline` / `m3u_generator` / `xmltv_generator` modules, the `export_publish` scheduled task, the `playlist_profiles` / `publish_configurations` / `publish_history` tables, and the export-only MCP tools (`list_export_profiles`, `create_export_profile`, `delete_export_profile`, `generate_export`, `list_publish_configs`, `publish_export`) are gone. **Cloud storage targets RELOCATED (not removed):** the cloud-target endpoints moved from `/api/export/cloud-targets` to a dedicated `/api/cloud-targets` router; the management UI moved into **Settings → Backup & Restore** (new `CloudTargetsCard`); the `list_cloud_targets` MCP tool was repointed to the new endpoint (tool contract key `cloud_list_targets`). DBAS backup uploads continue to work against the relocated surface. **Intentionally KEPT (load-bearing, not part of the tabs):** the `ffmpeg_profiles` table (still a DBAS config backup/restore section); stream profiles (`/api/stream-profiles` and the M3U/XMLTV lineup IPTV clients consume); the `SyncTarget` cross-instance-sync surface; the Guide tab's client-side print feature (PrintGuideModal); dummy-EPG XMLTV generation; all non-Export-tab "export" endpoints (`/api/backup/export-sections`, `/api/backup/export`, `/api/dummy-epg/profiles/export/yaml`, per-channel CSV export, normalization export, auto-creation YAML export). Schema migration `0029` drops `ffmpeg_saved_configs`, `playlist_profiles`, `publish_configurations`, and `publish_history`. (beads vrrxv / 1w428)

### Fixed
- **Channel Pipeline rule editor: the action card's remove "X" no longer has a card-height clickable area (bead enhancedchannelmanager-gbwfs, GH #726, build 0162).** Reported by @langtd19: in the rule editor's Actions section, the remove button's invisible hit area stretched to the full height of the action card (423px tall on a create-channel card, against an 18px glyph), so clicking anywhere in the blank space below the visible X silently deleted the action with no confirmation. Root cause: the button is a direct child of the action card's flex row, which stretches children to the card height by default. The button is now pinned to the top of the card (`align-self: flex-start`, matching the Conditions section's remove button), so the destructive click only fires on the visible X. The Conditions remove button, condition-group and modal close buttons, and other trailing-X controls in the editor family were audited and already constrained. CSS-only; no behavior change beyond the hit area.
- **Channel Pipeline Execution History no longer shows a broken "Rollback" button on snapshot-backed runs (bead enhancedchannelmanager-h2oxl, build 0159).** The Rollback button POSTs to `/rollback` without `confirm=true`; on runs that captured a pre-run snapshot the backend requires `confirm=true` for that path and returns 409, so the button did nothing while a working "Undo this run" (full snapshot restore) sat right next to it. Rollback is now hidden on snapshot-backed runs — "Undo this run" is the correct full-restore action there — and still renders on legacy no-snapshot runs, where `/rollback` works. The Rollback tooltip and confirm dialog copy were updated to describe the no-snapshot case accurately instead of cross-referencing a snapshot/"Undo this run" affordance that isn't present on those rows. Frontend-only; the backend `/rollback` endpoint is unchanged.
- **Channel Pipeline runs now surface a failed `assign_channel_profile` action as a non-green run outcome instead of reporting the whole run "completed", and Event Sync rules actually apply their channel-profile selection (bead enhancedchannelmanager-y3m6o.1, GH #720 hardening / Part 1 truthful-status, build 0152).** Part B (the M3U Auto-Sync-modal path, bead enhancedchannelmanager-y3m6o.3) remains open, so this build hardens Part 1's truthful-status behavior rather than closing GH #720. Fix-forward on build 0151: a team review found that 0151 made the *action* truthful (it returns `success=False` when it cannot prove exclusive membership) but the *run* still finalized as `completed` with a green success badge/toast, so the operator-visible outcome still hid the failure. This build closes five gaps. (1) **Run status** — the engine now aggregates failed actions at finalization; a run with any failed action finalizes as the new `completed_with_errors` terminal status (distinct from green `completed` — some channels may have succeeded, so it is deliberately not a blanket `failed`), persists an error count + short summary on the execution record, and the top-level API result reports `success: false`. The executions UI renders `completed_with_errors` as an amber/warning badge and toast (not color-only — it carries the "Completed with Errors" label and the failed-action summary), and keeps the run's rollback/undo affordances. The unattended auto-fire path (the post-M3U-refresh scheduled run) likewise no longer notifies green on a failed-action run: it reports the run as *completed with warnings* — a single warning notification that fires external alerts, rather than a green success (the GH #720 hidden-failure class on the 3 AM path) or a misleading hard "failed" — while the persisted pipeline `execution.status` stays the honest `completed_with_errors`. (2) **Dry-run preview** — a dry run whose channel-profile universe is unavailable now returns an explicit blocking outcome instead of a rosy "Would assign N…", so the preview no longer lies about a live run it could not honor. (3) **No-op write** — a total profile-write failure (every enable/disable PATCH raised) is no longer counted as a channel modification (`modified=False`), so it creates no phantom update count or rollback entity; a partial success still counts. (4) **Event Sync** — a configured `assign_channel_profile` action on an Event Sync rule now actually executes: the dedicated attach phase applies exclusive profile membership to the channels the rule touched this run (newly-attached master channels + promoted channels), the exact "new channel joined to all profiles" case GH #720 describes; the 0151 coverage exercised only a synthetic no-streams path and never ran the real `execute_event_sync_rule` production path. (5) **Rollback honesty** — `assign_channel_profile` carries no reversible previous-state, so it is now marked non-rollbackable and is no longer recorded as a rollback entity that claims to reverse a change neither the legacy rollback nor the snapshot restore can undo. **Post-review hardening (same build 0152).** A follow-up team review found the failure aggregation incomplete and three related surfaces still misleading; all are now fixed: **(a) Full failure aggregation** — every executed-action/phase failure now funnels through the one run-level aggregation chokepoint, not just `assign_channel_profile` action failures. Event Sync attach failures (master + promotion), Event Sync dummy-EPG assignment failures, deferred-EPG (Pass 5) retry failures, and the auto-create default-profile assignment failure (previously log-only — now escalated per PO decision) all finalize the run `completed_with_errors` + non-success. **(b) Compound capped + failed runs** are no longer lossy or noisy — a run that is both capped and had failed actions persists BOTH conditions on the execution row (the cap info AND the failed-action summary) and the unattended path emits ONE coherent warning carrying both, instead of a cap warning plus a separate "Completed with Warnings". **(c) Rollback disclosure** — a run that mutated channel-profile membership persists a `non_reversible_profile_changes` warning, and the executions UI's Rollback/Undo confirmations and tooltips now disclose that channel-profile membership will NOT be restored (rollback is kept for the reversible stream/field changes; it no longer over-promises "all affected"). **(d) Task History honesty** — a `success=True` run with `failed_count > 0` (the PO-chosen "Completed with Warnings" envelope) now renders an amber warning indicator in Task History instead of solid green. Regression tests drive the real run-level status via `run_pipeline` and the real Event Sync attach path via `execute_event_sync_rule`, with a per-failure-type regression for each aggregated phase. (bead enhancedchannelmanager-y3m6o.1)
- **Channel Pipeline `assign_channel_profile` now reports a non-success/partial result when it cannot prove exclusive profile membership, instead of falsely reporting success (bead enhancedchannelmanager-y3m6o.1, GH #720 hardening, build 0151).** Follow-up to Part A (build 0150): a team review found the subtractive-membership action reported `success=True` even when it had NOT actually achieved exclusive membership, in two ways. (1) Partial write failure — each per-profile enable/disable call was best-effort but a failed DISABLE was caught, counted only as a non-success, and swallowed, so a channel could be left in a profile it should not be in while execution history/UI showed success. The helper now collects the failed profile ids and the action returns a non-success result surfacing them (enabled/disabled counts plus the failed profile ids — reported as the run's `completed_with_errors` status from build 0152, not an informal "partial result"), so incomplete reconciliation is observable and safe to retry manually by rerunning the pipeline (every profile-write path is idempotent). (2) Universe-unavailable degradation — a failed profile-universe fetch was collapsed to an empty universe, silently degrading to enable-selected-only-and-report-success and recreating GH #720 during a transient read failure. The engine now passes an explicit "unavailable" sentinel (`None`, distinct from `[]` "genuinely empty") when `get_channel_profiles()` raises, and the action FAILS rather than enabling-only when the universe is unavailable. Best-effort continuation is preserved (one failing profile does not abort the rest); the global default-profile assignment path stays a benign no-op on an empty/unavailable universe. Covers standard and event-sync rule paths. (bead enhancedchannelmanager-y3m6o.1)
- **Channel Pipeline `assign_channel_profile` rule actions now honor the channel-profile selection subtractively instead of leaving new channels in ALL profiles (bead enhancedchannelmanager-y3m6o, GH #720 (Part 1 of 2), build 0150).** Reported by @langtd19: standard and event-sync rules with a channel-profile selection added created channels to *every* channel profile rather than only the selected one(s). Root cause: the action applied the selection ENABLE-ONLY, but Dispatcharr auto-joins every newly-created channel to all channel profiles by default, so honoring a selection is subtractive — the selected profiles must be enabled AND the channel disabled in every other known profile; enable-only was a no-op. The action now makes the channel a member of exactly the selected profiles via a shared `_apply_exclusive_profile_membership` helper (the same enable-in/disable-out pattern the global default-profile setting already used), and the engine now fetches the full channel-profile universe whenever any rule (standard or event-sync) uses an `assign_channel_profile` action — previously it fetched only when a global default was configured, so a per-rule selection with no global default got an empty universe and could not disable anything. When the profile universe is unavailable it degrades to enable-selected-only (never worse than before). **Superseded by builds 0151/0152:** the unavailable-universe behavior described here (silent enable-selected-only degradation) was found to be able to recreate GH #720 during a transient read failure — from build 0151 an unavailable universe FAILS the action rather than degrading, and from build 0152 that failure surfaces as the run's `completed_with_errors` status. This is the rule-action path; the M3U Auto-Sync-modal path is Part 2 (still open). (bead enhancedchannelmanager-y3m6o)
- **Portaled dropdowns remain reachable near the bottom of scrollable editors (bead enhancedchannelmanager-ffdx5, GH #717, build 0149).** The normalization-group picker and shared single-select now use the same viewport-aware placement logic: open upward when possible, preserve a usable options area in short viewports, close if their trigger scrolls fully out of view, and recompute through a throttled animation frame when a modal scrolls or the browser resizes. This keeps every group selectable in the Standard Rule Targeting step and fixes the same clipping risk across `CustomSelect` call sites without changing selection behavior.
- **`POST /api/client-errors` no longer 500s on a reported stack trace containing a URL with an out-of-range port (bead k6ud9, build 0142).** Observed live after a frontend deploy: a browser reported a stale-bundle fetch error whose stack trace embedded a URL with a malformed port, and `obfuscate.py`'s `obfuscate_url()` accessed `parsed.port` on the `urlparse()` result unguarded — Python raises `ValueError` for a port outside 0-65535 (or non-numeric), uncaught, so the endpoint itself crashed on exactly the kind of malformed input client-error reports contain, losing the report. `parsed.port` access is now wrapped in try/except, treating an unparseable port the same as "no port" — obfuscation behavior is otherwise unchanged. Audited `obfuscate.py` and its caller (`routers/client_errors.py`) for other unguarded `urlparse` attribute accesses; none found (`.hostname`, `.scheme`, `.path`, `.params`, `.query`, `.fragment` do not raise, and `urlparse()` itself was already wrapped). 12 new tests (`backend/tests/unit/test_obfuscate.py`, `backend/tests/routers/test_client_errors.py::TestHostileUrlPortInStack`) cover out-of-range, negative, non-numeric, and IPv6 hostile ports, plus an endpoint-level regression proving the 500 is gone. (bead enhancedchannelmanager-k6ud9)
- **Auto-created channels' EPG matcher is now region-aware — West channels no longer auto-link to their East counterpart's guide row (bead vznut.4, build 0134).** vznut.2 (build 0090) made the *manual* matcher (`match_channels_epg`) region-aware but explicitly scoped out auto-creation's separate, simpler matcher (`channel_pipeline_executor._match_epg_data`), which kept its own hardcoded timezone list and no region scoring — so a channel auto-created by a Channel Pipeline rule could still West-link to East. `_match_epg_data` now reuses `epg_matching.detect_region()` (one shared source of regional truth with vznut.2, no duplicated detection logic) via a new region-consistency tie-break applied within its existing exact/prefix match tiers, AHEAD of the pre-existing name-length-similarity tie-break — length is a coincidental signal, region is deliberate. Inert for channels with no detected region, so non-regional auto-creation matching is unchanged. 19 new tests (`backend/tests/unit/test_channel_pipeline_executor_epg_region.py`) split between characterization guards pinning pre-fix behavior and new regional coverage. (bead enhancedchannelmanager-vznut.4)
- **EPG audit and match endpoints no longer silently truncate at 10,000 channels (bead vznut.6, build 0134).** `GET /api/epg/audit-duplicates` and `POST /api/epg/match` fetched channels with a single `page_size=10000` call (pre-existing, flagged as code-review nit #4 on vznut.1) — a fleet larger than 10,000 channels would silently lose channels past the cutoff, with `total_channels` then misreporting the true fleet size. Both handlers now share a `_fetch_all_channels` helper (`backend/routers/epg.py`) that pages the full fleet (page_size=1000, same paged-scan shape as the stale-ids endpoint and `stream_stats.get_streams`), with a ~1M-channel hard-stop safety cap (logged, not silent) rather than an unbounded loop. No extra round-trip for the common small-fleet case — a single-page response still resolves in one call. 6 new tests covering multi-page, single-page, and empty-fleet fetches; live-verified on the running instance (identical 50-channel, no-shared-links audit result before/after). (bead enhancedchannelmanager-vznut.6)
- **Event Sync: recurring dateless events no longer require a daily re-accept in the review queue — decisions carry forward across days (bead t6bin, build 0132).** Under `assume_current_date`, the review fingerprint's event key embedded the parser-synthesized date, so a recurring dateless slot ("PPV 01: FURY vs HALL 6PM" listed every day) minted a NEW fingerprint at every midnight: operator accepts/rejects never re-applied, and the stale-dateless demote rail (jqwfq) re-demoted the same slot to the review queue every morning. The key now drops the fabricated date ONLY when the parse came from one of the three synthesized-date dateless patterns, keying on `<title>|dateless|<local clock HH:MM><standard tz offset>` instead — the standard (non-DST) offset so keys survive DST transitions, and distinct clock times or titles stay distinct slots. Accepts keep auto-attaching (outranking the demote rail) and rejects keep suppressing on every following day; the queue no longer refills with answered questions. Dated events are byte-for-byte unaffected — stored rows need no migration. One-time cost: review decisions made on DATELESS slots before this build re-ask once after upgrading (whether a stored key's date was synthesized is not recoverable); dated decisions do not re-ask. 15 new tests including DB-backed accept/reject carry-forward across a mocked day boundary. (bead enhancedchannelmanager-t6bin)
- **Channel logos now render when Dispatcharr is reached via docker-internal hostnames — ECM proxies logo images and serves client-resolvable URLs (bead hhmat, build 0131, GH-662).** Reported by @FractalBoy: when ECM connects to Dispatcharr via a docker-network hostname (localhost behind gluetun, or a container name), channel icons never rendered — Dispatcharr builds `cache_url` from the Host header it saw on ECM's *server-side* request, so the value shipped to the browser was often a hostname/LAN IP the operator's browser cannot resolve. New same-origin proxy endpoint `GET /api/channels/logos/{logo_id}/image` fetches the bytes server-side through ECM's authenticated Dispatcharr client and serves them from ECM's own origin (intentionally left out of the auth-exempt path list — the browser's `<img>` tag already carries the httpOnly session cookie, so no frontend change was needed for auth). Every logo response path (`GET /logos`, `GET /logos/{id}`, `POST /logos` create incl. the find-existing-by-url fallback, `POST /logos/upload`, `PATCH /logos/{id}`) now rewrites `cache_url` to point at the proxy, preserving Dispatcharr's `?v=<hash>` cache-buster so browser caching stays correct across logo replacements; a falsy `cache_url` is left alone (frontend falls back to `logo.url`) and `url` itself is never touched. The proxy forwards `If-None-Match` upstream (304 short-circuits), passes through upstream `Cache-Control`/`ETag` when present (falling back to a day-long default), and maps upstream 404 to 404 and other upstream failures to 502. 15 new tests in `backend/tests/routers/test_logos.py`; live-verified (401 unauthenticated, successful authenticated proxy fetch, rewritten `cache_url` in real API responses). (bead enhancedchannelmanager-hhmat)
- **Merge Streams "Exclude target groups" and the two normalization-group pickers now use a searchable multi-select dropdown instead of an unbounded checkbox list (bead zi85o, build 0131, GH-677).** Reported by @langtd19: the Merge Streams action's "Exclude target groups" field rendered every channel group inline with no scroll cap or search — unusable on a real instance with ~2900 groups. New shared `GroupMultiSelectDropdown` component (collapsed "N selected" button, search box, Select All/Clear All, checkbox list, outside-click and Escape close) replaces the inline list at all three sites that had it: `ActionEditor`'s Merge Streams "Exclude target groups", `RuleBuilder`'s Normalization Groups picker, and `BulkRuleSettingsModal`'s Apply-normalization-groups picker. Reuses the existing `.filter-dropdown-*` shared classes from `common.css` (the same idiom already proven in the Streams/Channels pane group filters) rather than introducing new CSS. Because this component lives inside scrollable modal bodies (unlike the toolbar filters it borrows styling from), the menu renders through a portal at a fixed position instead of in normal flow, which surfaced two portal-specific bugs fixed before shipping: the menu was painting behind the modal overlay (z-index raised to the shared `--z-topmost` token) and Escape was closing the dropdown and simultaneously triggering the parent modal's own Escape-to-close/dirty-check handler (fixed with a capture-phase listener). (bead enhancedchannelmanager-zi85o)
- **Channel Pipeline rules with a `{date+3d}`-style date token in a regex condition now save — the write-time validator was rejecting a documented, runtime-supported feature (bead qa43j, build 0128).** A user report: entering `{date+3d}` (or even the bare `{date}`) in a "Matches (Regex)" condition (`stream_name_matches`, `stream_group_matches`, `tvg_id_matches`, `channel_exists_matching`) returned a 422 `REGEX_VALIDATION_ERROR` — but `{date...}`/`{today...}` date-token expansion in these exact condition types is a documented feature (USER_GUIDE.md § "Date Expansion in Regex", contributed by @lpukatch) that `channel_pipeline_evaluator` correctly expands and runs at execution time. The gate was wrong, not the runtime: **two** independent write-time validators — `regex_lint.lint_conditions_json` (the 422 path) and `channel_pipeline_schema.Condition.validate` (a second, 400-returning gate that runs right after it in the same request) — both compiled the RAW value via `safe_regex`/`re`, and `{...}` with non-digit content is invalid quantifier syntax to the third-party `regex` library. Both gates now expand date tokens through a new shared module, `backend/date_placeholders.py` (single source of truth, also consumed by the evaluator), before compiling — validating exactly the string the evaluator will actually run, per condition type, matching the evaluator's own per-type routing. Normalization's `regex` condition type is deliberately excluded (no date-expansion feature at run time there, so a raw `{date+3d}` is genuinely invalid regex on both sides). Malformed tokens (`{date+}`, `{dat}`) and a date range wide enough to blow the 500-char pattern cap are still correctly rejected — both would also silently never match at run time (`safe_regex.search` enforces the identical length cap and returns "no match" on a compile error), so failing lint for them is consistent with runtime, not a new restriction. Frontend audited: the Rule Builder has no client-side regex-syntax check that blocks Save, so no frontend change was needed. (bead enhancedchannelmanager-qa43j)
- **Settings content pane now scrolls to top on sub-page navigation instead of preserving the previous sub-page's scroll position (bead 09x38.11, build 0117).** DOM-confirmed: leaving Notification Settings scrolled to its bottom (~2340px) and clicking Integrations landed at scrollTop 1431 of 2149 — mid-page, since the `.settings-content` pane re-renders in place and the browser preserves scroll position across the swap. New `useScrollTopReset` hook (`frontend/src/hooks/useScrollTopReset.ts`) resets the pane's `scrollTop` to 0 whenever `activePage` changes (skips the initial mount, so deep-linking to a sub-page via hash routing is unaffected). The reset is an instant assignment, not an animated scroll, so it needs no `prefers-reduced-motion` branch — no motion is introduced either way. (bead enhancedchannelmanager-09x38.11)
- **Form modals with no way to back out without saving now have a Cancel button — 8 modals fixed, plus a static-analysis regression guard (bead 09x38.3, build 0116).** UX audit found two confirmed instances (Add/Edit M3U Account, Add Logo) where the modal footer had ONLY the mutating primary button — no Cancel/secondary — violating the documented Cancel+Primary pattern (`docs/css_guidelines.md` → Modal Patterns); the header X-close was the sole escape hatch. Two independent misses pointed at a component-level gap rather than one-off accidents, so a full audit of every `.modal-footer` in the app followed. **Fixed (8):** `M3UAccountModal`, `LogoModal`, `BulkLCNFetchModal`, `DeleteOrphanedGroupsModal`, `NormalizeNamesModal`, `TaskEditorModal`, `M3ULinkedAccountsModal`, `BulkEPGAssignModal` — each gains a `modal-btn modal-btn-secondary` Cancel that closes without calling its save/assign/delete API. **Deliberately NOT touched:** `M3UGroupsModal` (PR #681's window.confirm dirty-guard already covers unsaved-changes on close — a bare Cancel would bypass it) and `EditChannelModal` (its own hasChanges + "Discard Changes" confirm dialog guards every close path the same way) — a plain Cancel next to an existing dirty-guard is a regression, not a fix. **Enforcement:** no shared modal-footer component exists in this codebase (every modal hand-assembles its own footer JSX against `ModalBase.css`'s shared classes), and introducing one now would mean refactoring ~50 call sites for a one-line invariant — disproportionate to the defect. Instead, a new static-analysis test (`frontend/src/uxAudits/formModalCancelButton.audit.test.ts`, modeled on the existing `iconOnlyButtons.a11y.test.ts` pattern) walks every component `.tsx` file and fails if a `.modal-footer` contains a mutating (primary/danger) button with no secondary/Cancel button beside it, with a documented, justification-required allowlist for the two dirty-guarded modals and one genuinely non-mutating info view (Settings' Probe Results panel). Self-verified: reverting one fix reproduces the failure the test is meant to catch. (bead enhancedchannelmanager-09x38.3)
- **Header/toolbar rows now have a single defined overflow behavior — no more squeezed titles or clipped buttons (bead 09x38.2, build 0115).** Header rows across the app were sized with no overflow strategy, so a wide-enough toolbar either squeezed the title column to min-content (M3U Manager's title wrapped to two lines and the subtitle one word per line at 1280px — the operator hit this in a shipped tutorial screenshot) or overflowed an `overflow:hidden` ancestor and clipped a button out of reach (Guide's "Print Guide" was fully invisible/unreachable below ~1400px; Stats' Live Stats "Refresh" was clipped already at 1440px). **One reusable idiom, applied everywhere: header rows wrap** (`flex-wrap: wrap` + `row-gap`) so the action cluster drops to a second row instead of fighting the title — matching the already-well-behaved Channel Pipeline header. This is baked into the shared `.page-header`/`PageHeader`, `.tab-header`, and `.header-actions` classes (every consumer gets it free) and applied to the bespoke Guide, Stats, and Normalization Rules Engine headers. **On top of that, M3U Manager's 7-button toolbar collapses its four setup/admin actions** (Server Groups, Stream Profiles, Manage Links, Sync Groups) **into a new shared `OverflowMenu` kebab**, keeping Refresh All + Add M3U Account as primary buttons; the title now renders un-wrapped at 1280px. Journal and M3U Changes headers adopted the same wrap idiom for consistency. Live-verified at 1280/1366/1440. Policy documented in `docs/css_guidelines.md` § "Header / toolbar overflow policy". (bead enhancedchannelmanager-09x38.2)
- **Manage Groups modal no longer silently discards unsaved toggles on X-close/Escape (bead 09x38.7, build 0114).** Confirmed live: toggling a group's Enabled/Auto-Sync in M3U Manager → Manage Groups tracks dirty state (it enables the "Save & Refresh" button), but the modal has no Cancel button — closing via the X button or Escape discarded every pending toggle instantly with zero confirmation, and stakes are higher since bead igqcy made Save & Refresh a real multi-field write that operators plausibly batch several toggles before saving. Both close paths now route through a shared handler that prompts "Discard unsaved changes?" whenever dirty state is set; declining leaves the modal open with in-progress toggles intact, and a clean close (no changes) remains instant with no prompt. Outside-click close does not exist for this modal (`ModalOverlay` disables backdrop-click-to-close unless a caller opts in, and this modal never does), so there is no third path to cover. Save & Refresh is unchanged — it never routes through the new guard. (bead enhancedchannelmanager-09x38.7)
- **Auto-sync group settings no longer clobber Dispatcharr's native config; group saves are now Save & Refresh; MCP group tools work again on Dispatcharr v0.24+ (bead igqcy, build 0111).** Dispatcharr v0.25.0+ made `update_group_settings` a full-row upsert — every field omitted from a row is silently reset to defaults — and a live QA pass against v0.27.2 (bead 478fe) proved an enabled-only ECM write wiped a natively-configured group's `auto_channel_sync` (true→false) and start channel (1.0→null). Three fixes, one surface: **(1) Read-modify-write everywhere.** The backend group-settings endpoint (the choke point for both modals, Sync Groups, linked-account cascade, MCP, and direct API callers) now completes every incoming row from the group's current stored state via a shared `merge_group_settings_row` helper before forwarding — omitted fields (including `auto_sync_channel_end`, new in v0.25.0 and never previously sent by ECM) are preserved, explicit nulls still clear. The Event Sync guided toggle uses the same helper. The M3U Groups modal sends complete rows (now incl. `auto_sync_channel_end` and full linked-cascade rows), and the per-group Auto-Sync Settings modal merges `custom_properties` over the group's current dict instead of rebuilding it from an ECM-known-keys whitelist — unknown keys Dispatcharr's sync consumes (`channel_numbering_mode`, `channel_numbering_fallback`, `name_match_exclude_regex`, `force_dummy_epg`, …) survive every save verbatim. **(2) Save & Refresh (Dispatcharr parity).** Dispatcharr's own groups modal only offers "Save and Refresh", so ECM saves used to look inert by comparison. The Manage Groups save button is now "Save & Refresh": after a successful settings save it starts an M3U refresh of the account (and any linked accounts it cascaded to), with a toast confirming the refresh started; a save failure never triggers a refresh. The backend endpoint's semantics are unchanged (no implicit refresh — parity with Dispatcharr's API), and the MCP tools gain an optional `trigger_refresh` flag (default false, response notes the change waits for the next refresh). **(3) MCP group name resolution.** Dispatcharr's serializer stopped providing `name` on `channel_groups[]` in v0.24.0, so `update_m3u_group_settings` / `bulk_update_m3u_group_settings` returned "group not found" for every call; names are now resolved by joining `channel_group` ids against the channel-groups list (pre-v0.24 embedded names still short-circuit the extra fetch), and both tools send complete rows. Regression tests recorded from the real v0.27.2 payload shapes pin all of it (backend 11, MCP 8, frontend 7 across both modals). Live-verified end-to-end against Dispatcharr v0.27.2: a native full row (auto-sync on, start 1, end 500, unknown custom_properties keys) survived an enabled-only API save, the guided toggle, and a UI Save & Refresh untouched; the chained refresh ran with zero channels created; systems restored to snapshot. (bead enhancedchannelmanager-igqcy)
- **Entrypoint hardening: the numeric env expansions on the uvicorn exec line are now quoted and digit-validated, failing closed to their defaults (bead 1xoiq, build 0110).** Follow-up from the wadu3 code review and the new style-guide Shell Scripting rule (build 0109): `ECM_PORT`, `ECM_LIMIT_CONCURRENCY`, and `ECM_TIMEOUT_KEEP_ALIVE` were expanded unquoted on `backend/entrypoint.sh`'s `exec gosu appuser uvicorn` line — the same word-splitting/glob-expansion class as the `ECM_UVICORN_LOOP` blocker fixed in wadu3 (a crafted value containing spaces could smuggle extra uvicorn flags into the argv; a `*` could pathname-expand against `/app`). All three expansions are now double-quoted, and each value is whitelist-validated to digits before the exec: an invalid (non-numeric) value logs a warning naming the rejected value and falls back to the default (6100 / 100 / 30) instead of exiting — under `set -e` + `exec`, an exit would crash-loop the container with the same bad env var set on every restart. Valid overrides and the defaults behave exactly as before; empty values still silently take the defaults. The wadu3 behavioral shell-test harness (runs the real launch tail of `entrypoint.sh` in a POSIX shell and asserts the exact argv uvicorn receives) now also covers the three numeric args: valid pass-through, non-numeric fallback + warning, empty values, flag-injection word-split attempts, glob non-expansion, and text-level quoting guards. Container-verified: clean startup, `--port 6100 --loop asyncio --limit-concurrency 100 --timeout-keep-alive 30` on the live argv, `/api/health` 200. Also clears the last three shellcheck SC2086 findings in the script. (bead enhancedchannelmanager-1xoiq)
- **Event Sync: stale dateless stream names no longer auto-attach to today's masters under `assume_current_date` — new snapshot-backed staleness guard routes them to the review queue (bead jqwfq, build 0107).** Field report (follow-up to the yjchp bundle): a provider left YESTERDAY's dateless slot name in the M3U; `assume_current_date` synthesized TODAY's date at that clock time, so identical title + tiny time delta scored 1.0 and attached the stale stream to the wrong master — undecidable from the name alone. The missing fact — *was this exact name already in the playlist before today?* — is now answered from the M3U change monitor's `M3USnapshot` per-group stream-name lists (Dispatcharr's per-stream `updated_at`/`last_seen` are deliberately NOT used: they bump for every stream on every refresh). **Stage 1 (observability):** every preview/debug-bundle stream row carries a tri-state `name_seen_before_today` (true = present in the account's latest pre-local-midnight snapshot, false = captured-uncapped and absent, null = unknown), with `stale_suspect_streams` / `freshness_unknown_streams` summary counts. **Stage 2 (behavior):** a threshold would-attach is DEMOTED to the review queue (`ambiguous_reason: "stale_dateless_stream_name"`, mirroring the yjchp venue-conflict rail) only when the name is POSITIVELY stale AND `assume_current_date` was load-bearing (counterfactual re-parse with the flag off yields no start) — dated names are never touched, and the signal FAILS OPEN (a missing snapshot, uncaptured group, or a group at the 500-name capture cap can only cause missed detections, never false demotes). A prior review-queue ACCEPT outranks the guard (standard accept-upgrade path); a prior REJECT still suppresses. Per-rule knob `demote_stale_dateless` (default **true**, absent-key default-fill so stored configs need nothing; editor: "Send stale dateless names to review instead of attaching" under Date handling) is the escape hatch for genuinely recurring daily events. Live-validated against a real instance: 41/41 provider names correctly flagged as predating today, 5/5 spot-checks byte-present in the qualifying snapshots. (bead enhancedchannelmanager-jqwfq)
- **Event Sync: four fixes from a user debug bundle — venue-conflict matcher rail, cross-rule pre-flight advice, Journal UI category, and bundle auto_run/pre-flight export (bead yjchp, build 0105).** (1) *Matcher venue-conflict rail:* a teamless pair whose titles BOTH carry unmatched identity tokens after the shared cleaning/bridging pipeline ('Lucas Oil Late Models **Adams** County' vs 'Lucas Oil Late Models at **Shelby** County' — different venue, different event) scored token_set 0.9032, cleared the 0.90 no-teams floor, and auto-attached. Such pairs are now DEMOTED from the attach band to the review queue (never auto-attached, never hard-rejected — a true match stays operator-rescuable), with a machine-readable `venue_token_conflict` marker surfaced through the candidate's reject_reason and the stream's `ambiguous_reason`. One-sided leftovers (master series/sponsor dressing, 'at', a year token, 'Co.'-style abbreviations, truncation) still attach; team-verdict AGREE pairs and operator-lowered sub-0.80 thresholds bypass the rail (manual-control contract). Seven pairs appended to the frozen corpus. (2) *Cross-rule pre-flight message:* when a rule's secondary group fails `secondary_auto_sync_off` because that group is the MASTER of another enabled event_sync rule (masters require auto-sync ON), the failure now names the conflicting rule and advises removing the group from this rule's secondaries (or re-mastering the other rule) instead of the stock "disable auto_channel_sync" advice that would have broken the other rule; failure gains a `conflicting_rule` field, check id unchanged. (3) *Journal UI:* the journal has recorded `event_sync` entries (attaches, review accepts/rejects) since Phase 1B, but the frontend was blind to the category — it now has its own filter option, icon, label, and header stat. (4) *Debug bundle:* `event_sync_matching.json` per-rule entries now export `matching_controls.auto_run` and a per-rule `preflight` result, so "why didn't this rule fire on refresh" is diagnosable from a bundle alone. (bead enhancedchannelmanager-yjchp)
- **Dev-tooling: the agent-side version-advance guard no longer false-blocks `gh pr create` when shipping from a git worktree (bead yxtuz, build 0106).** The `.claude/hooks/version-advance-guard.sh` PreToolUse hook always validated version state against `CLAUDE_PROJECT_DIR` — the MAIN checkout — even when the `gh pr create` being validated ran from a git worktree on a different, further-advanced branch (`cd <worktree> && gh pr create ...`). That checked the wrong checkout and could false-block a legitimately-advanced worktree branch, as happened shipping PR #666 (worked around at the time via `gh api`; CI's server-side version-consistency job re-enforces the same rule against the actual PR head, so it was a friction bug, not a safety gap). The hook now resolves the checkout root to validate from the command being inspected — a leading `cd <path> &&` or a `git -C <path>` in the `gh pr create` command — with an `ECM_VERSION_GUARD_ROOT` env override for cases the command text can't express. The plain main-checkout path (no `cd`, no `-C`, no override) is unchanged. (bead enhancedchannelmanager-yxtuz)
- **Merge rules no longer report a same-named in-scope channel as "not found" when the name exists in several groups (bead g0uuf, build 0103).** A user debug bundle surfaced the failure: five channels all literally named "ESPN", one per group — a supported layout, since GH-92 group scoping exists precisely so different rules can keep same-named channels in different groups. But every executor lookup index (by-name, base-name, normalized, core, fold) held exactly ONE channel per key, so a group-scoped `create_channel` lookup could only test one arbitrary survivor (whichever loaded last for by-name, first for the rest). When that survivor sat in another group, the lookup returned "not found" even though an exact-name match existed in the scoped group — `if_exists: merge_only` skipped every stream ("Channel 'ESPN' not found, skipped (merge only)") and `if_exists: merge` created a duplicate channel in the target group. Normalization was a red herring: the miss happened after normalization, in the index. Each lookup stage now keeps its legacy single-slot pick (historical winner semantics are unchanged whenever that pick passes the scope/manual gates) and, only when it fails those gates, scans every channel indexed under the same key — so scoped lookups find the twin IN the scoped group, unscoped lookups behave exactly as before, and the orzck manual-channel gate plus the wy6l5 blocked-manual breadcrumb keep applying to scanned candidates. Covers rename, live-create, and dry-run-create index updates within a run. (bead enhancedchannelmanager-g0uuf)
- **Pagination bounds validation extended to 8 sibling list endpoints — 422 instead of erroring or silently clamping (bead g4z2h, build 0104, systemic sibling of 1a5mf/PR-660).** `1a5mf` (build 0095) fixed only `GET /api/channels`; the same unvalidated `page: int = 1, page_size: int = 100` pattern existed on 8 more list endpoints, where `page < 1` either passed straight to the upstream Dispatcharr client (surfacing as a 500) or produced an invalid SQL OFFSET/LIMIT on DB-backed endpoints. All 8 now use FastAPI `Query(ge=1)` / `Query(ge=1, le=<ceiling>)` validation, mirroring the 1a5mf idiom, with ceilings chosen from each endpoint's real caller evidence (frontend/MCP) so no legitimate request is broken: `GET /api/channels/logos` (le=10000, matches App.tsx's `getAllLogos()`), `GET /api/streams` (le=1000, App.tsx requests up to 500), `GET /api/journal` (le=250, matches the Journal tab UI's largest page-size option — replaces a silent `min(page_size,200)` clamp that would have quietly truncated that option), `GET /api/m3u/changes` and `GET /api/m3u/accounts/{id}/changes` (le=200), `GET /api/notifications` (le=100), `GET /api/epg/data` (le=1000), and `GET /api/stats/watch-history` (le=100, replaces a silent `min(page_size,100)` clamp). `channel_merges.py`, `event_sync_reviews.py`, and `channel_pipeline.py` list endpoints were confirmed already validated (manual 400 checks or existing `Query` bounds) and needed no change. (bead enhancedchannelmanager-g4z2h)
- **Certificate expiry-days / renewal check no longer skewed by host timezone (bead wccvo, build 0102, follow-up to n5zw2).** `TLSSettings.get_expiry_days()` (and `needs_renewal()`, which calls it) compared `cert_expires_at` — stored as naive UTC — against `datetime.now()`, which is naive *local* time. On a host at a nonzero UTC offset this skewed the reported days-until-expiry and could trip (or miss) the renewal threshold by up to the host's offset. Same bug class as bead n5zw2 (`tls/storage.py`, PR #660) but in a different module that fix didn't cover. Mirrors n5zw2's fix exactly: a `_utcnow_naive()` helper (`datetime.now(timezone.utc)` dropped to naive) replaces the raw `datetime.now()` comparison, keeping the module on the naive-UTC convention. (bead enhancedchannelmanager-wccvo)
- **uvicorn now pins the stdlib `asyncio` event loop instead of `uvloop` (bead wadu3, build 0101, follow-up to GH #546).** `uvicorn[standard]` auto-selects `uvloop` when no `--loop` flag is given, at all three launch points (production `entrypoint.sh`, the HTTPS subprocess in `tls/https_server.py`, and the dev `__main__` run in `main.py`). Upstream `uvloop` 0.22.1 (latest as of 2026-07-16 — no fixed release exists) has two open-risk issues: a segfault under FastAPI-in-container (uvloop#706, still open) and responses leaking to the wrong request under load (uvloop#645, a data-exposure bug fixed on `master` but never released). Both are now eliminated by defaulting every launch point to `--loop asyncio`, with a new `ECM_UVICORN_LOOP` env var as a whitelisted (`auto`/`asyncio`/`uvloop`) escape hatch back to `uvloop` if ever needed — invalid values fall back to `asyncio` with a warning. The active loop implementation is logged at startup for auditability from `docker logs`. Load-tested (60-way concurrent bursts, 7,200 measured requests across both loops): `asyncio` measured statistically indistinguishable throughput from `uvloop` (~1.7% delta, within run-to-run noise) with zero errors and zero response-shape mismatches — SQLite and handler logic dominate latency, not the loop. The GH #546 `BaseException` containment middleware was re-verified identical on both loops. (bead enhancedchannelmanager-wadu3)
- **Auto-creation no longer multiplies channels whose names differ only by spacing/case — via a new opt-in per-rule flag (bead 0vao3, build 0100, refs GH #645).** A reporter's `if_exists: merge` rule produced four channels for `eurosport 2` / `Eurosport 2` / `Eurosport2` / `eurosport2`: the merge lookup compared names case-insensitively but never collapsed whitespace, and normalization rules never remove single interior spaces. New rule option **"Ignore spacing & case differences when matching"** (`fold_match_key`, default OFF — existing installs unchanged): when ON, the create-channel merge lookup falls back to a canonical comparison key (casefold + strip ALL whitespace) so spelling variants merge into one channel. Comparison key only — the stored/visible channel name is never rewritten, and exact matches, group scoping, and manual-channel protection all still win over the fold. **Find Duplicates** gains a matching opt-in **"Ignore spacing differences"** toggle using the same shared canonicalization (`backend/match_fold.py`), so existing spelling-variant duplicates group together for one bulk merge. Caveat (why it's opt-in): genuinely distinct channels whose names differ only by spacing/case merge too — including digit runs ("Canal 5 2" matches "Canal 52"); the rule editor and docs spell this out, and a pinning test documents the digit collision as the fold's contract. Round-trips through rules YAML export/import and DBAS backup; Alembic 0034. Also bundles regression tests pinning that `channel_number: auto` intentionally skips the rule-level renumber pass (bead bn0wa). GH #645 stays open pending reporter confirmation. (bead enhancedchannelmanager-0vao3)
- **Rule Builder now warns when "Channel Sort" won't actually renumber channels (bead bn0wa, build 0099, refs GH #644).** A reporter set `sort_field` expecting alphabetical channel numbers and got silent no-op renumbering instead — `sort_field` only orders stream *processing*, and the rule-level renumber it can trigger is skipped entirely when the rule's `create_channel` action leaves Channel Number at the `auto` sentinel (the default). The Channel Sort field hint was rewritten to state this precisely, and a conditional warning now appears in the Rule Builder whenever a sort is configured alongside an auto-numbered Create Channel action, pointing the operator at a `sort_group` action (always renumbers) or a fixed number range as the actual fix. New user-guide page `docs/user_guide/channel-pipeline/sort-vs-numbering.md` walks through `sort_field` vs. `sort_group`, the `auto`-numbering gotcha, and a worked PL(1)/US(400) example matching the reporter's exact scenario. GH #644 stays open pending reporter confirmation. (bead enhancedchannelmanager-bn0wa)
- **Event Sync: apostrophe-family and underscore in event names no longer strand matches in the review queue (bead 79k6b, build 0098).** Two teamless events (e.g. a Flo Racing "HLR Joker\`s Jackpot" using a backtick as its apostrophe, and "Weekly Racing at Utica_Rome" using an underscore) matched their stream counterparts at only 0.84 / 0.70 and landed in the ambiguous band instead of auto-attaching. The LOCALS name cleaner now fuses the whole apostrophe family (`'` `’` `ʼ` `` ` `` `´`) to nothing and splits underscore to a space, so `Joker\`s` reads as `jokers` and `Utica_Rome` as two tokens — both now reach a full match and attach. The team-token layer fuses the same apostrophe family for consistency. Strict-mode (dedup/exact) name normalization is byte-for-byte unchanged. (bead enhancedchannelmanager-79k6b)
- **Silent ExitCode-0 backend crash contained; [EXIT-DIAG] diagnostics added (bead xzdx9, build 0097, refs GH #546).** A reporter saw the backend die mid-request with no traceback, no faulthandler output, and no uvicorn shutdown sequence — `docker inspect` showed `ExitCode=0`. Verified mechanism on this stack (uvicorn + uvloop + Starlette `BaseHTTPMiddleware`): a `SystemExit`/`KeyboardInterrupt`-class `BaseException` raised while handling a request propagates out of `asyncio.Task.__step` **into the event loop itself**, aborting `uvicorn.serve()` with no shutdown sequence — a bare `SystemExit` then exits the interpreter with status 0 and no traceback. A new `exit_diagnostics.BaseExceptionContainmentMiddleware`, registered as the innermost user middleware (runs inside the same asyncio task as route handlers, before `Task.__step` can re-raise), catches such BaseExceptions, logs a `[EXIT-DIAG]` CRITICAL line with the full traceback, and converts them to an ordinary `RuntimeError` so the request surfaces as a normal logged 500 instead of killing the process. `Exception` subclasses, `asyncio.CancelledError`, and `GeneratorExit` pass through untouched; auth semantics are unaffected (the guard sits inside `auth_middleware`). Verified live: guarded probe survives repeated injected `SystemExit`s (500 each, `[EXIT-DIAG]` logged); burst concurrency testing (60×3 and 50×60 concurrent cookie-authenticated requests) stayed healthy throughout. The original raiser of the `SystemExit` in the reporter's environment remains unidentified — this fix contains the blast radius and gives the next occurrence a full traceback instead of a silent death; GH #546 stays open pending reporter confirmation. (bead enhancedchannelmanager-xzdx9)
- **Guide tab stayed stuck on "Loading guide data…" when opened directly (bead ogm9v, build 0096).** Navigating straight to the Guide tab — without visiting Channel Manager first — could leave it loading forever. The tab's data-load effect depended on the channel/logo lists, which the app streams in progressively (paginated); every update re-ran the whole effect, re-fetching the full EPG grid and resetting the loading state, so in large channel fleets it never settled. Guide-specific data (EPG grid, profiles, groups) now loads once, and channels/logos sync in from the app without re-fetching. (bead enhancedchannelmanager-ogm9v)
- **Three bugs surfaced by the 2026-07 test audit (beads 9rjgr / n5zw2 / 1a5mf, build 0095).** (1) Creating and deleting the same not-yet-committed (temp) channel within one batch now cancels out **regardless of order** — previously a delete-before-create left a dangling delete referencing a channel that was never created, which errored at execution (9rjgr). (2) Certificate expiry/validity checks (`is_expired` / `is_not_yet_valid` / `days_until_expiry`) now compare consistently in UTC instead of mixing naive-local and naive-UTC time, fixing an up-to-your-UTC-offset skew in expiry math (n5zw2). (3) `GET /api/channels` with `page`/`page_size` out of range now returns a 422 validation error instead of a 500 (1a5mf). (beads enhancedchannelmanager-9rjgr / n5zw2 / 1a5mf)

### Changed
- **M3U Manager's per-account "Manage Profiles" disambiguated from Channel Manager's "Manage Profiles" — both now use the vocabulary Settings already established (bead 09x38.6, build 0118).** UX pass 1 found the two features shared a label despite doing unrelated things: `M3UProfileModal` manages per-M3U-account connection-tier profiles (`max_streams`, optional stream rename pattern — type `M3UAccountProfile`), while `ChannelProfilesListModal` manages channel-visibility profiles. Settings (Channel Defaults page, Backup & Restore export list, MCP tool inventory) already uses "Stream Profiles" and "Channel Profiles" as two distinct, established terms — but "Stream Profiles" was already taken by a third, unrelated concept living in the same M3U Manager tab (the ffmpeg/transcode command profiles behind `StreamProfilesListModal`'s kebab-menu entry), so reusing it for the per-account modal would have created a fresh collision instead of fixing one. Chose **"Account Profiles"** for the per-account feature instead — it matches the underlying `M3UAccountProfile` type name exactly and doesn't collide with either existing term. Label-only change, no behavior change: M3U Manager's per-account row-action button title/aria-label goes from "Manage Profiles" to "Manage Account Profiles" (`M3UManagerTab.tsx`, preserving the row's existing "Manage Groups" / "Manage Filters" verb+noun pattern), its modal header goes from "Manage Profiles - {account}" to "Account Profiles - {account}" (`M3UProfileModal.tsx`), and Channel Manager's overflow-menu entry goes from bare "Manage Profiles" to "Channel Profiles" (`ChannelsPane.tsx`), matching `ChannelProfilesListModal`'s own modal title. No other user-facing "Manage Profiles" occurrences found in the frontend or `docs/user_guide/`. (bead enhancedchannelmanager-09x38.6)
- **Per-tab tutorial template + `docs/user_guide/` authoring-convention update — prerequisite for the per-tab tutorial epic (bead enhancedchannelmanager-gsnw0.1, build 0112).** No functional change — this is documentation only. `docs/user_guide/README.md` gains: (1) a literal, copyable "Common tasks" tutorial skeleton — title, a one-sentence audience blockquote, a 2-sentence-max "what this tab is for," numbered goal→steps→**Result** walkthroughs, and a "Going deeper" cross-link section — distilled from the existing `backup-restore/` ("Start here" task-router) and `integrations/` (goal-first setup + explicit end state) sections; (2) a formalized, repo-wide screenshot spec — 1280×720 viewport (matching the existing pin in `e2e/visual-regression.spec.ts`), dark theme, numbered kebab-case `<n>-<short-name>.png` filenames under `docs/images/user_guide/<section>/`, and mandatory inline placement immediately after the step illustrated, never an end-of-article dump — formalizing the pattern already used by `docs/images/event_sync/` (`docs/images/normalization/`'s two pre-existing images are grandfathered, not recaptured); and (3) a capture-mechanics note (Playwright against the dev container, representative seeded data, captured in the same PR as the tutorial bead that needs it). `docs/user_guide/index.md` gains a "By tab" navigation table — one row per UI tab in nav order, linking to the section that owns (or will own) that tab's tutorials, with the net-new sections (`m3u-manager/`, `guide/`, `m3u-changes/`, `logo-manager/`, `journal/`, `settings/`) marked **Planned**. Blocks the 15 remaining per-tab tutorial child beads (gsnw0.2–.16). (bead enhancedchannelmanager-gsnw0.1)
- **Backend test suite self-explains the cryptography-42 venv requirement instead of failing confusingly (bead enhancedchannelmanager-vol5d, build 0109).** No user-facing behavior change. Two engineers independently lost time to 7-9 failures in `backend/tests/unit/test_tls_storage.py` when running the backend suite under a bare system `python3` — its `cryptography` (41.0.7) predates the `x509.Certificate.not_valid_before_utc`/`not_valid_after_utc` properties (added in cryptography 42) that `tls/storage.py`'s `parse_certificate()` reads, and the code's broad exception handling turned the resulting `AttributeError` into confusing assertion failures (wrong subject, wrong validity) rather than an obvious error naming the cause. Fixed both ends: `docs/testing.md` gains a prominent note at the top of the backend-testing section directing agents to `.venv/bin/python -m pytest`; the 9 affected test methods (exactly the ones whose assertions require a genuinely-parsed certificate — directory perms, raw file round-trips, ACME JSON persistence, and the already-invalid/malformed/mismatched/timezone-skew paths are unaffected and still run under any cryptography version) get a `pytest.mark.skipif` gated on a dependency-free `cryptography.__version__` parse, with a reason string naming the fix. Zero new skips under the project venv (cryptography 42+): all 37 tests in the file still pass. (bead enhancedchannelmanager-vol5d)
- **Style guide gains shell-scripting quoting rules and a `docs/user_guide/` kebab-case filename exception (beads enhancedchannelmanager-0zorv / y8vt4, build 0109).** No user-facing behavior change. New Shell Scripting section in `docs/style_guide.md` codifies quoting parameter expansions that flow into exec argv (a class of bug that has appeared twice in `backend/entrypoint.sh`), whitelisting env vars that select behavior (the `ECM_UVICORN_LOOP` case-statement pattern), and failing closed with a warning instead of crash-looping. The Filenames section now documents the existing `docs/user_guide/` kebab-case exception to the general `snake_case.md` rule, matching that subtree's own README. (beads enhancedchannelmanager-0zorv / enhancedchannelmanager-y8vt4)
- **Test-suite hardening from the 2026-07 full-suite audit — batch 2 (beads u8qr6.2/.4/.5/.7, build 0094).** No user-facing behavior change. Tightened weak/smoke assertions that accepted failure (e.g. `status in (200, 500)`) as a pass — including a stream-preview test that was silently vacuous (its payload was SSRF-rejected before the code under test ran); added real behavioral coverage for previously-untested production paths (5 `ConditionEvaluator` condition types, `StruckStreamCleanupTask` error handler + pagination loop, `validate_alert_sources` branches); pinned forwarded args / side-effects in mock-wiring tests (digest `since`/account filter, alert `reload_method`); and added missing-direction regression tests. The reverse-order create+delete test surfaced a real (pre-existing) consolidator bug — captured as a strict-`xfail` linked to bead 9rjgr so it flips to a hard failure the moment the bug is fixed. (beads enhancedchannelmanager-u8qr6.2/.4/.5/.7)
- **Test-suite hardening from the 2026-07 full-suite audit — batch 1 (beads u8qr6.1/.3/.6, build 0093).** No user-facing behavior change. (1) The M3U-digest exclude-filter logic was extracted from `M3UDigestTask.execute()` into a module-level `apply_exclude_filters()` (behavior-preserving) so its tests exercise the **real** production path incl. the `safe_regex` ReDoS/oversize contract — the previous tests ran a hand-copied mirror that used bare `re` and never touched `safe_regex`. (2) Added a real `CertificateStorage` test suite (`tls/storage.py`) covering private-key/cert save/load/parse/validate-pair/delete/ACME persistence, including mismatched-pair rejection and on-disk permission bits (14 previously-untested methods handling key material). (3) De-flaked wall-clock timing assertions (bcrypt work-factor parsed from the hash; event-loop non-blocking checked by in-flight/completion-order; a microbench moved behind `@pytest.mark.slow`). (beads enhancedchannelmanager-u8qr6.1/.3/.6)

### Added
- **Event Sync: preferred-provider stream ordering on master channels via per-rule stream sort (bead io0tv, build 0108).** Event Sync attaches always appended, so the master's own Dispatcharr-synced stream stayed first and secondary-provider streams landed at the bottom — with no way to float a preferred provider's feed to the top (the stream that PLAYS). The rule's existing `stream_sort_field`/`stream_sort_order` columns — previously dead for event_sync rules (the engine's Pass 3.5 reorder never saw event_sync-touched masters) — are now live: the attach phase registers each rule's touched master channels for the Pass 3.5 stream reorder (including idempotent already-attached no-ops, so ordering re-heals on EVERY run — a provider-priority change applies on the next run without detaching anything) and extends the reorder's provider map from the event_sync fetch plus one bulk stream lookup for the masters' own streams, so `provider_order` scoring sees every stream on the channel. Rule editor: new **Behavior → Stream order** subgroup (Provider Order / Quality / Stream Name / Smart Sort; default "No sorting" — existing rules keep append-only behavior unchanged). Recipe (set M3U Manager priorities + rule `provider_order` desc) documented in `docs/event_sync.md`, including the dummy-EPG note: ordering changes which stream plays, not the guide text. Also pins the previously-untested skipped-merge → Pass 3.5 registration side effect on the standard-rule path. Live-verified: a two-provider master reordered `[native, P2, P3]` → `[P3(pri 90), P2(pri 80), native(pri 10)]`, and a fully no-op re-run still evaluated ordering ("already sorted … skipping"). (bead enhancedchannelmanager-io0tv)
- **MCP `list_tasks` now reports whether a task will actually run, not just its toggle (bead vkktd.5, build 0092).** Following the vkktd effective-enabled work, the `list_tasks` MCP tool surfaced only the parent task toggle, so an AI agent saw a gated-off task (task enabled but no active schedule) as plain "enabled" — the same "reads Enabled but won't fire" trap the rest of vkktd removed for the UI/API. It now renders three states from `effective_enabled`: **enabled**, **"enabled but WON'T RUN (no active schedule)"**, and **disabled** (with a safe fallback to the task toggle for older backend payloads). Closes the agent-facing half of the trap. (bead enhancedchannelmanager-vkktd.5)
- **Scheduled tasks that read "Enabled" but never fire are now visible and self-correcting (epic vkktd, build 0091).** A scheduled task only fires when BOTH its task-level toggle AND at least one of its schedules are enabled; opt-in tasks (e.g. Auto Channel Creation) ship with the task toggle off. Previously, when a task was gated off the scheduler skipped it **silently** (no log), the debug bundle reported the always-on child schedule as `enabled: true` (masking the real state), and turning the task "Enabled" only flipped the task gate — so if a schedule was disabled, the task read Enabled yet never ran, with no feedback. This surfaced as "matching isn't running on M3U refresh." Now: **(observability)** the scheduler logs a throttled reason when a due task is skipped because its parent is disabled or unregistered, and logs (throttled INFO) when a refresh watermark is pending but suppressed (no enabled run-on-refresh rule); the M3U-refresh watermark line no longer claims auto-creation "will pick it up" when it's gated off. **(diagnosability)** the debug bundle now reports `effective_enabled = parent AND ≥1 enabled schedule`, and `GET /api/tasks` exposes `effective_enabled`, so a gated-off task can no longer read a bare "enabled". **(self-correction)** enabling a task now auto-reconciles its schedule — it enables the task's schedule (the single one, or the most-recently-created when several exist) so "Enabled" actually runs; MANUAL tasks are never reconciled and disabling never touches schedules. Frontend surfacing of the new `effective_enabled` state is tracked as a follow-up. (epic enhancedchannelmanager-vkktd — beads vkktd.1/.2/.3)
- **Timezone-aware EPG matching — West channels prefer their (Pacific) guide row (bead vznut.2, build 0090).** The EPG matcher was timezone-*blind*: it stripped EAST/WEST when building the match key, and "Pacific" wasn't recognized as a West synonym, so a "…West" channel scored the East entry higher than its correct "(Pacific)" guide row (the root cause behind the shared-EPG-link field reports). Matching is now timezone-*aware*: region words (West≡Pacific, plus Central/Mountain) collapse into the match key so the regional entry becomes an exact candidate, region parentheticals/tokens are neutralized so the Pacific row stays token-symmetric with its base sibling, and a region-consistency tie-break prefers the region that matches the channel (West channel → West/Pacific row; East → East). Region is detected from the tvg_id parenthetical then the trailing name word, skipping quality/format tokens ("AMC West HD"→West) and bare trailing digits; adjective forms ("Eastern"/"Western") are guarded out. Band-preserving — it only re-orders genuine ties and never outranks country/exact matches, and is inert for non-regional channels. Scope: `match_channels_epg` only (auto-creation's separate matcher tracked as follow-up vznut.4). (bead enhancedchannelmanager-vznut.2)
- **Shared EPG-link audit — find channels silently pointing at the same EPG row (bead vznut.1, build 0089).** A read-only audit surfaces every set of channels that share one `epg_data_id` — the fingerprint of the "West feed silently showing East's schedule" bug, where a regional channel is linked to its counterpart's guide row. It appears two ways: a **"shared EPG links"** summary embedded in the EPG match preview (scoped to the channels in the current match set), and a whole-fleet check via the new `GET /api/epg/audit-duplicates` endpoint / `audit_epg_duplicates` MCP tool. Each reported group names the shared EPG row (id, name, `tvg_id`), the channels sharing it, and the count; NULL/unlinked channels are excluded and output is deterministic. Read-only — it reports, it never re-links. See `docs/user_guide/epg/finding-mislinked-channels.md`. (bead enhancedchannelmanager-vznut.1)
- **Build-number ADVANCE gate — CI hard-check + Claude Code agent ship hook (bead w9irb, build 0086).** The `version-consistency` CI job previously proved the three version touchpoints MATCH but not that the build number ADVANCED, so ~9 source PRs shipped to dev at `0.17.6-0084` with no bump, undetected. A new shared checker (`scripts/check_version_advances.py`) parses the `frontend/package.json` build number and fails if it is not strictly greater than the base branch's (`origin/dev`), suggesting the next number in the failure message. It is wired into CI as a required additive step on PRs to `dev` (docs-only PRs stay exempt via the existing `docs-only-pass.yml` classification; release-cut PRs to `main` are exempt because the version legitimately drops its `-BUILD` suffix), and into a checked-in Claude Code `PreToolUse` hook (`.claude/settings.json` + `.claude/hooks/version-advance-guard.sh`) that blocks a `gh pr create` ship when the local build did not advance. A `CHANGELOG.md [Unreleased]` touch check is included but SOFT — it warns, never blocks. (bead enhancedchannelmanager-w9irb)
- **New advisory endpoint `POST /api/channel-pipeline/rules/analyze-body` runs the Rule Analyzer against an UNSAVED rule body (bead m1s38.2, build 0085).** Lets the rule editor surface analyzer findings (guard/no-op/redundant-condition style warnings) while a rule is still being drafted, instead of only after it has been saved. Advisory-only — findings never block or gate save — with a typed frontend binding consuming the new endpoint. (bead enhancedchannelmanager-m1s38.2)
- **New `scripts/deploy-frontend.sh` performs an atomic frontend deploy — build, then clear stale assets, then copy (bead x7pck, build 0085).** Wraps the manual build → clear `/app/static/assets/` → copy sequence into one script, so a deploy can no longer skip the stale-asset cleanup step and leave an old Vite bundle mixed in with the new one. Documents the cleanup requirement alongside the script. (bead enhancedchannelmanager-x7pck)
- **Event Sync matching diagnostics added to the Channel Pipeline debug bundle (bead 03nji, build 0085).** The exportable debug bundle now includes per-stream parse/score/band/disposition/reject-reason detail for Event Sync rules, so an operator troubleshooting a matching problem can export one bundle instead of reproducing the issue live. Also adds gated per-candidate DEBUG logging in the matching resolver. Observability only — no change to matching behavior. (bead enhancedchannelmanager-03nji)
- **Event Sync matcher core now emits per-step DEBUG logging so a user report can be traced line-by-line (bead bbhpy, build 0087).** The matcher (`event_sync_matcher.py`) previously had a logger but zero calls, leaving the parse and scoring decisions dark below the resolver-layer logging from 03nji. It now emits, at `LOG_LEVEL=DEBUG`, one line per name parsed (`parsed` / `title_only_no_time` / `unparsed` with the matched pattern — directly exposing the `no_parsed_time` root cause), one line per scored pair (band/score/fuzzy/team-verdict/team-score/time-delta/reject-reason, covering every branch), a master-side summary (usable vs. unusable masters, naming each unusable one), and — closing a blind spot — one line per time-window-blocked pair (`reject=outside_time_window` with the delta and window), so the "right event, wrong time" case is visible instead of vanishing into a silent unmatched stream. All lines are `logger.isEnabledFor(DEBUG)`-guarded (zero cost when off); observability only — no change to matching behavior. Enable via Settings → backend log level = DEBUG (applied live, no restart). (bead enhancedchannelmanager-bbhpy)
- **Event Sync rules now show discoverable per-rule Run and Test buttons (bead utswf, build 0085).** Run/Test controls for Event Sync rules existed but were effectively hidden; they now appear in the rules list the same way standard-rule controls do. A manual live Run now asks for confirmation first, since it can attach streams. (bead enhancedchannelmanager-utswf)
- **New per-rule "refresh this rule's M3U providers before running" option for Event Sync rules (bead y8yby, build 0085).** Applies to manual Run and Test (auto-runs are excluded); best-effort — a refresh failure does not block the run. Documents the refresh-ordering/self-healing behavior in `docs/event_sync.md`. (bead enhancedchannelmanager-y8yby)
- **Event Sync matching controls: per-rule "ignore time window" toggle, operator-authoritative attach threshold, and acronym/hyphen-aware title scoring (beads krkm4 / amue8 / e0jzj, build 0084).** Three independent matcher improvements, driven by a real multi-provider FloRacing case where the same events are timestamped differently and named with heavy slot/venue noise. **Ignore time window (krkm4):** a new `event_sync_config.enforce_time_window` flag (default **true** — absent means enforced) disables the time-window candidacy gate when set false, so every parsed master event becomes a candidate and matches rank on title/team score alone. It rescues events whose providers publish different start times for the same fixture (one provider listed a race 6 hours off). Editor exposes it under Advanced as "Ignore time window (match on title only)", which greys the window input and warns it is safe only for a single-provider, same-day master group (recurring/serial titles can match the wrong day). The team-conflict/numeric-identity rails and the 0.90 no-teams floor still apply, so borderline pairs route to review, not auto-attach. **Operator-authoritative attach threshold (amue8):** `attach_threshold` is now any value in `[0, 1]` — 0.80 is the DEFAULT the validator fills, no longer a hard floor. `is_event_attachable` honors the operator's value directly; the 0.90 no-teams raise still applies while the threshold stays ≥ 0.80 (default configs and the frozen corpus are byte-identical) but is bypassed once the operator deliberately lowers below 0.80, so the lowering they asked for is not silently vetoed. Team-token conflict and different-event-number hard rejects fire at any threshold. **Acronym/hyphen title bridges (e0jzj):** `_fuzzy_title_score` now normalizes two classes of provider spelling variance before scoring, both corroboration-gated so they only ever ADD token agreement — a hyphenated token splits (`Shangri-La` → `Shangri La`, `Off-Road` → `Off Road`) ONLY when the other title carries that word-run (a blanket split spuriously matched `Pre-Race Show` to `Race Day Live` at the same venue — caught by the frozen corpus and fixed), and an acronym token collapses with the consecutive words it spells (`RoC` ↔ `Race of Champions`, `MUFC` ↔ `Manchester United`, FC-suffix aware) — reusing the same initialism logic the team-token layer already had, extended to teamless titles. The shared `clean_name` (used by dedup) is untouched. Together these took the FloRacing test rule from zero matches to a full clean attach at the default 0.80 threshold. Frozen precision/recall corpus unchanged (8/8). Docs: `docs/event_sync.md` → "Threshold and bands" (How the score is computed / Lowering the threshold / Disabling the time window). (beads enhancedchannelmanager-krkm4 / enhancedchannelmanager-amue8 / enhancedchannelmanager-e0jzj)
- **Event Sync rule editor: master + secondary group pickers default to ENABLED groups only, with a "Show all groups" escape hatch (bead x82s3, build 0074).** A real Dispatcharr instance can carry hundreds of channel groups, and the master-group select and secondary-groups multi-select previously listed every one of them — noisy, and mostly irrelevant to any single rule. Both pickers now default to groups where the group's provider setting is enabled (`M3UGroupSetting.enabled === true`; a group with no provider-settings entry at all counts as not-enabled and stays hidden). A new **Show all groups** checkbox (default off, shared by both pickers) reveals the full list for edge cases — a temporarily-disabled group, or one with no provider settings. The existing secondary name-filter composes with the enabled filter (both apply together). **Round-trip guard:** a group already referenced by the rule being edited — the saved master group, or an already-checked secondary — always renders, marked `(disabled)`, even when the enabled-only filter would otherwise hide it, and is never dropped from the saved config on save. Client-only change (no backend/API changes) — auto-sync pre-flight/guidance semantics are unchanged; this only affects which groups are *listed*. Docs: `docs/event_sync.md` → "Configure master + secondary groups". (bead enhancedchannelmanager-x82s3)
- **Event Sync Phase 2: ambiguous-band review queue keyed on event identity (bead ti939.3.2, build 0073).** Ambiguous-band event_sync matches — a candidate that scored below the attach threshold but above reject, or a contested tie between two masters — now enqueue into an operator **review queue** instead of being silently skipped, so a borderline event consolidates with one click instead of a threshold gamble. **Keying (hard security constraint):** every pending question and every accepted/rejected outcome keys on the **content fingerprint** `(rule_id, provider_id, stream_name_hash, event_key)` — **never** channel or stream IDs. Dispatcharr stream IDs churn on every provider refresh and channel IDs live only while an event's channel exists, so an ID-keyed queue would refill with duplicates of already-answered questions after every refresh; the fingerprint is DB-enforced by a full unique index on the new `event_sync_reviews` table (migration 0032), so an answered question can only refresh its row, never re-insert. Decisions **survive refreshes** and re-apply whenever the same provider string + event identity recurs. **Decisions are an input to the ONE resolver, not a second scorer** (`services/event_sync_resolver.py`, shared by preview and attach — dry-run parity by construction): a prior **accept** upgrades an ambiguous disposition to a queue-driven attach when the matcher still surfaces that master as an attach-/ambiguous-band candidate (accepts never override the matcher's hard rejects — team-token conflict, time window, below the ambiguous floor: precision over recall, the 1,341-merge trust benchmark); a prior **reject** removes the pairing from candidacy entirely, so it can neither attach nor re-enter the queue. **Accept** records the durable fingerprint decision, supersedes sibling pending pairings for the same stream, then best-effort attaches immediately — snapshot channel/stream ids in the display-only `evidence` JSON are re-verified against live Dispatcharr (channel name must still parse to the row's `event_key`; stream name must still hash to `stream_name_hash`) and a failed verification defers the attach to the next idempotent run; the decision is the durable artifact either way. **Journal distinction:** accepts/rejects write `journal_entries` rows (category `event_sync`, action `review_accept`/`review_reject`) and a queue-driven attach writes the standard `merge_stream` entry with `after_value.match.attach_source = "review_queue"` (threshold attaches carry `"threshold"`), keeping the two distinguishable and both covered by the journal-driven surgical unmerge. **UI (`EventSyncReviewQueue`, on the Channel Pipeline tab):** each pairing shows **per-candidate evidence** — both raw names side by side, parsed titles/starts, start-time delta, team-token verdict, score and band (badges are text + icon, never color alone) — because an opaque aggregate score was the human-factors condition that let the original incident scale. The **preview** decorates candidates of a saved rule with `Pending review` / `Accepted` / `Rejected` markers and counts queue-driven would-attaches separately. New endpoints: `GET /api/event-sync-reviews` (paginated, `RequireAuthIfEnabled`), `POST /api/event-sync-reviews/{id}/accept` and `/reject` (`RequireAdminIfEnabled`). Blast-radius controls: per-stream candidate cap and a per-run enqueue cap (default 500, warns and defers the remainder to the next idempotent run). Docs: `docs/event_sync.md` → "Reviewing ambiguous matches (Phase 2 review queue)" + "Review queue keying (ti939.3.2)"; `docs/api.md` → "Event Sync Reviews". (bead enhancedchannelmanager-ti939.3.2)
- **Event Sync Phase 2: dummy EPG auto-assignment for master event channels + guided one-click confirmed auto-sync fix (beads ti939.3.3 / ti939.3.4, build 0072).** **Dummy EPG (ti939.3.3):** an event_sync rule can now reference a dummy EPG profile (`event_sync_config.dummy_epg_profile_id` — optional, absent means off, must reference an existing profile or save fails with a teaching error; the rule editor exposes it under Advanced and API-set values round-trip) that gets assigned to the master group's channels on EVERY run, manual and auto-run, so new event channels get guide data automatically. The assignment rides the EXISTING machinery end to end: standard `assign_epg` execution against the Dispatcharr source serving the profile's XMLTV, with not-yet-covered channels deferring into the existing **Pass 5** refresh-and-retry (auto-adds the master group to the profile, regenerates the XMLTV, refreshes the source, retries — no parallel mechanism, exercised by test). Idempotent (already-assigned channels are no-ops), never clobbers foreign guide data, and degrades gracefully: a disabled profile or missing Dispatcharr source warns and skips only the EPG step — attaches are never blocked. Docs show an event profile whose patterns match the master provider's naming and call out that they can reuse the rule's own parse patterns (author once, paste twice). **Guided auto-sync fix (ti939.3.4, security-constrained):** when the rule editor's live status shows the master group with `auto_channel_sync` OFF or a secondary with it ON, a **Fix** button offers a one-click correction — behind its own confirmation dialog stating exactly what will change and why, including the consequence ("existing auto-created channels from it may be removed by Dispatcharr") and the recovery note (snapshot restore does NOT revert Dispatcharr group settings — the journal entry is the recovery breadcrumb). The toggle is a dedicated, admin-gated endpoint (`POST /api/m3u/accounts/{id}/group-auto-sync-toggle`) that hard-requires `confirm: true`, supports both directions (enable master / disable secondary), journals every toggle with before/after values, and is NEVER called as a side effect of saving a rule or running the pipeline (pinned by tests). It lives on the M3U router, outside the event_sync feature modules — the AST no-group-writes gate keeps proving the attach/preview path itself never writes group settings (and now also scans the new dummy-EPG assignment functions). Docs: `docs/event_sync.md` → "Automatic guide data for master channels (dummy EPG)" and "Guided setup: the confirmed auto-sync fix". (beads enhancedchannelmanager-ti939.3.3 / enhancedchannelmanager-ti939.3.4)
- **Event Sync Phase 2: opt-in auto-run on the refresh watermark (per-rule flag, default OFF) with unattended notifications, gated by the circuit breaker (beads ti939.3.1 / ixujz, build 0071).** An event_sync rule can now opt into UNATTENDED runs on the existing post-refresh watermark task by setting the new `event_sync_config.auto_run` flag — an explicit per-rule opt-in, **default false** (absent means false, so every stored config keeps manual-run-only behavior unchanged; the rule editor exposes it as an Advanced checkbox with honest copy, and API-authored values round-trip). The relaxation is surgical and layered, each layer pinned by its own test: the watermark task's rule query selects only opted-in event_sync rules (standard rules and non-opted event_sync rules are untouched); the engine's new per-rule trigger gate (`event_sync_trigger_allowed`) admits ONLY the watermark trigger for opted-in rules — "scheduled" and unidentified triggers stay denied for every rule (deny-by-default preserved); and the attach phase re-checks the gate as defense in depth. **Circuit breaker (bead ixujz):** a tripped run-on-refresh breaker (crash-sentinel after an abandoned run, or the `ECM_DISABLE_RUN_ON_REFRESH` break-glass) now gates the event_sync auto-run chain too — at the task tick AND inside the attach phase — while manual runs stay available as the recovery surface; reset restores auto-runs (trip and reset both tested end-to-end). **Unattended runs are indistinguishable from manual runs on the audit surfaces** (same journal provenance — category `event_sync`, batch_id = execution id, names+ids plus score/band/time-delta/team-verdict — and the same `event_sync: X attached, …` summary line, asserted field-for-field against a manual run), and misconfigurations notify instead of hiding: attach-cap overage and pre-flight failures raise warning notifications with alerts on (unattended runs pre-flight the Dispatcharr group settings and FAIL CLOSED — e.g. master auto-sync OFF skips the rule that run; manual runs are unchanged and never pre-flight), and the completion notification carries the attach count. The refresh-ordering race stays accepted-not-engineered-around: a watermark run that precedes master-channel materialization attaches nothing and converges next run (now covered on the unattended path too). Docs: `docs/event_sync.md` → "Automatic runs after refresh (Phase 2 opt-in)". (beads enhancedchannelmanager-ti939.3.1 / enhancedchannelmanager-ixujz)
- **Event Sync E2E happy path + documented pre-release manual attach script (bead ti939.2.4, build 0069).** New live-container E2E (`backend/tests/e2e/test_event_sync.py`) covers configure-with-shipped-patterns → Test Patterns parsing → zero-write preview with exactly-reconciling summary counts → manual single-rule run (completes with the `event_sync:` summary line, provably attach-nothing by picking a zero-channel master group) → journal provenance queryable by category + batch_id. The multi-provider attach segment cannot be demonstrated on the dev instance (no auto-sync master group exists, and ECM never toggles that Dispatcharr setting), so it is recorded as a documented pre-release manual script in `docs/event_sync.md` → "Testing & pre-release verification", with the mocked-Dispatcharr coverage (lifecycle + attach-execution unit tests) cross-referenced. (bead enhancedchannelmanager-ti939.2.4)
- **Event Sync Phase 1B: the attach path — event-mode merge execution, manual-run-only, fully reversible (beads ti939.2.1 / ti939.2.2 / ti939.2.3, build 0068).** A **manually triggered** pipeline run now executes event_sync rules through a dedicated attach phase: every secondary-group stream resolves through the SAME resolver the preview endpoint calls (`services/event_sync_resolver.py` — dry-run parity by construction, pinned by a frozen-time test asserting preview decisions equal live-run decisions on identical fixtures), and attach-band matches are attached to their master channel via the existing merge internals — idempotent (already-attached is a no-op, so re-running after every refresh is the intended usage), capped per run (`max_attach_per_run`, overage warns on the execution record), and journaled per attach (category `event_sync`, batch_id = execution id, names alongside IDs plus score/band/time-delta/team-verdict). Ambiguous or contested matches are skipped and counted, never attached. The run writes the operator's drift-detector summary line (`event_sync: X attached, Y ambiguous skipped, Z unmatched, W parse failures`) into the execution log. **Manual-run-only is enforced twice** — the unattended watermark task excludes event_sync rules from its query, and the engine refuses any non-manual/api trigger; `run_pipeline`'s `triggered_by` now defaults to a DENIED sentinel so an unidentified future caller can never execute event_sync rules by accident. **Recovery is verified end-to-end (SRE ship-blocker):** the pre-mutation snapshot includes the master group's channels; execution rollback removes every attachment the run made and never deletes the master channels; master channels are restored with a **streams-only** payload (their Dispatcharr-owned name/group/EPG metadata is never reverted to stale pre-run values); the channel-pipeline circuit breaker covers crashed event_sync runs; and an AST gate proves no event_sync code path writes Dispatcharr group settings. Lifecycle integration tests encode the verified Dispatcharr behaviors (UUID-preserving in-place refreshes, foreign-stream survival, master-scoped deletion) as fixtures: idempotent re-runs, refresh survival, event-end cleanup with no re-attach, refresh-ordering-race convergence, and in-place slot renames. New troubleshooting runbook: [Undo a bad event_sync run](docs/event_sync.md#undo-a-bad-event_sync-run) — including the reminder that a deterministic matcher repeats a bad match until the rule is adjusted. (beads enhancedchannelmanager-ti939.2.1 / ti939.2.2 / ti939.2.3)
- **Event Sync rule editor, shipped default parse patterns, Test Patterns panel, and match preview UI — preview-only, with NO apply/attach control anywhere (bead ti939.1.5, build 0067).** The Channel Pipeline tab's Create Rule flow now opens a rule-kind chooser (Standard vs Event Sync), and event_sync rules get their own editor built around the quick path *pick master → pick secondaries → keep the shipped patterns → Preview*: a master-group select showing LIVE Dispatcharr auto-sync status (guidance text when it's OFF — ECM never toggles group settings this phase), a secondary-groups multi-select with the inverse should-be-OFF validation and the same guidance-not-mutation stance, and a shipped-pattern picker (the two backend built-ins for "Title @ Day-first/Month-first Date Time" including "Fubo Sports Network 07 :"-style slot prefixes — kept verbatim-in-sync with `DEFAULT_EVENT_PATTERNS` and omitted from the saved config when unchanged so backend improvements flow through — plus two generic no-"@" variants and a custom-regex fallback; the whole shipped set lives in a JSON fixture consumed by both the UI and a backend parity test that runs every entry's example through the real `parse_event_name`, so a shipped pattern that cannot parse its own example fails CI) so most operators never write a regex. Advanced knobs stay collapsed: time window (1–1440 min), attach threshold (input clamped to the ≥ 0.80 hard floor), and per-group pattern overrides. A **Test Patterns panel** (pasted sample names or live samples fetched from the rule's groups) runs extraction server-side through the dummy-EPG batch preview — the exact `extract_groups`/`safe_regex` machinery the matcher uses — and renders a native table of raw name → extracted title/date/time per pattern, flagging incomplete date/times as parse failures (start times are never guessed). The **match preview** renders the zero-write preview endpoint's response: keyboard-navigable match cards showing each provider's RAW name side by side with parsed title/time, score, confidence band and team-token verdict as text labels + icons (never color alone), time-delta/disparity flags, a distinct unmatched-secondary-streams list, a parse-failure panel grouped by group/reason, pre-flight failures with their teaching messages, and a reconciling summary line ("42 would attach, 8 ambiguous (skipped), 3 unmatched, 1 parse failure"). Event Sync rules show an "Event Sync" badge in the rules list and hide the run/dry-run controls (they are excluded from pipeline execution entirely in Phase 1A); vitest pins the threshold clamp, the band/pattern-test rendering, and the absence of any apply/attach control. (bead enhancedchannelmanager-ti939.1.5)
- **Event Sync dry-run preview: `POST /api/channel-pipeline/event-sync-preview` + `preview_event_sync` MCP tool — match secondary streams against live master channels with ZERO writes (bead ti939.1.4, build 0066).** Phase 1A of Event Sync becomes exercisable end to end: the new endpoint accepts a saved event_sync rule id OR an inline `event_sync_config` (so the rule editor can preview before saving; inline configs run through the same `validate_event_sync_config` teaching-error rail set as rule save), runs the read-only pre-flight (master group auto-sync ON, secondaries OFF — failures SURFACE in the response but never block the preview, so a misconfiguration is visible alongside what the matcher would still do), fetches the master group's channels and every secondary group's streams from Dispatcharr (paginated, DoS-capped at 2,000 each with a `truncated` flag), and resolves matches through the new shared resolver `backend/services/event_sync_resolver.py` — the EXACT function the future Phase 1B attach path will call, so preview scoring and attach scoring cannot diverge (dry-run parity by construction). The response carries per-stream rows (raw name, provider, group, parsed title + start, disposition, up to 10 scored candidates with master name + current channel id, score, band, team-token verdict, time delta, and machine-readable reject reason), the unmatched-streams list (the master-as-ceiling visibility hedge), parse failures grouped by group/reason with counts and sample names (a silently broken pattern is LOUD), unparsable master channel names, and summary counts (would-attach / ambiguous-skipped / unmatched / parse-failed) that reconcile exactly with the detail rows. Zero writes anywhere in the path: no merges, no channel mutations, no Dispatcharr group-setting toggles (pinned by tests asserting no mutating client method is ever called). The matcher gains an additive `master_patterns` parameter so per-group pattern overrides (`group_patterns`) parse each side with its own group's patterns, and `match_streams` behavior is unchanged for existing callers. Also in this build (PR #611 review riders): the slot-prefix comment in `event_sync_matcher.py` now states the empirically-verified behavior (both sides retaining an unpadded slot prefix hard team-token-conflict — a silent false negative, not graceful degradation), the numeric-identity rail docstring names its false-negative class, and the frozen corpus gains a documented KNOWN RECALL MISS pair ('Lyon'/'Lyons' spelling variants under the 0.90 agree floor). (bead enhancedchannelmanager-ti939.1.4)
- **Event Sync rule kind: schema, write-time validation, migration, and engine guard rails — still preview-only, no execution path (bead ti939.1.3, build 0065).** Auto-creation rules can now carry an `event_sync_config` JSON column (Alembic migration `0031`, one nullable column on `auto_creation_rules`, guarded/idempotent add with a clean downgrade — no new tables per the stateless-recompute decision) that makes the rule the **event_sync kind**: `master_group_id`, `secondary_group_ids[]`, optional shared or per-group parse patterns (title/time/date regexes), `time_window_minutes` (default 30), `attach_threshold` (default 0.80, hard-clamped ≥ 0.80 against the matcher's own `EVENT_ATTACH_FLOOR`), and `enabled`. Write-time validation enforces mandatory scoping (master present, secondaries non-empty, master not in secondaries — the schema-enforced rail from the 1,341-incident trust benchmark), compiles every operator parse regex through `safe_regex` at save time, and rejects unknown keys; every rejection is a teaching error carrying the field, the submitted value, what was expected, and a link to the new `docs/event_sync.md`. Engine wiring is guard-rails-only this phase: event_sync rules are excluded from per-stream Pass 1/Pass 2 evaluation (a run scoped only to event_sync rules reports itself as preview-only instead of silently no-opping) and hard-bypass Pass 4 orphan reconciliation — `managed_channel_ids` is never populated, so the pipeline can never "reconcile" (delete/move) master channels that Dispatcharr owns. A new read-only pre-flight helper (`backend/services/event_sync_preflight.py`) verifies the master group has `auto_channel_sync` ON and every secondary has it OFF, surfacing failures in results — ECM never toggles Dispatcharr group settings. Pre-feature rules load unchanged (column reads back NULL); duplicate-rule, DBAS backup/restore, and YAML export/import all preserve the kind (imported configs route through the same validator; DBAS restore downgrades validation failures to warnings and restores as-is — the raw column keeps the rule excluded from execution); bulk-update refuses the field rather than silently dropping it; `time_window_minutes` is capped at 1440 (24h) so an oversized window cannot re-open the same-teams-different-day false-positive class. (bead enhancedchannelmanager-ti939.1.3)
- **Event Sync groundwork: isolated event-matcher service + frozen regression corpus CI gate (beads ti939.1.1 / ti939.1.2, build 0064).** First slice of the Event Sync epic (one channel per live event across providers). New pure service module `backend/services/event_sync_matcher.py` implements the layered matcher — parse provider event names to (title, start time) reusing the dummy-EPG regex machinery (safe_regex on untrusted strings; both observed date shapes `"11 Jul 06:00 PM ET"` and `"Jan 17 02:45 PM ET"`; year inference across the Dec/Jan boundary; a name with no parsed time is unmatchable, never guessed) → ±30-minute time-window candidate blocking → RapidFuzz token-set scoring of PARSED titles with LOCALS cleaning → order-insensitive team-token check where a clear team conflict (including women's/U21/reserves qualifier mismatches) is a HARD reject at score 0.0 → a structurally separate event admission policy (`EVENT_ATTACH_FLOOR` 0.80, per-rule adjustable, hard-clamped ≥ 0.80; a stricter 0.90 bar when no team tokens corroborate). Master channels are identified by name only — channel IDs are never cached across runs. Guarding it in CI: a frozen, add-only 49-pair corpus (`backend/tests/fixtures/event_sync/matcher_corpus.jsonl`, seeded from live provider event-group names plus engineered traps for the 1,341-false-positive incident shapes) with a pytest gate (`tests/test_event_sync_matcher_corpus.py`) enforcing per-label band correctness and attach-band precision ≥ 97% / recall ≥ 85%. No user-facing surface yet — the preview endpoint, rule schema, and UI land in subsequent Phase 1A beads. (beads enhancedchannelmanager-ti939.1.1 / enhancedchannelmanager-ti939.1.2)
- **`sort_group` channel-pipeline action: renumber a group's channels alphabetically (asc/desc, natural sort, strip-numbers / ignore-country options matching manual Sort & Renumber) as a once-per-group post-run pass; manual Sort & Renumber modal gains a descending option (beads vy4fl, hf8t9, build 0063).**
- **~40 new MCP tools close the MCP-vs-UI coverage gap — normalization CRUD, logo management, tag engine, cloud/sync targets, dummy-EPG profile CRUD, probe lifecycle + circuit breaker, and per-channel CSV export, plus parameter-drift fixes across 8 existing tool families (beads yd6qh / e5n8j / twadj / rv5w1 / omxy5 / dswrl / jcj0f / l3jf7, build 0062).** The MCP coverage audit found whole feature domains an AI client could not reach. New tool modules: **normalization** rule/group CRUD + import/export (e5n8j), **logos** (`tools/logos.py` — list/upload-from-URL/set/clear/cleanup, twadj), **tags** (`tools/tags.py` — tag-engine rule CRUD + test, dswrl), **cloud + sync targets** (`tools/sync_targets.py` — cloud-target CRUD/test-connection and cross-instance sync-target CRUD/run, jcj0f), **dummy-EPG profile CRUD** (omxy5), **probe lifecycle** (pause/resume/cancel + circuit-breaker status/reset, rv5w1), and **per-channel CSV export** (`tools/channels_csv.py`, with a new `ecm_client.get_text` text-response path, l3jf7). Separately, an 8-family parameter-drift sweep (yd6qh) added backend params that existing tools silently lacked (EPG source options, M3U account fields, channel/stream list filters, task-schedule fields, and more). (beads yd6qh / e5n8j / twadj / rv5w1 / omxy5 / dswrl / jcj0f / l3jf7)
- **UI gap closures: one-click restore buttons for saved backups (with a shared type-to-confirm dialog), an Alert Methods settings panel, and a 10-item small-UI sweep (beads rzhid / p4qt8 / hq3de, build 0062).** Settings → Backup & Restore gains restore buttons on saved backup artifacts, backed by a new reusable `TypeToConfirmDialog` and a `DbasRestoreSavedModal` for saved DBAS artifacts (rzhid). A new **Alert Methods** settings section manages notification delivery methods with per-method test buttons (p4qt8). The 10-item sweep (hq3de) adds previously API-only surfaces to the UI, including a Server Groups modal, a Stream Profiles list modal, and a per-channel Stats drill-down modal (`ChannelStatsDetailModal`), among other small additions. (beads rzhid / p4qt8 / hq3de)
- **Find Duplicates can now scan just your selected channels instead of the whole lineup (bead uahp6, build 0062).** The Channels pane's Find Duplicates action now scopes the scan to the checkbox-selected channels when a selection exists (backend `channel_ids` on the find-duplicates endpoint, wired through the UI and the MCP `find_duplicate_channels` tool). The modal header states the scope it scanned. (bead uahp6) and their failures surfaced at run time (beads enhancedchannelmanager-2uwi3 / 3gigl / yom3k / ock7c, build 0061).** A rule's `set_channel_name`-style transform with a `$N` backreference pointing at a capture group the pattern doesn't have (e.g. replacement `$3` against a 2-group pattern) previously saved fine and then failed silently at execution time — the transform just didn't apply, with nothing in the execution log to explain why. Three layers close the gap. **(1) Save-time validation:** creating or updating a rule now validates every `$N` replacement backreference against the pattern's actual group count and rejects the rule with a `422` and an actionable message naming the offending reference and the available groups; `$0` and leading-zero references (e.g. `$01`), which Python's `re` silently reinterprets as octal escapes, are rejected with the same clarity. **(2) Execution-time surfacing:** when a transform still fails at run time, the failure now appears in the execution log's action results, in the Rule Builder's Test output, and as a journal entry — instead of vanishing. **(3) Edit-time preview parity:** the Rule Builder's live name-transform preview now flags an out-of-range `$N` inline instead of rendering it as literal text that looked intentional. (beads enhancedchannelmanager-2uwi3 / 3gigl / yom3k / ock7c)
- **`stream_group_is` channel-pipeline condition: match a stream's provider group (M3U group_title) exactly, with a provider-group dropdown in the Rule Builder (bead enhancedchannelmanager-rgw9p, build 0060).** Rules can now match on the stream's provider group as a first-class condition — exact match, case-insensitive by default with a `case_sensitive` opt-in — instead of regexing the group name. The Rule Builder's condition editor offers "Stream Group is" with a dropdown of the provider groups already loaded, and the Rule Analyzer classifies the condition as a guard. (bead enhancedchannelmanager-rgw9p)
- **Refresh-token rotation grace window — two browser tabs racing the same token refresh no longer log the loser out (bead enhancedchannelmanager-x67qe, build 0059).** `/api/auth/refresh` rotates the refresh token on every use; previously, when two tabs (or a tab and a background poll) raced the same rotation, the second request presented the just-rotated-away token and was hard-rejected, killing the whole session. The backend now remembers the hash of the immediately-prior refresh token and when it was rotated away, and accepts the predecessor within a short grace window with idempotent-refresh semantics. Only ONE prior generation is kept — a normal rotation overwrites both grace fields, so a graced token can never chain to an older one. The frontend additionally serializes concurrent refresh attempts within one browser via `navigator.locks` in `httpClient.ts`, so the race becomes rare to begin with. **Includes a DB schema migration** (`0030_user_sessions_rotation_grace`): two new nullable columns on `user_sessions` (`prior_refresh_token_hash`, `rotated_at`) plus an index, applied automatically on upgrade. (bead enhancedchannelmanager-x67qe)
- **Auth responses now report the access token's lifetime, and the frontend refreshes proactively instead of waiting for a 401 (bead enhancedchannelmanager-3ymo4, build 0059).** Login, `/auth/me`, and `/auth/refresh` responses gain a read-only **`access_token_expires_in`** metadata field (seconds; additive — existing consumers are unaffected). `useAuth` schedules a proactive refresh at 80% of the reported lifetime (with a 30s floor so a tiny/zero lifetime can't hot-loop), so long-lived sessions renew in the background instead of bouncing a real request off a 401 first. (bead enhancedchannelmanager-3ymo4)
- **Accessibility sweep — icon-only buttons are now screen-reader labeled and group/accordion rows are keyboard-operable (beads enhancedchannelmanager-mtklp / 6n14l, build 0059).** 274 icon-only buttons across the app (row actions, modal close buttons, toolbar toggles, chevrons, etc.) now carry `aria-label`s, and a new AST-based regression test (`frontend/src/a11y/iconOnlyButtons.a11y.test.ts`) scans the component sources in CI and fails if a new icon-only button ships without an accessible name — so the sweep can't silently regress. Separately, expandable group/accordion header rows across 9 components are now real keyboard targets: focusable, Enter/Space to toggle, with `aria-expanded` state. (beads enhancedchannelmanager-mtklp / 6n14l)
- **Large groups now render incrementally with a "Show more" control instead of mounting every row at once (bead enhancedchannelmanager-bed9r, build 0059).** A new shared `ShowMoreRows` component caps the initially-rendered rows of very large lists/groups and appends the rest in increments on demand, keeping expansion of huge M3U groups responsive instead of janking the tab while thousands of rows mount. (bead enhancedchannelmanager-bed9r)
- **The Channel Pipeline's per-rule manual-channel protection is now visible and controllable everywhere — MCP, the Rule Builder UI, and blocked merges are now explained in the execution log and journal (beads enhancedchannelmanager-zrte6 / lu3fk / wy6l5, build 0058).** By default, hand-built (manual) channels are protected from Channel Pipeline merges: when a rule's merge target matches a manual channel, the match is treated as "not found" and the rule may create a new auto channel instead. That protection existed in the backend (`allow_manual_channel_merge`, PR #547) but was previously invisible and hard to reach. Three surfaces close the gap: **(1) MCP (zrte6):** `create_channel_pipeline_rule` and `update_channel_pipeline_rule` (and their deprecated `*_auto_creation_*` aliases) now accept a documented `allow_manual_channel_merge` parameter, so agents can opt a rule into merging into manual channels. **(2) UI (lu3fk):** the Rule Builder gains a "Manual channel protection" section with an "Allow merging into manual channels" checkbox (default off) and hint text explaining both modes. **(3) Blocked-merge visibility (wy6l5):** when the protection blocks a merge, the outcome is no longer an INFO-log-only mystery that made rules look like they were "skipping everything" or creating inexplicable duplicates — the execution log's action results now say which manual channel matched, why the merge was withheld, and what happened instead (new auto channel created, or stream skipped), and the journal records a `manual_channel_merge_blocked` entry (deduped to one per blocked channel per run). Merge/skip behavior itself is unchanged — this is control-surface and visibility work only. (beads enhancedchannelmanager-zrte6 / lu3fk / wy6l5)
- **The Stats tab's Active Channels cards now show what's Currently Showing from the EPG (bead enhancedchannelmanager-hoj54, build 0057).** Each active-channel card gains a "Now Showing" row with the program airing right now — title, subtitle when present, and the air-time window (e.g. `Now Showing · SportsCenter · 8:00 PM – 9:00 PM`), with the program description on hover. Programs come from the EPG grid the Guide tab already uses, matched the same way the Guide matches them: the channel's `epg_data_id → EPG tvg_id` hop first (so it works even when the channel's own `tvg_id` differs from the guide's), falling back to the channel's `tvg_id`, and finally to the channel UUID — which is how Dummy EPG programs are keyed, so dummy-EPG channels show their generated program too. Guide data refreshes on a slow 30-minute cadence (which program is "now" is recomputed locally every minute, including in Manual refresh mode), and if EPG data is unavailable the row simply doesn't render. Display-only — no backend or API changes; the shared program-matching helpers were extracted to `utils/epgProgram.ts` and the Guide tab now uses the same code. (bead enhancedchannelmanager-hoj54)
- **The Channel Manager rows are now a two-line layout with resolution capability pills, and the stream URL copies on click (bead enhancedchannelmanager-llosp, build 0055).** Building on the TVG subtitle from builds 0053/0054, each Channel Manager row is restructured into two lines with dedicated left-aligned columns. **Line 1:** channel number (its own fixed column, sized to the largest real channel number) · channel name · the stream URL. **Line 2:** the `EPG · TVG-ID · TVG-name` subtitle (full width, truncating) on the left, with **resolution capability pills** right-aligned — one pill per distinct resolution tier among the channel's successfully-probed streams, highest-first (`4K` purple / `FHD` blue / `HD` green / `SD` gray), derived client-side from the stream-stats the page already loads. The name (line 1) and EPG subtitle (line 2) now share the same left edge. The **stream-count indicator** is a compact `[sources-icon] N` (the neutral `lan` icon for healthy channels, or the existing status icon for no-streams/failed/black-screen/low-fps), with the full "N stream(s)" text moved to the hover tooltip. **Clicking the stream URL copies it** (with the existing copy-feedback toast; the "Copy URL" menu item remains). Also tightened the left-cluster and count-column spacing, and scoped a leaked global `.channel-item` CSS rule (a class-name collision with the Channel Profiles modal) so the fix applies to Channel Manager rows only. Display-only — no backend or API changes. (bead enhancedchannelmanager-llosp)
- **The Channel Manager TVG subtitle now leads with the EPG source name the TVG ID/name tie to (bead enhancedchannelmanager-1nf7u, build 0054).** The per-row subtitle introduced in build 0053 now reads `EPG source · TVG-ID · TVG name` — e.g. `epg.guru UK IPTV · BBCTwo(BBC2).uk · BBC Two` — so an operator can see at a glance not just the channel's TVG identity but which EPG source provides it. The source name appears only when the channel has a linked EPG record whose source resolves; channels showing only their own `tvg_id` (no EPG link) are unchanged. The hover tooltip gains a matching labeled segment (`EPG: … · TVG ID: … · TVG Name: …`). Display-only, resolved client-side from the EPG source list the page already loads — no backend or API changes. (bead enhancedchannelmanager-1nf7u)
- **Channel Manager rows now show the applied TVG ID and TVG name under the channel name (bead enhancedchannelmanager-ciply, build 0053).** Each channel row in the Channel Manager list gains a small muted subtitle beneath the name showing the channel's effective TVG identity — the channel's own `tvg_id` (falling back to the linked EPG record's `tvg_id`) plus the linked EPG record's name, e.g. `BBCTwo(BBC2).uk · BBC Two` — so an operator can see EPG linkage at a glance without opening the channel editor. Rows with no TVG data are unchanged, the subtitle hides during inline name editing, and hovering it shows a labeled tooltip (`TVG ID: … · TVG Name: …`). Display-only, resolved client-side from data the list already loads — no backend or API changes. (bead enhancedchannelmanager-ciply)
- **Exit-path diagnostics instrument GH #546's silent backend crash under concurrent JWT-cookie auth (bead enhancedchannelmanager-0gt2i, GH #546, build 0052).** A well-instrumented user report described the backend process exiting cleanly (ExitCode 0, no signal, no traceback, `PYTHONFAULTHANDLER` silent) under concurrent requests authenticated via the `access_token` cookie — single requests never crash, and the same requests over the MCP static-key path (which bypasses JWT decode) never crash either, pointing at shared state on the JWT-cookie path. The root cause is **not yet pinned**; this build adds instrumentation to make the next occurrence self-diagnosing rather than claiming a fix for the crash itself. New `backend/exit_diagnostics.py` installs an `atexit` hook, `sys.excepthook`, `threading.excepthook`, and an asyncio loop exception handler — all logging a loud `[EXIT-DIAG]` line (chaining to the prior handler, so existing behavior is unchanged) that lets an operator distinguish, from `docker logs` alone, a clean `SystemExit`-class exit (atexit line, no exception above it) from a real unhandled exception (exception logged with traceback) from a hard `os._exit()`/`SIGKILL`/OOM (total silence — no atexit line at all). See the module docstring for the full decision table. (bead enhancedchannelmanager-0gt2i)
- **New admin-only global setting to allow auto-syncing a shared M3U group from more than one provider (bead enhancedchannelmanager-dgs64, GH #591, build 0050).** Dispatcharr channel groups are global entities — a group with the same name on two M3U providers shares one underlying `channel_group` ID — so ECM's `M3UGroupsModal` locks a group's Auto-Sync toggle/Start #/Settings to whichever account enabled it first, to prevent two providers from silently double-creating channels for the same group. A new **Allow multi-provider auto-sync on shared groups** checkbox (Settings → Appearance → Display Options, admin-only) lets an operator opt out of that lock and auto-sync the same group from multiple providers, as Dispatcharr itself allows — an informational shared-ownership icon still marks the overlap in the Manage Groups modal. **Default is off**, preserving today's single-owner lock and its duplicate-channel protection; enabling it is a deliberate admin trade-off, since two providers auto-syncing the same group **can create duplicate channels** if both stream the same content. Changing the field is admin-gated the same way as the existing GH #473 safety caps — a non-admin (including the MCP service principal) attempting to change it is rejected `403`. A value change now also logs and journals a before/after entry (new `settings` journal category) for auditability. See [`docs/runbooks/duplicate-channels-unicode-suffix.md`](docs/runbooks/duplicate-channels-unicode-suffix.md) for a diagnostic pointer distinguishing this overlap from a Unicode-suffix normalization gap, including the rollback caveat that disabling the setting re-engages the lock but does not clean up channels already duplicated. (bead enhancedchannelmanager-dgs64)

- **Channel Pipeline canonical API route and MCP tool names (bead enhancedchannelmanager-dl0kk, build 0046).** Phase 3 of a multi-phase rename (epic enhancedchannelmanager-3udrl) that is preparing the "Auto-Creation" feature for a full rebrand to "Channel Pipeline." The backend now exposes a canonical `/api/channel-pipeline` route (dual-mounted alongside the still-functional `/api/auto-creation`), and the MCP server registers 14 new canonical tool names (`run_channel_pipeline`, `list_channel_pipeline_rules`, `create_channel_pipeline_rule`, and 11 others) as the primary implementation. New integrations should target the new route and tool names. (bead enhancedchannelmanager-dl0kk)

- **Auto-Creation Rule Builder gains a "Does Not Match (Regex)" operator for exclusion filters (bead vmmgy, build 0041).** The backend condition schema already supported inverting any condition (a generic `negate` flag), including regex-matching conditions — but the Rule Builder UI only exposed "Matches (Regex)" with no negated counterpart, and the `create_auto_creation_rule`/`update_auto_creation_rule` MCP tools didn't document `negate` at all. Now Stream Name, Stream Group, TVG-ID, and Channel Name each offer "Does Not Match (Regex)" alongside the existing operators, for excluding streams/channels matching a pattern; reloading a rule with a hand-crafted negated regex condition (e.g. via MCP) now correctly displays it as negated instead of silently showing it as a positive match. MCP tool docs now document `negate` as an available condition-dict key, with examples. (bead vmmgy)
- **M3U account Smart Sort priority is now readable and settable via MCP (bead 0u80f, build 0040).** `list_m3u_accounts` and `get_m3u_account` now surface each account's current Smart Sort priority (the ECM-local `m3u_account_priorities` setting consumed by the "m3u_priority" sort criterion), and a new `set_m3u_account_priority(account_id, priority)` MCP tool lets an agent set (1-100) or clear (0) it — previously only editable from Settings → M3U Manager. (bead 0u80f)
- **Settings → Stream Health now has a "Stale Streams" scan, surfacing streams whose health data has gone quiet or whose provider has stopped seeing them (bead n4g7g, build 0039).** Two independent signals are unioned into one list: **(1) ECM-side staleness** — a stream's own `StreamStats.last_probed` timestamp is older than a configurable N days (default 7), or the stream has never been probed at all; and **(2) provider-side staleness** — Dispatcharr's own per-stream `is_stale`/`last_seen` flags, meaning the source M3U's last refresh no longer re-matched the stream in the playlist (the provider may have dropped or renamed it upstream). Each stream in the results carries a `reasons` list explaining why it was flagged and its associated channel(s), so an operator can tell "just hasn't been probed lately" apart from "the provider may have removed this feed." A new **Stale Streams** section under Settings lets an operator run the scan, review reason badges per stream, and select streams for a non-destructive **Probe Selected** action (which re-probes without any automatic deletion or channel changes). Also available as the `get_stale_streams` MCP tool for AI-driven workflows. (bead n4g7g)
- **Stats tab: per-channel logos, username attribution, and by-user/by-IP viewer toggles (beads 2sfpt / 84l9m, build 0030).** Four operator-facing improvements to the Stats tab. **(1) Active-channel logos:** each currently-active channel row now shows a small channel logo beside its number (resolved from the channel's `logo_id` via the logos cache, matching the Channels pane; null logos render nothing — no broken-image icon). **(2) Recent Events usernames:** the live event feed now shows *which user* connected/disconnected — the backend enriches each event by batch-joining its client IP against ECM's recorded `UniqueClientConnection.username`; when no username is known it falls back to the IP address as before. **(3) Top Viewers by-user/by-IP toggle** and **(4) Channels by Unique Viewers by-user/by-IP toggle:** a shared "Group by: IP / User" control lets the operator group viewers / unique-viewer counts by username (`COALESCE(username, ip)`, so unresolved viewers still fall back to their IP) instead of only by raw IP. (beads 2sfpt / 84l9m)
- **The auto-creation runaway safety cap is now an adjustable, discoverable setting — Settings → Auto Creation — instead of a settings.json-only knob (bead skg35, build 0027).** When a single auto-creation run hits the per-run channel cap (the GH #473 OOM safety valve, default 500), the run stops early with status "capped" and an operator who needed to raise it had no supported way to: `max_auto_created_channels_per_run` lived only in `/config/settings.json`, wasn't in the settings API or UI, and the capped-run message literally told them to "raise max_auto_created_channels_per_run" — a key they couldn't find. Now both that cap and its sibling memory cap (`max_auto_creation_log_entries`) are surfaced on `GET /api/settings`, editable from a new **Runaway Safety Cap** control on the Auto Creation settings page, and persisted via `POST /api/settings`. Helper text explains the default (500), that **0 disables** the cap, and that auto-creation is **idempotent** — re-running a capped run continues from where it stopped (already-created channels persist), so the cap can be left in place. Changing the cap is **admin-only** (it joins the kgz3k field-level admin partition): a non-admin — including the MCP service principal — that attempts to change it is rejected `403`, since lowering/disabling it re-opens the runaway-creation OOM blast radius. The capped-run messages (execution record, run-summary log entry, and "Capped" notification) now point operators to **Settings → Auto Creation** and surface the idempotent-rerun hint instead of naming the raw settings.json key. (bead skg35)
- **Auto-creation now warns when a rule references DISABLED normalization groups instead of silently normalizing nothing (bead e8p1h, build 0025).** Closes a quiet data-quality footgun: a rule could carry `normalization_group_ids` pointing at normalization groups that are globally disabled (or no longer exist), in which case every `[NORMALIZE]` decision applied nothing — provider prefixes/suffixes (e.g. `USA PREMIUM MOVIES:`) were never stripped, stream names never matched the clean channel names, and `merge_streams target:auto` matched almost nothing — with no warning anywhere. Now, when an auto-creation run starts, the engine detects rules referencing disabled/missing normalization groups, logs a `[AUTO-CREATE-ENGINE]` warning, and records the finding on the run summary. The executions detail view shows a prominent amber banner ("Normalization applied no changes — disabled groups referenced") naming the affected rule(s) and group(s) and telling the operator to enable them under Settings → Normalization; a warning toast also fires on an otherwise-clean run. The **Rule Analyzer** (`analyze_auto_creation_rules`, backend + MCP) flags the same condition as a new `RULE_REFERENCES_DISABLED_NORMALIZATION_GROUP` finding when run live. This is detection-only — it does **not** change what gets merged or normalized. Persisted via new migration `0028` (`auto_creation_executions.warnings`). (bead e8p1h)
- **DBAS backup safety: first-run "where can backups go?" prompt, a Settings → Security home for it, and a "not scheduled yet" setup nudge (beads ikv8z/nngkg, build 0024).** Two operator-facing additions for v0.18.0 DBAS Phase 1. **(1) Backup-schedule nudge (ikv8z):** scheduled backups ship OFF by default, so the Backup & Restore page now shows a one-time amber banner — "Backups are not scheduled yet · Set one up →" — whenever no enabled backup schedule exists; the link jumps straight to the schedule editor. Dismissing it persists, and it never nags once a schedule is enabled. Stops a less-engaged operator from silently keeping zero backups. **(2) Backup-destination choice (nngkg):** a first-run modal asks once where ECM may upload backups — "my home network (recommended)" vs "public internet only" — in plain language; the choice is persisted and freely reversible later in the new **Settings → Security** section. Escaping/dismissing the modal accepts the safe home-network default and tells the operator where to change it. The cloud-target editor now shows a "Certificate check off" warning badge on any target with certificate checking disabled. The actual enforcement (blocking risky internal addresses) is always-on in the backend regardless of this choice; the UI only moves the home-network-vs-public band. Persisted via a new admin-gated `PATCH /api/settings/security` endpoint; the mode is also surfaced on `GET /api/settings` and preserved across normal settings saves. (epic 0i2vt, beads ikv8z / nngkg)
- **Anti-fake-test guardrails — convention + CI gate (epic ulp7q, build 0023).** Internal-only, closes out the test-validity audit. Added a "Test validity / anti-patterns" section to `docs/style_guide.md` (the "would it bite?" bar, banned patterns, the good pattern, and real exemplary tests to copy), plus a stdlib `scripts/check_fake_tests.py` guard wired as a `fake-test-guard` CI job that fails on the most blatant fake-test markers (`assert True` in backend/MCP tests, `expect(true).toBe(true)` in frontend tests) with a documented `# fake-test-ok: <reason>` suppression convention. Stops the class of issue the audit found from recurring. (epic ulp7q, bead .9)

### Changed
- **Event Sync master-side parse logging now explains WHY a master is unusable (bead at41p, build 0088).** When an Event Sync run produces no matches because the master group yields no usable events, DEBUG logs previously showed only a bare master name. The matcher now reports, per unusable master, the exact identity string parsed, the parsed title/start, and a reason (`missing-title` / `missing-start` / `missing-title+start`); the resolver echoes the identity-source flags (`parse_master_from_stream`, `include_master_group_streams`); and the fetch path logs the `master_group → effective_master_group` override remap plus channel/identity counts. Observability only (all DEBUG-guarded) — no change to scoring, banding, or attach behavior. (bead enhancedchannelmanager-at41p)
- **Event Sync rule editor reworked into a 4-step wizard (Scope → Matching → Behavior → Review) with a persistent, step-reactive live-impacts rail (bead m1s38.1, build 0085).** The editor replaces its prior single-scroll layout with four focused steps and a right-hand rail whose impact summary (groups, patterns, matches) updates as each step changes, so an operator sees the consequence of a choice before moving to the next step. The generic two-pane modal layout was extracted into shared modal-layout CSS primitives so future multi-step editors can reuse the same structure. No backend/API changes — editor UX only. (bead enhancedchannelmanager-m1s38.1)
- **Standard (conditions+actions) rule builder reworked into a grouped two-pane, Logic-first layout (bead m1s38.3, build 0085).** Consumes the same shared modal-layout primitives introduced for the Event Sync editor (above), grouping conditions and actions together behind an intent sentence that summarizes the rule in plain language, plus advisory Rule Analyzer chips surfaced inline while editing. (bead enhancedchannelmanager-m1s38.3)
- **Event Sync execution details now show an event_sync-aware summary (bead 7wuhd, build 0085).** The execution detail view for Event Sync runs now reports secondary streams evaluated, attached, already-attached, ambiguous, unmatched, and parse failures, including a distinct "fully in sync" state when there is nothing left to attach. Persisted via new execution columns (migration `0033`). (bead enhancedchannelmanager-7wuhd)
- **Confirmed rollback of an Event Sync attach run is now surgical whenever the journal proves it can be (bead sfysz, build 0070).** `POST /api/channel-pipeline/executions/{id}/rollback` with `confirm=true` previously always delegated to the snapshot full-replace when a pre-run snapshot existed — reverting Dispatcharr's own master-stream churn between run and rollback to stale pre-run stream ids that Dispatcharr never self-heals. Now, when the run's journal (category `event_sync`, `batch_id` = execution id) fully covers its attaches — no created entities, every modification a journaled stream attach, entry counts matching 1:1 — the rollback removes ONLY the stream ids the run added from each master's *current* stream list, preserving post-run churn, and returns `surgical_unmerge: true`. Anything short of proven coverage (standard-rule runs, mixed runs, legacy runs, a failed journal write) falls back to the snapshot restore exactly as before; the confirm gate and the standard-rule rollback path are unchanged. (bead enhancedchannelmanager-sfysz)
- **Every hit on the legacy `/api/admin/*` router now logs a deprecation WARNING, pending removal (bead d53lz, build 0062).** The legacy `/api/admin/*` user-CRUD router fully duplicates the canonical `/api/auth/admin/*` surface. Step 1 of a two-step deprecation: a middleware WARNING-logs each request to the legacy prefix (method, path, client IP, user) so operators and integrations can spot any remaining traffic before the router is removed in a later release. Behavior is otherwise unchanged — the legacy routes still work. (bead d53lz) (beads enhancedchannelmanager-3thyn / b3g0r / 7l7wi / juy2e / 9n08a / b2vf5 / jl7sb, build 0059).** A cluster of small consistency fixes from the UX audit. The tab navigation strip now scrolls horizontally with edge chevrons when tabs overflow the window, instead of clipping — keyboard Tab-focus auto-scrolls the focused tab into view (3thyn). Four tab headers migrated onto a new shared `PageHeader` component so title/subtitle/action rows are structurally identical across tabs (b3g0r / 7l7wi). Timestamps across tabs now use one unified time-format helper, so the same moment renders identically everywhere (juy2e). Status pills use a consistent outline style (9n08a). Two previously-unexplained UI elements gained explanatory tooltips: the edit-mode countdown timer (b2vf5) and the EPG priority tie-break behavior (jl7sb). (beads enhancedchannelmanager-3thyn / b3g0r / 7l7wi / juy2e / 9n08a / b2vf5 / jl7sb)
- **The Notification Center now collapses matching start/complete notification pairs into one entry (bead enhancedchannelmanager-ib2w3, build 0059).** Long-running operations previously left both their "started" and "completed" notifications in the list; a new grouping pass (`notificationGrouping.ts`) collapses each start/complete pair into the completed entry so the list shows outcomes, not lifecycle noise. (bead enhancedchannelmanager-ib2w3)
- **The Auto-Creation tab and all its UI text are now "Channel Pipeline" (bead enhancedchannelmanager-xwwe4, build 0047).** Phase 4 of the multi-phase rename (epic enhancedchannelmanager-3udrl) that started with the phase-3 canonical API route above. The frontend now calls the canonical `/api/channel-pipeline` route (the deprecated `/api/auto-creation` alias still works for any other integration, per phase 3); old bookmarked URLs (`#auto-creation`, `#settings/auto-creation`) still resolve correctly via a compatibility redirect. This is purely a rename — no functional/behavioral change to rules, executions, or settings, and all underlying data and Settings field names are unchanged. (bead enhancedchannelmanager-xwwe4)
- **Documentation for the Auto-Creation → Channel Pipeline rename is now complete (bead enhancedchannelmanager-cuktn, build 0048).** Phase 5 of 5 (FINAL) in the multi-phase rename (epic enhancedchannelmanager-3udrl). The user guide, API reference, README, architecture docs, runbooks, and style guide now consistently refer to "Channel Pipeline"; two historical ADRs (ADR-010, ADR-011) received terminology-note addenda rather than being rewritten. No functional change — this is documentation only. (bead enhancedchannelmanager-cuktn)

### Deprecated
- **`/api/auto-creation` and its 14 corresponding MCP tool names are deprecated in favor of `/api/channel-pipeline` and their `channel_pipeline`-named equivalents (bead enhancedchannelmanager-dl0kk, build 0046).** Part of the same phase-3 rename above. The old route and the 14 old MCP tool names (`run_auto_creation`, `create_auto_creation_rule`, etc.) continue to work unchanged — no immediate action needed from operators or existing scripts/agents — but they are hidden from the OpenAPI schema and will be removed in a future dated release once the phase-4 frontend UI rename ships and a follow-up removal bead is filed. (bead enhancedchannelmanager-dl0kk)

### Fixed
- **The standard rule builder no longer silently discards unsaved edits (bead m1s38.3, build 0085).** The header's ×/Escape close previously discarded a dirty rule with no warning, and the dirty-check itself missed deep edits to existing conditions/actions and to option/sort/scope fields, so some edits could be lost even when the discard guard did fire. Both are fixed: closing a dirty rule now prompts for confirmation, and the dirty-check covers every field the builder can edit. (bead enhancedchannelmanager-m1s38.3)
- **Event Sync manual runs no longer show a misleading "Created 0 channels" completion toast (bead utswf, build 0085).** Event Sync runs don't create channels — they attach streams to an existing master channel — so the standard-rule completion toast always read "0" and looked like the run had done nothing even when it attached streams successfully. (bead enhancedchannelmanager-utswf)
- **Event Sync execution details no longer show standard-rule counters that always read 0 (bead 7wuhd, build 0085).** The execution detail view previously reused standard-rule action counters, which have no meaning for Event Sync's attach-based execution and always displayed 0 regardless of outcome — replaced by the event_sync-aware summary described above. (bead enhancedchannelmanager-7wuhd)
- **The Event Sync Test Patterns panel no longer shows 'Parsed' for names the matcher would reject (bead hirm6, build 0070).** The panel's verdict was group-presence only, so `A vs B @ 45 Jul 06:00 PM ET` (day 45) or a custom pattern capturing a garbage month rendered as 'Parsed' while being unmatchable at preview/run time. The dummy-EPG batch preview response (still zero-write) now carries a matcher-level `event_sync_start_valid` flag computed from the matcher's own never-guess semantics (`services/event_sync_matcher.groups_would_build_start` — valid month name, hour ≤ 23, real calendar date), and the panel renders 'Parsed' only from that flag, with a distinct **Invalid date/time** state for captured-but-impossible dates alongside the existing **Incomplete date/time** state. (bead enhancedchannelmanager-hirm6)
- **The Event Sync rule editor no longer silently rewrites API-authored multi-pattern configs on resave (bead z4y4a, build 0070).** Opening and saving a rule whose `event_sync_config` was authored by hand or over the API used to keep only the first non-shipped shared pattern, keep only `patterns[0]` of each per-group override list, and re-add the built-in patterns to an all-custom selection. The editor now round-trips the full `patterns`/`group_patterns` arrays: an untouched save emits the saved arrays verbatim; pattern entries the UI cannot express (a second+ custom shared pattern, a group's second+ override pattern) are preserved unchanged behind the editable ones — surfaced with a read-only indicator, applied in the Test Patterns panel and preview, and never dropped or reordered; edited entries keep their API-authored names; and built-ins (recognized by verbatim equality, not name alone) are never silently re-added. UI-authored configs save exactly as before. (bead enhancedchannelmanager-z4y4a)
- **Stream-probe pause/resume is reachable again — the HTTP endpoints were missing since the v0.11 router split (bead vdrku, build 0062).** The stream prober has had integrated pause/resume support internally, but the `POST /api/stream-stats/probe/pause` and `/probe/resume` routes that expose it were lost when the probe routes moved into `routers/stream_stats.py`, leaving the capability unreachable from the UI, API, and MCP. The two routes are restored (and surfaced as MCP probe-lifecycle tools, see Added). (bead vdrku)
- **MCP `create_m3u_account` now actually applies `account_type` (bead yd6qh, build 0062).** The tool accepted an `account_type` parameter but never forwarded it to the backend, so every account created over MCP silently landed on the default type. Found and fixed during the 8-family parameter-drift sweep. (bead yd6qh)
- **Find Duplicates can no longer merge blind, and huge result sets no longer hang the modal (bead uahp6, build 0062).** Two safety/robustness fixes to the duplicates modal: a render-guard now disables the Merge action whenever the duplicate-group list fails to render (previously you could confirm a merge against groups you literally could not see), and the result list is virtualized so a scan returning thousands of duplicate groups renders incrementally instead of janking or hanging the tab. (bead uahp6) from a pattern compile error (beads enhancedchannelmanager-2uwi3 / 3gigl / yom3k / ock7c, build 0061).** A replacement-template failure (e.g. an out-of-range `$N` group reference during substitution) was previously logged under the same label as a pattern compile error, sending anyone debugging a silent name-transform down the wrong path. The two failure classes now carry distinct labels. The default swallow-and-continue behavior for other `safe_regex` call sites is unchanged — only the Channel Pipeline name-transform path opts into error propagation. (beads enhancedchannelmanager-2uwi3 / 3gigl / yom3k / ock7c)
- **Access logging and metrics now cover auth-rejected requests, and JSON log lines are no longer emitted twice (bead enhancedchannelmanager-cng0d, build 0059).** Two observability defects. **(1) Middleware ordering:** the observability middleware (structured `ecm.access` log line, Prometheus sample, `X-Request-ID` header) was registered first and therefore ran innermost, so requests the auth middleware rejected with 401/403 short-circuited before ever reaching it — no access line, no metrics sample, no request ID on exactly the traffic you most want visible. It is now registered last (outermost, per Starlette's prepend semantics) so every request is instrumented, including auth rejections. **(2) Duplicate log lines:** enabling JSON logging left the import-time `logging.basicConfig` console handler on the root logger alongside the JSON handler, double-emitting every record; the plain console handler is now removed (narrowly — file handlers and test-capture handlers are left alone). (bead enhancedchannelmanager-cng0d)
- **Stats-tab toggle CSS no longer leaks into other components (bead enhancedchannelmanager-p5f70, build 0059).** The Stats tab's group-by toggle styles were defined with class names generic enough to restyle unrelated toggles elsewhere; they are now scoped to the Stats panel. (bead enhancedchannelmanager-p5f70)
- **The M3U Changes tab's summary count now says which time range it covers (bead enhancedchannelmanager-fzny4, build 0059).** The header's "N changes" figure is bounded by the Time Range filter while the table footer's total is not — the two could legitimately disagree with no explanation. The header count now names its range inline (e.g. "12 changes (last 24 hours)") with a tooltip explaining the difference. (bead enhancedchannelmanager-fzny4)
- **MCP rule tools now accept stringified booleans/numbers/lists nested in `conditions`/`actions` (bead enhancedchannelmanager-3aczd, GH #600, build 0056).** GH #600 reported that `create_auto_creation_rule`/`update_auto_creation_rule` couldn't deliver `loose_name_match: true`, `min_score: 0.75`, or `target_channel_in_group: [912, 913]` to the backend — blocking the documented scored-fuzzy merge path from MCP. Live reproduction showed ECM's MCP server and backend handle correctly-typed nested values fine; the failure occurs when the *MCP client* (some LLM-driven clients do this) sends nested values as JSON strings (`"true"`, `"0.75"`, `"[912, 913]"`). Top-level tool params are typed and get lax coercion at the FastMCP/Pydantic boundary, but values nested inside the untyped `list[dict]` params rode through verbatim and failed the backend's strict type validation — or worse, a stringified `"false"` for a condition's `negate` was truthy and silently *inverted* the condition. The MCP tool boundary now coerces the known typed fields nested in `conditions`/`actions` (`create_channel_pipeline_rule`/`update_channel_pipeline_rule` and their deprecated `*_auto_creation_*` aliases) with the same leniency the top-level params already get: `"true"`/`"false"` → boolean, numeric strings → numbers, JSON-encoded lists → lists (including string elements, e.g. `["912", "913"]`). Field-aware — string-typed params like `name_template` are never touched, and unparseable values pass through unchanged so the backend's clear validation errors still surface. (bead enhancedchannelmanager-3aczd, GH #600)
- **Checking an empty group in the Channels pane's group filter dropdown now actually shows it (bead enhancedchannelmanager-5gvnb, build 0049).** The group filter dropdown always listed every channel group, including empty ones (with a `(0)` count badge), but checking an empty group had no visible effect — the group's section (and its drop target for dragging streams over from the Streams pane) only rendered if the separate "Show Empty Groups" toggle, in a different menu and off by default, was also enabled. From the user's perspective this looked identical to the group simply not being selectable. `shouldShowGroup()` now treats an empty group being explicitly checked in the filter dropdown as its own override, independent of the "Show Empty Groups" preference, so selecting it makes it appear immediately as a valid drop target. (bead enhancedchannelmanager-5gvnb)

- **Cross-instance sync no longer refuses an entire plan just because the source has one duplicate name (bead ia4pm, build 0044).** A dry run or apply could report "0 items processed" for the WHOLE sync — not just the affected item — whenever the source had two entities sharing a name in a name-unique category (M3U accounts, channel groups, channel profiles), e.g. two channel groups both named the same thing. Root cause: the shared preflight gate (`backend/dbas/preflight.py`, used by both backup/restore AND cross-instance sync) refuses the whole plan on any such collision — correct for a one-shot restore's atomic snapshot semantics, but wrong for a continuously-converging sync, where the documented behavior is to skip just the ambiguous item and keep going (as it already does for an ambiguous no-channel-number collision). Sync now degrades a source-side name collision to a per-item CONFLICT: it keeps the first occurrence, flags the rest as excluded, and remaps any channel that referenced the excluded entity onto the surviving one's destination id — so everything else still syncs normally. Backup/restore's atomic all-or-nothing refusal is unchanged. Also fixes a related reporting bug where a dry-run's real per-item conflicts were always hidden behind a hardcoded 0 failed-count, now summed correctly and mentioned in the dry-run summary message. (bead ia4pm)
- **Cross-instance sync dry-run/apply Task History summary badges now show real per-category item counts instead of a hardcoded "1 total / 1 ok" (bead zu2de, build 0043).** `DbasSyncTask` reported the whole sync run — dry-run preview or apply — as one hardcoded task-level unit regardless of how many channels, groups, profiles, M3U accounts, or EPG sources it actually touched, so Task History's quick-glance badges always showed "1 total / 1 ok" even when a sync spanned dozens of items across categories, misleading an operator into thinking almost nothing synced. The real per-category create/update/skip/fail counts were already computed correctly for the human-readable notification message, just never surfaced in the `TaskResult` badges. A new `SyncCounts` NamedTuple and `_counts_from_report()` method now sum those real counts, so the badges match what the notification text already said. (bead zu2de)
- **Stream probing (and auto-creation) no longer gets permanently stuck on stale Dispatcharr credentials after a settings-page save (bead snryv, build 0042).** Saving a new Dispatcharr URL/username/password/API key only recreated the low-level HTTP client singleton — the already-running `BandwidthTracker`, `StreamProber` (backing every "Probe Now" action and the scheduled `stream_probe`/`failed_stream_reprobe`/`black_screen_scan` tasks), and the `AutoCreationEngine` each captured their OWN Dispatcharr client at process startup and never picked up the correction, so every probe and auto-creation run kept hitting Dispatcharr with the old credentials — 401ing forever — until an operator separately discovered and clicked **Settings → Restart Services**, or restarted the container. Saving connection-relevant settings now automatically rebuilds and reconnects all of these in the background (fire-and-forget, so the save itself still returns immediately even if Dispatcharr is briefly slow/unreachable). Also fixes several issues the rebuild introduced: an in-progress probe being silently hard-cancelled by an unrelated settings save now logs a warning; a failed rebuild is now logged at WARNING instead of INFO; concurrent rebuild calls (a manual restart racing an automatic one) are now serialized so a live tracker/prober can no longer be orphaned; and the tracker/prober singletons are now cleared to `None` (not left stopped-but-reusable) when Dispatcharr is unconfigured. (bead snryv)
- **Bulk EPG assignment now matches numbered network variants correctly instead of losing to the base network's HD entry (bead gl3sd, build 0038).** A channel like `ESPN 2` (space-separated) was matching `ESPN HD` instead of the correct `ESPN2` entry. Root cause: the token-overlap scoring signal (bd-a6445) tokenized "ESPN 2" and "ESPN2" inconsistently — the space-separated channel name split into `["espn", "2"]` with the lone digit dropped by a length filter, while the guide's fused "ESPN2" stayed one token — so the two forms shared zero tokens even though they already normalize to the identical match key. The correct exact match was penalized for an "unmatched" token while the merely-prefix-matching `ESPN HD` collected a full token-overlap bonus plus the HD-variant bonus and won. A standalone digit token now fuses onto its immediately preceding token before scoring (mirroring how the match-key normalization already treats them), with guards so ATSC subchannel markers (`WTVT-DT2`, `KOCE-DT2`) and quality-suffix-adjacent digits aren't affected — the a6445/m4hp1 subchannel tie-break behavior is unchanged. Likely fixes the same class of mismatch for other fused-vs-spaced numbered variants (`FS1`/`FS 1`, etc.). EPG-match-only — no effect on auto-creation. (bead gl3sd)
- **Bulk EPG assignment now matches channels named after a network's short acronym, and match results no longer go stale after an EPG source's data changes (beads 6rz70 / 41pcv, build 0037).** Two fixes. **(1) Acronym matching:** many guide sources encode a network's canonical short id as a parenthetical inside the `tvg_id` (e.g. `FamilyEntertainmentTelevision(FETV).us`, `Home&GardenTelevision(HGTV).us`, `OprahWinfreyNetwork(OWN).us`) or inside the display name. The matcher previously flattened the whole `tvg_id` into one string, burying the acronym mid-word where prefix matching could never find it — so a channel literally named after the acronym (`FETV`, `HGTV`, `OWN`) could get zero candidates, or only a wrong low-confidence one. The matcher now extracts that parenthetical acronym from both `tvg_id` and the display name and matches it exactly against the channel's normalized name (new `match_type="acronym"`, weighted equal to an exact name match); the existing country/league/token-overlap scoring still ranks among acronym matches the same way it does for any other match type. **(2) Stale match cache:** the `/api/epg/match` response cache (5-minute TTL) was keyed only on the requested channel/source ids and source priority order — never on the EPG source's actual data — so editing an EPG source in place (e.g. swapping its feed URL) or letting a scheduled refresh pull new data left a same-selection match request serving the pre-change response, TVG-IDs and all, until the cache aged out. Updating, deleting, or completing a refresh of an EPG source now busts the cached match responses immediately. EPG-match-only — no effect on auto-creation. (beads 6rz70, 41pcv)
- **Bulk EPG assignment now honors the selected EPG source and ranks the clean primary feed over noisy subchannels (bead m4hp1, build 0036).** Two fixes. **(1) Selected EPG source is now enforced:** the modal's chosen source(s) were forwarded to Dispatcharr as a query filter but never enforced in ECM, so if Dispatcharr ignored it you got candidates from every source. ECM now drops any candidate that isn't from a selected source. **(2) Ranking prefers the clean, primary-feed entry:** for over-the-air call-sign matches that previously tied, the matcher was picking longer/noisier entries — e.g. `70 | ABC: KABC Los Angeles` matched `TW BAK KABC B/O (ABC)`, `75 | MY: KCOP Los Angeles` matched `KCOP-DT4 (HEROICN)`, and `77 | PBS: KOCE Los Angeles` matched `KOCE-DT2 (PBS)` over `KOCE-DT (PBS)`. The misleading name-length term was removed from both the confidence score and the sort tiebreak; the token score is now **precision-aware** (it penalizes extra EPG tokens like `TW BAK`/`Stream`/`HEROICN`/`THENEST`, not just rewards shared ones); and a **main-subchannel tiebreak** prefers the primary feed (`-DT`/no-suffix) over diginets (`-DT2`/`-DT3`/`-DT4`) when candidates are otherwise tied. These cases now resolve to `KABC-DT (ABC)`, `KCOP-DT`, and `KOCE-DT (PBS)`; the prior WTVT/KEYE fixes and the exact/country/league hierarchy still hold. EPG-match-only — no effect on auto-creation. (bead m4hp1)
- **Bulk EPG assignment now ranks matches by shared call-sign + network/affiliate tokens, not coincidental name length (bead a6445, build 0035).** A channel like `64 | FOX: WTVT Tampa` was matching `WTVT-DT2 (MVIES)` ahead of the correct `WTVT-DT (FOX)` — both share the call sign `WTVT`, but the wrong subchannel happened to normalize to the same character length as the channel name, and the matcher's confidence score rewarded that coincidental length (+20) while nothing rewarded the shared `FOX` network token. The confidence scoring now adds a **token-overlap signal** (12 pts per shared meaningful token, capped) and reduces the name-length-similarity weight (20 → 10), so an EPG entry that shares more of the channel's real tokens — call sign *and* network/affiliate/city — outranks one that shares only the call sign. Subchannel markers (`DT`, `DT2`) and bare channel numbers are excluded from the token set so they can't create spurious overlap. The existing match hierarchy (exact / country / league preferences) is unchanged and still dominates. EPG-match-only — no effect on auto-creation. (bead a6445)
- **Bulk EPG assignment now ignores channel-number noise in names and uses ECM's one shared normalization engine (bead xxzxe, build 0034).** Bulk EPG matching was missing obvious matches because its name-cleaning was a separate, weaker copy that only stripped a channel number when it was pipe-delimited (`535 | ESPN`) — so `201 ESPN HD` normalized to `201espn` and never matched the guide's `ESPN`, and `ESPN (201)` became `espn201`. Matching now routes through the shared `NormalizationEngine` (the same path auto-creation and Test Rules use — one engine for everything), and the engine's channel-number stripping was extended to also handle number-then-space prefixes (`201 ESPN` → `ESPN`) and trailing parenthesized numbers (`ESPN (201)` → `ESPN`), unconditionally for all consumers. Names whose number is part of the name (`Fox 5`, `CBS 2`, `WGN 9`) and leading-digit brands (`3ABN`, `9Gem`) are preserved. Timezone stripping (needed so `ESPN East` matches the guide's `ESPN`) is applied **only in matching mode**, so auto-creation still keeps `ESPN East`/`ESPN West` as distinct channels. The EPG match path is also ~5–10× faster (shared engine + per-batch memo vs the old per-entry regex loop). The duplicate client-side matcher (`frontend/src/utils/epgMatching.ts`) was already dead code and is slated for removal separately. (bead xxzxe)
- **Scheduled auto-creation is now OPT-IN (disabled by default) — it no longer starts firing autonomously after an upgrade (bead i2xad, build 0033).** ADR-011 Phase 2 (build `v0.17.6-0002`) turned auto-creation from a button-only (MANUAL) task into a self-firing ~60-second task **seeded enabled**, and shipped a startup migration that flipped every existing instance's persisted `auto_creation` task `manual → interval` while leaving it enabled. The combined effect on upgrade was that auto-creation began running on its own on every instance — operators reported it "kicking off all of a sudden" on production. The decoupling architecture itself is fine and is **preserved** (the 60s interval cadence, the AUTO-FIRE GUARD, the GH #473 created-channel cap, the exo4j circuit breaker, and `run_on_refresh` semantics are all unchanged); the regression was purely the **default**. Now the `auto_creation` task ships **disabled by default** (`default_enabled = False`): a fresh install never auto-fires until an operator turns the task on with the single **Enabled** toggle in Settings → Tasks (the 60-second schedule underneath stays in place, so one toggle is all it takes; the choice persists across restarts). A one-time, idempotent startup migration also **auto-corrects already-affected instances** by disabling the auto_creation task once on upgrade — so instances already flipped-and-enabled stop firing as soon as they take this build, with no manual step. (This corrective is intentionally a `_run_migrations` data migration rather than an Alembic revision — the smart-bootstrap fast-path would silently skip a data-only Alembic migration on exactly the upgraded installs that need it — and it is gated by a persisted one-time marker so it never re-disables an operator who later opts back in.) **Operator note:** the corrective disable is unconditional — an operator who had *deliberately* enabled scheduled auto-creation will find it disabled after this upgrade (we cannot distinguish a deliberate enable from the earlier migration-driven enable) and should simply re-enable it once under Settings → Tasks; the choice then persists. Supersedes the ADR-011 §D2 "enabled by default" rollout decision (ADR-011 amended). (bead i2xad)
- **The "Where can ECM send backups?" prompt no longer appears on login — it now fires only when a user actively configures backups (bead s5a3o, build 0032).** The first-run backup-destination choice previously mounted on every login for anyone who hadn't answered it, gated only on a per-browser flag with no relation to whether backups existed — which wrongly implied backups were already running automatically. The unconditional login mount is removed; the prompt now appears only when the user takes a real backup-configuration action: **adding/saving a cloud upload target** or **creating/enabling a backup schedule** (whichever comes first). The existing "answered once → never re-prompt" behavior is preserved (so operators who already responded are unaffected), the safe `lan_friendly` default still applies on dismissal, and the choice remains changeable under Settings → Security. No backend change was needed. (bead s5a3o, successor to nngkg)
- **Gracenote ID retrieval from EPG now reads `<gnid>` (modern XMLTV) with `<lcn>` as a legacy fallback (bead nq9mg, build 0031).** The "Fetch Gracenote IDs" action silently returned nothing because it only looked for an `<lcn>` child element of each XMLTV `<channel>`, and upstream EPG sources have stopped populating `<lcn>` — they now carry the Gracenote station id in a `<gnid>` element. Both the single (`GET /api/epg/lcn`) and batch (`POST /api/epg/lcn/batch`) parse helpers in `backend/routers/epg.py` now prefer `<gnid>` and fall back to `<lcn>` for legacy EPGs (gnid wins regardless of document order; whitespace-only values are ignored). The API response key is unchanged (still `lcn`), so the channel `tvc_guide_stationid` write path is unaffected. (bead nq9mg)
- **Stats tab "Bitrate by Provider" undercounted concurrent sessions and hid HDHomeRun/local-tuner traffic (bead oj02b, build 0030).** Two distinct defects in the per-provider bitrate chart. **(1) Concurrent-session SUM bug:** when several genuinely-distinct sessions watched the *same channel* in the same poll tick, the aggregation collapsed them with `MAX(bytes_delta)` and reported only the single largest stream — e.g. five concurrent 7/4/7/4/20 Mbps sessions on one provider showed **20 Mbps instead of 42**. The `_distinct_provider_poll_subquery` is now a two-stage aggregation: stage 1 dedupes multi-client-per-session overcount (`MAX` grouped by session), stage 2 `SUM`s `bytes_delta` across the distinct sessions of a channel-tick while keeping **one** poll interval per tick (summing the interval too would have reported the per-session *average*, ~8 Mbps, not the aggregate). The provider watch-time and heatmap endpoints are unaffected. **(2) HDHomeRun/local-tuner traffic mislabeled:** streams with no M3U account (HDHomeRun and other local tuners) resolved to `provider_id = NULL` and were folded into the single "Unknown" bucket, so a concurrent HDHomeRun session never appeared as its own line. Such telemetry rows are now tagged at ingest with a synthetic local-tuner identity (negative sentinel `provider_id`, no schema migration needed) and surface as their own **"HD HomeRun"** series; genuinely-unresolved providers still group under "Unknown". Tagging is forward-only — pre-existing NULL rows stay "Unknown" (they can't be reliably reclassified after the fact). (bead oj02b)
- **Test-validity audit remediation — frontend, wave 3 (epic ulp7q, build 0022).** Internal-only. Implemented the 8 `expect(true).toBe(true)` placeholders in `useAuth.test.tsx` as real auth-state tests (redirect / spinner / authed / login), deleted 3 no-op "tested via integration" markers, and turned two vacuous `toBeDefined` hook tests into real ones (auto-refresh timer re-fire; `isOperationPending` true→false flip). Fixed 3 hard fakes in the ffmpegBuilder suites (asserting behavior that either didn't exist or couldn't fail) and tightened ScheduleEditor role-count + ffmpegBuilder substring assertions to exact values. vitest 1662 passed, 0 `act()` warnings. (epic ulp7q, beads .5/.6)
- **Test-validity audit remediation — backend, wave 2 (epic ulp7q, build 0021).** Internal-only. The ffmpeg validate/generate-command/capabilities tests mocked the very functions under test (so the real validation/command-generation path had zero coverage) — rewritten to exercise ECM's real logic; two now-dead `isinstance(result, dict)` test-accommodation branches removed from `backend/routers/ffmpeg.py` (behavior-preserving). Swept ~70 router passthrough tests that asserted only `status_code == 200` to also assert the forwarded body + call args (a handler mutated to `return {}` no longer passes). Fixed integration `status_code in (200,…,500)` tuples that swallowed error paths, two literally-cannot-fail tests (`assert True` / commented-out body), and backend-unit hygiene (real-session row assertions, deterministic frozen-time schedule checks, croniter fallback coverage). Backend suite 6201 passed. (epic ulp7q, beads .2/.3/.4/.7)
- **Test-validity audit remediation — MCP, wave 1 (epic ulp7q, build 0020).** Internal-only, no user-facing change. A suite-wide audit for "fake tests" (pass but don't verify behavior) found two MCP tests that had gone *signal-blind* — they no longer exercised the code path they claimed to. Fixed: `clear_auto_created` tests now supply a real confirm-token and drive the actual clear path (asserting exact "Cleared N ... in M group(s)" wording) instead of silently hitting the preview path; added a test pinning that `match_channels_epg` omits the deprecated `source_order` from the request body. Plus a sweep of ~13 weak MCP assertions (bare-digit / "Error"-only / source-text greps) tightened to assert the tool's built request token or labelled output. (epic ulp7q, beads .1/.8)
- **Frontend test suite is now React `act()`-warning clean (bead bmtoc, build 0019).** Internal-only, no user-facing change: cleared ~88 `act()` async-state warnings across 8 test files (`BackupRestoreSection`, `PreviewStreamModal`, `M3UChangesTab`, `BackupRestoreModal`, `DbasRestoreModal`, `BandwidthPanel`, `EnhancedStatsPanel`, `WatchHistoryPanel`) by awaiting the trailing mount-effect state updates with `waitFor` (the same pattern as the earlier `PopularityPanel` fix). No production code touched, no assertions weakened, all 1665 tests still pass — the warnings no longer bury real signal in CI output. (bead bmtoc)
- **Frontend dev-quality cleanup (beads 2gg44/v7oql/qpw9a, build 0018).** Internal-only, no user-facing behavior change: (1) `.dist_root` (a local frontend build-output dir) is now git-ignored and excluded from eslint, so a stray local build can no longer flood root-level `npm run lint` with errors from minified bundles; (2) removed the `act()` async-state warnings from `PopularityPanel.test.tsx` by awaiting the settling state updates (all tests still pass); (3) memoized the `epgSourcePriority` map in `EditChannelModal` for consistency with `BulkEPGAssignModal`. (beads 2gg44 / v7oql / qpw9a)

### Security
- **Two latent auth hardening fixes found while investigating GH #546's silent backend crash (bead enhancedchannelmanager-0gt2i, build 0052).** Found alongside the exit-path diagnostics above (see Added); neither is confirmed as GH #546's root cause, but both are real bugs worth closing on their own merits. **(1) `_get_secret_key` no longer silently swallows unexpected errors:** the JWT secret-key loader's bare `except Exception` — which falls back to an ephemeral random secret (invalidating every existing session) on failure — is narrowed to `(ImportError, OSError)`, the failure modes it was actually written to handle; the random-secret fallback now logs loudly (ERROR with traceback, plus a repeated WARN that all sessions are now invalid), and any other exception type propagates as a visible `500` instead of a silent, unlogged auth lockout. **(2) `auth/settings.py` load/save/`get_jwt_secret_key()` are now serialized by a module-level `threading.RLock`:** two threads/coroutines racing to first-generate the JWT secret could each generate and persist a *different* random secret, so requests validated against one thread's secret would 401 against the other's — a divergent-secret race consistent with (though not proven to be) the reported concurrency-shaped crash. (bead enhancedchannelmanager-0gt2i)
- **The MCP static-key service principal can no longer bypass the per-field admin settings gate by restoring a backup (bead enhancedchannelmanager-6n76m, build 0051).** The kgz3k field-level gate on `POST /api/settings` (build 0026, below) deliberately treats the MCP service principal as non-admin, so the MCP key cannot rewrite outbound base URLs (SSRF sinks), integration secrets, or the GH #473 safety caps. But the backup-restore endpoints were gated by the generic admin check (which the MCP principal passes — it carries `is_admin=True`) and write the settings blob **wholesale** with no per-field check — so an MCP-key holder could flip every admin-only field, and restore non-redacted credentials from a legacy ZIP, simply by driving a restore (the MCP server ships a `restore_backup` tool that calls exactly this path). The three ECM-settings-writing restore endpoints (`POST /api/backup/restore`, `/restore-saved`, `/restore-yaml`) now use a new `RequireHumanAdminIfEnabled` dependency that rejects the MCP service principal with `403` while still admitting a human operator admin; the MCP `restore_backup` tool surfaces the rejection as a clean error. Every other admin-gated route the MCP key legitimately drives (channel/stream/pipeline management, backup **creation**, and the guardrailed dry-run-by-default DBAS restore, which applies denylist-filtered settings to the Dispatcharr upstream rather than ECM's own settings file) is unchanged. (bead enhancedchannelmanager-6n76m)
- **`POST /api/settings` outbound URLs/keys are now admin-gated and SSRF-validated on save, closing an authenticated SSRF + credential-exfiltration hole (bead kgz3k, build 0026).** The settings-save endpoint had NO admin dependency, so any authenticated caller — including the static MCP API key — could rewrite outbound base URLs (Dispatcharr / Emby / Plex / Jellyfin) and integration secrets, and the existing `_sanitize_base_url` SSRF guard only ran in the test-connection endpoints, never on save. A malicious base URL (e.g. `http://169.254.169.254/`) was therefore stored verbatim and the runtime media-server pollers happily GET'd it with the stored API key every few seconds — turning the save endpoint into an SSRF primitive and a key-exfiltration channel; the `discord_webhook_url` (POSTed verbatim by the notifier) was a worse POST-SSRF sink. Three fixes: **(1) field-level admin gate** — a non-admin (when auth is enabled), including the MCP service principal, is rejected `403` if the request attempts to *change* any admin-only field (connection, outbound URLs, secrets, notification credentials) while still being allowed to save personal preferences (theme / date format / timezone); **(2) SSRF-on-save** — every changed, non-empty outbound base URL is routed through the canonical mode-aware SSRF chokepoint (`security/ssrf.py`, respecting `ssrf_outbound_mode`: LAN-friendly allows RFC1918, public-only blocks it; loopback / link-local / IMDS / ULA / CGNAT / multicast denied in both modes) plus `_sanitize_base_url` for scheme allowlisting and path stripping, rejecting blocked hosts with `400` (an unresolvable LAN host is still saveable — the runtime client re-validates before connecting — but a host that resolves to or is literally a denied address is refused); **(3) Discord webhook allowlist** — `discord_webhook_url` is validated against the Discord webhook host/path allowlist on save (path preserved, not sanitized), rejecting non-Discord hosts with `400`. Empty values remain allowed (operator disabling an integration). (bead kgz3k)
- **MCP/AI guardrail pass — auto-creation can no longer silently overwrite manually-managed channels, destructive MCP tools require confirmation, bulk ops are capped, and every AI mutation is now audited (epic 7afgg → beads orzck/onazy/p8fx9/vp1rx, build 0016).** Closes a class of "the AI did the wrong thing" data-loss reports. **(1) Manual-channel isolation (orzck):** auto-creation matched a stream to an existing channel purely by name (exact / normalized / core-name / call-sign / number-prefix) across *all* channels with no `auto_created` filter, so an `if_exists=merge` rule could silently adopt a hand-built ("regular") channel as its merge target and overwrite its name/streams/filters. The single resolution chokepoint (`_find_channel_by_name`) now refuses to adopt an `auto_created=False` channel unless the firing rule explicitly sets the new `allow_manual_channel_merge` flag (default off, journaled when used); a missing `auto_created` key is treated as manual/protected (ADR-010 convention). Manual channels are *not* stripped from the lookup maps (that would cause duplicate creation) — they are protected at the decision point, so a collision now creates a new auto channel instead of clobbering yours. **(2) Confirmation on destructive MCP tools (onazy):** `delete_channel` / `delete_m3u_account` / `delete_auto_creation_rule` / manual-channel `update_channel` now preview-then-`confirm`; `bulk_delete_channels` / `clear_auto_created` / `bulk_merge_duplicate_channels` require a server-computed `confirm_token` recomputed from the live target set on the apply call (closing the preview→apply TOCTOU window — a bare boolean is theater for an LLM). `bulk_merge_duplicate_channels` loudly flags a manual target. **(3) Batch caps (p8fx9):** settings-driven soft caps (force the token) and hard caps (refuse with a narrow-your-selection error — never silent truncation) on the bulk operations. **(4) AI-mutation audit (vp1rx):** a `mutation_source` dimension on the journal (`mcp_ai` / `ui` / `auto_creation` / `scheduler`) stamped principal-based at the request layer, with a pre-mutation `before_value` and a shared `batch_id` for forensics and recovery; dry-run/preview writes zero journal rows. Migrations `0025`–`0027`. (epic 7afgg)

### Changed
- **Internal: renamed the Auto-Creation backend module/class layer to Channel Pipeline in preparation for a full feature rename (bead enhancedchannelmanager-mteyy, build 0045).** Phase 2 of a multi-phase rename (epic enhancedchannelmanager-3udrl) — backend Python modules, ORM classes, and internal identifiers renamed from `auto_creation_*`/`AutoCreation*` to `channel_pipeline_*`/`ChannelPipeline*`. Zero behavior change: the API route, MCP tool names, database table names, and the operator-facing UI are all unchanged in this phase — those are separate, later phases. No action needed from operators. (bead enhancedchannelmanager-mteyy)
- **"Assign EPG" now lets you choose which EPG sources to match against *before* it runs, instead of auto-matching against every source on open (bead 0m0nu, build 0028).** Choosing **Assign EPG** from the channel three-dot menu previously opened straight into an analysis that — unless you'd narrowed the source filter — matched the selected channels against *all* active EPG sources; on a large guide (e.g. a full US OTA feed with tens of thousands of entries) that scan is slow enough to look like the match "hung" and returned nothing. The modal now opens to a **configure step**: a checkbox list of your active EPG sources (each with its entry count, plus Select All / Clear) and a **"Match N Channels"** button. Matching runs only when you click it; results then land on the same review step as before, and the review step's source dropdown still drives a re-analyze. (bead 0m0nu)
- **`[EMBY-RESOLVER]` per-call and per-session diagnostics moved from INFO to DEBUG (bead 69avc, build 0028).** The Emby user-resolver logged a `resolver_call` line on every resolution attempt and a `session_inspect` line *per cached Emby session* (N lines per call) at INFO, flooding production logs during normal playback. Both are now DEBUG. The lower-frequency forensic lines kept for "Emby shows User #0" triage — `tier_match` (one line per successful attribution) and the `no_server_ip` misconfig early-return — remain at INFO. (bead 69avc)
- **EPG auto-match and channel-edit EPG search now honor each EPG source's configured priority (epic s5vyz, build 0017).** EPG source priority — set by drag-reordering sources in the EPG Manager — was previously a no-op in matching. (1) **Auto-match (`POST /api/epg/match`):** when two EPG candidates score within a 5-point confidence band, the one from the higher-priority source now wins (a *banded tiebreaker* — it never overrides a clearly-better match from a lower-priority source, and the confidence score itself is never altered). Priority is resolved **server-side** from the EPG source records (single source of truth), and a priority reorder now correctly busts the match cache. (2) **Channel-edit + bulk-assign EPG search:** results are ordered so higher-priority sources surface first (stable within a source — existing relevance order is preserved), applied *before* the result-list cap so a preferred-source match can no longer be truncated out of view. A latent bug where the existing (unused) `source_order` plumbing indexed a `list` as a `dict` is removed in the process. (epic s5vyz)
- **Auto-creation rules now default to matching existing channels within the rule's target group only, and existing rules are migrated onto that safe default (bead orzck, build 0016).** Previously the existing-channel name lookup defaulted to group-agnostic (`match_scope_target_group=False`), which let a same-named channel in an *unrelated* group be adopted as a merge target — the amplifier behind cross-group "bleed." The default is now group-scoped, and migration `0026` flips existing rules (left at `False` by the `0002` backfill) onto the scoped behavior. **Operator-visible:** a rule that genuinely relied on cross-group matching must now set `match_scope_target_group=False` explicitly; the manual-channel gate above is the backstop in either case. (bead orzck)

### Deprecated
- **`source_order` on `POST /api/epg/match` is deprecated and ignored (epic s5vyz, build 0017).** EPG source priority is now resolved server-side, so a client-sent `source_order` is no longer needed; any value supplied is ignored and logs a deprecation warning. The frontend and MCP `match_channels_epg` tool no longer send it. The field is scheduled for removal from the request contract in **v0.19.0**. (epic s5vyz)

### Added
- **Journal now shows *who* made each change — UI, AI (MCP), Auto-creation, or Scheduler — with a per-source filter (beads vp1rx/go1ka/t44t2, build 0016).** The journal/activity view renders a source badge on every entry and a "Source" filter wired to the new `GET /api/journal?mutation_source=` parameter, so an operator can see (and isolate) exactly which mutations an AI client made versus the UI, the auto-creation pipeline, or a scheduled task. Scheduled-task mutations are attributed `scheduler` via the task-engine dispatch chokepoint. (beads vp1rx / go1ka / t44t2)
- **DBAS backup/restore over MCP — trigger backups (incl. encrypted) and restore a saved artifact from an MCP client (bead 0wmeg, build 0015).** The MCP server previously only exposed the legacy backup path. It now has `create_dbas_backup` (triggers the v0.18.0 `dbas_backup` task — redacted by default, or an **encrypted** cred-carrying backup when given a passphrase + `acknowledge_unrecoverable`) and `restore_dbas_backup_saved` (restores a saved DBAS artifact by filename, **dry-run preview by default**, apply on explicit confirm, passphrase for encrypted artifacts). A new admin-gated `POST /api/backup/restore-dbas-saved` backs the restore tool — it resolves the filename under the backups directory with the same inlined path-containment guard the saved-restore path uses, then runs the `dbas_restore` task against the on-disk file (never deleting the operator's saved backup). **The passphrase is never logged, echoed in a tool result, or returned in any response** — enforced and tested on both the backend and MCP layers. (bead 0wmeg)

### Security
- **v0.18.1 cross-instance sync — SSRF-guarded remote-client factory, internal substrate (bead i39wu/1t3al, build 0008).** The factory that builds a Dispatcharr-B client from a `SyncTarget` row (`backend/tasks/dbas_sync_client.py`) routes **every** outbound request through the SSRF chokepoint via a custom httpx transport: `validate_outbound_url` at execute time (resolve-then-connect-by-IP, closing the DNS-rebinding window), the original hostname preserved for TLS SNI + `Host:`, and redirect hops re-validated (https→http downgrade refused). Credentials are Fernet-decrypted only at call time and never logged; a fire-time freshness helper aborts a stale/revoked/rotated credential (mirrors the cloud-target contract); `insecure=true` wires verify-skip + a per-cycle audit helper. The CI chokepoint guard (`test_ssrf_chokepoint_guard.py`) was **extended to scan the sync module** (proven red-before-green) so no raw outbound call can bypass the chokepoint. No new crypto. (bead i39wu/1t3al)

### Fixed
- **DBAS restore/sync: a failure late in an apply could leave EPG sources and stream profiles stranded on the destination instead of rolling them back (bead v1uz9, build 0014).** The compensating-rollback delete map (`restore_orchestrator._delete_dispatch`) registered undo-deletes for M3U accounts, channel groups, channel profiles, channels, streams, and users — but **not** EPG sources or stream profiles. So a restore (or a cross-instance sync) that failed *after* those categories were created could only reach `FAILED_ROLLBACK_INCOMPLETE`, leaving residue on the target. Both now have compensators (a new `DispatcharrClient.delete_stream_profile`, endpoint live-confirmed; `delete_epg_source` already existed), so a late-step failure rolls back cleanly (`PARTIAL_FAILED_ROLLED_BACK`, target left as it started). Surfaced by the cross-instance sync test harness (bead 46pkq). (bead v1uz9)
- **DBAS restore: a channel with no channel number that matched an existing no-number channel was silently skipped, hiding a real second channel (bead i39wu/kcxie, build 0010).** The channels importer keyed on `(name, channel_number)` and treated a `(name, null)` ↔ `(name, null)` match as `ALREADY_EXISTS_IDENTICAL` (silent skip) — but a null number is not a stable identity, so a genuinely different second "ESPN" (no number) would never be created and the operator saw a clean no-op. Now an ambiguous null-number match is surfaced as `CONFLICT` (failed-with-reason in the restore report), never a silent skip; a non-null channel-number equality match is unchanged. Fixed uniformly (it was a latent one-shot-restore bug; under continuous sync it would have been silent recurring divergence). (bead i39wu/kcxie)

### Added
- **v0.18.1 cross-instance sync — operator UI for sync targets (bead i39wu/nnl9s, build 0013).** The first operator-facing surface for cross-instance sync: a **Cross-Instance Sync** card in **Settings → Backup & Restore** to add/edit/delete a Dispatcharr-B sync target, see its last-sync time + tri-state status, flip the **enable/disable kill switch**, and run a sync. "Sync now" runs a **dry-run preview first** (no changes); only after a successful preview does an **Apply** appear, behind an explicit source-wins-overwrite confirmation. The card prominently states the two load-bearing semantics: B is a **managed replica** (edits on B are overwritten by A) and **credentials are not synced** (re-enter them on B). Admin-gated. The sync engine itself remains gated on a reachable second Dispatcharr for live validation. (bead i39wu/nnl9s)
- **v0.18.1 cross-instance sync — staleness observability (bead i39wu/k78ja, build 0012).** The cheapest signal that catches the #1 operator risk (a sync silently failing while B drifts from A, discovered only at failover): a tri-state `ecm_sync_runs_total{result}` metric emitted per run, and an **`ECMSyncStalledTargetDrift`** Prometheus alert (warning, not page) that fires when `dbas_sync` has had no successful run for ~3 sync intervals — riding the per-task last-success gauge that already exists for free. Because only a *full* success advances that gauge, the alert also fires on a sustained *partial* loop (the responder triages via `ecm_sync_runs_total{result}`). Adds a `sync-target-stalled-drift` runbook stub. No dashboard. (bead i39wu/k78ja)
- **v0.18.1 cross-instance sync — scheduled + manual sync task, internal substrate (bead i39wu/5gzg5, build 0011).** Wraps the sync engine in a `DbasSyncTask` (`backend/tasks/dbas_sync.py`) so an operator can run a one-way A→B sync **on a schedule** or **force it on demand** (`POST /api/tasks/dbas_sync/run` with `{sync_target_id, confirm_apply}` — admin/privileged-gated). It re-checks the target's credential freshness at fire time (aborts non-silently on a rotated/revoked/disabled credential, before any remote connection), reads the source live each cycle (converge-over-cycles), defaults to a **dry-run preview** (apply only on `confirm_apply=true`, source-wins), and maps the result to a tri-state success/partial/failed task outcome. v1 serializes runs across targets under one task id (safe; per-target concurrency is a small follow-up). No settings UI yet (`nnl9s`). (bead i39wu/5gzg5)
- **v0.18.1 cross-instance sync — channels + streams replication with a collision-safe floor, internal substrate (bead i39wu/kcxie, build 0010).** Extends the one-way sync engine (`tjaey`) to channels and their embedded streams. To stay safe under *continuous* sync (where a one-shot importer's loose match becomes silent recurring divergence), the sync path floors stream matching at **Tier-3 exact** — Tier-4 fuzzy is opt-in per target via `SyncTarget.fuzzy_stream_matching` (default off) and, when on, a fuzzy hit is reported low-confidence rather than a silent update. Logos are excluded from the routine per-cycle slice (ADR-013 S9 — the logo importer's destructive `clear_existing` + streaming cost don't belong in a continuous loop). Still no operator-reachable trigger (`5gzg5`). (bead i39wu/kcxie)
- **v0.18.1 cross-instance sync — one-way sync engine core (config categories), internal substrate (bead i39wu/tjaey, build 0009).** The engine that replicates LOCAL config to a remote Dispatcharr-B (`backend/tasks/dbas_sync_engine.py`): `run_sync(target, confirm_apply=False)` reuses the **unchanged** DBAS restore orchestrator + importers pointed at the SSRF-guarded remote client (`1t3al`). It gathers the local config categories (M3U accounts, EPG sources, channel groups, channel/stream profiles), **redacts to topology-only** via the shared denylist (no secrets on the wire), stamps the plan `schema_version`, runs a **dry-run preview by default** (source-wins apply only on `confirm_apply=True`), and journals each run. `users` is a **code-enforced never-sync category** (continuous push would overwrite B's privilege flags / lock out its operator); channels/streams/logos are a later slice. Still no operator-reachable trigger — the scheduled-task wrapper is `5gzg5`. (bead i39wu/tjaey)
- **v0.18.1 cross-instance sync — SyncTarget CRUD foundation, internal substrate (bead i39wu/vigbu, build 0007).** First code of the v0.18.1 one-way A→B configuration-sync epic ([ADR-013](docs/adr/ADR-013-cross-instance-live-sync.md)), with **no operator-reachable sync yet**. Adds the admin-gated `/api/sync-targets` CRUD (create/list/update/delete a Dispatcharr-B sync destination — base_url + Fernet-encrypted credentials, credentials masked on every response and never returned in clear), four persisted-state columns on `sync_targets` (`last_full_sync_at`, `last_outcome`, `last_source_fingerprint`, `fuzzy_stream_matching`) via additive Alembic `0024`, and `base_url` scheme validation on write. The authoritative SSRF resolve-by-IP gate and the actual sync engine land in later beads (`1t3al`/`tjaey`); per ADR-013 the sync payload is redact-by-default (topology only) and `users` never sync. (bead i39wu/vigbu)
- **Restore a DBAS backup artifact from the UI — dry-run preview, apply, and encrypted-artifact passphrase (bead 7euap, build 0006).** The async DBAS restore endpoint shipped earlier (`POST /api/backup/restore-dbas` + the `dbas_restore` task) had no UI; restoring an artifact was API-only. **Settings → Backup & Restore** now has a **"Restore DBAS Backup"** card opening a modal that: uploads a `.zip` artifact (the format scheduled backups and the Encrypted Backup card produce), sniffs the encryption magic client-side and **prompts for the passphrase** when the artifact is encrypted (ADR-012 D12 / u81kh — the run button stays disabled until a passphrase is entered), runs a **dry-run preview by default** (counts-only, no changes) with an explicit **Apply** option, shows live 13-stage progress, and renders the terminal `RestoreReport` (per-category created/updated/skipped/failed + the logo-miss banner). A dry-run result offers an **"Apply these changes"** follow-through so the operator previews before the one-way apply. Wrong-passphrase / corrupted-artifact / unsupported-version failures surface the sanitized server message. (bead 7euap)
- **DBAS (Backup & Restore) v0.18.0 foundation — internal substrate, not yet user-exposed (epic bd-0i2vt, ADR-012).** First slice of the DBAS-absorption epic. Lands the pieces the user-facing backup/restore feature will build on, with **no operator-reachable surface yet** (no new endpoints wired into the app): (1) a **streaming backup-artifact builder** (`build_backup_artifact`) producing a ZIP of per-category YAML + binary subtree + a manifest carrying a dedicated integer `schema_version` + a SHA-256 sidecar, built to a temp file (not in RAM) for the multi-GB logo case, with a **non-bypassable deep credential-redaction** stage over every category (bd-0i2vt.7); (2) `CloudStorageTarget` extended with credential-freshness columns (`credential_version`, `token_revoked_at`) + an `insecure` TLS-skip flag, and a reserved `SyncTarget` schema (v0.18.1 sync; lands now so the later cut isn't migration-gated) via additive/reversible Alembic `0023` (bd-0i2vt.4); (3) the shared Phase-2 restore **contracts** (response schema, ID-remap table, durable rollback ledger) in `backend/dbas/restore_contracts.py` (bd-kxuj2); (4) a pinned Dispatcharr **test-environment** harness + production-shaped seed tooling (bd-zqtjj). (epic bd-0i2vt)

### Security
- **Optional whole-artifact passphrase encryption for DBAS backups — opt-in, redact-by-default preserved (bd-u81kh, ADR-012 D12, build 0005).** A new crypto primitive (`backend/dbas/artifact_crypto.py`), intentionally parallel to the static-key Fernet at-rest path: **scrypt** KDF (N=2¹⁵, r=8, p=1; per-artifact random salt) → **chunked streaming ChaCha20-Poly1305**, with an authenticated cleartext header carrying a `format_version` that is **separate from** the backup `schema_version` (so a restore-time version gate can refuse an unsupported envelope before asking for a passphrase). Each chunk's AAD binds the header + chunk-index + an `is_final` flag, so chunk swap, reorder, and stream truncation all fail authentication. **Structural no-oracle:** a wrong passphrase and a corrupted artifact raise the identical exception and release **zero plaintext** on any failure (the partial output is destroyed before the error propagates). Wired at exactly two seams — the backup-builder encrypt stage (`build_backup_artifact`, off the event loop) and a single decrypt-at-ingest gate on restore (the per-category importers need no crypto awareness). The encryption is **opt-in**; the default backup stays redact-by-default. An explicit "include credentials for migration" choice **requires** a passphrase and only re-injects an approved migration-cred allowlist (SMTP/source creds; **never** a user `password_hash`) — redaction always runs (structural redact-then-encrypt; there is no "skip redaction" switch). Passphrase minimum is **12 characters** (API-enforced), and producing an encrypted backup requires acknowledging that a **lost passphrase makes the artifact permanently unrecoverable** (a hard-gate checkbox in the new Settings → Encrypted Backup card, not a tooltip). Encrypted backups are **manual-only** (a passphrase is never persisted to the schedule store). Hardening found en route: a passphrase passed as an ad-hoc task parameter would have leaked into task logs **and** the `journal.db` audit row — added a secret-parameter redaction chokepoint in `task_engine`. (bd-u81kh, ADR-012 D12)
- **SSRF egress chokepoint for DBAS outbound traffic — denylist + LAN/public modes + DNS-rebinding mitigation (bd-0i2vt.5, ADR-012 D4).** A standalone outbound-URL validator (`backend/security/ssrf.py`) with an always-on denylist (loopback, link-local, IMDS 169.254.169.254, CGNAT 100.64/10, 0.0.0.0/8, multicast, IPv6 ULA/link-local/site-local, IPv4-mapped — rejected in **both** modes), a first-run-selectable **LAN-friendly (default) vs public-only** policy, and **DNS-rebinding mitigation** (resolve-once → validate every A/AAAA → connect by the validated IP with the hostname preserved for SNI; redirect re-validation; fail-closed on unresolvable). A CI guard fails the build if any `cloud_storage` adapter makes a raw outbound call that bypasses the validator. The chokepoint is in place now; adapters are wired through it in bd-0i2vt.8. Also hardens the Fernet export-key bootstrap (mode/ownership integrity check, repair-or-fail-closed, never silently regenerating a key that has live ciphertext) (bd-0i2vt.2). The DBAS threat model (`docs/security/threat_model_dbas_import.md`) was re-pointed to ADR-012, lifted out of Draft, and given a passphrase-encryption addendum (bd-ujqze). (epic bd-0i2vt)

### Changed
- **Auto-creation after M3U refresh is now its OWN scheduled task, not a refresh sub-step (ADR-011, bd-ka7j9).** Phase 2 of the GH #473 work, building on the exo4j circuit-breaker (PR #505). Previously scheduled M3U refresh *hard-chained* auto-creation as a synchronous side-effect (`run_auto_creation_after_refresh`), with no back-pressure — the structural coupling behind the crash loop. Now M3U refresh just advances a `last_m3u_refresh_completed_at` watermark in settings (on **every** successful refresh — scheduled or manual single-account — preserving the "runs after every refresh" behavior), and the `AutoCreationTask` runs on its own **~60s interval schedule** and decides *for itself* whether to fire: it runs the post-refresh pipeline only when the task is enabled, the breaker/break-glass are clear (read fresh), at least one `enabled + run_on_refresh` rule exists, and the refresh watermark is newer than the last one it consumed (it consumes the watermark *before* running, so a crash or an overlapping tick can't re-fire against the same refresh). **Operator-visible effects:** (1) auto-creation after a refresh now appears as its own task run (own notification + task-history row) rather than a step inside the refresh; (2) there is up to ~60s latency between a refresh completing and auto-creation firing; (3) a single failed M3U account in a batch no longer suppresses auto-creation for the rest of the batch. The two former entry points are collapsed into one (one breaker gate, one created-channel cap path, one notification style). Manual "Run Now" (`POST /api/auto-creation/run`) is still never gated. A one-time, idempotent startup migration flips existing installs' `auto_creation` task schedule MANUAL → INTERVAL 60s (an operator who deliberately set MANUAL is flipped once; it only actually runs when a `run_on_refresh` rule and a refresh exist). The exo4j crash-sentinel, circuit-breaker flag, reset endpoints, and `ECM_DISABLE_RUN_ON_REFRESH` break-glass are all preserved unchanged. (ADR-011, bd-ka7j9)

### Fixed
- **Auto-creation OOM-killed the container and then crash-looped on every restart (GH #473, bd-sjdsq + bd-exo4j, build 0001).** Two coupled fixes for a persistent outage. **(1) Memory (bd-sjdsq):** the engine accumulated an `execution_log` plus per-stream verbose rule/condition traces in memory for the entire run — O(streams×rules×conditions) — so RSS grew steadily until the kernel OOM-killed the process (reporter recurred even at 32 GiB). `results["execution_log"]` is now a `BoundedExecutionLog` (a `list` subclass, so every append site is governed by construction): non-dry-run runs strip the verbose per-stream trace incrementally and retain at most `MAX_AUTO_CREATION_LOG_ENTRIES` (default 500, settings-overridable) with an aggregate "…N more" summary + a `log_truncated` marker; dry-run keeps the full trace. The run now logs peak RSS and a run-size summary. **(2) Crash loop (bd-exo4j):** a kernel OOM-kill leaves the `AutoCreationExecution` row stuck `status="running"`, and scheduled M3U refresh re-fired `run_on_refresh` auto-creation unconditionally → re-OOM on every restart (doubling RAM didn't help). A startup crash-sentinel now marks orphaned `running` executions `abandoned` (before the scheduler arms) and trips a persisted circuit-breaker that **skips `run_on_refresh` auto-creation until an operator explicitly re-enables it** (`POST /api/auto-creation/reset-circuit-breaker`); a `ECM_DISABLE_RUN_ON_REFRESH=1` env var is a break-glass. Manual "Run Now" is never gated. (GH #473, bd-sjdsq, bd-exo4j, build 0001)

### Added
- **Per-run created-channel cap for auto-creation — a safety valve against runaway rules (GH #473, bd-h2xnl, build 0001).** An over-broad rule (e.g. one matching per-event PPV/event streams) could expand a lineup from ~186 to 2400+ channels in a single run, the upstream amplifier of the OOM. Auto-creation now soft-aborts a run once it would create more than `MAX_AUTO_CREATED_CHANNELS_PER_RUN` channels (default 500, settings-overridable): it stops creating further channels (already-created channels are left consistent), marks the execution `status="capped"`, and surfaces a non-silent "capped at N of M — review the rule or raise the cap" alert. (GH #473, bd-h2xnl, build 0001)

## [0.17.5] — 2026-06-16

### Security
- **Bumped dependencies to clear known CVEs flagged by the release security scans (bd-3hgrf, build 0008).** The main-targeting CI security scans (`pip-audit`, `npm audit`) — which dev-targeting PRs skip — flagged 16 backend CVEs and a frontend high that had gone latent. Backend: `aiohttp` 3.14.0→3.14.1 (8 CVEs, CVE-2026-54273..54280), `python-multipart` 0.0.27→0.0.32 (3 CVEs), `starlette` 1.0.1→1.3.1 (4 CVEs, CVE-2026-48818/48817/54283/54282 — FastAPI 0.136.1's unbounded `starlette>=0.46.0` pin already permits it, so no FastAPI bump was needed), and `cryptography` 46.0.7→49.0.0 (GHSA-537c-gmf6-5ccf). Frontend: `vite` 8.0.10→8.0.16 (GHSA-fx2h-pf6j-xcff `server.fs.deny` bypass) plus transitive patch bumps via non-`--force` `npm audit fix` (no major bumps). `pip-audit` and `npm audit --audit-level=high` now both report zero; full backend (5340 passed) and frontend (1573 passed) suites stayed green against the bumped versions. (bd-3hgrf, build 0008)

### Changed
- **Cut ECM's API call volume against the Dispatcharr backend per refresh cycle (bd-iwfr7, bd-05h8w, bd-1qmn0, build 0006).** Dispatcharr-side profiling (nginx logs + live DB) showed ECM hammering the REST API each cycle. Three connected fixes: (1) M3U change-capture replaced ~1,000 per-group `?channel_group_name=&page_size=1` count probes plus per-group paged pulls with ONE bulk per-account `get_streams` loop grouped client-side; (2) `get_stream_groups_with_counts` now reads `channel_groups[].stream_count` from the single `GET /api/m3u/accounts/` response instead of one probe per group (zero per-group probes; also captures authoritative `enabled`/`is_stale` group state into the M3U snapshot); (3) `BandwidthTracker` no longer re-fetches the ~734 KB `/api/m3u/accounts/` payload on every 10s poll — it caches the accounts snapshot on a 300s TTL (mirroring the channel-map refresh), threaded into the provider resolver, with the on-demand `/api/stats/channels` path keeping its fallback fetch. Stream-pull `page_size` bumped 500→1000 (and auto-creation's per-account sweep 100→1000) to halve round-trips and lower burst RPM. Net: per-cycle `GET /api/channels/streams/` collapses from ~2,283 to a few dozen, and the continuous 734 KB/10s heartbeat (the likely driver of Dispatcharr uWSGI worker memory creep) is eliminated. (bd-iwfr7, bd-05h8w, bd-1qmn0, build 0006)

### Fixed
- **The Channel Manager Streams pane showed empty/disabled groups on first load (bd-rmyn7, build 0007).** A regression from build 0006: switching group counts to read `channel_groups[]` from `/api/m3u/accounts/` (which includes disabled and zero-stream groups) only dropped zero-count groups in the provider-filtered code path. The all-accounts path the Streams pane hits on first load (before any provider filter) therefore surfaced empty/disabled groups; selecting a provider filter "fixed" it. The pre-0006 group universe came from `/api/channels/streams/groups/`, which only lists groups that actually have streams, so empty groups never appeared. Fix: the `count > 0` drop is now unconditional (both the all-accounts and provider-filtered paths), restoring the prior "only populated groups" behavior. Adds a focused regression test. (bd-rmyn7, build 0007)
- **Pressing the spacebar while searching in any dropdown selected the highlighted item instead of typing a space, making it impossible to search for options whose names contain spaces (GH #489, bd-jc4v7, build 0005).** `CustomSelect`'s `handleKeyDown` is bound on the container `<div>` and treated `Enter` and `' '` (Space) identically — `preventDefault()` + select the highlighted option. Because the searchable variant's `<input>` lives inside that container, a space typed into the search box bubbled up to the handler, got its default suppressed, and triggered a selection — so typing e.g. `us | News` auto-selected whatever was highlighted after `us`. This affected **all** searchable `CustomSelect` dropdowns (Scope Group, Normalized Match in Group, M3U-account and channel modals, alert config, etc.). Fix: Space is split out of the `Enter` case and now only opens/selects when focus is on the trigger button (`event.target !== searchInputRef.current`); when the search input is focused it falls through and inserts a literal space. Enter-to-select while searching and arrow/Escape/Home/End navigation are unchanged. Adds the first `CustomSelect` test file covering space-types-into-search (no selection), Enter-still-selects, and Space-opens-from-trigger. (GH #489, bd-jc4v7, build 0005)
- **Large auto-creation runs committed one merge-journal row per merge, churning the SQLite WAL and growing it to very large sizes during a single run (GH #473, PR #474, build 0004).** Each live `merge_stream` audit row (written for the surgical journal-driven un-merge path, bd-0emgo.5) was committed individually, so a run touching thousands of streams produced thousands of tiny transactions. Fix (contributed by @Mirabis): `ActionExecutor` now buffers merge journal rows and flushes them in one transaction via a new `journal.log_entries()` — at a 100-entry threshold and again at pipeline completion — keeping the identical audit payload (`batch_id`, stream-ID-only `before`/`after` values that never embed provider-credentialed URLs, and scored-fuzzy provenance) while cutting transaction frequency. The end-of-pipeline flush is wrapped in `try/finally` so buffered rows describing merges that **actually executed** against Dispatcharr are persisted even when a later pipeline pass (reorder, reconcile-orphans, EPG retry, probe) raises — preserving recoverability of an aborted run. `JournalEntry.timestamp` is now stamped at flush time rather than per-merge. Adds focused coverage for batch journal writes, the threshold flush, and the exception-path flush. Note: this reduces WAL growth but does not by itself resolve #473's large-run memory crash, whose separate root cause (the in-memory `execution_log` + end-of-run `json.dumps`) is tracked independently. (GH #473, PR #474, build 0004)
- **The "Match League Tags" normalization rule's "Only strip when followed by a delimiter" toggle reset to on after every upgrade/restart (GH #484, bd-ov11h, build 0003).** The strong-delimiter requirement on the league-strip rule (`require_delimiter`, bd-0emgo.2) was applied by a startup heal — `set_league_strip_require_delimiter()` — that ran on **every boot** and flipped the flag `False → True` whenever it found it `False`. It was meant as a one-time upgrade fix (so "NFL RedZone" stays a brand instead of collapsing to "RedZone"), but as an every-boot operation it couldn't tell "False because never healed" from "False because the operator deliberately turned it off" — so it re-enabled the option on every restart, clobbering the user's choice (the reporter disabled it, upgraded, and found it back on). Fix: the heal now runs **at most once per install**, gated by a persistent `league_delimiter_heal_applied` marker in settings; once it has run, a value the operator later changes is never touched again. (It must be a settings marker rather than an Alembic data migration: ECM's smart-bootstrap stamps `alembic_version` forward past data-only migrations whenever the live schema already matches the model — bd-5w6jz — so a data migration would be silently skipped on existing installs.) Operators upgrading from a pre-0.17.3 build still get the brand-corruption fix once; everyone already healed (flag currently `True`, including the reporter) sees a no-op heal that just sets the marker, so a subsequent edit to `False` now **sticks** across restarts. Fresh installs are unchanged (they already seed `require_delimiter=True`). Adds gated-heal tests (applies-once-and-sets-marker, skips-when-marker-set, already-true-still-sets-marker). (GH #484, bd-ov11h, build 0003)
- **Stream Probe (and Re-probe Failed Streams, and manual probes) still sent a "probe started" external alert even with only warning/error alerts enabled — the other half of GH #462 (bd-yqnzs, build 0002).** bd-on4sr (v0.17.4-0005) fixed the "starting" external alert for tasks that use the `TaskScheduler` base class's progress notification (e.g. Auto Creation) by gating it on `send_alerts AND alert_on_info`. But the **stream prober has its own separate notification path** (`StreamProber._create_probe_notification`) that hardcoded `send_alerts=True` — a value deliberately set back in **v0.14.0-0008** (2026-02-24, "Re-enable external alerts on probe start notification"), long before the per-task `alert_on_info` gate existed. So an info-level "Stream probe started" message was Telegram/Discord/email-blasted on every run regardless of the task's alert level — the exact symptom the reporter still saw after upgrading, and which they traced to the hardcoded line themselves. Fix: `_create_probe_notification` takes a `send_alerts` flag (threaded through `probe_all_streams` / `probe_streams_by_ids` as `start_send_alerts`, default `True` to preserve any other caller). The two **scheduled** tasks (`stream_probe`, `failed_stream_reprobe`) pass their engine-gated `self._send_alerts` (`send_alerts AND alert_on_info`); the **manual** probe endpoints (`POST /api/stream-stats/probe/all`, `/probe/bulk`) look up the same gate via a new `task_start_alerts_enabled("stream_probe")` helper (info alerts are opt-in, so it defaults to off when the task has no row). Net: the "probe started" message dispatches externally **only** when the operator explicitly enabled info-level alerts — for scheduled *and* manual probes alike. The in-app NotificationCenter toast is unchanged. Adds 3 prober pass-through tests + 4 helper-gate tests + an updated bulk-probe router test. (GH #462, bd-yqnzs, build 0002)

### Added
- **Schedules Direct (SD) EPG support — add an SD account, manage lineups, and pull station logos/program posters from the EPG Manager (bd-ynfal, build 0001).** ECM previously had only a `schedules_direct` stub that targeted Dispatcharr's *old* SD schema (a single `api_key` field), which Dispatcharr migration 0024 removed in favor of `username`/`password` (SHA1-hashed at fetch time). This makes SD a first-class EPG source type alongside XMLTV and dummy EPG. **Credentials:** the EPG source create/update path now accepts `username` + `password` for SD sources; `password` is **write-only** — send it to set or change the credential, never read it back (it is not on the `EPGSource` read type, only accepted on update). **Lineup management:** SD lineups live on the SD account (not Dispatcharr's DB), so ECM proxies Dispatcharr's live SD endpoints — `GET/POST/DELETE /api/epg/sources/{id}/sd-lineups` (list / add / remove a `{lineup}`) and `POST /api/epg/sources/{id}/sd-lineups/search` (`{country, postalcode}` → matching headends). The list response surfaces SD's rate-limit state (`max_lineups`, `changes_remaining`, `changes_reset_at`) so the operator can see how many of SD's ~6-changes/24h budget remain before adding. **Posters:** `GET /api/epg/programs/{id}/poster` streams an SD program poster image (bytes + `Content-Type`) through from Dispatcharr. **Thin-proxy discipline:** Dispatcharr authenticates to SD live on each of these calls and is itself rate-limited by SD; ECM adds **no** retry/polling of its own (in the backend client, the frontend API layer, or MCP) so it never amplifies the SD call count. **SD custom properties** (`logo_style`, `poster_style`, `auto_apply_epg_logos`, `fetch_posters`, and read-only `sd_changes_remaining`/`sd_changes_reset_at`) ride in the source's `custom_properties` bag, mirroring Dispatcharr. **Frontend:** EPG Manager gains an SD settings panel (credentials + logo/poster options) and a lineup manager (search by country/postal code, add/remove, remaining-changes indicator). **MCP:** new `list_sd_lineups` / `search_sd_lineups` / `add_sd_lineup` / `remove_sd_lineup` tools, and `create_epg_source`/`update_epg_source` now accept the SD `username`/`password`/`custom_properties` fields. **Docs:** API reference, Dispatcharr API notes, and the EPG user guide (add-account → add-lineups → logo/poster options → refresh, with SD's 4-lineup cap and 2-hour minimum refresh) all updated. (bd-ynfal, build 0001)

## [0.17.4] — 2026-06-06

### Added
- **Clear Emby Logos — flush Emby's cached channel logos so it re-fetches fresh ones (GH #475, bd-v9tp7, build 0009).** Emby caches channel logos and keeps serving a stale image even after the logo changes upstream in Dispatcharr; the operator had no way to force a refresh. Replicates Channel Identifiarr's "clear logos" feature, **reusing ECM's existing Emby connection** — the saved `emby_base_url` + `emby_api_key` with `X-Emby-Token` auth — so no new credentials and no username/password login (a static Emby API key authenticates `GET /LiveTv/Channels` enumeration and the per-channel `DELETE /Items/{id}/Images/{type}` calls just as well; deleting the cached image makes Emby re-download the logo from its source on next channel access). **Backend:** new `EmbyClient.get_livetv_channels()` (the auth gate) + `delete_item_image(id, type)` with a `Primary`/`LogoLight`/`LogoLightColor` image-type whitelist; new admin-gated `POST /api/emby/clear-logos` returns **202 + {job_id}** and runs a supervised background task (the full-lineup sweep — hundreds of channels × up to 3 types — would otherwise exceed the 30s request-timeout middleware, same 202+poll pattern as bulk-commit, bd-ggxks), with a `GET /api/emby/clear-logos/{job_id}` poll endpoint. The worker clears **all** Live TV channels (accepts an optional `channel_ids` filter for a future per-group UI), is per-channel resilient (one channel lacking a logo type never aborts the run), and validates Emby is configured (400 otherwise). **Progress via the notification system, like stream probe:** the job emits a `task_clear_emby_logos` progress notification (create → rate-limited updates → finalize success/warning) that the NotificationCenter renders live — a progress bar, deleted/skipped/error counts, and the current channel name — driven by a new `clearing` active status. **Frontend:** a "Clear Cached Logos" block in Settings → Integrations → Emby with per-type checkboxes (default all three) and a fire-and-forget button gated on a saved Emby key. **MCP:** new `clear_emby_logos` tool (202+poll, same whitelist). (GH #475, bd-v9tp7, build 0009)
- **Global date-format preference under Settings → Appearance (bd-8j47e, build 0007).** ECM now has a single instance-wide "Date Format" control (Automatic / Month-Day-Year / Day-Month-Year / Year-Month-Day ISO) that drives every date and time shown in the UI. Previously date rendering was inconsistent: `frontend/src/utils/formatting.ts` and ~14 components each formatted dates independently — some hardcoded `en-US` (always m/d/y), others passed no locale and silently deferred to each viewer's browser locale. That split is exactly why one operator saw `06/04` while another saw `04/06` on the same screen. Fix: a single `setDateFormatLocale`/`getDateLocale` resolver in `formatting.ts` maps the preference to a locale (`auto`→browser, `mdy`→en-US, `dmy`→en-GB, `iso`→en-CA), and every date call site — the shared formatters plus all direct `toLocaleString`/`toLocaleDateString`/`Intl.DateTimeFormat` callers — routes through it. The setting is applied on app init and updates live (no reload) when changed. Backend persists `date_format` in `DispatcharrSettings` alongside `theme`. **Scope:** settings are instance-wide, so this is one shared format for all users; the default is `auto`, which preserves the prior per-browser behavior, so no display changes until an operator explicitly pins a format. Time-of-day 12h/24h rendering is unchanged. (bd-8j47e, build 0007)

### Changed
- **`{normalized_name}` now applies the rule's normalization groups in *every* auto-creation action, not just Create Channel (GH #466, bd-6gvt8, build 0006).** The template variable is documented as "Name after normalization rules", but it only behaved that way in Create Channel — which re-normalizes its expanded name in a separate step. Everywhere else (Assign TVG-ID, Set Variable, Assign Logo, …) `{normalized_name}` resolved to the **raw** stream name, because `StreamContext.normalized_name` is never populated during a run and the variable fell back to the stream name. So with a "Strip country prefix" group, `{normalized_name}` produced `CHANNEL` in Create Channel but `US | CHANNEL` in Assign TVG-ID. Fix: `{normalized_name}` is now resolved once at the per-action template-context chokepoint (`_build_template_context`) using the firing rule's `normalization_group_ids`, so it means the same thing in every action. **Scope of change:** only rules that actually have normalization groups are affected — a rule with no groups still yields the raw stream name (unchanged), and Create Channel output is unchanged (its post-expansion normalization is idempotent on the already-normalized value). (GH #466, bd-6gvt8, build 0006)

### Fixed
- **Print Channel Guide rendered nothing but greyed-out phantom rows for groups numbered with float/decimal channel numbers (e.g. OTA 2.1, 5.1) (bd-szsb2, build 0008).** With "Show empty slots" on (the default), `generatePrintHtml` walked the group's range by **integer** steps and looked each slot up in a map keyed by the channel's *exact* number. For a float-numbered group no integer ever matched a float key, so every real channel was skipped (no data) and every integer in the range printed as a greyed `—` placeholder — the operator saw "2–38, all empty, plus channels that don't exist." A secondary bug parsed the per-group From/To bounds with `parseInt`, truncating e.g. `38.5 → 38` and dropping the highest float channel from the range. Fix: a group containing **any** non-integer channel number now skips integer gap-fill entirely and renders only its real channels (the integer gaps between 2.1 and 5.1 are unused major numbers, not "missing channels" — PO decision); pure-integer groups keep the existing empty-slot fill unchanged. The four From/To parse sites switch `parseInt`→`parseFloat` so decimal bounds and the top float channel are respected. Adds 3 float-specific tests (render-not-skipped, no-placeholders-for-float-group, no-`parseInt`-truncation regression). (bd-szsb2, build 0008)
- **Scheduled tasks sent a "starting" external alert (Telegram/Discord/email) on every run even when only warning/error alerts were enabled (GH #462, bd-on4sr, build 0005).** A task's start/progress notification (`task_scheduler._create_progress_notification`) is an "info"-level "<task> starting..." message, but `task_engine._execute_task` wired its external-alert dispatch straight from the master `send_alerts` toggle, never consulting the per-task `alert_on_info` flag. So a task configured with only warning + error alerts (`alert_on_info=False`, the column default) still pushed a "starting" alert to every configured channel on every run — the reporter saw it via Telegram for the auto-creation schedule. The completion alert was already gated per-level (`check_and_run_tasks` honours `alert_on_success`/`_warning`/`_error`/`_info`); only the start notification leaked. Fix: `_execute_task` now passes `send_alerts AND alert_on_info` as the progress-notification alert flag, so the "starting" message dispatches externally only when info alerts are explicitly enabled. The in-app NotificationCenter toast is unchanged — it stays gated by `show_notifications`, independent of the external-alert level. (GH #462, bd-on4sr, build 0005)
- **Deleting a normalization rule group left a dangling reference in auto-creation rules that then refused to save (GH #465, bd-miut3, build 0004).** An auto-creation rule stores the normalization rule groups it applies as `normalization_group_ids` (the "Normalization Groups" selector in the rule editor — the groups that strip quality tags). `DELETE /api/normalization/groups/{id}` removed the group and its rules but never stripped that id from any rule's `normalization_group_ids`, so the rule kept a reference to a group that no longer existed. On the next edit the rule editor reloaded the full id list but couldn't render a checkbox for the missing group, and the write-time validator (`_validate_normalization_group_ids`, bd-i75ax) then rejected every save with `422 — normalization_group_ids do not exist`; the only workaround was "Clear all + re-select". **Impact was save-block only** — at run time `normalize(group_ids=…)` filters by membership, so a missing id was silently ignored and auto-creation still ran correctly. Fix: deleting a normalization group now cascade-strips its id from every auto-creation rule's `normalization_group_ids` in the same transaction (`_strip_normalization_group_ref`), and a startup heal (`_heal_orphaned_normalization_group_refs`, in `_run_migrations`) repairs rules orphaned by deletions that happened before this fix shipped — so already-broken rules become saveable again without operator action. (GH #465, bd-miut3, build 0004)
- **Scheduled tasks showed "Next Run: Never" in Settings → Scheduled Tasks despite having an active schedule (GH #468, bd-a80u2, build 0003).** The "Next Run" the UI renders came from the registry's in-memory `instance._next_run`, which is loaded from the DB only once at startup (`sync_from_database`). Every path that maintains schedules — the schedule create/update/delete endpoints (`_update_task_next_run`) and `task_engine.check_and_run_tasks` after a run — updates the **database** (`scheduled_tasks.next_run_at` + `task_schedules.next_run_at`) but never refreshes the in-memory instance. Tasks whose default schedule is `MANUAL` (Stream Probe, Re-probe Failed Streams, EPG/M3U refresh, etc.) start with `instance._next_run = None`, so adding a schedule through the multi-schedule UI left the in-memory value at `None` and the UI showed "Never" until the next container restart. Compounding this, `_save_task_to_db` wrote the stale in-memory `None` back over the correctly-computed `scheduled_tasks.next_run_at`, so any later `sync_to_database` (e.g. editing alert settings) clobbered the DB value and made "Never" persist across restarts. **Impact was display-only** — the task engine fires off the child `task_schedules.next_run_at` rows, which were always correct, so the tasks did run on schedule. Fix: `GET /api/tasks` and `GET /api/tasks/{id}` now source "Next Run" from the earliest enabled `task_schedules.next_run_at` (the real firing source) instead of the stale in-memory value, falling back to the in-memory value only for tasks with no schedule rows; and `_save_task_to_db` no longer overwrites `next_run_at` for tasks that have `task_schedules` rows (those are DB-managed). (GH #468, bd-a80u2, build 0003)
- **Bulk channel creation re-paginated the entire Dispatcharr logo catalog once *per channel*, making large batches O(channels × catalog-pages) and exhausting the request budget (bd-raehx, build 0002).** Every `createChannel` op carrying a `logoUrl` but no `logoId` called `find_logo_by_url()`, which paginates the whole logo catalog (500/page) on every call with no caching. Two real debug bundles confirm the cost: a 441-op SiriusXM batch issued **2,391** logo GETs (~5-page catalog, finished server-side in ~6 min after a 504), and a 113-op batch against a larger ~25-page catalog issued **859** logo GETs and reached only ~channel 22 before the 30s `504`. This is why batches "created channels but didn't assign streams past ~15", why an op reported *failed* while the write still happened (the handler keeps running after the client's 504), and why adding streams manually afterward worked. Fix: `_run_bulk_commit` now builds a per-run `{url → logo}` index **once**, lazily (only the first time an op actually needs a logo lookup, so `validateOnly` and logo-free batches pay nothing), and every subsequent op reuses it; logos created mid-batch are inserted into the index so a later op sharing the same `logoUrl` reuses them instead of creating a duplicate (also fixes a latent duplicate-logo bug). Catalog fetches drop from ~859/2,391 to ~25/5 — one pagination per batch. The per-channel `find_logo_by_url` first-match semantics and the "logo failure still creates the channel without a logo" fallthrough are preserved. (bd-raehx, build 0002)
- **Bulk-commit partial outcomes are now flagged explicitly so the client can distinguish "some applied, some failed" from a total failure (bd-5xciq, build 0002).** The `BulkCommitResponse` envelope gains a `partial` boolean (`operationsApplied > 0 AND operationsFailed > 0`), present on every return path. This makes a partially-applied batch reconcilable via `tempIdMap` instead of reading as a flat failure that prompts a duplicate-creating retry. The frontend `BulkCommitResponse` type carries the field; existing `useEditMode` rendering already derives partial state from the applied/failed counts, so behavior is unchanged and the flag is exposed for future use. (bd-5xciq, build 0002)
- **`POST /api/channels/bulk-commit` returned 504 mid-flight on large batches while the handler kept running, producing duplicates on retry (bd-ggxks, build 0001).** The endpoint was synchronous and pinned behind the 30s `ECM_REQUEST_TIMEOUT_SECONDS` middleware. A 441-op SiriusXM batch (~6 min of sequential Dispatcharr POSTs) caused the middleware to fire `504 Gateway Timeout` to the operator while the handler kept running in the background; the operator retried, each retry committed another ~30 partial channels, and Dispatcharr accumulated duplicates. Fix: bulk-commit now follows the bd-cns7j / bd-enfsy 202+poll pattern. Non-`validateOnly` `POST /api/channels/bulk-commit` returns `202 + {job_id, status: "running"}` immediately and dispatches a supervised background task; clients poll `GET /api/channels/bulk-commit/{job_id}` until the status is terminal (`completed` with the full `BulkCommitResponse` under `result`, or `failed` with an `error` message). `validateOnly` stays synchronous (200 with the response body inline) because pre-commit validation is fast and the frontend uses it for instant feedback. Frontend `bulkCommit()` and the MCP `bulk_commit_channels` / `build_channel_lineup` tools drive the new POST→poll loop transparently — their public shapes are unchanged so all three `useEditMode` call sites and existing MCP consumers keep working. Job state is in-memory with a 30-min TTL prune on every new POST; completed jobs are evicted on first read so RAM stays bounded; failed jobs persist until the TTL so operators can re-poll the error message. (bd-ggxks, build 0001)

## [0.17.3] — 2026-05-25

### Removed
- **Retired MCP OAuth 2.1 feature fully removed from the codebase (bead jir0m, bd-9axgc).** The MCP OAuth 2.1 "Custom Connector" offering — already disabled by PO decision in bd-9axgc because it required public-internet exposure the operator did not want — is now deleted, not just dormant. Removed: the backend OAuth Authorization Server (`routers/oauth_mcp.py`, `routers/oauth_discovery.py`, `auth/oauth_provider.py`, `auth/oauth_store.py`, `auth/oauth_clients.py`, `auth/oauth_rate_limit.py`, `auth/oauth_discovery.py`), the MCP-server OAuth bits (`oauth_discovery.py` and the `verify_oauth_token` HS256 verifier in `oauth_rs.py`), the OAuth config fields/helpers (`mcp_oauth_signing_secret`, `oauth_allow_insecure`, `get_or_create_oauth_signing_secret`, and the MCP-server `get_signing_key*` / `get_oauth_allow_insecure` / `OAUTH_ISSUER` helpers), the frontend consent UI (`OAuthConsentPage`) and OAuth grant/consent API functions, and ~60 OAuth-only tests. **The supported MCP authentication method is unchanged: the static `?api_key=` path.** The security-critical CD1 no-fail-cascade guard is preserved — `looks_like_jwt` still rejects any JWT-shaped Bearer with 401 so it can never fall through to the static-key compare. ADR-009 and `docs/security/threat_model_mcp_oauth.md` are retained (marked Superseded) for decision history. ([bead jir0m], bd-9axgc)

### Added
- **Auto-creation pre-run snapshot + full revert (epic uc51o, ADR-010, builds 0018–0022).** Before each non-dry auto-creation run ECM now captures a point-in-time snapshot of every non-Dispatcharr-auto-created channel and its stream-ID assignments. The snapshot closes the revert gap that existed in the previous rollback: the old facility only undid *created* entities and surgically un-merged run-added streams; it could not restore streams removed from existing channels nor the general pre-run channel↔stream state. **Schema + capture (uc51o.2):** new `AutoCreationSnapshot` table (Alembic 0022), 1:1 FK to `AutoCreationExecution`; stores stream IDs only — never stream URLs — so Xtream-Codes credentials are never persisted in the snapshot. Capture runs in `run_pipeline` after execution-record creation and before any mutations; failure logs a WARNING and the run proceeds without a snapshot. **Restore algorithm + endpoint (uc51o.4):** `POST /api/auto-creation/executions/{id}/restore-snapshot?confirm=true` (admin-gated, `confirm` is the §D5 optimistic-overwrite acknowledgement) deletes run-created channels then full-replaces each snapshotted channel's stream set via `update_channel(streams=[ids])`; partial failures are collected and surfaced — never silent, never fatal mid-run; the endpoint is idempotent (terminal-state guard). **Retention (uc51o.3):** the `CleanupTask` prunes `auto_creation_snapshots` before VACUUM by age window (default 30 days) and count cap (default 50), whichever fires first; `ecm_auto_creation_snapshot_count` / `_bytes` observability gauges published post-prune. **Unified rollback surface (uc51o.5):** `POST /rollback` is now the single revert entry point — it probes for a snapshot and delegates to the full restore when one exists (requiring `confirm=true`), else falls back to the legacy delete-created-only path byte-compatible with pre-uc51o behaviour; `GET /executions` derives a `has_snapshot` boolean per execution in one batched `IN` query (no N+1). **MCP (uc51o.6):** new `restore_auto_creation_snapshot(execution_id, confirm=False)` tool; `confirm=False` (default) returns the optimistic-overwrite warning with channel count and snapshot timestamp before any write; `list_auto_creation_executions` now surfaces a `[snapshot]` / `[no snapshot]` marker per run. **Revert UI (uc51o.7):** "Undo this run" button in Auto-Creation execution history, gated on `has_snapshot`; shows a mandatory overwrite-warning confirm dialog with snapshot timestamp; post-revert result renders `removed_channels` / `restored_channels` / `failed_channels` — partial failures are never presented as plain success. (bd-uc51o, ADR-010, builds 0018–0022)
- **Print Channel Guide: per-group editable channel range, defaulting to each group's first/last used channel number (bd-9eyqp, build 0026).** The previous global From/To range (bd-9q9z0) and a short-lived intermediate redesign (build 0025) are replaced by per-group range inputs rendered inline in each group row. Each group's From/To defaults to that group's minimum and maximum *used* channel number (computed client-side from already-loaded data, no backend change); operators can edit each group's range independently within the modal session. The empty-slot placeholder fill (bd-w532y, build 0025 — em-dash "—" rows for sparse auto-sync ranges, toggle-on by default, 500-slot cap per group) uses each group's own range. The print button is disabled only when *no* selected group has a valid printable range; the global clamp, rangeError hint, and rangeFilteredGroups state are removed. (bd-9eyqp, bd-w532y, builds 0025–0026)
- **MCP: `set_normalization_group_enabled` tool to toggle normalization rule groups (bd-svixy, build 0024).** New MCP tool wraps the existing `PATCH /api/normalization/groups/{group_id}` endpoint to enable or disable a `NormalizationRuleGroup` by id; group ids are discoverable via the existing `list_normalization_rules` output. (bd-svixy, build 0024)
- **`custom_streams` is now a first-class Smart Sort criterion (bead ap1ud, GH #244).** Operators enable and drag operator-added custom streams in the Smart Sort priority order — exactly like `resolution` or `m3u_priority` — instead of tuning a separate M3U-row priority control. It is a binary criterion: a stream scores 1 if it is a custom stream (Dispatcharr `is_custom == true`, i.e. it belongs to the real "custom" M3U account), else 0 (negated for descending so custom streams sort first); ties among custom streams fall through to the next enabled criterion. **Default: present but disabled** (like `m3u_priority`); existing installs auto-merge it (disabled) via the frontend `mergeSortCriteria`/`ALL_SORT_CRITERIA` mechanism, so no operator action is required and saved priority orders are preserved. **Backend:** `smart_sort_streams` (and the auto-creation engine's `_smart_sort_streams`) take an optional `custom_stream_ids: set[int]` threaded through every sort surface and internal caller; each call site builds the set from the stream data it already fetches (`is_custom`), only when the criterion is active — mirroring how `m3u_priority` gates its M3U-account fetch. When `custom_stream_ids` is not supplied the criterion is inert (scores 0 everywhere), so callers degrade gracefully. **Frontend:** the criterion appears in Settings → Smart Sort Priority as a draggable, toggleable row, in the channel/group/toolbar sort dropdowns, and in the `SortMode`/`SortCriterion` types. **This supersedes the bd-4g2t1 "Custom streams" M3U-row priority control** (the row UX the PO disliked); that work never reached a release cut, so the row, its CSS, and its tests were removed in favour of this criterion. **Tests:** 5 backend criterion tests in `test_sort_criteria.py` (prober) + 5 in `test_auto_creation_engine.py` (engine) covering ranks-first-when-top, no-effect-when-disabled, pure-M3U-unaffected, inert-without-ids, and tie-fallthrough; 5 frontend tests in `SmartSortCustomStreamsCriterion.test.tsx` covering render/default-disabled/saved-enabled/toggle/auto-merge. ([bead ap1ud], GH #244)
- **Fuzzy stream→channel matching for OTA/callsign "Local" channels, built on ONE shared backend core + ONE shared name-cleaner (bead jnzst, v0.17.3-0006).** OTA locals never auto-matched because the global merge path is intentionally strict normalized-name *exact* equality (the strictness that ended a prior ~1,341 false-positive merge incident), while providers name locals inconsistently (`CITY:`/`US|`/`WI |` prefixes, `RAW`/superscript quality tags, `GREENBAY` vs `Green Bay`, channel-number prefixes). This adds a scored fuzzy path that does NOT touch the exact rule. **Core (`backend/services/dedup_matcher.py`):** the existing rapidfuzz `token_set_ratio` scorer is extended into `score_one`/`score_all` returning a `ScoredMatch` (score + `callsign_verdict` + `tvg_id_override` + `signal`), and the single cleaner `_normalize(value, mode)` gains a `LOCALS` mode (dotted/`N.N |` number-prefix strip, source/market-prefix strip, `convert_superscripts` reuse, quality-tag strip, run-together token split) while `CONSERVATIVE` (the default every existing dedup/exact caller uses) stays byte-for-byte unchanged. **Rule path (Component A):** `loose_name_match` rules can now opt into scored fuzzy via a required `min_score` (0–1, floored at `CONFIDENCE_FLOOR=0.60`) plus optional `tie_break`/`max_candidates`, scoped to the Local channel groups via the existing `target_channel_in_group` allowlist; per-merge scores + provenance are written to the journal and surfaced in the run summary. **Preview (Component B):** new no-write `GET /api/auto-creation/fuzzy-preview` (admin-gated, paginated, group-scoped, N×M-capped) returns scored `(stream_id, channel_id, score, callsign_verdict)` triples before any write. **MCP (Component C):** `match_streams_to_channels` and `fuzzy_match_stream` are repointed onto the backend core and the MCP-local scorer (`_score_match`/`_generate_variants`/`_normalize_channel`) is deleted; a new `preview_fuzzy_matches(group_ids, min_score)` tool wraps the preview endpoint; `create_auto_creation_rule`/`update_auto_creation_rule` docstrings document the fuzzy `loose_name_match` + `min_score` + `target_channel_in_group` path. **Tests:** 113 new tests incl. the tombstone property (all 4 WBAY variants match the WBAY channel and the WGBA control is excluded at every `min_score` in [0.60, 1.0]); backend suite 5187 passed/69 skipped, mcp-server suite 396 passed/37 skipped. ([bead jnzst], v0.17.3-0006)

### Changed
- **MCP `match_streams_to_channels` now defaults to dry-run (bead jnzst).** It returns ranked candidates + confidence scores by default and writes only with explicit `apply=true`; it accepts a *list* of `group_ids` and gains an opt-in flag to also backfill channels that already have streams (default off = prior behavior). `match_by` remains a deliberate runtime no-op; the real fuzzy lever is `loose_name_match` + `min_score`. ([bead jnzst])
- Internal: resolved CodeQL code-quality findings (unused imports, import-cycles) surfaced during the 0.17.3 release cut (bd-0nabr); test-suite hardening from validity audit — strengthen error-swallow tests, CI anti-silent-skip guard for symlink-escape security tests, test-layer documentation, de-dup health probes (bd-1qnvg, build 0007); e2e test-isolation flake fixed twice (orphaned `create_task` coroutines bd-gqtyf build 0015; auth-fixture 429 retry with `Retry-After` honouring bd-4sned build 0024); worktree-bootstrap EACCES hardening + CLAUDE.md agent-isolation defaults encoded (builds 0016–0017); Stats hot-query unused MAX column drop (bd-ly5sa, build 0016); SLO-6 runbook + user-guide alert rename `rate`→`ratio` (bd-06jyv, build 0023).

### Fixed
- **Stats tab footer hover-scroll pushed the whole UI off-page (bd-tkcmu, build 0012; GH bug).** Hovering the bottom status bar on the Stats tab and scrolling propagated the event to the document root, dragging the entire UI viewport. Fix: `overflow:hidden` added to `html`/`body`/`#root` in `index.css` (an SPA never needs document-level scroll) plus `overscroll-behavior:contain` on `.stats-tab` and `.stats-content` as a belt-and-suspenders scroll-chain guard. (bd-tkcmu, build 0012)
- **`bulk_merge_channels` stale-ID hardening: target-ID 404 now returns 422 with a refresh hint, `source_names` made complete, non-404 errors re-raised correctly (bd-4xxax, build 0009).** The source-ID 404 guard (bd-ozhkf) was not mirrored on the target ID; a stale target fell through to the per-item catch-all, returning 200 + a failed count instead of the 422 refresh prompt. Fixed. Also: `source_names` is now populated in the 422 branch (one entry per requested source ID) to prevent a misaligned audit record if the path ever becomes partial-success; 410/500 upstream errors on both source and target correctly re-raise rather than being misclassified as stale-ID 422. (bd-4xxax, build 0009)
- **`get_channels(channel_group=0)` silently fetched all channels (bd-d5z9u, build 0009).** The `if channel_group:` guard in `dispatcharr_client.py` is falsy for `0`, so requests scoped to group 0 returned the unfiltered channel list. Fixed to `if channel_group is not None:`. (bd-d5z9u, build 0009)
- **Modal error banner suppressed when backend returns `detail: []` (bd-p3jpl, build 0024).** `extractDetail()` returned an empty string for empty FastAPI validation arrays, which the `{error && …}` render guard silently hid. Now trims the assembled string and falls back to `'Server returned an empty error'` so callers always receive a renderable message. (bd-p3jpl, build 0024)

### Security
- **Operator-only: all channel-configuration write endpoints now require admin (bd-um30y + bd-v7n9f, builds 0013–0014; auto-creation writes bd-757hc, build 0009).** ECM is an operator-only system; Dispatcharr-federated users are created `is_admin=False` by default, so authenticated non-admin principals could previously invoke destructive or bulk channel operations. Three separate gating passes closed this. **Build 0009 (bd-757hc):** 11 mutating `auto_creation.py` endpoints gated with `RequireAdminIfEnabled` (create/update/bulk-update/delete/reorder/toggle/duplicate rule, run pipeline, run rule, execution rollback, import YAML); read-only and validation endpoints stay ungated. **Build 0013 (bd-um30y):** destructive/bulk `channels.py` class gated — `clear-auto-created`, merge, bulk-merge, bulk-commit, assign-numbers, import-CSV. **Build 0014 (bd-v7n9f):** PO decision that ECM is operator-only extended the gate to all remaining channel-config writes — `create_channel`, `update_channel`, `delete_channel`, add-stream, add-streams, remove-stream, reorder-streams, and logo CRUD (create/upload/update/delete logo). Read-only GETs and preview/validation scan endpoints (`preview-csv`, `normalize-preview-batch`, `find-duplicates`) remain ungated. `RequireAdminIfEnabled` is a no-op when auth is disabled, so single-user deployments are unchanged. (bd-757hc, bd-um30y, bd-v7n9f, builds 0009/0013/0014)
- **SSRF denylist on media-server saved-URL test-connection endpoints (bd-fbc50, build 0024).** `_sanitize_base_url` in `routers/settings.py` already blocked non-`http`/`https` schemes but still permitted loopback and link-local hosts, leaving an admin-gated SSRF surface on the Emby/Plex/Jellyfin "Test Connection" endpoints. Two new stdlib-only helpers (`ipaddress` + `socket`, no regex): **`_ip_is_blocked`** denies loopback (`127.0.0.0/8`, `::1`), link-local IPv4 (`169.254.0.0/16`, including the AWS/GCP `169.254.169.254` metadata endpoint), link-local IPv6 (`fe80::/10`), and IPv4-mapped IPv6 unwrapping (`::ffff:127.x.x.x`, `::ffff:169.254.x.x`); RFC 1918 private ranges are intentionally left reachable (Plex/Emby/Jellyfin run on the operator's LAN). **`_host_is_blocked`** applies the IP check to literal IPs, blocks the `localhost` alias by name, and performs best-effort hostname resolution so a hostname resolving to a blocked IP is also caught; resolution failures do not block (offline LAN servers stay testable). Denied hosts return the same `{ok: false, error: <msg>}` envelope as the scheme-block path. (bd-fbc50, build 0024)
- **Reintroduced fuzzy matching is gated so it cannot recreate the ~1,341 false-positive merge incident (bead jnzst).** The admission decision is a single shared chokepoint (`dedup_matcher.is_admissible`) that the rule executor, the preview endpoint, and — transitively — both MCP write tools all funnel through, so no consumer can apply a score-only policy and re-admit an incident-class false positive. Invariants enforced + test-pinned: **callsign hard-reject** (when both names parse a callsign and they differ, the pair is rejected before any threshold, beating `min_score` and the tvg_id override); **callsign required on both sides by default** for Locals fuzzy (no-callsign pairs admitted only via explicit opt-in and only at score ≥ 0.90); **refuse unscoped scored fuzzy** (schema validation rejects a `min_score` rule without a non-empty `target_channel_in_group`; legacy `loose_name_match` rules without `min_score` are unaffected); **tvg_id-callsign override fails closed** under a name-callsign conflict; **mandatory dry-run** with a test asserting the preview path performs zero writes; **journaled scores + provenance** (score, effective threshold, signal fired, both parsed callsigns, rule id, allowlist) for forensic reconstruction; and **journal-driven surgical rollback** that removes only the stream-ids a fuzzy merge added (preserving edits made after the merge), falling back to snapshot-restore only when journal data is missing. The new preview endpoint is `RequireAdminIfEnabled`-gated with `group_ids` validation/caps to bound the N×M scoring surface. **ReDoS hardening** on the `LOCALS` cleaner: input is length-capped before any regex and whitespace is collapsed once up front so the source-prefix regex is linear-time (the prior pattern was O(n²) on whitespace-padded, attacker-controllable M3U names). The global exact-match rule and the `match_by` no-op are untouched. ([bead jnzst])

## [0.17.2] — 2026-05-23

### Added
- **MCP: expose the 0.17.0/0.17.1 Stats v2 + media-server attribution features through Claude (epic co5wh, v0.17.2-0002).** These features shipped to the UI/API but were never queryable via MCP. New/updated tools: `get_channel_stats` now renders the media-server attribution fields the Active Channels endpoint already returns (`emby_user_name`/`plex_user_name`/`jellyfin_user_name` + `*_viewers`, `client_ip`/`client_ips`, `provider_name`) so Claude can answer "who's watching channel X" (co5wh.1); a new `get_provider_stats` tool wraps the Providers panel (`/api/stats/providers/{buffering,watch-time,channel-heatmap,bitrate}`, GH-59) via a `metric` selector (co5wh.2); `get_user_watch_time` + `get_user_channel_breakdown` wrap the Users panel and per-source breakdowns (`/api/stats/watch-time`, `/users/dispatcharr/{id}`, `/users/emby/{id}`, GH-62; surfaces `attribution_source`) (co5wh.3); `get_trending` + `get_channel_popularity` wrap `/popularity/trending` and `/popularity/channel/{id}` (co5wh.4); and `get_activity` + `get_channel_bandwidth` round out stats coverage (co5wh.5). 8 new tools (124 total). All new contract entries match the backend's untyped-list OpenAPI shape (`response_is_list` default) to satisfy the cross-repo contract test. **Tests:** 30 new mcp-server tests; mcp-server suite 250 passed, contract test 120 passed. Live-verified against the real backend (provider watch-time, per-user totals with "via dispatcharr" attribution, activity feed). ([epic co5wh])

### Deprecated
- **`ECM_TELEMETRY_EXCLUDE_USERS` env var is deprecated; slated for removal in v0.18.0 (bead ye075).** The var was a bd-uqbob workaround for the ECM/Dispatcharr user-id namespace collision in the Stats v2 Watch Time panel — ECM's local `users` table and Dispatcharr's `users` table have separate, coincidentally-overlapping integer IDs, so a Dispatcharr viewer could be mis-attributed to an ECM login. bd-gsn3r's migration 0011 (`session_telemetry.dispatcharr_username` + FK drop) fixed that collision at the source, so the var is no longer needed for its original purpose. It still works for ad-hoc "exclude this Dispatcharr viewer" needs, but ECM now logs a one-time `[BANDWIDTH] ECM_TELEMETRY_EXCLUDE_USERS is set … but is DEPRECATED … will be REMOVED in v0.18.0` WARNING the first poll the var is observed set. Operator guide updated (`docs/user_guide/stats/stats-v2-history-cutover.md` → "Excluding a specific viewer"); v0.18.0 removal tracked under bead lbpl7. **Tests:** 2 new unit tests in `test_bandwidth_tracker_exclude_users.py` (warning fires exactly once when the var is set, silent when unset). ([bead ye075])

### Fixed
- **MCP display/data-rendering tail from the full test-plan sweep (epic lq38l: lq38l.11 + lq38l.13, v0.17.2).** `get_journal` returned "No journal entries found" for every call — it unwrapped a nonexistent `entries` key instead of the backend's `results` pagination envelope; it now reads `results` (None-safe, and appends `entity_name`). Plus a 12-item display/cosmetic cluster: channel numbers no longer render a `.0` float suffix (`list_channels`/`get_channel`/`create_channel`/`update_channel`); `get_streams_for_channel`/`get_streams_by_ids` resolve provider + group names instead of raw ids; `get_epg_grid` resolves real channel names via `channel_uuid` (was `[Unknown]`, and channel_id-filtering now returns results); `get_auto_creation_rule` extracts the create-channel descriptor (was `create_channel: ?`); `run_auto_creation` dry-run renders real sample names and a "N more" count consistent with the created total; `get_top_watched` shows `N views` from `watch_count` (the metric the endpoint provides) instead of `? unique viewers`; `create_export_profile` rejects an empty/blank name; `dismiss_channel_merge` returns a clean `MERGE_NOT_FOUND` envelope (matching `accept_channel_merge`); `list_pending_channel_merges` coerces `candidate_channel_id` to int; `get_probe_results` reports "No probe results available." when none completed; `cancel_probe` drops the contradictory "cancelled" wording when nothing is running; and `create_auto_creation_rule` gains `quality_tie_break_order` + `match_scope_target_group` params. All fixes verified against confirmed backend response shapes. **Tests:** 27 new mcp-server tests (`test_lq38l_journal_and_nits.py`); mcp-server suite 328 passed, contract test 122 passed. ([epic lq38l])
- **MCP integration: a batch of 21 fixes across the MCP tool surface, found by a live test sweep of all 116 tools (epic bd-1wq7z, v0.17.2-0001).** Two cross-cutting blockers plus a wave of per-tool correctness fixes. **Auth (bd-1wq7z.1):** the static `mcp_api_key` was rejected (401 "Malformed token") by every route guarded with `RequireAuthIfEnabled`/`RequireAdminIfEnabled` because the route dependency only accepted a JWT — `get_current_user` now also accepts the static key (constant-time compare) as an admin-equivalent service principal, aligning the dependency layer with the grant the global middleware already makes; this revives the dedup tools, `add_stream` prompt/merge modes, and all backup tools. **Connectivity (bd-1wq7z.2):** documented that host-networking deployments must set `ECM_URL=http://localhost:6100` (the `ecm` service name doesn't resolve on a shared host network) — the symmetric partner to the existing `MCP_HOST` callout. **Data loss (bd-1wq7z.3 + bd-1wq7z.25):** `reorder-streams` and the bulk-commit reorder path REPLACED a channel's stream set, so a partial list silently detached the omitted streams; both now validate the list is a true permutation of the channel's current streams and reject (HTTP 400 / per-op error) instead of detaching. **Crashes from un-unwrapped response envelopes (bd-1wq7z.4):** `get_orphaned_groups`, `get_auto_created_groups`, `get_groups_with_streams`, and `list_tasks` iterated the backend's `{...: [...]}` dict and raised `'str' object has no attribute 'get'` — all now unwrap correctly. **Wrong result keys (bd-1wq7z.5):** `match_channels_epg` (read `matched`/`unmatched` vs the real `exact`/`multiple`/`none`, and sent no request body → 422), `delete_orphaned_groups` (`deleted`/`groups` vs `deleted_groups`/`failed_groups`), and `rollback_auto_creation` (`deleted` vs `entities_removed`) all reported zero/garbled results. **Other tool fixes:** `add_stream` now prefers an exact stream-name match instead of the first substring hit (bd-1wq7z.6); `update_m3u_group_settings`/`bulk_update_m3u_group_settings` send the backend's structured `{group_settings:[{channel_group:<int>,enabled}]}` body instead of a name-keyed dict (bd-1wq7z.7); `run_auto_creation` polls the execution to completion instead of reporting the 202 accept envelope as a finished "0 channels" run (bd-1wq7z.8); `create_epg_source` sends the required `source_type` (bd-1wq7z.9); `list_epg_sources` no longer crashes on a null `url` (bd-1wq7z.10); `list_channels(group_id=…)` works again — the backend now resolves the group id to the group name Dispatcharr filters on (bd-1wq7z.11); `cancel_task` reports the real status instead of always "cancelled" (bd-1wq7z.12); `apply_profile_to_channels` sends an explicit `enabled` instead of silently enabling every targeted channel (bd-1wq7z.13); `delete_all_notifications` can target unread via `include_unread` (bd-1wq7z.14); `delete_channel_group` reports "hidden" vs "deleted" for M3U-synced groups (bd-1wq7z.15); `cleanup_struck_out_streams` surfaces channel-deletion failures instead of swallowing them (bd-1wq7z.16); `probe_single_stream` uses a 300s timeout like the bulk variants (bd-1wq7z.17); `clear_auto_created` requires explicit `group_ids` or `all_groups=True` so an empty list can't trigger a system-wide clear (bd-1wq7z.18); `create_task_schedule` accepts a `timezone` (bd-1wq7z.19); a new `list_publish_configs` tool gives `publish_export` a config-id discovery path (bd-1wq7z.21); and several backend routes (`create_channel`, `get_channel`, `delete_channel_group`, `create_epg_source`) now surface the upstream Dispatcharr 4xx detail (400/404) instead of an opaque 500 (bd-1wq7z.22). **Tests:** ~80 new unit/integration tests across the mcp-server and backend suites; mcp-server suite green (220 passed), backend suite green (4805 passed, 69 skipped). ([epic bd-1wq7z])

### Security
- **MCP static-key hardening tail (1wq7z.24 + i3axt, v0.17.2).** Low-severity hardening on the PO-locked permanent static-key auth path (threat model EP2; none reachable by the real MCP client). The static-key comparisons in `mcp-server/server.py` and `backend/main.py` now use `hmac.compare_digest` (constant-time) instead of `!=`/`==`, removing a theoretical timing oracle; the MCP resource server also parses the `Bearer` scheme case-insensitively and strips credential whitespace per RFC 6750 §2.1 (malformed/empty headers still fail safe with 401). Self-mutation auth routes (`PUT /api/auth/me`, `POST /api/auth/change-password`) now reject the transient MCP service principal with a clean 403 instead of raising a 500 (`session.refresh` on a non-DB `User`). The MCP threat model gains an EP3 row documenting that `mcp_api_key` is admin-equivalent **including ECM user-account management** (confirmed redacted in backups via `_SETTINGS_CREDENTIAL_FIELDS`). **Tests:** 9 backend + 25 mcp-server security tests. ([1wq7z.24], [i3axt])

## [0.17.1] — 2026-05-22

### Added
- **Stats: surface the real requestor device IP from media-server sessions as a separate "Client IP" field (bd-7ncci, v0.17.1-0072).** The Stats → Active Channels → Connected Clients view shows the Dispatcharr-observed connection IP, which for a media-server-mediated stream is the media server's / Docker bridge's IP — not the device the human is actually watching on. Emby and Jellyfin sessions expose the real device IP in `RemoteEndPoint`; Plex exposes it in `<Player address=...>`. ECM already captured these into `EmbySession.remote_endpoint` / `JellyfinSession.remote_endpoint` / `PlexSession.remote_endpoint` but dropped them at the attribution boundary. This change threads the matched session's real client IP through the full attribution pipeline and surfaces it as a NEW, SEPARATE field — the existing Dispatcharr connection IP display is unchanged. **Step 0 live verification (ecm-ecm-1, all three sources enabled):** Emby `RemoteEndPoint` confirmed live as a bare IP (`gbsweep`=`159.112.42.143`); Jellyfin/Plex confirmed via the identical capture paths + existing client-parse tests (no active session at probe time). Normalization strips a `host:port` suffix, unwraps bracketed IPv6 (`[2001:db8::1]:51514` → `2001:db8::1`), takes the first hop of an XFF-style list, and validates the result is a real IP (junk → blank). **Plumbing:** new source-agnostic `normalize_client_ip()` + `rollup_client_ips()` in `services/attribution_reconciler.py`; `client_ip` added to `EmbyAttribution`/`JellyfinAttribution`/`PlexAttribution` (threaded from each matched session's `remote_endpoint`) and to the reconciler's `CandidateUser`, so it rides through every assignment shape. **API (live path `routers/stats.py`):** each per-client connection now carries a `client_ip` (the single attributed viewer's real device IP) and `client_ips` (the distinct set for a server-proxy or Option-B rollup connection), plus a `client_ip` key inside each `*_viewers` dict; both seeded blank (`null` / `[]`) so the TypeScript shape is stable for unattributed and direct-XC connections. **Persisted path (`bandwidth_tracker.py`):** `client_ip` rides inside the existing `*_viewers` JSON columns (no schema migration). **Frontend (`StatsTab.tsx` + `types/index.ts` + `StatsTab.css`):** a separate "Client IP" element renders next to the connection IP when present (single IP, or comma-joined set for proxy/rollup), blank otherwise. **Attribution correctness is unchanged** — this is a display-only field; all bd-mlcla/bd-rools invariants (anti-collapse, anti-broadcast, account-identity discriminator, B1 direct-first/proxy-remainder, Option-B rollup) are untouched. **Live end-to-end (ecm-ecm-1, deployed code, real Emby session):** the production `_enrich_channels_with_attribution` path attributed `gbsweep` and stamped the real device IP `159.112.42.143` into the new `client_ip` field while the connection's observed `ip_address` stayed unchanged; an unassigned connection showed `client_ip` blank. **Tests:** resolver client-IP capture + normalization (host:port/IPv6/missing) per source; reconciler `client_ip` single + `client_ips` rollup set + unattributed→none; stats live-path attributed→populated / unattributed→blank / proxy→IP set; persisted-path `client_ip` in viewers JSON (populated + null); frontend renders the field when present, omits when absent, and renders the rollup set. Full backend + frontend suites green. ([bd-7ncci], v0.17.1-0072)
- **CI: gate mcp-server/tests/ suite before any OAuth work (bd-ak7xa, v0.17.1-0067).** Hygiene precondition for the OAuth 2.1 epic (bd-buiqr). `mcp-server/tests/` (72 tests, 4 files, ~1.6k LOC) was not gated by CI — `.github/workflows/test.yml` only ran `backend/tests/` and frontend, so any new OAuth tests would have rotted silently. **`.github/workflows/test.yml`**: new `mcp-server` job (check name "MCP Server Tests") mirrors the backend job structure — checkout, Python 3.12 setup with pip cache keyed on `mcp-server/requirements.txt`, install runtime deps + pin `pytest pytest-asyncio pytest-cov`, run pytest with `--tb=short --no-header -p no:warnings --cov=. --cov-config=.coveragerc --cov-fail-under=26 --junitxml=junit.xml`, upload `mcp-server-coverage` artifact, upload `junit-mcp` artifact (30-day retention to match the flake-pr-comment aggregator window). **`mcp-server/.coveragerc`** (new): `source = .` with `tests/` and `__pycache__/*` omitted so the self-coverage of test files doesn't inflate the TOTAL. Baseline measured 2026-05-19 at **28%** line coverage on dev tip; floor set at **26%** per the 2-point regression buffer in `docs/testing.md` § "Coverage ratchet cadence". **`scripts/aggregate_flakes.py`**: `JUNIT_ARTIFACT_NAMES` extended with `junit-mcp` so the existing flake-pr-comment workflow picks up mcp-server failures alongside backend and frontend. **Operator-visible follow-up**: branch protection on `dev` (and `main`) does not include the new "MCP Server Tests" check by default — the PO must add it via the GitHub branch-protection UI after this PR lands so the gate becomes blocking. Until then the job runs and reports status but cannot block a merge. **Tests:** all 72 mcp-server tests pass clean locally (`72 passed in 2.37s` with `Required test coverage of 26% reached. Total coverage: 28.14%`). ([bd-ak7xa], v0.17.1-0067)

### Fixed
- **SMTP "Test Connection" failed with 530 Authentication Required even though scheduled notification emails sent fine (bd-air4z, GH #380, v0.17.1-0090).** The Settings UI never re-sends the saved SMTP password — it is masked and cleared on load (`setSmtpPassword('') // Never load password from server` in `SettingsTab.tsx`) — so a Test Connection request normally arrives with `smtp_password=""`. The `POST /api/settings/test-smtp` handler called `server.login()` only `if request.smtp_user and request.smtp_password`, so with the empty password it skipped authentication entirely and Gmail rejected the send with `530 5.7.0 Authentication Required`. The scheduled-notification path (`tasks/m3u_digest.py`, `alert_methods_smtp.py`) reads the stored password directly and was unaffected — exactly matching the reporter's observation that overnight digest emails arrived while the test kept failing. **Fix:** `test_smtp_connection` now resolves credentials with the same preserve-on-omit contract as `update_settings` — an empty `smtp_user`/`smtp_password` in the request falls back to the stored settings before `server.login()`. A password typed into the test form still takes precedence (operators validating new credentials before saving). **Tests:** 2 new cases in `test_settings.py::TestTestSMTP` — `test_falls_back_to_stored_password_when_omitted` (asserts `login()` is called with the stored secret when the request omits the password) and `test_uses_request_password_over_stored`. Full backend suite green (4859 passed, 1 skipped). ([bd-air4z], GH #380, v0.17.1-0090)
- **Per-connection Dispatcharr-account discriminator: a genuine direct XC subscriber no longer absorbs a media-server viewer it shares a channel with (bd-rools, re-fix for the bd-cat70 direct-client cross-attribution, v0.17.1-0071).** Fix-forward for bd-4w9w6 (PR #366, already merged into `dev` at v0.17.1-0070). bd-4w9w6 fixed the "User #0" bug by setting `has_url_identity=False` for every proxy connection — correct, but because the bd-mlcla resolver is no longer IP-gated (it returns the channel's full matched-user set for ANY source IP), removing the only discriminator re-exposed the **bd-cat70 direct-client cross-attribution**: a genuine direct XC subscriber (e.g. `kmfelmer`, identified by a real Dispatcharr account `user_id`) sharing a channel with a media-server viewer (`MotWakorb`) became eligible for reconciliation and, via the B1 direct-first ordering, ABSORBED the media-server user — `kmfelmer`'s row showed `MotWakorb` and the real Emby/Jellyfin viewer dropped to "User #0". **Root cause:** the bd-4w9w6 commit had NO per-connection discriminator at all — the Dispatcharr ACCOUNT identity present on the `/proxy/ts/status` client dict (`user_id`/`username`) was read for the username-join but never carried into the reconciler's `Connection`. **Fix:** a real per-connection discriminator `Connection.has_account_identity`, set at BOTH call sites (`routers/stats.py` `_build_attribution_connections` and `bandwidth_tracker.py` `_build_persisted_connection`) from the Dispatcharr account `user_id`/`username` via the shared `services.attribution_reconciler.has_dispatcharr_account_identity()` helper. A connection with a genuine account identity (positive `user_id`, or a real non-sentinel `username`) is a direct-IPTV subscriber: it is excluded from `eligible_connections()` and never flagged `is_server_proxy`, keeping its existing Dispatcharr/bd-gy5nd attribution. Anonymous connections — Dispatcharr `user_id` `"0"`/`0`/`None`, which is how Dispatcharr serves EVERY media-server pull (the transcoding proxy AND NAT'd browser-direct playback) — carry no account identity and stay eligible. The `bandwidth_tracker` persisted snapshot now threads `user_id` through `client_meta` so the persisted path applies the identical discriminator. The channel-upstream URL is no longer used for eligibility (bd-4w9w6); the bd-gy5nd `provider_name` label remains independent. **Step 0 live data verification (Dispatcharr `172.16.0.19:9191`):** the active media-server pull on the live channel carried `user_id="0"` (anonymous); the Dispatcharr account list confirms genuine subscribers (`kmfelmer`=`user_id 3`, etc.) carry distinct positive ids — the two populations are cleanly separable by account-identity presence. **Live end-to-end verification (ecm-ecm-1, deployed code, real Emby with 7 active sessions):** (a) TSN5/`172.18.0.1` anonymous NAT'd browser-direct → `MotWakorb` (User#0 fix holds); (b) a shared channel with `kmfelmer` (user_id 3, ranked first) + an anonymous pull → `kmfelmer` keeps `emby_user_name=None` (NOT cross-attributed) and the anonymous pull gets `MotWakorb`; (c) jkaisersoze (4 distinct anonymous connections, 1 user) → exactly ONE connection attributed, the rest User #0 (no collapse). **Tests:** repaired the bd-cat70 regression test that previously passed for the WRONG reason (it used an IP-gated resolver mock — old semantics) — `test_direct_xc_subscriber_does_not_absorb_media_server_viewer` and `test_single_user_not_broadcast_to_every_eligible_connection` now use realistic non-IP-gated resolver semantics and assert the account-identity protection; both proven to FAIL on pre-fix code and PASS after. New `TestAccountIdentityHelper` + eligibility cases in `test_attribution_reconciler.py`, and persisted-path `test_nat_browser_direct_no_account_stays_eligible_persisted_bd_rools` + `test_direct_subscriber_does_not_absorb_media_viewer_persisted_bd_rools`. All bd-mlcla invariants (anti-collapse jkaisersoze, anti-broadcast, B1 direct-first/proxy-remainder, Option-B rollup) and the bd-4w9w6 User#0 end-to-end tests remain green. Full backend suite green (4573 passed, 1 skipped). ([bd-rools], re-fix for [bd-cat70], fix-forward for [bd-4w9w6]/[bd-mlcla], v0.17.1-0071)
- **Attribution integration gap: an XC-credentialed UPSTREAM channel URL silently excluded every media-server viewer from reconciliation → "User #0" (bd-4w9w6, fix-forward for bd-mlcla, v0.17.1-0070).** Live bug on the PO's container, deployed v0.17.1-0069: the Jellyfin resolver correctly matched the browser-direct TSN5 connection (`[JELLYFIN-RESOLVER] tier_match ecm_channel='494 | TSN 5' match_count=1 top_user=MotWakorb`, client `172.18.0.1` NAT'd through the Docker bridge, `ip_is_server=False`), but the Stats page still showed "User #0" and ZERO reconciler result-log lines appeared — the resolver's match never reached the connection. **Root cause:** bd-mlcla's "no-URL-identity" discriminator was derived from the channel's **upstream provider URL** (`ch["url"]`, e.g. `https://infinity.gives/live/mot/16118141/108495.ts`). That URL embeds the operator's **Xtream-Codes provider account** credentials (`/live/<user>/<pass>/<id>.ts`), which `url_embeds_username()` matches — so `has_url_identity` was `True` for the channel, `eligible_connections()` filtered the connection out, and `_enrich_one_source` returned early at the empty-`eligible_ips` guard before ever calling the reconciler. Because virtually every channel in an XC-sourced deployment carries such a URL, this excluded **all** media-server-mediated viewers, not just genuine direct-IPTV clients. The discriminator conflated the channel's upstream SOURCE (a per-channel provider account, shared by all viewers) with the client's IDENTITY. Dispatcharr `/proxy/ts/status` connections are proxy clients — their payload carries only `client_id`/`ip_address`/`user_agent`/`connected_at`, never per-client credentials — so the upstream URL is never a per-client signal. **Fix:** both call sites (`routers/stats.py` `_build_attribution_connections` and `bandwidth_tracker.py` `_reconcile_persisted_attributions`) now set `has_url_identity=False` for every proxy connection; the channel-upstream URL no longer gates reconciliation. The bd-gy5nd provider/hostname label is computed independently and continues to surface alongside the media-server user (the two are not mutually exclusive — a viewer can watch an XC-sourced channel through Jellyfin/Emby/Plex). All bd-mlcla invariants are unchanged (anti-collapse, anti-broadcast, direct-first/proxy-remainder B1, Option-B rollup, no phantom clients) — only the (always-wrong) eligibility gate is removed. **Diagnosability:** the reconciler now emits a concise INFO `[RECONCILE] channel=… conns=N eligible=N candidate_users=[…] assignments={client_id->user} unattributed=N` result line on every reconciliation (it previously had NO result logging, which is why this class of bug produced zero downstream log lines). **Live verification (ecm-ecm-1, v0.17.1-0070, real HTTP `/api/stats/channels` through the auth stack):** the TSN5 / `172.18.0.1` connection now returns `jellyfin_user_name="MotWakorb"` + `jellyfin_viewers=[{user_id:72be7…, user_name:MotWakorb}]`, with `provider_name="Infinity"` coexisting, and `emby_user_name`/`plex_user_name` correctly `None` (those sources run and don't match — the multi-source case where one match survives others' empty results). **Tests:** the synthetic bd-mlcla call-site tests used a non-credentialed Dispatcharr proxy URL as `ch["url"]`, which is why they passed while the real path (XC upstream URL) was broken — root-caused and corrected. New end-to-end regressions drive the realistic XC upstream URL through the full `_enrich_channels_with_attribution` (live path) and `_collect_stats` (persisted path) wiring: `TestStatsBd4w9w6XcUpstreamUrl` (live TSN5/Jellyfin multi-source match survival + `[RECONCILE]` logging assertion), the retargeted `test_xc_sourced_channel_still_attributes_via_media_server`, and `test_xc_upstream_url_jellyfin_match_persisted_bd4w9w6` — all proven to FAIL on pre-fix code (`None`/NULL attribution, no `[RECONCILE]` line) and PASS after. Full backend suite green (4548 passed, 1 skipped). ([bd-4w9w6], fix-forward for [bd-mlcla], v0.17.1-0070)
- **Media-server attribution is now networking-agnostic — per-channel set reconciliation replaces the brittle IP gate (bd-mlcla, supersedes bd-podx3/bd-ost8o/bd-cat70 attribution model, v0.17.1-0069).** Live bug on the PO's container: browser-direct Jellyfin playback (Jellyfin Web) is NAT'd through the media box's Docker bridge gateway, so ECM observed source `172.18.0.1` instead of the configured `172.16.0.19`. The strict resolver IP gate restored by the bd-podx3 revert (`if ecm_session_ip != server_ip: return []`) rejected it → "User #0". **Core change:** Dispatcharr `/proxy/ts/status` connections carry a stable `client_id`; media-server sessions carry a stable `session_id` + `user_name` + `last_activity_date`. The two sides' source IPs do NOT correlate across the NAT boundary, so attribution is now a per-channel **set reconciliation** keyed on stable ids, NOT an IP join. **Resolver layer (`backend/services/{emby,jellyfin,plex}_resolver.py`):** the hard `return []` IP gate is removed — the resolver runs the existing Tier 1 (channel-name strict) + Tier 2 (channel-number strict) match for ANY source IP and returns the distinct matched-user set for the channel. Tier 3 (fuzzy stream-name) stays gated to the server-IP case only (`allow_fuzzy_tier3=ecm_session_ip==server_ip`) — fuzzy matching with no IP signal is the documented bd-ost8o VOD-onto-live false-positive vector. Source IP becomes a soft RANKING hint, never a reject; the `resolver_call` INFO log now carries `ip_is_server=<bool>` (rank-not-gate) instead of the old `ip_mismatch` reject. **Reconciliation layer (new `backend/services/attribution_reconciler.py`, used by both `routers/stats.py` `_enrich_one_source` and `bandwidth_tracker.py` `_resolve_attributions`):** eligible connections = clients with NO URL identity (channels whose active stream URL embeds XC/M3U credentials keep the bd-gy5nd provider path and are excluded — the discriminator that replaces the IP gate); candidate users = the resolver's distinct set. **Each user is assigned to AT MOST ONE connection** (structural anti-collapse — the jkaisersoze regression stays fixed). **Direct (browser-direct/NAT'd) connections reconcile FIRST**, each consuming a distinct user; a server-proxy connection (source IP == resolved media-server IP) then carries the **remaining (unconsumed)** users as its rollup (bd-mlcla B1 — PO directive "never drop the viewer"). This guarantees a browser-direct viewer sharing a channel with a tuner/proxy pull always gets its own distinct name when an unconsumed matching user exists (the TSN5 mixed case), while a proxy serving N app-viewers with no direct connection still carries the full set unchanged (preserves the bd-r5f0c.9 multi-viewer-on-one-proxy case). The `is_server_proxy` flag is exact source-IP-equality with no corroborating signal, so a browser-direct connection whose observed IP happens to equal the server IP is mis-flagged as the proxy; the direct-first ordering bounds the blast radius to that one connection's display label. Count behavior: `users==connections` → exact 1:1; `users<connections` → fill top-ranked, rest stay User #0 (never broadcast — the bd-cat70 fix); `users>connections` → surplus surfaces only in the channel-level `*_viewers` list (no phantom clients). **Residual ambiguity = Option B:** when a group of browser-direct connections is genuinely indistinguishable (same IP-priority bucket, no per-connection signal) AND there are 2+ candidate users, each row shows a `"N viewers: <comma-list>"` rollup instead of a possibly-wrong single pinned name. **Soft IP ranking (`backend/config.py`):** new operator setting `trusted_media_networks` (CIDRs/IPs, default empty) + auto-detected local Docker bridge gateways feed candidate RANKING only — getting them wrong changes tie-break order, never which user attributes (asserted by a flip-the-gateway test). New Settings → Trusted Media Networks field + TS types. **Live verification (ecm-ecm-1, v0.17.1-0069):** TSN5 / `172.18.0.1` browser-direct now attributes to `MotWakorb` (no more User #0); CNBC server-mediated from `172.16.0.19` still attributes; jkaisersoze (3 distinct IPs, 1 user) attributes to exactly 1 connection, never broadcast. **Tests:** new `test_attribution_reconciler.py` (40 cases — anti-collapse/anti-broadcast/no-phantom/Option-B/topology-agnostic/IP-ranking-correctness-neutral), `test_config_trusted_media_networks.py`, `test_bandwidth_tracker_mlcla_attribution.py` (persisted-path bridge-NAT/anti-collapse/anti-broadcast/Option-B), bd-mlcla stats reconciliation cases in `test_stats_attribution.py`, retargeted resolver IP-gate tests (off-server traffic now strict-matches; the bd-podx3 `TestPodx3DistinctIpsDoNotCollapse` class supersedes the gate contract — collapse-prevention moved to the reconciler), plus an Option-B rollup render test in `StatsTab.test.tsx`. Full backend suite + frontend suite green. ([bd-mlcla], supersedes [bd-podx3]/[bd-ost8o]/[bd-cat70], cross-ref [bd-gy5nd], v0.17.1-0069)
- **Stats attribution collapse: distinct client IPs all mapped to one media-server user — revert bd-ost8o two-mode dispatch (bd-podx3, fixes bd-ost8o, v0.17.1-0068).** Live production regression: on the PO's Stats page, four connected clients — each from a DIFFERENT IP — were all attributed to the single Emby user ``jkaisersoze``. **Root cause (confirmed by live container logs):** bd-ost8o (v0.17.1-0062) removed the strict IP-gate short-circuit (``if ecm_session_ip != server_ip: return []``) in all three resolvers (``emby_resolver.py`` / ``jellyfin_resolver.py`` / ``plex_resolver.py``) and replaced it with a two-mode dispatch. Its new ``direct_fallback`` mode (Mode B, run for any IP that is NOT the media-server IP) matched an ECM client to a media-server session by **channel name/number alone, with no signal tying the client to the session.** Every ECM client watching channel ``679 | Milwaukee Brewers`` — regardless of its physical IP (``98.144.168.47``, ``192.168.1.108``, ``172.16.0.19``) — resolved to whichever Emby user (``jkaisersoze``) was watching that same channel. bd-ost8o's ``ambiguous_skip`` guard only fired when 2+ *media-server sessions* matched; it did nothing about the actual failure mode (1 session, N ECM clients). The pre-bd-ost8o strict gate was the load-bearing correctness guarantee: only traffic egressing through the media server's IP can be a genuine media-server viewer. bd-cat70's per-IP enrichment scoping in ``stats.py`` was verified correct and is NOT the cause — the collapse was entirely in the resolver returning a positive match for non-viewer IPs. **Fix vs. forward-fix decision:** reverted bd-ost8o's resolver behavior change (restored the strict IP gate; removed ``mode`` dispatch, the ``direct_fallback`` branch, the ``ambiguous_skip`` guard, and the Mode-B tier-3 gate) in all three resolvers. A forward fix preserving browser-direct-play would need a real per-client identity signal (the captured-but-unused ``EmbySession.remote_endpoint`` client IP), which is net-new design unsuitable for a live hotfix — browser-direct-play attribution is deferred to a fresh bead. Mode B was unsound by construction: channel-name matching cannot identify a specific viewer, so it systematically mis-attributed every co-channel non-media-server viewer. The functional resolver code is now byte-identical to the pre-bd-ost8o (v0.17.1-0061) version. **Live verification (PO's ecm-ecm-1 container):** after deploy + restart, ``mode=`` log markers are gone (new code live) and ``[EMBY-RESOLVER] resolver_call`` now fires ONLY for the Emby server IP ``172.16.0.19`` — the non-server client IPs short-circuit at the restored gate and ``jkaisersoze`` is no longer broadcast across four IPs. **Tests:** reverted the bd-ost8o Mode-B test additions across all three ``test_*_resolver.py`` files (restored the strict-gate test contract), plus a new regression class ``TestPodx3DistinctIpsDoNotCollapse`` in ``test_emby_resolver.py`` reproducing the live scenario — three distinct non-server client IPs on the same channel as a matching Emby session each return ``[]`` (proven to FAIL against the bd-ost8o resolver and PASS against the fix), the genuine server-IP client still attributes correctly, and the non-server path does not consult the session cache. Resolver suites: 194 passed; stats suites: 265 passed. ([bd-podx3], fixes [bd-ost8o], v0.17.1-0068)
- **README: surface MCP_HOST upgrade callout for host-networking operators (bd-0eimc, v0.17.1-0066).** Operators running ECM and ecm-mcp with `network_mode: host` who pull dev after v0.17.1-0064 will silently break: the probe default changed from `localhost` to `ecm-mcp`, and `ecm-mcp` does not resolve on host-networking topologies. The existing "Reaching the MCP container from ECM" paragraph in README covered the configuration option but read as new-installer guidance, not an action item for upgraders. Added a distinct blockquote callout immediately below that paragraph labelled "Upgrade note (v0.17.1-0066+)" explaining the behavior change, who is affected, and the exact fix (`MCP_HOST=localhost` in the ECM service's environment block).
- **MCP Integration: copy buttons silently failed on plain-HTTP deployments + missing Node.js prerequisite docs (bd-rowlk + bd-fpjz3, v0.17.1-0065).** Two related Settings → MCP Integration fixes shipped together. **bd-rowlk (copy buttons):** the four copy buttons on the MCP Integration panel (API Key, MCP Endpoint URL, Claude Desktop config, Claude Code config) used `navigator.clipboard.writeText` directly. `navigator.clipboard` is gated behind a secure-context check by the browser, so on plain-HTTP deployments (the typical ECM home-server shape on a LAN IP) the API is undefined; the call silently rejected and the "Copied to clipboard" toast fired regardless. **`frontend/src/components/settings/MCPSettingsSection.tsx`**: `handleCopy` now uses the project's existing `copyToClipboard` utility (`frontend/src/utils/clipboard.ts`) which falls back to `document.execCommand('copy')` on HTTP origins where `navigator.clipboard` is undefined. The other ECM components (`StreamListItem`, `ChannelListItem`, `PreviewStreamModal`, etc.) already use this utility; `MCPSettingsSection` was the outlier. The four call sites now pass a label argument and surface a `notifications.error('Failed to copy to clipboard')` toast on failure instead of falsely claiming success. **bd-fpjz3 (Node.js prereq docs):** Claude Desktop does not bundle Node.js, but the `mcp-remote` bridge in our published `claude_desktop_config.json` snippet runs via `npx`. Operators without Node hit `spawn npx ENOENT` in Claude Desktop's logs after pasting the config — README and the in-app Settings panel did not call out the prerequisite. **`README.md`**: new prerequisite callout under the Claude Desktop config block explaining Node.js LTS 18+ is required, with install pointers (`winget install OpenJS.NodeJS.LTS`, `brew install node`, `apt install nodejs npm`). Also adds a "Why not Claude Desktop's Custom Connectors (no-Node path)?" note explaining that Claude Desktop's built-in Custom Connectors UI would let you skip Node entirely but requires OAuth 2.1 with PKCE per the MCP spec — ECM's MCP server currently uses a static API key, so the Custom Connector path is **not supported yet** (tracked as enhancedchannelmanager-buiqr). Claude Code is unaffected by the Node requirement — it talks Streamable HTTP directly. **`frontend/src/components/settings/MCPSettingsSection.tsx` + `.css`**: matching in-app callout (`.mcp-prereq-note` class with accent-bordered styling) above the Claude Desktop config snippet so operators see the same warning in the UI without reading the README. **No new tests** — the existing `MCPSettingsSection.test.tsx` suite covers the Server Status diagnostic panel (bd-ix1g6); copy-button behavior is covered indirectly by the `copyToClipboard` utility's own test suite (`clipboard.test.ts`). Full frontend suite stays green. ([bd-rowlk] + [bd-fpjz3], v0.17.1-0065)
- **MCP Integration status badge always showed "not reachable" on canonical Docker compose (bd-d2171, v0.17.1-0064).** `GET /api/settings/mcp-status` hard-coded `http://localhost:6101/health` as the probe target. Under canonical bridge networking (`docker-compose.mcp.yml` — MCP runs as a separate `ecm-mcp` container), `localhost` from inside ECM resolves to ECM itself, not the MCP container, so the probe always failed and the Settings → MCP Integration badge displayed "MCP server not reachable" even when MCP was healthy. The bug was invisible to operators running `network_mode: host` on both containers because the host network namespace is shared. **`backend/routers/settings.py`**: new `MCP_HOST` env var, defaults to `ecm-mcp` (matches the canonical compose service name), URL is built as `http://{MCP_HOST}:{MCP_PORT}/health`. **Operator action for `network_mode: host` deployments**: set `MCP_HOST=localhost` on the `ecm` service in your compose file to restore the previous behavior. **README**: one paragraph under "With MCP Server" explains the default and the host-networking override. **Tests:** 2 new tests in `backend/tests/routers/test_settings.py::TestMCPStatusHostResolution` lock both contracts — unset env → `http://ecm-mcp:6101/health`, `MCP_HOST=localhost` → `http://localhost:6101/health`. Uses `monkeypatch.setenv`/`delenv` and a captor stub for `httpx.AsyncClient`. Full backend suite (3808 passed, 1 skipped) stays green. ([bd-d2171], v0.17.1-0064)
- **Stats page shows "Unknown" provider on live channels — replace broken proxy-session-UUID channel-streams fallback with URL-hostname matching against M3U accounts (bd-gy5nd, v0.17.1-0063).** PO upgraded to v0.17.1-0062 and the Stats page still showed "Unknown" for the provider on the channel they were watching. Live probe of the PO's ecm-ecm-1 container showed the actual failure: ECM's resolver was calling ``GET /api/channels/channels/<uuid>/streams/`` with the proxy-session UUID from ``/proxy/ts/status`` (the ``channel_id`` field on that payload, e.g. ``"893c18f3-5aaf-4254-b1d0-3b926deb4c78"``), but that endpoint expects a Dispatcharr REST channel id (integer or REST UUID — different identifier space). Dispatcharr returned the SPA ``index.html`` (200 + ``text/html``) because no API route matched, JSON parse blew up on ``<`` and the resolver logged ``[STATS_V2] provider_resolution_failed reason=channel_streams_lookup_raised`` every 10s. The original bd-5g7kx channel-streams URL-match fallback (which depended on ``get_channel_streams(<channel_uuid>)``) has never worked for proxy-session UUIDs and is structurally broken — superseded by the bd-gy5nd path. The original bd-ef1go framing diagnosed this as "Dispatcharr returns empty body" — wrong; the body is the SPA HTML, not empty. ``backend/bandwidth_tracker.py``: new ``_match_provider_from_url(url, m3u_accounts)`` derives provider identity from the ``url`` field that is already in the ``/proxy/ts/status`` snapshot — parses the URL hostname (``infinity.gives``), matches against each M3U account's ``server_url`` hostname (case-insensitive, port-tolerant), and returns the matched account's ``id``/``name`` plus the bare hostname. When no M3U account matches the hostname, the function still returns the bare hostname so the response always carries a meaningful provider string for the operator. ``resolve_active_channel_streams`` now fetches the M3U accounts list once per poll (best-effort, empty list on failure — never blocks the resolver) and uses the URL-hostname match as the fallback path when the stream-id direct lookup misses. The legacy ``_resolve_via_channel_streams`` channel-streams fallback that called ``client.get_channel_streams(<channel_uuid>)`` is removed because the identifier is wrong — that endpoint never resolved for proxy-session UUIDs. ``ProviderResolution`` NamedTuple gains two trailing fields ``provider_name: Optional[str]`` and ``hostname: Optional[str]`` (defaults preserve existing call sites), populated whenever URL parsing succeeds even when no M3U match exists. ``backend/dispatcharr_client.py``: ``get_channel_streams(channel_id: int)`` keeps its existing integer-ID contract — only the proxy-session-UUID misuse goes away. ``backend/routers/stats.py``: the Active Channels endpoint surfaces ``m3u_account_id``/``provider_name``/``provider_hostname`` per channel; the prior ``stream_name`` field stays populated when the direct-id path resolves. ``frontend/src/components/tabs/StatsTab.tsx``: when a channel carries a backend ``provider_name`` (M3U source name or bare hostname), the badge derivation uses that string; only truly null provider attribution (no URL, no hostname) falls into the existing "Unknown" bucket. The 10s WARN spam is replaced with an INFO-level ``[STATS_V2] provider_resolved channel=<uuid> host=<hostname> source=<account-name-or-null>`` line per channel when URL-hostname resolution succeeds, and a single ``provider_resolution resolved=N unresolved=N`` SLI summary per poll as before. **Critical premise correction**: the brief assumed an ECM-side ``M3USource`` model in ``backend/models.py``; reality is that M3U accounts live in Dispatcharr (``/api/m3u/accounts/``) and ECM consumes them live via ``client.get_m3u_accounts()``. The fix design is functionally identical — the source of truth is Dispatcharr, not ECM's DB. **Tests**: 7 new tests in ``backend/tests/unit/test_bandwidth_tracker_url_provider.py`` lock the URL-hostname match contract (M3U match by hostname, port-tolerance, scheme-tolerance, query-string-tolerance, no-match returns bare hostname, malformed URL returns None triple, M3U accounts fetch failure degrades gracefully). 4 new tests in ``backend/tests/routers/test_stats.py::TestChannelStatsUrlProvider`` lock the endpoint contract (resolved M3U name surfaces, bare hostname surfaces when no M3U match, ``stream_name`` still resolves via stream-id direct path, no ``provider_resolution_failed`` WARN emitted for the URL-hostname path). Existing ``channel_streams_fallback_*`` tests retargeted to the URL-hostname path — the bd-5g7kx tests have served their purpose and the underlying code is gone. Existing ``test_get_channel_streams_with_int_id_still_works`` integration in ``test_channels.py`` preserved (the REST API method is correct for integer channel ids; only the proxy-session-UUID misuse goes away). 5 retargeted tests in ``frontend/src/components/tabs/StatsTab.test.tsx`` (the new ``provider_name`` display contract) plus 3 new tests for the badge sum invariant under the URL-hostname path. Full backend + frontend suites green. ([bd-gy5nd], v0.17.1-0063)
- **Resolver IP-gate too strict for browser-direct-play attribution (bd-ost8o, v0.17.1-0062).** PO upgraded to v0.17.1-0061 (bd-dok7u NowPlayingQueue fallback + forensic logging) and Jellyfin attribution still showed "User #0". The new forensic logs revealed the actual remaining gap: ``[STATS-ENRICH] channel_trace decision=proceed`` fires with ``client_ips=['172.18.0.1']`` (the Docker bridge gateway — the PO's browser egress), but then ZERO ``[JELLYFIN-RESOLVER] resolver_call`` INFO logs follow because the resolver's IP-gate short-circuits before fetching sessions. The gate assumes the ECM client comes from the source server's IP — TRUE for server-mediated playback (Emby/Jellyfin/Plex transcodes the stream from ECM and re-serves to the user) but FALSE for browser-direct-play (the operator opens Jellyfin Web in their browser, the browser fetches the IPTV stream URL directly from Dispatcharr, ECM sees the browser's egress IP, NOT the Jellyfin server). **`backend/services/jellyfin_resolver.py`** / **`emby_resolver.py`** / **`plex_resolver.py`** now implement two-mode dispatch: **Mode A (server-mediated, existing fast path)** runs when ``ecm_session_ip == source_server_ip`` — behavior unchanged, all tiers active including tier-3 fuzzy. **Mode B (browser-direct fallback, new)** runs when the IPs differ — fetch sessions and run tier-1 (channel-name strict) and tier-2 (channel-number strict) only; tier-3 fuzzy is disabled because in a multi-user setup a sufficiently generic stream name would cross-attribute viewers without the IP gate's implicit tiebreaker. Plex's bd-ma6r3 EPG cross-reference tier remains active in Mode B (it is a Tier-1 alternative relying on strict EPG ``channelCallSign`` equality, not fuzzy matching). When Mode B produces >1 candidate, the resolver returns ``[]`` and emits an ambiguous-skip diagnostic INFO log (``result=ambiguous_skip matched_sessions=N matched_users=[...]``) rather than attributing arbitrarily — the IP gate's "the server vouched for the playback" tiebreaker is no longer available in direct-play, so a coin-flip attribution would risk credit theft. The existing ``resolver_call`` INFO log gains a ``mode=server_ip|direct_fallback`` field so operators can confirm which dispatch branch ran. The no-match WARN is now emitted on Mode B no-match paths too (previously the IP short-circuit silenced it on those calls) — direct-play debugging needs the forensic surface as much as server-mediated debugging does. The Jellyfin bd-dok7u NowPlayingQueue resolution happens at cache-fetch time and is therefore already populated by the time Mode B's tier-1/tier-2 run, so the PO's exact symptom (browser-direct-play of ``'430 | Tennis Channel'``) now resolves to ``MotWakorb`` end-to-end. **bd-dok7u + bd-f68c8 retrospective:** both prior fixes remain correct as defense-in-depth — bd-dok7u (queue-resolved session data) and bd-f68c8 (ECM-side pipe-prefix parsing) operate at orthogonal layers (data shape and tier-comparison respectively). Mode B exposes their value: without them, tier-1/tier-2 in Mode B would have nothing to match against for the PO's data shape. Neither is obsoleted by bd-ost8o; rather, bd-ost8o is the dispatch-layer fix that lets the prior two finally take effect on the operator's direct-play path. **Hard rules respected:** ``_resolve_<source>_server_ip`` helpers retained (Mode A still uses them), Mode A behavior unchanged, ``stats.py`` enrichment untouched (per bd-cat70), bandwidth_tracker / SessionTelemetry writer untouched, Plex EPG path untouched (per bd-ma6r3), no new resolver tiers added, no channel renames. **Tests:** 7 new tests per source-resolver test file in ``backend/tests/services/test_jellyfin_resolver.py::TestModeBDirectFallback`` (+ ``test_emby_resolver.py`` + ``test_plex_resolver.py``) cover Mode B Tier-1 match, Tier-2 match, no-match diagnostic, ambiguous-skip diagnostic, Tier-3 fuzzy explicitly disabled (Mode A control + Mode B control), and the ``mode=direct_fallback`` log marker. Jellyfin gets an additional end-to-end test reproducing the PO's exact Tennis Channel scenario with a queue-resolved session ending in a ``MotWakorb`` attribution. Plex gets a bonus EPG-tier-in-Mode-B test confirming the bd-ma6r3 path works without modification when ECM client IP ≠ Plex server IP. Existing IP-mismatch / hostname-non-match tests were retargeted to reflect Mode B's new "cache IS consulted" contract (the old "assert_not_awaited" semantics intentionally no longer hold — that was the bug). Full backend suite + frontend suite stay green. ([bd-ost8o], v0.17.1-0062)
- **Jellyfin attribution: resolve NowPlayingQueue item IDs when NowPlayingItem is absent + comprehensive enrichment forensic logging (bd-dok7u, v0.17.1-0061).** Third attempt at the PO's "Jellyfin shows User #0" — the prior two (bd-f68c8, bd-r5f0c.11) were resolver-tier fixes that compiled cleanly against the brief but did not reach the production data shape. Live API probe against the PO's container (this PR's investigation step) showed that the PO's Jellyfin Web 10.11.8 sessions are reported with ``IsActive=True``, ``NowPlayingItem`` absent entirely, and ``NowPlayingQueue=[{Id: <channel-id>, PlaylistItemId: "playlistItem0"}]``. No combination of resolver-tier work could fix this; the resolver had no name / number / channel data to match against. The channel data is reachable via a second API call: ``GET /Users/{user_id}/Items/{item_id}`` returns the Live TV channel item including ``Name`` (e.g. ``"| ABC: WBAY Green Bay"``), ``ChannelNumber`` (``"2.1"``), and ``Type=TvChannel``. The user-scoped path is required — the unscoped ``/Items/{id}`` returns 400 for ``Type=TvChannel`` items.

  **`backend/jellyfin_client.py`**: ``JellyfinSession`` gains ``now_playing_queue_item_id: str | None`` (default ``None``). ``_map_session`` extracts ``NowPlayingQueue[0].Id`` when the queue is non-empty. New method ``JellyfinClient.get_user_item(user_id, item_id)`` calls ``GET /Users/{user_id}/Items/{item_id}`` and returns the raw dict on 200 or ``None`` on any failure (empty input, non-200, network error) — best-effort by design so the fallback never raises into the resolver hot path.

  **`backend/services/jellyfin_cache.py`**: after ``get_sessions()`` returns, new helper ``_resolve_queue_items`` walks the list and for any session whose ``now_playing_item_name`` is empty AND ``now_playing_queue_item_id`` is populated, calls ``get_user_item`` and rebuilds the session via ``dataclasses.replace`` with ``Name`` → ``now_playing_item_name``, ``ChannelName`` → ``now_playing_channel_name``, ``ChannelNumber`` (or ``Number``) → ``channel_number``. Sessions with NowPlayingItem already populated skip the lookup (zero extra round-trips for working clients). Lookup failure passes the session through unchanged so the resolver's existing no-match path takes over — the pre-bd-dok7u behavior is the worst-case here, not a regression. One INFO summary line per cache miss reports ``NowPlayingQueue fallback resolved <succeeded>/<attempted> sessions`` for production observability.

  **Choice A vs B vs C** (per brief): implemented **Option A** (alternative endpoint). The ``/Sessions?activeWithinSeconds=N`` and ``/Sessions/Playing`` endpoints were probed live; ``?activeWithinSeconds=`` returned ``[]`` for the PO's setup (PlayState reports the sessions as not actively playing even when ``IsActive=True``), and ``/Sessions/Playing`` returns 405 on Jellyfin 10.11.8. ``NowPlayingQueue[0].Id`` was the only signal that survived. Option B (RemoteEndPoint-based IP attribution) would not have known WHICH channel the user was on — only THAT they were active — and the PO's RemoteEndPoint values (``172.16.0.2``, ``192.168.1.61``) are end-user IPs, not the Jellyfin server IP that ECM clients arrive from. Option C (defense-in-depth combining A + B) was rejected because option A is sufficient and the implementation surface is one extra HTTP call per stale-NowPlayingItem session per cache miss (5-second TTL) — at PO scale that is 0-2 extra calls per 5 s.

  **Forensic logging — `backend/services/jellyfin_resolver.py`** (and parallel additions to ``emby_resolver.py`` + ``plex_resolver.py``): every resolver call on the IP-matched + sessions-available path now emits three INFO-level traces so any future "User #0" can be diagnosed from production logs without DEBUG. (1) ``[<SOURCE>-RESOLVER] resolver_call ip=... ecm_channel=... ecm_channel_number=... ecm_stream=... sessions_count=N sessions_with_now_playing=M`` — entry-point snapshot. (2) ``[<SOURCE>-RESOLVER] session_inspect user=... has_now_playing=<bool> item_name=... channel_name=... channel_number=... last_activity=... [queue_item_id=...]`` — one line per cached session exposing exactly what the matcher sees. (3) ``[<SOURCE>-RESOLVER] tier_match ecm_channel=... match_count=N top_user=... top_user_id=...`` — fires only on successful match (parallel to the existing no-match WARN, so the success path is no longer silent). IP-mismatch short-circuits remain DEBUG-only (hot path called on every non-source IP — INFO there would flood the log). **`backend/routers/stats.py`**: ``_enrich_channels_with_attribution`` adds ``[STATS-ENRICH] channel_trace`` (one line per channel — ``decision=proceed|skip:no_data|skip:no_clients``) + ``[STATS-ENRICH] source_dispatch`` (one line per proceeding channel — ``emby_enabled=<bool> plex_enabled=<bool> jellyfin_enabled=<bool>``). Operators can now distinguish "Jellyfin disabled" from "Jellyfin enabled but no match" from "channel short-circuited before resolver" in one log read.

  **Expected production log sequence after deploy** (PO's exact channel 2.1 scenario):
  ```
  [JELLYFIN] NowPlayingQueue fallback resolved 1/1 sessions (bd-dok7u)
  [STATS-ENRICH] channel_trace ch_id=c0c84148... stream_name='WI | Green Bay | ABC 2 WBAY' channel_name='2.1 | ABC: WBAY Green Bay' channel_number=None client_ips=['<jellyfin-server-ip>'] decision=proceed (bd-dok7u)
  [STATS-ENRICH] source_dispatch ch_id=c0c84148... emby_enabled=True plex_enabled=True jellyfin_enabled=True (bd-dok7u)
  [JELLYFIN-RESOLVER] resolver_call ip=<jellyfin-server-ip> ecm_channel='2.1 | ABC: WBAY Green Bay' ecm_channel_number=None ecm_stream='WI | Green Bay | ABC 2 WBAY' sessions_count=2 sessions_with_now_playing=2 (bd-dok7u)
  [JELLYFIN-RESOLVER] session_inspect user=MotWakorb active_session_id=b2d89d65 has_now_playing=True item_name='| ABC: WBAY Green Bay' channel_name=None channel_number='2.1' last_activity=2026-05-19T02:33:36Z queue_item_id=2b0159285ef4370ec3c8c91534bc076d
  [JELLYFIN-RESOLVER] tier_match ecm_channel='2.1 | ABC: WBAY Green Bay' ecm_stream='WI | Green Bay | ABC 2 WBAY' match_count=1 top_user=MotWakorb top_user_id=72be7fac4ca046cf8198573ad285c963 (bd-dok7u)
  ```

  **Tests:** 8 new tests in ``backend/tests/unit/test_jellyfin_client.py`` cover NowPlayingQueue capture (present / absent / empty list), ``get_user_item`` happy path + non-200 + network error + empty-input short-circuit. 6 new tests in ``backend/tests/services/test_jellyfin_cache.py::TestNowPlayingQueueFallback`` cover the PO's exact production shape (queue-only session resolves to ``"| ABC: WBAY Green Bay"`` + ``channel_number="2.1"``), already-populated sessions skip the lookup (zero extra round-trips), idle sessions with no queue pass through, lookup failure passes through unchanged, multi-session independent resolution (one succeeds, one fails — both surface), INFO summary line emitted. 6 new tests in ``backend/tests/services/test_jellyfin_resolver.py::TestForensicInfoLogging`` cover the three INFO trace shapes — entry-point resolver_call payload, per-session session_inspect payload, tier_match on success, absence on no-match, queue_item_id surfaced in session_inspect, IP-mismatch hot path stays DEBUG-only. 5 new tests in ``backend/tests/routers/test_stats_attribution.py::TestEnrichmentForensicLogging`` cover the two STATS-ENRICH trace shapes — channel_trace decision=proceed, decision=skip:no_data, decision=skip:no_clients, source_dispatch emitted only on proceed. Total: 25 new backend tests. Full suite: 4448 backend tests + 1 skipped + 1492 frontend tests stay green.

  **bd-f68c8 retrospective**: the prior fix (Tier-2 ECM pipe-prefix parse) is correct as additive defense-in-depth and should NOT be reverted — it covers the divergent-name-but-matching-number case for all three sources and the bd-dok7u fix is structurally orthogonal (one operates at the JellyfinSession data layer, the other at the tier-comparison layer). Keeping both means future Jellyfin attribution survives both NowPlayingItem-absent AND name-divergence failure modes. ([bd-dok7u], v0.17.1-0061)
- **Stats: Connected Clients header counts actual upstream viewers, not ECM-side proxy connections (bd-w9c2a, v0.17.1-0060).** PO was watching a channel with 1 Dispatcharr XC client ('Hap') + 1 Emby-proxy connection carrying 2 deduped Emby users ('MotWakorb' + 'jkaisersoze') — 3 humans total — and the Active Channels card's Connected Clients section header read "Connected Clients (2)". Root cause: `frontend/src/components/tabs/StatsTab.tsx` rendered the count as `channel.clients.length`, which is the ECM-side connection count. The transcoding-proxy pattern (Emby / Plex / Jellyfin server fans out one upstream connection back to ECM that carries N viewers) was undercounted because the W9 / bd-r5f0c.9 per-client `emby_viewers[]` / `plex_viewers[]` / `jellyfin_viewers[]` payloads weren't being read at the count site. The per-client renderer already used those payloads correctly (each viewer surfaces with an AttributionBadge), and the channel-header `(N viewers)` rollup at lines 911-929 already aggregated them — only the Connected Clients section header was stale. **Fix:** the count expression now reduces over `channel.clients` and adds `Math.max(emby_viewers?.length ?? 0, plex_viewers?.length ?? 0, jellyfin_viewers?.length ?? 0)` per client (or 1 when all three source lists are empty/absent, preserving the pure-Dispatcharr semantic). MAX-across-sources rather than SUM-across-sources because a single ECM client carrying viewers from two source servers concurrently would imply the same humans being visible to two resolvers at once (rare; would double-count). Matches the per-client renderer's source-precedence semantic (emby > plex > jellyfin in lookup order). **Out of scope (verified):** the top-level "Connected Clients" summary stat at `StatsTab.tsx:751` reads `totalClients = sum(channel.client_count)` — a different aggregate (cross-channel sum of backend-supplied connection counts, not per-channel `clients.length`). That metric was NOT changed in this bead; if the PO wants the top-level summary to also reflect upstream viewers, file a follow-up because the fix would also need to touch the backend `client_count` provenance. **Tests:** 4 new tests in `frontend/src/components/tabs/StatsTab.test.tsx::StatsTab — Connected Clients header viewer count (bd-w9c2a)` lock the contract — PO's exact scenario (1 dispatcharr + 1 emby-proxy with 2 viewers → header reads "(3)"), boundary (1 client with 5 emby_viewers → "(5)"), pure-Dispatcharr regression (2 dispatcharr-only clients → "(2)"), and the mixed-source MAX precedence (1 client with 2 emby_viewers + 1 plex_viewers → "(2)" via MAX, not "(3)" via SUM). Per-client rendering (lines 1182-1310), channel-header rollup (lines 902-929), backend enrichment, and bandwidth_tracker are UNCHANGED. ([bd-w9c2a], v0.17.1-0060)
- **Jellyfin / Emby / Plex Live TV attribution: parse ECM channel_name pipe-prefix as Tier-2 channel-number candidate (bd-f68c8, v0.17.1-0059).** PO was watching ATSC sub-channel ``2.1 | ABC: WBAY Green Bay`` via Jellyfin and ECM Stats showed "User #0" instead of "MotWakorb via Jellyfin". Investigation surfaced a parallel-pattern gap in all three media-source resolvers: Tier-2 (channel-number exact match) was gated on the caller supplying a non-None ``ecm_channel_number``, but Dispatcharr does not always push a separate ``channel_number`` field for ATSC sub-channels — the number is carried in the channel_name itself as the ``"<number> | <rest>"`` pipe-prefix. With ``ecm_channel_number=None``, Tier-2 short-circuited; with Jellyfin's ``NowPlayingItem.Name="| ABC: WBAY Green Bay"`` (Jellyfin strips the channel-number prefix from the name for live-TV) the resolver still had the session's ``channel_number="2.1"`` available — but no way to reach it. **`backend/services/jellyfin_resolver.py`** / **`emby_resolver.py`** / **`plex_resolver.py`**: each resolver gains a ``_parse_numeric_pipe_prefix(ecm_channel_name)`` helper that extracts the left-hand side of ``"<number> | <rest>"`` when (and only when) it parses as a non-negative number (integer or ATSC ``major.sub`` form). Word-prefixed channel names like ``"News | CNN"`` are explicitly rejected so they cannot collide with arbitrary session ``channel_number`` strings. A second helper ``_coerce_tier2_number(ecm_channel_number, ecm_channel_name)`` centralizes precedence: caller-supplied ``ecm_channel_number`` always wins; otherwise the helper falls back to the parsed prefix. Tier-2 now compares ``_coerce_tier2_number(...)`` against ``session.channel_number`` (Emby/Jellyfin — dedicated field) or ``_parse_pipe_prefix(item_name)`` (Plex — session number lives in the item-name pipe-prefix). The bd-r5f0c.11 Tier-1 ECM-side pipe-suffix tolerance still handles the case where session-side and ECM-side names share a suffix; the bd-f68c8 Tier-2 path adds redundancy for cases where the names diverge but the numbers line up. **Diagnostic note**: with the PO's documented data shape (``ecm_channel_name='2.1 | ABC: WBAY Green Bay'`` + session ``item_name='| ABC: WBAY Green Bay'`` + session ``channel_number='2.1'``), the existing bd-r5f0c.11 Tier-1 logic already produces a match in isolation, so the bd-f68c8 Tier-2 path is additive defense-in-depth rather than the strict root-cause fix the brief described. The Tier-2 prefix path matters whenever name surfaces diverge entirely (Plex Live TV programs vs channel names, etc.) but channel numbers line up. **Tests:** 7 new tests in ``backend/tests/services/test_jellyfin_resolver.py`` (new ``TestEcmPipePrefixAsChannelNumber`` class) cover the PO's exact data shape, a Tier-2-only scenario where Tier-1 cannot help, the ATSC ``"2.1"`` sub-channel format, non-numeric-prefix rejection, mismatched-number rejection, no-pipe-prefix skip, and the explicit-number-wins precedence. 5 parallel tests in ``test_emby_resolver.py`` (same class name) lock the Emby-resolver path. 4 parallel tests in ``test_plex_resolver.py`` lock the Plex item-name-pipe-prefix path. Full suite: 4424 backend tests + 1 skipped pass (38 → 54 net new tests since v0.17.1-0058's count; bd-f68c8 adds 16). ([bd-f68c8], v0.17.1-0059)
- **Stats attribution: per-IP scoping for per-client emby/plex/jellyfin enrichment + channel-header dedup (bd-cat70, v0.17.1-0057).** Two connected post-v0.17.1-0056 stats-tab attribution bugs. **Bug #3 (correctness — read-side broadcast):** `_enrich_one_source` in `backend/routers/stats.py` broadcast a single matched-IP resolver hit onto ALL clients in `ch["clients"]` and then `return`ed on the first match. The bd-5kbyf assumption "every client of a source-mediated stream comes from the source server's IP" only holds when the channel is purely source-mediated; for the PO's actual mixed scenario (one Emby-mediated viewer + one direct Dispatcharr XC viewer on the same channel) the second client was mis-attributed to the first client's resolved Emby user. DB-layer attribution (`SessionTelemetry`) was already per-client-correct via `BandwidthTracker`; this was strictly a read-side enrichment bug. **Fix:** the loop now (a) iterates every IP rather than returning on first match, (b) stamps `client[user_name_key]` / `client[viewers_key]` only on clients whose `ip_address` matches the iteration's IP, and (c) sets `ch[user_name_key]` / `ch[viewers_key]` only from the first match (channel-level fields keep the "first match wins" back-compat contract while per-client fields are correctly per-IP). **Bug #1 (visual dedup — frontend):** the Active Channels channel header rendered the single-viewer name and the legacy `emby_user_name` fallback as `(watching: <name>)` next to the stream badge, duplicating the same name already shown in the Connected Clients section with the AttributionBadge. **Fix:** `frontend/src/components/tabs/StatsTab.tsx` channel-header viewer block now only renders the multi-viewer `(N viewers)` rollup (when `totalViewers > 1`); the single-viewer name and the legacy `emby_user_name` fallback branches are removed. The per-client display in Connected Clients is unchanged. **Tests:** 2 new backend tests in `backend/tests/routers/test_stats_emby.py` lock the per-IP contract — mixed-IP channel where only the Emby-server-IP client resolves: that client carries `emby_user_name="MotWakorb"` while the other client keeps `emby_user_name=None`; same scoping applied to the W5 `emby_viewers` list (matched client gets the payload, non-matched client keeps `[]`). 2 frontend tests in `StatsTab.test.tsx` updated to assert the channel-header viewer suffix is ABSENT for the single-viewer case (replacing the legacy "(watching: alice)" / "(watching: OnlyUser)" assertions that locked the now-removed markup). All 67 stats router tests + 30 StatsTab tests stay green. **Bug #2 deferred** — the bd-2zcvf assumption that `@grandparentTitle` carries the Plex channel name is wrong (it's the PROGRAM name); resolving Plex Live TV → channel requires a new EPG cross-reference tier against `/tv.plex.providers.epg.xmltv:<dvr>/sections/<id>/all?type=4` where `<Media>` elements carry `channelCallSign` / `channelIdentifier`. Filed as bd-ma6r3 (dependent on bd-cat70) with the full API exploration and proposed design captured in that bead. ([bd-cat70], v0.17.1-0057)
- **Plex Live TV attribution: extract channel name from `@grandparentTitle` + `@parentTitle` so the Plex resolver can match (bd-2zcvf, v0.17.1-0056).** PO was watching Plex channel 2.1 via their Plex DVR and ECM Stats showed "User #0" for the client row instead of the Plex username. The strong starting hypothesis (W11 ECM-side pipe-prefix tolerance missing from `plex_resolver.py`) was wrong on inspection — that fix was already in place. The real bug was one layer above the resolver: `plex_client._map_item` only extracted `<Video>/@title`, which for Plex Live TV from a DVR/Tuner is the PROGRAM currently airing (e.g., `"Saturday Night Live"`) — NOT the channel. Plex puts the operator-visible channel name on `<Video>/@grandparentTitle` (and sometimes `@parentTitle` for alternate DVR shapes that synthesize a per-channel "show"). With only `@title` available, no resolver tier could match an operator's ECM `channel_name` against the program title; the resolver fell through to empty; `plex_user_name` stayed NULL; the frontend display chain fell all the way to `\`User #${client.user_id}\`` with `user_id=0`. Emby + Jellyfin were already structurally immune because their session dataclasses extract a dedicated `now_playing_channel_name` from `NowPlayingItem.ChannelName` — Plex was the missing parallel structure. **`backend/plex_client.py`**: `PlexSession` dataclass gains `now_playing_channel_name` (from `@grandparentTitle`) and `now_playing_parent_title` (from `@parentTitle`). `_map_item` extracts both — no schema cost, both default to `None` for VOD / movies and Plex versions that don't set them. **`backend/services/plex_resolver.py`**: tier-1 now compares `ecm_channel_name` against all three candidate session fields (item, channel, parent), each considered both whole and as the right-hand part of a `"<number> | <name>"` pipe-prefix. bd-r5f0c.11's ECM-side pipe-prefix tolerance applies symmetrically to the new candidates (the `_match_against_candidates` helper centralizes the comparison matrix so the three fields don't triplicate the whole/suffix/ECM-side-suffix logic). Tier-3 fuzzy fallback also now compares `ecm_stream_name` against all three candidates — parallels Emby's tier-3 which already compares against both `item_name` and `channel_name`. Tier-2 is unchanged (the pipe-prefix on Plex's `@grandparentTitle` / `@parentTitle` carries no numeric prefix in practice; only `item_name`'s pipe-prefix is examined). The `_log_plex_resolver_no_match` payload is extended to surface `channel_name` and `parent_title` (raw + normalized + pipe-suffix forms) so future no-match forensics expose the new fields without a follow-up instrumentation pass. **Tests:** new `TestTier1LiveTvChannelMatch` class in `backend/tests/services/test_plex_resolver.py` covers 6 cases — grandparent-title whole match, grandparent + ECM-side pipe-prefix, parent_title fallback, grandparent pipe-suffix on Plex side, regression case where new fields are `None` (pre-bd-2zcvf shape stays green), defensive no-match-on-any-field. New `TestTier3FuzzyFallbackLiveTv` class (2 cases — grandparent fuzzy, parent fuzzy). New diagnostic-payload assertion that asserts the new fields appear in the WARN payload. `backend/tests/unit/test_plex_client.py` gains an assertion on the existing pipe-prefix fixture that `now_playing_channel_name="ESPN"` (from `grandparentTitle="ESPN"` which was already in the fixture) lands on the dataclass, plus a new `test_get_sessions_live_tv_program_in_title_with_channel_in_grandparent` regression target that locks the program-in-title shape with channel-on-grandparent. New fixture `sessions_live_tv_program_in_title.xml`. All 4368 backend tests + 1 skipped + 1488 frontend tests stay green. Emby + Jellyfin resolvers + clients UNTOUCHED — they already had the parallel structure via the dedicated `ChannelName` extraction. The frontend display chain was correct already; no frontend changes ship in this bead. ([bd-2zcvf], v0.17.1-0056)
- **Per-client Emby attribution: surface emby_user_name in Connected Clients list (bd-5kbyf, fix-forward for v0.17.1-0035).** Active Channels header showed `(watching: MotWakorb)` correctly (channel-level `emby_user_name` from bd-fm23o was working), but the per-client list below still rendered "User #0" for Emby-mediated streams. Root cause: `_enrich_channels_with_emby` in `backend/routers/stats.py` only set `emby_user_name` on the channel dict, never propagating it to the entries in `ch["clients"]`. The frontend's `client-user` render path read `client.username` (NULL for Emby-mediated streams) and fell back to `User #${client.user_id}`. **`backend/routers/stats.py`**: `_enrich_channels_with_emby` now propagates the resolved `attribution.user_name` to every client dict in `ch["clients"]` immediately after setting `ch["emby_user_name"]`. All short-circuit paths (Emby disabled, settings-access error, no-tier-args, no-client-IPs, resolver-returns-None) also seed `client["emby_user_name"] = None` on each client so the field is consistently present on the wire regardless of resolution outcome — no undefined keys for TypeScript to stumble on. **`frontend/src/types/index.ts`**: `StreamClient` interface gains `emby_user_name?: string | null`. **`frontend/src/components/tabs/StatsTab.tsx`**: the per-client `client-user` render block now prefers `client.emby_user_name` over `client.username`, with a `badge attribution-source-badge` "via Emby" badge (same class as the UserStatsPanel badge from bd-fm23o) when the Emby name is present, falling back to `username` or `User #${user_id}` otherwise. **Tests:** 3 new backend integration tests in `backend/tests/routers/test_stats_emby.py` lock the per-client propagation contract (emby_user_name on every client when resolver matches; emby_user_name=null on every client when resolver returns None; empty clients[] handles cleanly without crash); 3 new frontend tests in `frontend/src/components/tabs/StatsTab.test.tsx` lock the render logic (emby_user_name rendered with "via Emby" badge; fallback to username without badge; fallback to User #id when both names null). ([bd-5kbyf], fix-forward for v0.17.1-0035)
- **Emby resolver: channel-name primary match (bd-zldrq, fix-forward for v0.17.1-0033 live test).** Live operator test on v0.17.1-0033 surfaced the Emby attribution chain returning "User #0" instead of "MotWakorb (via Emby)" for live-TV sessions on channel 408. Root cause: Dispatcharr stream names like `"US: ESPN FHD"` do not RapidFuzz token_set_ratio above the 0.85 floor against Emby's live-TV `NowPlayingItem.Name` format `"408 | ESPN"` — the only overlapping token is "ESPN". Confirmed via live `/Sessions` capture from the operator's container: `NowPlayingItem.ChannelNumber='408'`, `Name='408 | ESPN'`, `Type='TvChannel'`, `MediaType='Video'`, `ChannelName=None` (Emby uses `Name` for the live-TV display string and leaves `ChannelName` empty even on TV channels — so the prior fallback to `now_playing_channel_name` produced nothing). **`backend/emby_client.py`**: `EmbySession` dataclass gains `channel_number: str | None = None`, extracted from `NowPlayingItem.ChannelNumber` in `_map_session`. The field preserves Emby's raw string type (no int-cast) so sub-channel numbers like `"408.1"` survive intact. Defaults to `None` for backwards compatibility with any pre-existing dataclass construction in tests. **`backend/services/emby_resolver.py`**: `resolve_emby_user` signature extends to `(ecm_session_ip, ecm_stream_name, ecm_channel_name=None, ecm_channel_number=None)`; the two new args are keyword-only-by-convention but accepted positionally for back-compat. New tiered matching algorithm replaces the single fuzzy pass — **Tier 1 (channel_name primary)**: parse each Emby session's `now_playing_item_name` as `"<number> | <name>"` (split on `|`, strip whitespace) and case-insensitively compare the right-hand part to `ecm_channel_name`; whole-string equality also counts (some Emby installs surface the channel name without the `"<number> | "` prefix). **Tier 2 (channel_number exact)**: when both `ecm_channel_number` and the Emby session's `channel_number` are populated, string-compare (cast ECM input to `str`); defensive against name divergence between ECM and Emby. **Tier 3 (fuzzy stream_name fallback)**: the legacy RapidFuzz `token_set_ratio` path against `now_playing_item_name` OR `now_playing_channel_name` with the 0.85 threshold — still correct for non-live-TV content (movies, episodes) where channel args are absent. Matches across tiers pool into one disambiguation set (de-duped by session identity); the most-recent `last_activity_date` wins as before. When N > 1 candidates resolve, a `[EMBY] resolver: %d candidates for ip=%s name=%s, picked %s by recency` DEBUG line surfaces the tie-break so operators see disambiguation in trace. **Hot-path discipline** — Emby strings are normalized (NFC + lowercase + strip) ONCE per `_find_matching_sessions` call into a prepared list, NOT re-normalized inside each per-tier loop; a busy poll with N channels × M sessions × 3 tiers would otherwise re-normalize the same Emby strings 3×N×M times. The IP-mismatch short-circuit (load-bearing optimization for non-Emby-mediated sessions) is unchanged, and the resolver still never raises. **`backend/bandwidth_tracker.py`**: `_collect_stats` adds `channel_name` to the `telemetry_channel_snapshot` entries (the value was already computed locally for `channel_bandwidth_updates` — this is a one-key add, not a new lookup). `_resolve_emby_attributions` extracts `channel_name` and `channel_number` from each snapshot entry and forwards them to `resolve_emby_user` as kwargs. The skip-when-empty guard widens from "stream_name empty" to "every tier args empty" so the live-TV path doesn't drop when `provider_resolution.stream_name` is None but the channel args are populated. **`backend/routers/stats.py`** (operator-visible surface — the "User #0" badge fix): `_enrich_channels_with_emby` pulls `channel_name` and `channel_number` off each Active Channels entry and passes them as the new resolver kwargs. Without this the Active Channels live view's "(watching: ...)" badge would continue to fall through to fuzzy tier-3 and fail for live TV, even with the BandwidthTracker side fixed. **Tests:** `backend/tests/services/test_emby_resolver.py` gains 8 new tests (tier-1 hits on `"408 | ESPN"`, tier-1 hits without the pipe prefix, tier-1 case-insensitive, tier-2 string match, tier-2 skip when session has no number, tier-3 still works for movies, tier-3 still works when channel args present but no match, multi-match tiebreak across new tiers, DEBUG log fires when N > 1 candidates); `backend/tests/unit/test_emby_client.py` gains 1 new test (VOD playback with no ChannelNumber → `channel_number is None`) plus updated assertions on the canned-payload mapping so the live-TV `"408 | ESPN"` + `ChannelNumber: "408"` shape is locked; `backend/tests/unit/test_bandwidth_tracker_emby.py` gains 1 new test pinning the wiring contract (resolver receives `ecm_channel_name="ESPN"` + `ecm_channel_number=408` kwargs); `backend/tests/routers/test_stats_emby.py` gains 1 new test pinning the same contract on the Active Channels endpoint surface. Existing mocks were updated to accept `**kwargs` so back-compat call shapes still work. All 4079 backend tests + 1 skipped (the volume test) pass. Disabled-Emby short-circuit and resolver-never-raises contracts are preserved. ([bd-zldrq], fix-forward for v0.17.1-0033 live test bug)

### Added
- **Emby usernames in Stats endpoints + UI — FINAL bead of EPIC bd-2cenq (bd-fm23o).** Closes the Emby user-attribution epic shipped over bd-6c0g6 → bd-8wc6q → bd-gpeot → bd-k026g → bd-6802c → bd-gih6d by surfacing the captured `session_telemetry.emby_user_name` on the operator-visible Stats read APIs and in the Stats tab. Once this lands, v0.17.1 can cut. **Backend `routers/stats.py`:** `/api/stats/watch-time` (`group_by=total` and `group_by=day`) now denormalizes `emby_user_name` alongside `dispatcharr_username` via two new helpers (`_pick_display_name_and_source`, `_build_watch_time_row_total`, `_build_watch_time_row_day`) — Emby wins over Dispatcharr at the per-user-aggregation level (defensive `MAX(emby_user_name)` on the existing `_distinct_poll_subquery` so multi-client polls collapse correctly) and the new `attribution_source: "emby" | "dispatcharr"` discriminator surfaces the chain so the frontend can render a "via Emby" badge. `/api/stats/channels` (Active Channels live view) calls the new `_enrich_channels_with_emby(channels)` async helper after the bd-ox5q8 stream-identity enrichment — gated on `settings.emby_enabled` (short-circuits with `None` per channel when disabled), iterates each channel's clients, and calls `services.emby_resolver.resolve_emby_user(client_ip, stream_name)` per client; the first non-`None` attribution wins and populates `channel.emby_user_name`. Three new routes implement the PO-decided URL-split for per-user breakdowns: `/api/stats/users/dispatcharr/{user_id}` (source-prefixed alias for the legacy `/watch-time/{user_id}` behavior), `/api/stats/users/emby/{emby_user_id}` (per-channel breakdown filtered by `session_telemetry.emby_user_id` — the new identity axis the epic was built to expose), and `/api/stats/users/{user_id}` as a back-compat deprecated alias that routes to the dispatcharr behavior with a per-call WARN log so operators / log analysis can see if any consumer is still using it. The shared aggregation logic lives in `_per_user_breakdown_by_channel(db, *, source, identifier, from_, to)` with a parallel `_load_latest_stream_identity_per_emby_user` side-loader so the Emby-source endpoint renders the same `latest_stream_id`/`latest_stream_name` cells (MAX-observed-at-per-channel) as the Dispatcharr-source endpoint. The deprecation marker is comment-only — removal is not yet scheduled. **Frontend `types/index.ts`:** new `AttributionSource = "emby" | "dispatcharr"` type; `WatchTimeUserTotalRow` and `WatchTimeUserDayRow` gain a required `attribution_source` field; `ChannelStats` gains an optional `emby_user_name`. **`UserStatsPanel.tsx`:** the user totals table renders a `(badge attribution-source-badge)` "via Emby" badge alongside the username when `row.attribution_source === 'emby'` (shared `.badge` class from `shared/common.css`; `UserStatsPanel.css` adds only an 8px left-margin scoped to the new class so the visual style stays consistent with every other badge in the app). **`StatsTab.tsx`:** the Active Channels card renders a `(watching: <emby_user>)` badge next to the existing `[Provider] - Stream` stream-name badge whenever `channel.emby_user_name` is non-null — purely additive to the bd-ox5q8 row layout. **Tests:** 12 new backend integration tests in `backend/tests/routers/test_stats_emby.py` lock the full read-API contract (watch-time Emby precedence + attribution_source on both group_by modes; Active Channels emby_user_name on resolver match / on resolver miss / on disabled-Emby short-circuit; dispatcharr/{id} renamed route; emby/{id} filtering correctness + empty-identifier returns empty envelope; deprecated alias parity with dispatcharr/{id} + WARN-log emission); 2 new frontend tests in `UserStatsPanel.test.tsx` (badge renders when attribution_source=emby; badge absent when dispatcharr); 2 new frontend tests in `StatsTab.test.tsx` (Active Channels emby-viewer badge renders when populated; absent when null). All 4066 backend tests + 1427 frontend tests stay green. **EPIC bd-2cenq closed** with this bead — operators with Emby-mediated streams now see real Emby usernames in Stats > User Watch Time instead of the proxy/API account, and the per-channel "(watching: ...)" badge in Active Channels completes the live attribution chain. ([bd-fm23o], FINAL bead of EPIC [bd-2cenq])
- **BandwidthTracker Emby attribution wiring (bd-gih6d, epic bd-2cenq).** Wires the bd-6802c cross-ref resolver into `backend/bandwidth_tracker.py` so `session_telemetry` rows carry the resolved Emby viewer's identity. **New method `BandwidthTracker._resolve_emby_attributions(telemetry_channel_snapshot, provider_by_channel)`** is called from `_collect_stats` after the bd-skqln.14 provider resolver runs and BEFORE `_write_session_telemetry`; it builds a sparse `{(channel_uuid, client_ip): EmbyAttribution}` map by iterating the per-channel snapshot's `(channel, ip)` pairs and awaiting `services.emby_resolver.resolve_emby_user(client_ip, provider_resolution.stream_name)` for each. **Settings gate optimization** reads `settings.emby_enabled` at the top via a local `from config import get_settings` and short-circuits with an empty map when False — the cache and resolver already short-circuit on disabled, but skipping the per-(channel, ip) loop, the per-call settings access, and the resolver function-call overhead on the common disabled-Emby install is the cheap optimization the bead spec explicitly calls out. **Stream-name skip** also avoids the resolver call when `provider_resolution.stream_name` is None (the cache match is name-based so the resolver would short-circuit inside anyway — skipping at the call site saves the function-call overhead). **Failure handling** wraps each resolver call AND the settings read in try/except: any exception (resolver is documented to never raise, but a future refactor or unforeseen runtime fault like cache failure or settings access raise must not block telemetry writes) collapses to a single rate-limited `[BANDWIDTH] [EMBY]` WARN via the new module-level `_log_emby_resolver_failure(exc)` helper. The rate-limit window is **60 seconds** anchored on a module-level `time.monotonic()` timestamp (`_emby_resolver_last_warn_at`) so multiple resolver failures within one busy poll cycle — and across poll cycles inside the window — collapse to ONE WARN line, preserving the operational signal-to-noise ratio. The module-level (not instance-level) timestamp means multi-tracker test seams share the same clock and the failed-resolver row still writes with NULL Emby columns. **`_write_session_telemetry` signature extended** with a keyword-only `emby_attributions: Optional[dict[tuple[str, str], EmbyAttribution]] = None` parameter (default empty for back-compat with older test seams); the per-row construction looks up `emby_attributions.get((channel_uuid, ip))` and passes `attribution.user_id` / `attribution.user_name` (or `None` when missing) to the `SessionTelemetry(...)` constructor's new `emby_user_id` / `emby_user_name` kwargs from migration 0016. Both columns move together — the dataclass returns them atomically, and a future split into a separate `emby_users` table can migrate this writer in one place. **The hot-path discipline mandate** "Telemetry write must NEVER block on Emby failure" is enforced by the try/except wrapper AND by keeping the resolver call entirely OUTSIDE the SQLAlchemy session that owns the write — a resolver raise cannot poison the session commit, and the row still lands with NULL Emby columns regardless of which IP / channel pair triggered the failure. **Tests:** 7 new tests in `backend/tests/unit/test_bandwidth_tracker_emby.py` lock the full behavioral matrix — (1) resolver returns `EmbyAttribution` → row has both Emby columns populated; (2) resolver returns `None` → row has both Emby columns NULL; (3) `settings.emby_enabled = False` → resolver mock asserts NOT awaited and rows have both Emby columns NULL; (4) resolver raises `EmbyClientError` → row STILL writes with NULL Emby columns AND exactly ONE `[BANDWIDTH] [EMBY]` WARN is emitted; (5) rate-limit guard: 6 consecutive failures across one busy poll cycle collapse to exactly ONE WARN; (6) end-to-end realistic scenario: Dispatcharr poll with one client whose IP is the Emby server's IP + matching cached Emby session → telemetry row carries the resolved Emby attribution alongside the bd-skqln/bd-kh23e Stats v2 fields (`channel_id`, `stream_id`, `stream_name`); (7) sparse-map per-IP attribution: two clients on one channel where only one resolves → matching client's row gets the attribution, non-matching client's row keeps NULL Emby columns without spilling another row's attribution. The Stats endpoints (bd-fm23o) are intentionally NOT touched in this bead — surfacing Emby usernames in `Stats > User Watch Time` ships as the next wave. ([bd-gih6d])
- **Emby cross-ref resolver: ECM stream → Emby user (bd-6802c, epic bd-2cenq).** New `backend/services/emby_resolver.py` answers the load-bearing user-attribution question for the v0.17.x Emby epic: given an ECM stream session's client IP and stream name, which Emby user — if any — is the actual viewer? Without this cross-reference Stats > User Watch Time collapses every Emby viewer into a single proxy-IP identity. **Module-level `async resolve_emby_user(ecm_session_ip, ecm_stream_name) -> EmbyAttribution | None`** is the only public entry point and consumes the bd-gpeot `get_cached_emby_sessions()` cache (NEVER instantiates `EmbyClient` directly — going around the cache would defeat the thundering-herd guard, stale-fallback, and settings gate). **Algorithm:** (1) extract Emby server IP from `settings.emby_base_url` via `urlparse(...).hostname` — IP literals (`http://192.168.1.10:8096`) are used verbatim, hostnames (`https://emby.local:8920`) are resolved once via `socket.gethostbyname` and cached module-level (a successful resolution is cached as the IP string, a `socket.gaierror` failure is cached as the `_UNRESOLVED` sentinel so the WARN log fires exactly once per process and subsequent polls short-circuit cheaply); IP-literal detection uses `socket.inet_pton` for both AF_INET and AF_INET6 rather than naive dot-counting; (2) if the ECM session IP does not equal the Emby server IP, return `None` BEFORE the cache call — every poll cycle hits this branch for every non-Emby-mediated session, so the short-circuit is the load-bearing optimization; (3) fetch cached Emby sessions (the cache transparently handles the disabled-Emby case by returning `[]`, so the resolver does not need to inspect `emby_enabled`); (4) for each session, score `ecm_stream_name` against BOTH `now_playing_item_name` AND `now_playing_channel_name` (live-TV sessions populate channel_name, VOD populates only item_name, idle sessions populate neither) — exact match after NFC + lowercase + strip normalization wins immediately, else RapidFuzz `fuzz.token_set_ratio / 100` against the bead-mandated `FUZZY_MATCH_THRESHOLD = 0.85`; (5) zero matches → `None`, one match → `EmbyAttribution(user_id, user_name)`, multiple matches (rare — same channel on multiple Emby clients) → tie-break on most-recent `last_activity_date` (Emby ISO timestamps sort lexicographically the same as chronologically; `None` timestamps lose to any populated value via the `or ""` fallback in the key function). **`EmbyAttribution` is a frozen dataclass** with `user_id` and `user_name` so callers cannot mutate the attribution between the resolver returning it and the BandwidthTracker writing it to `session_telemetry.emby_user_id` / `emby_user_name`. **Never raises** — the BandwidthTracker poll loop calls this on every active session every ~5 seconds, so defensive failures (empty URL, malformed URL, DNS failure) return `None` after a one-shot WARN rather than propagating up. **Normalization is duplicated from `dedup_matcher._normalize`** rather than imported — the two services answer different questions with different threshold policies, and coupling them via a shared helper would couple policies that may evolve independently; the cost of a one-liner duplication is lower than the cost of forcing a shared module on two unrelated callers. **Logging** uses the `[EMBY]` prefix with lazy `%` formatting per `backend/CLAUDE.md`; resolver decisions log at DEBUG (don't spam INFO on every poll), DNS failure logs at WARN exactly once via the cache sentinel. **Settings-disabled short-circuit** is implicitly handled by the cache layer returning `[]`; no explicit `emby_enabled` check in the resolver. **Tests:** 17 new tests in `backend/tests/services/test_emby_resolver.py` lock the full behavioral matrix — IP mismatch returns `None` without a cache call (the load-bearing optimization), exact match on item_name returns attribution, case-insensitive match ("cnn" matches "CNN"), channel_name match when item_name is None (live-TV path), fuzzy match above 0.85 threshold returns attribution, fuzzy match below 0.85 returns None, empty cache returns None (Emby disabled / idle), all-idle sessions return None, multi-match tiebreak by most-recent `last_activity_date`, populated timestamp beats None timestamp, hostname-based base_url resolves to matching IP and attributes, hostname resolving to non-matching IP short-circuits before cache, DNS failure logs WARN with `[EMBY]` prefix and hostname and returns None gracefully, DNS resolution result is cached (3 successive calls fire `gethostbyname` exactly once), empty base_url returns None defensively, malformed base_url returns None defensively. The resolver is now ready for bd-gih6d's BandwidthTracker enrichment wiring; this bead intentionally does NOT touch BandwidthTracker (bd-gih6d) or the Stats endpoints (bd-fm23o). ([bd-6802c])
- **Emby Settings UI + persistence + test-connection endpoint (bd-8wc6q, epic bd-2cenq — operator-facing surface for the v0.17.x Emby user-attribution feature).** Wires the bd-6c0g6 `EmbyClient` into Settings so operators can configure the Emby integration without editing `settings.json` by hand. **`backend/config.py`** adds three new fields to `DispatcharrSettings`: `emby_enabled: bool = False` (operator must opt in), `emby_base_url: str = ""` (no validation — operator's responsibility), and `emby_api_key: str = ""` (PLAINTEXT at rest, same approach as `dispatcharr_api_key` — no encryption in this release). **`backend/routers/settings.py`** plumbs all three fields through the `SettingsRequest` and `SettingsResponse` Pydantic models, the `GET /api/settings` handler (with `emby_api_key_configured` boolean replacing the raw key — never returned), and the `POST /api/settings` handler. The API key uses the same preserve-on-omit contract as `smtp_password` and `mcp_api_key` (bd-vj8n9) — a partial POST that omits `emby_api_key` keeps the stored value, so operators can save the toggle and base URL without re-entering the secret on every save. **New endpoint `POST /api/settings/emby/test-connection`** (admin-only via `RequireAdminIfEnabled`) takes `{base_url, api_key}` inline in the request body — NOT from saved settings, since the operator may be testing BEFORE saving. Constructs an `EmbyClient` per request, calls `get_sessions()`, releases the underlying httpx pool via `finally: await client.close()`, and returns `{ok: true}` on success or `{ok: false, error: <message>}` on `EmbyClientError`. The endpoint deliberately does NOT raise `HTTPException` on connection failure — the operator wants to SEE the error message inline in the UI, not get a generic 500. An unexpected exception class is rendered as `Unexpected error: <ClassName>` without leaking the exception message (mirrors the CodeQL #1415 / bd-m8i9q sanitization posture for `/api/settings/mcp-status`). **Frontend** adds a new **Integrations** settings page (`SettingsPage` type extended in `frontend/src/hooks/useHashRoute.ts`) with the Emby Integration section as its first occupant — distinct from the General page's Dispatcharr connection (the primary upstream, not a side integration) and from Linked Accounts (ECM-internal M3U linking). The section renders an `enabled` checkbox, a `base_url` text input, an `api_key` password input (`type="password"`, masked, `autoComplete="new-password"`), and a Test Connection button. The button calls the new `api.testEmbyConnection(baseUrl, apiKey)` helper with the form-state values and renders `Connection successful` (green) or the backend's error string (red) inline next to the button. Save uses the existing page-level Save button on the Integrations page; the handler only sends `emby_api_key` when the operator has entered a fresh value (matches the backend's preserve-on-omit contract). The API key field on load is blank — only `emby_api_key_configured` reflects whether a key is stored, with the placeholder text switching between `••••••••` and `Paste your Emby API key`. Reuses existing shared CSS (`.settings-section`, `.settings-section-header`, `.form-group-vertical`, `.checkbox-group`, `.btn-secondary`, `.form-hint`, `.badge`, `.badge-success`, `.material-icons`) — no new CSS files. **Tests:** 6 new backend tests in `backend/tests/routers/test_settings.py::TestEmbySettingsPersistence` (GET returns enabled/base_url, GET masks api_key as `emby_api_key_configured`, GET reports unconfigured when no key, POST persists all three fields, POST defaults when fields absent, partial POST preserves the stored API key); 8 new backend tests in `backend/tests/routers/test_emby_settings.py::TestEmbyTestConnection` (success returns ok=true and closes the client, EmbyClientError surfaces as `{ok: false, error: ...}` with the 401 message, network failure surfaces inline, unexpected exception renders only the class name without leaking the message, the endpoint uses inline credentials not saved settings, missing required fields return 422, the client is closed even on error); 4 new backend tests in `backend/tests/unit/test_config_dedup_settings.py` (Emby field defaults and operator overrides); 9 new frontend tests in `frontend/src/components/settings/EmbyIntegrationSection.test.tsx` (section renders on the Integrations page, fields populate from loaded settings with the API key masked, the input is type=password, Test Connection calls the API with form-state values, success message renders inline, backend error renders inline with the verbatim message, blank base_url is rejected client-side without a network call, save persists all three fields when fresh, save omits `emby_api_key` from the payload when the field is blank to honor the preserve-on-omit contract). The bd-gpeot session cache, bd-6802c resolver, and BandwidthTracker enrichment ship as separate beads in the epic — this bead is the configuration substrate they all depend on. ([bd-8wc6q])
- **Emby session cache with TTL + thundering-herd guard (bd-gpeot, epic bd-2cenq).** New `backend/services/emby_cache.py` wraps `EmbyClient.get_sessions()` so the BandwidthTracker poll loop (~5s cadence, bd-gih6d follow-up) and the resolver (bd-6802c follow-up) can read the live Emby session list without pounding the operator's Emby server on every cycle. **Module-level `async get_cached_emby_sessions() -> list[EmbySession]`** is the only public entry point; cache key is the constant `"emby:sessions"` (single-key — Emby is one configured upstream per ECM install) and TTL is 5 seconds (matched to the Dispatcharr poll cadence so the next BandwidthTracker poll sees a fresh snapshot and intermediate readers hit cache). **Settings gate** short-circuits before any cache or client interaction when Emby is disabled or unconfigured: `getattr(settings, "emby_enabled", False)` is False OR `getattr(settings, "emby_base_url", "")` is empty → return `[]` immediately, no `EmbyClient` constructed. The `getattr` defaults defensively handle the pre-bd-8wc6q state where the `DispatcharrSettings` model has not yet declared the `emby_*` fields; once bd-8wc6q lands, the defaults become inert. **Stale-fallback** is the deliberate departure from the generic `cache.py` helper: when the upstream fetch fails AFTER a value was previously cached, the stale list is returned (with a `[EMBY]` WARN naming the failure and the stale age) rather than empty list — a momentary Emby outage should not erase the "who is watching" picture mid-resolver. Cold-start failure (no cache + fetch fails) still returns `[]` (always a list, never `None` to callers) and logs a `[EMBY]` WARN with `"no prior cache"` phrasing so operators can distinguish the two failure modes in logs. Cold-start failure deliberately does NOT poison the cache with `[]` — the next call retries against Emby rather than serving empty for the TTL window. **Thundering-herd prevention** via a lazy-initialized module-level `asyncio.Lock` (constructed on first call so module import is not bound to a running loop): concurrent callers during a cache-miss queue at the lock; the holder fetches once and the waiters re-check the cache under the lock (double-checked locking — without the re-check, every waiter would still fire its own fetch after acquiring the lock, defeating the guard entirely) and return the freshly-cached value. The `EmbyClient` is constructed and closed per fetch (httpx connection pool released on success AND failure via try/finally so a transport error never leaks sockets). Logging uses the `[EMBY]` prefix with lazy `%` formatting per `backend/CLAUDE.md`. Caching is deliberately the only concern here: resolver / matching logic is bd-6802c's domain (next wave), BandwidthTracker wiring is bd-gih6d's domain (next wave), settings fields are bd-8wc6q's domain (parallel). **Tests:** 11 new tests in `backend/tests/services/test_emby_cache.py` lock the bead's full behavioral matrix — (1) cache hit: first call constructs one `EmbyClient` and fires one `get_sessions`, client closed once; (2) cache hit: second call within TTL returns cached value without constructing a new client; (3) TTL expiry: monkey-patched `time.monotonic` past the TTL boundary triggers a refetch and the new value wins; (4) stale-fallback: prior success + later `EmbyClientError` returns the stale list with a WARN containing `"stale"`; (5) cold-start failure: no prior cache + `EmbyClientError` returns `[]` with a WARN containing `"no prior cache"`; (6) cold-start failure does not poison the cache: subsequent successful fetch fires a new upstream call rather than serving an empty cached value; (7) disabled (`emby_enabled=False`) returns `[]` without constructing a client; (8) empty `emby_base_url` treated as disabled (defensive against the bd-8wc6q-not-yet-shipped state); (9) settings model without any `emby_*` attributes treated as disabled (bare-object stand-in for pre-bd-8wc6q `DispatcharrSettings`); (10) disabled short-circuit even with a pre-populated cache (operator disabling Emby after a cache load must NOT surface stale data); (11) thundering-herd: 10 concurrent `asyncio.gather` callers during cold-start fire exactly ONE `EmbyClient` construction and ONE `get_sessions()` call, all 10 receive the same session list — implemented with a real `CountingClient` counter class (not Mock) for deterministic concurrent counting and an `asyncio.Event` gate to hold the in-flight fetch open until all waiters arrive at the lock. ([bd-gpeot])

- **Emby user attribution columns on `session_telemetry` (bd-k026g, parent epic bd-2cenq).** Schema substrate for the Emby user attribution epic — ECM only sees the Dispatcharr stream session's IP, and when users watch via an Emby server ALL stream pulls share that one IP, so Stats cannot distinguish individual Emby viewers without enrichment. **Alembic migration 0016** adds two NULLABLE TEXT columns to `session_telemetry`: `emby_user_id` (TEXT NULL — Emby user IDs are GUIDs, NOT integers like the Dispatcharr `user_id` column) and `emby_user_name` (TEXT NULL — denormalized at write time from the per-poll Emby `/Sessions` lookup the resolver maintains, mirroring the bd-gsn3r `dispatcharr_username` denormalization). Both columns are NULL for non-Emby rows (most rows on most installs — only sessions whose client IP matches the configured Emby server IP AND has a concurrent matching Emby session are enriched). Plain nullable columns, NOT FKs — Emby's user table is upstream and not an ECM table. **Schema posture is deliberately denormalized** per the bead spec: keeping Stats v2 query patterns flat (per-poll projection alongside `dispatcharr_username`) avoids the per-row JOIN cost a separate `emby_users` table would impose; the resolver lookup that populates these columns already runs once per poll window. **Idempotency (bd-5w6jz):** each ADD COLUMN is independently guarded via `inspect()` so partial drift (one column already present from `create_all()` against the post-0016 ORM, or from a previous interrupted run) is safe to re-encounter; if both columns are already present, the migration returns immediately. No view drop/recreate dance needed — `channel_watch_stats_v` only references `channel_id`, `observed_at`, and `poll_interval_ms`, so adding columns to the underlying table cannot disturb it (same reasoning as migration 0013). Native SQLite `DROP COLUMN` (3.35+) on downgrade — no batch rebuild. **`SessionTelemetry` ORM model** updated to declare both columns. **Tests:** 6 new migration smoke tests in `TestMigration0016` / `TestMigration0016Idempotent` (fresh up with NULLABLE TEXT shape assertion, fresh down preserves `dispatcharr_username`, both-columns-pre-added idempotency, partial drift with `emby_user_id` pre-added, partial drift with `emby_user_name` pre-added, full round-trip up→down→up). Wiring into `BandwidthTracker` and the cross-ref resolver are tracked separately (bd-gih6d and downstream Emby epic children). ([bd-k026g])

- **Emby API client + auth (bd-6c0g6, epic bd-2cenq — foundational module for the v0.17.x Emby user-attribution feature).** New read-only async HTTP client at `backend/emby_client.py` modeled on `dispatcharr_client.py` — single concern: fetch the operator's Emby `/Sessions` feed so downstream code (bd-gpeot TTL cache, bd-6802c resolver, BandwidthTracker enrichment) can cross-reference live Emby viewers against ECM's active streams and attribute the real Emby username instead of collapsing every Emby-mediated pull to the proxy IP. **`EmbyClient(base_url, api_key)`** normalizes the base URL (strips a trailing slash but preserves any reverse-proxy sub-path) and constructs an `httpx.AsyncClient` with the bead-mandated timeout (`httpx.Timeout(connect=5.0, read=10.0)`); the auth scheme is stateless `X-Emby-Token: <api_key>` per request, no token-refresh lifecycle (unlike Dispatcharr's JWT flow). **`async get_sessions() -> list[EmbySession]`** calls `GET <base>/Sessions` and maps the upstream PascalCase payload to the snake_case `EmbySession` frozen dataclass (`session_id` ← `Id`, `user_id` ← `UserId`, `user_name` ← `UserName`, `remote_endpoint` ← `RemoteEndPoint`, `now_playing_item_name` ← `NowPlayingItem.Name`, `now_playing_channel_name` ← `NowPlayingItem.ChannelName`, `last_activity_date` ← `LastActivityDate`); idle sessions (no `NowPlayingItem`) produce `None` for the playing fields rather than raising; empty `/Sessions` responses return `[]` (a normal idle-server state, not an error). **`EmbyClientError`** wraps every failure path — 401 (bad/expired API key) surfaces a distinct message string the bd-8wc6q Settings UI can route on; any other non-2xx response surfaces the status code; transport-level failures (`httpx.ConnectError`, `ReadTimeout`, etc.) wrap the underlying exception in the `__cause__` chain so structured loggers can still capture root cause. **`async test_connection() -> bool`** wraps `get_sessions()` for the bd-8wc6q Settings UI 'Test Connection' button — returns `True` on any 2xx response, swallows `EmbyClientError` to `False` so the UI handler only needs to render a bool. Caching is deliberately not in this layer (bd-gpeot owns the TTL cache); resolver / matching logic is deliberately not in this layer (bd-6802c owns `ECM stream → Emby user`); Settings UI plumbing is deliberately not in this layer (bd-8wc6q wires `test_connection` into Settings). All logging uses the `[EMBY]` prefix with lazy `%` formatting per `backend/CLAUDE.md`. **Tests:** 11 new tests in `backend/tests/unit/test_emby_client.py` cover base-URL trailing-slash normalization (with and without sub-path preservation), success-path PascalCase → snake_case mapping for both playing and idle sessions, the `X-Emby-Token` header + `/Sessions` URL contract, empty-list → `[]`, 401 → `EmbyClientError` with `"401"` in the message, non-2xx (500) → `EmbyClientError` with status in the message, `httpx.ConnectError` → `EmbyClientError` with the underlying exception in `__cause__`, `test_connection` True on success, False on auth failure, and False on network failure. ([bd-6c0g6])
- **`ecm_pending_merges_queue_depth` gauge instrumentation (bd-wvr1d, BD-F follow-up).** Adds a Prometheus `Gauge` that reflects the current number of `pending_merges` rows with `status='pending'`. The gauge is declared in `backend/observability.py` adjacent to the LOCKED CONTRACT counter `ecm_pending_merges_queue_depth_added_total` with an explicit comment that it is NOT a contract SLI — alerts and runbooks must not take a dependency on the gauge without cross-team review. A shared helper `set_pending_merges_queue_depth_gauge(db)` in `observability.py` issues `SELECT COUNT(*) FROM pending_merges WHERE status='pending'` and calls `gauge.set(count)`; all four call sites share this helper to avoid SQL/try-except copy-paste. **Wiring:** BD-F insert (`backend/services/m3u_dedup_hook.py`) sets the gauge after the fresh-insert commit + counter increment; BD-E accept (`backend/routers/channel_merges.py`) sets the gauge after the `merged` commit; BD-E dismiss sets the gauge after the `dismissed` commit; app startup (`backend/main.py`) seeds the gauge immediately after `init_db()` so the metric is non-zero from the first scrape. All four sites wrap the helper in an outer try/except that logs at WARN with the `[DEDUP]` prefix on failure — a gauge-set error never blocks a queue-state transition. `docs/sre/slos.md` updated under SLO-10's Cardinality discipline block with a "Companion gauge (bd-wvr1d) — NOT a contract SLI" paragraph that explains the accuracy trade-off and directs operators to use the counter for SLI math and the gauge for dashboards. **Tests:** 4 new pytest cases in `backend/tests/unit/test_pending_merges_gauge.py` cover (a) BD-F insert sets gauge to the post-insert count, (b) BD-E accept decrements the gauge, (c) BD-E dismiss decrements the gauge, and (d) the startup seed sets the gauge to a pre-populated count. ([bd-wvr1d])
- **End-to-end test coverage for the v0.17.1 stream-dedup epic (bd-etdeb, BLOCKING gate for v0.17.1 release).** Two complementary test additions lock the dedup feature's operator-visible and audit-substrate contracts as the v0.17.1 release-cut quality gate. **(1) Playwright spec `e2e/dedup-flow.spec.ts`** drives all three operator-visible trigger surfaces (drag_drop, add_stream, m3u_refresh) through the actual UI: the Channel Manager subnav badge appears with the queued count, the operator navigates into PendingMergesPage, clicks Merge on rows tagged with each `trigger_context`, the row disappears from the optimistic-remove client list, and the next subnav poll reflects the drained queue. Hermetic by design — every backend route the dedup feature touches (`GET /api/channel-merges?status=...`, `POST /api/channel-merges/{id}/accept`, `POST /api/channel-merges/{id}/dismiss`, `GET /api/notifications`) is intercepted with `page.route()` so the spec never round-trips to Dispatcharr and never writes a real `pending_merges` row, matching the `e2e/re-normalize.spec.ts` (bd-eio04.12) precedent. A captured accept-log proves which row id was resolved per scenario so each `trigger_context` surface's Merge button click is verified to call the correct endpoint. **(2) Backend integration test `backend/tests/integration/test_dedup_audit_trail.py`** is the cross-trigger §D6 audit-field proof — parametrised across all three v0.17.1 trigger_contexts (`drag_drop` / `add_stream` / `m3u_refresh`), each parametrize case asserts that accepting a pending row writes a `pending_merge_journal` entry with all seven §D6 fields populated (`actor_token_id`, `action_type='merge_confirmed'`, `source_channel_id` resolved to the Dispatcharr stream id, `target_channel_id`, `confidence_score` captured at action time, `timestamp_utc > 0`, and `trigger_context` mirrored verbatim from the pending row). The cross-surface invariant — every operator-visible surface writes the SAME audit shape, with the surface tag preserved — is what makes the §D6 "are MCP-agent merges accepted at a higher rate than operator-driven ones?" analytical question answerable. **Deviation from bead spec (documented in the Playwright spec docstring):** the bead asks for Scenarios A (drag_drop) and B (add_stream) to be driven through the source UI affordances (HTML5 drag-and-drop onto a channel, "Create channel(s) in group" context menu); driving those surfaces in a hermetic E2E would require mocking the full channels + streams + groups + providers API surface and reliably emulating HTML5 drag-and-drop on a virtualised list, neither of which is achievable in the v0.17.1-cut time budget without producing a brittle, flake-prone spec. The hook-level integration is already covered by existing vitest specs (`useDedupOnDrop.test.ts`, `useAddStreamDedup.test.ts`, `StreamDedupModal.test.tsx`); the Playwright spec covers the cross-surface operator UI flow (page navigation, badge visibility, Merge-click resolution) and the backend test covers the per-trigger journal-audit field set. Total new test surface: 4 Playwright scenarios + 3 parametrised backend integration cases. ([bd-etdeb])
- **BD-I "Add Stream" dedup integration on the Streams pane (bd-1lznl, ADR-008 §D1 `trigger_context='add_stream'`).** Wires `StreamDedupModal` (BD-G) into the single-stream "Create channel(s) in group" context-menu flow on `StreamsPane` so an operator promoting a channel-less stream into a channel is prompted before silently creating a duplicate. On a single-stream selection the new `useAddStreamDedup` hook (`frontend/src/hooks/useAddStreamDedup.ts`) calls `GET /api/channel-merges/candidates?stream_name=…&group_id=…` (BD-D's synchronous lookup); if a candidate clears the ADR-008 §D2 60% floor the modal opens with the stream name + candidate + confidence; if not, the original `openBulkCreateModalForStreamIds` path runs unchanged (auto-creation rules consulted as usual). **onMerge** parses the ADR-008 §D8 string `channel_id` to the numeric Dispatcharr id and calls `POST /api/channels/{id}/add-stream` (the existing `addStreamToChannel` helper) to append the source stream to the candidate channel, then fires the new `onChannelsChanged` callback so the parent re-fetches the channels list and the mapped-streams set reflects the new binding. **onCreateNew** runs the original create-channel callback (the bulk-create modal). **onCancel** just closes the modal — per the bd-1lznl spec there is no card to bounce back, so the BD-H cancel-animation does not apply here. **Lookup failure intentionally falls through** to the create-channel path so a network blip never silently swallows the operator's click. Multi-stream selections proceed unchanged — bulk dedup is BD-J's surface. New API helper `getChannelMergeCandidates(streamName, groupId?)` plus typed `ChannelMergeCandidate` / `ChannelMergeCandidatesResponse` interfaces in `frontend/src/services/api.ts`. Plumbed through `App.tsx` → `ChannelManagerTab` → `StreamsPane` via the new `onChannelsChanged` prop. **Tests:** 7 Vitest tests in `useAddStreamDedup.test.ts` cover no-candidate falls through to `onProceed`, candidate-found opens the modal (and does NOT call `onProceed`), `onMerge` calls `addStreamToChannel(parsedId, streamId)` and closes, `onCreateNew` runs `onProceed` and closes, `onCancel` closes without downstream effects, candidate-lookup failure falls through, and `group_id=null` (search-all-groups) is forwarded verbatim. 2 new API-layer tests in `api.test.ts` lock the candidates endpoint's query-string contract (forwards `stream_name` + `group_id`; omits `group_id` when null). (bd-1lznl / BD-I)
- **Pending Merges page + Channel Manager subnav + post-M3U-refresh toast (bd-gfxrz / BD-J, ADR-008 §D1).** Operator-facing surface for the v0.17.1 interactive stream-dedup epic. **New page `frontend/src/components/tabs/PendingMergesPage.tsx`** renders the `pending_merges` queue (BD-E `GET /api/channel-merges?status=pending&page=1&page_size=50`) one row per record with the incoming stream name, candidate channel id, a confidence badge (visually mirroring StreamDedupModal — exact-match candidates get a green "Exact match" badge and primary-styled Merge button, fuzzy candidates show `"NN% match"`), and per-row **Merge** (calls BD-E `POST /api/channel-merges/{id}/accept`) and **Create New** (calls `POST /api/channel-merges/{id}/dismiss`) buttons. Optimistic remove on success (the backend has flipped the row to a terminal state and the default `status='pending'` filter would exclude it on reload anyway); backend `detail` strings surface verbatim in a per-row inline error banner on failure (matching the bd-7j6v1 / bd-9q9z0 pattern — the row stays in place so the operator can retry or pick the other action). Empty state copy ratified by the PO via the parent epic: `"No pending merges"` + `"Pending Merges will appear here after an M3U refresh detects potential duplicates."`. **Bulk-action buttons ("Resolve All …") are deliberately NOT rendered** — deferred to backlog bead `enhancedchannelmanager-qpgsx` (P3) per the PO; the hard ADR-008 §D2 confidence floor is the architectural backstop against mass-destruction footguns but a low-default-threshold operator could still queue many low-quality rows, so v0.17.1 ships per-row resolution only. **Channel Manager subnav** (`ChannelManagerTab.tsx`) — UX-ratified placement, **NOT** a new top-level tab (the tab bar is already at 10 entries). A subnav header appears above the channels/streams split-pane with a "Channels & Streams" / "Pending Merges" toggle. The subnav link renders **only** when the queue depth is non-zero OR when the operator is already on the page (so a single-resolve doesn't strand them on a view with no way back via the subnav); the count badge appears on the subnav link only — **NOT on the top-level Channel Manager tab** — per the UX alert-fatigue decision. Count is polled every 30s via `getPendingMerges({pageSize: 1})` (we only need `total`, not the rows); a poll failure logs at DEBUG and the badge stays at its last-known value rather than dropping to 0 (which would falsely hide the affordance). **Post-M3U-refresh toast** is decorated by a new `pendingMergesToast.ts` module: when `NotificationCenter` polls and observes a new `auto_creation` notification whose title carries the `"N pending merge(s) queued"` suffix emitted by `run_auto_creation_after_refresh` (BD-F), the decorator returns `{title: 'Pending Merges', message: 'N stream(s) queued for dedup review', actionLabel: 'View'}` and NotificationCenter fires the toast with a "View" action button that dispatches a `ecm:open-pending-merges` `CustomEvent` — `ChannelManagerTab` listens for that event and switches into the Pending Merges view (no prop drilling required; NotificationCenter lives next to TabNavigation in App.tsx, not inside the tab). **Suppress respect**: when `settings.dedup_m3u_toast_suppressed` is true (BD-K Settings toggle) the decorator returns null AND NotificationCenter suppresses the plain auto_creation toast for the same notification so the operator does not see two stacked toasts for the event they explicitly silenced (the notification still appears in NotificationCenter so they can find it later). **One toast per refresh** via NotificationCenter's existing seen-notification-id de-dupe contract. **New API service entries** in `frontend/src/services/api.ts`: `PendingMergeRecord` / `PendingMergesListResponse` / `AcceptMergeOutcome` / `DismissMergeOutcome` interfaces; `getPendingMerges()`, `acceptPendingMerge()`, `dismissPendingMerge()` named exports per the style guide convention. **Styles** reuse shared/common.css (`.btn-primary`, `.btn-secondary`, `.empty-state`, `.error-banner`, `.spinning-cw`) plus a scoped `.pending-merges-page .confidence-badge` clone mirroring the StreamDedupModal pattern (if a third consumer appears, the standing TODO is to promote `.confidence-badge` to common.css). **Tests:** 6 Vitest tests in `PendingMergesPage.test.tsx` (rows render with stream name + confidence badge, empty state with PO nudge text, no bulk-action buttons, Merge calls accept then drops the row, Create New calls dismiss then drops the row, backend error surfaces verbatim with the row preserved); 5 tests in `ChannelManagerTab.test.tsx` (subnav hidden at count=0, subnav with badge at count > 0, badge contained inside the subnav link button — structural proxy for the "NOT on top-level tab" UX spec, subnav click toggles into PendingMergesPage, `PENDING_MERGES_EVENT` switches the view); 9 tests in `pendingMergesToast.test.ts` (suppression returns null, plural / singular title-pattern matching, ignores unrelated auto_creation notifications, ignores notifications with no title, returns null on zero count, action label "View" + title "Pending Merges"). Total 20 new tests across 3 files; full frontend suite stays green (post-rebase count ≈ 1397+8 from BD-H). Closes the v0.17.1 dedup-epic UI gap so the bd-a5lb2 release-cut production gate ("BD-F can merge to dev but MUST NOT deploy to production until BD-J ships") is satisfied — operators now have an in-app surface to resolve pending merges, no longer dependent on MCP / direct API calls. ([bd-gfxrz / BD-J], ADR-008 §D1)
- **Drag-drop integration for the stream-to-channel dedup modal (bd-u6ftw / BD-H, ADR-008 §D1 + §D5).** Wires `StreamDedupModal` (BD-G) into the existing single-stream drag-drop path on Channel Manager. When the operator drops one stream onto a channel group, the new `useDedupOnDrop` hook (`frontend/src/hooks/useDedupOnDrop.ts`) first calls `GET /api/channel-merges/candidates?stream_name=…&group_id=…` (BD-D) and branches on the result: empty `candidates` → fall through to the existing bulk-create-from-group flow exactly as before (the no-match path is fully unchanged); top-1 candidate above the §D2 floor → the dedup modal opens with `trigger="drag_drop"`. **Operator decisions**: `onMerge(channelId)` calls the existing `POST /api/channels/{id}/add-stream` (which already writes a `channel.stream_add` journal entry — backend not modified per the bead's frontend-only scope), then reloads channels and closes the modal; `onCreateNew` runs the original drop-into-group fallback (auto-creation rules are still consulted as usual); `onCancel` aborts the merge and pulses an accent-color outline on the source stream row in the streams pane for 500ms via the new `.is-dedup-returning` class (`@keyframes stream-dedup-return-pulse` in `StreamsPane.css`) so the operator visually re-anchors on the stream they just dragged. **Multi-stream drops are intentionally NOT routed through dedup** — bulk dedup is a separate epic surface (`bd-a5lb2` / BD-F bulk M3U dedup hook); the dedup-on-drop integration only fires when `streamIds.length === 1`. **`prefers-reduced-motion: reduce` is honored at the source**: the hook never adds the streamId to `returningStreamIds` when reduced motion is active, so no animation runs and no `@media` override is needed in CSS. **Candidate-lookup failure is non-blocking**: a backend error from the candidates endpoint logs a WARN and falls through to the existing create path — the dedup feature must never block a drop the operator already initiated. **API client surface** (`frontend/src/services/api.ts`): new `getDedupCandidates(streamName, groupId)` named export plus `DedupCandidate` and `DedupCandidatesResponse` interfaces. **Tests:** 8 Vitest tests in `useDedupOnDrop.test.ts` lock the contract: no-candidate branch runs the fallback and never opens the modal; backend error also falls through to the fallback (the bead's "unchanged behavior" contract); candidate-found branch opens the modal with the candidate and skips the fallback; `onMerge` calls `addStreamToChannel` with the parsed numeric id and reloads channels; `onMerge` rethrows on API failure so the modal can surface the error inline; `onCreateNew` closes the modal and runs the fallback; `onCancel` registers the streamId in `returningStreamIds` for the 500ms pulse window and clears it on timeout (the cancel-animation class application contract from the bead); `prefers-reduced-motion` short-circuits the registration so no animation runs. **Pre-assigned version slot 0017** (0017 was the next free slot above origin/dev's 0016 at the time of branch; 0018 is held by the open `feat/bd-7u8ms` PR). ([bd-u6ftw], ADR-008 §D1/§D5)
- **BD-I "Add Stream" dedup integration on the Streams pane (bd-1lznl, ADR-008 §D1 `trigger_context='add_stream'`).** Wires `StreamDedupModal` (BD-G) into the single-stream "Create channel(s) in group" context-menu flow on `StreamsPane` so an operator promoting a channel-less stream into a channel is prompted before silently creating a duplicate. On a single-stream selection the new `useAddStreamDedup` hook (`frontend/src/hooks/useAddStreamDedup.ts`) calls `GET /api/channel-merges/candidates?stream_name=…&group_id=…` (BD-D's synchronous lookup); if a candidate clears the ADR-008 §D2 60% floor the modal opens with the stream name + candidate + confidence; if not, the original `openBulkCreateModalForStreamIds` path runs unchanged (auto-creation rules consulted as usual). **onMerge** parses the ADR-008 §D8 string `channel_id` to the numeric Dispatcharr id and calls `POST /api/channels/{id}/add-stream` (the existing `addStreamToChannel` helper) to append the source stream to the candidate channel, then fires the new `onChannelsChanged` callback so the parent re-fetches the channels list and the mapped-streams set reflects the new binding. **onCreateNew** runs the original create-channel callback (the bulk-create modal). **onCancel** just closes the modal — per the bd-1lznl spec there is no card to bounce back, so the BD-H cancel-animation does not apply here. **Lookup failure intentionally falls through** to the create-channel path so a network blip never silently swallows the operator's click. Multi-stream selections proceed unchanged — bulk dedup is BD-J's surface. New API helper `getChannelMergeCandidates(streamName, groupId?)` plus typed `ChannelMergeCandidate` / `ChannelMergeCandidatesResponse` interfaces in `frontend/src/services/api.ts`. Plumbed through `App.tsx` → `ChannelManagerTab` → `StreamsPane` via the new `onChannelsChanged` prop. **Tests:** 7 Vitest tests in `useAddStreamDedup.test.ts` cover no-candidate falls through to `onProceed`, candidate-found opens the modal (and does NOT call `onProceed`), `onMerge` calls `addStreamToChannel(parsedId, streamId)` and closes, `onCreateNew` runs `onProceed` and closes, `onCancel` closes without downstream effects, candidate-lookup failure falls through, and `group_id=null` (search-all-groups) is forwarded verbatim. 2 new API-layer tests in `api.test.ts` lock the candidates endpoint's query-string contract (forwards `stream_name` + `group_id`; omits `group_id` when null). (bd-1lznl / BD-I)
- `StreamDedupModal` component (`frontend/src/components/StreamDedupModal.tsx`) — the operator-facing decision surface for the v0.17.1 interactive stream-dedup epic (BD-G / bd-4vxjj, ADR-008 §D1 + §D2). Distinct from `MergeChannelsModal` (which is an editing surface for an already-decided merge), this modal answers "merge into this candidate channel, create a new channel, or cancel?". Renders the incoming stream name, the top-1 candidate (name + confidence as a `92% match` badge), and three buttons in tab order Cancel → Create New → Merge. Exact (100%) matches autofocus Merge and apply `.modal-btn-primary` styling so a single Enter press confirms; fuzzy matches force a conscious selection per ADR-008 §D2. Includes a bespoke focus trap confined to the three footer buttons (the systemic ModalOverlay-level fix is tracked in deferred bead `enhancedchannelmanager-bfbk8` per ADR-008 §D10). Respects `prefers-reduced-motion` (suppresses the modal-container slide-in animation). When `onMerge` rejects, the modal stays open with an inline `modal-error-banner` surfacing the backend's detail string verbatim (matching the bd-7j6v1 / bd-9q9z0 error-banner pattern) and the Merge button re-enables for retry. The no-candidate state ("No candidate found above the confidence floor — create a new channel or cancel.") trusts BD-D's endpoint to enforce ADR-008 §D2's hard floor — the client never sees and therefore never surfaces a sub-floor candidate. Reuses ModalBase.css shared classes (`.modal-container`, `.modal-md`, `.modal-header`, `.modal-body`, `.modal-footer`, `.modal-btn-primary`, `.modal-btn-secondary`, `.modal-form-group`, `.modal-error-banner`, `.modal-empty-state`); adds `StreamDedupModal.css` with scoped `.stream-dedup-*` classes plus a scoped `.confidence-badge` that visually mirrors the BulkEPGAssignModal pattern. Accompanied by 19 Vitest tests covering: render gates (isOpen toggle, stream-name display), no-candidate state (empty state + Merge disabled + Cancel/Create New still present), confidence rendering (`0.92 → "92% match"`), autofocus discipline (no autofocus on fuzzy, autofocus on exact, "Exact match" aria-label), tab order (Cancel → Create New → Merge), focus trap (Tab wraps last→first, Shift+Tab wraps first→last via `@testing-library/user-event`), action callbacks (`onMerge` called with candidate.channel_id, `onCreateNew`, `onCancel`), async merge error (modal stays open, backend detail surfaced, Merge re-enabled), and `prefers-reduced-motion` (class applied/absent based on `matchMedia`). BD-H / BD-I / BD-J integrations (drag-drop, Add Stream button, Pending Merges page) consume this component in subsequent beads. (bd-4vxjj / BD-G)
- **MCP `add_stream` tool with `dedup_action` parameter (bd-7u8ms / BD-P, ADR-008 §D7).** New `mcp__ecm__add_stream(stream_name, group_id, dedup_action='prompt')` tool extending the stream-add workflow with three deduplication modes. `force_new` skips the candidate lookup and always creates a new channel (bypasses BD-D's `/api/channel-merges/candidates` entirely). `prompt` (default) calls the candidates endpoint; if a match above the operator-configured threshold is found, returns the candidate to the agent as `action=pending_merge` with `candidate_channel_id`, `candidate_channel_name`, and `confidence` so the agent can call back with `force_new` or `merge_if_found`; if no candidate is found, proceeds with normal channel creation. `merge_if_found` calls the candidates endpoint; if a match is found, immediately adds the stream to the candidate channel via the existing `channels_add_stream` endpoint (auto-accept without requiring a pending_merge row); if no candidate is found, proceeds with normal channel creation. In all create-paths the tool creates the channel with `channel_group_id` via `channels_create`, resolves the stream by name via `streams_list`, and attaches it via `channels_add_stream`. Endpoint contract `channel_merges_candidates` added to `_endpoint_contracts.py` (BD-O owns `list`/`accept`/`dismiss`; BD-P only owns `candidates`). 5 new tests in `test_tools.py` cover: `force_new` skips candidates and creates channel, `prompt` with no candidate falls through to create, `prompt` with candidate returns structured response without triggering channel create, `merge_if_found` with candidate adds stream to existing channel, and invalid `dedup_action` value is rejected before any backend call. **Note:** `prompt` mode returns re-call instructions to the agent without creating a `pending_merges` row from MCP context (no POST endpoint exists for this from MCP). ADR-008 §D7 alignment is tracked in follow-up `enhancedchannelmanager-b3czq`. ([bd-7u8ms], ADR-008 §D7)
- Settings UI for stream deduplication controls (BD-K / bd-ugzn4, ADR-008 §D2): **Dedup confidence threshold** numeric input (60–100%, default 80, stored as 0.60–1.00 float; UI clamps to floor with visible feedback, server enforces floor) and **Suppress "pending merges queued" toast** checkbox (default unchecked; pending merges are still queued and visible on the Pending Merges page — only the post-refresh toast is hidden). Both controls land in **Settings → Channel Defaults → Stream Deduplication** section, using the existing `.threshold-input-group` / `.threshold-input` / `.threshold-percent` CSS pattern cloned from the EPG auto-match threshold control (no new CSS). Persisted via the existing `/api/settings` POST endpoint. Accompanied by 12 Vitest tests covering render with operator values, load defaults when fields are absent, threshold clamp at 60 floor and 100 ceiling, save-sends-correct-float, toast toggle save, and ADR-008 hint text. (bd-ugzn4 / BD-K)
- **Pending Channel Merges API** (`backend/routers/channel_merges.py`) — three endpoints implementing ADR-008 §D1 + §D3 + §D6 (bd-acqkb / BD-E). `GET /api/channel-merges?status=pending&group_id=Y&page=1&page_size=50` paginates the queue (status ∈ {pending, merged, dismissed}; `created_at DESC` ordering; read-only — `RequireAuthIfEnabled`); `POST /api/channel-merges/{id}/accept` triggers the Dispatcharr-side merge (`RequireAdminIfEnabled` — write-mutates Dispatcharr channel structure, aligned with `POST /api/channels/merge`) with §D4 lazy resolution — a deleted `candidate_channel_id` returns 404 with the operator-actionable `"Target channel no longer exists … dismiss this pending merge and refresh"`, idempotent on terminal `merged` (returns prior outcome envelope), 409 on `dismissed` (invalid cross-transition), flat-outcome `{merged_into_channel_id, journal_entry_id, source_stream_id, confidence, status: 'merged'}` per §D1 (the §D6 audit set surfaces directly in the response so the operator/MCP client sees what the journal recorded without a second round-trip); `POST /api/channel-merges/{id}/dismiss` is a pure ECM-side state flip (`RequireAdminIfEnabled`), idempotent on terminal `dismissed`, 409 on `merged`, flat `{journal_entry_id, status: 'dismissed'}`. **Audit trail (§D6):** every accept / dismiss writes a `pending_merge_journal` row with all seven required fields populated (`actor_token_id`, `action_type`, `source_channel_id`, `target_channel_id`, `confidence_score` captured at action time, `timestamp_utc` in epoch-ms, `trigger_context` carried from the queue row). The pending_merges status flip and the journal write commit in a single transaction so the queue cannot reach a terminal state without an audit row. **Stream-resolution semantics:** the schema gap (pending_merges stores `stream_name`, not stream UUID) means the accept endpoint resolves the source stream by name; the lookup uses `page_size=500` (matching the bulk-import pattern in `dispatcharr_client.py`) and logs a WARN when results hit the page_size ceiling so operators can see ambiguity in trace. The audit-first contract is documented at `PendingMergeJournal.source_channel_id` and ADR-008 §D6: `source_channel_id` carries the resolved Dispatcharr stream id when the lookup is unambiguous, else the raw `stream_name` as the recorded fallback. **Metrics (BD-M LOCKED CONTRACT):** every accept / dismiss bumps `ecm_dedup_merge_requests_total{status="success"|"error"|"dismissed"}` (the runbook-canonical label set; `cancelled` is reserved for the BD-G modal surface). The accept endpoint maps stale-target 404 and invalid-state 409 to 4xx-by-design per the bd-ct9wl / bd-ozhkf precedent — those do NOT count as SLI-10c errors. A new `ecm_pending_merges_queue_depth_added_total` (label-free) counter is also declared for BD-F's bulk-import hook to consume; BD-E does not emit it (it transitions status, it does not INSERT). 22 tests in `backend/tests/routers/test_channel_merges.py` lock the list/accept/dismiss happy paths, idempotency on both terminal states, stale-target 404, invalid-state 409, Dispatcharr-5xx → status='error', ambiguous-stream-match audit-first contract, the page_size-ceiling WARN, and the BD-M metric name + label contract. (bd-acqkb / BD-E, ADR-008 §D1/§D3/§D4/§D6)
- Dedup matcher service (`backend/services/dedup_matcher.py`) for the v0.17.1 interactive stream-dedup epic. Implements `find_candidate(stream_name, candidates, threshold) -> MatchResult | None` using RapidFuzz `token_set_ratio` with hard `CONFIDENCE_FLOOR = 0.60` (operator-tunable threshold clamped to floor per ADR-008 §D2). Includes property-based tests covering the floor-enforcement contract (`test_find_candidate_returns_no_match_below_floor`), identical-input invariant, no-shared-fields invariant, tie-break (lower channel_id), and edge cases. (bd-7xo8e / BD-A)
- Alembic migration 0014 creates `pending_merges` and `pending_merge_journal` tables for the v0.17.1 dedup epic, per ADR-008 §D8. `pending_merges` carries the queue rows (status='pending'|'merged'|'dismissed') with a partial unique index on (stream_name, candidate_channel_id) WHERE status='pending' to prevent duplicate pending rows from repeated bulk M3U imports. `pending_merge_journal` carries the audit trail (actor_token_id, action_type, source/target channel IDs, confidence at action time, trigger context). Migration follows the bd-5w6jz idempotency pattern (CREATE TABLE/INDEX IF NOT EXISTS) and is compatible with the smart-bootstrap fast-path. (bd-6by2n / BD-C)
- **Stats v2 nightly rollup extended to cover per-type event counters (bd-d0ha9).** Migration 0013 (bd-ov5vb) added `reconnect_event_count`, `error_event_count`, and `switch_event_count` to `session_telemetry` but the nightly rollup (`_rollup_provider_daily`) only projected `buffer_event_count` — so the three new counters were silently dropped at rollup time and permanently unrecoverable beyond the 30-day raw-row retention window (ADR-007 D1). This bundle closes the gap: **Alembic migration 0015** adds `reconnect_event_count`, `error_event_count`, and `switch_event_count` to `session_telemetry_provider_daily` as `INTEGER NOT NULL DEFAULT 0` using the bd-5w6jz idempotency pattern (each column-add independently guarded; early-return if all three already present). **`_rollup_provider_daily` SQL** extended from a 6-column to a 9-column `INSERT OR REPLACE` — the `bb` subquery SUMs all four event counters alongside `bytes_delta_sum`; the outer projection passes all four through `COALESCE(..., 0)`. The rollup task's idempotency contract (re-running a day updates in place) means operators can recover historical per-type counts for any date still within the 30-day raw-row window by re-triggering the task. **`SessionTelemetryProviderDaily` ORM model** updated to declare the three new columns, keeping the model in sync with the migration head (baseline test enforces this). **Tests:** 5 new migration smoke tests (fresh up/down + 3 idempotency scenarios: all columns pre-added, one column pre-added, two columns pre-added); 5 new rollup correctness tests (one per new counter + `buffer_event_count` no-regression + all-four-counters mixed with cross-contamination check). Operators with stats v2 dashboards now see the full event-type breakdown in rolled-up provider data beyond the raw-row retention window. ([bd-d0ha9])
- Dedup settings additions (`backend/config.py`): `dedup_threshold` (default 0.80, clamped to ADR-008 §D2 floor of 0.60 with one-time floor-clamp WARN) and `dedup_m3u_toast_suppressed` (default False — operator may disable the toast notification after M3U refresh creates new pending merges). Validator imports `CONFIDENCE_FLOOR` from `services.dedup_matcher` so the matcher (BD-A) and the validator stay locked to the same constant per ADR-008 §D2's single-source-of-truth requirement. UI surface for these settings ships separately in BD-K. (bd-0b6xj / BD-B)
- Print Channel Guide modal now supports an operator-selectable channel-number range filter (`From #` / `To #` inputs). Defaults to the full range so existing print behavior is unchanged for the all-channels case; operators with hundreds of channels can print a specific range (e.g., channels 100–199) for booklet sections without manually post-processing. Range is applied before the existing time-window filter so the printed page count reflects operator intent. (bd-9q9z0)
- ADR-008: Interactive Stream-to-Channel Deduplication design contract (bd-2pd5i / BD-L). Locks API surface, confidence-floor integrity constraint, MCP tool signatures, `pending_merges` schema, and audit field set for the v0.17.1 dedup epic.
- **Bulk M3U import dedup hook populates the `pending_merges` queue (bd-a5lb2 / BD-F).** When the auto-creation pipeline runs after an M3U refresh and would create a new channel from an incoming stream, the new bulk-M3U dedup hook (`backend/services/m3u_dedup_hook.py`) first scores the stream name against existing channels in the same target group using BD-A's matcher (`services.dedup_matcher.find_candidate`). A confidence above the operator-configured `dedup_threshold` (clamped to ADR-008 §D2 floor of 0.60 by the matcher itself) enqueues a `pending_merges` row with `trigger_context='m3u_refresh'` and signals the executor to SKIP the channel-creation API call — the pending row encodes the deferred operator decision for BD-J's UI (and BD-E's accept/dismiss endpoints) to resolve. The hook fires AFTER the executor's existing exact / normalized lookups (`_find_channel_by_name`) have failed and BEFORE the new-channel `create_channel` API call, so all current "merge into existing" and "skip if exists" paths are unchanged. **ADR-008 §D5 partial-unique-index idempotency:** repeat M3U refreshes of the same stream against the same candidate produce one INSERT then catch `IntegrityError` on subsequent attempts, logging at INFO (`[DEDUP] Pending merge for stream=... candidate=... already queued; skipping`) and returning "skip channel creation" because the prior pending row is still authoritative — no crash, no duplicate row. **ADR-008 §D9 broker-free:** writes go directly to `pending_merges` via the existing ECM session; no Celery, no Redis, no scheduler daemon. **Hook is gated to `triggered_by='m3u_refresh'`:** scheduled / manual auto-creation runs predate the dedup epic and stay on legacy "always create" semantics — they are not one of the four ADR-008 §D1 interactive trigger surfaces. **Dry-run preview** never mutates the queue. **LOCKED CONTRACT metric (BD-M):** new `Counter` `ecm_pending_merges_queue_depth_added_total` registered in `backend/observability.py`, +1 per fresh INSERT (collisions do NOT increment — they are not new rows). The companion gauge `ecm_pending_merges_queue_depth` is not in BD-M's locked contract; gauge maintenance (cross-cutting BD-F insert + BD-E accept/dismiss) is filed as the P3 follow-up bead `enhancedchannelmanager-wvr1d`. **Engine wiring:** `triggered_by` threads from `AutoCreationEngine.run_pipeline` → `_process_streams` → `ActionExecutor.__init__`, so the hook only activates on the M3U-refresh path. **Operator-facing toast plumbing:** `ExecutionContext.pending_merges_added` aggregates the per-stream enqueue count; `_process_streams` rolls it up onto the pipeline result as `pending_merges_added`; `run_auto_creation_after_refresh` surfaces the count in the post-refresh notification title (`"Auto-Creation: 0 created, 3 pending merges queued"`) so operators see the signal even before BD-J's UI lands. **Tests:** 14 service-level tests in `backend/tests/services/test_m3u_dedup_hook.py` (constants, dry-run + non-m3u_refresh + empty-candidates short-circuits, fresh-insert happy path, integer→string id stringification, NULL group_id, metric increment, below-threshold silent refusal, hard-floor clamp, §D5 collision with INFO log, collision-does-not-increment-metric, terminal-state row does not block fresh pending) and 8 executor-integration tests in `backend/tests/unit/test_auto_creation_executor_dedup_hook.py` (fuzzy-match enqueues + skips create, no-candidate falls through, high operator threshold falls through, parametric `triggered_by`-gate, §D5 collision via two real executor calls, dry-run bypass). **Release-cut production gate:** BD-F can merge to `dev` independently but MUST NOT deploy to production until BD-J (Pending Merges page UI) ships — otherwise `pending_merges` rows accumulate with no operator-facing UI to resolve them. MCP-only resolution lands with BD-E. This is a release-cut concern, not a PR concern. ([bd-a5lb2])
- **SLO-10 + Prometheus alert rules for the v0.17.1 dedup epic (bd-ft3hk / BD-M).** New `SLO-10: Channel Deduplication` section in `docs/sre/slos.md` codifies three SLIs: SLI-10a candidate lookup p99 latency (<500ms / 7d / 99%), SLI-10b pending merge resolution rate (95% of merges added to the pending queue reach a terminal state — `success` or `dismissed` — within 24h), SLI-10c merge API error rate (<1% / 5m / 99%). Companion alert rules ship in a new `ecm_dedup` group in `docs/sre/prometheus_rules.yaml`: `ECMDedupCandidateLookupLatencyHigh` (warn), `ECMDedupPendingMergeResolutionStale` (warn), `ECMDedupMergeApiErrorRateHigh` (**page** — the merge endpoint is the load-bearing write path of the dedup epic; a failed accept potentially leaves a channel in a half-merged state). The metrics `ecm_dedup_candidate_lookup_duration_seconds`, `ecm_dedup_merge_requests_total{status}`, `ecm_pending_merges_queue_depth_added_total` do not exist yet — they will be emitted by BD-D/E/F as the dedup epic ships. Alert rules are intentionally vacuous (no data) until then; this is deliberate per the team-plan SRE position: rules-without-emitters fire nothing and cost nothing, and the rule definitions are the contract that tells the engineer wiring the emitters which metric names and labels to produce. Three runbook stubs ship at `docs/runbooks/dedup-candidate-lookup-latency.md`, `docs/runbooks/dedup-pending-merge-resolution-stale.md`, `docs/runbooks/dedup-merge-api-error-rate-high.md` — each is explicitly self-identified as a stub, with section headers, summary, metric names in the triage queries, and TODO placeholders for the resolution procedures that fill in as the team accumulates real incident experience. **Also closes the v0.17.0 alert-rules gap:** `ECMTaskSchedulerNextRunNull` was shipped commented-out at v0.17.0 cut so existing operators would not page themselves before Bundle H's heal (bd-1weac) reached their installs; v0.17.0 has now shipped, the heal is in operators' hands, and the alert is now live. Operators using their own Prometheus + Alertmanager get page coverage for the bd-p5b8i regression class going forward. Audit of the other four v0.17.0 metrics flagged in standup (`ecm_task_schedule_last_success_timestamp`, `ecm_database_size_bytes`, `ecm_database_wal_size_bytes`, week-over-week growth) confirmed they already had corresponding alerts shipped at v0.17.0 (`ecm_task_scheduler` and `ecm_database_size` groups respectively); the NextRunNull uncomment was the only genuine gap. ([bd-ft3hk])
- **`GET /api/channel-merges/candidates` — synchronous dedup candidate lookup (bd-kbqwb, BD-D).** New router `backend/routers/channel_merges.py` under the `/api/channel-merges/*` family (ADR-008 §D1 D4-override plural-noun path). Accepts `stream_name` (required), `group_id` (optional — forwarded to Dispatcharr as `channel_group` filter to restrict the candidate pool), `page`, and `page_size`. Fetches channels from Dispatcharr, builds the candidate tuple list, and delegates to `services.dedup_matcher.find_candidate` (BD-A) with the operator-configured `dedup_threshold` from settings (BD-B). Returns top-1 as a 1-element `candidates` list (or empty list on no match) in the ECM flat-outcome envelope with pagination fields always present for forward-compat. Confidence floor enforcement is fully delegated to the matcher (ADR-008 §D2 — no duplicate check in the router). Metric `ecm_dedup_candidate_lookup_duration_seconds` Histogram (BD-M locked contract, SLO-10 latency SLI) emitted on every request wrapping the matcher call. Auth via `RequireAdminIfEnabled` consistent with all protected endpoints. 13 new tests in `backend/tests/routers/test_dedup.py`. `observability.py` extended with the locked metric. ([bd-kbqwb], ADR-008 §D1)

- **MCP dedup tools: `list_pending_channel_merges`, `accept_channel_merge`, `dismiss_channel_merge` (bd-70ylc / BD-O, ADR-008 §D7).** Three new MCP tools in `mcp-server/tools/dedup.py` expose the interactive stream-to-channel dedup queue to AI agents. `list_pending_channel_merges(group_id?, status?)` paginates the `/api/channel-merges?status=…` queue with `status` defaulting to `'pending'` per the ADR §D7 locked contract; accepts all three lifecycle states (`pending` / `merged` / `dismissed`) so agents can inspect history as well as the live queue. `accept_channel_merge(merge_id)` wraps `POST /api/channel-merges/{id}/accept` — on success returns the flat outcome envelope `{merged_into_channel_id, journal_entry_id, source_stream_id, confidence, status: 'merged'}`; on 4xx returns a structured `{error: {code, message}}` envelope the agent can act on rather than raising (ADR-008 §D7 error-envelope contract): `code: 'TARGET_NOT_FOUND'` (404, stale target channel) includes a `dismiss_channel_merge` recovery instruction in the message per §D4 self-recovery guidance; `code: 'INVALID_STATE'` (409) signals a terminal-state collision. `dismiss_channel_merge(merge_id)` wraps `POST /api/channel-merges/{id}/dismiss` — same 4xx → structured-error semantics (409 → `INVALID_STATE`); this is the §D4 cleanup path for a stale-target accept. 5xx from either POST is re-raised as `ToolError` (uncatchable upstream error). `actor_token_id` in the `pending_merge_journal` audit row flows from the MCP bearer token through the ECM auth layer server-side — the tools do not pass identity explicitly. All three tools route through `_endpoint_contracts.ENDPOINTS` via `call_endpoint` (three new contract entries: `channel_merges_list`, `channel_merges_accept`, `channel_merges_dismiss`). `mcp-server/tools/__init__.py` registers the `dedup` module alongside the existing 14 tool modules. 11 tests in `mcp-server/tests/test_dedup.py` cover: list happy-path (pagination envelope), default-status-is-pending, status filter forwarded, group_id filter forwarded, accept happy-path (status injected), accept 404 → TARGET_NOT_FOUND (message contains recovery instruction), accept 409 → INVALID_STATE, accept 5xx → ToolError, dismiss happy-path (status injected), dismiss 409 → INVALID_STATE, dismiss 5xx → ToolError. ([bd-70ylc])

### Documentation
- **Worktree bootstrap script + docs for agents spawned into `.claude/worktrees/agent-*` (bd-scv37).** Adds `scripts/worktree-bootstrap.sh` — an idempotent 45-line Bash script that symlinks the main checkout's `frontend/node_modules` into the active worktree so frontend tooling works without a full `npm install`. The script uses `git worktree list --porcelain` to locate the main checkout path and prints a `PATH` export hint for `fnm`/`nvm` node. Updated `docs/shipping.md` §6 with a "Worktree quirks" subsection documenting the root cause (sparse-checkout + no npm on PATH), the bootstrap command, the direct `.bin/` invocation pattern for `vitest`/`eslint`/`tsc`/`vite`, and the node PATH export. Updated `frontend/CLAUDE.md` with a "Worktree workaround" quick-reference section near the top, cross-linking to the full explanation in `docs/shipping.md`. ([bd-scv37])
- **Stream deduplication documentation for v0.17.1 (bd-0lsas / BD-N, ADR-008).** Three new/updated artifacts covering the full dedup feature surface: (1) `docs/user_guide/channels-streams/stream-dedup.md` — new operator-facing guide covering all three trigger paths (drag-drop, Add Stream button, bulk M3U refresh), the Pending Merges page workflow (navigation via Channel Manager subnav badge, per-row Merge/Create New/error handling), Settings controls (dedup threshold 60–100%, suppress M3U toast), and MCP agent access. Terminology: "merge into existing channel" for the modal action, "candidate channel" for the match result, "dedup" as the feature name — distinct from the existing two-channel merge surface. (2) `docs/api.md` additions — `GET /api/channel-merges/candidates` (query params, response shape, metric emitted), `GET /api/channel-merges` (status filter, pagination envelope), `POST /api/channel-merges/{id}/accept` (auth, flat outcome envelope, audit fields, idempotency contract), `POST /api/channel-merges/{id}/dismiss` (auth, flat outcome), error codes `TARGET_NOT_FOUND` (404, stale target) and `INVALID_STATE` (409, invalid transition). (3) `docs/architecture.md` additions — new "Stream Deduplication Pipeline" section covering the `pending_merges` + `pending_merge_journal` database tables, the matcher service, the `/api/channel-merges/*` router family, the bulk M3U hook wiring, interactive trigger flow diagram, MCP tools, and the boundary with the auto-creation pipeline's own unattended collision detection. Router count updated (20 → 21); MCP tool module count updated (14 → 15). ([bd-0lsas])

### Fixed
- **Auto-creation Smart Sort: account-less streams now participate in priority ordering via a `"custom"` fallback key (bd-sgtmx, GH #244).** Previously the Smart Sort logic gave streams with no M3U account (`m3u_account_id is None`) no way to be ranked by the `m3u_priority` criterion. This added a reserved `"custom"` key in `Settings → M3U account priorities` so such account-less streams can be assigned a priority; `{"1": 100, "custom": 200}` places account-less streams above M3U account 1, and when `"custom"` is unset the default is `0` (no behavioral change for operators who leave it unset). The fix applies to both the auto-creation engine (Pass 3.5 stream reorder) and the stream-prober smart sort. **Correction (bd-o529v / bead ap1ud):** operator-added custom streams are NOT account-less — they belong to the real Dispatcharr "custom" M3U account and carry that account's `m3u_account_id`, so they never reach this `"custom"` fallback. They are now ranked by the dedicated `custom_streams` Smart Sort criterion (bead ap1ud). The `m3u_priority` `"custom"` key described here is therefore a vestigial defensive fallback that only fires for genuinely account-less streams. ([bd-sgtmx], [GH #244])
- **`settings.json` `api_key` field-name collision with the MCP integration (bd-jmi1c, GH #273).** Resolves GH #273 by renaming the Dispatcharr REST API token field from `api_key` to `dispatcharr_api_key`, with one release of back-compat for the legacy name. Three distinct credentials shared a confusingly-overloaded namespace in `settings.json`: ECM read `api_key` as its Dispatcharr REST API token, the MCP "Generate API key" flow correctly writes `mcp_api_key`, and the Settings UI labels both as "API Key". Operators following the redeployment procedure (Settings → MCP Integration → Regenerate Key, copy the new key) were placing the MCP key in `api_key`, which made ECM send the MCP key to Dispatcharr and return 401 on every channel/stream operation. The v0.17.0 `bd-ix1g6` `/health` self-diagnosis helped operators recognize a missing MCP key but did not address this collision. The rename eliminates the lexical hazard at the source. **Backend (`backend/config.py`):** new canonical `dispatcharr_api_key` field on `DispatcharrSettings`; legacy `api_key` field retained as a deprecated alias for v0.17.x with a one-time-per-process `[CONFIG] Reading deprecated 'api_key' field as Dispatcharr token — please rename to 'dispatcharr_api_key' in settings.json` WARN on first read; `load_settings()` silently migrates legacy → canonical in the loaded dict (canonical wins when both are populated); `save_settings()` mirrors canonical → legacy on write so external scripts that read `settings.json:api_key` directly (the workaround in the GH #273 issue body) keep working until the legacy field is removed in a future release. **Backend (`backend/dispatcharr_client.py`):** the X-API-Key header reads `dispatcharr_api_key` with a legacy fallback; the `_settings_hash` includes both fields so the singleton client recreates on any rotation. **Backend (`backend/routers/settings.py`):** `SettingsRequest` accepts either field name (canonical wins when both are present); `SettingsResponse` surfaces both `dispatcharr_api_key_configured` (canonical) and `api_key_configured` (legacy alias) so older cached frontend bundles keep working; `TestConnectionRequest` accepts either field name. **Backend (`backend/routers/backup.py`):** `_SETTINGS_CREDENTIAL_FIELDS` lists both `dispatcharr_api_key` and `api_key` so the backup redactor never leaks either form. **Frontend (`frontend/src/services/api.ts`, `SettingsModal.tsx`, `SettingsTab.tsx`):** type definitions and POST bodies switched to the canonical field; the indicator-state hook prefers `dispatcharr_api_key_configured` with a fallback so a new frontend bundle still works against an older backend mid-rollout. **Tests:** 12 new unit tests in `backend/tests/unit/test_config_dispatcharr_api_key_migration.py` cover the canonical-only read, legacy-only read with WARN, both-populated canonical-wins, one-WARN-per-process discipline, save-time canonical→legacy mirror, and full load→save→load idempotency. 4 new integration tests in `backend/tests/routers/test_settings.py` verify the POST endpoint accepts both field names, picks canonical when both are sent, and GET returns both indicators. **Operator migration path:** existing v0.17.x operators see one `[CONFIG] Reading deprecated 'api_key' field …` WARN on the first start after upgrade; the next save (any settings change, or the next MCP key rotation) silently mirrors the value into the new canonical field. No manual `settings.json` edit is required. Manual cleanup (removing the legacy `api_key` line) is allowed once the operator has confirmed `dispatcharr_api_key` is populated. **Field-rename scope is intentionally narrow:** `mcp_api_key` is correctly named and unchanged; the `auth_method: "api_key"` enum value is unchanged (it is the mode name, not the field name); the bd-ix1g6 `/health` MCP self-diagnostic is unchanged. Legacy 'api_key' field is deprecated and will be removed in v0.19.0 (see bd-ewm4h). ([bd-jmi1c], [GH #273])
- **Bulk channel merge: pre-validate source IDs to return 422 instead of silently producing DELETE 404 noise on stale rows (bd-ozhkf — follow-up to bd-ct9wl).** The `bulk_merge_channels` endpoint had the same bare-`except` footgun as the single-channel `merge_channels` endpoint before bd-ct9wl: a `get_channel` 404 was swallowed and execution fell through to `delete_channel`, generating `[DISPATCHARR] API request failed: DELETE 404` log noise for every stale source ID. The fix mirrors the bd-ct9wl pattern exactly: catch `httpx.HTTPStatusError` with `status_code == 404`, collect missing IDs, raise HTTP 422 with the same detail string (`"Source channels [...] no longer exist — refresh the channels list and try again"`) before any mutations. Two regression tests in `TestBulkMergeChannelsStaleSourceIds` assert the 422 path and that `delete_channel` is not called on stale IDs.
- **Channel merge UX: surface backend 422 detail to operator instead of swallowing into generic "Merge failed" error (bd-7j6v1 — follow-up to bd-ct9wl).** The `httpClient.ts` `fetchJson` function already extracts the FastAPI `detail` field from non-2xx responses and throws it as `HttpError.message`, so `MergeChannelsModal` and `FindDuplicatesModal` already propagate the 422 detail string to the error banner through their `setError` catch handlers — no code change required. Added regression tests to lock this contract: `MergeChannelsModal.test.tsx` and `FindDuplicatesModal.test.tsx` each assert 422 detail renders verbatim in the error banner, a non-422 `HttpError` renders its message, and a non-`Error` thrown value falls back to the generic copy.

- **Plex user attribution (bd-r5f0c.2/r5f0c.4).** Connected Clients and User Stats now show Plex usernames for sessions routed through a Plex Media Server. Configure under Settings → Integrations → Plex Integration. Uses a server-local Plex token (not a plex.tv account token) scoped to a single server.
- **Jellyfin user attribution (bd-r5f0c.3/r5f0c.4).** Connected Clients and User Stats now show Jellyfin usernames for sessions routed through a Jellyfin server. Configure under Settings → Integrations → Jellyfin Integration.
- **Multi-viewer attribution display (bd-r5f0c.9).** When multiple users watch the same channel through the same media server simultaneously, all their names are listed in Connected Clients. The previous behaviour surfaced only the most-recent user's name; the new `*_viewers` JSON arrays capture every concurrent viewer. The legacy `*_user_name` singular fields remain for back-compat.
- **`AttributionBadge` UI component (bd-r5f0c.5).** Renders "via Emby" / "via Plex" / "via Jellyfin" badges in the Connected Clients list, keying on the `attribution_source` field in the stats API response.
- **Operator integrations documentation (bd-r5f0c.7).** `docs/user_guide/integrations/index.md` — side-by-side setup guide for all three media-server integrations, including Plex server-local token guidance, multi-viewer behaviour, privacy posture, and troubleshooting. Partially addresses bd-jn30g (Emby docs gap).

### Security

- **SSRF mitigation on test-connection endpoints (bd-r5f0c.4).** `POST /api/settings/test_emby_connection`, `POST /api/settings/test_plex_connection`, and `POST /api/settings/test_jellyfin_connection` now apply a scheme allowlist (`http`, `https`) and reconstruct URLs using netloc-only (no path interpolation), preventing SSRF via operator-supplied base URLs.

### Changed

- **`_resolve_emby_attributions` → `_resolve_attributions` (bd-r5f0c.4).** Multi-source `asyncio.gather` replacing the Emby-only implementation. The old name is preserved as an alias.
- **`_enrich_channels_with_emby` → `_enrich_channels_with_attribution` (bd-r5f0c.4).** The Active Channels endpoint now calls the unified multi-source helper. Legacy alias `_enrich_channels_with_emby` preserved for any external callers.

## [0.17.0] — 2026-05-16

### Added
- **Debug logging for the channel-edit-modal logo-selection path (bd-nh50y).** Operators reported "logos not loading in the edit-channel modal" but the existing code path had zero observability between the API response and the rendered grid — backend access logs showed `GET /api/channels/logos` returning 200 OK in ~60ms per page, the App.tsx pagination loop and EditChannelModal render path were silent, and the `<img onError>` handlers swapped `cache_url` → `url` without logging the failed URL. This bundle is **instrumentation only** — no fix attempt, deliberately, because the PO chose "add logging then diagnose" over a blind fix. **Backend (`backend/routers/channels.py` `get_logos`):** new single-line INFO diagnostic `[CHANNELS-LOGO] GET /logos page=X page_size=Y search=Q returned N logos in MMM.Mms next=true|false` fires once per request — operator-grepable, names every input field plus the result count, elapsed ms, and next-page flag. Per-row logging deliberately avoided (page_size=500 would flood). **Frontend API layer (`frontend/src/services/api.ts` `getLogos`):** wraps the fetch with DEBUG before (`[LogoApi] GET /logos page=X pageSize=Y search=Z`), DEBUG after (`[LogoApi] Received N logos, next=URL_OR_NULL`), and ERROR on catch (params + error). **Frontend pagination loop:** extracted to `api.getAllLogos(pageSize)` so the App.tsx and LogoManagerTab callsites share one log-line contract — INFO on start (`[LogoLoader] Starting logo load (pagination, pageSize=N)`), DEBUG per page (`[LogoLoader] Page N: fetched X logos, hasMore=Y`), INFO on completion (`[LogoLoader] Loaded TOTAL logos across PAGES pages`), ERROR on catch with the failing page number + partial-results length so operators see how much of the list was lost. **Modal (`frontend/src/components/EditChannelModal.tsx`):** mount DEBUG (`[LogoPicker] Mounted — received N logos in props`), `logos.length`-dependent useEffect (`[LogoPicker] logos prop updated — now N logos`) — `[logos.length]` not `[logos]` to avoid firing on every parent render — and render-time DEBUG (`[LogoPicker] Rendering grid: X filtered (of Y total) logos, search="Q"`). **Critical onError instrumentation:** the logo grid and current-logo preview `<img>` tags now log ERROR with the failed src + cache_url + url BEFORE the existing fallback chain fires, so silent 404s become visible — previously a broken `cache_url` swapped silently to `url` and a broken `url` produced no signal at all. **Test contracts:** two new frontend tests in `frontend/src/services/api.test.ts` (`getAllLogos (debug-logging contract — bd-nh50y)`) lock the happy-path log sequence across two pages and the mid-pagination-error contract (ERROR line must name failing page + partial-results length). One new backend test in `backend/tests/routers/test_channels.py` (`test_emits_per_request_info_diagnostic`) locks the INFO line shape against caplog. A future refactor that silently drops any of these log lines will fail the tests rather than dropping observability into production. **Operator action on the next repro**: reload the channel edit modal once the container update lands, then share browser DevTools console output (filter on `[LogoPicker]`, `[LogoLoader]`, `[LogoApi]`) and `docker logs ecm-ecm-1 | grep '\[CHANNELS-LOGO\]'` for the time of the repro. ([bd-nh50y])

### Fixed
- **Ghost source channels after "Merge channels" in edit mode (bd-ct9wl).** With edit mode active, selecting 2+ channels and choosing "Merge channels" merged them server-side (target channel got the streams, sources were deleted) but the source rows remained visible in the channels grid. The cause was the `useEditMode` working-copy sync effect only adding new channels — it never removed channels that disappeared from the API. The downstream consequence was operator-visible 404 noise: a user who then re-selected ghost rows for a second merge sent the now-stale source IDs to the backend, which forwarded the DELETEs to Dispatcharr and got `404 Not Found` per source (logged as `[DISPATCHARR] API request failed`), leaving the operator with no useful error to act on. The sync effect now also removes channels with persisted IDs that vanish from the API, while preserving locally-created not-yet-saved channels (negative temp IDs from `stageCreateChannel`) so an in-progress local-add does not get nuked by a routine API refresh. Two paired regression tests in `frontend/src/hooks/useEditMode.test.ts` lock the fix and the persisted-ID guard. Defense in depth: `/api/channels/merge` now catches a 404 from `get_channel(source_id)` and returns 422 with `"Source channels [...] no longer exist — refresh the channels list and try again"` instead of falling through to a generic 500. Backend regression test in `test_channels.py::TestMergeChannelsStaleSourceIds`.

- **Stats v2 "Buffering events" metric was a no-op on real installs (bd-ov5vb + bd-1x5v0).** ECM's previous "Buffering" metric returned 0 on every poll because Dispatcharr's `channel_buffering` event only fires on rare ffmpeg-speed threshold trips. The events that actually represent channel-health problems are `channel_reconnect`, `channel_error`, and `stream_switch` — which ECM was filtering out at the API call (`event_type=buffering` was passed as a hard query-param filter). This bundle broadens the ingest to all 4 event types, surfaces them as 4 distinct per-channel counters in `session_telemetry`, and reframes the Providers panel UI so operators see meaningful data. Discovered during the bd-p5b8i investigation when verifying buffer-event filter against the PO's live instance returned `fetched=0` on every poll (the PO's actual 100-event Dispatcharr window had zero `buffering` events but 8 `channel_reconnect`, 4 `channel_error`, and 3 `stream_switch`). ([bd-ov5vb], [bd-1x5v0])
  - **Backend ingest (`backend/bandwidth_tracker.py`):** renamed `_collect_buffer_events` → `_collect_channel_events`, dropped the `event_type=buffering` API filter, buckets the response client-side into four counters (`channel_buffering` / `channel_reconnect` / `channel_error` / `stream_switch`), and drops noise event types (`client_connect`, `login_success`, `epg_*`, `m3u_refresh`, `channel_start/stop`) BEFORE the LRU dedup lookup so the bounded working-set stays focused on the four health types. Return shape changes from `dict[str, int]` to `dict[str, dict[str, int]]`; the writer (`_write_session_telemetry`) now writes per-type counters with the same first-row-wins attribution rule the legacy buffer counter used. New regression-guard test asserts the helper never re-introduces the API-side `event_type` filter.
  - **Schema migration 0013 (`backend/alembic/versions/20260515_2000_0013_session_telemetry_channel_event_counters.py`):** adds `reconnect_event_count`, `error_event_count`, `switch_event_count` to `session_telemetry` as `INTEGER NOT NULL DEFAULT 0`. The pre-existing `buffer_event_count` is preserved verbatim — pre-bd-ov5vb read paths (the GH-59 buffering chart, `SessionTelemetryProviderDaily.buffer_event_count`, `test_perf_provider_stats`) keep returning the same shape with no contract break. Migration follows the bd-5w6jz idempotency pattern: each column-add is independently guarded, partial drift (one or two columns already present from `create_all()`) returns cleanly without raising. Five new migration smoke tests cover fresh up, fresh down, full drift (all three columns pre-added), partial drift (1 column), partial drift (2 columns). Schema-parity boot-guard (`_assert_schema_matches_models`) locks model and head migration in sync; the existing `TestSessionTelemetryMigration::test_upgrade_creates_table_indexes_and_check` head-shape assertion is extended to require all three new columns. The migration's revision-id was bumped from 0012 → 0013 after rebase to avoid a collision with the bd-lbkck `task_schedules_interval_positive` migration that landed on `dev` concurrently.
  - **SLI log line:** renamed from `[STATS_V2] buffer_event_ingest fetched=X deduped=Y attributed=Z` to `[STATS_V2] channel_event_ingest fetched=X deduped=Y attributed_buffer=A attributed_reconnect=B attributed_error=C attributed_switch=D` so SRE's log-derived counter can grow per-type series. Per-channel "unattributed events" warning line gains a per-type breakdown for the same reason. The `[STATS_V2]` prefix is preserved.
  - **API response shape (`GET /api/stats/providers/buffering`):** additive change — the row shape now carries `reconnect_event_count`, `error_event_count`, `switch_event_count`, and a pre-summed `total_event_count` alongside the pre-bd-ov5vb `buffer_event_count`. URL path stays `/buffering` for back-compat with any external dashboard or alerting integration that hard-coded the path. Two new integration tests assert the four-counter shape and the pre-summed total.
  - **Providers panel UI (UX option A — single column with breakdown):** chart title relabeled "Buffering events by provider" → "Channel events by provider"; chart value switched from `buffer_event_count` to `total_event_count` (pre-summed by the backend so the render path stays simple); data-table fallback gains four per-type columns (`Reconnect | Error | Switch | Buffer | Total`) so screen-reader users see the same breakdown sighted users would get from hovering chart points; chart description names the four event types operators care about. Option A was chosen over option B (three separate columns) because it preserves the chart's visual real estate, keeps the legend coherent (one series per provider rather than per provider × event_type), and avoids breaking the visual-baseline screenshot more than necessary.
  - **Out of scope (filed as future work if discovered to matter):** stream-unhealthy log lines (`Stream unhealthy for channel X - no data for Ns`) are upstream-log-only — never written to `SystemEvent`, so not retrievable from any Dispatcharr API today; would require an upstream PR. Cursor-based polling (`since=<id>`) on the system-events endpoint would be cleaner than ECM's current offset-pagination but requires an upstream Dispatcharr PR; not blocking this bundle.

### Added
- **Schema CHECK constraint + Pydantic validator preventing `task_schedules` interval/0 placeholder rows (bd-lbkck, defense in depth for bd-p5b8i).** The DBA spike's verbatim conclusion on the bd-p5b8i scheduling subsystem regression: "interval/0 as a valid state is always a bug." Two-layer defense closes the bad-row attack surface so the placeholder bug Bundle H (bd-1weac) fixes cannot recur via backup-restore or a future code path. **Layer 1 — Alembic migration 0012** adds `ck_task_schedules_interval_positive` CHECK constraint: `schedule_type != 'interval' OR (interval_seconds IS NOT NULL AND interval_seconds > 0)`. SQLite enforces CHECK on INSERT/UPDATE so the placeholder bug becomes `IntegrityError` at the write call site instead of silently corrupting the scheduling subsystem. Migration uses `batch_alter_table.create_check_constraint` (SQLite has no `ALTER TABLE ADD CONSTRAINT`); pattern mirrors bd-gsn3r migration 0011. **Pre-flight repair**: the constraint cannot be added if existing rows violate it. Since Bundle H's `_heal_task_schedules_null_next_run_at` runs in `_run_migrations` AFTER `alembic upgrade head`, the migration cannot rely on Bundle H having already fixed the data. Pre-flight `DELETE` removes any violating row unconditionally; `task_registry._create_default_task_schedule` rebuilds correct-shape rows on the next startup sync. DELETE rowcount logged at WARNING for operator visibility. **bd-5w6jz idempotency**: re-run detection via substring check on `sqlite_master.sql` DDL for the constraint name; downgrade also skips cleanly when the constraint is already absent. **Smart-bootstrap fast-path caveat (documented in the migration docstring)**: `database._schema_matches_head` only inspects columns + tables, NOT constraints, so on long-running installs where every model column is already present, the fast-path stamps `alembic_version` forward without running this migration — the CHECK constraint does NOT land on existing operators' DBs. This is acceptable defense in depth: Layer 2 (the Pydantic validator) covers existing operators' write surface; the constraint primarily protects fresh installs and DBAS backup-restore (which initializes the target DB from scratch and runs migrations). **Layer 2 — Pydantic `model_validator(mode="after")`** on `TaskScheduleCreate` and `TaskScheduleUpdate` in `backend/routers/tasks.py` rejects `schedule_type='interval'` with NULL or `<=0` `interval_seconds` at the API surface (HTTP 422 with message `"interval_seconds must be > 0 when schedule_type is 'interval'"`). The PATCH handler in `update_task_schedule` adds a final cross-check against the loaded row to cover the half-PATCH case where only `interval_seconds=0` arrives (no `schedule_type` in the body) and the existing row is already an interval schedule — that case can't be caught at the request-shape validator without DB context. **Regression lock**: `TestMigration0012` in `test_alembic_smoke.py` covers fresh up, drift (pre-existing interval/0 rows repaired pre-add), and idempotency (re-run is a no-op). New API tests in `test_tasks.py` cover all five rejection paths (POST with interval/0, POST with interval/NULL, PATCH with interval/0 in same body, PATCH with type-change-only into interval, PATCH with interval_seconds-only=0 onto existing interval row). ([bd-lbkck])
- **Task scheduler health observability (bd-qxi02, recommendation from bd-p5b8i SRE spike).** The scheduling subsystem was silently broken for 39+ days on the PO's install (4 months across all operators) and existing observability (`ecm_telemetry_rollup_last_success_timestamp` from bd-7i2vv) did not catch it — that gauge fired only for rollup-specific tasks, no alert rule consumed it, and no Prometheus deployment was ingesting the metric. Three layers of "the signal exists, nobody sees it." This bundle closes the detection gap with two new generic gauges in `backend/observability.py`: `ecm_task_schedule_last_success_timestamp{task_id}` (Unix-epoch seconds of the most recent successful execution per task, label cardinality bounded by the task_registry's ~15 code-constant task IDs) — stamped by `task_engine._execute_task` immediately after every `success=True` TaskResult via the new defensive `observability.record_task_success(task_id)` helper — and `ecm_task_schedule_next_run_null_count` (label-free count of `task_schedules` rows where `next_run_at IS NULL AND enabled=1 AND schedule_type != 'manual'`) — the canonical "scheduler subsystem is broken" signal, the exact bd-p5b8i symptom. The null-count gauge is published at startup post-`_run_migrations` in `database.init_db` and re-emitted on every `TaskRegistry.sync_from_database` (so Bundle H's heal is visible on the next scrape without a container restart), via the new `observability.update_task_schedule_null_count(count=None)` helper that lazily queries the DB when no count is provided. Both helpers are defensive — observability instrumentation must not break the task engine, registry sync, or boot path it's wrapping; failures log at DEBUG. New alert group `ecm_task_scheduler` in `docs/sre/prometheus_rules.yaml` ships 5 alerts and 1 recording rule: `ECMTaskSchedulerNextRunNull` (count > 0 for 5m → page — but ONLY useful AFTER Bundle H's heal ships; pre-heal it would page on every existing operator's first scrape), per-task staleness warnings sized at 2× each task's documented cadence (`ECMTaskScheduleStaleStatsRollup` 25h budget, `ECMTaskScheduleStaleCleanup` 8d, `ECMTaskScheduleStaleM3UMonitor` 12h, `ECMTaskScheduleStaleStreamProbe` 48h), and recording rule `ecm:task_schedule:hours_since_success` for dashboard / ad-hoc PromQL stability. New SLO entry in `docs/sre/slos.md` ("Capacity planning: Task scheduler health") documents the capacity-planning posture (not a numbered SLO — operator-configurable task cadence makes a portable commitment impossible) with explicit deploy-order callout. New runbook `docs/runbooks/task_scheduler_stalled.md` covers the NULL `next_run_at` diagnostic SQL, Bundle H heal recovery path, manual one-shot recovery (UPDATE setting `next_run_at = now+1m` for healing pre-Bundle-H installs), per-task triage from `task_executions` history, and validation queries to confirm recovery. 14 new unit tests in `backend/tests/unit/test_observability_task_scheduler.py` lock the gauge contract (install, label cardinality, per-task isolation, last-write-wins semantics, defensive no-raise on bad input), 2 new integration tests in `backend/tests/integration/test_task_engine_success_metric.py` lock the success-path wiring (success TaskResult stamps the gauge; failure TaskResult does NOT). Bundles H, I, J are parallel — this metrics + alerts bundle works regardless of when they land, but the page-severity NextRunNull alert should only be enabled in production AFTER Bundle H deploys the heal. ([bd-qxi02], [bd-p5b8i])
- **`ecm_database_size_bytes` + `ecm_database_wal_size_bytes` Prometheus gauges (bd-ygoqr).** Two new label-free gauges in `backend/observability.py` expose the on-disk size of `journal.db` and `journal.db-wal` so operators can answer "is my DB growing?" without shelling into the container. Sampling is wired at the two points that already do real DB I/O: `database._perform_maintenance` (after the boot-time VACUUM) and `tasks.cleanup.CleanupTask.execute` (after the weekly VACUUM, regardless of VACUUM success — the operator cares more about "what is it now?" than "did the most recent VACUUM succeed?"). Cost per emit is two `os.path.getsize` calls — single syscalls each. Companion alert rules in `docs/sre/prometheus_rules.yaml` (new group `ecm_database_size`): `ECMDatabaseSizeWarn` (body > 500 MB / 30m / warning), `ECMDatabaseSizePage` (body > 2 GB / 10m / page), `ECMDatabaseWALSizeWarn` (WAL > 200 MB / 15m / warning), `ECMDatabaseSizeGrowthAnomaly` (week-over-week delta > 200 MB / 24h / warning). New recording rule `ecm:database_size_bytes:weekly_delta` is the stable input for the growth-anomaly alert. New SLO entry in `docs/sre/slos.md` documents this as capacity-planning class (NOT a numbered SLO — disk-pressure failure modes are environment-dependent and ECM cannot make a portable commitment about them). New runbook `docs/runbooks/database-size-warn.md` covers the WAL-vs-body triage table, the `PRAGMA wal_checkpoint(TRUNCATE)` recovery, the per-table size-attribution query via `dbstat`, and the explicit step for "operator on pre-bd-ygoqr persisted MANUAL schedule" who needs to flip the cleanup task to CRON via the Settings UI. Cross-references bd-ej995 (WAL checkpoint at startup, in-flight), bd-7i2vv (Stats v2 retention), and bd-dmu8w (journal-entry retention). Six new unit tests in `backend/tests/unit/test_database_size_metrics.py` lock the helper contract on a synthetic file layout (both files present, WAL absent, body absent, defensive no-raise on uninitialized registry, render output, default-path resolution from `database.JOURNAL_DB_FILE`). ([bd-ygoqr])
- **v0.17.0-0035 polish round (bd-9rtlc + bd-8axhi):** corrected the `TOUCHPOINTS` type annotation in `scripts/check_version_consistency.py` from the `callable` predicate to `Callable[[str], str | None]` (added `from collections.abc import Callable`), added a parametrized regression test in `backend/tests/unit/test_check_version_consistency.py` (`test_main_surfaces_both_files_on_simultaneous_mismatch`) that mirrors the actual production divergence — `package.json="0.17.0-0034"`, `backup.py="0.17.0-0033"`, `main.py="0.16.0-0003"` — and asserts the diagnostic surfaces both divergent file names + values plus names the canonical reference, and added an inline scope comment above the `isinstance(e, OperationalError) and _is_schema_drift_error(e)` gate in `backend/bandwidth_tracker.py` documenting that the gate is deliberately SQLAlchemy-only (sqlite3 errors arrive wrapped) while the helper itself walks the chain so future scope expansions only need to loosen the gate. Per code review.
- **CI guard against version-bump skew across the three hand-edited touchpoints.** The version literal lives in `frontend/package.json` (canonical), `backend/routers/backup.py` (`APP_VERSION`), and `backend/main.py` (FastAPI `version=` kwarg). Today's bump workflow requires editing all three in lockstep; historically it didn't — PR #277 caught `backend/routers/backup.py` at `"0.16.0"` while `frontend/package.json` had been bumped 27 times to `"0.17.0-0027"`, and the bd-9rtlc audit grep surfaced a second long-standing skew where `backend/main.py` was at `"0.16.0-0003"` while `frontend/package.json` was at `"0.17.0-0033"` (the FastAPI version only shows in the OpenAPI schema, which no external consumer cited, so nobody noticed for ~30 builds). New script `scripts/check_version_consistency.py` reads each touchpoint, compares against the canonical `frontend/package.json` value, and exits non-zero with a diagnostic naming the divergent file + both versions on mismatch. New `version-consistency` job in `.github/workflows/test.yml` runs the script on every push + PR to `main`/`dev`, so the next missed bump fails the PR before merge. Both pre-existing skews are corrected at this bump (all three touchpoints now `"0.17.0-0034"`). When a fourth touchpoint is added, document it in the new "Touchpoints" section of `docs/versioning.md` and add it to `TOUCHPOINTS` in the script. 13 new unit tests in `backend/tests/unit/test_check_version_consistency.py` lock the extractor + main() exit-code contract on a synthetic repo layout (so the tests don't churn on every version bump). ([bd-9rtlc])
- Visual regression coverage for the Stats v2 Channels-by-Provider heatmap. New Playwright config (`playwright.visual.config.ts`) screenshots the `Heatmap` chart primitive in isolation via a standalone Vite fixture page (`frontend/visual-fixture-heatmap.html` + `frontend/src/visual-fixture/heatmap-fixture.tsx`) and diffs against a committed baseline PNG (`e2e/visual/__screenshots__/heatmap.spec.ts/heatmap-providers-panel.png`). The fixture mounts only the `Heatmap` with a deterministic dataset (6 providers including "Unknown" × 24 channels including the ~145px-wide "917 | Milwaukee Brewers" label that exposed `bd-wdpve`) so the rendered pixels are reproducible across runs and OSes. Tolerance is `maxDiffPixels: 500` — empirically catches the bd-wdpve-class regression (~3360 differing pixels when the column-label rotation is wrong) while tolerating cross-OS font-hinting noise (<100 differing pixels). New CI job `Visual Regression` in `.github/workflows/test.yml` installs Chromium with the standard `actions/cache` Playwright pattern and runs the visual gate on every PR — defense in depth on top of the existing jsdom-based DOM rotation lock in `Heatmap.test.tsx`, which catches the rotation *attribute* but not the rendered *pixels*. The fixture page is dev-server-only and does not appear in `vite build` output. Follow-up to `bd-wdpve`. ([bd-lln2x])
- Stats v2 Active Channels live stream-identity badge: `GET /api/stats/channels` now enriches each active-channel row with `stream_name` and `m3u_account_id` resolved at request time via the same resolver chain the BandwidthTracker poll uses (direct `stream_id` → URL-derived → channel-streams cross-ref). UI: StatsTab Active Channels section renders a `[<provider>] - <stream_name>` badge next to the channel name (e.g., `[Infinity] - US: TNT`) using `streamLabel` helper from `bd-kh23e` and the `getM3UAccounts` side-load pattern from `bd-vjv7k`. Edge cases: when `m3u_account_id` is unmapped or NULL, falls back to bare `<stream_name>` (no `[Provider <id>]` leak); when `stream_name` equals the channel display name, suppresses the badge to avoid duplication; when both are absent, no badge. Shared resolver entry point `resolve_active_channel_streams()` extracted to `bandwidth_tracker.py` as a free function; the endpoint instantiates a fresh `ChannelStreamsCache` per request and passes `emit_metrics=False` so on-demand calls don't dilute the cyclic-poll SLI signal owned by the polling hot path. Closes the "I don't see the stream name next to the active channels" gap (`bd-kh23e` covered historical aggregates only, not the live view). ([bd-ox5q8])
- Stats v2 stream-identity capture: `session_telemetry` persists `stream_id` and `stream_name` per row (Alembic `0010`). Resolver refactored to return a `ProviderResolution(provider_id, stream_id, stream_name)` NamedTuple per channel. APIs `GET /api/stats/watch-time/{user_id}` and `GET /api/stats/providers/channel-heatmap` gain `latest_stream_id` + `latest_stream_name` (picked via `MAX(observed_at)` per channel / per `(provider, channel)`). UI: Users panel per-user breakdown adds a "Stream" column; Providers panel heatmap data-table shows `[<provider>] - <stream>` (e.g., `[Infinity] - US: TNT`). Format helper at `frontend/src/utils/formatting.ts` falls back to bare stream name when provider context isn't available. Closes the "we don't show which stream is playing" gap. ([bd-kh23e])

### Fixed
- **Root-cause fix: CRON-default scheduled tasks have been silently not firing on every operator install for 4 months (bd-1weac, bd-p5b8i, GH #243).** `task_registry.TaskRegistry._create_default_task_schedule` was added in v0.8.7-0023 (2026-01-29) when every registered task had either an `INTERVAL` or `MANUAL` default. The CRON branch fell through to a default that wrote `schedule_type='interval', interval_seconds=0` into `task_schedules`. `schedule_calculator.calculate_next_run` returns `None` for `interval_seconds <= 0` (see `_calculate_interval_next_run`), so the row's `next_run_at` column was `NULL` from the moment it was written. `task_engine.check_and_run_tasks` filters `WHERE next_run_at IS NOT NULL` (see `TaskEngine.check_and_run_tasks`), so the row exists in the DB and shows up in the Settings → Tasks UI but **never fires**. Tasks have been silently not firing on every operator install for 4 months due to bd-p5b8i; this restores execution. Affected operators see the heal log line on next container restart; no operator action required. **Affected tasks**: `CleanupTask` (after bd-ygoqr flipped its default to CRON `0 2 * * 0`), `StatsV2RollupTask` (since it landed at CRON `30 3 * * *` per bd-7i2vv) — and any future task that ships with a CRON default. The bd-ygoqr fix flipped the constructor default to CRON but never reached the load-bearing `task_schedules` row that `task_engine` actually reads; bd-ifmr5's database migration flipped existing-operator `scheduled_tasks` rows to CRON but again never reached `task_schedules`. **Why not an Alembic migration**: same constraint as bd-ifmr5 — the bd-5w6jz smart-bootstrap fast-path in `database._bootstrap_alembic` stamps `alembic_version` forward when `_schema_matches_head()` returns True, so an Alembic data migration would be SILENTLY SKIPPED on every existing v0.17.0 install. The `_run_migrations` pattern in `backend/database.py` runs unconditionally on every startup with WHERE-clause idempotency, which is exactly the shape this fix needs. **Fix scope**: (1) `task_registry._create_default_task_schedule` now delegates CRON parsing to `database._convert_cron_to_schedule` (the same helper that powers the v0.8.7 `scheduled_tasks → task_schedules` migration), so a `CleanupTask` CRON `0 2 * * 0` produces a `task_schedules` row at `schedule_type='weekly', days_of_week='0', schedule_time='02:00'` with `next_run_at` populated, and a `StatsV2RollupTask` CRON `30 3 * * *` produces `schedule_type='daily', schedule_time='03:30'` with `next_run_at` populated. Unknown cron expressions fall back to daily at the configured time (no longer the interval/0 silent-skip trap). (2) New `_heal_task_schedules_null_next_run_at()` helper wired into `_run_migrations` after `_migrate_cleanup_task_manual_to_cron`. It scans `task_schedules` for `next_run_at IS NULL AND enabled = 1`, identifies rows in the pre-fix `interval/0` shape and rewrites them from the registered task class's `schedule_config`, and for operator-customised schedules (e.g., the cleanup row was set to `daily 06:30 America/New_York` via the UI but `next_run_at` is NULL for a separate reason) just recomputes `next_run_at` from the existing columns — operator intent survives the heal. Operator-visible INFO log `[DATABASE] Healed N task_schedules row(s) with NULL next_run_at — class defaults rehydrated (bd-1weac)` fires only when N > 0, so the second container restart and every restart thereafter is silent (same contract as bd-ifmr5). Disabled rows (legitimately NULL by design — no point computing a run time for a disabled schedule) are NOT touched. **Regression lock**: 12 new unit tests across two files. `backend/tests/unit/test_task_registry_cron_default_schedule.py` (5 tests) locks the Part 1 write-path fix: `test_cron_weekly_translates_for_cleanup_task_sunday_0200`, `test_cron_daily_translates_for_stats_v2_rollup_03_30`, `test_interval_passthrough_unchanged_for_m3u_change_monitor` (no-regression guard for the path the bug didn't touch), `test_noop_when_row_already_exists` (idempotency — preserves operator UI customisations), and `test_pre_fix_symptom_does_not_recur_no_interval_0_no_null_next_run` (direct sentinel against the bd-p5b8i shape). `backend/tests/unit/test_heal_task_schedules_null_next_run_at.py` (7 tests) locks the Part 2 heal: `test_heal_rewrites_broken_cleanup_row_to_weekly_with_next_run_at` (the GH #243 happy path for existing operators), `test_heal_rewrites_broken_stats_v2_rollup_row_to_daily`, `test_heal_is_idempotent_across_restarts` (the production contract — second run logs nothing), `test_heal_preserves_disabled_rows`, `test_heal_preserves_healthy_interval_row` (no-regression guard for m3u_change_monitor and other interval/300 tasks), `test_heal_preserves_operator_customised_schedule_only_recomputes_next_run` (operator intent survives), and `test_heal_noop_when_table_missing` (defensive, matches the rest of this file's table-existence guards). All tests run against real file-backed SQLite (bd-ej995 pattern) — not mocks, so the UPDATE is observably hitting actual SQLite. **Operator recovery**: automatic on next container restart — no manual SQL, no settings change, no UI workaround. ([bd-1weac], [bd-p5b8i], GH #243)
- **`auto_creation_executions` BLOB + `health_checks` + `notifications` retention added to `CleanupTask` (bd-ia28g, bd-p5b8i DBA spike attribution correction).** The bd-f9gd8 spike's `session_telemetry` retention story was the wrong target — `session_telemetry` already has its own retention via `StatsV2RollupTask` (bd-7i2vv). The bd-p5b8i DBA re-attribution against the PO's 165MB operator DB found the actual large tables: **`auto_creation_executions` BLOBs are 127MB / 77% of the DB**, `health_checks` is 20MB / 53k rows / 14%, and `notifications` is 2.2MB. None of these three had retention. This bundle adds three new prune blocks to `backend/tasks/cleanup.py` against the actual offenders, with three companion config keys exposed via `get_config()` / `update_config()` and editable from Settings → Tasks → Database Cleanup (`TaskEditorModal.tsx`).
  - **Block 1 — auto_creation_executions BLOB null-out (`auto_creation_blob_days`, default 30):** the four large JSON BLOB columns (`execution_log`, `dry_run_results`, `created_entities`, `modified_entities`) on rows older than the cutoff are NULLed via a single `UPDATE ... SET ... WHERE started_at < cutoff AND (execution_log IS NOT NULL OR dry_run_results IS NOT NULL OR created_entities IS NOT NULL OR modified_entities IS NOT NULL)`. The summary row (id, status, started_at, completed_at, statistics counts) is **preserved** for audit history per DBA recommendation — operators keep their "what ran when, what was the result" trail while the multi-MB per-row payloads age out. The OR'd `IS NOT NULL` filter across all four BLOB columns is the idempotency gate: a second cleanup pass on the same row is a strict no-op (rowcount=0), and the gate correctly catches the empty-execution-log / non-empty-entities shape that `auto_creation_engine.set_execution_log(log)` produces when the per-stream log list is empty but channels were still created (filtering on `execution_log` alone would miss those rows). **Operator log line distinguishes deleted vs nulled:** the post-run summary now reads `Cleanup completed. Deleted N records, nulled M auto-creation BLOBs.` — `auto_creation_blobs_pruned` is excluded from the rolled-up "deleted" count because nulling a BLOB column is not the same as deleting a row.
  - **Block 2 — health_checks DELETE (`health_checks_days`, default 7):** the `health_checks` table is **NOT** in `models.py` — it's a legacy table created by the v0.11.6 Background Health Check Service spec (bd-lq8dq.1.13). The service itself was removed from the codebase, but the table persists on long-running installs (53k rows / 20MB on the GH #243 operator). The prune uses raw SQL with an `inspect()`-based table-existence check so fresh installs (which never had the legacy table) are silent no-ops, not error-noise. The default of 7 days follows the DBA recommendation: high-frequency polling data loses diagnostic value quickly. The block auto-detects which timestamp column is present (`created_at` → `checked_at` → `timestamp`) since the legacy schema didn't standardize, falling back to skip-with-warning if none match. The dynamic-column DELETE is documented inline as safe because `ts_column` is drawn from a hardcoded allowlist tuple, not user input.
  - **Block 3 — notifications DELETE (`notifications_days`, default 30):** uses each row's `expires_at` if set (auto-expiring notifications honor their own TTL), otherwise falls back to `created_at` + cutoff. This avoids deleting a notification deliberately given a long `expires_at` ("keep this for 90 days") just because it happens to be older than 30 days by `created_at`. The SQL OR-clause encodes this exactly: `(expires_at IS NOT NULL AND expires_at < now) OR (expires_at IS NULL AND created_at < cutoff)`.

  **Frontend:** three new number inputs added to the existing retention-fields section in `TaskEditorModal.tsx` for the `cleanup` task, matching the established `retention-item` / `form-hint` pattern; each input documents what its block does so operators understand the trade-off before lowering the value. **Tests:** new file `backend/tests/unit/test_cleanup_task_blob_health_notifications_retention.py` with 16 tests across real file-backed SQLite (bd-ej995 pattern, not mocks) covering: config schema round-trip (3 tests), BLOB null-out happy path + fresh-row preservation + idempotency + the null-execution-log/populated-entities regression (4 tests), health_checks delete + absent-table no-op + alternate-timestamp-column + idempotency (4 tests), notifications by created_at + by expired TTL + future-TTL protection + idempotency (4 tests), and one end-to-end test asserting all 7 cleanup steps run (was 4: probe + tasks + journal + vacuum). Companion frontend tests in `frontend/src/components/TaskEditorModal.test.tsx` (4 tests) lock the three input render contracts and the save-handler round-trip into `api.updateTask`. **Operator recovery:** automatic on next weekly cleanup cron run (Sunday 02:00 UTC per bd-ygoqr default) — no manual SQL, no settings change required. Operators with persisted MANUAL cleanup schedules are migrated to CRON by bd-ifmr5 (already shipped at v0.17.0-0041), so this fix lands for the GH #243 population that needed it most. ([bd-ia28g], [bd-p5b8i])
- **Existing installs now actually get the bd-ygoqr CRON default (bd-ifmr5, GH #243 follow-up).** _Note: this `scheduled_tasks` data flip didn't reach existing operators because of the broader bd-p5b8i `task_schedules` placeholder bug. The bd-1weac fix above is the load-bearing piece that actually starts cleanup running for existing operators — bd-ifmr5 flipped the right source-of-truth (`scheduled_tasks`) but `task_engine` reads `task_schedules`, which stayed at the pre-fix interval/0/NULL placeholder shape until bd-1weac shipped._ bd-ygoqr (PR #289) changed the `CleanupTask` constructor default from `ScheduleType.MANUAL` to `ScheduleType.CRON` (Sunday 02:00 UTC) so journal entries (90d hot retention per bd-dmu8w), task-execution history (30d default), and failed/timeout `stream_stats` rows (30d default) actually enforce. But that fix is reached ONLY on **fresh** installs — every existing install (including the PO's own 130MB instance and the original GH #243 reporter's container) has a persisted `scheduled_tasks` row written at first-boot with `schedule_type='manual'`, and `task_registry.TaskRegistry.sync_from_database` faithfully rehydrates it into the constructor, so the bd-ygoqr default is never reached for exactly the population the fix was for. **Why not an Alembic migration**: the bd-5w6jz smart-bootstrap fast-path in `database._bootstrap_alembic` stamps `alembic_version` forward when `_schema_matches_head()` returns True (every model table + column exists). For every existing v0.17.0 install, the `scheduled_tasks` table + columns are already present, so a regular Alembic data migration would be SILENTLY SKIPPED by the fast-path's stamp-forward — the data fix would never apply. The codebase's `_run_migrations` pattern in `backend/database.py` runs unconditionally on every startup with idempotency enforced via WHERE clauses, which is exactly the shape this fix needs (same pattern as `_migrate_normalize_names_to_normalization_group_ids`, `_add_unique_client_connections_user_columns`, and the rest of the in-place data migrations). **Fix scope**: new `_migrate_cleanup_task_manual_to_cron()` helper wired into `_run_migrations` after `_add_unique_client_connections_user_columns`. SQL is one statement — `UPDATE scheduled_tasks SET schedule_type='cron', cron_expression='0 2 * * 0' WHERE task_id='cleanup' AND schedule_type='manual'` — and the WHERE clause is the idempotency gate: already-CRON, already-INTERVAL, and absent-row installs are all natural no-ops. Operator-visible INFO log `[DATABASE] Flipped cleanup task schedule MANUAL -> CRON Sunday 02:00 UTC for N existing operator(s) (bd-ifmr5)` fires only when the rowcount is non-zero, so the second container restart and every restart thereafter is silent (no false "your schedule got changed again" signal in logs). **Operator caveat — read this before upgrading**: if you deliberately set Database Cleanup to MANUAL via Settings → Tasks (rare — most operators never touched the schedule because the UI didn't surface the retention consequence), this one-time migration will flip you to CRON Sunday 02:00 UTC. Re-set MANUAL from Settings → Tasks after upgrading; the migration assumes MANUAL was the historical default (it was) and not an explicit operator choice (it usually wasn't, per GH #243). **Regression lock**: 6 new unit tests in `backend/tests/unit/test_cleanup_task_manual_to_cron_migration.py` against real file-backed SQLite (the bd-ej995 fixture pattern — not mocks, so the UPDATE is observably hitting actual SQLite): `test_flips_existing_manual_row_to_cron_and_logs_count` (the GH #243 happy path), `test_preserves_existing_cron_row_with_different_expression` (operator's custom CRON like `30 4 * * *` survives untouched), `test_preserves_existing_interval_row` (INTERVAL stays INTERVAL), `test_noop_when_no_cleanup_row_exists` (fresh install — constructor default already handles it, migration must not invent a row), `test_is_idempotent_across_restarts` (the production contract — second run logs nothing, third run logs nothing, …), and `test_noop_when_scheduled_tasks_table_missing` (defensive — matches the rest of this file's table-existence guards). **Operator recovery**: automatic on next container restart — no manual SQL, no settings change, no `UPDATE scheduled_tasks SET schedule_type='cron' WHERE …` workaround. ([bd-ifmr5], GH #243)
- **Startup WAL checkpoint prevents bloated-WAL migrations from tripping the Docker health check.** A user (GH #274) reported a long-running install where `journal.db-wal` had grown to 1.4 GB+ — typically because some connection had been pinning a read transaction across the SQLite auto-checkpointer's would-be merge window. The bloated WAL stretches every read because SQLite walks the WAL to satisfy the read, and the v0.16.0 `normalize_names → normalization_group_ids` migration took 15-20s to complete. With the Dockerfile HEALTHCHECK `start_period` at the upstream default of 5s, the container was marked unhealthy mid-migration; `ecm-mcp` (which `depends_on: { ecm: { condition: service_healthy } }`) refused to start. The rule_lint_findings half of GH #274 was fixed by bd-ax3uj (PR #280) — this entry closes the WAL half. **Fix scope**: new `_wal_checkpoint_truncate()` helper in `backend/database.py` runs `PRAGMA wal_checkpoint(TRUNCATE)` as the first action in `init_db` after the engine is created — BEFORE `_bootstrap_alembic` and `_assert_schema_matches_models` so the migration timeline runs against a clean baseline. Pattern + try/except shape lifted from `backend/routers/backup.py:_create_backup_zip`, which already does this same checkpoint before backup. Logs WAL size before/after at INFO (`[DATABASE] WAL checkpoint: X.X MB -> Y.Y MB`) for operator visibility. Failure mode (file lock, disk full, read-only filesystem) is non-fatal — WARN-and-continue matches the backup.py posture; bootstrap will still run, just against the same bloated WAL the user is already living with. **Regression lock**: 4 new unit tests in `backend/tests/unit/test_database_bootstrap.py::TestWalCheckpointTruncate` — `test_truncate_reduces_dirty_wal_to_zero_bytes` (real file-backed SQLite + pinned reader fixture so the WAL is genuinely dirty pre-call; asserts WAL shrinks to 0 bytes post-call — mocks alone would not have proved SQLite actually truncated), `test_truncate_is_safe_when_no_wal_file_exists` (fresh-install path), `test_truncate_failure_is_non_fatal` (broken-engine raise must be swallowed as WARN), `test_init_db_invokes_wal_checkpoint_before_bootstrap` (ordering contract — captures the call order via a side-effect list so a future refactor cannot silently invert WAL-checkpoint vs bootstrap-alembic). **Operator recovery**: automatic on next container restart — no settings change, no manual SQL. **Polish round (post-review)**: `_wal_checkpoint_truncate` now inspects the `PRAGMA wal_checkpoint(TRUNCATE)` return tuple `(busy, log, checkpointed)` and logs at WARNING (`[DATABASE] WAL checkpoint: X.X MB -> Y.Y MB (incomplete -- WAL busy)`) when `busy=1` — SQLite couldn't acquire the exclusive WAL lock and the WAL was NOT fully truncated, which is exactly the GH #274 disease vector and must not hide behind an INFO log. Same return-inspection extended to `backend/routers/backup.py:_create_backup_zip` for consistency (a busy checkpoint there means the zipped journal.db may not contain the most recent writes). New regression tests `test_busy_checkpoint_logs_warning_not_info` (unit) and `test_wal_checkpoint_busy_logs_warning` (router) lock the WARN-on-busy contract on both call sites. Test fixture `_build_engine_with_dirty_wal` reorganized so `reader.close()` runs in a guaranteed-execution `finally` block instead of leaking until GC if `writer.executemany` raises. Per code review. ([bd-ej995])
- **Docker HEALTHCHECK `start_period` bumped 5s -> 120s to absorb edge-case slow migrations.** Defense in depth on top of bd-ej995's startup WAL checkpoint: even after the WAL truncate, a particularly large migration on a long-running install may still take longer than the upstream-default 5s window. The Dockerfile `HEALTHCHECK` directive (the canonical source — there is no `docker-compose.yml` in the repo; image consumers pick up the baked-in healthcheck) now uses `--start-period=120s` with an inline comment documenting the rationale: WAL checkpoint addresses the common case; this start_period absorbs the edge case where a particularly large migration still runs longer than the default. Bumped consistently across `Dockerfile`, `Dockerfile.dev`, and the README docker-compose example block (which gains a `healthcheck:` comment pointing operators at the override pattern if their installs have consistently fast startups). Operators on installs with consistent fast startups can lower this; the default favors safety over startup time. ([bd-ul0ah])
- **`CleanupTask` default schedule flipped from MANUAL → CRON (Sunday 02:00 UTC) for fresh installs (bd-ygoqr).** _Note: this default-flip didn't reach existing operators because of the broader bd-p5b8i `task_schedules` placeholder bug. The bd-1weac fix above is the load-bearing piece that actually starts cleanup running for existing operators — the constructor change here was correct, but `task_registry._create_default_task_schedule` was miscompiling the new CRON default into a `task_schedules` row that `task_engine` silently skipped._ The single most impactful fix in v0.17.0 polish for GH #243 per the bd-f9gd8 DBA spike: a fresh-install ECM previously left the database cleanup task on `ScheduleType.MANUAL`, so journal entries (90d hot retention per bd-dmu8w), task-execution history (30d default), and failed/timeout `stream_stats` rows (30d default) accumulated unboundedly until the operator discovered the Settings → Tasks panel and configured a schedule by hand. The constructor's default in `backend/tasks/cleanup.py:CleanupTask.__init__` now ships as `ScheduleType.CRON` with cron expression `0 2 * * 0`. **Existing operators are NOT clobbered**: `task_registry.TaskRegistry.sync_from_database` rehydrates the persisted `ScheduleConfig` from the `ScheduledTask` DB row and passes it to the constructor, so the new CRON default is reached ONLY on installs that have no DB row yet (the `if schedule_config is None` branch). Six new unit tests in `backend/tests/unit/test_cleanup_task_default_schedule.py` lock both halves of the contract: fresh installs get the CRON default; explicit MANUAL/INTERVAL/CRON-with-other-cron configs are preserved across reconstruction; and `sync_from_database` round-trips a persisted MANUAL config without falling through to the new CRON default. **Operator guidance (per DBA spike):** the weekly VACUUM in `CleanupTask.execute()` holds an exclusive SQLite lock for seconds-to-minutes on multi-GB databases, which can disrupt streaming. The Sunday 02:00 UTC default is chosen to put the lock in an off-hours window. If your DB is large enough that the weekly VACUUM disrupts streaming OR if your install runs at non-US-aligned hours, override the schedule via Settings → Tasks → Database Cleanup. The new `ecm_database_size_bytes` gauge (above) and the `ECMDatabaseSizeGrowthAnomaly` alert give operators the leading indicator that they may want to do this. Bundle D's bd-ul0ah bumped the healthcheck `start_period` to 120s, which absorbs the worst case for the very first VACUUM after a long-running install upgrades to this default. **Polish round (post-review, v0.17.0-0040):** VACUUM moved off `session.execute` to a raw `engine.connect()` context (matching the established `database._perform_maintenance` pattern) — the pre-existing SQLAlchemy 2.0 implicit-transaction hazard would have raised `OperationalError: cannot VACUUM from within a transaction` on every fresh-install weekly cron run, hidden until now because every cleanup test set `task.vacuum_db = False`; the MANUAL→CRON flip raised the risk profile high enough that the polish round had to fix it. New regression test `TestCleanupTaskVacuumExecutesAgainstRealEngine.test_vacuum_completes_without_operational_error` exercises the real VACUUM path against a file-backed SQLite engine WITHOUT disabling `vacuum_db` (with an explicit sentinel asserting it stays enabled, so a future maintainer can't accidentally re-mask the bug). Companion fix: added `slo: database_capacity` label to all four `ecm_database_size` alerts in `docs/sre/prometheus_rules.yaml` (`ECMDatabaseSizeWarn`, `ECMDatabaseSizePage`, `ECMDatabaseWALSizeWarn`, `ECMDatabaseSizeGrowthAnomaly`) — every other alert group in the file carries an `slo:` label, and Alertmanager routing rules that match on `slo=~".*"` would have silently dropped these (matches `ecm_stats_v2_storage`'s `slo: stats_v2_storage` style for the other capacity-planning class without a numbered SLO). Per code review. ([bd-ygoqr])
- **Defense-in-depth for the hot-deploy hazard: BandwidthTracker now loud-fails on runtime schema drift.** The bd-zaaey loud-fail at `init_db` (`_assert_schema_matches_models`) catches model-vs-DB column drift at boot, but cannot catch drift introduced AFTER the process is already running — for example, the hot-deploy workflow where a developer `docker cp`'s a new alembic migration + writer code into a live container without `docker restart`. The boot guard already passed on the previous restart; the runtime drift surfaces only when the new writer first executes against the still-unmigrated schema, raising `OperationalError: no such column: <name>`. Until now the writer's existing try/except swallowed this as WARN, where it blended into ordinary noise and the telemetry stream silently dropped rows for hours. New `_is_schema_drift_error()` helper detects the SQLite signature (`"no such column"` / `"no such table"`, walking the exception chain so a SQLAlchemy wrapper around a sqlite3 inner is detected on either layer), and `_write_session_telemetry`'s except block escalates the FIRST such error to ERROR-level with an actionable recovery path naming `docker restart ecm-ecm-1` and pointing at bd-zaaey for the structural diagnostic. A one-shot armed flag (`self._schema_drift_alarm_armed`) prevents log flooding — subsequent failures fall back to WARN while the operator repairs the container, and the alarm re-arms on the next successful write so a future drift event triggers another ERROR rather than silently degrading. Non-drift `OperationalError`s (e.g., `database is locked`) and unrelated `RuntimeError`s stay at the existing bd-skqln.12 WARN level — only the schema-drift signature escalates. 10 new unit tests in `backend/tests/unit/test_bandwidth_tracker_schema_drift.py` lock the detector behavior and the escalate/suppress/re-arm contract. ([bd-8axhi])
- **Stats v2 Providers heatmap column labels now render fully (bd-wdpve).** bd-yteek shipped `rotate(-45)` for column labels; in SVG's y-down coordinate system that sends rotated text DOWN-AND-LEFT from the pivot — directly onto the cells below the label band, leaving only the rightmost 1-2 chars visible at the anchor. Fix: flips to `rotate(+45)` (UP-AND-RIGHT in y-down, labels project diagonally into the band above the grid) and widens `COLUMN_LABEL_HEIGHT` 80px → 140px to contain labels like "917 | Milwaukee Brewers" (~95-100px projected height at 45°). Hot-patched in container 2026-05-14; this commit is the proper PR. Test updated to assert `rotate(45)` and explicitly assert NOT `rotate(-45)`. ([bd-wdpve])
- **`ecm_session_telemetry_row_count` gauge semantic conflict resolved (bd-ae58c, Option B).** bd-skqln.12 (BandwidthTracker) set the gauge to per-poll batch size (~10); bd-7i2vv (nightly rollup task) set it to post-prune table total (~250k). The resulting oscillation would trip the `ECMStatsRowCountGrowthAnomaly` alert spuriously. Fix (Option B): BandwidthTracker no longer writes the gauge — `StatsV2RollupTask` is now the sole writer, setting it once per nightly run to the post-prune table total. Per-poll batch size is derivable from the `ecm_session_telemetry_writes_total` counter rate. Alert rule comments updated; gauge docstring updated; `test_successful_write_publishes_row_count_gauge` renamed and inverted to assert BandwidthTracker does NOT touch the gauge. ([bd-ae58c])
- **`docs/security/threat_model_stats_v2.md` §7.8 and §9 updated to reflect bd-gsn3r FK drop (bd-qum5e).** bd-gsn3r (PR #283, migration 0011) dropped the FK on `session_telemetry.user_id`, making the §7.8 data-deletion control ("account deletion scrubs the behavioral trail via FK ON DELETE SET NULL") and the §9 sign-off condition "(a) user_id FK keeps ON DELETE SET NULL" stale. §7.8 now documents the new contract: viewer privacy is operator-managed via ADR-007 D1 raw-row retention (default 30 days); `session_telemetry.user_id` is an opaque Dispatcharr-side identifier with no FK to ECM `users`. §9 condition (a) is struck through and marked superseded, with a forward reference to bd-gsn3r. Schema table updated to remove the old FK annotation. ([bd-qum5e])
- **Stats v2 live header badges no longer silently undercount active channels.** A user reported 5 active channels in the page-header counter but the per-provider live-stats badges summed to only 4 (HD Homerun 2/4 + Infinity 2/10 + Lumen 0/4 + Strong 0/0 = 4). Root cause: the `StatsTab` `m3uConnectionStats` aggregator iterated over `channelStats.channels` but only incremented a provider counter when `ch.m3u_profile_id` was truthy AND the profile mapped to a known account — any channel whose live resolver chain (`bandwidth_tracker._resolve_provider_ids`) couldn't pin down a provider was silently dropped from the badge sum. The 1-missing pattern matched a known resolver gap from the post-`bd-ox5q8` era: when Dispatcharr's `/proxy/ts/status` payload omits `stream_id` AND the URL-derived id misses (`bd-kbgey`/`bd-5g7kx` fallbacks didn't catch the upstream-id case), the backend now returns `m3u_account_id: null` for that row, and the old aggregator just dropped the row instead of surfacing it. **Fix scope**: the aggregator now prefers the backend-resolved `m3u_account_id` (bd-ox5q8's authoritative live result) over the legacy `m3u_profile_id` → account-map fallback, and any channel that doesn't attribute to a displayed provider — null `m3u_account_id`, account filtered out as "Custom"/`is_active=false`, or account not yet side-loaded into `m3uAccounts` — falls into a new "Unknown" bucket rendered with a warning-tinted border + `help_outline` icon next to the real provider badges. **Regression lock**: backend `TestChannelStatsBadgeSumInvariant` (2 tests) pins the endpoint contract — every channel in `GET /api/stats/channels` MUST surface the `m3u_account_id` key (possibly `None`), and resolver-failure mode must still return every active row with `m3u_account_id: None` instead of stripping the row, so the frontend can route unattributed channels to the Unknown bucket. Frontend `StatsTab — provider badge sum invariant (bd-lhxfu)` (3 tests) locks the user-visible invariant: provider badge sum + Unknown bucket count == active-channel count, in three resolver-outcome scenarios (resolved + unresolved mix, all resolved, attributed-to-unknown-account side-load gap). The Unknown badge is also a visible signal of resolver attribution gaps that until now hid in the silent-drop noise — any non-zero Unknown count points at an active stream the resolver couldn't attribute, which is the leading signal for SLO-8 (provider attribution rate, warn-only) regressions. **Operator recovery**: automatic on next browser refresh — no settings change, no DB rewrite. ([bd-lhxfu])
- **Stats v2 User Watch Time panel: architectural fix for the namespace-collision bug bd-uqbob worked around with an env-var exclude filter.** ECM's local `users` table (auth identities for ECM logins) and Dispatcharr's `users` table (stream viewers) are two separate namespaces with coincidentally-overlapping integer IDs. `session_telemetry.user_id` previously had a FK to ECM `users.id` (per `models.py` `SessionTelemetry`) and the read APIs joined against ECM's `User` to surface "username" — but the values written into that column came from Dispatcharr's namespace. When the integer IDs collided (e.g., Dispatcharr viewer id=3 + ECM admin "claude" at users.id=3), the panel attributed real human viewing sessions to the ECM admin. **Fix**: new Alembic migration `0011` adds `session_telemetry.dispatcharr_username TEXT NULL` and drops the FK on `user_id`. The writer (`BandwidthTracker._write_session_telemetry`) populates `dispatcharr_username` directly from the per-poll Dispatcharr `user_id → username` map it already maintains for the bd-uqbob exclude filter — zero extra round-trips, denormalized at write time. The read APIs (`GET /api/stats/watch-time` + `GET /api/stats/watch-time/{user_id}`) read `dispatcharr_username` straight from the telemetry row's DISTINCT-collapse subquery — no more joins against ECM `users`. The `_load_usernames` helper (which queried `User`) is removed. PO directive 2026-05-13: "we should be able to get the active user account from the Dispatcharr API; we never need to touch a table." **Backfill policy**: pre-migration rows have `dispatcharr_username = NULL` and surface as "Unknown viewer" on the frontend (per the Stats v2 history-cutover policy from bd-skqln.3). No automatic backfill — operators who want a clean slate can purge stale rows with the existing one-liner from the bd-uqbob CHANGELOG entry. **bd-uqbob env var (`ECM_TELEMETRY_EXCLUDE_USERS`) is now moot for its original purpose** (the namespace collision goes away) but stays in place — operators may still want to exclude a specific Dispatcharr viewer for any other reason; no harm. Migration 0011 follows the bd-5w6jz idempotency pattern: column add and FK drop are independently guarded so a long-running install where `create_all()` materialised the post-0011 ORM shape (or where the smart-bootstrap fast-path stamped past 0011 without dropping the FK) re-applies cleanly. New regression tests in `backend/tests/integration/test_alembic_smoke.py`: `TestMigration0011` (fresh up + fresh down) and `TestMigration0011Idempotent` (column-already-added drift + FK-already-dropped drift). Existing watch-time integration tests (`backend/tests/integration/test_api_watch_time.py`) updated to seed `dispatcharr_username` on telemetry rows instead of relying on the dropped FK to ECM `User` rows. ([bd-gsn3r])
- **Alembic migrations 0006-0010 are now idempotent against pre-existing artifacts, and a new smart-bootstrap fast-path ends the whack-a-mole architecturally.** A user reported a second outage in the same family as `bd-ax3uj` (which fixed 0003/0004): `sqlite3.OperationalError: table session_telemetry already exists` during `Running upgrade 0005 -> 0006`, aborting container startup (post-`bd-zaaey`, bootstrap loud-fails — prober and everything else went with it). Same disease vector on a different migration: `init_db`'s `Base.metadata.create_all()` materialises ORM-declared artifacts from `models.py` ahead of the migration timeline on long-running installs. The risk inventory across 0006-0010 is broad — 0006 creates `session_telemetry` + 5 indexes from `SessionTelemetry`, 0007 alters `session_telemetry.channel_id` (post-0007 ORM shape lands ahead of the alter), 0008 creates the `channel_watch_stats_v` view, 0009 creates 3 rollup tables (`session_telemetry_user_daily`, `session_telemetry_provider_daily`, `telemetry_rollup_state`) + 3 indexes from their ORM models, 0010 adds `stream_id` and `stream_name` to `session_telemetry` (both columns declared on the ORM model). All five migrations now `inspect()` the live schema and skip `op.create_table` / `op.create_index` / `op.add_column` / `op.alter_column` for any artifact already present — `op.execute("CREATE VIEW ...")` becomes `CREATE VIEW IF NOT EXISTS` (SQLite-supported); 0007's alter checks the column type + nullability and skips when `channel_id` is already `String(64) NOT NULL`; 0010's add-column dance skips the entire view-drop/recreate block when both target columns already exist. **Strategic fix in `database.py`**: new `_schema_matches_head()` helper diffs `Base.metadata` against the live DB's tables + columns; new fast-path in `_bootstrap_alembic` detects "alembic_version lags head AND live schema covers the model shape" and stamps forward instead of running `upgrade head` — ends the whack-a-mole for any future migration that creates an ORM-declared artifact (the per-migration idempotency guards stay as defense in depth). The fast-path is guarded against fresh installs (`current_rev` is unset) and the historical bd-fwpzw stamped-at-HEAD scenario (the existing `_BOOTSTRAP_CANARIES` self-heal still covers that). New regression tests in `backend/tests/integration/test_alembic_smoke.py`: `TestMigration0006Idempotent` (drift scenario + fresh-fix path), `TestMigration0007Idempotent`, `TestMigration0008IfNotExists`, `TestMigration0009Idempotent`, `TestMigration0010Idempotent`, and `TestSmartBootstrapFastPath` (locks the fast-path triggers on schema-matches-head AND skips on fresh installs). Affected users recover automatically on the next container restart — no manual SQL required, no `UPDATE alembic_version SET version_num='0010'` workaround. Polish round (v0.17.0-0031): tightened `TestMigration0007Idempotent` post-upgrade assertion to verify both the type and length of `channel_id` (not just nullability), standardized bd-5w6jz drift-test class naming with an `Idempotent` suffix, and added safety comments in `database.py` documenting the symmetric guard direction, the concurrent-startup assumption on the `command.stamp` write, and the WARNING (vs INFO) log-level choice on the fast-path branch — per code review. ([bd-5w6jz])
- **Alembic migrations 0003 and 0004 are now idempotent against pre-existing artifacts.** A user reported `sqlite3.OperationalError: table rule_lint_findings already exists` during `Running upgrade 0002 -> 0003`, aborting container startup (post-`bd-zaaey`, bootstrap loud-fails — the user's prober and everything else went with it). Root cause is the same class of bug `bd-m2k7p` previously fixed in migration 0005: on a long-running install, `Base.metadata.create_all()` materialises the `rule_lint_findings` table + its two indexes (from the `RuleLintFinding` ORM model in `models.py`), and `idx_journal_batch_id` + `idx_journal_entity` on `journal_entries` (from `JournalEntry.__table_args__`), while `alembic_version` lags at 0002. The next start re-runs 0003 / 0004 and SQLite raises "already exists". Both migrations now `inspect()` the live schema and skip `op.create_table` / `op.create_index` for any artifact already present (matching the 0005 pattern). New regression tests in `backend/tests/integration/test_alembic_smoke.py` (`TestMigration0003`, `TestMigration0004`) lock in the fresh-fix path and the drift path (`create_all()` first, then `alembic upgrade head`) — both pre-fix migrations would have failed these tests with `OperationalError: ... already exists`. ([bd-ax3uj])
- **MCP `/health` is now self-diagnosing** when `api_key_configured` is false. The endpoint returns a new `api_key_status` field that distinguishes the four ways the key can appear missing: `file_not_found` (settings.json absent on the MCP container's `/config` mount — typically a volume mount mismatch between ECM and MCP), `invalid_json` (file present but corrupted), `field_missing` (legacy settings.json that predates the MCP feature and was never re-saved), or `field_empty` (no key generated yet, or key was revoked). The MCP Settings UI's Server Status panel now surfaces the status code plus a remediation hint tailored to the cause, so operators reporting "I generated a key but `/health` still says `api_key_configured: false`" can diagnose the failure mode from the UI alone instead of needing container shell access. Closes the diagnostic gap that motivated `bd-ix1g6`: code-only audit of the v0.16.0 generate→save→reload path (`backend/routers/settings.py:generate_mcp_api_key` → `backend/config.py:save_settings` → `mcp-server/config.py:get_mcp_api_key`) found no reproducible code defect — the persistent `api_key_configured: false` reports almost certainly trace to a config/deployment difference between the two containers' `/config` mounts, which until now was indistinguishable from a real bug at the API surface. New tests: 5 in `mcp-server/tests/test_config.py` (`TestGetMCPApiKeyStatus`) and 5 in `mcp-server/tests/test_server.py` (`TestHealthEndpoint` reports-status branch) lock the contract; `MCPSettingsSection.test.tsx` adds 4 component tests for the diagnostic UI. `get_mcp_api_key()` keeps its original `str`-returning signature so existing callers (auth middleware, `ecm_client`) are untouched. Cherry-picked from `release/v0.16.1` (`bd-ix1g6`).
- Stats v2 User Watch Time panel no longer attributes Dispatcharr-side viewer rows to the ECM admin/API account. ECM's local `users` table and Dispatcharr's `users` table are separate namespaces with coincidentally-overlapping integer ids — when ECM `users.id=3` is the local admin ("claude") and Dispatcharr `users.id=3` is a real human viewer, the Stats panel renders the human's viewing sessions under the admin's name (the panel joins `session_telemetry.user_id` against ECM's `users` table). New env var `ECM_TELEMETRY_EXCLUDE_USERS` (comma-separated, default empty/unset = no filtering, no behavior change) suppresses `_write_session_telemetry` rows whose Dispatcharr-side `user_id` OR resolved username matches any token (case-insensitive). New Prometheus counter `ecm_session_telemetry_rows_excluded_total{reason="excluded_user"}` surfaces suppression rate honestly — skipped writes are counted, not silently zeroed. Per-row `[BANDWIDTH] Skipped telemetry write for excluded user user=… reason=excluded_user` debug log for operator traceability. Backfill of existing mis-attributed rows is operator-driven (NOT auto-applied at startup): `docker exec ecm-ecm-1 python3 -c "import sqlite3; c=sqlite3.connect('/config/journal.db'); n=c.execute('DELETE FROM session_telemetry WHERE user_id=?', (3,)).rowcount; c.commit(); print(f'deleted {n} rows')"` (substitute the local admin's `users.id`). ([bd-uqbob])
- Stats v2 Providers panel polish — "Time spent per provider" + "Bitrate by provider" charts are now self-documenting. The time-spent chart was a single-bucket stacked `AreaChart` rendering one tall "Total" stack with no Y-axis label, so a PO couldn't tell from the chart which provider was which or what the Y-axis values meant. It's now a per-provider `BarChart`: one bar per provider with the provider name on the X-axis, watch minutes on the Y-axis, an explicit "Watch minutes" axis label, and a one-line description above the chart. The bitrate chart already rendered per-provider time-series correctly with auto-scaled tick formatting (bps / Kbps / Mbps), but had no Y-axis label and no caption explaining what the metric was; both are now in place — Y-axis labeled "Bitrate (auto-scaled)" and a one-line description noting the formula derivation (`SUM(bytes_delta) * 8 * 1000 / SUM(poll_interval_ms)` per `(provider, time_bucket)`, equivalently `bytes / seconds * 8`). Backend bitrate + watch-time endpoints are unchanged — both already return per-provider rows and the bitrate formula was already correct; two new contract-shape regression tests (`test_response_row_shape_matches_documented_contract`) pin the field names the panel binds to. ([bd-tknci], [bd-zrk05])
- Stats v2 provider resolver: channel-streams URL-match fallback for the upstream-id case. kbgey's URL fallback parses the trailing `<id>.ts` integer from the active stream URL, but empirically that integer is the **upstream M3U provider's** stream id (e.g. Infinity's `85796`), NOT Dispatcharr's stream row id — `get_streams_by_ids([85796])` returns nothing, the resolver previously gave up with `reason=stream_not_found_url_derived`, and `provider_id` stayed NULL (Unknown bucket). The resolver now falls back to `GET /channels/<channel_id>/streams`, matches by URL substring, and uses the matched stream's `m3u_account_id`. Per-poll cache + small cross-poll LRU keeps the API call rate bounded. ([bd-5g7kx])
- Stats v2 session_telemetry: anonymous `user_id=0` (and `"0"`) no longer poison the per-poll batch commit. Dispatcharr returns `user_id="0"` for anonymous viewing sessions; `session_telemetry.user_id` is INTEGER NULL with an FK to `users.id`. The 0 sentinel doesn't exist in `users` → `sqlite3.IntegrityError` at `session.commit()` rolled back the ENTIRE batch including correctly-resolved rows. New `_coerce_session_user_id(raw) -> Optional[int]` helper normalizes 0 / "0" / "" / None / negative ints / bools / floats / unparseable strings to None; positive ints / int-parseable strings pass through. Defense-in-depth: `_write_session_telemetry` now also catches `IntegrityError` at commit time and logs `[STATS_V2] session_telemetry batch FK violation observed_at=... rows_attempted=...` so any future FK trip-up doesn't silently zero-out a batch. ([bd-gbxmj])

### Fixed (prior)
- Stats v2 provider resolver — URL fallback when Dispatcharr's `/proxy/ts/status` omits `stream_id`. The skqln.14 resolver previously gave up with `reason=no_stream_id` for any active channel where Dispatcharr didn't surface `stream_id` directly, leaving `provider_id=NULL` (the "Unknown" bucket). In dev, this hit 91% of polls (214 of 235). The resolver now also extracts the stream id from the URL path's trailing `<id>.ts` segment (`r"/(\d+)\.ts(?:\?|$)"`) and feeds it into the same `get_streams_by_ids` batch the resolver already runs per poll — no additional API calls. URL-derived lookups that miss log `reason=stream_not_found_url_derived` so ops can differentiate from the direct-stream-id miss case. ([bd-kbgey])
- Stats v2 Users panel — daily watch-minutes chart now displays dates in the operator's **local timezone** and visually marks today as "in progress" (amber dot vs teal for completed days), with a caption explaining the UTC bucketing + 10s update cadence. Resolves the misleading appearance of watch-time "going down" across the UTC date boundary when yesterday-UTC is complete but today-UTC is still accumulating. Server-side aggregation stays UTC; the relabeling anchors each bucket at noon UTC then formats in the operator's tz to pick the most-overlapping local day. ([bd-1qxo9])

### Fixed (existing entry continues below)
- Stats v2 Providers panel polish: provider rows now display the actual M3U account name instead of `Provider <id>`. Frontend side-loads the existing `/api/providers` endpoint at panel mount, builds an id→name map, and uses it in chart legends, heatmap row labels, and data tables. Falls back to `Provider <id>` for unmapped IDs (e.g., during the fetch); NULL stays "Unknown". ([bd-vjv7k])
- Stats v2 Providers heatmap polish: column labels rotated -45° (anchored at column center) and default `cellSize` bumped 32px → 48px; column-label band 24px → 80px to fit rotated text. Resolves the "everything super compressed" overlap when real channel names ("Discovery Channel", "ESPN HD") are wider than the column. The `.heatmap` container's existing `overflow-x: auto` handles wider heatmaps via horizontal scroll. Heatmap primitive interface preserved — `cellSize` prop still overridable for the time-of-day use case. ([bd-yteek])

### Added
- Stats v2 rollup tables + nightly prune (ADR-007 implementation): Alembic migration `0009` adds 3 new tables — `session_telemetry_user_daily` (PK `(user_id, channel_id, day)`) for the GH-62 Users panel rollup, `session_telemetry_provider_daily` (PK `(provider_id, channel_id, day)`) for the GH-59 Providers panel rollup, and `telemetry_rollup_state` (marker table per rollup_name with last_completed_day/run_status/run_error). Nightly scheduled task `stats_v2_rollup` (registered via `task_registry`) aggregates the previous day's raw `session_telemetry` rows into both rollup tables (idempotent via `INSERT OR REPLACE`), updates the state marker, and **after successful rollup** prunes raw rows older than 30 days (per ADR-007 retention) — prune is **skipped on rollup failure** so raw data isn't lost. NULL provider_id raw rows bucket into the `'unknown'` sentinel string in the provider rollup; NULL user_id raw rows are excluded from the user rollup (no behavioral subject to attribute, documented on the model). Watch-seconds use the DISTINCT-`(channel, observed_at)` collapse pattern; bytes_delta_sum and buffer_event_count are summed across raw rows since they're per-poll-per-client measurements. 5 new Prometheus metrics: `ecm_telemetry_rollup_last_success_timestamp{rollup_name}`, `ecm_telemetry_rollup_duration_seconds{rollup_name}`, `ecm_telemetry_rollup_days_processed{rollup_name}`, `ecm_telemetry_raw_rows_pruned`, `ecm_telemetry_rollup_errors_total{phase}` — all bounded-cardinality. ([bd-7i2vv])

### Known issue
- Operator-facing Stats v2 telemetry opt-out: new env var `ECM_STATS_TELEMETRY_OPT_OUT` (default `false`, truthy enum `true|1|yes|on` case-insensitive). When set, `BandwidthTracker._collect_stats` short-circuits the Stats v2 write path immediately after legacy `ChannelBandwidth`/`BandwidthDaily`/`UniqueClientConnection` writes — skipping `_resolve_provider_ids` (saves the Dispatcharr `get_streams_by_ids` call), `_collect_buffer_events` (saves the `get_system_events` call), and `_write_session_telemetry` (skips all `session_telemetry` row writes). Read per-poll so runtime flips take effect on the next cycle. One-shot startup INFO log when enabled: `[STATS_V2] telemetry opt-out is ENABLED — no session_telemetry data will be collected`. Documented in `docs/user_guide/stats/stats-v2-history-cutover.md`. Closes the CONDITIONAL item in the privacy sign-off (`bd-skqln.8`) requiring an operator-global toggle. ([bd-tp1pd])
- Stats v2 Providers panel (GH-59): new **6th panel** in the Stats tab — `ProvidersPanel` — wired to skqln.16's 4 provider-stats endpoints. Four visualizations: buffering events by provider (multi-series line), time spent per provider (stacked area), channels-by-provider heatmap (reuses skqln.17's `Heatmap` primitive + Tol-bright colorblind-safe palette), and bitrate-by-provider over time (multi-series line). Window selector (7d/30d/90d) drives all four; bucket selector (hour/day) drives the two time-series charts. a11y matches skqln.6: semantic `<h3>` heading, data-table fallbacks for all four charts (visually-hidden `<table>` with `<caption>`, "Show chart data" toggle), `role="status" aria-live="polite"` empty states, `aria-label` on selectors, WCAG-AA contrast via theme tokens, focus-visible outlines. NULL `provider_id` rows surface as a labeled "Unknown" bucket in both chart legend and data table. **Admin-only**: non-admin callers see the admin-access notice (`role="note"`), no API calls. Closes `bd-39ao6` (GH-59 per-provider stream stats tracker) together with `bd-skqln.16`. ([bd-skqln.18])
- Stats v2 SLOs + Prometheus alert rules + deployment-safety runbooks. New SLOs in `docs/sre/slos.md`: **SLO-7** (telemetry write success rate, 99.5% over 30d), **SLO-8** (provider attribution rate, 95% over 24h, warn-only — degraded resolution surfaces as "Unknown" bucket, not a fault), **SLO-9** (Stats v2 query latency, p99 < 2s + p95 < 800ms on `/api/stats/*`, 7d). New alert rules in `docs/sre/prometheus_rules.yaml`: `ECMStatsTelemetryWriteFailing` (page on 30m sustained), `ECMStatsTelemetryWriteFailingWarn` (ticket on 5m), `ECMStatsProviderResolutionDegraded` (warn-only), `ECMStatsQueryLatencyHigh` + `ECMStatsQueryLatencyP99High` (warn), `ECMStatsRowCountGrowthAnomaly` (warn — 2× the 7-day rolling avg over 24h). New runbooks: `stats-v2-write-failures.md`, `stats-v2-provider-resolution-degraded.md`, `stats-v2-row-growth.md`, `stats-v2-deployment-safety.md`. The obsolete dual-write divergence alert from the bead's original spec is omitted by design — skqln.3 step (d) retired the legacy ChannelWatchStats writer, so the modern data-consistency SLI is the resolver success rate (SLO-8). ([bd-skqln.11])
- Stats v2 user-facing documentation: `docs/user_guide/stats/users-panel.md` (Users panel operator guide — admin-only gate, multi-client DISTINCT collapse explanation, polling cadence, 5 named caveats) and `docs/user_guide/stats/metric-glossary.md` (every user-facing Stats v2 number defined and verified against the shipped code — `total_watch_seconds`, `session_count`, `last_watched`, `buffer_event_count`, `provider_id` / "Unknown" bucket, `bytes_delta`, derived-only `bitrate_bps`, plus the 5 Prometheus metrics from `bd-skqln.12`). Cross-linked from `docs/user_guide/index.md` and `docs/user_guide/stats/index.md`. Screenshot placeholders marked TODO. ([bd-skqln.9])
- `docs/security/privacy_signoff_stats_v2.md` — post-implementation privacy sign-off for Stats v2 (Security Engineer review against the pre-impl threat model from `skqln.7`). **Status: APPROVED WITH CONDITIONS**. PASS on 5 of 7 review items (data classification, logging discipline, metric cardinality, admin-only auth boundary, external data flow). CONDITIONAL on 2 items (retention policy enforcement awaits `bd-7i2vv` rollup + prune; operator opt-out awaits `bd-tp1pd`) — both in flight. Includes auditable evidence trail (file paths + line numbers + test names) for each PASS item, the release-gate hand-off rule for the conditional items, and an evidence-of-record for the post-skqln.3 dual-write scope deviation. ([bd-skqln.8])
- Stats v2 GH-59 performance benchmarks (follow-up to skqln.10): 4 new pytest-benchmark tests at `backend/tests/performance/test_perf_provider_stats.py` covering the `/api/stats/providers/*` aggregations from skqln.16 (buffering, watch-time, channel-heatmap, bitrate). Reuses the existing 250k-row Zipfian-distributed fixture from skqln.10. Baseline numbers (dev workstation): heatmap median 28.6ms, watch-time 99.3ms, buffering 84.9ms, bitrate 105.9ms — all 7×–28× under the 800ms p95 target. The CI workflow (`.github/workflows/perf-benchmarks.yml`) picks them up automatically; same `--benchmark-compare-fail=median:20%` regression gate. ([bd-zgsbn])
- Stats v2 performance baseline + CI regression gate (`bd-skqln.10`): new `pytest-benchmark` suite at `backend/tests/performance/` covering the watch-time-by-user query (`bd-skqln.5`'s `GET /api/stats/watch-time`). Deterministic 250k-row synthetic fixture in CI (50 users × 5k polls, seed `1_234_567`, Zipfian provider distribution with ~5% NULL provider — already shaped for the 4 GH-59 follow-up benchmarks); 5M-row stretch fixture available via `ECM_PERF_ROW_COUNT=5000000` for local pre-release sanity. New GitHub Actions workflow `.github/workflows/perf-benchmarks.yml` runs **same-runner baseline-vs-PR comparison** (`--benchmark-compare-fail=median:20%`) — sidesteps heterogeneous-runner timing noise that committed-baseline JSONs are prone to. Default test workflow gains `--ignore=tests/performance` so the perf suite runs only in its own job. `.gitignore` excludes `.benchmarks/` stores. Baseline numbers (250k rows, AMD Ryzen 7 PRO 8845HS, Python 3.12.3): min/median/max ≈ 352/363/368 ms, σ ≈ 5.8 ms. Follow-up bead `bd-zgsbn` files the 4 GH-59 provider-aggregate benchmarks now that `bd-skqln.16` shipped — the fixture is already engineered for them. ([bd-skqln.10])
- Stats v2 observability instrumentation: 5 new Prometheus metric families on the existing `/metrics` endpoint — `ecm_session_telemetry_writes_total{result=success|failure}` (counter), `ecm_session_telemetry_write_duration_seconds` (histogram), `ecm_session_telemetry_row_count` (gauge for storage-growth alerts), `ecm_provider_resolution_total{result=resolved|unresolved}` (counter; wires skqln.14's `[STATS_V2] provider_resolution_*` log emission into Prometheus), and `ecm_stats_query_duration_seconds{endpoint, granularity}` (histogram; endpoint = matched FastAPI route pattern, NOT resolved path, so `user_id`/`channel_id` never become label values — bounded ~5×3 = 15 series). **Cardinality discipline**: `user_id`, `channel_id`, `session_id`, `target_id`, `client_ip`, `trace_id` are NEVER metric labels — parametrized test asserts the rule across every Stats v2 metric. `provider_id` is allowed (bounded <20 per SRE pre-clearance). The bead's original dual-write-divergence SLI is omitted by design: skqln.3 step (d) retired the legacy `ChannelWatchStats` writer, so there is no dual-write to compare; the modern data-consistency SLI is the resolver success rate (`ecm_provider_resolution_total{result=resolved}` / total), target ≥95% steady state. SLO definitions, alert rules, and runbook entries land in `bd-skqln.11` (unblocked by this). ([bd-skqln.12])
- Stats v2 Users panel (GH-62): new **5th panel** in the Stats tab — `UserStatsPanel` — wired to skqln.5's watch-time API. Primary table = per-user totals (`user | total minutes | last watched`) with a per-user drill-down showing the channel breakdown (`channel name | total minutes | last watched`). Date-range selector with 7/30/90-day presets. One daily-trend line chart (watch-minutes, 30-day window). a11y: semantic `<h3>` heading consistent with sibling Stats panels, **chart data-table fallback** always-rendered as visually-hidden `<table>` with `<caption>` (screen readers cannot read SVG; "Show chart data" toggle reveals it visually), `role="status" aria-live="polite"` empty-state announcement, explicit `aria-label` on date-range select and drill-down buttons, WCAG-AA contrast via theme tokens (`var(--text-primary)`) overriding Recharts defaults, focus-visible outlines, keyboard traversal verified. Non-admin callers see a **"User watch-time statistics require admin access"** notice (`role="note"`) rather than the panel silently disappearing — keeps the feature discoverable while honoring the admin-only auth boundary from skqln.5. ([bd-skqln.6])
- Stats v2 per-provider read APIs (GH-59): four new endpoints on `/api/stats/providers/*` backing the upcoming Providers panel — `GET /buffering` (time-series of `buffer_event_count` grouped by `(provider_id, time_bucket)`; params `window=7d|30d|90d`, `bucket=hour|day`), `GET /watch-time` (total per-provider watch time over window; aggregates `SUM(poll_interval_ms)` grouped by `provider_id`), `GET /channel-heatmap` (2D rows×cols grid: providers × top-N channels by bytes; default `top_n=50`, hard cap `500`; two-pass query so the cross-provider top-channel set is selected once then re-grouped by provider), `GET /bitrate` (derived bitrate time-series: `SUM(bytes_delta)*8*1000 / SUM(poll_interval_ms)` per `(provider_id, time_bucket)`). Queries 2 and 4 reuse the DISTINCT-`(provider_id, channel_id, observed_at)` collapse pattern from skqln.3 migration 0008 / skqln.5 to guard against multi-client overcount; queries 1 and 3 sum inherently per-poll columns and don't need the collapse. NULL `provider_id` rows surface as a single "Unknown" bucket row on every endpoint (SQLite's GROUP BY NULL handling) — never silently excluded. Response envelope matches skqln.5: `{data, meta: {from_iso, to_iso, total_rows, window, ...}, pagination: null}`. Property-based test confirms per-provider watch-time sum equals per-user watch-time sum over the same universe (consistency invariant). **Admin-only** per PO directive 2026-05-13; non-admin callers receive 403. ([bd-skqln.16])
- Stats v2 buffer-event ingestion: `BandwidthTracker._collect_stats` now polls Dispatcharr's `GET /api/core/system-events/?event_type=buffering` once per cycle and increments `session_telemetry.buffer_event_count` for matched channels. Cross-poll dedup by `event.id` via a 10k-entry LRU (system-events feed re-delivers recent events; without cross-poll dedup the same event would be counted N times). Provider attribution piggybacks on skqln.14's `_resolve_provider_ids` — one shared resolver call per poll, not two. Count attributes to exactly **one row per (channel_uuid, observed_at) bucket** (the first emitted client-row gets the count, sibling rows write 0) so `SUM(buffer_event_count) GROUP BY provider, time_bucket` stays well-defined for the future per-provider read APIs without per-client double-counting. Events for channels that don't appear in the current poll's snapshot are dropped with `[STATS_V2] buffer_event_unmapped_channel` log. ([bd-skqln.15])
- Stats v2 provider attribution: `BandwidthTracker._collect_stats` now resolves the active stream's `m3u_account_id` for every channel in every poll and writes it as `session_telemetry.provider_id` (the data foundation the upcoming Providers panel and per-provider stats endpoints will read). Resolution piggybacks on the same `DispatcharrClient.get_streams_by_ids()` call ECM already uses for stream metadata — one batched API call per poll, not N. Resolver cache is scoped to a single poll cycle so a stream's failover hop is picked up next poll without staleness. Unresolvable channels (no active stream, mid-failover, or Dispatcharr returns the stream-without-provider) surface as `provider_id = NULL` and feed the "Unknown" bucket in the future provider panel — failures are logged with `[STATS_V2] provider_resolution_failed channel=... stream=... reason=...` and an SLI counter (`[STATS_V2] provider_resolution resolved=X unresolved=Y`) lands per-poll so `skqln.12` can wire it to Prometheus later. ([bd-skqln.14])
- Stats v2 watch-time-by-user read API: two new endpoints on the `/api/stats` namespace — `GET /api/stats/watch-time` (per-user totals, optional `group_by=total|day`, optional `user_id` filter and `from`/`to` ISO-8601 UTC range) and `GET /api/stats/watch-time/{user_id}` (per-user breakdown by channel; `channel_name` side-loaded from `UniqueClientConnection` with a `Channel <uuid[:8]>...` fallback). Both read `session_telemetry` directly (the view doesn't expose `user_id`) and use a `DISTINCT (user_id, channel_id, observed_at)` subquery to guard against multi-client overcount. Response envelope: `{data, meta: {from_iso, to_iso, group_by, total_rows}, pagination: null}`. **Admin-only**: non-admin callers receive 403 from both endpoints regardless of which `user_id` they query. ([bd-skqln.5])
- Stats v2 keystone: BandwidthTracker now writes per-poll viewing telemetry to `session_telemetry` (the foundation for the Users/Providers panels and per-provider stats). Alembic migration `0007` corrects `session_telemetry.channel_id` from `Integer` to `String(64)` UUID for consistency with every other channel-keyed table and with ADR-007's rollup schemas. Alembic migration `0008` adds the `channel_watch_stats_v` SQL view — a read-compat aggregation over `session_telemetry` that exposes `channel_id`, `total_watch_seconds`, and `last_watched` (the columns that faithfully derive from per-poll observation). Migration 0008 uses a `DISTINCT (channel_id, observed_at)` subquery so multi-client concurrent viewing does not silently inflate watch time. ([bd-skqln.3])
- `docs/user_guide/stats-v2-history-cutover.md` — operator-facing note explaining that Stats v2 metrics begin on the day v0.17.0 deploys and prior watch-time history is not reconstructable. The `accept-the-gap` backfill policy is documented in `docs/database_migrations.md`. ([bd-skqln.3])
- Stats v2 foundation: new `session_telemetry` table and Alembic migration `0006` — per-session bandwidth/buffer-event telemetry; the data source for the upcoming Users panel. Internal schema only, no UI yet. ([bd-skqln.2])

### Changed
- BandwidthTracker no longer writes to `ChannelWatchStats` — that aggregate table is now served by the `channel_watch_stats_v` view over `session_telemetry`. The legacy `ChannelWatchStats` table schema is retained for pre-cutover rows; no production code reads it post-skqln.3. Readers repointed in this release: `popularity_calculator._gather_metrics`, `BandwidthTracker.get_top_watched_channels` (the `/api/stats/top-watched` endpoint), and `BandwidthTracker._log_watch_stop_events`. `ChannelBandwidth` and `BandwidthDaily` writes are unchanged — they have independent live readers (per-day breakdown charts, bandwidth summaries). ([bd-skqln.3])
- Channel popularity formula's `watch_count` axis now uses `COUNT(DISTINCT session_id)` (distinct viewing sessions per channel) instead of the legacy state-transition counter. Operators may notice channel rankings shift visibly on the first calculator run after upgrade — a one-time event documented in `stats-v2-history-cutover.md`. The other three axes (`watch_time`, `viewers`, `bandwidth`) and the default weights are unchanged. ([bd-skqln.3])
- `ECM_SESSION_TELEMETRY_WRITE_ENABLED` environment-variable kill-switch retired. The flag's job ended when legacy writes were removed; `BandwidthTracker._write_session_telemetry` now runs unconditionally. Operators who set the env var manually can remove it from their compose / .env — it is silently ignored. ([bd-skqln.3])

## [0.16.0] — 2026-05-12

_This is the shipping 0.16.0. An earlier 0.16.0 build was cut on 2026-04-20 and rolled back before any consumer pulled it (see `[0.16.0-yanked]` below); everything from that attempt is included here, with the blocking bugs fixed._

### Added
- Auto-creation **Stream sort → Quality (resolution)** breaks ties at equal resolution using ECM **M3U account priorities** (same values as M3U Manager Save Priorities / Provider Order). Per-rule **`quality_tie_break_order`** (`desc` = higher priority first, default). Per-rule **`quality_m3u_tie_break_enabled`** (default on) disables that tie-break so equal-resolution ordering falls back to stream-id order only; Rule Builder and bulk-edit expose a checkbox and gate the direction dropdown. Alembic revision **`0005`** adds `quality_tie_break_order` and `quality_m3u_tie_break_enabled` on `auto_creation_rules` (legacy `database._run_migrations` remains for installs predating Alembic); YAML export/import, backup restore, and duplicate carry the fields (`enhancedchannelmanager-3j9su`).
- Auto-creation rule analyzer — advisory linter for auto-creation rules. Surfaces structural and regex-style configuration bugs (`UK|` regex matches everything; `^4K` typed under "Contains" matches nothing; `^\^4k` double-escape typo; OR-arms that drop a `normalized_name_in_group` guard; `merge_streams` targeting a channel group with zero channels) without running the rule. Two endpoints: `POST /api/auto-creation/rules/analyze` analyzes the live DB; `POST /api/auto-creation/rules/analyze/from-bundle` accepts a debug-bundle tar.gz upload and never touches the DB so it's safe for support diagnosis of any user's bundle. New MCP tool `analyze_auto_creation_rules(bundle_path=None)` exposes both. All findings are warnings; rule saves are never blocked. Three new advisory codes added to `backend/regex_lint.py` (`REGEX_TRIVIALLY_MATCHES_ALL`, `OPERATOR_VALUE_LOOKS_LIKE_REGEX`, `REGEX_REDUNDANT_ESCAPE_CARET`) on top of the strict bd-eio04.7 codes; new `severity` field on `LintViolation` defaults to `error` for back-compat. Documented in `docs/channel_pipeline_rule_analyzer.md` (`bd-0gntx`).
- `docs/adr/ADR-004-release-cut-promotion-discipline.md` — mandates short-lived `release/vX.Y.Z` branches + merge-commit PR + a seven-item pre-cut gate (G1a–G7) for all `dev`→`main` promotions. Narrow hotfix carve-out with a mechanical ceiling (>2 hotfixes between cuts triggers a mandatory incident review). Directly closes the 0.16.0-rollback root cause (zero-open-P0/P1 gate) and the PR #82 scope-sprawl pattern (non-release PRs to `main` forbidden) (`bd-4lk1q`).
- `docs/adr/ADR-005-code-security-gating-strategy.md` — extends CodeQL to run on PR-to-`dev` and push-to-`dev` (currently only fires on `main`-adjacent events); enforces delta-zero via GitHub Code Scanning merge protection rules on both `dev` and `main`; disables "Allow administrators to bypass required status checks" on both branches. Three-week phased rollout (advisory → dirty-base remediation → enforcement). Dismiss-with-comment policy with only two Phase-1 categories (false-positive / test-only); "won't fix" intentionally excluded until a mechanical co-sign enforcer exists (`bd-sm3n3`).
- Structured JSON logging on stdout with `trace_id` correlation middleware (honors inbound `X-Request-ID`, generates UUIDv4 otherwise, echoes in response header). `bind_context()` helper for attaching structured fields to long-running tasks (`bd-ak1db`, PR #80).
- Prometheus `/metrics` endpoint exposing `ecm_http_requests_total`, `ecm_http_request_duration_seconds`, `ecm_health_ready_ok`, and `ecm_health_ready_check_duration_seconds`. HTTP middleware labels by matched FastAPI route pattern so cardinality stays bounded. `/metrics` is unauthenticated by design (Prometheus scrapers have no session context) — hardening via reverse-proxy allowlist or bearer-token scrape credential is a separate future bead (`bd-ak1db`, PR #80).
- Alembic schema migration system with baseline revision `0001` capturing the current SQLAlchemy metadata (36 tables). `alembic upgrade head` wired into app startup; legacy pre-Alembic DBs are `stamp`ed rather than re-DDLed (`bd-c5wf5`, PR #81).
- `GET /api/health/schema` public endpoint returning `current_revision`, `head_revision`, `up_to_date`, `foreign_keys_enabled`, and `journal_mode` — required by DBAS restore/sync to gate backup imports on schema version (`bd-c5wf5`, PR #81).
- `docs/database_migrations.md` authoring conventions (`bd-c5wf5`, PR #81).
- `docs/runbooks/` scaffold with template and v0.16.0 hard-rollback exemplar (`bd-bwly4`).
- New Settings → Normalization Rules → "Apply to existing channels" button with dry-run diff preview + per-row rename/merge/skip actions. Exposes POST /api/normalization/apply-to-channels endpoint (PR #107, bd-u9odj, GH-104).
- Auto-creation per-rule flag `match_scope_target_group` (default off) that restricts the duplicate channel-name lookup in `create_channel` to the rule's target group. Two rules targeting different groups can now create separate channels with the same name instead of merging into an existing channel in another group. Opt-in, backwards-compatible (bd-r9mtd, GH-92). Adds Alembic revision `0002`.
- Auto-Creation rules — bulk edit. Multi-select rules and apply per-section changes (options, normalization groups, channel/stream sort, orphan cleanup, Merge Streams prune) in one action. Adds POST `/api/auto-creation/rules/bulk-update`; omitted fields are left unchanged. (PR #99)
- `GET /api/journal` now accepts an optional `?batch_id=` query parameter that filters to a single bulk operation's journal rows. Hits the `idx_journal_batch_id` index shipped in bd-dmu8w; an unknown `batch_id` returns `200` with an empty list (not 422 — `batch_id` is just a filter). Composes with the existing `?search=` filter via AND. Closes the integrator gap that bd-pf82j flagged in `docs/api.md` and obviates direct DB lookups for forensic batch correlation (PR #143, bd-s4sph).
- `.github/PULL_REQUEST_TEMPLATE/release.md` — release-cut PR template per ADR-004 §Implementation Sketch §3. Invoked via `gh pr create --base main --head release/vX.Y.Z --template release.md`. Carries the verbatim G1a–G7 pre-cut gate checklist, separate G1a-Justifications and G1b-Waivers sections matching the formally-waived semantics in `docs/shipping.md`, the hotfix-coexistence note (G7 tiebreaker), and post-merge tasks (tag + release + back-merge + dev counter bump). Default repo PR template (`.github/pull_request_template.md`) remains the catch-all for non-release PRs (`bd-tvlxz` item 3).
- `.github/workflows/release-cut-gate.yml` — Phase 2 mechanical CI enforcement of ADR-004 pre-cut gates G1a (zero open P0/P1 beads), G1b (zero open HIGH/CRITICAL CodeQL alerts), G5 (CHANGELOG `[Unreleased]` promoted to `[X.Y.Z]` with today's UTC date and a fresh `[Unreleased]` above), G6 (`frontend/package.json` version matches `release/vX.Y.Z` branch name), and G7 (no other release-cut or hotfix PR open against `main`). Triggered on PR open/sync/edit/reopen against `main`; classifies release-cut PRs by title-regex (`^Release vX.Y.Z$`) AND head-branch-regex (`^release/vX.Y.Z$`); short-circuits to a passing check for non-release PRs so branch protection isn't blocked on hotfix/feature PRs. Installs `bd v0.49.6` (statically-linked Go binary from upstream `gastownhall/beads` releases, pinned to maintainer-local v0.49.x line) for G1a. Added as the `Release Cut Gate` required status check on `main` branch protection — failure of any gate blocks the cut PR (`bd-3d0tv`).

### Changed
- All MCP tools now route every backend call through the declarative endpoint-contract registry (`mcp-server/_endpoint_contracts.py`) — Phase 2 of `enhancedchannelmanager-vtghg` migrates the remaining 11 tool domains (`channel_groups`, `epg`, `export`, `m3u`, `normalization`, `notifications`, `profiles`, `stats`, `streams`, `system`, `tasks`) plus `resources/overview.py` onto `ECMClient.call_endpoint(...)`, bringing the registry to ~100 endpoints (99 today). The contract test's tool-source guard is now **FAIL-mode across the whole MCP surface**: any raw `client.<verb>("/api/...")` literal in `mcp-server/tools/*.py` / `resources/*.py` that isn't `# contract-exempt:`-marked fails the build — only a handful of multi-call / cross-domain flows and the dynamic-key M3U group-settings PATCH remain exempt. Mutating tools now report the **actual resulting state** rather than just "OK": `update_channel`/`create_channel` echo the response object's `name`/`channel_number`/`channel_group_id`; `update_epg_source`/`update_m3u_account` echo the saved `name`/`url`; `bulk_remove_streams` reports the post-update stream count from the response; `delete_channel_group`/`delete_saved_backup`/`delete_task_schedule`/`mark_notifications_read`/`delete_all_notifications`/`apply_profile_to_channels` do a cheap one-call read-back and WARN if the change didn't take (the GH #221 "200 OK, nothing changed" failure mode). Drift the migration surfaced and fixed: the streams tools sent `group`/`provider_id` query params to `GET /api/streams` (backend wants `channel_group_name`/`m3u_account` — silently ignored); `delete_channel_group`/`match_streams_to_channels`/`build_channel_lineup` filtered `GET /api/channels` by `group_id` instead of `channel_group` (the GH #221 class again); `get_journal` paginated via `limit` instead of `page_size` (ignored); `get_epg_grid` sent `channel_id`/`limit` query params that `GET /api/epg/grid` doesn't accept (now filtered/truncated client-side); and `create_task_schedule` sent `{cron_expression}` to a backend that has no cron-style scheduling and requires `schedule_type` — the tool's signature changed from `cron_expression` to `schedule_type` plus the interval/time/day fields the backend actually accepts. Behaviour is otherwise plumbing-only (`bd-vtghg` Phase 2).
- The `channels` and `auto-creation` MCP tools now route every backend call through a declarative endpoint-contract registry (`mcp-server/_endpoint_contracts.py`) instead of hand-rolled `client.post("/api/...", json_data={...})` literals. `ECMClient.call_endpoint(Endpoint, path_args=, body=, query=)` enforces — at call time, always on — that the body/query keys a tool sends are a subset of the keys the registry declares (a tool that sends `group_id` when the backend wants `channel_group_id` now raises `ContractError` loudly instead of silently no-op'ing at the backend), and delegates to the existing verb methods so it inherits their timeout/`detail`-surfacing error handling. A new backend test (`backend/tests/integration/test_mcp_tool_contracts.py`) cross-checks the registry against the live OpenAPI spec — `(method, path)` exists, `request_fields ⊆` the request-body schema's properties (the GH #221 catcher), `query_params ⊆` declared query params, `response_is_list` matches — plus a tool-source guard that FAILs on any un-migrated raw `/api/` literal in the two migrated files (the other ~11 tool domains are inventoried for Phase 2). This is Phase 1 of `enhancedchannelmanager-vtghg`, the structural fix for the drift class behind GH #221-225 (`group_id` vs `channel_group_id`; the `{"rules":[...]}` envelope; missing bulk endpoints; swallowed 422 detail). Plumbing/no behaviour change except three drift fixes the migration surfaced: `list_channels`/`build_channel_lineup` passed `group_id` as the channels-list query param (backend wants `channel_group`); `merge_channels` sent `{target_channel_id, source_channel_ids}` to `POST /api/channels/merge`, which never accepted `target_channel_id` (it now routes through `POST /api/channels/bulk-merge`, which has the "keep target, absorb sources" semantics); and `bulk_commit_channels` read `result["idMappings"]`, which `BulkCommitResponse` doesn't expose (it's `tempIdMap`/`groupIdMap`) — that key always read empty (`bd-vtghg` Phase 1).
- `docs/backend_architecture.md` expanded with an Observability section covering logging schema, metric naming conventions, cardinality rules, `bind_context` usage, and the process for adding new metrics (`bd-ak1db`, PR #80).
- `docs/shipping.md` — replaced the "Release Workflow (Merging to Main)" section with the ADR-004-authoritative Cut Mechanics (steps 0–10), a verbatim Pre-Cut Gate Checklist (G1a–G7) for pasting into release-cut PR descriptions, and a new Hotfix Path subsection covering the one carve-out from the "no non-release PRs to `main`" rule. The "When User Says 'Ship the Fix'" dev-cycle section is unchanged (`bd-tvlxz`).
- `docs/runbooks/v0.16.0-rollback.md` — reordered the two force-push paths: Option 2 (temporary `allow_force_pushes` flip) is now primary per ADR-005 (admin-bypass disabled on `main`), with Option 1 (admin bypass) retained as a fallback only for the transitional window. Added a mandatory post-action verification step (`gh api ... | jq '.allow_force_pushes.enabled'` must return `false`) and a design note considering a short-TTL wrapper that auto-reverts via `trap` regardless of outcome (`bd-tvlxz`).
- `docs/shipping.md` — added a §G1b "formally waived" semantics subsection in the Pre-Cut Gate Checklist clarifying that a HIGH/CRITICAL alert is waived for G1b only when **both** (a) it is dismissed in the GitHub Security tab with a non-empty rationale matching ADR-005's permitted dismissal categories (false-positive-with-evidence or test-only sink; "won't fix" remains excluded) AND (b) the release-cut PR description carries a cross-reference line citing the alert number, dismissal category, and dismissing user. Belt-and-suspenders gate that mirrors and extends ADR-005's Dismiss-With-Comment Policy item 1 to the release-cut surface. Resolves the ambiguity flagged in `bd-tvlxz` item 5 (`bd-tvlxz`).
- Auto-creation `POST /api/auto-creation/run` and `POST /api/auto-creation/rules/{id}/run` are now async-enqueue endpoints — they pre-create the `AutoCreationExecution(status="running")` row, return `202` with `{execution_id, status, ...}` immediately, and dispatch the engine call as a supervised background task (`asyncio.create_task` + strong-ref set + done-callback for GC safety + wrapping `try/except` that marks the row `failed` with `error_message` so failures are observable, not lost). The frontend `useAutoCreationExecution.runPipeline` hook polls `GET /api/auto-creation/executions/{id}` every 1s (30-min safety cap, `AbortSignal`-aware) and splices snapshots into the executions state for incremental UI updates. `fetchExecutions()` moved into a `finally` block so the list refreshes on both success and error paths. Per-rule endpoint now pre-validates rule existence (returns `404` instead of FK-violation `500`). With this change, `/api/auto-creation/` is removed from `_TIMEOUT_EXEMPT_PREFIXES` in `backend/main.py` — the 30s defense-in-depth that bd-zv6pi exempted as a hotfix is restored, and a regression test guards against re-introduction. Completes the follow-up flagged in the bd-zv6pi Fixed entry below (PR #134, bd-enfsy).
- `PUT /api/auto-creation/rules/{id}` and `POST /api/auto-creation/rules/bulk-update` now perform write-time foreign-key validation on `normalization_group_ids` against the `normalization_rule_groups` table. A submission referencing a missing or deleted group returns `422` with `detail.invalid_normalization_group_ids` listing every offending ID; previously the IDs were stored as-is and surfaced later as a silent "no normalization happened" runtime symptom. Validation runs after the rule lookup but before scalar mutations on PUT (no partial state on a bad ID), and after the rules-by-id fetch but before the per-rule mutation loop on bulk-update (single 422 rolls back the whole batch — verified by the integration test asserting zero journal entries + zero scalar mutations on rollback). Delta-on-write semantics: only IDs being newly submitted are validated, so future group deletions cannot retroactively break already-stored rules. POST `/api/auto-creation/rules` (create endpoint) has the same gap; tracked separately as bd-j5p4k for symmetric coverage (PR #145, bd-i75ax).
- `POST /api/auto-creation/rules` (create endpoint) now performs the same write-time foreign-key validation on `normalization_group_ids` as PUT and bulk-update — closes the symmetric coverage gap flagged in bd-i75ax. Same `_validate_normalization_group_ids` helper, same `422` shape with `detail.invalid_normalization_group_ids`. All three rule write paths (POST create, PUT update, POST bulk-update) now reject invalid normalization-group references at write-time instead of silently storing them (PR #150, bd-j5p4k).
- Auto-creation debug bundle now ships a `normalization_rules.yaml` file alongside `rules.yaml`. Every NormalizationRuleGroup is emitted in priority order with its rules nested (priority-ordered, with full condition/action fields and `tag_group_name` resolved). Group `id` is preserved so the cross-reference from `rules.yaml`'s `normalization_group_ids` resolves directly; numeric per-rule ids and timestamps are stripped to keep the YAML readable. `manifest.json` gains `normalization_group_count` and `normalization_rule_count` for at-a-glance counts. Without this, "normalization isn't stripping X" reports could not be diagnosed from the bundle alone — the bundle had the auto-creation rule referencing groups [1,2,5,6,7,8] but no record of what those groups actually did. Container smoke-test produced a 5,156-byte `normalization_rules.yaml` for a 5-group install. Closes the follow-up gap flagged on the bd-cns7j ship (`bd-cns7j` follow-up).
- The `ecm-mcp` sidecar now uses the modern **Streamable HTTP** transport at **`/mcp`** instead of the deprecated SSE transport at `/sse` + `/messages/`. This fixes GH #218 — `mcp-remote` (the bridge Claude Desktop uses) runs mandatory OAuth discovery before connecting, and the MCP Python SDK 1.27.0's SSE handler rejected that probe with a 421, so Claude Desktop couldn't connect; the Streamable HTTP transport handles it cleanly. The SDK's DNS-rebinding (Host-header) protection is disabled on the transport so the sidecar stays reachable from any host by IP or hostname — the static API key remains the access control. **Breaking for existing configs:** update any client pointing at `http://HOST:6101/sse?api_key=...` (or `"type": "sse"` in a `.mcp.json`) to `http://HOST:6101/mcp?api_key=...` (and `"type": "http"`); Claude Desktop now connects via `npx mcp-remote http://HOST:6101/mcp?api_key=... --allow-http`. API-key auth is unchanged (`?api_key=` query param or `Authorization: Bearer`). The `mcp` SDK pin is untouched (1.27.0 already supports Streamable HTTP). Thanks to @swarthyplacebo for the report and diagnosis (`bd-uvh0a`, GH #218).
- New auto-creation rules now default **`match_scope_target_group`** to **on** (was off). When a Create Channel action has `if_exists=merge` (or `merge_only`) and ECM finds an existing channel with a matching normalized name, the lookup is now restricted to the rule's target group by default — so the rule creates a new channel in its target group instead of merging into a same-name channel in some other group (the "channels updated, 0 created" footgun GH #226 hit). **Existing rules keep their stored value (off) — there is no migration; nothing changes for rules you already have.** Toggle the per-rule **Merge lookup scope** option off to restore the original all-groups lookup. The rule analyzer now emits a `MERGE_SCOPE_NOT_TARGET_GROUP` advisory (`info`) on existing rules that still have it off and use a merging Create Channel action, so you can decide whether the all-groups behavior is intentional (documented in `docs/channel_pipeline_rule_analyzer.md` and `docs/user_guide/channel-pipeline/debugging-rules.md`). Relates to GH-92 (the flag's original opt-in introduction). Thanks to @Koalamanx for the report (`bd-p6ko9`, GH #226).

### Fixed
- Alembic revision **`0005`** (`auto_creation_quality_m3u_tie_break`) — SQLite `upgrade head` no longer fails with **`CircularDependencyError`** when adding `quality_tie_break_order` and `quality_m3u_tie_break_enabled` in one batch (each column is added in its own `batch_alter_table`). If Alembic previously failed and startup fell back to **`create_all()`**, those columns can already exist while **`alembic_version`** stayed at **`0004`**; the migration now **introspects** the table and **skips** `add_column` for existing columns, avoiding **`duplicate column name`**. Downgrade **drops** each column only if present. Thanks to @stevencoutts for the fix (`enhancedchannelmanager-m2k7p`; original columns shipped with `enhancedchannelmanager-3j9su`).
- **Auto-creation `stop_processing` action** no longer terminates the entire Pass 2 loop. The `if stop_processing: break` at the end of the per-stream block in `AutoCreationEngine._process_streams` Pass 2 exited the whole `sorted_entries` loop, so the first stream whose winning rule had a `stop_processing` action ended Pass 2 for **all** remaining streams (`streams_matched=1` every run when any rule used the action). Changed to `continue`: Pass 1 already resolves exactly one winning rule per stream, so `STOP_PROCESSING` has no "further rules" left to stop in Pass 2 — it is now a per-stream no-op and all matched streams are processed. The inner per-action loop is unchanged (the action's own description is "stop processing further *rules*", not further actions of this rule). Regression test in `backend/tests/unit/test_auto_creation_engine.py` (`bd-iqm50`, GH #225).
- **MCP `update_channel` / `create_channel`** now send `channel_group_id` instead of `group_id`. The backend channel PATCH forwards keys straight to Dispatcharr (whose channel field is `channel_group_id`) and POST `/api/channels` uses `channel_group_id` — FastAPI/Dispatcharr silently dropped the unknown `group_id` key, so the tool returned `200` but the channel never moved groups. The tool's public `group_id` argument is unchanged; only the wire field was fixed. Tests in `mcp-server/tests/test_tools.py` (`bd-7q9l3`, GH #221).
- **MCP `list_auto_creation_rules`** no longer raises `str object has no attribute get`. `GET /api/auto-creation/rules` returns `{"rules": [...]}`; the tool iterated the dict's keys (`"rules"`, a string) and called `.get()` on it. Now unwraps the envelope defensively, matching the `analyze` tool in the same module. Test in `mcp-server/tests/test_tools.py` (`bd-pvw35`, GH #222).
- **MCP `bulk_add_streams_to_channel`** no longer times out on slow hardware for batches of ~10 streams. It used to fake a bulk op by looping `POST /api/channels/{id}/add-stream` once per stream — 2 Dispatcharr roundtrips × N. Added a real `POST /api/channels/{id}/add-streams` (plural) backend endpoint that fetches the channel once, appends every new stream (dedup, request order preserved), and PUTs once — one Dispatcharr roundtrip total — and wired the tool to it (with a generous 120 s timeout). Documented in `docs/api.md`. Router tests in `backend/tests/routers/test_channels.py`; tool test in `mcp-server/tests/test_tools.py` (`bd-02xjj`, GH #223).
- **MCP `bulk_commit_channels`** (and every `ECMClient` HTTP method) now surface the response body's `detail` on a `4xx`/`5xx` instead of a bare "HTTP 422". FastAPI's auto-validation `422` carries a structured `detail` list whose `loc` pinpoints the bad operation index and field; `ECMClient`'s `HTTPStatusError` handler discarded it. The bulk-commit operation schema (the `type` enum values and the fields each op uses) is now documented in `docs/api.md` under `POST /api/channels/bulk-commit`. Tests in `mcp-server/tests/test_tools.py` (`bd-mjtxn`, GH #224).
- **Normalization rules with Compound (OR) logic** no longer strip the entire channel name when the *first* condition mismatches but a later condition matches. `NormalizationEngine._match_compound_conditions` only captured the action's match span from condition 1 (the `i == 0` guard); for an OR rule where condition 1 missed and condition 2 hit, `primary_match` stayed `None`, fell back to `match_end = len(text)`, and a **Strip Prefix** / **Remove** / **Replace** action then operated on the whole string → blank channel name. Now the first *matching*, non-negated condition's span is used regardless of index — which also fixes the analogous AND-logic case where condition 1 is a negated guard. Repro from GH #217: rule Logic=OR, cond1=`Starts With VariantA` (mismatch), cond2=`Starts With VariantB` (match), action=`Strip Prefix` → channels matching `VariantB` became empty strings. New regression tests in `backend/tests/unit/test_normalization_compound_conditions.py`. Thanks to @swarthyplacebo for the report and the diagnosis (`bd-hssif`, GH #217).
- **Scheduled tasks:** enabling or disabling a task in the UI now persists to the database. `TaskRegistry._save_task_to_db` was restoring `instance._enabled` from the previous row and never writing the flag back, so `PATCH /api/tasks/{id}` had no effect after save or reload. **YAML Backup** was especially visible because it defaults to off (`default_enabled=False`). Unit test: `test_sync_to_database_persists_enabled_for_existing_task` (`bd-7k2n9`).
- Removed two unused imports from `backend/auto_creation_rule_analyzer.py` (`Any` from typing; `lint_pattern_advisory` from regex_lint). Both were imported speculatively while drafting the bd-0gntx Phase 1 module — the advisory regex linting goes through `lint_conditions_json_advisory` which calls `lint_pattern_advisory` internally, so the direct import is dead. No behavior change. Follow-up to the v0.16.0-0074 ship (`bd-0gntx` follow-up, bd-4c02g).
- Auto-creation debug bundle no longer fails with "Failed to generate debug bundle" on large catalogs. The previous `GET /api/auto-creation/debug-bundle` walked the full Dispatcharr catalog inline — ~150 sequential page fetches plus ~150 sequential stream-detail batches — and on a 15K-channel install (a single PPV rule pulling everything into one group) blew the 30s `ECM_REQUEST_TIMEOUT_SECONDS` budget so the response never reached the browser. Replaced with a 202+poll pair matching the bd-enfsy `/run` shape: `POST /api/auto-creation/debug-bundle` returns `{job_id, status: "running"}` immediately and dispatches a supervised background task; `GET /api/auto-creation/debug-bundle/{job_id}` returns JSON status while running, JSON `{status: "failed", error}` on failure, or the tar.gz `application/gzip` attachment when ready (job evicted on read; 30-min TTL prune for abandoned jobs). Inside the worker, page fetches and stream-detail batches now run via `asyncio.gather` with a concurrency-8 semaphore so even multi-thousand-channel catalogs finish in seconds. Frontend `autoCreationApi.ts` gains `startDebugBundle()` + `pollDebugBundle()` + `generateAndFetchDebugBundle()`; both `SettingsTab` and `AutoCreationTab` Debug Bundle handlers switched to the new helper. 7 new backend tests cover the 202 contract, status while running, completed download with eviction, failed-status surfacing, 404 unknown id, sub-5s enqueue under stuck worker, and TTL pruning; 6 new frontend tests cover enqueue, binary download, failed-status throw, 404 throw, running→completed flip, and the full enqueue→poll→download flow. Container smoke-test confirmed enqueue in 1.8ms and a 571-channel bundle delivered in 531ms (`bd-cns7j`).
- Auto-creation YAML export (`GET /api/auto-creation/export/yaml`) coerces **`quality_tie_break_order`** / **`quality_m3u_tie_break_enabled`** to plain `str`/`bool` before `yaml.dump`, so tests using `MagicMock` rule rows cannot leak non-serializable placeholders into the export (`enhancedchannelmanager-3j9su`).
- Settings sub-page memory leak (Channel Normalization / Channel Defaults) reduced ~2,400 KB/s → near-zero on idle. Follow-up to the render-loop fix in PR #209: with the loop fixed, a second leak family emerged from `@dnd-kit/core`'s `useRect()` hook, which attaches a `MutationObserver` to `document.body` with `subtree: true` for every mounted sortable element. With ~30 sortable rows mounted on Channel Normalization plus a heavy notification toast stream (reporter had 20 active "Export Publish" task-completion notifications driving the 2 s poller tier), every toast mount/unmount fired `childList` records to all 30+ observers — millions of `MutationRecord` instances retained, with `MutationRecord` dominating the heap-snapshot diff (+234 MB Size Delta over 30 s). Fix: sweep all 9 `<DndContext>` mount sites across 8 files (`SubstitutionPairsEditor`, `ChannelDetail`, `ChannelsPane` ×2, `NormalizationEngineSection` ×2, `EPGManagerTab`, `SettingsTab` ×2 in Channel Defaults) to gate the `<DndContext>` mount on a local `isReorderMode`/`isEditMode` toggle. Drag handles only render when the user enters reorder/edit mode (via a "Reorder" button per section, exited via "Done"); idle navigation does not mount any sortable observers. Browser-verified on Chromium: 0 `MutationObserver` instances on `#settings/normalization` idle (pre-fix: 19), 60 s heap slope −453 KB/s (pre-fix: +2,400 KB/s on reporter's Edge 147). All 1231 frontend tests pass; lint + typecheck clean. Reporter reverification on Edge 147 / Win 11 is the authoritative signal — Chromium-Playwright cannot reproduce the absolute leak rate due to scheduler differences (`bd-5xg17`, gh #207, follow-up to PR #209).
- Settings tab no longer leaks memory (reported up to 5 GB on Edge before browser pressure relief). Two changes break a cross-component render loop that armed when the `ecm:open-task-editor` sessionStorage key was set (NotificationCenter writes it on Edit-Schedule clicks; ScheduledTasksSection only clears it after the matching task loads, so a stale or unmatched taskId stranded the key indefinitely): (1) the `useEffect` in `SettingsTab.tsx` that consumes the key was missing a dependency array and therefore re-ran every render — adding `[]` deps confines it to mount, breaking the loop driver; (2) `useHashRoute.setHash` always allocated a fresh `HashRoute` object literal and called `pushState` + `setRoute` even when the route was unchanged, so any caller invoking it with the same value (e.g., the buggy effect calling `setActivePage('scheduled-tasks')` with `activePage === 'scheduled-tasks'`) churned `history.length` and re-rendered every iteration — `setHash` now bails out when `tab` and `settingsPage` match the current route. Same family as the recharts Stats loop in `bd-a7l1z`; latent under React 18 batching, runaway under React 19.2.5 concurrent rendering. Browser verification with the trigger primed: 7 settings sub-page navigations produced exactly 7 history entries (pre-fix: tens of thousands within seconds), heap stable across idle and navigation (`bd-5xg17`, gh #207).
- Scheduled task email alerts now deliver via the SMTP Email alert method (recipients in `to_emails`) instead of being a no-op, and Notification Settings now exposes an "Email Alert Recipients" field to configure those recipients in the UI (`bd-bq27w`, `bd-lfmuw`).
- Scheduled task **success** emails (and shared alert metadata) no longer label **Auto-Create Channels** and **Stream Probe** with generic “Total Items” / “Success Count” that disagreed with the execution UI (e.g. tens of thousands “items” vs dozens of channel changes). `task_engine.py` now emits task-specific fields (streams evaluated/matched, channels created/updated, groups, conflicts, execution id / pipeline mode for auto-creation; streams scheduled/ok/failed/skipped and black-screen/low-FPS counts for probe) and human-readable success lines aligned with `TaskResult.message` / probe totals. `tasks/stream_probe.py` adds quality counters to `TaskResult.details`; `stream_prober.py` final external alert uses the same naming. Unit tests in `test_task_notification_formatting.py` (`enhancedchannelmanager-e3ts6`).
- Auto-creation `/run` and `/rules/{id}/run` no longer return `504 Gateway Timeout` on large catalogs. The bd-w3z4h request-timeout middleware (30s default) did not exempt `/api/auto-creation/`, so pipeline runs that exceeded 30s were cancelled client-side even though the work continued to completion server-side (the engine commits as it progresses). Added `/api/auto-creation/` to `_TIMEOUT_EXEMPT_PREFIXES` in `backend/main.py` alongside the other inherently long-running prefixes (`/api/tasks/`, `/api/backup/`, `/api/ffmpeg/`). Secondary symptom — the Execution History panel not auto-refreshing until browser reload — resolves with this fix because the frontend's post-run `fetchExecutions()` now runs on the success path instead of being skipped by the error. Follow-up refactor to convert `/run` to an async enqueue + polling pattern tracked as bd-enfsy (bd-zv6pi).
- Auto-creation no longer renumbers foreign-group channels that were matched via PR #107's normalized-name fallback. A rule with `sort_field` set would pick up any pre-existing channel found via the new normalized-search path — even channels owned by a different rule — and Pass 3 (channel renumber) would then reassign their channel numbers to fit its own sort. Fix: gate the Pass 3 append so a rule only tracks channels it created this run OR that were already in its pre-run managed set; cross-rule matches via fallback are still merged/skipped correctly but no longer trigger a foreign renumber (bd-yj5yi, GH-104 regression from PR #107).
- Auto-creation now strips letter-superscripts (ᴿᴬᵂ → RAW, ᴴᴰ → HD) from channel names it creates, while still preserving numeric superscripts (ESPN² stays ESPN²). PR #61 added a `preserve_superscripts=True` flag to `normalize()` that short-circuited ALL superscript conversion — including letter-superscripts used as quality-tag cruft — so auto-creation output retained `ᴿᴬᵂ`/`ᴴᴰ` even when Settings → Normalization's "Test" button stripped them. Fix splits the internal superscript map into `LETTER_SUPERSCRIPTS` (always converted) and `NUMERIC_SUPERSCRIPTS` (preserved when flag set); `convert_superscripts()` gains a `preserve_numeric` argument; the `preserve_superscripts` call-site in `normalize()` now routes through the letter-only path. Backward-compatible: the public `SUPERSCRIPT_MAP` still exists as the union of both (bd-yui1k, supersedes coarser PR #61 behavior).
- Stream probing now works on HTTPS streams. The ffprobe/ffmpeg `-protocol_whitelist` was missing `tls` (and `crypto`), so every HTTPS `.ts`/`.m3u8` source failed with `[tls @ ...] Protocol 'tls' not on whitelist ...!` and `Invalid argument`. Added `tls,crypto` to the whitelist in `backend/stream_prober.py`, `backend/ffmpeg_builder/probe.py`, and `backend/routers/stream_preview.py`. Neither is a user-supplied URL scheme (they are internal demuxers chained from `https://` / encrypted HLS), so the existing threat model against `file://`, `data:`, `concat:`, `subfile:`, etc. is preserved. New regression tests lock `tls` and `crypto` into the whitelist and assert dangerous schemes stay out (GH-106, bd-4awsl).
- Auto-creation rule duplicate preserves stream/channel sort, normalization groups, orphan cleanup, and option toggles — duplicates now match the original's configuration instead of silently dropping fields (PR #94, external contributor stevencoutts, bd-x3lto).
- Auto-creation no longer creates duplicate channels when an existing channel's name differs only by a normalization-matched suffix (e.g., "RTL ᴿᴬᵂ" and incoming "RTL"). Root causes: normalization engine wasn't initialized unless at least one rule opted in via normalize_names=True, and the channel-lookup map only indexed stored-name → normalized. Fix: always initialize engine when any NormalizationRuleGroup is enabled; add symmetric normalized-search + core-name fallbacks in _find_channel_by_name (PR #107, bd-u9odj, GH-104).
- Frontend test `NormalizationEngineSection.applyToChannels.test.tsx` no longer hangs in CI — the `useNotifications` test mock returned a fresh object on every call, which invalidated the component's `loadData` `useCallback` and triggered an infinite re-render loop that kept the test stuck in the loading state. Mock now returns a stable singleton (PR #107, bd-u9odj).
- Auto-Creation Pass 3.5 stream reorder now runs on channels modified via `merge_stream`/`merge_streams_prune` — previously silent when Channels Created = 0, leaving streams unsorted after a pure-merge run. (PR #113, enhancedchannelmanager-f55hm, gh-113)
- Auto-Creation name-based stream sort now works when probe stats are unavailable — the reorder path now seeds stream names from the channel's stream payload so natural-order and alphabetic sort have something to sort on. (PR #113, enhancedchannelmanager-f55hm, gh-113)
- Auto-Creation quality (resolution) sort now honors `deprioritize_failed_streams` — black-screen / failed / low-fps streams no longer promote to the top ahead of working higher-resolution streams. (PR #113, enhancedchannelmanager-f55hm, gh-113)
- `journal_entries` table now has the indexes its access patterns require. bd-91mcq introduced per-entity journal rows with a shared `batch_id` for bulk auto-creation operations, but the table only had indexes on `timestamp DESC`, `category`, and `action_type` — so the forensic "show me everything in batch X" lookup full-scanned, and "history for entity N" queries fell back on the timestamp index. Adds Alembic revision `0004` creating `idx_journal_batch_id` on `(batch_id)` and `idx_journal_entity` on `(category, entity_id, timestamp DESC)`; mirrors them in the SQLAlchemy model so model + migration stay in sync (drift test confirms). Also raises `CleanupTask.journal_days` default from `30` → `90` to align with `journal.purge_old_entries`'s existing 90-day default — bulk operations now amplify journal row volume N-fold, and the inconsistent retention default would have masked unbounded table growth. CleanupTask was already wired into the scheduler; no integration changes required (PR #133, bd-dmu8w).
- Stats tab no longer crashes with React error #185 (Maximum update depth exceeded) on every load. Recharts 3.x bundles `react-redux` + `@reduxjs/toolkit` for internal state; under React 19's stricter concurrent rendering, the chart's `YAxis` → `RenderedTicksReporter` `useEffect` re-dispatched `setRenderedTicks` with a fresh `ticks` array reference on every render, which forced a re-render that re-ran the effect, looping until React aborted with #185 inside the ErrorBoundary scoped to the Stats tab. The trigger was the React 18.3.1 → 19.2.5 bump in `ca6e5019` (bd-in620, 2026-04-24); the recharts 3.7.0 → 3.8.1 bump in `62ea5c5f` (2026-04-16) seeded `RenderedTicksReporter` itself, but the loop only manifested under React 19. Pin `recharts` to `2.15.4` — the latest 2.x line, declares React 19 support in peerDeps, and pre-dates the redux refactor entirely. Our chart usage (`LineChart`/`AreaChart`/`BarChart` + `XAxis`/`YAxis` + `Tooltip` + `ReferenceLine` + `ResponsiveContainer`) is all stable across the 2.x→3.x boundary; tsc clean and 110 stats panel tests pass against 2.15.4 (bd-a7l1z).

### Security
- `backend/routers/backup.py` — canonicalize-and-verify (`resolve()` + `relative_to()`) applied to `download_saved_backup` and `delete_saved_backup` as defense-in-depth alongside the existing `_BACKUP_FILENAME_RE` guard. Closes CodeQL py/path-injection alerts 1416-1419 (CWE-22/23/36/73/99). Pattern mirrors the zip-restore remediation in the same file. New tests cover traversal, absolute paths, symlink-escape, and null bytes (PR #98, bd-0a1pr).
- `backend/cloud_storage/onedrive_adapter.py` — Pydantic + adapter-level validators on `tenant_id` (GUID or verified domain) and `drive_id` (base64url-style) prevent SSRF via Microsoft Graph URL interpolation. Closes CodeQL CRITICAL py/partial-ssrf alerts 1361, 1362 (CWE-918). New tests at `backend/tests/test_cloud_storage.py` cover validator unit coverage + HTTP 422 at the cloud-target API (PR #101, bd-zbt74).
- `backend/auto_creation_schema.py` — three write-time `re.compile(<user_input>)` sites (condition regex validation, set_variable pattern validation, name_transform_pattern validation) now route through `safe_regex.compile`, which enforces a length cap and uniform compile-error surface. User-controlled regex from auto-creation rule definitions can no longer pin a worker via crafted ReDoS patterns. The bd-91mcq auto-creation bulk merges expanded request volume through these paths; this hardening closes the exposure window. Removes the three `# nosemgrep: no-bare-re-on-dynamic-pattern` annotations + their `TODO(enhancedchannelmanager-ltjyx)` markers. Backward-compatible: error message substrings (`"Invalid regex"`, `"Invalid name_transform_pattern"`) preserved for existing test/UI assertions. New `TestSafeRegexMigrationWriteTimeValidation` (10 tests) covers oversize-pattern rejection at all three sites + boundary-at-cap acceptance (PR #136, bd-ltjyx).
- `backend/routers/m3u_digest.py` — two write-time `re.compile(<user_input>)` sites validating `exclude_group_patterns` and `exclude_stream_patterns` from request bodies now route through `safe_regex.compile`. Same ReDoS class as bd-ltjyx, different surface (M3U digest endpoint instead of auto-creation rule endpoint). Removes the two `# nosemgrep` annotations. New tests in `TestUpdateDigestSettings` cover oversize-pattern rejection on both fields plus a `TestRouterUsesSafeRegex` source-level revert guard (PR #135, bd-3u6p0).
- Eight CodeQL py/stack-trace-exposure HIGH alerts (1412-1415, 1350-1353) remediated across `backend/routers/{backup,channels,export,health,settings}.py` + `backend/cloud_storage/`. Pattern: `logger.exception(...)` for full trace correlated by `X-Request-ID` + return only the exception class name (`type(e).__name__`) in the API response — never `str(e)` (which can include connection strings, file paths, or credentials). For `ImportError` paths in cloud_storage adapters, `e.name` (the missing module name) is used so the response stays useful without leaking interpreter paths. **Material risk closed**: alert 1414 in `health.py` — `/api/health/ready` is in `AUTH_EXEMPT_PATHS`, meaning DBAPI exception messages were echoed unauthenticated; this PR closes that. ADR-005 Phase 1 disallows "won't fix" dismissal so every open stack-trace alert needed a real fix. Six new sanitization tests + 1 updated, each asserting both the positive contract (response equals exception class) and the negative contract (the secret-bearing message body MUST NOT appear in the response) (PR #160, bd-m8i9q).
- `backend/export_manager.py` + `backend/routers/export.py` — defense-in-depth canonicalize-and-verify backstop for path traversal in the M3U/XMLTV export download paths. New `_safe_export_path(profile_id, *parts)` helper resolves and verifies containment under `EXPORTS_DIR.resolve()` via `Path.relative_to()`, raising `ExportPathError` on escape. Wired into `ExportManager.get_export_path` and `ExportManager.cleanup` (cleanup short-circuits before `rmtree` on escape). Also re-verifies path containment in `download_m3u`/`download_xmltv` before serving. CodeQL py/path-injection alerts 1354-1359 were already dismissed as false-positive (FastAPI int path-param + Pydantic `FILENAME_RE` allowlist make traversal unreachable upstream), but per ADR-005 sequencing 2 and the bd-0a1pr precedent the property is now guaranteed at the sink rather than depending on caller hygiene. New `TestExportPathInjection` (9 tests) mirrors `TestSavedBackupsPathInjection` from bd-0a1pr — traversal payloads, absolute paths, null-byte, backslash, cleanup sentinel-file regression, symlink-escape (PR #159, bd-h5rfv).

### Documentation
- `README.md` MCP setup section — disambiguates `api_key` (Dispatcharr REST token) from `mcp_api_key` (ECM MCP key) with a callout table, explicit "do not touch `api_key`" guidance in the key-rotation flow, a note clarifying that the `?api_key=` URL query parameter in client configs is the `mcp_api_key` value, and a cross-reference to the `/health` `api_key_status` diagnostic for post-rotation troubleshooting. Closes GH #273 (`bd-6gm3l`).
- `docs/api.md` — `POST /api/auto-creation/rules/bulk-update` now documents three previously-undocumented behaviors that shipped in earlier merges: the rejected-fields contract (`conditions` and `actions` return 422 per bd-gjoe5), the single-fetch response shape `{rules, updated_count}` (bd-bh1hh), and the `batch_id` correlation primitive — every bulk operation writes N per-entity journal rows sharing one 8-char `batch_id` (bd-91mcq), retrievable today via direct `journal_entries.batch_id` lookup against `idx_journal_batch_id` (bd-dmu8w; a `?batch_id=` query param on `GET /api/journal` is tracked as bd-s4sph for a future release). Also adds a Journal-section paragraph documenting the actual `GET /api/journal` filter set. Surfaces two write-time validation gaps as separate beads (bd-s4sph + bd-i75ax). Style matches existing inline-narrative blocks (PR #139, bd-pf82j).

## [0.16.0-yanked] — 2026-04-20 (rolled back)

**This build was rolled back** before any external consumer pulled the tag. The GitHub Release, git tag, and GHCR image for `0.16.0` were deleted; `:latest` was retagged back to the `v0.15.2` multi-arch index. Rollback executed per `bd-vgm4l`; see `docs/runbooks/v0.16.0-rollback.md` for the procedure. The PO chose rollback over a forward hotfix because open P0/P1 bugs needed to clear before a release could ship. The work originally cut here re-ships in the **[0.16.0] — 2026-05-12** release above, with the blocking bugs fixed.

## [0.15.2] — 2026-04-16

### Security
- Upgraded `vite` out of the vulnerable 7.0.0–7.3.1 range, resolving three high-severity advisories affecting the frontend dev tooling:
  - [GHSA-4w7w-66w2-5vf9](https://github.com/advisories/GHSA-4w7w-66w2-5vf9) — path traversal in optimized-deps `.map` handling.
  - [GHSA-v2wj-q39q-566r](https://github.com/advisories/GHSA-v2wj-q39q-566r) — `server.fs.deny` bypass with queries.
  - [GHSA-p9ff-h696-f583](https://github.com/advisories/GHSA-p9ff-h696-f583) — arbitrary file read via dev server WebSocket.
- No runtime code changes; only the frontend build tooling is affected. Dev server users should upgrade promptly.

## [0.15.1] — 2026-03-31

### Added
- Login rate limiting — 5 attempts per minute per IP via slowapi on both local and Dispatcharr login endpoints.
- Security headers: `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`.
- Client IP in login-failure logs (includes `X-Forwarded-For` for forensics).

### Changed
- **Password policy now follows NIST 800-63B** — dropped composition rules (uppercase/lowercase/number), replaced the 33-word list with the 10k common-passwords list, uniform 8-character minimum for all users including admin-created.
- CORS tightened — explicit method and header allow-lists instead of wildcards.

### Fixed
- EPG matching: channel names with a number prefix (e.g. `535 | ESPN`) were normalizing to `535espn` instead of `espn`, causing zero matches against EPG data.

### Security
- Injection / SSRF (OWASP A03/A10):
  - ffmpeg protocol allow-list on all invocations — blocks `file://`, `data://`, `concat:` and other dangerous protocols.
  - URL-scheme validation at input time on M3U, Xtream Codes, and EPG source endpoints — only `http://` and `https://` allowed.
  - Zip backup path canonicalization — defense-in-depth `resolve()` check alongside existing traversal guards.
- Logging hardening (OWASP A09):
  - 500-response scrubbing — global handler returns generic `Internal server error` to clients; real error stays in server logs.
  - Validation-error log redaction — `Authorization` / `Cookie` headers and auth-endpoint request bodies redacted from logs.

## [0.15.0] — 2026-03-29

### Added
- **Export & Publish** — generate M3U/XMLTV playlists from channel profiles and publish to cloud targets (S3, WebDAV, etc.) on a cron schedule. Includes channel selector, playlist preview, cloud-target management, and publish history.
- **Low-FPS detection** — streams below a configurable threshold (5/10/15/20 FPS) are flagged with an amber icon and deprioritized in Smart Sort. Zero-overhead, always-on, configurable in Settings → Maintenance.
- **Black-screen detection** — optional ffmpeg `signalstats` check after each probe flags dark/blank streams with a purple icon. Configurable sample duration (3–30s) in Settings → Maintenance.
- **Normalize Names engine** — tag-based rules engine for cleaning up stream names during bulk channel creation. YAML import/export, edit/revert controls, drag-and-drop priority ordering.
- **Backup & Restore** — full configuration backup/restore from Settings, with restore option during the first-run setup wizard.
- **Merge Channels** — combine two or more channels into one, consolidating their streams.
- Auto-creation enhancements — EPG logo-source action, Probe Streams action, Set Channel Profile action; Smart Sort option in rule sort dropdown; rule selection in Auto-Create schedule; separate stream sort from channel sort per rule.
- PUID/PGID support for Docker container user identity.
- TV Guide print view.
- JWT authentication in Swagger UI with `/swagger` redirect.
- Debug bundle for troubleshooting auth issues.

### Changed
- Server-side migration of EPG matching, stream normalization, and edit consolidation (previously client-side).
- Settings UI consistency — compact dropdowns, unified admin section CSS, proper field ordering.
- Scoped reprobe to last scheduled probe's channel groups.
- Capped Dispatcharr HTTP client connection pool.
- All GitHub Actions updated to Node.js 24-compatible versions.
- Migrated 161 `console.log` call sites to the structured logger.

### Fixed
- Persistent 503 when stream prober not initialized — prober now self-heals and auto-creates.
- `assign_logo` from EPG and duplicate rule priorities.
- Radio buttons, undefined CSS variables, and card backgrounds in admin tabs.
- Push-down renumbering, menu overflow, task-card grid.

### Removed
- ~2,000+ lines of dead code across backend and frontend, including duplicate `formatDate` implementations.

### Security
- Secured backup endpoints; improved token handling.
- Fixed npm audit vulnerabilities in `brace-expansion`, `flatted`, `picomatch`.

## [0.14.0] — 2026-03-06

Highlights: black-screen detection during probes; failed-stream re-probe scheduled task; auto-creation per-rule `skip_struck_streams`, provider order, and channel-number sort; pattern-builder UI for normalization rules; configurable HTTP/HTTPS ports via env vars. Numerous auto-creation and notification-center bug fixes. Full notes: [GitHub Release v0.14.0](https://github.com/MotWakorb/enhancedchannelmanager/releases/tag/v0.14.0).

## [0.13.1] — 2026-02-20

Bug-fix release. Full notes: [GitHub Release v0.13.1](https://github.com/MotWakorb/enhancedchannelmanager/releases/tag/v0.13.1).

## [0.13.0] — 2026-02-19

Full notes: [GitHub Release v0.13.0](https://github.com/MotWakorb/enhancedchannelmanager/releases/tag/v0.13.0).

## Earlier versions

Entries for `0.12.x` and earlier have not been backfilled into this file. See the [GitHub Releases page](https://github.com/MotWakorb/enhancedchannelmanager/releases) for the original release notes. Future releases will be recorded here under the appropriate Keep-a-Changelog sections.

[Unreleased]: https://github.com/MotWakorb/enhancedchannelmanager/compare/v0.18.1...HEAD
[0.18.1]: https://github.com/MotWakorb/enhancedchannelmanager/releases/tag/v0.18.1
[0.15.2]: https://github.com/MotWakorb/enhancedchannelmanager/releases/tag/v0.15.2
[0.15.1]: https://github.com/MotWakorb/enhancedchannelmanager/releases/tag/v0.15.1
[0.15.0]: https://github.com/MotWakorb/enhancedchannelmanager/releases/tag/v0.15.0
[0.14.0]: https://github.com/MotWakorb/enhancedchannelmanager/releases/tag/v0.14.0
[0.13.1]: https://github.com/MotWakorb/enhancedchannelmanager/releases/tag/v0.13.1
[0.13.0]: https://github.com/MotWakorb/enhancedchannelmanager/releases/tag/v0.13.0
